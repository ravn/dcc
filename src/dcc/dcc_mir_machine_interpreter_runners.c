/* dcc_mir_machine_interpreter_runners.c - strict control/interpreter schedules. */

#include "dcc_mir_machine_internal.h"

enum MirInterpreterFileLocationKind {
    MIR_INTERPRETER_FILE_DIRECT,
    MIR_INTERPRETER_FILE_STRUCT,
    MIR_INTERPRETER_FILE_POINTER_STRUCT
};

struct MirInterpreterFileLocation {
    struct Sym *root;
    int offset;
    enum MirInterpreterFileLocationKind kind;
};

enum MirInterpreterFileKind {
    MIR_INTERPRETER_FILE_FORTH,
    MIR_INTERPRETER_FILE_FORTRAN,
    MIR_INTERPRETER_FILE_PREPROCESS_POINTER,
    MIR_INTERPRETER_FILE_COBOL,
    MIR_INTERPRETER_FILE_PASCAL,
    MIR_INTERPRETER_FILE_BASIC,
    MIR_INTERPRETER_FILE_PREPROCESS_STRUCT
};

struct MirInterpreterFileSchedule {
    struct Sym *open_function;
    struct Sym *perror_function;
    struct Sym *seek_function;
    struct Sym *tell_function;
    struct Sym *close_function;
    struct Sym *print_function;
    struct Sym *allocate_function;
    struct Sym *read_function;
    struct Sym *free_function;
    struct Sym *preprocess_function;
    struct Sym *length_function;
    struct MirInterpreterFileLocation source;
    struct MirInterpreterFileLocation source_length;
    enum MirInterpreterFileKind kind;
    int name_stack_offset;
    int mode_string_id;
    int too_large_string_id;
    int allocation_string_id;
    int read_string_id;
    int maximum_source;
    char print_name[64];
};

struct MirForthLexerSchedule {
    struct Sym *state;
    struct Sym *space_function;
    struct Sym *comment_function;
    struct Sym *digit_function;
    int position_offset;
    int length_offset;
    int source_offset;
    int line_offset;
    int text_offset;
    int token_offset;
    int integer_offset;
};

struct MirBcdByteMathSchedule {
    struct Sym *state;
    int math_stack_offset;
    int rhs_stack_offset;
    int accumulator_offset;
    int zero_offset;
    int carry_offset;
};

enum MirPascalScanKind {
    MIR_PASCAL_SCAN_IDENT,
    MIR_PASCAL_SCAN_NUMBER,
    MIR_PASCAL_SCAN_STRING
};

struct MirPascalScanSchedule {
    struct Sym *source;
    struct Sym *position;
    struct Sym *length;
    struct Sym *text;
    struct Sym *token;
    struct Sym *integer;
    struct Sym *first_function;
    struct Sym *second_function;
    enum MirPascalScanKind kind;
    int character_stack_offset;
    int format_string_id;
    char format_name[64];
};

struct MirPascalNextSchedule {
    struct Sym *source;
    struct Sym *position;
    struct Sym *length;
    struct Sym *text;
    struct Sym *token;
    struct Sym *integer;
    struct Sym *skip_function;
    struct Sym *alpha_function;
    struct Sym *ident_function;
    struct Sym *digit_function;
    struct Sym *number_function;
    struct Sym *string_function;
    struct Sym *copy_function;
    int strings[5];
};

enum MirPascalPredicateKind {
    MIR_PASCAL_PREDICATE_NEED,
    MIR_PASCAL_PREDICATE_ACCEPT,
    MIR_PASCAL_PREDICATE_WORD
};

struct MirPascalPredicateSchedule {
    struct Sym *token;
    struct Sym *text;
    struct Sym *compare_function;
    struct Sym *failure_function;
    struct Sym *next_function;
    enum MirPascalPredicateKind kind;
    int token_stack_offset;
    int string_stack_offset;
    int failure_string_id;
};

struct MirPascalEmitSchedule {
    struct Sym *code;
    struct Sym *code_position;
    struct Sym *code_limit;
    struct Sym *failure_function;
    int opcode_stack_offset;
    int first_stack_offset;
    int second_stack_offset;
    int opcode_offset;
    int first_offset;
    int second_offset;
    int stride;
    int failure_string_id;
};

struct MirAdaWhitespaceSchedule {
    struct Sym *state;
    struct Sym *space_function;
    int position_offset;
    int length_offset;
    int source_offset;
    int line_offset;
};

struct MirAdaLexerSchedule {
    struct Sym *state;
    struct Sym *skip_function;
    struct Sym *alpha_function;
    struct Sym *alnum_function;
    struct Sym *lower_function;
    struct Sym *compare_function;
    struct Sym *digit_function;
    struct Sym *format_function;
    struct Sym *copy_function;
    int source_offset;
    int length_offset;
    int position_offset;
    int token_offset;
    int integer_offset;
    int text_offset;
    int strings[8];
    char format_name[64];
};

struct MirStorageOpcodeSchedule {
    int scope_stack_offset;
    int size_stack_offset;
    int array_stack_offset;
    int results[8];
};

struct MirCobAddSubSchedule {
    struct Sym *state;
    struct Sym *expression_function;
    struct Sym *failure_function;
    struct Sym *emit_function;
    int token_position_offset;
    int token_end_offset;
    int tokens_offset;
    int keyword;
    int indexed_opcode;
    int scalar_opcode;
    int syntax_string_id;
    int variable_string_id;
};

struct MirCobTokenizerSchedule {
    struct Sym *state;
    struct Sym *space_function;
    struct Sym *emit_function;
    struct Sym *add_string_function;
    struct Sym *digit_function;
    struct Sym *alpha_function;
    struct Sym *upper_function;
    struct Sym *alnum_function;
    struct Sym *keyword_function;
    struct Sym *find_variable_function;
    struct Sym *find_paragraph_function;
    struct Sym *print_function;
    int statement_stack_offset;
    int statements_offset;
    int token_count_offset;
    int statement_text_offset;
    int statement_start_offset;
    int statement_end_offset;
    int statement_stride;
    int unknown_string_id;
    char print_name[64];
    char exit_name[64];
};

struct MirCobPerformSchedule {
    struct Sym *state;
    struct Sym *failure_function;
    struct Sym *expression_function;
    struct Sym *emit_function;
    struct Sym *condition_function;
    struct Sym *patch_function;
    int token_position_offset;
    int token_end_offset;
    int tokens_offset;
    int code_position_offset;
    int perform_count_offset;
    int performs_offset;
    int perform_stride;
    int perform_field_offsets[6];
    int error_strings[5];
};

struct MirCobChainSchedule {
    struct Sym *state;
    struct Sym *child_function;
    struct Sym *emit_function;
    int token_position_offset;
    int token_end_offset;
    int tokens_offset;
    int first_token;
    int second_token;
    int first_opcode;
    int second_opcode;
};

struct MirFortranStatementAppendSchedule {
    struct Sym *statements;
    struct Sym *statement_count;
    struct Sym *failure_function;
    struct Sym *duplicate_function;
    int label_stack_offset;
    int text_stack_offset;
    int unit_stack_offset;
    int stride;
    int offsets[8];
    int limit;
    int failure_string_id;
};

struct MirFortranUppercaseSchedule {
    int destination_stack_offset;
    int source_stack_offset;
    int limit;
};

struct MirFortranDeclarationSchedule {
    struct Sym *space_function;
    struct Sym *alnum_function;
    struct Sym *upper_function;
    struct Sym *digit_function;
    struct Sym *find_function;
    struct Sym *add_function;
    int source_stack_offset;
    int type_stack_offset;
};

struct MirFortranSourceSchedule {
    struct Sym *source;
    struct Sym *length;
    struct Sym *trim_function;
    struct Sym *digit_function;
    struct Sym *space_function;
    struct Sym *copy_function;
    struct Sym *prefix_function;
    struct Sym *append_function;
    int subroutine_string_id;
};

struct MirFortranLhsSchedule {
    struct Sym *trim_function;
    struct Sym *alnum_function;
    struct Sym *upper_function;
    struct Sym *ensure_function;
    struct Sym *space_function;
    struct Sym *search_function;
    struct Sym *duplicate_function;
    int lhs_stack_offset;
    int symbol_pointer_stack_offset;
    int index_pointer_stack_offset;
};

struct MirFortranMainSchedule {
    struct Sym *initialize_function;
    struct Sym *compare_function;
    struct Sym *verbose;
    struct Sym *print_function;
    struct Sym *load_function;
    struct Sym *pipeline[5];
    int argc_stack_offset;
    int argv_stack_offset;
    int strings[3];
    char print_name[64];
};

struct MirFortranGrowSchedule {
    struct Sym *capacity;
    struct Sym *memory;
    struct Sym *failure_function;
    struct Sym *resize_function;
    struct Sym *clear_function;
    int need_stack_offset;
    int limit;
    int small_limit;
    int small_step;
    int large_step;
    int full_string_id;
    int memory_string_id;
};

struct MirFortranFatalSchedule {
    struct Sym *statements;
    struct Sym *program_counter;
    struct Sym *statement_count;
    struct Sym *print_function;
    int message_stack_offset;
    int statement_stride;
    int text_offset;
    int format_string_id;
    int empty_string_id;
    char print_name[64];
    char exit_name[64];
};

struct MirFortranReturnSchedule {
    struct Sym *call_count;
    struct Sym *halted;
    struct Sym *calls;
    struct Sym *program_counter;
    int call_stride;
    int return_offset;
};

static const unsigned char mir_interpreter_file_forth_opcodes[212] = {
    MIR_LABEL, MIR_PARAM, MIR_LOAD, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_NOP, MIR_ARG,
    MIR_STRING_ADDRESS, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP,
    MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN,
    MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_UNARY, MIR_CONST, MIR_NOP,
    MIR_BINARY, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP,
    MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_NOP, MIR_ARG, MIR_CONST, MIR_NOP,
    MIR_ARG, MIR_LOAD, MIR_UNARY, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_UNARY,
    MIR_NOP, MIR_STORE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LOAD, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_NOP, MIR_ARG, MIR_CALL, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_CONST, MIR_NOP, MIR_STORE_INDIRECT, MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD, MIR_STORE_INDIRECT, MIR_CONST, MIR_RETURN
};

static const unsigned char mir_interpreter_file_fortran_opcodes[190] = {
    MIR_LABEL, MIR_PARAM, MIR_LOAD, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_NOP, MIR_ARG,
    MIR_STRING_ADDRESS, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP,
    MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN,
    MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_UNARY, MIR_CONST, MIR_NOP, MIR_BINARY, MIR_ARG,
    MIR_CALL, MIR_NOP, MIR_UNARY, MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_NOP,
    MIR_ARG, MIR_CONST, MIR_NOP, MIR_ARG, MIR_LOAD, MIR_UNARY, MIR_ARG, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_UNARY, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_NOP, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_RETURN, MIR_LABEL, MIR_LABEL,
    MIR_LOAD, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY,
    MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_NOP, MIR_STORE, MIR_CONST, MIR_RETURN
};

static const unsigned char mir_interpreter_file_preprocess_pointer_opcodes[144] = {
    MIR_LABEL, MIR_PARAM, MIR_LOAD, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE, MIR_LOAD,
    MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL, MIR_NOP, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_NOP, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_UNARY, MIR_CONST, MIR_NOP,
    MIR_BINARY, MIR_ARG, MIR_CALL, MIR_NOP, MIR_UNARY, MIR_STORE, MIR_LOAD, MIR_UNARY,
    MIR_BRANCH_FALSE, MIR_CONST, MIR_RETURN, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_ARG, MIR_CONST,
    MIR_NOP, MIR_ARG, MIR_LOAD, MIR_UNARY, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_UNARY, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LOAD,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_RETURN, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS,
    MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_NOP, MIR_ARG, MIR_CALL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ARG, MIR_CALL, MIR_UNARY, MIR_STORE_INDIRECT, MIR_CONST, MIR_RETURN
};

static const unsigned char mir_interpreter_file_cobol_opcodes[178] = {
    MIR_LABEL, MIR_PARAM, MIR_LOAD, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE, MIR_LOAD,
    MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL, MIR_NOP, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_NOP, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_UNARY,
    MIR_CONST, MIR_NOP, MIR_BINARY, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE_INDIRECT, MIR_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_NOP, MIR_ARG,
    MIR_CONST, MIR_NOP, MIR_ARG, MIR_LOAD, MIR_UNARY, MIR_ARG, MIR_LOAD, MIR_ARG,
    MIR_CALL, MIR_UNARY, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_RETURN, MIR_LABEL, MIR_LABEL, MIR_LOAD,
    MIR_NOP, MIR_NOP, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY,
    MIR_STORE, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD,
    MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_STORE_INDIRECT,
    MIR_CONST, MIR_RETURN
};

static const unsigned char mir_interpreter_file_pascal_opcodes[176] = {
    MIR_LABEL, MIR_PARAM, MIR_LOAD, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_NOP, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG,
    MIR_CONST, MIR_ARG, MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG,
    MIR_CALL, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_LOAD, MIR_UNARY, MIR_CONST, MIR_NOP, MIR_BINARY, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_UNARY, MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_ARG, MIR_CONST,
    MIR_NOP, MIR_ARG, MIR_LOAD, MIR_UNARY, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_UNARY, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LOAD,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_NOP, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_LOAD, MIR_NOP, MIR_ARG, MIR_CALL, MIR_NOP, MIR_CONST,
    MIR_STORE, MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS,
    MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_NOP, MIR_STORE, MIR_CONST, MIR_RETURN
};

static const unsigned char mir_interpreter_file_basic_opcodes[183] = {
    MIR_LABEL, MIR_PARAM, MIR_LOAD, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE, MIR_LOAD,
    MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL, MIR_NOP, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_NOP, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_UNARY,
    MIR_CONST, MIR_NOP, MIR_BINARY, MIR_ARG, MIR_CALL, MIR_NOP, MIR_UNARY, MIR_STORE,
    MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN,
    MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_ARG, MIR_CONST, MIR_NOP, MIR_ARG,
    MIR_LOAD, MIR_UNARY, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_UNARY, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_NOP, MIR_ARG, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_INDEX_ADDRESS, MIR_NOP,
    MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_NOP, MIR_STORE, MIR_CONST, MIR_RETURN
};

static const unsigned char mir_interpreter_file_preprocess_struct_opcodes[144] = {
    MIR_LABEL, MIR_PARAM, MIR_LOAD, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE, MIR_LOAD,
    MIR_ARG, MIR_CONST, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL, MIR_NOP, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_NOP, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL,
    MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_UNARY, MIR_CONST, MIR_NOP,
    MIR_BINARY, MIR_ARG, MIR_CALL, MIR_NOP, MIR_UNARY, MIR_STORE, MIR_LOAD, MIR_UNARY,
    MIR_BRANCH_FALSE, MIR_CONST, MIR_RETURN, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_ARG, MIR_CONST,
    MIR_NOP, MIR_ARG, MIR_LOAD, MIR_UNARY, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_UNARY, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LOAD,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_RETURN, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS,
    MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_ARG, MIR_CALL,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_NOP, MIR_ARG, MIR_CALL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ARG, MIR_CALL, MIR_UNARY, MIR_STORE_INDIRECT, MIR_CONST, MIR_RETURN
};

static const unsigned char mir_forth_lexer_opcodes[551] = {
    MIR_LABEL, MIR_LABEL, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_CONST,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_STORE_INDIRECT,
    MIR_NOP, MIR_JUMP, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_UNARY,
    MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_NOP, MIR_LABEL, MIR_JUMP,
    MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_CALL, MIR_NOP, MIR_JUMP, MIR_NOP, MIR_LABEL,
    MIR_NOP, MIR_JUMP, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_CONST, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_RETURN, MIR_LABEL, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT,
    MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_NOP,
    MIR_NOP, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_NOP,
    MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_NOP,
    MIR_UNARY, MIR_STORE_INDIRECT, MIR_LABEL, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY,
    MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_CONST, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_UNARY, MIR_STORE_INDIRECT, MIR_LABEL, MIR_NOP, MIR_NOP,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_NOP,
    MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_NOP,
    MIR_UNARY, MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY,
    MIR_STORE_INDIRECT, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD,
    MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_CONST, MIR_NOP, MIR_STORE, MIR_CONST,
    MIR_NOP, MIR_STORE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_NOP, MIR_CONST, MIR_NOP, MIR_STORE, MIR_CONST, MIR_NOP, MIR_STORE,
    MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY,
    MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_NOP, MIR_NOP, MIR_CONST, MIR_STORE,
    MIR_LABEL, MIR_NOP, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST,
    MIR_BINARY, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_UNARY, MIR_BINARY, MIR_CONST, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_LOAD, MIR_BINARY,
    MIR_UNARY, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_RETURN, MIR_NOP,
    MIR_LABEL, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT
};

static const int mir_forth_lexer_edges[][2] = {
    {10, 64}, {25, 35}, {33, 64}, {40, 43}, {42, 64},
    {47, 54}, {63, 2}, {72, 88}, {84, 88}, {87, 90},
    {92, 109}, {101, 1}, {105, 109}, {108, 1}, {132, 134},
    {152, 164}, {160, 164}, {163, 166}, {168, 184},
    {180, 184}, {183, 186}, {188, 319}, {208, 224},
    {220, 224}, {223, 226}, {228, 270}, {246, 249},
    {248, 270}, {255, 266}, {269, 198}, {278, 294},
    {290, 294}, {293, 296}, {298, 305}, {343, 428},
    {358, 362}, {361, 376}, {366, 370}, {369, 372},
    {375, 376}, {378, 382}, {381, 396}, {386, 390},
    {389, 392}, {395, 396}, {398, 401}, {400, 428},
    {407, 418}, {427, 333}, {450, 460}, {456, 460},
    {459, 462}, {464, 473}, {483, 546}, {501, 521},
    {520, 488}, {530, 544}
};

static const unsigned char mir_bcd_byte_math_opcodes[240] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY,
    MIR_BINARY, MIR_UNARY, MIR_STORE, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY,
    MIR_BINARY, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_UNARY,
    MIR_STORE, MIR_NOP, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_UNARY, MIR_STORE, MIR_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_NOP, MIR_CONST, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_UNARY,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP,
    MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_RETURN, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_NOP, MIR_UNARY,
    MIR_BINARY, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_NOP,
    MIR_UNARY, MIR_BINARY, MIR_UNARY, MIR_STORE, MIR_CONST, MIR_NOP, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_BRANCH_FALSE, MIR_NOP,
    MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_UNARY, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_NOP,
    MIR_LOAD, MIR_UNARY, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_NOP, MIR_LOAD,
    MIR_UNARY, MIR_UNARY, MIR_BINARY, MIR_UNARY, MIR_STORE, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP,
    MIR_CONST, MIR_STORE_INDIRECT, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_NOP, MIR_UNARY,
    MIR_BINARY, MIR_LOAD, MIR_UNARY, MIR_BINARY, MIR_UNARY, MIR_STORE, MIR_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_NOP, MIR_LABEL, MIR_NOP, MIR_JUMP, MIR_LABEL,
    MIR_NOP, MIR_NOP, MIR_UNARY, MIR_UNARY, MIR_BINARY, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_BINARY, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_CONST, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_UNARY, MIR_NOP,
    MIR_STORE, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_NOP, MIR_JUMP,
    MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LABEL, MIR_NOP,
    MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_CONST,
    MIR_BINARY, MIR_LOAD, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BINARY, MIR_UNARY, MIR_STORE_INDIRECT
};

static const unsigned char mir_pascal_scan_ident_opcodes[89] = {
    MIR_LABEL, MIR_PARAM, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP,
    MIR_LOAD, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_UNARY,
    MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP,
    MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_STORE,
    MIR_NOP, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_JUMP, MIR_LABEL, MIR_NOP,
    MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_LABEL, MIR_CONST, MIR_BRANCH_FALSE, MIR_JUMP, MIR_LABEL,
    MIR_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_CONST, MIR_NOP,
    MIR_STORE
};

static const unsigned char mir_pascal_scan_number_opcodes[81] = {
    MIR_LABEL, MIR_PARAM, MIR_NOP, MIR_CONST, MIR_STORE, MIR_LABEL, MIR_PHI, MIR_PHI,
    MIR_NOP, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_NOP, MIR_UNARY, MIR_BINARY, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_NOP, MIR_STORE,
    MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_LOAD,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY,
    MIR_STORE, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_NOP, MIR_ARG, MIR_CALL, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST,
    MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_LOAD, MIR_UNARY, MIR_NOP, MIR_STORE, MIR_CONST,
    MIR_NOP, MIR_STORE, MIR_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_LOAD, MIR_ARG,
    MIR_CALL
};

static const unsigned char mir_pascal_scan_string_opcodes[70] = {
    MIR_LABEL, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_LOAD, MIR_LOAD,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_ADDRESS, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_LOAD,
    MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE,
    MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS,
    MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_CONST, MIR_NOP, MIR_STORE
};

static const unsigned char mir_pascal_next_opcodes[332] = {
    MIR_LABEL, MIR_CALL, MIR_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT,
    MIR_CONST, MIR_NOP, MIR_STORE, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_LOAD,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_NOP, MIR_STORE, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_LOAD, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY,
    MIR_UNARY, MIR_STORE, MIR_NOP, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_NOP, MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_LOAD, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_RETURN,
    MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CALL, MIR_RETURN,
    MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_CONST, MIR_NOP,
    MIR_STORE, MIR_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP,
    MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_CONST, MIR_NOP, MIR_STORE,
    MIR_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_CONST, MIR_NOP, MIR_STORE, MIR_ADDRESS,
    MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_CONST, MIR_NOP, MIR_STORE, MIR_ADDRESS, MIR_ARG,
    MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_CONST,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD,
    MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST,
    MIR_BINARY, MIR_STORE, MIR_CONST, MIR_NOP, MIR_STORE, MIR_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS,
    MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_STORE,
    MIR_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_UNARY, MIR_STORE_INDIRECT, MIR_ADDRESS, MIR_CONST,
    MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT
};

static const unsigned char mir_pascal_need_opcodes[44] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_LOAD, MIR_NOP, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_ARG, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_STRING_ADDRESS,
    MIR_ARG, MIR_CALL, MIR_LABEL, MIR_CALL
};

static const unsigned char mir_pascal_acc_opcodes[49] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_LOAD, MIR_NOP, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_UNARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_ARG,
    MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_CALL, MIR_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_CONST,
    MIR_RETURN
};

static const unsigned char mir_pascal_emit_opcodes[36] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_PARAM, MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_NOP, MIR_UNARY, MIR_STORE_INDIRECT, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP, MIR_STORE_INDIRECT, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_RETURN
};

static const unsigned char mir_pascal_isword_opcodes[23] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS,
    MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_RETURN
};

static const unsigned char mir_ada_whitespace_opcodes[237] = {
    MIR_LABEL, MIR_LABEL, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_BINARY, MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY,
    MIR_STORE_INDIRECT, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_NOP, MIR_CONST, MIR_BINARY, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_LABEL, MIR_JUMP,
    MIR_LABEL, MIR_NOP, MIR_JUMP, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_NOP, MIR_STORE,
    MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_NOP, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_JUMP, MIR_NOP, MIR_LABEL, MIR_NOP,
    MIR_JUMP, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL
};

static const unsigned char mir_ada_lexer_opcodes[736] = {
    MIR_LABEL, MIR_CALL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_RETURN, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY,
    MIR_STORE, MIR_NOP, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_UNARY,
    MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_STORE,
    MIR_NOP, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_JUMP,
    MIR_LABEL, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG,
    MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_ARG,
    MIR_CALL, MIR_BRANCH_FALSE, MIR_NOP, MIR_CONST, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP,
    MIR_NOP, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LOAD, MIR_NOP, MIR_CONST,
    MIR_BINARY, MIR_LOAD, MIR_UNARY, MIR_BINARY, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_NOP,
    MIR_STORE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY,
    MIR_UNARY, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_ARG,
    MIR_CALL, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY,
    MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD, MIR_UNARY, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL,
    MIR_NOP, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT,
    MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_STORE, MIR_LOAD, MIR_NOP, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_CONST, MIR_BINARY,
    MIR_STORE, MIR_INDEX_ADDRESS, MIR_NOP, MIR_UNARY, MIR_STORE_INDIRECT, MIR_LABEL, MIR_NOP, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT,
    MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_RETURN,
    MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS,
    MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL,
    MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_ARG,
    MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_MEMBER_ADDRESS,
    MIR_LOAD, MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_UNARY,
    MIR_STORE_INDIRECT, MIR_LOAD, MIR_MEMBER_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT
};

static const unsigned char mir_storage_opcode_opcodes[66] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_PARAM, MIR_NOP, MIR_BRANCH_FALSE, MIR_NOP, MIR_BRANCH_FALSE,
    MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_RETURN, MIR_LABEL, MIR_NOP, MIR_CONST,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_LABEL, MIR_PHI, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_RETURN, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_LABEL,
    MIR_PHI, MIR_RETURN
};

static const unsigned char mir_cob_add_sub_opcodes[228] = {
    MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_CALL,
    MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_CONST,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT,
    MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_LABEL, MIR_PHI,
    MIR_UNARY, MIR_BRANCH_FALSE, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_LABEL, MIR_NOP, MIR_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_LABEL, MIR_LABEL,
    MIR_PHI, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_LABEL,
    MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_LABEL,
    MIR_LABEL, MIR_PHI, MIR_UNARY, MIR_STORE, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CALL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_LABEL, MIR_LABEL,
    MIR_PHI, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_BINARY, MIR_STORE_INDIRECT, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_LABEL, MIR_PHI, MIR_UNARY, MIR_BRANCH_FALSE, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_LABEL,
    MIR_NOP, MIR_CONST, MIR_ARG, MIR_NOP, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL,
    MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_ARG, MIR_NOP, MIR_ARG, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_NOP, MIR_LABEL
};

static const unsigned char mir_cob_tokenizer_opcodes[520] = {
    MIR_LABEL, MIR_PARAM, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_NOP, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_STORE_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_NOP,
    MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_LOAD,
    MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LABEL, MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST,
    MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY,
    MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE,
    MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_STORE, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_NOP,
    MIR_LOAD, MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_STORE_INDIRECT,
    MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_JUMP,
    MIR_LABEL, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP,
    MIR_CONST, MIR_STORE_INDIRECT, MIR_CONST, MIR_ARG, MIR_CALL, MIR_ADDRESS, MIR_ARG, MIR_CALL,
    MIR_ARG, MIR_CALL, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_NOP, MIR_NOP, MIR_CONST, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP,
    MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_BINARY, MIR_CONST,
    MIR_BINARY, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_NOP,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_ARG, MIR_CALL, MIR_LOAD, MIR_UNARY,
    MIR_NOP, MIR_ARG, MIR_CALL, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_ARG,
    MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_UNARY,
    MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_BINARY, MIR_CONST, MIR_BINARY,
    MIR_NOP, MIR_STORE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_NOP, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_ARG, MIR_CALL, MIR_LOAD, MIR_UNARY, MIR_ARG,
    MIR_CALL, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_NOP, MIR_STORE,
    MIR_ADDRESS, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_NOP, MIR_ARG,
    MIR_CALL, MIR_UNARY, MIR_STORE_INDIRECT, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_NOP, MIR_LOAD,
    MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_NOP, MIR_NOP,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE,
    MIR_INDEX_ADDRESS, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_UNARY,
    MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_NOP, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT,
    MIR_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_ARG, MIR_CALL, MIR_CONST, MIR_ARG, MIR_CALL,
    MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_BRANCH_FALSE, MIR_LABEL, MIR_NOP, MIR_ARG,
    MIR_CALL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_ARG, MIR_CALL, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_ARG, MIR_CALL,
    MIR_NOP, MIR_STORE, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_ARG,
    MIR_CALL, MIR_NOP, MIR_ARG, MIR_CALL, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_NOP, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_ADDRESS, MIR_ARG, MIR_CALL, MIR_CONST,
    MIR_ARG, MIR_CALL, MIR_NOP, MIR_LABEL, MIR_LABEL, MIR_LABEL, MIR_LABEL, MIR_NOP,
    MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LABEL, MIR_LABEL,
    MIR_LABEL, MIR_LABEL, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_NOP, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_STORE_INDIRECT
};

static const unsigned char mir_cob_var_access_opcodes[171] = {
    MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_STRING_ADDRESS, MIR_ARG,
    MIR_CALL, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_INDEX_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_UNARY, MIR_STORE, MIR_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY, MIR_STORE_INDIRECT, MIR_CONST, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CALL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_LABEL, MIR_LABEL, MIR_PHI,
    MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_BINARY,
    MIR_STORE_INDIRECT, MIR_CONST, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_LABEL,
    MIR_PHI, MIR_UNARY, MIR_BRANCH_FALSE, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_LABEL, MIR_NOP,
    MIR_CONST, MIR_ARG, MIR_NOP, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL, MIR_NOP,
    MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_ARG, MIR_NOP, MIR_ARG, MIR_CONST, MIR_ARG,
    MIR_CALL, MIR_NOP, MIR_LABEL
};

static const unsigned char mir_fortran_add_stmt_opcodes[71] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_PARAM, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL, MIR_LABEL, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_NOP, MIR_STORE_INDIRECT, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_LOAD, MIR_ARG,
    MIR_CALL, MIR_STORE_INDIRECT, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP, MIR_STORE_INDIRECT,
    MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD,
    MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_LOAD,
    MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS,
    MIR_MEMBER_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_MEMBER_ADDRESS,
    MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE
};

static const unsigned char mir_fortran_parse_decl_opcodes[394] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_LOAD, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_LOAD,
    MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY,
    MIR_ARG, MIR_CALL, MIR_UNARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_LABEL, MIR_LOAD,
    MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY,
    MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_LOAD,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_JUMP, MIR_LABEL,
    MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_NOP, MIR_STORE, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_PHI, MIR_LOAD, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_NOP, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL,
    MIR_UNARY, MIR_STORE_INDIRECT, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_NOP,
    MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_NOP, MIR_STORE,
    MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_NOP,
    MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY,
    MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_NOP,
    MIR_NOP, MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY,
    MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
    MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_LOAD,
    MIR_NOP, MIR_NOP, MIR_NOP, MIR_NOP, MIR_PHI, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY,
    MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_BINARY, MIR_CONST, MIR_BINARY,
    MIR_NOP, MIR_STORE, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_STORE,
    MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_NOP,
    MIR_NOP, MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_LABEL, MIR_ADDRESS,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_ARG, MIR_LOAD,
    MIR_ARG, MIR_NOP, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_LABEL, MIR_LABEL,
    MIR_LOAD, MIR_NOP, MIR_NOP, MIR_NOP, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_LABEL,
    MIR_JUMP, MIR_LABEL
};

static const unsigned char mir_fortran_parse_source_opcodes[337] = {
    MIR_LABEL, MIR_NOP, MIR_CONST, MIR_STORE, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL,
    MIR_NOP, MIR_NOP, MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE, MIR_CONST, MIR_NOP,
    MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_LOAD, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY,
    MIR_STORE, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_NOP, MIR_STORE,
    MIR_NOP, MIR_JUMP, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
    MIR_ADDRESS, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_UNARY,
    MIR_STORE_INDIRECT, MIR_LABEL, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LABEL, MIR_NOP,
    MIR_NOP, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_LOAD, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
    MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD,
    MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY,
    MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_ADDRESS,
    MIR_LOAD, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
    MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL,
    MIR_ADDRESS, MIR_ARG, MIR_CALL, MIR_ADDRESS, MIR_CONST, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY,
    MIR_BRANCH_FALSE, MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_NOP, MIR_STORE, MIR_CONST,
    MIR_NOP, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_PHI, MIR_NOP, MIR_PHI,
    MIR_ADDRESS, MIR_NOP, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL,
    MIR_BRANCH_FALSE, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_ADDRESS, MIR_NOP, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_BINARY, MIR_CONST, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_NOP, MIR_CONST,
    MIR_BINARY, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LABEL, MIR_NOP,
    MIR_NOP, MIR_PHI, MIR_NOP, MIR_NOP, MIR_ADDRESS, MIR_NOP, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
    MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_NOP, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG,
    MIR_CALL, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL,
    MIR_PHI, MIR_BRANCH_FALSE, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_JUMP,
    MIR_LABEL, MIR_ADDRESS, MIR_ARG, MIR_ADDRESS, MIR_NOP, MIR_BINARY, MIR_ARG, MIR_CALL,
    MIR_ADDRESS, MIR_ARG, MIR_CALL, MIR_ADDRESS, MIR_ARG, MIR_STRING_ADDRESS, MIR_ARG, MIR_CALL,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_NOP, MIR_ARG,
    MIR_ADDRESS, MIR_ARG, MIR_LOAD, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LABEL, MIR_JUMP,
    MIR_LABEL
};

static const unsigned char mir_fortran_parse_lhs_opcodes[158] = {
    MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_PARAM, MIR_LOAD, MIR_NOP, MIR_STORE, MIR_LOAD,
    MIR_ARG, MIR_CALL, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LABEL, MIR_LOAD, MIR_NOP,
    MIR_NOP, MIR_PHI, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL,
    MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_CONST,
    MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST,
    MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_NOP,
    MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP,
    MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_ADDRESS, MIR_NOP, MIR_CONST,
    MIR_BINARY, MIR_STORE, MIR_INDEX_ADDRESS, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LOAD_INDIRECT,
    MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_UNARY, MIR_STORE_INDIRECT, MIR_NOP, MIR_LABEL,
    MIR_JUMP, MIR_LABEL, MIR_ADDRESS, MIR_NOP, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT,
    MIR_NOP, MIR_ADDRESS, MIR_ARG, MIR_CALL, MIR_STORE_INDIRECT, MIR_NOP, MIR_CONST, MIR_NOP,
    MIR_STORE_INDIRECT, MIR_LABEL, MIR_LOAD, MIR_NOP, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_LOAD_INDIRECT,
    MIR_BRANCH_FALSE, MIR_LOAD, MIR_LOAD_INDIRECT, MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_BRANCH_FALSE,
    MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE,
    MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_LOAD,
    MIR_LOAD_INDIRECT, MIR_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY,
    MIR_STORE, MIR_LOAD, MIR_ARG, MIR_CONST, MIR_ARG, MIR_CALL, MIR_NOP, MIR_STORE,
    MIR_LOAD, MIR_BRANCH_FALSE, MIR_LOAD, MIR_NOP, MIR_CONST, MIR_STORE_INDIRECT, MIR_LABEL, MIR_NOP,
    MIR_LOAD, MIR_ARG, MIR_CALL, MIR_STORE_INDIRECT, MIR_NOP, MIR_LABEL
};

static void mir_interpreter_file_load_ix_word(
    MirStream *out, int offset);

static void mir_interpreter_file_store_ix_word(
    MirStream *out, int offset);

static void mir_interpreter_file_cleanup(
    MirStream *out, int words);

enum {
    MIR_STRADDR = MIR_STRING_ADDRESS,
    MIR_BRFALSE = MIR_BRANCH_FALSE
};

static int mir_machine_call_arguments(
    const struct MirInsn *call, int expected_count, int *arguments)
{
    int count = 0;
    int instruction;
    int item;

    for (item = 0; item < expected_count; ++item)
        arguments[item] = -1;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *arg = &mir.insns[instruction];
        int index;

        if (arg->opcode != MIR_ARG ||
            arg->secondary_offset != call->secondary_offset)
            continue;
        index = (int)arg->immediate;
        if (index < 0 || index >= expected_count ||
            arguments[index] >= 0)
            return 0;
        arguments[index] = arg->src1;
        ++count;
    }
    return count == expected_count;
}

static struct Sym *mir_interpreter_file_function(
    int instruction, int variadic, int fixed_arguments)
{
    const struct MirInsn *call = &mir.insns[instruction];
    struct Sym *function;

    if (call->opcode != MIR_CALL || call->src1 >= 0 ||
        ((call->memory_flags & MIR_CALL_FLAG_VARIADIC) != 0) !=
            variadic ||
        (call->memory_flags & MIR_CALL_FLAG_FORMAT_RUNTIME) != 0 ||
        (function = find_global(call->name)) == NULL ||
        function->storage != SC_FUNC || function->is_funcptr ||
        function->is_noreturn || !function->has_proto ||
        function->proto_variadic != variadic ||
        function->proto_nargs != fixed_arguments)
        return NULL;
    return function;
}

static int mir_interpreter_file_call_arguments(
    int call_instruction, int argument_count,
    const int *definitions)
{
    int arguments[4];
    int argument;

    if (argument_count < 1 || argument_count > 4 ||
        !mir_machine_call_arguments(
            &mir.insns[call_instruction],
            argument_count, arguments))
        return 0;
    for (argument = 0; argument < argument_count; ++argument)
        if (arguments[argument] !=
                mir.insns[definitions[argument]].dst)
            return 0;
    return 1;
}

static int mir_interpreter_file_direct_location(
    int instruction, int width,
    struct MirInterpreterFileLocation *location)
{
    const struct MirInsn *insn = &mir.insns[instruction];
    struct Sym *symbol;

    if ((insn->opcode != MIR_LOAD &&
         insn->opcode != MIR_STORE) ||
        !mir_machine_named_nonvolatile(insn) ||
        (symbol = find_global(insn->name)) == NULL ||
        !symbol->is_defined || symbol->is_volatile ||
        type_size(symbol->type) != width)
        return 0;
    memset(location, 0, sizeof(*location));
    location->root = symbol;
    location->kind = MIR_INTERPRETER_FILE_DIRECT;
    return 1;
}

static int mir_interpreter_file_member_location(
    int base_instruction, int member_instruction, int width,
    struct MirInterpreterFileLocation *location)
{
    const struct MirInsn *base = &mir.insns[base_instruction];
    const struct MirInsn *member = &mir.insns[member_instruction];
    struct Sym *root;

    if ((base->opcode != MIR_ADDRESS &&
         base->opcode != MIR_LOAD) ||
        !mir_machine_named_nonvolatile(base) ||
        (root = find_global(base->name)) == NULL ||
        !root->is_defined || root->is_volatile ||
        member->opcode != MIR_MEMBER_ADDRESS ||
        member->src1 != base->dst ||
        member->memory_size != width ||
        (member->memory_flags & (1 | 8)) != 0 ||
        member->immediate < -32768 ||
        member->immediate > 32767)
        return 0;
    if (base->opcode == MIR_LOAD &&
        (type_ptr_depth(base->type) == 0 ||
         type_size(base->type) != 2))
        return 0;
    memset(location, 0, sizeof(*location));
    location->root = root;
    location->offset = (int)member->immediate;
    location->kind = base->opcode == MIR_LOAD
        ? MIR_INTERPRETER_FILE_POINTER_STRUCT
        : MIR_INTERPRETER_FILE_STRUCT;
    return 1;
}

static int mir_interpreter_file_same_function(
    struct Sym *expected, int instruction,
    int variadic, int fixed_arguments)
{
    return expected != NULL &&
           mir_interpreter_file_function(
               instruction, variadic, fixed_arguments) == expected;
}

static int mir_interpreter_file_shape(
    struct MirInterpreterFileSchedule *plan,
    const unsigned char **opcodes_out, size_t *count_out,
    int *blocks_out)
{
    memset(plan, 0, sizeof(*plan));
    switch (mir.count) {
    case 212:
        plan->kind = MIR_INTERPRETER_FILE_FORTH;
        *opcodes_out = mir_interpreter_file_forth_opcodes;
        *count_out = sizeof(mir_interpreter_file_forth_opcodes);
        *blocks_out = 20;
        return 1;
    case 190:
        plan->kind = MIR_INTERPRETER_FILE_FORTRAN;
        *opcodes_out = mir_interpreter_file_fortran_opcodes;
        *count_out = sizeof(mir_interpreter_file_fortran_opcodes);
        *blocks_out = 20;
        return 1;
    case 178:
        plan->kind = MIR_INTERPRETER_FILE_COBOL;
        *opcodes_out = mir_interpreter_file_cobol_opcodes;
        *count_out = sizeof(mir_interpreter_file_cobol_opcodes);
        *blocks_out = 18;
        return 1;
    case 176:
        plan->kind = MIR_INTERPRETER_FILE_PASCAL;
        *opcodes_out = mir_interpreter_file_pascal_opcodes;
        *count_out = sizeof(mir_interpreter_file_pascal_opcodes);
        *blocks_out = 14;
        return 1;
    case 183:
        plan->kind = MIR_INTERPRETER_FILE_BASIC;
        *opcodes_out = mir_interpreter_file_basic_opcodes;
        *count_out = sizeof(mir_interpreter_file_basic_opcodes);
        *blocks_out = 18;
        return 1;
    case 144:
        {
            int instruction;
            int pointer_shape = 1;

            for (instruction = 0; instruction < mir.count;
                 ++instruction)
                if (mir.insns[instruction].opcode !=
                        mir_interpreter_file_preprocess_pointer_opcodes[
                            instruction]) {
                    pointer_shape = 0;
                    break;
                }
            plan->kind = pointer_shape
                ? MIR_INTERPRETER_FILE_PREPROCESS_POINTER
                : MIR_INTERPRETER_FILE_PREPROCESS_STRUCT;
        }
        *opcodes_out =
            plan->kind == MIR_INTERPRETER_FILE_PREPROCESS_POINTER
                ? mir_interpreter_file_preprocess_pointer_opcodes
                : mir_interpreter_file_preprocess_struct_opcodes;
        *count_out = sizeof(mir_interpreter_file_preprocess_pointer_opcodes);
        *blocks_out = 12;
        return 1;
    default:
        return 0;
    }
}

static int mir_match_interpreter_file_schedule(
    struct MirInterpreterFileSchedule *plan)
{
    const unsigned char *opcodes;
    size_t opcode_count;
    int expected_blocks;
    int instruction;
    int open_arguments[2] = {2, 4};
    int perror_arguments[1] = {12};
    int seek_arguments[3] = {19, 21, 23};
    int close_indices[4];
    int close_count = 0;
    int tell_instruction;
    int print_instruction;
    int allocate_instruction;
    int read_instruction;
    int maximum_instruction;
    int source_instruction = -1;
    int length_instruction = -1;
    int source_base = -1;
    int source_member = -1;
    int length_base = -1;
    int length_member = -1;
    int second_seek = -1;
    int free_instruction = -1;
    int preprocess_instruction = -1;
    int length_call_instruction = -1;
    long maximum_source;

    if (!mir_interpreter_file_shape(
            plan, &opcodes, &opcode_count, &expected_blocks) ||
        (size_t)mir.count != opcode_count ||
        mir_cfg_block_count() != expected_blocks ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        type_ptr_depth(mir.return_type) != 0 ||
        (mir.return_type & 15) != TYPE_INT ||
        type_size(mir.return_type) != 2)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_PARAM ||
             insn->opcode == MIR_LOAD ||
             insn->opcode == MIR_STORE) &&
            insn->name[0] != 0 &&
            !mir_machine_named_nonvolatile(insn))
            return mir_machine_reject(
                "interpreter-file-schedule",
                "volatile-named-memory");
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "interpreter-file-schedule",
                "volatile-indirect-memory");
    }
    if (type_ptr_depth(mir.insns[1].type) != 1 ||
        (mir.insns[1].type & 15) != TYPE_CHAR ||
        type_size(mir.insns[1].type) != 2 ||
        mir_machine_pointee_is_volatile(&mir.insns[1]) ||
        !mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->name_stack_offset) ||
        !mir_machine_same_location(
            &mir.insns[1], &mir.insns[2]) ||
        mir.insns[3].src1 != mir.insns[2].dst ||
        mir.insns[5].src1 != mir.insns[4].dst ||
        mir.insns[4].immediate < 0)
        return mir_machine_reject(
            "interpreter-file-schedule", "entry");
    plan->mode_string_id = (int)mir.insns[4].immediate;
    plan->open_function =
        mir_interpreter_file_function(6, 0, 2);
    plan->perror_function =
        mir_interpreter_file_function(14, 0, 1);
    plan->seek_function =
        mir_interpreter_file_function(25, 0, 3);
    if (plan->open_function == NULL ||
        plan->perror_function == NULL ||
        plan->seek_function == NULL ||
        !mir_interpreter_file_call_arguments(
            6, 2, open_arguments) ||
        !mir_interpreter_file_call_arguments(
            14, 1, perror_arguments) ||
        !mir_interpreter_file_call_arguments(
            25, 3, seek_arguments) ||
        !mir_machine_constant_equals(mir.insns[21].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[23].dst, 2))
        return mir_machine_reject(
            "interpreter-file-schedule", "entry-calls");

    switch (plan->kind) {
    case MIR_INTERPRETER_FILE_FORTH:
        tell_instruction = 38;
        print_instruction = 76;
        allocate_instruction = 106;
        read_instruction = 134;
        maximum_instruction = 51;
        second_seek = 87;
        free_instruction = 150;
        close_indices[close_count++] = 31;
        close_indices[close_count++] = 68;
        close_indices[close_count++] = 93;
        close_indices[close_count++] = 116;
        source_base = 98;
        source_member = 99;
        length_base = 206;
        length_member = 207;
        plan->too_large_string_id = (int)mir.insns[72].immediate;
        break;
    case MIR_INTERPRETER_FILE_FORTRAN:
        tell_instruction = 38;
        print_instruction = 76;
        allocate_instruction = 104;
        read_instruction = 129;
        maximum_instruction = 51;
        second_seek = 87;
        close_indices[close_count++] = 31;
        close_indices[close_count++] = 68;
        close_indices[close_count++] = 93;
        close_indices[close_count++] = 113;
        source_instruction = 107;
        length_instruction = 187;
        plan->too_large_string_id = (int)mir.insns[72].immediate;
        break;
    case MIR_INTERPRETER_FILE_PREPROCESS_POINTER:
    case MIR_INTERPRETER_FILE_PREPROCESS_STRUCT:
        tell_instruction = 28;
        print_instruction = 71;
        allocate_instruction = 82;
        read_instruction = 103;
        maximum_instruction = 48;
        close_indices[close_count++] = 65;
        close_indices[close_count++] = 109;
        free_instruction = 132;
        preprocess_instruction = 127;
        length_call_instruction = 139;
        source_base = 123;
        source_member = 124;
        length_base = 133;
        length_member = 134;
        plan->too_large_string_id = (int)mir.insns[69].immediate;
        break;
    case MIR_INTERPRETER_FILE_COBOL:
        tell_instruction = 28;
        print_instruction = 71;
        allocate_instruction = 84;
        read_instruction = 112;
        maximum_instruction = 48;
        close_indices[close_count++] = 65;
        close_indices[close_count++] = 94;
        close_indices[close_count++] = 118;
        source_base = 76;
        source_member = 77;
        length_base = 172;
        length_member = 173;
        plan->too_large_string_id = (int)mir.insns[69].immediate;
        break;
    case MIR_INTERPRETER_FILE_PASCAL:
        tell_instruction = 41;
        print_instruction = 79;
        allocate_instruction = 110;
        read_instruction = 135;
        maximum_instruction = 54;
        second_seek = 90;
        free_instruction = 157;
        close_indices[close_count++] = 31;
        close_indices[close_count++] = 71;
        close_indices[close_count++] = 96;
        close_indices[close_count++] = 141;
        source_instruction = 113;
        length_instruction = 173;
        plan->too_large_string_id = (int)mir.insns[75].immediate;
        plan->allocation_string_id = (int)mir.insns[117].immediate;
        plan->read_string_id = (int)mir.insns[149].immediate;
        break;
    case MIR_INTERPRETER_FILE_BASIC:
        tell_instruction = 28;
        print_instruction = 73;
        allocate_instruction = 84;
        read_instruction = 109;
        maximum_instruction = 48;
        free_instruction = 123;
        close_indices[close_count++] = 65;
        close_indices[close_count++] = 93;
        close_indices[close_count++] = 115;
        source_instruction = 87;
        length_instruction = 180;
        plan->too_large_string_id = (int)mir.insns[70].immediate;
        break;
    default:
        return 0;
    }

    plan->tell_function =
        mir_interpreter_file_function(tell_instruction, 0, 1);
    plan->close_function =
        mir_interpreter_file_function(close_indices[0], 0, 1);
    plan->print_function =
        mir_interpreter_file_function(print_instruction, 1, 2);
    plan->allocate_function =
        mir_interpreter_file_function(allocate_instruction, 0, 1);
    plan->read_function =
        mir_interpreter_file_function(read_instruction, 0, 4);
    if (plan->tell_function == NULL ||
        plan->close_function == NULL ||
        plan->print_function == NULL ||
        plan->allocate_function == NULL ||
        plan->read_function == NULL ||
        plan->too_large_string_id < 0 ||
        !mir_machine_evaluate_constant(
            mir.insns[maximum_instruction].dst,
            &maximum_source, 0) ||
        maximum_source <= 0 || maximum_source > 65534)
        return mir_machine_reject(
            "interpreter-file-schedule", "functions");
    plan->maximum_source = (int)maximum_source;
    for (instruction = 1; instruction < close_count; ++instruction)
        if (!mir_interpreter_file_same_function(
                plan->close_function, close_indices[instruction],
                0, 1))
            return mir_machine_reject(
                "interpreter-file-schedule", "close-calls");
    if (second_seek >= 0 &&
        !mir_interpreter_file_same_function(
            plan->seek_function, second_seek, 0, 3))
        return mir_machine_reject(
            "interpreter-file-schedule", "seek-calls");
    if (free_instruction >= 0) {
        plan->free_function =
            mir_interpreter_file_function(
                free_instruction, 0, 1);
        if (plan->free_function == NULL)
            return mir_machine_reject(
                "interpreter-file-schedule", "free-call");
    }
    if (preprocess_instruction >= 0) {
        plan->preprocess_function =
            mir_interpreter_file_function(
                preprocess_instruction, 0, 1);
        plan->length_function =
            mir_interpreter_file_function(
                length_call_instruction, 0, 1);
        if (plan->preprocess_function == NULL ||
            plan->length_function == NULL)
            return mir_machine_reject(
                "interpreter-file-schedule",
                "preprocess-calls");
    }
    if (source_base >= 0) {
        if (!mir_interpreter_file_member_location(
                source_base, source_member, 2,
                &plan->source) ||
            !mir_interpreter_file_member_location(
                length_base, length_member, 4,
                &plan->source_length) ||
            plan->source.root != plan->source_length.root ||
            plan->source.kind != plan->source_length.kind)
            return mir_machine_reject(
                "interpreter-file-schedule",
                "state-locations");
    } else if (!mir_interpreter_file_direct_location(
                   source_instruction, 2, &plan->source) ||
               !mir_interpreter_file_direct_location(
                   length_instruction, 4,
                   &plan->source_length)) {
        return mir_machine_reject(
            "interpreter-file-schedule",
            "global-locations");
    }
    if (plan->source.root->pointee_is_volatile ||
        plan->source_length.root->pointee_is_volatile)
        return mir_machine_reject(
            "interpreter-file-schedule",
            "volatile-state");
    snprintf(plan->print_name, sizeof(plan->print_name), "%s",
             mir.insns[print_instruction].base_name[0] != 0
                 ? mir.insns[print_instruction].base_name
                 : asm_name_for(sym_asm_name(plan->print_function)));
    return plan->print_name[0] != 0;
}

static int mir_forth_lexer_member_group(
    struct MirForthLexerSchedule *plan, const int *members,
    size_t count, int width, int *offset_out)
{
    size_t item;
    int offset = (int)mir.insns[members[0]].immediate;

    if (offset < -128 || offset + width - 1 > 127)
        return 0;
    for (item = 0; item < count; ++item) {
        const struct MirInsn *member = &mir.insns[members[item]];
        const struct MirInsn *base = &mir.insns[members[item] - 1];

        if (base->opcode != MIR_LOAD ||
            !mir_machine_named_nonvolatile(base) ||
            find_global(base->name) != plan->state ||
            member->opcode != MIR_MEMBER_ADDRESS ||
            member->src1 != base->dst ||
            member->memory_size != width ||
            member->immediate != offset ||
            (member->memory_flags & (1 | 8)) != 0)
            return 0;
    }
    *offset_out = offset;
    return 1;
}

static int mir_match_forth_lexer_schedule(
    struct MirForthLexerSchedule *plan)
{
    static const int position_members[] = {
        4, 15, 27, 56, 66, 77, 94, 126, 139, 154, 173,
        190, 202, 213, 233, 272, 283, 300, 337, 348, 420
    };
    static const int length_members[] = {
        7, 29, 69, 129, 157, 205, 275, 340
    };
    static const int source_members[] = {
        12, 74, 136, 170, 210, 230, 280, 345
    };
    static const int line_members[] = {49};
    static const int text_members[] = {
        111, 257, 307, 324, 409, 430,
        443, 452, 475, 493, 506, 523
    };
    static const int token_members[] = {118, 314, 539, 548};
    static const int integer_members[] = {122, 532};
    static const int first_space_argument[] = {36};
    static const int second_space_argument[] = {353};
    static const int first_digit_argument[] = {480};
    static const int second_digit_argument[] = {498};
    struct Sym *state;
    int instruction;
    size_t edge;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 551 || mir_cfg_block_count() != 62 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != mir_forth_lexer_opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_LOAD ||
             insn->opcode == MIR_STORE) &&
            insn->name[0] != 0 &&
            !mir_machine_named_nonvolatile(insn))
            return mir_machine_reject(
                "forth-lexer-schedule",
                "volatile-named-memory");
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "forth-lexer-schedule",
                "volatile-indirect-memory");
    }
    for (edge = 0;
         edge < sizeof(mir_forth_lexer_edges) /
                    sizeof(mir_forth_lexer_edges[0]);
         ++edge)
        if (mir.insns[mir_forth_lexer_edges[edge][0]].label !=
                mir.insns[mir_forth_lexer_edges[edge][1]].label)
            return mir_machine_reject(
                "forth-lexer-schedule", "control-flow");
    state = find_global(mir.insns[3].name);
    if (state == NULL || !state->is_defined ||
        state->is_volatile || state->pointee_is_volatile ||
        type_ptr_depth(mir.insns[3].type) == 0 ||
        type_size(mir.insns[3].type) != 2)
        return mir_machine_reject(
            "forth-lexer-schedule", "state");
    plan->state = state;
    if (!mir_forth_lexer_member_group(
            plan, position_members,
            sizeof(position_members) / sizeof(position_members[0]),
            4, &plan->position_offset) ||
        !mir_forth_lexer_member_group(
            plan, length_members,
            sizeof(length_members) / sizeof(length_members[0]),
            4, &plan->length_offset) ||
        !mir_forth_lexer_member_group(
            plan, source_members,
            sizeof(source_members) / sizeof(source_members[0]),
            2, &plan->source_offset) ||
        !mir_forth_lexer_member_group(
            plan, line_members,
            sizeof(line_members) / sizeof(line_members[0]),
            2, &plan->line_offset) ||
        !mir_forth_lexer_member_group(
            plan, text_members,
            sizeof(text_members) / sizeof(text_members[0]),
            64, &plan->text_offset) ||
        !mir_forth_lexer_member_group(
            plan, token_members,
            sizeof(token_members) / sizeof(token_members[0]),
            2, &plan->token_offset) ||
        !mir_forth_lexer_member_group(
            plan, integer_members,
            sizeof(integer_members) / sizeof(integer_members[0]),
            2, &plan->integer_offset))
        return mir_machine_reject(
            "forth-lexer-schedule", "members");
    if (plan->source_offset == plan->position_offset ||
        plan->source_offset == plan->length_offset ||
        plan->text_offset == plan->source_offset ||
        plan->token_offset == plan->integer_offset)
        return mir_machine_reject(
            "forth-lexer-schedule", "member-alias");
    plan->space_function =
        mir_interpreter_file_function(38, 0, 1);
    plan->comment_function =
        mir_interpreter_file_function(99, 0, 0);
    plan->digit_function =
        mir_interpreter_file_function(482, 0, 1);
    if (plan->space_function == NULL ||
        plan->comment_function == NULL ||
        plan->digit_function == NULL ||
        !mir_interpreter_file_same_function(
            plan->space_function, 365, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->digit_function, 500, 0, 1) ||
        !mir_interpreter_file_call_arguments(
            38, 1, first_space_argument) ||
        !mir_interpreter_file_call_arguments(
            365, 1, second_space_argument) ||
        !mir_interpreter_file_call_arguments(
            482, 1, first_digit_argument) ||
        !mir_interpreter_file_call_arguments(
            500, 1, second_digit_argument))
        return mir_machine_reject(
            "forth-lexer-schedule", "calls");
    if (!mir_machine_constant_equals(mir.insns[23].dst, 26) ||
        !mir_machine_constant_equals(mir.insns[45].dst, '\n') ||
        !mir_machine_constant_equals(mir.insns[51].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[58].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[81].dst, '(') ||
        !mir_machine_constant_equals(mir.insns[96].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[112].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[119].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[123].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[141].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[150].dst, '.') ||
        !mir_machine_constant_equals(mir.insns[177].dst, '"') ||
        !mir_machine_constant_equals(mir.insns[217].dst, '"') ||
        !mir_machine_constant_equals(mir.insns[244].dst, 26) ||
        !mir_machine_constant_equals(mir.insns[253].dst, 63) ||
        !mir_machine_constant_equals(mir.insns[287].dst, '"') ||
        !mir_machine_constant_equals(mir.insns[311].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[315].dst, 258) ||
        !mir_machine_constant_equals(mir.insns[356].dst, 26) ||
        !mir_machine_constant_equals(mir.insns[384].dst, '(') ||
        !mir_machine_constant_equals(mir.insns[405].dst, 63) ||
        !mir_machine_constant_equals(mir.insns[434].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[447].dst, '-') ||
        !mir_machine_constant_equals(mir.insns[466].dst, 65535) ||
        !mir_machine_constant_equals(mir.insns[486].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[503].dst, 10) ||
        !mir_machine_constant_equals(mir.insns[515].dst, '0') ||
        !mir_machine_constant_equals(mir.insns[527].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[540].dst, 257) ||
        !mir_machine_constant_equals(mir.insns[549].dst, 256))
        return mir_machine_reject(
            "forth-lexer-schedule", "constants");
    return 1;
}

static void mir_forth_load_iy_word(
    MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld l,(iy%+d)\n\tld h,(iy%+d)\n",
            offset, offset + 1);
}

static void mir_forth_store_iy_word(
    MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld (iy%+d),l\n\tld (iy%+d),h\n",
            offset, offset + 1);
}

static void mir_forth_increment_position(
    MirStream *out, const struct MirForthLexerSchedule *plan)
{
    int done = new_label();

    mir_stream_printf(out,
            "\tinc (iy%+d)\n\tjp nz,L%d\n"
            "\tinc (iy%+d)\nL%d:\n",
            plan->position_offset, done,
            plan->position_offset + 1, done);
}

static void mir_forth_load_source_character(
    MirStream *out, const struct MirForthLexerSchedule *plan)
{
    mir_forth_load_iy_word(out, plan->source_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tadd hl,de\n\tld a,(hl)\n",
            plan->position_offset, plan->position_offset + 1);
}

static void mir_forth_text_address(
    MirStream *out, const struct MirForthLexerSchedule *plan)
{
    mir_stream_puts("\tpush iy\n\tpop hl\n", out);
    mir_machine_emit_hl_offset(out, plan->text_offset, 0);
    mir_stream_puts("\tld e,(ix-1)\n\tld d,0\n\tadd hl,de\n", out);
}

static void mir_forth_call_character_function(
    MirStream *out, struct Sym *function)
{
    mir_stream_puts("\tld l,a\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, function);
    mir_stream_puts("\tpop bc\n\tld a,h\n\tor l\n", out);
}

static void mir_forth_set_token(
    MirStream *out, const struct MirForthLexerSchedule *plan,
    int token)
{
    mir_stream_printf(out,
            "\tld a,%d\n\tld (iy%+d),a\n"
            "\tld a,%d\n\tld (iy%+d),a\n",
            token & 255, plan->token_offset,
            (token >> 8) & 255, plan->token_offset + 1);
}

static void mir_emit_forth_lexer_schedule(
    MirStream *out, const struct MirForthLexerSchedule *plan)
{
    int scan_space = new_label();
    int not_eof_marker = new_label();
    int space_done = new_label();
    int not_newline = new_label();
    int no_comment = new_label();
    int have_input = new_label();
    int normal_token = new_label();
    int string_loop = new_label();
    int string_done = new_label();
    int string_store_done = new_label();
    int no_closing_quote = new_label();
    int token_loop = new_label();
    int token_done = new_label();
    int token_store_done = new_label();
    int positive = new_label();
    int not_number = new_label();
    int digit_loop = new_label();
    int digits_done = new_label();
    int positive_value = new_label();
    int epilogue = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-6\n\tadd hl,sp\n\tld sp,hl\n",
          out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_global_word(out, plan->state, 0);
    mir_stream_puts("\tpush hl\n\tpop iy\n", out);

    mir_stream_printf(out, "L%d:\n", scan_space);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            space_done);
    mir_forth_load_source_character(out, plan);
    mir_stream_puts("\tld (ix-2),a\n\tcp 26\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", not_eof_marker);
    mir_forth_load_iy_word(out, plan->length_offset);
    mir_forth_store_iy_word(out, plan->position_offset);
    mir_stream_puts("\txor a\n", out);
    mir_stream_printf(out,
            "\tld (iy%+d),a\n\tld (iy%+d),a\n"
            "\tjp L%d\nL%d:\n",
            plan->position_offset + 2,
            plan->position_offset + 3,
            space_done, not_eof_marker);
    mir_stream_puts("\tld a,(ix-2)\n", out);
    mir_forth_call_character_function(out, plan->space_function);
    mir_stream_printf(out, "\tjp z,L%d\n", space_done);
    mir_stream_puts("\tld a,(ix-2)\n\tcp 10\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", not_newline);
    mir_forth_load_iy_word(out, plan->line_offset);
    mir_stream_puts("\tinc hl\n", out);
    mir_forth_store_iy_word(out, plan->line_offset);
    mir_stream_printf(out, "L%d:\n", not_newline);
    mir_forth_increment_position(out, plan);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", scan_space, space_done);

    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            no_comment);
    mir_forth_load_source_character(out, plan);
    mir_stream_puts("\tcp '('\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_comment);
    mir_forth_increment_position(out, plan);
    mir_machine_emit_symbol_call(out, plan->comment_function);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", scan_space, no_comment);

    mir_stream_printf(out,
            "\txor a\n\tld (iy%+d),a\n"
            "\tld (iy%+d),a\n\tld (iy%+d),a\n"
            "\tld (iy%+d),a\n\tld (iy%+d),a\n",
            plan->text_offset,
            plan->token_offset, plan->token_offset + 1,
            plan->integer_offset, plan->integer_offset + 1);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp c,L%d\n"
            "\tjp L%d\nL%d:\n",
            plan->length_offset, plan->length_offset + 1,
            have_input, epilogue, have_input);
    mir_forth_load_source_character(out, plan);
    mir_stream_puts("\tld (ix-2),a\n", out);
    mir_forth_increment_position(out, plan);
    mir_stream_puts("\tcp '.'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", normal_token);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            normal_token);
    mir_forth_load_source_character(out, plan);
    mir_stream_puts("\tcp '\"'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", normal_token);
    mir_forth_increment_position(out, plan);
    mir_stream_puts("\txor a\n\tld (ix-1),a\n", out);
    mir_stream_printf(out, "L%d:\n", string_loop);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            string_done);
    mir_forth_load_source_character(out, plan);
    mir_stream_puts("\tcp '\"'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", string_done);
    mir_stream_puts("\tld (ix-2),a\n", out);
    mir_forth_increment_position(out, plan);
    mir_stream_puts("\tld a,(ix-2)\n\tcp 26\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", string_done);
    mir_stream_puts("\tld a,(ix-1)\n\tcp 63\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", string_store_done);
    mir_forth_text_address(out, plan);
    mir_stream_puts("\tld a,(ix-2)\n\tld (hl),a\n\tinc (ix-1)\n", out);
    mir_stream_printf(out, "L%d:\n\tjp L%d\nL%d:\n",
            string_store_done, string_loop, string_done);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            no_closing_quote);
    mir_forth_load_source_character(out, plan);
    mir_stream_puts("\tcp '\"'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_closing_quote);
    mir_forth_increment_position(out, plan);
    mir_stream_printf(out, "L%d:\n", no_closing_quote);
    mir_forth_text_address(out, plan);
    mir_stream_puts("\tld (hl),0\n", out);
    mir_forth_set_token(out, plan, 258);
    mir_stream_printf(out, "\tjp L%d\n", epilogue);

    mir_stream_printf(out, "L%d:\n", normal_token);
    mir_stream_puts("\txor a\n\tld (ix-1),a\n", out);
    mir_forth_text_address(out, plan);
    mir_stream_puts("\tld a,(ix-2)\n\tld (hl),a\n\tinc (ix-1)\n", out);
    mir_stream_printf(out, "L%d:\n", token_loop);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            token_done);
    mir_forth_load_source_character(out, plan);
    mir_stream_puts("\tld (ix-2),a\n\tcp 26\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", token_done);
    mir_stream_puts("\tld a,(ix-2)\n", out);
    mir_forth_call_character_function(out, plan->space_function);
    mir_stream_printf(out, "\tjp nz,L%d\n", token_done);
    mir_stream_puts("\tld a,(ix-2)\n\tcp '('\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", token_done);
    mir_stream_puts("\tld a,(ix-1)\n\tcp 63\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", token_store_done);
    mir_forth_text_address(out, plan);
    mir_stream_puts("\tld a,(ix-2)\n\tld (hl),a\n\tinc (ix-1)\n", out);
    mir_stream_printf(out, "L%d:\n", token_store_done);
    mir_forth_increment_position(out, plan);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", token_loop, token_done);
    mir_forth_text_address(out, plan);
    mir_stream_puts("\tld (hl),0\n"
          "\txor a\n\tld (ix-3),a\n\tld (ix-1),a\n", out);
    mir_stream_printf(out,
            "\tld a,(iy%+d)\n\tcp '-'\n\tjp nz,L%d\n"
            "\tld a,(iy%+d)\n\tor a\n\tjp z,L%d\n"
            "\tld a,1\n\tld (ix-3),a\n\tld (ix-1),a\n"
            "L%d:\n",
            plan->text_offset, positive,
            plan->text_offset + 1, positive, positive);
    mir_forth_text_address(out, plan);
    mir_stream_puts("\tld a,(hl)\n", out);
    mir_forth_call_character_function(out, plan->digit_function);
    mir_stream_printf(out, "\tjp z,L%d\n", not_number);
    mir_stream_puts("\tld hl,0\n", out);
    mir_interpreter_file_store_ix_word(out, -5);
    mir_stream_printf(out, "L%d:\n", digit_loop);
    mir_forth_text_address(out, plan);
    mir_stream_puts("\tld a,(hl)\n\tld (ix-2),a\n", out);
    mir_forth_call_character_function(out, plan->digit_function);
    mir_stream_printf(out, "\tjp z,L%d\n", digits_done);
    mir_interpreter_file_load_ix_word(out, -5);
    mir_stream_puts("\tld de,0\n\tld e,l\n\tld d,h\n"
          "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
          "\tadd hl,hl\n\tld a,(ix-2)\n\tsub '0'\n"
          "\tld e,a\n\tld d,0\n\tadd hl,de\n", out);
    mir_interpreter_file_store_ix_word(out, -5);
    mir_stream_puts("\tinc (ix-1)\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", digit_loop, digits_done);
    mir_forth_text_address(out, plan);
    mir_stream_puts("\tld a,(hl)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", not_number);
    mir_interpreter_file_load_ix_word(out, -5);
    mir_stream_puts("\tld a,(ix-3)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", positive_value);
    mir_stream_puts("\txor a\n\tsub l\n\tld l,a\n"
          "\tld a,0\n\tsbc a,h\n\tld h,a\n", out);
    mir_stream_printf(out, "L%d:\n", positive_value);
    mir_forth_store_iy_word(out, plan->integer_offset);
    mir_forth_set_token(out, plan, 257);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", epilogue, not_number);
    mir_forth_set_token(out, plan, 256);
    mir_stream_printf(out,
            "L%d:\n\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n",
            epilogue);
}

static int mir_bcd_byte_member(
    struct MirBcdByteMathSchedule *plan, const int *members,
    size_t count, int *offset_out)
{
    size_t item;
    int offset = (int)mir.insns[members[0]].immediate;

    if (offset < -128 || offset > 127)
        return 0;
    for (item = 0; item < count; ++item) {
        const struct MirInsn *member = &mir.insns[members[item]];
        const struct MirInsn *base = &mir.insns[members[item] - 1];

        if (base->opcode != MIR_ADDRESS ||
            !mir_machine_named_nonvolatile(base) ||
            find_global(base->name) != plan->state ||
            member->opcode != MIR_MEMBER_ADDRESS ||
            member->src1 != base->dst ||
            member->memory_size != 1 ||
            member->immediate != offset ||
            (member->memory_flags & (1 | 8)) != 0)
            return 0;
    }
    *offset_out = offset;
    return 1;
}

static int mir_match_bcd_byte_math_schedule(
    struct MirBcdByteMathSchedule *plan)
{
    static const int edges[][2] = {
        {40, 44}, {43, 59}, {49, 53}, {52, 55}, {58, 59},
        {61, 65}, {64, 80}, {70, 74}, {73, 76}, {79, 80},
        {82, 86}, {85, 101}, {91, 95}, {94, 97}, {100, 101},
        {103, 105}, {128, 183}, {134, 142}, {148, 164},
        {163, 224}, {182, 224}, {200, 216}, {215, 222}
    };
    static const int accumulator_members[] = {4, 12, 226};
    static const int zero_members[] = {32};
    static const int carry_members[] = {
        131, 158, 175, 190, 210, 218
    };
    int instruction;
    size_t edge;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 240 || mir_cfg_block_count() != 33 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != mir_bcd_byte_math_opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "bcd-byte-math-schedule",
                "volatile-memory");
    }
    for (edge = 0; edge < sizeof(edges) / sizeof(edges[0]); ++edge)
        if (mir.insns[edges[edge][0]].label !=
                mir.insns[edges[edge][1]].label)
            return mir_machine_reject(
                "bcd-byte-math-schedule", "control-flow");
    if (type_ptr_depth(mir.insns[1].type) != 0 ||
        type_ptr_depth(mir.insns[2].type) != 0 ||
        type_size(mir.insns[1].type) != 1 ||
        type_size(mir.insns[2].type) != 1 ||
        (mir.insns[1].type & TYPE_UNSIGNED) == 0 ||
        (mir.insns[2].type & TYPE_UNSIGNED) == 0 ||
        !mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->math_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst, &plan->rhs_stack_offset) ||
        plan->rhs_stack_offset != plan->math_stack_offset + 2)
        return mir_machine_reject(
            "bcd-byte-math-schedule", "parameters");
    plan->state = find_global(mir.insns[3].name);
    if (plan->state == NULL || !plan->state->is_defined ||
        plan->state->is_volatile || plan->state->pointee_is_volatile ||
        !mir_bcd_byte_member(
            plan, accumulator_members,
            sizeof(accumulator_members) /
                sizeof(accumulator_members[0]),
            &plan->accumulator_offset) ||
        !mir_bcd_byte_member(
            plan, zero_members,
            sizeof(zero_members) / sizeof(zero_members[0]),
            &plan->zero_offset) ||
        !mir_bcd_byte_member(
            plan, carry_members,
            sizeof(carry_members) / sizeof(carry_members[0]),
            &plan->carry_offset) ||
        plan->accumulator_offset == plan->zero_offset ||
        plan->accumulator_offset == plan->carry_offset ||
        plan->zero_offset == plan->carry_offset)
        return mir_machine_reject(
            "bcd-byte-math-schedule", "state");
    if (!mir_machine_constant_equals(mir.insns[6].dst, 15) ||
        !mir_machine_constant_equals(mir.insns[14].dst, 4) ||
        !mir_machine_constant_equals(mir.insns[20].dst, 15) ||
        !mir_machine_constant_equals(mir.insns[26].dst, 4) ||
        !mir_machine_constant_equals(mir.insns[34].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[37].dst, 9) ||
        !mir_machine_constant_equals(mir.insns[46].dst, 9) ||
        !mir_machine_constant_equals(mir.insns[67].dst, 9) ||
        !mir_machine_constant_equals(mir.insns[88].dst, 9) ||
        !mir_machine_constant_equals(mir.insns[107].dst, 10) ||
        !mir_machine_constant_equals(mir.insns[116].dst, 10) ||
        !mir_machine_constant_equals(mir.insns[124].dst, 224) ||
        !mir_machine_constant_equals(mir.insns[136].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[160].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[165].dst, 100) ||
        !mir_machine_constant_equals(mir.insns[177].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[197].dst, 99) ||
        !mir_machine_constant_equals(mir.insns[203].dst, 100) ||
        !mir_machine_constant_equals(mir.insns[212].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[220].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[228].dst, 10) ||
        !mir_machine_constant_equals(mir.insns[231].dst, 4) ||
        !mir_machine_constant_equals(mir.insns[234].dst, 10))
        return mir_machine_reject(
            "bcd-byte-math-schedule", "constants");
    return 1;
}

static void mir_emit_bcd_byte_math_schedule(
    MirStream *out, const struct MirBcdByteMathSchedule *plan)
{
    const char *state_name =
        asm_name_for(sym_asm_name(plan->state));
    int invalid = new_label();
    int subtract = new_label();
    int subtract_have_rhs = new_label();
    int subtract_borrow = new_label();
    int result_ready = new_label();
    int add_no_carry = new_label();
    int decimal_loop = new_label();
    int decimal_done = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-8\n\tadd hl,sp\n\tld sp,hl\n",
          out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_printf(out,
            "\tld a,(%s%+d)\n\tand 15\n\tld (ix-1),a\n"
            "\tld a,(%s%+d)\n\trrca\n\trrca\n\trrca\n\trrca\n"
            "\tand 15\n\tld (ix-2),a\n"
            "\tld a,(ix%+d)\n\tand 15\n\tld (ix-3),a\n"
            "\tld a,(ix%+d)\n\trrca\n\trrca\n\trrca\n\trrca\n"
            "\tand 15\n\tld (ix-4),a\n"
            "\txor a\n\tld (%s%+d),a\n",
            state_name, plan->accumulator_offset,
            state_name, plan->accumulator_offset,
            plan->rhs_stack_offset + 2,
            plan->rhs_stack_offset + 2,
            state_name, plan->zero_offset);
    mir_stream_puts("\tld a,(ix-1)\n\tcp 10\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", invalid);
    mir_stream_puts("\tld a,(ix-2)\n\tcp 10\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", invalid);
    mir_stream_puts("\tld a,(ix-3)\n\tcp 10\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", invalid);
    mir_stream_puts("\tld a,(ix-4)\n\tcp 10\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", invalid);
    mir_stream_puts("\tld a,(ix-2)\n\tld b,a\n\tadd a,a\n\tadd a,a\n"
          "\tadd a,b\n\tadd a,a\n\tadd a,(ix-1)\n"
          "\tld (ix-5),a\n"
          "\tld a,(ix-4)\n\tld b,a\n\tadd a,a\n\tadd a,a\n"
          "\tadd a,b\n\tadd a,a\n\tadd a,(ix-3)\n"
          "\tld (ix-6),a\n", out);
    mir_stream_printf(out,
            "\tld a,(ix%+d)\n\tcp 224\n\tjp z,L%d\n",
            plan->math_stack_offset + 2, subtract);
    mir_stream_puts("\tld a,(ix-5)\n\tadd a,(ix-6)\n\tld b,a\n", out);
    mir_stream_printf(out, "\tld a,(%s%+d)\n\tadd a,b\n",
            state_name, plan->carry_offset);
    mir_stream_puts("\tcp 100\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n\tsub 100\n\tld (ix-7),a\n"
            "\tld a,1\n\tld (%s%+d),a\n\tjp L%d\n"
            "L%d:\n\tld (ix-7),a\n\txor a\n\tld (%s%+d),a\n"
            "\tjp L%d\n",
            add_no_carry, state_name, plan->carry_offset,
            result_ready, add_no_carry,
            state_name, plan->carry_offset, result_ready);
    mir_stream_printf(out, "L%d:\n", subtract);
    mir_stream_printf(out, "\tld a,(%s%+d)\n\tor a\n",
            state_name, plan->carry_offset);
    mir_stream_printf(out, "\tjp nz,L%d\n\tinc (ix-6)\nL%d:\n",
            subtract_have_rhs, subtract_have_rhs);
    mir_stream_puts("\tld a,(ix-5)\n\tld b,(ix-6)\n\tcp b\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n\tsub b\n\tld (ix-7),a\n"
            "\tld a,1\n\tld (%s%+d),a\n\tjp L%d\n"
            "L%d:\n\tadd a,100\n\tsub b\n\tld (ix-7),a\n"
            "\txor a\n\tld (%s%+d),a\n",
            subtract_borrow, state_name, plan->carry_offset,
            result_ready, subtract_borrow,
            state_name, plan->carry_offset);
    mir_stream_printf(out, "L%d:\n", result_ready);
    mir_stream_puts("\tld a,(ix-7)\n\tld b,0\n", out);
    mir_stream_printf(out, "L%d:\n\tcp 10\n\tjp c,L%d\n"
            "\tsub 10\n\tinc b\n\tjp L%d\nL%d:\n"
            "\tsla b\n\tsla b\n\tsla b\n\tsla b\n"
            "\tadd a,b\n\tld (%s%+d),a\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            decimal_loop, decimal_done, decimal_loop,
            decimal_done, state_name, plan->accumulator_offset,
            invalid);
}

static struct Sym *mir_pascal_scan_symbol(
    int instruction, int width, int require_array)
{
    const struct MirInsn *insn = &mir.insns[instruction];
    struct Sym *symbol;

    if ((insn->opcode != MIR_LOAD &&
         insn->opcode != MIR_STORE &&
         insn->opcode != MIR_ADDRESS) ||
        !mir_machine_named_nonvolatile(insn) ||
        (symbol = find_global(insn->name)) == NULL ||
        !symbol->is_defined || symbol->is_volatile ||
        (require_array
             ? (!symbol->is_array || symbol->elem_size != 1 ||
                symbol->array_len < width ||
                symbol->pointee_is_volatile)
             : type_size(symbol->type) != width))
        return NULL;
    return symbol;
}

static int mir_pascal_scan_same_symbol(
    struct Sym *symbol, int instruction)
{
    const struct MirInsn *insn = &mir.insns[instruction];

    return insn->name[0] != 0 &&
           mir_machine_named_nonvolatile(insn) &&
           find_global(insn->name) == symbol;
}

static int mir_pascal_scan_edges(
    const int (*edges)[2], size_t count)
{
    size_t edge;

    for (edge = 0; edge < count; ++edge)
        if (mir.insns[edges[edge][0]].label !=
                mir.insns[edges[edge][1]].label)
            return 0;
    return 1;
}

static int mir_match_pascal_scan_schedule(
    struct MirPascalScanSchedule *plan)
{
    static const int ident_edges[][2] = {
        {13, 25}, {29, 32}, {31, 79}, {43, 47}, {46, 61},
        {51, 55}, {54, 57}, {60, 61}, {63, 79}, {69, 73},
        {72, 79}, {77, 79}, {78, 5}
    };
    static const int number_edges[][2] = {
        {11, 44}, {27, 30}, {29, 44}, {43, 5},
        {48, 57}, {53, 57}, {56, 59}, {61, 66}
    };
    static const int string_edges[][2] = {
        {9, 21}, {17, 21}, {20, 23}, {25, 51},
        {31, 43}, {50, 4}, {55, 60}
    };
    const unsigned char *opcodes;
    size_t opcode_count;
    int expected_blocks;
    int instruction;

    memset(plan, 0, sizeof(*plan));
    if (mir.count == 89) {
        plan->kind = MIR_PASCAL_SCAN_IDENT;
        opcodes = mir_pascal_scan_ident_opcodes;
        opcode_count = sizeof(mir_pascal_scan_ident_opcodes);
        expected_blocks = 16;
    } else if (mir.count == 81) {
        plan->kind = MIR_PASCAL_SCAN_NUMBER;
        opcodes = mir_pascal_scan_number_opcodes;
        opcode_count = sizeof(mir_pascal_scan_number_opcodes);
        expected_blocks = 9;
    } else if (mir.count == 70) {
        plan->kind = MIR_PASCAL_SCAN_STRING;
        opcodes = mir_pascal_scan_string_opcodes;
        opcode_count = sizeof(mir_pascal_scan_string_opcodes);
        expected_blocks = 9;
    } else {
        return 0;
    }
    if ((size_t)mir.count != opcode_count ||
        mir_cfg_block_count() != expected_blocks ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_LOAD ||
             insn->opcode == MIR_STORE ||
             insn->opcode == MIR_ADDRESS) &&
            insn->name[0] != 0 &&
            !mir_machine_named_nonvolatile(insn))
            return mir_machine_reject(
                "pascal-scan-schedule",
                "volatile-named-memory");
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "pascal-scan-schedule",
                "volatile-indirect-memory");
    }

    if (plan->kind == MIR_PASCAL_SCAN_IDENT) {
        if (!mir_pascal_scan_edges(
                ident_edges,
                sizeof(ident_edges) / sizeof(ident_edges[0])) ||
            type_ptr_depth(mir.insns[1].type) != 0 ||
            type_size(mir.insns[1].type) != 2 ||
            !mir_machine_parameter_value_offset(
                mir.insns[1].dst,
                &plan->character_stack_offset))
            return mir_machine_reject(
                "pascal-scan-schedule", "ident-control");
        plan->text = mir_pascal_scan_symbol(14, 16, 1);
        plan->position = mir_pascal_scan_symbol(26, 4, 0);
        plan->length = mir_pascal_scan_symbol(27, 4, 0);
        plan->source = mir_pascal_scan_symbol(33, 2, 0);
        plan->token = mir_pascal_scan_symbol(88, 2, 0);
        plan->first_function =
            mir_interpreter_file_function(22, 0, 1);
        plan->second_function =
            mir_interpreter_file_function(42, 0, 1);
        if (plan->text == NULL || plan->position == NULL ||
            plan->length == NULL || plan->source == NULL ||
            plan->token == NULL)
            return mir_machine_reject(
                "pascal-scan-schedule", "ident-symbols");
        if (plan->first_function == NULL ||
            plan->second_function == NULL)
            return mir_machine_reject(
                "pascal-scan-schedule", "ident-functions");
        if (!mir_pascal_scan_same_symbol(plan->position, 34) ||
            !mir_pascal_scan_same_symbol(plan->position, 65) ||
            !mir_pascal_scan_same_symbol(plan->text, 80) ||
            mir.insns[21].src1 != mir.insns[20].dst ||
            mir.insns[41].src1 != mir.insns[38].dst)
            return mir_machine_reject(
                "pascal-scan-schedule", "ident-relations");
        if (
            !mir_machine_constant_equals(mir.insns[2].dst, 0) ||
            !mir_machine_constant_equals(mir.insns[11].dst, 15) ||
            !mir_machine_constant_equals(mir.insns[16].dst, 1) ||
            !mir_machine_constant_equals(mir.insns[45].dst, 1) ||
            !mir_machine_constant_equals(mir.insns[49].dst, '_') ||
            !mir_machine_constant_equals(mir.insns[76].dst, 1) ||
            !mir_machine_constant_equals(mir.insns[84].dst, 0) ||
            !mir_machine_constant_equals(mir.insns[86].dst, 256))
            return mir_machine_reject(
                "pascal-scan-schedule", "ident-semantics");
        return 1;
    }

    if (plan->kind == MIR_PASCAL_SCAN_NUMBER) {
        if (!mir_pascal_scan_edges(
                number_edges,
                sizeof(number_edges) / sizeof(number_edges[0])) ||
            type_ptr_depth(mir.insns[1].type) != 0 ||
            type_size(mir.insns[1].type) != 2 ||
            !mir_machine_parameter_value_offset(
                mir.insns[1].dst,
                &plan->character_stack_offset))
            return mir_machine_reject(
                "pascal-scan-schedule", "number-control");
        plan->position = mir_pascal_scan_symbol(24, 4, 0);
        plan->length = mir_pascal_scan_symbol(25, 4, 0);
        plan->source = mir_pascal_scan_symbol(31, 2, 0);
        plan->integer = mir_pascal_scan_symbol(70, 2, 0);
        plan->token = mir_pascal_scan_symbol(73, 2, 0);
        plan->text = mir_pascal_scan_symbol(74, 64, 1);
        plan->first_function =
            mir_interpreter_file_function(10, 0, 1);
        plan->second_function =
            mir_interpreter_file_function(80, 1, 2);
        plan->format_string_id = (int)mir.insns[76].immediate;
        if (plan->position == NULL || plan->length == NULL ||
            plan->source == NULL || plan->integer == NULL ||
            plan->token == NULL || plan->text == NULL)
            return mir_machine_reject(
                "pascal-scan-schedule", "number-symbols");
        if (plan->first_function == NULL ||
            plan->second_function == NULL)
            return mir_machine_reject(
                "pascal-scan-schedule", "number-functions");
        if (plan->format_string_id < 0 ||
            !mir_interpreter_file_same_function(
                plan->first_function, 51, 0, 1) ||
            !mir_pascal_scan_same_symbol(plan->position, 32) ||
            !mir_pascal_scan_same_symbol(plan->position, 35) ||
            !mir_pascal_scan_same_symbol(plan->position, 45) ||
            !mir_pascal_scan_same_symbol(plan->position, 62) ||
            !mir_pascal_scan_same_symbol(plan->position, 65) ||
            !mir_pascal_scan_same_symbol(plan->length, 46) ||
            !mir_pascal_scan_same_symbol(plan->integer, 78) ||
            mir.insns[9].src1 != mir.insns[6].dst ||
            mir.insns[50].src1 != mir.insns[6].dst)
            return mir_machine_reject(
                "pascal-scan-schedule", "number-relations");
        if (
            !mir_machine_constant_equals(mir.insns[3].dst, 0) ||
            !mir_machine_constant_equals(mir.insns[14].dst, 10) ||
            !mir_machine_constant_equals(mir.insns[20].dst, '0') ||
            !mir_machine_constant_equals(mir.insns[33].dst, 1) ||
            !mir_machine_constant_equals(mir.insns[55].dst, 1) ||
            !mir_machine_constant_equals(mir.insns[63].dst, 1) ||
            !mir_machine_constant_equals(mir.insns[71].dst, 257))
            return mir_machine_reject(
                "pascal-scan-schedule", "number-semantics");
        snprintf(
            plan->format_name, sizeof(plan->format_name), "%s",
            mir.insns[80].base_name[0] != 0
                ? mir.insns[80].base_name
                : asm_name_for(sym_asm_name(plan->second_function)));
        return plan->format_name[0] != 0;
    }

    if (!mir_pascal_scan_edges(
            string_edges,
            sizeof(string_edges) / sizeof(string_edges[0])))
        return mir_machine_reject(
            "pascal-scan-schedule", "string-control");
    plan->position = mir_pascal_scan_symbol(6, 4, 0);
    plan->length = mir_pascal_scan_symbol(7, 4, 0);
    plan->source = mir_pascal_scan_symbol(10, 2, 0);
    plan->text = mir_pascal_scan_symbol(32, 64, 1);
    plan->token = mir_pascal_scan_symbol(69, 2, 0);
    if (plan->position == NULL || plan->length == NULL ||
        plan->source == NULL || plan->text == NULL ||
        plan->token == NULL ||
        !mir_pascal_scan_same_symbol(plan->position, 11) ||
        !mir_pascal_scan_same_symbol(plan->position, 44) ||
        !mir_pascal_scan_same_symbol(plan->position, 47) ||
        !mir_pascal_scan_same_symbol(plan->position, 52) ||
        !mir_pascal_scan_same_symbol(plan->position, 56) ||
        !mir_pascal_scan_same_symbol(plan->position, 59) ||
        !mir_pascal_scan_same_symbol(plan->length, 53) ||
        !mir_pascal_scan_same_symbol(plan->source, 38) ||
        !mir_pascal_scan_same_symbol(plan->text, 61) ||
        !mir_machine_constant_equals(mir.insns[1].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[14].dst, '\'') ||
        !mir_machine_constant_equals(mir.insns[19].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[29].dst, 63) ||
        !mir_machine_constant_equals(mir.insns[34].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[45].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[57].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[65].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[67].dst, 258))
        return mir_machine_reject(
            "pascal-scan-schedule", "string-semantics");
    return 1;
}

static void mir_pascal_increment_position(
    MirStream *out, struct Sym *position)
{
    mir_machine_emit_global_word(out, position, 0);
    mir_stream_puts("\tinc hl\n", out);
    mir_machine_emit_global_word_store(out, position, 0);
}

static void mir_pascal_text_address(
    MirStream *out, const struct MirPascalScanSchedule *plan,
    int index_offset)
{
    mir_machine_emit_global_address_de(out, plan->text, 0);
    mir_stream_puts("\tex de,hl\n", out);
    if (index_offset != 0)
        mir_stream_printf(out,
                "\tld e,(ix%+d)\n\tld d,0\n\tadd hl,de\n",
                index_offset);
}

static void mir_pascal_source_character(
    MirStream *out, struct Sym *position, struct Sym *source)
{
    mir_machine_emit_global_word(out, position, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, source, 0);
    mir_stream_puts("\tadd hl,de\n\tld a,(hl)\n", out);
}

static void mir_pascal_set_token(
    MirStream *out, const struct MirPascalScanSchedule *plan,
    int token)
{
    mir_stream_printf(out, "\tld hl,%d\n", token);
    mir_machine_emit_global_word_store(out, plan->token, 0);
}

static void mir_emit_pascal_scan_ident(
    MirStream *out, const struct MirPascalScanSchedule *plan)
{
    int loop = new_label();
    int no_store = new_label();
    int done = new_label();
    int accept = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tdec sp\n\tdec sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_puts("\txor a\n\tld (ix-1),a\n", out);
    mir_stream_printf(out,
            "\tld a,(ix%+d)\n\tld (ix-2),a\nL%d:\n"
            "\tld a,(ix-1)\n\tcp 15\n\tjp nc,L%d\n"
            "\tld a,(ix-2)\n",
            plan->character_stack_offset + 2,
            loop, no_store);
    mir_forth_call_character_function(out, plan->first_function);
    mir_stream_puts("\tld a,l\n\tld (ix-2),a\n", out);
    mir_pascal_text_address(out, plan, -1);
    mir_stream_puts("\tld a,(ix-2)\n\tld (hl),a\n\tinc (ix-1)\n", out);
    mir_stream_printf(out, "L%d:\n", no_store);
    mir_machine_emit_global_word(out, plan->position, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", done);
    mir_pascal_source_character(
        out, plan->position, plan->source);
    mir_stream_puts("\tld (ix-2),a\n", out);
    mir_forth_call_character_function(out, plan->second_function);
    mir_stream_printf(out, "\tjp nz,L%d\n", accept);
    mir_stream_puts("\tld a,(ix-2)\n\tcp '_'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n",
            done, accept);
    mir_pascal_increment_position(out, plan->position);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done);
    mir_pascal_text_address(out, plan, -1);
    mir_stream_puts("\tld (hl),0\n", out);
    mir_pascal_set_token(out, plan, 256);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_pascal_scan_number(
    MirStream *out, const struct MirPascalScanSchedule *plan)
{
    int loop = new_label();
    int digits_done = new_label();
    int no_rewind = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-4\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_printf(out,
            "\tld a,(ix%+d)\n\tld (ix-1),a\n"
            "\txor a\n\tld (ix-4),a\n\tld (ix-3),a\n"
            "L%d:\n\tld a,(ix-1)\n",
            plan->character_stack_offset + 2, loop);
    mir_forth_call_character_function(out, plan->first_function);
    mir_stream_printf(out, "\tjp z,L%d\n", digits_done);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tld de,0\n\tld e,l\n\tld d,h\n"
          "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
          "\tadd hl,hl\n\tld a,(ix-1)\n\tsub '0'\n"
          "\tld e,a\n\tld d,0\n\tadd hl,de\n", out);
    mir_interpreter_file_store_ix_word(out, -4);
    mir_machine_emit_global_word(out, plan->position, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", digits_done);
    mir_pascal_source_character(
        out, plan->position, plan->source);
    mir_stream_puts("\tld (ix-1),a\n", out);
    mir_pascal_increment_position(out, plan->position);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, digits_done);
    mir_stream_puts("\tld a,(ix-1)\n", out);
    mir_forth_call_character_function(out, plan->first_function);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_rewind);
    mir_machine_emit_global_word(out, plan->position, 0);
    mir_stream_puts("\tdec hl\n", out);
    mir_machine_emit_global_word_store(out, plan->position, 0);
    mir_stream_printf(out, "L%d:\n", no_rewind);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_machine_emit_global_word_store(out, plan->integer, 0);
    mir_pascal_set_token(out, plan, 257);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
            plan->format_string_id);
    mir_machine_emit_global_address_de(out, plan->text, 0);
    mir_stream_puts("\tpush de\n", out);
    mir_emit_runtime_call(out, plan->format_name);
    mir_interpreter_file_cleanup(out, 3);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_pascal_scan_string(
    MirStream *out, const struct MirPascalScanSchedule *plan)
{
    int loop = new_label();
    int no_store = new_label();
    int done = new_label();
    int no_quote = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tdec sp\n\tdec sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_puts("\txor a\n\tld (ix-1),a\n", out);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_machine_emit_global_word(out, plan->position, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", done);
    mir_pascal_source_character(
        out, plan->position, plan->source);
    mir_stream_puts("\tcp 39\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", done);
    mir_stream_puts("\tld (ix-2),a\n\tld a,(ix-1)\n\tcp 63\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", no_store);
    mir_pascal_text_address(out, plan, -1);
    mir_stream_puts("\tld a,(ix-2)\n\tld (hl),a\n\tinc (ix-1)\n", out);
    mir_stream_printf(out, "L%d:\n", no_store);
    mir_pascal_increment_position(out, plan->position);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done);
    mir_machine_emit_global_word(out, plan->position, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", no_quote);
    mir_pascal_increment_position(out, plan->position);
    mir_stream_printf(out, "L%d:\n", no_quote);
    mir_pascal_text_address(out, plan, -1);
    mir_stream_puts("\tld (hl),0\n", out);
    mir_pascal_set_token(out, plan, 258);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_pascal_scan_schedule(
    MirStream *out, const struct MirPascalScanSchedule *plan)
{
    if (plan->kind == MIR_PASCAL_SCAN_IDENT)
        mir_emit_pascal_scan_ident(out, plan);
    else if (plan->kind == MIR_PASCAL_SCAN_NUMBER)
        mir_emit_pascal_scan_number(out, plan);
    else
        mir_emit_pascal_scan_string(out, plan);
}

static int mir_match_pascal_next_schedule(
    struct MirPascalNextSchedule *plan)
{
    static const int edges[][2] = {
        {17, 23}, {37, 41}, {40, 55}, {45, 49}, {48, 51},
        {54, 55}, {57, 63}, {67, 73}, {77, 81}, {85, 93},
        {89, 93}, {92, 95}, {97, 109}, {105, 109},
        {108, 111}, {113, 128}, {132, 140}, {136, 140},
        {139, 142}, {144, 156}, {152, 156}, {155, 158},
        {160, 175}, {179, 187}, {183, 187}, {186, 189},
        {191, 203}, {199, 203}, {202, 205}, {207, 222},
        {226, 234}, {230, 234}, {233, 236}, {238, 250},
        {246, 250}, {249, 252}, {254, 269}, {273, 281},
        {277, 281}, {280, 283}, {285, 297}, {293, 297},
        {296, 299}, {301, 316}
    };
    static const int copy_calls[] = {125, 172, 219, 266, 313};
    static const int string_insns[] = {123, 170, 217, 264, 311};
    int instruction;
    size_t item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 332 || mir_cfg_block_count() != 47 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID ||
        !mir_pascal_scan_edges(
            edges, sizeof(edges) / sizeof(edges[0])))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != mir_pascal_next_opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_LOAD ||
             insn->opcode == MIR_STORE ||
             insn->opcode == MIR_ADDRESS) &&
            insn->name[0] != 0 &&
            !mir_machine_named_nonvolatile(insn))
            return mir_machine_reject(
                "pascal-next-schedule",
                "volatile-named-memory");
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "pascal-next-schedule",
                "volatile-indirect-memory");
    }
    plan->text = mir_pascal_scan_symbol(2, 64, 1);
    plan->token = mir_pascal_scan_symbol(10, 2, 0);
    plan->integer = mir_pascal_scan_symbol(13, 2, 0);
    plan->position = mir_pascal_scan_symbol(14, 4, 0);
    plan->length = mir_pascal_scan_symbol(15, 4, 0);
    plan->source = mir_pascal_scan_symbol(24, 2, 0);
    if (plan->text == NULL || plan->token == NULL ||
        plan->integer == NULL || plan->position == NULL ||
        plan->length == NULL || plan->source == NULL)
        return mir_machine_reject(
            "pascal-next-schedule", "symbols");
    plan->skip_function =
        mir_interpreter_file_function(1, 0, 0);
    plan->alpha_function =
        mir_interpreter_file_function(36, 0, 1);
    plan->ident_function =
        mir_interpreter_file_function(60, 0, 1);
    plan->digit_function =
        mir_interpreter_file_function(66, 0, 1);
    plan->number_function =
        mir_interpreter_file_function(70, 0, 1);
    plan->string_function =
        mir_interpreter_file_function(78, 0, 0);
    plan->copy_function =
        mir_interpreter_file_function(copy_calls[0], 0, 2);
    if (plan->skip_function == NULL ||
        plan->alpha_function == NULL ||
        plan->ident_function == NULL ||
        plan->digit_function == NULL ||
        plan->number_function == NULL ||
        plan->string_function == NULL ||
        plan->copy_function == NULL)
        return mir_machine_reject(
            "pascal-next-schedule", "functions");
    for (item = 1;
         item < sizeof(copy_calls) / sizeof(copy_calls[0]);
         ++item)
        if (!mir_interpreter_file_same_function(
                plan->copy_function, copy_calls[item], 0, 2))
            return mir_machine_reject(
                "pascal-next-schedule", "copy-functions");
    for (item = 0;
         item < sizeof(string_insns) / sizeof(string_insns[0]);
         ++item) {
        if (mir.insns[string_insns[item]].immediate < 0)
            return mir_machine_reject(
                "pascal-next-schedule", "strings");
        plan->strings[item] =
            (int)mir.insns[string_insns[item]].immediate;
    }
    if (!mir_pascal_scan_same_symbol(plan->token, 20) ||
        !mir_pascal_scan_same_symbol(plan->token, 120) ||
        !mir_pascal_scan_same_symbol(plan->token, 167) ||
        !mir_pascal_scan_same_symbol(plan->token, 214) ||
        !mir_pascal_scan_same_symbol(plan->token, 261) ||
        !mir_pascal_scan_same_symbol(plan->token, 308) ||
        !mir_pascal_scan_same_symbol(plan->token, 319) ||
        !mir_pascal_scan_same_symbol(plan->position, 25) ||
        !mir_pascal_scan_same_symbol(plan->position, 28) ||
        !mir_pascal_scan_same_symbol(plan->position, 86) ||
        !mir_pascal_scan_same_symbol(plan->position, 114) ||
        !mir_pascal_scan_same_symbol(plan->position, 117) ||
        !mir_pascal_scan_same_symbol(plan->length, 87) ||
        !mir_pascal_scan_same_symbol(plan->source, 98) ||
        !mir_pascal_scan_same_symbol(plan->text, 121) ||
        mir.insns[35].src1 != mir.insns[32].dst ||
        mir.insns[59].src1 != mir.insns[32].dst ||
        mir.insns[65].src1 != mir.insns[64].dst ||
        mir.insns[69].src1 != mir.insns[68].dst)
        return mir_machine_reject(
            "pascal-next-schedule", "relations");
    if (!mir_machine_constant_equals(mir.insns[3].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[6].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[8].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[11].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[18].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[26].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[43].dst, '_') ||
        !mir_machine_constant_equals(mir.insns[75].dst, '\'') ||
        !mir_machine_constant_equals(mir.insns[83].dst, ':') ||
        !mir_machine_constant_equals(mir.insns[102].dst, '=') ||
        !mir_machine_constant_equals(mir.insns[115].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[118].dst, 300) ||
        !mir_machine_constant_equals(mir.insns[130].dst, '<') ||
        !mir_machine_constant_equals(mir.insns[149].dst, '>') ||
        !mir_machine_constant_equals(mir.insns[162].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[165].dst, 301) ||
        !mir_machine_constant_equals(mir.insns[177].dst, '<') ||
        !mir_machine_constant_equals(mir.insns[196].dst, '=') ||
        !mir_machine_constant_equals(mir.insns[209].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[212].dst, 302) ||
        !mir_machine_constant_equals(mir.insns[224].dst, '>') ||
        !mir_machine_constant_equals(mir.insns[243].dst, '=') ||
        !mir_machine_constant_equals(mir.insns[256].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[259].dst, 303) ||
        !mir_machine_constant_equals(mir.insns[271].dst, '.') ||
        !mir_machine_constant_equals(mir.insns[290].dst, '.') ||
        !mir_machine_constant_equals(mir.insns[303].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[306].dst, 304) ||
        !mir_machine_constant_equals(mir.insns[321].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[327].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[330].dst, 0))
        return mir_machine_reject(
            "pascal-next-schedule", "constants");
    return 1;
}

static void mir_pascal_next_call_character(
    MirStream *out, struct Sym *function)
{
    mir_stream_puts("\tld l,(ix-1)\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, function);
    mir_stream_puts("\tpop bc\n", out);
}

static void mir_emit_pascal_next_schedule(
    MirStream *out, const struct MirPascalNextSchedule *plan)
{
    int identifiers = new_label();
    int numbers = new_label();
    int strings = new_label();
    int default_token = new_label();
    int epilogue = new_label();
    int colon = new_label();
    int less = new_label();
    int less_equal = new_label();
    int greater = new_label();
    int dot = new_label();
    int store_pair = new_label();
    int match_second = new_label();
    int no_match = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tdec sp\n\tdec sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_symbol_call(out, plan->skip_function);
    mir_machine_emit_global_address_de(out, plan->text, 0);
    mir_stream_puts("\txor a\n\tld (de),a\n\tld hl,0\n", out);
    mir_machine_emit_global_word_store(out, plan->token, 0);
    mir_machine_emit_global_word_store(out, plan->integer, 0);
    mir_machine_emit_global_word(out, plan->position, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", epilogue);
    mir_pascal_source_character(
        out, plan->position, plan->source);
    mir_stream_puts("\tld (ix-1),a\n", out);
    mir_pascal_increment_position(out, plan->position);
    mir_stream_puts("\tld a,(ix-1)\n", out);
    mir_forth_call_character_function(out, plan->alpha_function);
    mir_stream_printf(out, "\tjp nz,L%d\n", identifiers);
    mir_stream_puts("\tld a,(ix-1)\n\tcp '_'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", identifiers);
    mir_stream_puts("\tld a,(ix-1)\n", out);
    mir_forth_call_character_function(out, plan->digit_function);
    mir_stream_printf(out, "\tjp nz,L%d\n", numbers);
    mir_stream_puts("\tld a,(ix-1)\n\tcp 39\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", strings);

    mir_stream_puts("\tld a,(ix-1)\n\tcp ':'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tcp '<'\n\tjp z,L%d\n"
            "\tcp '>'\n\tjp z,L%d\n\tcp '.'\n\tjp z,L%d\n"
            "\tjp L%d\n",
            colon, less, greater, dot, default_token);
    mir_stream_printf(out, "L%d:\n\tld a,'='\n\tcall L%d\n"
            "\tjp nz,L%d\n\tld hl,300\n\tld de,S%d\n"
            "\tjp L%d\n",
            colon, match_second, default_token,
            plan->strings[0], store_pair);
    mir_stream_printf(out, "L%d:\n\tld a,'>'\n\tcall L%d\n"
            "\tjp nz,L%d\n\tld hl,301\n\tld de,S%d\n"
            "\tjp L%d\n",
            less, match_second, less_equal,
            plan->strings[1], store_pair);
    mir_stream_printf(out, "L%d:\n\tld a,'='\n\tcall L%d\n"
            "\tjp nz,L%d\n\tld hl,302\n\tld de,S%d\n"
            "\tjp L%d\n",
            less_equal, match_second, default_token,
            plan->strings[2], store_pair);
    mir_stream_printf(out, "L%d:\n\tld a,'='\n\tcall L%d\n"
            "\tjp nz,L%d\n\tld hl,303\n\tld de,S%d\n"
            "\tjp L%d\n",
            greater, match_second, default_token,
            plan->strings[3], store_pair);
    mir_stream_printf(out, "L%d:\n\tld a,'.'\n\tcall L%d\n"
            "\tjp nz,L%d\n\tld hl,304\n\tld de,S%d\n"
            "\tjp L%d\n",
            dot, match_second, default_token,
            plan->strings[4], store_pair);
    mir_stream_printf(out, "L%d:\n\tpush de\n", store_pair);
    mir_machine_emit_global_word_store(out, plan->token, 0);
    mir_machine_emit_global_address_de(out, plan->text, 0);
    mir_stream_puts("\tpush de\n", out);
    mir_machine_emit_symbol_call(out, plan->copy_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_stream_printf(out, "\tjp L%d\n", epilogue);
    mir_stream_printf(out, "L%d:\n", identifiers);
    mir_pascal_next_call_character(out, plan->ident_function);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", epilogue, numbers);
    mir_pascal_next_call_character(out, plan->number_function);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", epilogue, strings);
    mir_machine_emit_symbol_call(out, plan->string_function);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", epilogue, default_token);
    mir_stream_puts("\tld l,(ix-1)\n\tld h,0\n", out);
    mir_machine_emit_global_word_store(out, plan->token, 0);
    mir_machine_emit_global_address_de(out, plan->text, 0);
    mir_stream_puts("\tld a,(ix-1)\n\tld (de),a\n\tinc de\n\txor a\n"
          "\tld (de),a\n", out);
    mir_stream_printf(out,
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            epilogue);
    mir_stream_printf(out, "L%d:\n\tpush af\n", match_second);
    mir_machine_emit_global_word(out, plan->position, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tjp c,L%d\n", no_match, no_match);
    mir_machine_emit_global_word(out, plan->source, 0);
    mir_stream_puts("\tadd hl,de\n\tpop bc\n\tld a,(hl)\n\tcp b\n"
          "\tret nz\n", out);
    mir_machine_emit_global_word(out, plan->position, 0);
    mir_stream_puts("\tinc hl\n", out);
    mir_machine_emit_global_word_store(out, plan->position, 0);
    mir_stream_puts("\txor a\n\tret\n", out);
    mir_stream_printf(out, "L%d:\n\tpop af\n\tor 1\n\tret\n", no_match);
}

static int mir_match_pascal_predicate_schedule(
    struct MirPascalPredicateSchedule *plan)
{
    static const int need_edges[][2] = {
        {6, 10}, {9, 36}, {12, 22}, {18, 22}, {21, 24},
        {26, 30}, {29, 32}, {35, 36}, {38, 42}
    };
    static const int accept_edges[][2] = {
        {6, 37}, {9, 13}, {12, 31}, {21, 25}, {24, 27},
        {30, 31}, {33, 37}, {36, 39}, {41, 46}
    };
    static const int word_edges[][2] = {
        {6, 18}, {14, 18}, {17, 20}
    };
    const unsigned char *opcodes;
    const int (*edges)[2];
    size_t opcode_count;
    size_t edge_count;
    int compare_instruction;
    int instruction;

    memset(plan, 0, sizeof(*plan));
    if (mir.count == 44) {
        plan->kind = MIR_PASCAL_PREDICATE_NEED;
        opcodes = mir_pascal_need_opcodes;
        opcode_count = sizeof(mir_pascal_need_opcodes);
        edges = need_edges;
        edge_count = sizeof(need_edges) / sizeof(need_edges[0]);
        compare_instruction = 17;
    } else if (mir.count == 49) {
        plan->kind = MIR_PASCAL_PREDICATE_ACCEPT;
        opcodes = mir_pascal_acc_opcodes;
        opcode_count = sizeof(mir_pascal_acc_opcodes);
        edges = accept_edges;
        edge_count = sizeof(accept_edges) / sizeof(accept_edges[0]);
        compare_instruction = 18;
    } else if (mir.count == 23) {
        plan->kind = MIR_PASCAL_PREDICATE_WORD;
        opcodes = mir_pascal_isword_opcodes;
        opcode_count = sizeof(mir_pascal_isword_opcodes);
        edges = word_edges;
        edge_count = sizeof(word_edges) / sizeof(word_edges[0]);
        compare_instruction = 11;
    } else {
        return 0;
    }
    if ((size_t)mir.count != opcode_count ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        !mir_pascal_scan_edges(edges, edge_count))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode != opcodes[instruction])
            return 0;
    if (!mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->token_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst, &plan->string_stack_offset) ||
        plan->string_stack_offset != plan->token_stack_offset + 2 ||
        type_size(mir.insns[1].type) != 2 ||
        type_ptr_depth(mir.insns[2].type) != 1 ||
        type_size(mir.insns[2].type) != 2 ||
        mir_machine_pointee_is_volatile(&mir.insns[2]))
        return mir_machine_reject(
            "pascal-predicate-schedule", "parameters");
    plan->compare_function =
        mir_interpreter_file_function(
            compare_instruction, 0, 2);
    if (plan->compare_function == NULL)
        return mir_machine_reject(
            "pascal-predicate-schedule", "compare");
    if (plan->kind == MIR_PASCAL_PREDICATE_WORD) {
        plan->text = mir_pascal_scan_symbol(7, 64, 1);
        if (plan->text == NULL ||
            !mir_machine_constant_equals(mir.insns[4].dst, 256) ||
            !mir_machine_constant_equals(mir.insns[12].dst, 0) ||
            mir.insns[8].src1 != mir.insns[7].dst ||
            mir.insns[10].src1 != mir.insns[9].dst)
            return mir_machine_reject(
                "pascal-predicate-schedule", "word");
        return 1;
    }
    plan->token = mir_pascal_scan_symbol(3, 2, 0);
    plan->text = mir_pascal_scan_symbol(
        plan->kind == MIR_PASCAL_PREDICATE_NEED ? 13 : 14,
        64, 1);
    plan->next_function =
        mir_interpreter_file_function(
            plan->kind == MIR_PASCAL_PREDICATE_NEED ? 43 : 42,
            0, 0);
    if (plan->token == NULL || plan->text == NULL ||
        plan->next_function == NULL)
        return mir_machine_reject(
            "pascal-predicate-schedule", "state");
    if (plan->kind == MIR_PASCAL_PREDICATE_NEED) {
        plan->failure_function =
            mir_interpreter_file_function(41, 0, 1);
        plan->failure_string_id = (int)mir.insns[39].immediate;
        if (plan->failure_function == NULL ||
            plan->failure_string_id < 0)
            return mir_machine_reject(
                "pascal-predicate-schedule", "failure");
    }
    return 1;
}

static void mir_pascal_predicate_compare(
    MirStream *out, const struct MirPascalPredicateSchedule *plan)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            plan->string_stack_offset + 2,
            plan->string_stack_offset + 3);
    mir_machine_emit_global_address_de(out, plan->text, 0);
    mir_stream_puts("\tpush de\n", out);
    mir_machine_emit_symbol_call(out, plan->compare_function);
    mir_interpreter_file_cleanup(out, 2);
}

static void mir_emit_pascal_predicate_schedule(
    MirStream *out, const struct MirPascalPredicateSchedule *plan)
{
    int failed = new_label();
    int success = new_label();
    int done = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    if (plan->kind == MIR_PASCAL_PREDICATE_WORD) {
        mir_stream_printf(out,
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
                "\tld de,256\n\tor a\n\tsbc hl,de\n"
                "\tjp nz,L%d\n",
                plan->token_stack_offset + 2,
                plan->token_stack_offset + 3, failed);
        mir_pascal_predicate_compare(out, plan);
        mir_stream_puts("\tld a,h\n\tor l\n", out);
        mir_stream_printf(out,
                "\tjp nz,L%d\n\tld hl,1\n\tjp L%d\n"
                "L%d:\n\tld hl,0\nL%d:\n"
                "\tpop ix\n\tret\n",
                failed, done, failed, done);
        return;
    }
    mir_machine_emit_global_word(out, plan->token, 0);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tor a\n\tsbc hl,de\n",
            plan->token_stack_offset + 2,
            plan->token_stack_offset + 3);
    mir_stream_printf(out, "\tjp nz,L%d\n", failed);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld a,h\n\tor l\n\tjp z,L%d\n",
            plan->string_stack_offset + 2,
            plan->string_stack_offset + 3, success);
    mir_pascal_predicate_compare(out, plan);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n", failed, success);
    mir_machine_emit_symbol_call(out, plan->next_function);
    if (plan->kind == MIR_PASCAL_PREDICATE_ACCEPT) {
        mir_stream_puts("\tld hl,1\n\tpop ix\n\tret\n", out);
        mir_stream_printf(out, "L%d:\n\tld hl,0\n\tpop ix\n\tret\n",
                failed);
        return;
    }
    mir_stream_printf(out, "\tpop ix\n\tret\nL%d:\n", failed);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
            plan->failure_string_id);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n\tpop ix\n\tret\n", out);
}

static int mir_match_pascal_emit_schedule(
    struct MirPascalEmitSchedule *plan)
{
    int instruction;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 36 || mir_cfg_block_count() != 2 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        type_size(mir.return_type) != 2 ||
        mir.insns[7].label != mir.insns[11].label)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
                mir_pascal_emit_opcodes[instruction])
            return 0;
    if (!mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->opcode_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst, &plan->first_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[3].dst, &plan->second_stack_offset) ||
        plan->first_stack_offset != plan->opcode_stack_offset + 2 ||
        plan->second_stack_offset != plan->first_stack_offset + 2)
        return mir_machine_reject(
            "pascal-emit-schedule", "parameters");
    plan->code_position = mir_pascal_scan_symbol(4, 2, 0);
    plan->code_limit = mir_pascal_scan_symbol(5, 2, 0);
    plan->code = mir_pascal_scan_symbol(12, 2, 0);
    plan->failure_function =
        mir_interpreter_file_function(10, 0, 1);
    plan->failure_string_id = (int)mir.insns[8].immediate;
    plan->stride = (int)mir.insns[14].immediate;
    plan->opcode_offset = (int)mir.insns[15].immediate;
    plan->first_offset = (int)mir.insns[22].immediate;
    plan->second_offset = (int)mir.insns[28].immediate;
    if (plan->code_position == NULL || plan->code_limit == NULL ||
        plan->code == NULL || plan->failure_function == NULL ||
        plan->failure_string_id < 0 ||
        plan->stride <= 0 || plan->stride > 16 ||
        plan->opcode_offset < 0 ||
        plan->first_offset < 0 || plan->second_offset < 0 ||
        plan->opcode_offset >= plan->stride ||
        plan->first_offset + 1 >= plan->stride ||
        plan->second_offset + 1 >= plan->stride ||
        !mir_pascal_scan_same_symbol(plan->code, 19) ||
        !mir_pascal_scan_same_symbol(plan->code, 25) ||
        !mir_pascal_scan_same_symbol(plan->code_position, 13) ||
        !mir_pascal_scan_same_symbol(plan->code_position, 20) ||
        !mir_pascal_scan_same_symbol(plan->code_position, 26) ||
        !mir_pascal_scan_same_symbol(plan->code_position, 31) ||
        !mir_pascal_scan_same_symbol(plan->code_position, 34) ||
        !mir_machine_constant_equals(mir.insns[32].dst, 1))
        return mir_machine_reject(
            "pascal-emit-schedule", "semantics");
    return 1;
}

static void mir_pascal_emit_record_address(
    MirStream *out, const struct MirPascalEmitSchedule *plan)
{
    mir_machine_emit_global_word(out, plan->code_position, 0);
    if (plan->stride == 5)
        mir_stream_puts("\tld e,l\n\tld d,h\n\tadd hl,hl\n"
              "\tadd hl,hl\n\tadd hl,de\n", out);
    else
        mir_stream_printf(out, "\tld de,%d\n\tcall __mulu\n",
                plan->stride);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->code, 0);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_emit_pascal_emit_schedule(
    MirStream *out, const struct MirPascalEmitSchedule *plan)
{
    int room = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_global_word(out, plan->code_position, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->code_limit, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tld hl,S%d\n\tpush hl\n",
            room, plan->failure_string_id);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", room);
    mir_pascal_emit_record_address(out, plan);
    if (plan->opcode_offset != 0)
        mir_machine_emit_hl_offset(
            out, plan->opcode_offset, 0);
    mir_stream_printf(out, "\tld a,(ix%+d)\n\tld (hl),a\n",
            plan->opcode_stack_offset + 2);
    mir_pascal_emit_record_address(out, plan);
    mir_machine_emit_hl_offset(out, plan->first_offset, 0);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
            plan->first_stack_offset + 2,
            plan->first_stack_offset + 3);
    mir_pascal_emit_record_address(out, plan);
    mir_machine_emit_hl_offset(out, plan->second_offset, 0);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
            plan->second_stack_offset + 2,
            plan->second_stack_offset + 3);
    mir_machine_emit_global_word(out, plan->code_position, 0);
    mir_stream_puts("\tpush hl\n\tinc hl\n", out);
    mir_machine_emit_global_word_store(out, plan->code_position, 0);
    mir_stream_puts("\tpop hl\n\tpop ix\n\tret\n", out);
}

static int mir_match_ada_whitespace_schedule(
    struct MirAdaWhitespaceSchedule *plan)
{
    static const int edges[][2] = {
        {10, 27}, {23, 27}, {26, 29}, {31, 60}, {43, 50},
        {59, 2}, {71, 87}, {83, 87}, {86, 89}, {91, 110},
        {106, 110}, {109, 112}, {114, 156}, {123, 139},
        {135, 139}, {138, 141}, {143, 1}, {151, 115},
        {154, 1}, {164, 177}, {176, 180}, {187, 236},
        {197, 213}, {209, 213}, {212, 215}, {217, 1},
        {225, 188}, {228, 1}, {232, 236}, {235, 1}
    };
    static const int position_members[] = {
        4, 15, 36, 52, 62, 76, 96, 117,
        128, 145, 158, 169, 191, 202, 219
    };
    static const int length_members[] = {7, 68, 120, 161, 194};
    static const int source_members[] = {
        12, 33, 73, 93, 125, 166, 199
    };
    static const int line_members[] = {45};
    struct MirForthLexerSchedule common;
    int instruction;

    memset(plan, 0, sizeof(*plan));
    memset(&common, 0, sizeof(common));
    if (mir.count != 237 || mir_cfg_block_count() != 35 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID ||
        !mir_pascal_scan_edges(
            edges, sizeof(edges) / sizeof(edges[0])))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != mir_ada_whitespace_opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "ada-whitespace-schedule",
                "volatile-memory");
    }
    common.state = find_global(mir.insns[3].name);
    if (common.state == NULL || !common.state->is_defined ||
        common.state->is_volatile ||
        common.state->pointee_is_volatile ||
        type_ptr_depth(mir.insns[3].type) == 0 ||
        !mir_forth_lexer_member_group(
            &common, position_members,
            sizeof(position_members) / sizeof(position_members[0]),
            4, &plan->position_offset) ||
        !mir_forth_lexer_member_group(
            &common, length_members,
            sizeof(length_members) / sizeof(length_members[0]),
            4, &plan->length_offset) ||
        !mir_forth_lexer_member_group(
            &common, source_members,
            sizeof(source_members) / sizeof(source_members[0]),
            2, &plan->source_offset) ||
        !mir_forth_lexer_member_group(
            &common, line_members,
            sizeof(line_members) / sizeof(line_members[0]),
            2, &plan->line_offset))
        return mir_machine_reject(
            "ada-whitespace-schedule", "state");
    plan->state = common.state;
    plan->space_function =
        mir_interpreter_file_function(22, 0, 1);
    if (plan->space_function == NULL ||
        mir.insns[21].src1 != mir.insns[20].dst ||
        !mir_machine_constant_equals(mir.insns[40].dst, '\n') ||
        !mir_machine_constant_equals(mir.insns[47].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[54].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[65].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[80].dst, '-') ||
        !mir_machine_constant_equals(mir.insns[99].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[103].dst, '-') ||
        !mir_machine_constant_equals(mir.insns[132].dst, '\n') ||
        !mir_machine_constant_equals(mir.insns[147].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[178].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[185].dst, '@') ||
        !mir_machine_constant_equals(mir.insns[206].dst, '\n') ||
        !mir_machine_constant_equals(mir.insns[221].dst, 1))
        return mir_machine_reject(
            "ada-whitespace-schedule", "semantics");
    return 1;
}

static void mir_emit_ada_whitespace_schedule(
    MirStream *out, const struct MirAdaWhitespaceSchedule *plan)
{
    struct MirForthLexerSchedule common;
    int outer = new_label();
    int spaces = new_label();
    int spaces_done = new_label();
    int not_newline = new_label();
    int no_dash_comment = new_label();
    int dash_loop = new_label();
    int no_at_comment = new_label();
    int at_loop = new_label();
    int done = new_label();

    memset(&common, 0, sizeof(common));
    common.state = plan->state;
    common.position_offset = plan->position_offset;
    common.length_offset = plan->length_offset;
    common.source_offset = plan->source_offset;
    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n\tpush iy\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_global_word(out, plan->state, 0);
    mir_stream_puts("\tpush hl\n\tpop iy\n", out);
    mir_stream_printf(out, "L%d:\nL%d:\n", outer, spaces);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            spaces_done);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tpush af\n\tld l,a\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->space_function);
    mir_stream_puts("\tpop bc\n\tpop af\n\tld b,a\n\tld a,h\n\tor l\n",
          out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld a,b\n\tcp 10\n\tjp nz,L%d\n",
            spaces_done, not_newline);
    mir_forth_load_iy_word(out, plan->line_offset);
    mir_stream_puts("\tinc hl\n", out);
    mir_forth_store_iy_word(out, plan->line_offset);
    mir_stream_printf(out, "L%d:\n", not_newline);
    mir_forth_increment_position(out, &common);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", spaces, spaces_done);

    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_puts("\tinc hl\n", out);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            no_dash_comment);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tcp '-'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_dash_comment);
    mir_forth_load_iy_word(out, plan->source_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tinc de\n\tadd hl,de\n\tld a,(hl)\n",
            plan->position_offset, plan->position_offset + 1);
    mir_stream_puts("\tcp '-'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_dash_comment);
    mir_stream_printf(out, "L%d:\n", dash_loop);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            outer);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tcp 10\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", outer);
    mir_forth_increment_position(out, &common);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n",
            dash_loop, no_dash_comment);

    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            done);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tcp '@'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n",
            no_at_comment, at_loop);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            outer);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tcp 10\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", outer);
    mir_forth_increment_position(out, &common);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n\tpop iy\n\tret\n"
            "L%d:\n\tpop iy\n\tret\n",
            at_loop, done, no_at_comment);
}

static int mir_match_ada_lexer_schedule(
    struct MirAdaLexerSchedule *plan)
{
    static const int edges[][2] = {
        {24, 26}, {44, 48}, {47, 62}, {52, 56}, {55, 58},
        {61, 62}, {64, 229}, {74, 78}, {77, 92}, {82, 86},
        {85, 88}, {91, 92}, {94, 175}, {100, 113},
        {121, 124}, {123, 175}, {139, 143}, {142, 157},
        {147, 151}, {150, 153}, {156, 157}, {159, 175},
        {167, 171}, {170, 175}, {174, 68}, {191, 202},
        {211, 222}, {233, 319}, {244, 285}, {264, 267},
        {266, 285}, {284, 237}, {290, 297}, {323, 422},
        {338, 354}, {350, 354}, {353, 356}, {358, 393},
        {378, 389}, {392, 327}, {401, 408}, {426, 438},
        {434, 438}, {437, 440}, {442, 458}, {454, 458},
        {457, 460}, {462, 481}, {485, 497}, {493, 497},
        {496, 499}, {501, 517}, {513, 517}, {516, 519},
        {521, 540}, {544, 556}, {552, 556}, {555, 558},
        {560, 576}, {572, 576}, {575, 578}, {580, 599},
        {603, 615}, {611, 615}, {614, 617}, {619, 635},
        {631, 635}, {634, 637}, {639, 658}, {662, 674},
        {670, 674}, {673, 676}, {678, 694}, {690, 694},
        {693, 696}, {698, 717}
    };
    static const int text_members[] = {
        3, 102, 177, 184, 204, 308, 380, 410,
        474, 533, 592, 651, 710, 723, 730
    };
    static const int token_members[] = {
        10, 193, 213, 224, 299, 417,
        470, 529, 588, 647, 706, 719
    };
    static const int integer_members[] = {14, 197, 217, 303, 313};
    static const int position_members[] = {
        18, 31, 115, 129, 162, 258, 272, 292, 332, 343,
        363, 395, 403, 428, 447, 464, 487, 506, 523,
        546, 565, 582, 605, 624, 641, 664, 683, 700
    };
    static const int length_members[] = {
        21, 118, 261, 335, 398, 431, 490, 549, 608, 667
    };
    static const int source_members[] = {
        28, 126, 269, 340, 360, 444, 503, 562, 621, 680
    };
    static const int copy_calls[] = {478, 537, 596, 655, 714};
    static const int string_insns[] = {
        186, 206, 310, 476, 535, 594, 653, 712
    };
    struct MirForthLexerSchedule common;
    int instruction;
    size_t item;

    memset(plan, 0, sizeof(*plan));
    memset(&common, 0, sizeof(common));
    if (mir.count != 736 || mir_cfg_block_count() != 84 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID ||
        !mir_pascal_scan_edges(
            edges, sizeof(edges) / sizeof(edges[0])))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != mir_ada_lexer_opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "ada-lexer-schedule", "volatile-memory");
    }
    common.state = find_global(mir.insns[2].name);
    if (common.state == NULL || !common.state->is_defined ||
        common.state->is_volatile ||
        common.state->pointee_is_volatile)
        return mir_machine_reject(
            "ada-lexer-schedule", "state-root");
    if (!mir_forth_lexer_member_group(
            &common, source_members,
            sizeof(source_members) / sizeof(source_members[0]),
            2, &plan->source_offset))
        return mir_machine_reject(
            "ada-lexer-schedule", "source-members");
    if (!mir_forth_lexer_member_group(
            &common, length_members,
            sizeof(length_members) / sizeof(length_members[0]),
            4, &plan->length_offset))
        return mir_machine_reject(
            "ada-lexer-schedule", "length-members");
    if (!mir_forth_lexer_member_group(
            &common, position_members,
            sizeof(position_members) / sizeof(position_members[0]),
            4, &plan->position_offset))
        return mir_machine_reject(
            "ada-lexer-schedule", "position-members");
    if (!mir_forth_lexer_member_group(
            &common, token_members,
            sizeof(token_members) / sizeof(token_members[0]),
            2, &plan->token_offset))
        return mir_machine_reject(
            "ada-lexer-schedule", "token-members");
    if (!mir_forth_lexer_member_group(
            &common, integer_members,
            sizeof(integer_members) / sizeof(integer_members[0]),
            2, &plan->integer_offset))
        return mir_machine_reject(
            "ada-lexer-schedule", "integer-members");
    if (!mir_forth_lexer_member_group(
            &common, text_members,
            sizeof(text_members) / sizeof(text_members[0]),
        80, &plan->text_offset))
        return mir_machine_reject(
            "ada-lexer-schedule", "text-members");
    plan->state = common.state;
    plan->skip_function =
        mir_interpreter_file_function(1, 0, 0);
    plan->alpha_function =
        mir_interpreter_file_function(43, 0, 1);
    plan->alnum_function =
        mir_interpreter_file_function(73, 0, 1);
    plan->lower_function =
        mir_interpreter_file_function(110, 0, 1);
    plan->compare_function =
        mir_interpreter_file_function(188, 0, 2);
    plan->digit_function =
        mir_interpreter_file_function(232, 0, 1);
    plan->format_function = find_global(mir.insns[316].name);
    plan->copy_function =
        mir_interpreter_file_function(copy_calls[0], 0, 2);
    if (plan->skip_function == NULL ||
        plan->alpha_function == NULL ||
        plan->alnum_function == NULL ||
        plan->lower_function == NULL ||
        plan->compare_function == NULL ||
        plan->digit_function == NULL ||
        plan->format_function == NULL ||
        plan->copy_function == NULL ||
        !mir_interpreter_file_same_function(
            plan->alnum_function, 138, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->compare_function, 208, 0, 2) ||
        !mir_interpreter_file_same_function(
            plan->digit_function, 243, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->digit_function, 288, 0, 1))
        return mir_machine_reject(
            "ada-lexer-schedule", "functions");
    for (item = 1;
         item < sizeof(copy_calls) / sizeof(copy_calls[0]);
         ++item)
        if (!mir_interpreter_file_same_function(
                plan->copy_function, copy_calls[item], 0, 2))
            return mir_machine_reject(
                "ada-lexer-schedule", "copy-functions");
    for (item = 0;
         item < sizeof(string_insns) / sizeof(string_insns[0]);
         ++item) {
        if (mir.insns[string_insns[item]].immediate < 0)
            return mir_machine_reject(
                "ada-lexer-schedule", "strings");
        plan->strings[item] =
            (int)mir.insns[string_insns[item]].immediate;
    }
    snprintf(
        plan->format_name, sizeof(plan->format_name), "%s",
        mir.insns[316].base_name[0] != 0
            ? mir.insns[316].base_name
            : asm_name_for(sym_asm_name(plan->format_function)));
    if (plan->format_name[0] == 0 ||
        !mir_machine_constant_equals(mir.insns[4].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[11].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[15].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[33].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[50].dst, '_') ||
        !mir_machine_constant_equals(mir.insns[98].dst, 79) ||
        !mir_machine_constant_equals(mir.insns[104].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[164].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[189].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[194].dst, 257) ||
        !mir_machine_constant_equals(mir.insns[198].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[209].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[214].dst, 257) ||
        !mir_machine_constant_equals(mir.insns[218].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[225].dst, 256) ||
        !mir_machine_constant_equals(mir.insns[235].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[247].dst, 10) ||
        !mir_machine_constant_equals(mir.insns[253].dst, '0') ||
        !mir_machine_constant_equals(mir.insns[274].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[300].dst, 257) ||
        !mir_machine_constant_equals(mir.insns[321].dst, '"') ||
        !mir_machine_constant_equals(mir.insns[324].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[347].dst, '"') ||
        !mir_machine_constant_equals(mir.insns[365].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[376].dst, 79) ||
        !mir_machine_constant_equals(mir.insns[405].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[414].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[418].dst, 258) ||
        !mir_machine_constant_equals(mir.insns[424].dst, ':') ||
        !mir_machine_constant_equals(mir.insns[451].dst, '=') ||
        !mir_machine_constant_equals(mir.insns[466].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[471].dst, 259) ||
        !mir_machine_constant_equals(mir.insns[483].dst, '/') ||
        !mir_machine_constant_equals(mir.insns[510].dst, '=') ||
        !mir_machine_constant_equals(mir.insns[525].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[530].dst, 260) ||
        !mir_machine_constant_equals(mir.insns[542].dst, '<') ||
        !mir_machine_constant_equals(mir.insns[569].dst, '=') ||
        !mir_machine_constant_equals(mir.insns[584].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[589].dst, 261) ||
        !mir_machine_constant_equals(mir.insns[601].dst, '>') ||
        !mir_machine_constant_equals(mir.insns[628].dst, '=') ||
        !mir_machine_constant_equals(mir.insns[643].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[648].dst, 262) ||
        !mir_machine_constant_equals(mir.insns[660].dst, '.') ||
        !mir_machine_constant_equals(mir.insns[687].dst, '.') ||
        !mir_machine_constant_equals(mir.insns[702].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[707].dst, 263) ||
        !mir_machine_constant_equals(mir.insns[724].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[731].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[734].dst, 0))
        return mir_machine_reject(
            "ada-lexer-schedule", "constants");
    return 1;
}

static void mir_ada_call_local_character(
    MirStream *out, struct Sym *function)
{
    mir_stream_puts("\tld l,(ix-2)\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, function);
    mir_stream_puts("\tpop bc\n", out);
}

static void mir_ada_store_text_character(
    MirStream *out, const struct MirAdaLexerSchedule *plan)
{
    mir_stream_puts("\tpush iy\n\tpop hl\n", out);
    mir_machine_emit_hl_offset(out, plan->text_offset, 0);
    mir_stream_puts("\tld e,(ix-1)\n\tld d,0\n\tadd hl,de\n"
          "\tld a,(ix-2)\n\tld (hl),a\n", out);
}

static void mir_ada_terminate_text(
    MirStream *out, const struct MirAdaLexerSchedule *plan)
{
    mir_stream_puts("\tpush iy\n\tpop hl\n", out);
    mir_machine_emit_hl_offset(out, plan->text_offset, 0);
    mir_stream_puts("\tld e,(ix-1)\n\tld d,0\n\tadd hl,de\n"
          "\tld (hl),0\n", out);
}

static void mir_ada_compare_word(
    MirStream *out, const struct MirAdaLexerSchedule *plan,
    int string_id)
{
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", string_id);
    mir_stream_puts("\tpush iy\n\tpop hl\n", out);
    mir_machine_emit_hl_offset(out, plan->text_offset, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->compare_function);
    mir_interpreter_file_cleanup(out, 2);
}

static void mir_ada_copy_operator(
    MirStream *out, const struct MirAdaLexerSchedule *plan,
    int string_id)
{
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", string_id);
    mir_stream_puts("\tpush iy\n\tpop hl\n", out);
    mir_machine_emit_hl_offset(out, plan->text_offset, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->copy_function);
    mir_interpreter_file_cleanup(out, 2);
}

static void mir_emit_ada_lexer_schedule(
    MirStream *out, const struct MirAdaLexerSchedule *plan)
{
    static const int first_chars[5] = {':', '/', '<', '>', '.'};
    static const int second_chars[5] = {'=', '=', '=', '=', '.'};
    static const int tokens[5] = {259, 260, 261, 262, 263};
    struct MirForthLexerSchedule common;
    int identifier = new_label();
    int identifier_loop = new_label();
    int identifier_done = new_label();
    int identifier_store_done = new_label();
    int identifier_advance = new_label();
    int identifier_accept_next = new_label();
    int not_true = new_label();
    int identifier_default = new_label();
    int number = new_label();
    int number_loop = new_label();
    int number_done = new_label();
    int number_no_rewind = new_label();
    int string = new_label();
    int string_loop = new_label();
    int string_done = new_label();
    int string_store_done = new_label();
    int string_no_quote = new_label();
    int default_token = new_label();
    int epilogue = new_label();
    int pair_labels[5];
    int pair;

    memset(&common, 0, sizeof(common));
    common.state = plan->state;
    common.source_offset = plan->source_offset;
    common.length_offset = plan->length_offset;
    common.position_offset = plan->position_offset;
    common.text_offset = plan->text_offset;
    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-4\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_symbol_call(out, plan->skip_function);
    mir_machine_emit_global_word(out, plan->state, 0);
    mir_stream_puts("\tpush hl\n\tpop iy\n\txor a\n", out);
    mir_stream_printf(out,
            "\tld (iy%+d),a\n\tld (iy%+d),a\n"
            "\tld (iy%+d),a\n\tld (iy%+d),a\n"
            "\tld (iy%+d),a\n",
            plan->text_offset,
            plan->token_offset, plan->token_offset + 1,
            plan->integer_offset, plan->integer_offset + 1);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            epilogue);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tld (ix-2),a\n", out);
    mir_forth_increment_position(out, &common);
    mir_ada_call_local_character(out, plan->alpha_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", identifier);
    mir_stream_puts("\tld a,(ix-2)\n\tcp '_'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", identifier);
    mir_ada_call_local_character(out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", number);
    mir_stream_puts("\tld a,(ix-2)\n\tcp '\"'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", string);

    for (pair = 0; pair < 5; ++pair)
        pair_labels[pair] = new_label();
    for (pair = 0; pair < 5; ++pair) {
        mir_stream_puts("\tld a,(ix-2)\n", out);
        mir_stream_printf(out, "\tcp %d\n\tjp nz,L%d\n",
                first_chars[pair],
                pair + 1 < 5 ? pair_labels[pair + 1] : default_token);
        mir_forth_load_iy_word(out, plan->position_offset);
        mir_stream_printf(out,
                "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
                "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
                plan->length_offset, plan->length_offset + 1,
                default_token);
        mir_forth_load_source_character(out, &common);
        mir_stream_printf(out, "\tcp %d\n\tjp nz,L%d\n",
                second_chars[pair], default_token);
        mir_forth_increment_position(out, &common);
        mir_stream_printf(out,
                "\tld hl,%d\n\tld (iy%+d),l\n"
                "\tld (iy%+d),h\n",
                tokens[pair], plan->token_offset,
                plan->token_offset + 1);
        mir_ada_copy_operator(
            out, plan, plan->strings[pair + 3]);
        mir_stream_printf(out, "\tjp L%d\n", epilogue);
        if (pair + 1 < 5)
            mir_stream_printf(out, "L%d:\n", pair_labels[pair + 1]);
    }

    mir_stream_printf(out, "L%d:\n\txor a\n\tld (ix-1),a\nL%d:\n",
            identifier, identifier_loop);
    mir_ada_call_local_character(out, plan->alnum_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", identifier_advance);
    mir_stream_puts("\tld a,(ix-2)\n\tcp '_'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n",
            identifier_done, identifier_advance);
    mir_stream_puts("\tld a,(ix-1)\n\tcp 79\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", identifier_store_done);
    mir_ada_call_local_character(out, plan->lower_function);
    mir_stream_puts("\tld (ix-2),l\n", out);
    mir_ada_store_text_character(out, plan);
    mir_stream_puts("\tinc (ix-1)\n", out);
    mir_stream_printf(out, "L%d:\n", identifier_store_done);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            identifier_done);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tld (ix-2),a\n", out);
    mir_ada_call_local_character(out, plan->alnum_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", identifier_accept_next);
    mir_stream_puts("\tld a,(ix-2)\n\tcp '_'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n",
            identifier_done, identifier_accept_next);
    mir_forth_increment_position(out, &common);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n",
            identifier_loop, identifier_done);
    mir_ada_terminate_text(out, plan);
    mir_ada_compare_word(out, plan, plan->strings[0]);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", not_true);
    mir_stream_printf(out,
            "\tld hl,257\n\tld (iy%+d),l\n\tld (iy%+d),h\n"
            "\tld hl,1\n\tld (iy%+d),l\n\tld (iy%+d),h\n"
            "\tjp L%d\nL%d:\n",
            plan->token_offset, plan->token_offset + 1,
            plan->integer_offset, plan->integer_offset + 1,
            epilogue, not_true);
    mir_ada_compare_word(out, plan, plan->strings[1]);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", identifier_default);
    mir_stream_printf(out,
            "\tld hl,257\n\tld (iy%+d),l\n\tld (iy%+d),h\n"
            "\txor a\n\tld (iy%+d),a\n\tld (iy%+d),a\n"
            "\tjp L%d\n",
            plan->token_offset, plan->token_offset + 1,
            plan->integer_offset, plan->integer_offset + 1,
            epilogue);
    mir_stream_printf(out, "L%d:\n", identifier_default);
    mir_stream_printf(out,
            "\tld hl,256\n\tld (iy%+d),l\n\tld (iy%+d),h\n"
            "\tjp L%d\n",
            plan->token_offset, plan->token_offset + 1,
            epilogue);

    mir_stream_printf(out, "L%d:\n\txor a\n\tld (ix-4),a\n"
            "\tld (ix-3),a\nL%d:\n",
            number, number_loop);
    mir_ada_call_local_character(out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", number_done);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tld de,0\n\tld e,l\n\tld d,h\n"
          "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
          "\tadd hl,hl\n\tld a,(ix-2)\n\tsub '0'\n"
          "\tld e,a\n\tld d,0\n\tadd hl,de\n", out);
    mir_interpreter_file_store_ix_word(out, -4);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            number_done);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tld (ix-2),a\n", out);
    mir_forth_increment_position(out, &common);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", number_loop, number_done);
    mir_ada_call_local_character(out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", number_no_rewind);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_puts("\tdec hl\n", out);
    mir_forth_store_iy_word(out, plan->position_offset);
    mir_stream_printf(out, "L%d:\n", number_no_rewind);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_forth_store_iy_word(out, plan->integer_offset);
    mir_stream_printf(out,
            "\tld hl,257\n\tld (iy%+d),l\n\tld (iy%+d),h\n",
            plan->token_offset, plan->token_offset + 1);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->strings[2]);
    mir_stream_puts("\tpush iy\n\tpop hl\n", out);
    mir_machine_emit_hl_offset(out, plan->text_offset, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_runtime_call(out, plan->format_name);
    mir_interpreter_file_cleanup(out, 3);
    mir_stream_printf(out, "\tjp L%d\n", epilogue);

    mir_stream_printf(out, "L%d:\n\txor a\n\tld (ix-1),a\nL%d:\n",
            string, string_loop);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            string_done);
    mir_forth_load_source_character(out, &common);
    mir_stream_puts("\tcp '\"'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-2),a\n",
            string_done);
    mir_forth_increment_position(out, &common);
    mir_stream_puts("\tld a,(ix-1)\n\tcp 79\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", string_store_done);
    mir_ada_store_text_character(out, plan);
    mir_stream_puts("\tinc (ix-1)\n", out);
    mir_stream_printf(out, "L%d:\n\tjp L%d\nL%d:\n",
            string_store_done, string_loop, string_done);
    mir_forth_load_iy_word(out, plan->position_offset);
    mir_stream_printf(out,
            "\tld e,(iy%+d)\n\tld d,(iy%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->length_offset, plan->length_offset + 1,
            string_no_quote);
    mir_forth_increment_position(out, &common);
    mir_stream_printf(out, "L%d:\n", string_no_quote);
    mir_ada_terminate_text(out, plan);
    mir_stream_printf(out,
            "\tld hl,258\n\tld (iy%+d),l\n\tld (iy%+d),h\n"
            "\tjp L%d\n",
            plan->token_offset, plan->token_offset + 1,
            epilogue);

    mir_stream_printf(out, "L%d:\n", default_token);
    mir_stream_puts("\tld l,(ix-2)\n\tld h,0\n", out);
    mir_forth_store_iy_word(out, plan->token_offset);
    mir_stream_puts("\tld a,(ix-2)\n", out);
    mir_stream_printf(out,
            "\tld (iy%+d),a\n\txor a\n\tld (iy%+d),a\n",
            plan->text_offset, plan->text_offset + 1);
    mir_stream_printf(out,
            "L%d:\n\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n",
            epilogue);
}

static int mir_match_storage_opcode_schedule(
    struct MirStorageOpcodeSchedule *plan)
{
    static const int edges[][2] = {
        {5, 36}, {7, 21}, {11, 15}, {14, 18}, {25, 29},
        {28, 32}, {38, 52}, {42, 46}, {45, 49},
        {56, 60}, {59, 63}
    };
    static const int result_instructions[8] = {
        12, 16, 26, 30, 43, 47, 57, 61
    };
    int instruction;
    int result;
    long value;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 66 || mir_cfg_block_count() != 20 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        type_size(mir.return_type) != 2 ||
        !mir_pascal_scan_edges(
            edges, sizeof(edges) / sizeof(edges[0])))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
                mir_storage_opcode_opcodes[instruction])
            return 0;
    if (!mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->scope_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst, &plan->size_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[3].dst, &plan->array_stack_offset) ||
        plan->size_stack_offset != plan->scope_stack_offset + 2 ||
        plan->array_stack_offset != plan->size_stack_offset + 2 ||
        !mir_machine_constant_equals(mir.insns[9].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[23].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[40].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[54].dst, 1))
        return mir_machine_reject(
            "storage-opcode-schedule", "shape");
    for (result = 0; result < 8; ++result) {
        if (!mir_machine_evaluate_constant(
                mir.insns[result_instructions[result]].dst,
                &value, 0) ||
            value < -32768 || value > 65535)
            return mir_machine_reject(
                "storage-opcode-schedule", "results");
        plan->results[result] = (int)value;
    }
    return 1;
}

static void mir_emit_storage_opcode_schedule(
    MirStream *out, const struct MirStorageOpcodeSchedule *plan)
{
    int not_array = new_label();
    int array_global = new_label();
    int scalar_global = new_label();
    int array_word = new_label();
    int array_global_word = new_label();
    int scalar_word = new_label();
    int scalar_global_word = new_label();
    int done = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld a,h\n\tor l\n\tjp z,L%d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld a,h\n\tor l\n\tjp z,L%d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,1\n\tor a\n\tsbc hl,de\n"
            "\tjp nz,L%d\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,1\n\tor a\n\tsbc hl,de\n"
            "\tjp nz,L%d\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld a,h\n\tor l\n\tjp z,L%d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,1\n\tor a\n\tsbc hl,de\n"
            "\tjp nz,L%d\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,1\n\tor a\n\tsbc hl,de\n"
            "\tjp nz,L%d\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n"
            "L%d:\n\tpop ix\n\tret\n",
            plan->array_stack_offset + 2,
            plan->array_stack_offset + 3, not_array,
            plan->scope_stack_offset + 2,
            plan->scope_stack_offset + 3, array_global,
            plan->size_stack_offset + 2,
            plan->size_stack_offset + 3, array_word,
            plan->results[0], done,
            array_word, plan->results[1], done,
            array_global,
            plan->size_stack_offset + 2,
            plan->size_stack_offset + 3, array_global_word,
            plan->results[2], done,
            array_global_word, plan->results[3], done,
            not_array,
            plan->scope_stack_offset + 2,
            plan->scope_stack_offset + 3, scalar_global,
            plan->size_stack_offset + 2,
            plan->size_stack_offset + 3, scalar_word,
            plan->results[4], done,
            scalar_word, plan->results[5], done,
            scalar_global,
            plan->size_stack_offset + 2,
            plan->size_stack_offset + 3, scalar_global_word,
            plan->results[6], done,
            scalar_global_word, plan->results[7], done);
}

static int mir_cob_member_group(
    struct Sym *state, const int *members, size_t count,
    int width, int *offset_out)
{
    size_t item;
    int offset = (int)mir.insns[members[0]].immediate;

    if (offset < -32768 || offset > 32767)
        return 0;
    for (item = 0; item < count; ++item) {
        const struct MirInsn *member = &mir.insns[members[item]];
        const struct MirInsn *base = &mir.insns[members[item] - 1];

        if (base->opcode != MIR_ADDRESS ||
            !mir_machine_named_nonvolatile(base) ||
            find_global(base->name) != state ||
            member->opcode != MIR_MEMBER_ADDRESS ||
            member->src1 != base->dst ||
            member->memory_size != width ||
            member->immediate != offset ||
            (member->memory_flags & (1 | 8)) != 0)
            return 0;
    }
    *offset_out = offset;
    return 1;
}

static int mir_match_cob_add_sub_schedule(
    struct MirCobAddSubSchedule *plan)
{
    static const int position_members[] = {
        2, 9, 24, 35, 56, 71, 89,
        104, 117, 132, 143, 163, 178, 189
    };
    static const int end_members[] = {12, 59, 92, 120, 166};
    static const int token_members[] = {21, 68, 101, 129, 175};
    int instruction;
    long keyword;
    long indexed_opcode;
    long scalar_opcode;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 228 || mir_cfg_block_count() != 39 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
                mir_cob_add_sub_opcodes[instruction])
            return 0;
    plan->state = find_global(mir.insns[1].name);
    if (plan->state == NULL || !plan->state->is_defined ||
        plan->state->is_volatile ||
        !mir_cob_member_group(
            plan->state, position_members,
            sizeof(position_members) / sizeof(position_members[0]),
            2, &plan->token_position_offset) ||
        !mir_cob_member_group(
            plan->state, end_members,
            sizeof(end_members) / sizeof(end_members[0]),
            2, &plan->token_end_offset) ||
        !mir_cob_member_group(
            plan->state, token_members,
            sizeof(token_members) / sizeof(token_members[0]),
            2, &plan->tokens_offset))
        return mir_machine_reject(
            "cob-add-sub-schedule", "state");
    plan->expression_function =
        mir_interpreter_file_function(7, 0, 0);
    plan->failure_function =
        mir_interpreter_file_function(52, 0, 1);
    plan->emit_function =
        mir_interpreter_file_function(215, 0, 3);
    plan->syntax_string_id = (int)mir.insns[50].immediate;
    plan->variable_string_id = (int)mir.insns[84].immediate;
    if (plan->expression_function == NULL ||
        plan->failure_function == NULL ||
        plan->emit_function == NULL ||
        plan->syntax_string_id < 0 ||
        plan->variable_string_id < 0 ||
        !mir_interpreter_file_same_function(
            plan->expression_function, 161, 0, 0) ||
        !mir_interpreter_file_same_function(
            plan->failure_function, 86, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->failure_function, 206, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->emit_function, 225, 0, 3))
        return mir_machine_reject(
            "cob-add-sub-schedule", "functions");
    if (!mir_machine_evaluate_constant(
            mir.insns[31].dst, &keyword, 0) ||
        !mir_machine_evaluate_constant(
            mir.insns[209].dst, &indexed_opcode, 0) ||
        !mir_machine_evaluate_constant(
            mir.insns[219].dst, &scalar_opcode, 0) ||
        keyword < -32768 || keyword > 65535 ||
        indexed_opcode < -32768 || indexed_opcode > 65535 ||
        scalar_opcode < -32768 || scalar_opcode > 65535 ||
        !mir_machine_constant_equals(mir.insns[4].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[81].dst, 258) ||
        !mir_machine_constant_equals(mir.insns[139].dst, '(') ||
        !mir_machine_constant_equals(mir.insns[185].dst, ')'))
        return mir_machine_reject(
            "cob-add-sub-schedule", "constants");
    plan->keyword = (int)keyword;
    plan->indexed_opcode = (int)indexed_opcode;
    plan->scalar_opcode = (int)scalar_opcode;
    return 1;
}

static void mir_cob_load_field(
    MirStream *out, const struct MirCobAddSubSchedule *plan,
    int offset)
{
    mir_machine_emit_global_word(out, plan->state, offset);
}

static void mir_cob_store_field(
    MirStream *out, const struct MirCobAddSubSchedule *plan,
    int offset)
{
    mir_machine_emit_global_word_store(out, plan->state, offset);
}

static void mir_cob_peek(
    MirStream *out, const struct MirCobAddSubSchedule *plan)
{
    int available = new_label();
    int done = new_label();

    mir_cob_load_field(out, plan, plan->token_position_offset);
    mir_stream_puts("\tex de,hl\n", out);
    mir_cob_load_field(out, plan, plan->token_end_offset);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tld hl,0\n\tjp L%d\nL%d:\n",
            available, done, available);
    mir_cob_load_field(out, plan, plan->tokens_offset);
    mir_stream_puts("\tadd hl,de\n\tadd hl,de\n"
          "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    mir_stream_printf(out, "L%d:\n", done);
}

static void mir_cob_increment_token_position(
    MirStream *out, const struct MirCobAddSubSchedule *plan)
{
    mir_cob_load_field(out, plan, plan->token_position_offset);
    mir_stream_puts("\tinc hl\n", out);
    mir_cob_store_field(out, plan, plan->token_position_offset);
}

static void mir_cob_require(
    MirStream *out, const struct MirCobAddSubSchedule *plan,
    int token, int failure_string)
{
    int matched = new_label();

    mir_cob_peek(out, plan);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp z,L%d\n\tld hl,S%d\n\tpush hl\n",
            token, matched, failure_string);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", matched);
    mir_cob_increment_token_position(out, plan);
}

static void mir_cob_get(
    MirStream *out, const struct MirCobAddSubSchedule *plan)
{
    mir_cob_peek(out, plan);
    mir_stream_puts("\tpush hl\n", out);
    mir_cob_increment_token_position(out, plan);
    mir_stream_puts("\tpop hl\n", out);
}

static void mir_cob_emit_call(
    MirStream *out, const struct MirCobAddSubSchedule *plan,
    int opcode, int value_offset)
{
    mir_stream_puts("\tld hl,0\n\tpush hl\n", out);
    if (value_offset != 0)
        mir_stream_printf(out,
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
                value_offset, value_offset + 1);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%d\n\tpush hl\n", opcode);
    mir_machine_emit_symbol_call(out, plan->emit_function);
    mir_interpreter_file_cleanup(out, 3);
}

static void mir_emit_cob_add_sub_schedule(
    MirStream *out, const struct MirCobAddSubSchedule *plan)
{
    int indexed = new_label();
    int done = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tdec sp\n\tdec sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_cob_increment_token_position(out, plan);
    mir_machine_emit_symbol_call(out, plan->expression_function);
    mir_cob_require(
        out, plan, plan->keyword, plan->syntax_string_id);
    mir_cob_get(out, plan);
    mir_stream_printf(out,
            "\tld de,258\n\tor a\n\tsbc hl,de\n"
            "\tjp z,L%d\n\tld hl,S%d\n\tpush hl\n",
            indexed, plan->variable_string_id);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", indexed);
    mir_cob_get(out, plan);
    mir_interpreter_file_store_ix_word(out, -2);
    mir_cob_peek(out, plan);
    mir_stream_puts("\tld de,40\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", done);
    mir_cob_increment_token_position(out, plan);
    mir_machine_emit_symbol_call(out, plan->expression_function);
    mir_cob_require(out, plan, ')', plan->syntax_string_id);
    mir_cob_emit_call(out, plan, plan->indexed_opcode, -2);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
    mir_stream_printf(out, "L%d:\n", done);
    mir_cob_emit_call(out, plan, plan->scalar_opcode, -2);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static int mir_match_cob_var_access_schedule(
    struct MirCobAddSubSchedule *plan)
{
    static const int position_members[] = {
        2, 17, 35, 50, 63, 78, 89, 106, 121, 132
    };
    static const int end_members[] = {5, 38, 66, 109};
    static const int token_members[] = {14, 47, 75, 118};
    int instruction;
    long indexed_opcode;
    long scalar_opcode;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 171 || mir_cfg_block_count() != 30 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
                mir_cob_var_access_opcodes[instruction])
            return 0;
    plan->state = find_global(mir.insns[1].name);
    if (plan->state == NULL || !plan->state->is_defined ||
        plan->state->is_volatile ||
        !mir_cob_member_group(
            plan->state, position_members,
            sizeof(position_members) / sizeof(position_members[0]),
            2, &plan->token_position_offset) ||
        !mir_cob_member_group(
            plan->state, end_members,
            sizeof(end_members) / sizeof(end_members[0]),
            2, &plan->token_end_offset) ||
        !mir_cob_member_group(
            plan->state, token_members,
            sizeof(token_members) / sizeof(token_members[0]),
            2, &plan->tokens_offset))
        return mir_machine_reject(
            "cob-var-access-schedule", "state");
    plan->failure_function =
        mir_interpreter_file_function(32, 0, 1);
    plan->expression_function =
        mir_interpreter_file_function(104, 0, 0);
    plan->emit_function =
        mir_interpreter_file_function(158, 0, 3);
    plan->variable_string_id = (int)mir.insns[30].immediate;
    plan->syntax_string_id = (int)mir.insns[147].immediate;
    if (plan->failure_function == NULL ||
        plan->expression_function == NULL ||
        plan->emit_function == NULL ||
        plan->variable_string_id < 0 ||
        plan->syntax_string_id < 0 ||
        !mir_interpreter_file_same_function(
            plan->emit_function, 168, 0, 3) ||
        !mir_machine_evaluate_constant(
            mir.insns[152].dst, &indexed_opcode, 0) ||
        !mir_machine_evaluate_constant(
            mir.insns[162].dst, &scalar_opcode, 0) ||
        indexed_opcode < -32768 || indexed_opcode > 65535 ||
        scalar_opcode < -32768 || scalar_opcode > 65535 ||
        !mir_machine_constant_equals(mir.insns[27].dst, 258) ||
        !mir_machine_constant_equals(mir.insns[85].dst, '(') ||
        !mir_machine_constant_equals(mir.insns[128].dst, ')'))
        return mir_machine_reject(
            "cob-var-access-schedule", "semantics");
    plan->indexed_opcode = (int)indexed_opcode;
    plan->scalar_opcode = (int)scalar_opcode;
    return 1;
}

static void mir_emit_cob_var_access_schedule(
    MirStream *out, const struct MirCobAddSubSchedule *plan)
{
    int have_variable = new_label();
    int scalar = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tdec sp\n\tdec sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_cob_get(out, plan);
    mir_stream_puts("\tld de,258\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld hl,S%d\n\tpush hl\n",
            have_variable, plan->variable_string_id);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", have_variable);
    mir_cob_get(out, plan);
    mir_interpreter_file_store_ix_word(out, -2);
    mir_cob_peek(out, plan);
    mir_stream_puts("\tld de,40\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", scalar);
    mir_cob_increment_token_position(out, plan);
    mir_machine_emit_symbol_call(out, plan->expression_function);
    mir_cob_require(out, plan, ')', plan->syntax_string_id);
    mir_cob_emit_call(out, plan, plan->indexed_opcode, -2);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
    mir_stream_printf(out, "L%d:\n", scalar);
    mir_cob_emit_call(out, plan, plan->scalar_opcode, -2);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static int mir_match_cob_tokenizer_schedule(
    struct MirCobTokenizerSchedule *plan)
{
    static const int statement_members[] = {3, 13, 511};
    static const int token_count_members[] = {9, 517};
    static const int emit_calls[] = {
        140, 145, 205, 210, 268, 272, 428, 431,
        440, 454, 457, 472, 475, 500
    };
    int instruction;
    size_t item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 520 || mir_cfg_block_count() != 83 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != mir_cob_tokenizer_opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "cob-tokenizer-schedule", "volatile-memory");
    }
    if (!mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->statement_stack_offset) ||
        type_size(mir.insns[1].type) != 2)
        return mir_machine_reject(
            "cob-tokenizer-schedule", "parameter");
    plan->state = find_global(mir.insns[2].name);
    if (plan->state == NULL || !plan->state->is_defined ||
        plan->state->is_volatile ||
        !mir_cob_member_group(
            plan->state, statement_members,
            sizeof(statement_members) / sizeof(statement_members[0]),
            2, &plan->statements_offset) ||
        !mir_cob_member_group(
            plan->state, token_count_members,
            sizeof(token_count_members) / sizeof(token_count_members[0]),
            2, &plan->token_count_offset))
        return mir_machine_reject(
            "cob-tokenizer-schedule", "state");
    plan->statement_stride = (int)mir.insns[6].immediate;
    plan->statement_start_offset = (int)mir.insns[7].immediate;
    plan->statement_text_offset = (int)mir.insns[17].immediate;
    plan->statement_end_offset = (int)mir.insns[515].immediate;
    if (plan->statement_stride <= 0 ||
        plan->statement_stride > 32 ||
        plan->statement_text_offset < 0 ||
        plan->statement_start_offset < 0 ||
        plan->statement_end_offset < 0 ||
        plan->statement_text_offset + 1 >= plan->statement_stride ||
        plan->statement_start_offset + 1 >= plan->statement_stride ||
        plan->statement_end_offset + 1 >= plan->statement_stride)
        return mir_machine_reject(
            "cob-tokenizer-schedule", "record-layout");
    plan->space_function =
        mir_interpreter_file_function(36, 0, 1);
    plan->emit_function =
        mir_interpreter_file_function(emit_calls[0], 0, 1);
    plan->add_string_function =
        mir_interpreter_file_function(143, 0, 1);
    plan->digit_function =
        mir_interpreter_file_function(158, 0, 1);
    plan->alpha_function =
        mir_interpreter_file_function(278, 0, 1);
    plan->upper_function =
        mir_interpreter_file_function(312, 0, 1);
    plan->alnum_function =
        mir_interpreter_file_function(327, 0, 1);
    plan->keyword_function =
        mir_interpreter_file_function(418, 0, 1);
    plan->find_variable_function =
        mir_interpreter_file_function(445, 0, 1);
    plan->find_paragraph_function =
        mir_interpreter_file_function(463, 0, 1);
    plan->print_function = find_global(mir.insns[486].name);
    if (plan->space_function == NULL ||
        plan->emit_function == NULL ||
        plan->add_string_function == NULL ||
        plan->digit_function == NULL ||
        plan->alpha_function == NULL ||
        plan->upper_function == NULL ||
        plan->alnum_function == NULL ||
        plan->keyword_function == NULL ||
        plan->find_variable_function == NULL ||
        plan->find_paragraph_function == NULL ||
        plan->print_function == NULL ||
        !mir_interpreter_file_same_function(
            plan->digit_function, 182, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->digit_function, 216, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->digit_function, 237, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->upper_function, 398, 0, 1))
        return mir_machine_reject(
            "cob-tokenizer-schedule", "functions");
    for (item = 1;
         item < sizeof(emit_calls) / sizeof(emit_calls[0]);
         ++item)
        if (!mir_interpreter_file_same_function(
                plan->emit_function, emit_calls[item], 0, 1))
            return mir_machine_reject(
                "cob-tokenizer-schedule", "emit-calls");
    plan->unknown_string_id = (int)mir.insns[482].immediate;
    snprintf(
        plan->print_name, sizeof(plan->print_name), "%s",
        mir.insns[486].base_name[0] != 0
            ? mir.insns[486].base_name
            : asm_name_for(sym_asm_name(plan->print_function)));
    {
        const char *exit_name = mir.insns[489].base_name[0] != 0
            ? mir.insns[489].base_name
            : mir.insns[489].name;

        if (exit_name[0] == '_')
            dcc_copy_str(
                plan->exit_name, sizeof(plan->exit_name),
                exit_name);
        else if (strlen(exit_name) + 2 <= sizeof(plan->exit_name)) {
            plan->exit_name[0] = '_';
            dcc_copy_str(
                plan->exit_name + 1,
                sizeof(plan->exit_name) - 1, exit_name);
        }
    }
    if (plan->unknown_string_id < 0 ||
        plan->print_name[0] == 0 ||
        plan->exit_name[0] == 0)
        return mir_machine_reject(
            "cob-tokenizer-schedule", "reports");
    return 1;
}

static void mir_cob_tokenizer_word_address(MirStream *out)
{
    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-64\n\tadd hl,de\n",
          out);
}

static void mir_cob_tokenizer_record_address(
    MirStream *out, const struct MirCobTokenizerSchedule *plan)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            plan->statement_stack_offset + 4,
            plan->statement_stack_offset + 5);
    if (plan->statement_stride == 10)
        mir_stream_puts("\tld e,l\n\tld d,h\n\tadd hl,hl\n"
              "\tadd hl,hl\n\tadd hl,de\n\tadd hl,hl\n", out);
    else {
        mir_stream_printf(out, "\tld de,%d\n\tcall __mulu\n",
                plan->statement_stride);
    }
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(
        out, plan->state, plan->statements_offset);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_cob_tokenizer_emit(
    MirStream *out, const struct MirCobTokenizerSchedule *plan,
    int value)
{
    mir_stream_printf(out, "\tld hl,%d\n\tpush hl\n", value);
    mir_machine_emit_symbol_call(out, plan->emit_function);
    mir_stream_puts("\tpop bc\n", out);
}

static void mir_cob_tokenizer_emit_local(
    MirStream *out, const struct MirCobTokenizerSchedule *plan,
    int offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            offset, offset + 1);
    mir_machine_emit_symbol_call(out, plan->emit_function);
    mir_stream_puts("\tpop bc\n", out);
}

static void mir_cob_tokenizer_call_char(
    MirStream *out, struct Sym *function)
{
    mir_stream_puts("\tld l,(ix-68)\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, function);
    mir_stream_puts("\tpop bc\n", out);
}

static void mir_emit_cob_tokenizer_schedule(
    MirStream *out, const struct MirCobTokenizerSchedule *plan)
{
    int loop = new_label();
    int spaces = new_label();
    int have_character = new_label();
    int string_case = new_label();
    int string_loop = new_label();
    int string_done = new_label();
    int string_no_store = new_label();
    int string_no_quote = new_label();
    int negative = new_label();
    int negative_loop = new_label();
    int negative_done = new_label();
    int number = new_label();
    int number_loop = new_label();
    int number_scan = new_label();
    int number_done = new_label();
    int identifier = new_label();
    int identifier_loop = new_label();
    int identifier_done = new_label();
    int identifier_no_store = new_label();
    int identifier_accept = new_label();
    int not_keyword_number = new_label();
    int not_keyword = new_label();
    int not_variable = new_label();
    int not_paragraph = new_label();
    int default_token = new_label();
    int done = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-76\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_cob_tokenizer_record_address(out, plan);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_global_word(
        out, plan->state, plan->token_count_offset);
    mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
    mir_machine_emit_hl_offset(
        out, plan->statement_start_offset, 0);
    mir_stream_puts("\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_cob_tokenizer_record_address(out, plan);
    mir_machine_emit_hl_offset(
        out, plan->statement_text_offset, 0);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tpush de\n\tpop iy\n", out);

    mir_stream_printf(out, "L%d:\nL%d:\n\tld a,(iy+0)\n\tor a\n",
            loop, spaces);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-68),a\n", done);
    mir_cob_tokenizer_call_char(out, plan->space_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tinc iy\n\tjp L%d\nL%d:\n",
            have_character, spaces, have_character);
    mir_stream_puts("\tld a,(iy+0)\n\tinc iy\n\tld (ix-68),a\n"
          "\tcp 39\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", string_case);
    mir_stream_puts("\tcp '-'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", number);
    mir_stream_puts("\tld a,(iy+0)\n\tld (ix-68),a\n", out);
    mir_cob_tokenizer_call_char(out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", negative);
    mir_stream_puts("\tld a,'-'\n\tld (ix-68),a\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", default_token, number);
    mir_stream_puts("\tld a,(ix-68)\n", out);
    mir_cob_tokenizer_call_char(out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", number_loop);
    mir_stream_puts("\tld a,(ix-68)\n", out);
    mir_cob_tokenizer_call_char(out, plan->alpha_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", identifier);
    mir_stream_puts("\tld a,(ix-68)\n\tcp '_'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tjp L%d\n",
            identifier, default_token);

    mir_stream_printf(out, "L%d:\n\txor a\n\tld (ix-66),a\nL%d:\n",
            string_case, string_loop);
    mir_stream_puts("\tld a,(iy+0)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tcp 39\n\tjp z,L%d\n",
            string_done, string_done);
    mir_stream_puts("\tld (ix-68),a\n\tld a,(ix-66)\n\tcp 63\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", string_no_store);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tld e,(ix-66)\n\tld d,0\n\tadd hl,de\n"
          "\tld a,(ix-68)\n\tld (hl),a\n\tinc (ix-66)\n",
          out);
    mir_stream_printf(out, "L%d:\n\tinc iy\n\tjp L%d\nL%d:\n",
            string_no_store, string_loop, string_done);
    mir_stream_puts("\tld a,(iy+0)\n\tcp 39\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tinc iy\nL%d:\n",
            string_no_quote, string_no_quote);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tld e,(ix-66)\n\tld d,0\n\tadd hl,de\n"
          "\tld (hl),0\n", out);
    mir_cob_tokenizer_emit(out, plan, 257);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->add_string_function);
    mir_stream_puts("\tpop bc\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->emit_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\n", loop);

    mir_stream_printf(out, "L%d:\n\txor a\n\tld (ix-76),a\n"
            "\tld (ix-75),a\nL%d:\n",
            negative, negative_loop);
    mir_stream_puts("\tld a,(iy+0)\n\tld (ix-68),a\n", out);
    mir_cob_tokenizer_call_char(out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", negative_done);
    mir_interpreter_file_load_ix_word(out, -76);
    mir_stream_puts("\tld de,0\n\tld e,l\n\tld d,h\n"
          "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
          "\tadd hl,hl\n\tld a,(ix-68)\n\tsub '0'\n"
          "\tld e,a\n\tld d,0\n\tadd hl,de\n", out);
    mir_interpreter_file_store_ix_word(out, -76);
    mir_stream_puts("\tinc iy\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", negative_loop, negative_done);
    mir_cob_tokenizer_emit(out, plan, 256);
    mir_interpreter_file_load_ix_word(out, -76);
    mir_stream_puts("\txor a\n\tsub l\n\tld l,a\n"
          "\tld a,0\n\tsbc a,h\n\tld h,a\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->emit_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\n", loop);

    mir_stream_printf(out, "L%d:\n", number_loop);
    mir_stream_puts("\tld a,(ix-68)\n\tsub '0'\n\tld l,a\n\tld h,0\n",
          out);
    mir_interpreter_file_store_ix_word(out, -76);
    mir_stream_printf(out, "L%d:\n\tld a,(iy+0)\n\tor a\n",
            number_scan);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-68),a\n", number_done);
    mir_cob_tokenizer_call_char(out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", number_done);
    mir_interpreter_file_load_ix_word(out, -76);
    mir_stream_puts("\tld de,0\n\tld e,l\n\tld d,h\n"
          "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
          "\tadd hl,hl\n\tld a,(ix-68)\n\tsub '0'\n"
          "\tld e,a\n\tld d,0\n\tadd hl,de\n", out);
    mir_interpreter_file_store_ix_word(out, -76);
    mir_stream_puts("\tinc iy\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", number_scan, number_done);
    mir_cob_tokenizer_emit(out, plan, 256);
    mir_cob_tokenizer_emit_local(out, plan, -76);
    mir_stream_printf(out, "\tjp L%d\n", loop);

    mir_stream_printf(out, "L%d:\n\txor a\n\tld (ix-66),a\n"
            "\tld a,(ix-68)\n", identifier);
    mir_cob_tokenizer_call_char(out, plan->upper_function);
    mir_stream_puts("\tld (ix-68),l\n", out);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tld a,(ix-68)\n\tld (hl),a\n\tinc (ix-66)\n", out);
    mir_stream_printf(out, "L%d:\n\tld a,(iy+0)\n\tor a\n",
            identifier_loop);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-68),a\n",
            identifier_done);
    mir_cob_tokenizer_call_char(out, plan->alnum_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", identifier_accept);
    mir_stream_puts("\tld a,(ix-68)\n\tcp '-'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tcp '_'\n\tjp nz,L%d\nL%d:\n",
            identifier_accept, identifier_done, identifier_accept);
    mir_stream_puts("\tld a,(ix-66)\n\tcp 63\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", identifier_no_store);
    mir_stream_puts("\tld a,(ix-68)\n", out);
    mir_cob_tokenizer_call_char(out, plan->upper_function);
    mir_stream_puts("\tld (ix-68),l\n", out);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tld e,(ix-66)\n\tld d,0\n\tadd hl,de\n"
          "\tld a,(ix-68)\n\tld (hl),a\n\tinc (ix-66)\n", out);
    mir_stream_printf(out, "L%d:\n\tinc iy\n\tjp L%d\nL%d:\n",
            identifier_no_store, identifier_loop, identifier_done);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tld e,(ix-66)\n\tld d,0\n\tadd hl,de\n\tld (hl),0\n",
          out);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->keyword_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_interpreter_file_store_ix_word(out, -70);
    mir_stream_puts("\tld de,256\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", not_keyword_number);
    mir_cob_tokenizer_emit(out, plan, 256);
    mir_cob_tokenizer_emit(out, plan, 0);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, not_keyword_number);
    mir_interpreter_file_load_ix_word(out, -70);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", not_keyword);
    mir_cob_tokenizer_emit_local(out, plan, -70);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, not_keyword);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->find_variable_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_interpreter_file_store_ix_word(out, -72);
    mir_stream_puts("\tbit 7,h\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", not_variable);
    mir_cob_tokenizer_emit(out, plan, 258);
    mir_cob_tokenizer_emit_local(out, plan, -72);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, not_variable);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->find_paragraph_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_interpreter_file_store_ix_word(out, -74);
    mir_stream_puts("\tbit 7,h\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", not_paragraph);
    mir_cob_tokenizer_emit(out, plan, 259);
    mir_cob_tokenizer_emit_local(out, plan, -74);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, not_paragraph);
    mir_cob_tokenizer_word_address(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n\tld hl,2\n\tpush hl\n",
            plan->unknown_string_id);
    mir_emit_runtime_call(out, plan->print_name);
    mir_interpreter_file_cleanup(out, 3);
    mir_stream_puts("\tld hl,1\n\tpush hl\n", out);
    mir_emit_runtime_call(out, plan->exit_name);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n",
            loop, default_token);
    mir_stream_puts("\tld l,(ix-68)\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->emit_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done);
    mir_cob_tokenizer_record_address(out, plan);
    mir_machine_emit_hl_offset(out, plan->statement_end_offset, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_global_word(
        out, plan->state, plan->token_count_offset);
    mir_stream_puts("\tex de,hl\n\tpop hl\n"
          "\tld (hl),e\n\tinc hl\n\tld (hl),d\n"
          "\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n", out);
}

static int mir_match_cob_perform_schedule(
    struct MirCobPerformSchedule *plan)
{
    static const int position_members[] = {
        2, 8, 23, 38, 44, 59, 87, 102, 113, 128, 143, 158,
        164, 179, 194, 209, 220, 226, 241, 254, 269, 280,
        303, 318, 329, 347, 362, 377, 383, 398, 411, 426,
        437, 459, 474, 485, 507, 522, 533
    };
    static const int end_members[] = {
        11, 47, 90, 131, 167, 197, 229,
        257, 306, 350, 386, 414, 462, 510
    };
    static const int token_members[] = {
        20, 56, 99, 140, 176, 206, 238,
        266, 315, 359, 395, 423, 471, 519
    };
    static const int code_position_members[] = {563, 578};
    static const int perform_count_members[] = {585, 595};
    static const int performs_members[] = {
        603, 611, 619, 627, 635, 643
    };
    static const int field_members[] = {
        607, 615, 623, 631, 639, 647
    };
    static const int error_insns[] = {33, 153, 372, 452, 590};
    int instruction;
    int field;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 657 || mir_cfg_block_count() != 92 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return mir_machine_reject(
                "cob-perform-schedule", "volatile-memory");
    }
    plan->state = find_global(mir.insns[1].name);
    if (plan->state == NULL || !plan->state->is_defined ||
        plan->state->is_volatile ||
        !mir_cob_member_group(
            plan->state, position_members,
            sizeof(position_members) / sizeof(position_members[0]),
            2, &plan->token_position_offset) ||
        !mir_cob_member_group(
            plan->state, end_members,
            sizeof(end_members) / sizeof(end_members[0]),
            2, &plan->token_end_offset) ||
        !mir_cob_member_group(
            plan->state, token_members,
            sizeof(token_members) / sizeof(token_members[0]),
            2, &plan->tokens_offset) ||
        !mir_cob_member_group(
            plan->state, code_position_members,
            sizeof(code_position_members) /
                sizeof(code_position_members[0]),
            2, &plan->code_position_offset) ||
        !mir_cob_member_group(
            plan->state, perform_count_members,
            sizeof(perform_count_members) /
                sizeof(perform_count_members[0]),
            2, &plan->perform_count_offset) ||
        !mir_cob_member_group(
            plan->state, performs_members,
            sizeof(performs_members) / sizeof(performs_members[0]),
            2, &plan->performs_offset))
        return mir_machine_reject(
            "cob-perform-schedule", "state");
    plan->failure_function =
        mir_interpreter_file_function(35, 0, 1);
    plan->expression_function =
        mir_interpreter_file_function(457, 0, 0);
    plan->emit_function =
        mir_interpreter_file_function(559, 0, 3);
    plan->condition_function =
        mir_interpreter_file_function(567, 0, 0);
    plan->patch_function =
        mir_interpreter_file_function(581, 0, 2);
    if (plan->failure_function == NULL ||
        plan->expression_function == NULL ||
        plan->emit_function == NULL ||
        plan->condition_function == NULL ||
        plan->patch_function == NULL ||
        !mir_interpreter_file_same_function(
            plan->failure_function, 155, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->failure_function, 297, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->failure_function, 374, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->failure_function, 454, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->failure_function, 502, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->failure_function, 550, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->expression_function, 505, 0, 0) ||
        !mir_interpreter_file_same_function(
            plan->emit_function, 574, 0, 3) ||
        !mir_interpreter_file_same_function(
            plan->emit_function, 656, 0, 3))
        return mir_machine_reject(
            "cob-perform-schedule", "functions");
    for (field = 0; field < 5; ++field) {
        if (mir.insns[error_insns[field]].immediate < 0)
            return mir_machine_reject(
                "cob-perform-schedule", "strings");
        plan->error_strings[field] =
            (int)mir.insns[error_insns[field]].immediate;
    }
    plan->perform_stride = (int)mir.insns[606].immediate;
    if (plan->perform_stride <= 0 || plan->perform_stride > 32)
        return mir_machine_reject(
            "cob-perform-schedule", "perform-stride");
    for (field = 0; field < 6; ++field) {
        const struct MirInsn *member = &mir.insns[field_members[field]];

        if (member->opcode != MIR_MEMBER_ADDRESS ||
            member->memory_size != 2 ||
            member->immediate < 0 ||
            member->immediate + 1 >= plan->perform_stride)
            return mir_machine_reject(
                "cob-perform-schedule", "perform-fields");
        plan->perform_field_offsets[field] =
            (int)member->immediate;
    }
    return 1;
}

static void mir_cob_perform_access(
    struct MirCobAddSubSchedule *access,
    const struct MirCobPerformSchedule *plan)
{
    memset(access, 0, sizeof(*access));
    access->state = plan->state;
    access->token_position_offset = plan->token_position_offset;
    access->token_end_offset = plan->token_end_offset;
    access->tokens_offset = plan->tokens_offset;
    access->failure_function = plan->failure_function;
}

static void mir_cob_perform_emit(
    MirStream *out, const struct MirCobPerformSchedule *plan,
    int opcode, int value_offset)
{
    mir_stream_puts("\tld hl,0\n\tpush hl\n", out);
    if (value_offset != 0)
        mir_stream_printf(out,
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
                value_offset, value_offset + 1);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%d\n\tpush hl\n", opcode);
    mir_machine_emit_symbol_call(out, plan->emit_function);
    mir_interpreter_file_cleanup(out, 3);
}

static void mir_cob_perform_record_address(
    MirStream *out, const struct MirCobPerformSchedule *plan,
    int field)
{
    mir_machine_emit_global_word(
        out, plan->state, plan->performs_offset);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_puts("\tld l,(ix-12)\n\tld h,(ix-11)\n", out);
    if (plan->perform_stride == 12)
        mir_stream_puts("\tld e,l\n\tld d,h\n\tadd hl,hl\n"
              "\tadd hl,de\n\tadd hl,hl\n\tadd hl,hl\n", out);
    else {
        mir_stream_printf(out, "\tld de,%d\n\tcall __mulu\n",
                plan->perform_stride);
    }
    mir_stream_puts("\tpop de\n\tadd hl,de\n", out);
    mir_machine_emit_hl_offset(
        out, plan->perform_field_offsets[field], 0);
}

static void mir_cob_perform_store_field(
    MirStream *out, const struct MirCobPerformSchedule *plan,
    int field, int local_offset)
{
    mir_cob_perform_record_address(out, plan, field);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
            local_offset, local_offset + 1);
}

static void mir_cob_perform_require_local(
    MirStream *out, const struct MirCobPerformSchedule *plan,
    int peek_label, int increment_label,
    int token, int string_id)
{
    int matched = new_label();

    mir_stream_printf(out,
            "\tcall L%d\n\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp z,L%d\n\tld hl,S%d\n\tpush hl\n",
            peek_label, token, matched, string_id);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n\tcall L%d\n",
            matched, increment_label);
}

static void mir_emit_cob_perform_schedule(
    MirStream *out, const struct MirCobPerformSchedule *plan)
{
    int have_paragraph = new_label();
    int no_thru = new_label();
    int have_thru = new_label();
    int no_count = new_label();
    int no_varying = new_label();
    int have_variable = new_label();
    int have_capacity = new_label();
    int peek_helper = new_label();
    int increment_helper = new_label();
    int get_helper = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-16\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_printf(out, "\tcall L%d\n\tcall L%d\n",
            increment_helper, peek_helper);
    mir_stream_puts("\tld de,259\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld hl,S%d\n\tpush hl\n",
            have_paragraph, plan->error_strings[0]);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", have_paragraph);
    mir_stream_printf(out, "\tcall L%d\n\tcall L%d\n",
            increment_helper, get_helper);
    mir_interpreter_file_store_ix_word(out, -2);
    mir_interpreter_file_store_ix_word(out, -4);
    mir_stream_puts("\tld hl,1\n", out);
    mir_interpreter_file_store_ix_word(out, -10);
    mir_stream_puts("\tld hl,0\n", out);
    mir_interpreter_file_store_ix_word(out, -8);
    mir_interpreter_file_store_ix_word(out, -6);
    mir_interpreter_file_store_ix_word(out, -16);

    mir_stream_printf(out, "\tcall L%d\n", peek_helper);
    mir_stream_puts("\tld de,312\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_thru);
    mir_stream_printf(out, "\tcall L%d\n\tcall L%d\n",
            increment_helper, peek_helper);
    mir_stream_puts("\tld de,259\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld hl,S%d\n\tpush hl\n",
            have_thru, plan->error_strings[1]);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", have_thru);
    mir_stream_printf(out, "\tcall L%d\n\tcall L%d\n",
            increment_helper, get_helper);
    mir_interpreter_file_store_ix_word(out, -4);
    mir_stream_printf(out, "L%d:\n", no_thru);

    mir_stream_printf(out, "\tcall L%d\n", peek_helper);
    mir_stream_puts("\tld de,256\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_count);
    mir_stream_printf(out, "\tcall L%d\n\tcall L%d\n",
            increment_helper, get_helper);
    mir_interpreter_file_store_ix_word(out, -10);
    mir_cob_perform_require_local(
        out, plan, peek_helper, increment_helper,
        313, plan->error_strings[3]);
    mir_stream_printf(out, "L%d:\n", no_count);

    mir_stream_printf(out, "\tcall L%d\n", peek_helper);
    mir_stream_puts("\tld de,314\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_varying);
    mir_stream_printf(out, "\tcall L%d\n", increment_helper);
    mir_stream_puts("\tld hl,1\n", out);
    mir_interpreter_file_store_ix_word(out, -8);
    mir_stream_printf(out, "\tcall L%d\n", peek_helper);
    mir_stream_puts("\tld de,258\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld hl,S%d\n\tpush hl\n",
            have_variable, plan->error_strings[2]);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", have_variable);
    mir_stream_printf(out, "\tcall L%d\n\tcall L%d\n",
            increment_helper, get_helper);
    mir_interpreter_file_store_ix_word(out, -6);
    mir_cob_perform_require_local(
        out, plan, peek_helper, increment_helper,
        305, plan->error_strings[3]);
    mir_machine_emit_symbol_call(out, plan->expression_function);
    mir_cob_perform_require_local(
        out, plan, peek_helper, increment_helper,
        307, plan->error_strings[3]);
    mir_machine_emit_symbol_call(out, plan->expression_function);
    mir_cob_perform_require_local(
        out, plan, peek_helper, increment_helper,
        315, plan->error_strings[3]);
    mir_cob_perform_emit(out, plan, 25, 0);
    mir_interpreter_file_store_ix_word(out, -14);
    mir_machine_emit_global_word(
        out, plan->state, plan->code_position_offset);
    mir_interpreter_file_store_ix_word(out, -16);
    mir_machine_emit_symbol_call(out, plan->condition_function);
    mir_cob_perform_emit(out, plan, 1, 0);
    mir_machine_emit_global_word(
        out, plan->state, plan->code_position_offset);
    mir_stream_puts("\tpush hl\n", out);
    mir_interpreter_file_load_ix_word(out, -14);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->patch_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_stream_printf(out, "L%d:\n", no_varying);

    mir_machine_emit_global_word(
        out, plan->state, plan->perform_count_offset);
    mir_stream_puts("\tld de,32\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n\tld hl,S%d\n\tpush hl\n",
            have_capacity, plan->error_strings[4]);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", have_capacity);
    mir_machine_emit_global_word(
        out, plan->state, plan->perform_count_offset);
    mir_interpreter_file_store_ix_word(out, -12);
    mir_stream_puts("\tinc hl\n", out);
    mir_machine_emit_global_word_store(
        out, plan->state, plan->perform_count_offset);
    mir_cob_perform_store_field(out, plan, 0, -2);
    mir_cob_perform_store_field(out, plan, 1, -4);
    mir_cob_perform_store_field(out, plan, 2, -8);
    mir_cob_perform_store_field(out, plan, 3, -10);
    mir_cob_perform_store_field(out, plan, 4, -6);
    mir_cob_perform_store_field(out, plan, 5, -16);
    mir_cob_perform_emit(out, plan, 34, -12);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
    mir_stream_printf(out, "L%d:\n", peek_helper);
    {
        struct MirCobAddSubSchedule access;

        mir_cob_perform_access(&access, plan);
        mir_cob_peek(out, &access);
    }
    mir_stream_puts("\tret\n", out);
    mir_stream_printf(out, "L%d:\n", increment_helper);
    mir_machine_emit_global_word(
        out, plan->state, plan->token_position_offset);
    mir_stream_puts("\tinc hl\n", out);
    mir_machine_emit_global_word_store(
        out, plan->state, plan->token_position_offset);
    mir_stream_puts("\tret\n", out);
    mir_stream_printf(out, "L%d:\n\tcall L%d\n\tpush hl\n"
            "\tcall L%d\n\tpop hl\n\tret\n",
            get_helper, peek_helper, increment_helper);
}

static int mir_match_cob_chain_schedule(
    struct MirCobChainSchedule *plan)
{
    static const int position_members[] = {4, 19, 34, 49, 72, 87};
    static const int condition_position_members[] = {
        4, 19, 34, 49, 72, 87, 99
    };
    static const int end_members[] = {7, 37, 75};
    static const int token_members[] = {16, 46, 84};
    const int *positions;
    size_t position_count;
    int second_child;
    int emit_instruction;
    int first_opcode_instruction;
    int second_opcode_instruction;
    long value;

    memset(plan, 0, sizeof(*plan));
    if ((mir.count != 122 && mir.count != 125) ||
        mir_cfg_block_count() != 27 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    positions = mir.count == 125
        ? condition_position_members : position_members;
    position_count = mir.count == 125
        ? sizeof(condition_position_members) /
              sizeof(condition_position_members[0])
        : sizeof(position_members) / sizeof(position_members[0]);
    second_child = mir.count == 125 ? 104 : 99;
    emit_instruction = mir.count == 125 ? 120 : 117;
    first_opcode_instruction = mir.count == 125 ? 107 : 104;
    second_opcode_instruction = mir.count == 125 ? 111 : 108;
    plan->state = find_global(mir.insns[3].name);
    if (plan->state == NULL || !plan->state->is_defined ||
        plan->state->is_volatile ||
        !mir_cob_member_group(
            plan->state, positions, position_count,
            2, &plan->token_position_offset) ||
        !mir_cob_member_group(
            plan->state, end_members,
            sizeof(end_members) / sizeof(end_members[0]),
            2, &plan->token_end_offset) ||
        !mir_cob_member_group(
            plan->state, token_members,
            sizeof(token_members) / sizeof(token_members[0]),
            2, &plan->tokens_offset))
        return 0;
    plan->child_function =
        mir_interpreter_file_function(1, 0, 0);
    plan->emit_function =
        mir_interpreter_file_function(emit_instruction, 0, 3);
    if (plan->child_function == NULL ||
        plan->emit_function == NULL ||
        !mir_interpreter_file_same_function(
            plan->child_function, second_child, 0, 0))
        return 0;
    if (!mir_machine_evaluate_constant(
            mir.insns[26].dst, &value, 0))
        return 0;
    plan->first_token = (int)value;
    if (!mir_machine_evaluate_constant(
            mir.insns[56].dst, &value, 0))
        return 0;
    plan->second_token = (int)value;
    if (!mir_machine_evaluate_constant(
            mir.insns[first_opcode_instruction].dst,
            &value, 0))
        return 0;
    plan->first_opcode = (int)value;
    if (!mir_machine_evaluate_constant(
            mir.insns[second_opcode_instruction].dst,
            &value, 0))
        return 0;
    plan->second_opcode = (int)value;
    return plan->first_token != plan->second_token &&
           plan->first_opcode != plan->second_opcode;
}

static void mir_emit_cob_chain_schedule(
    MirStream *out, const struct MirCobChainSchedule *plan)
{
    struct MirCobAddSubSchedule access;
    int loop = new_label();
    int second = new_label();
    int done = new_label();
    int emit = new_label();

    memset(&access, 0, sizeof(access));
    access.state = plan->state;
    access.token_position_offset = plan->token_position_offset;
    access.token_end_offset = plan->token_end_offset;
    access.tokens_offset = plan->tokens_offset;
    access.emit_function = plan->emit_function;
    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tdec sp\n\tdec sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_symbol_call(out, plan->child_function);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_cob_peek(out, &access);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nz,L%d\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n",
            plan->first_token, second,
            plan->first_opcode, emit, second);
    mir_cob_peek(out, &access);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nz,L%d\n\tld hl,%d\n"
            "L%d:\n",
            plan->second_token, done,
            plan->second_opcode, emit);
    mir_interpreter_file_store_ix_word(out, -2);
    mir_cob_increment_token_position(out, &access);
    mir_machine_emit_symbol_call(out, plan->child_function);
    mir_stream_puts("\tld hl,0\n\tpush hl\n\tpush hl\n"
          "\tld l,(ix-2)\n\tld h,(ix-1)\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->emit_function);
    mir_interpreter_file_cleanup(out, 3);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n"
            "\tld sp,ix\n\tpop ix\n\tret\n",
            loop, done);
}

static int mir_match_fortran_statement_append_schedule(
    struct MirFortranStatementAppendSchedule *plan)
{
    static const int member_indices[8] = {
        15, 21, 29, 35, 42, 49, 56, 63
    };
    int instruction;
    int member;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 71 || mir_cfg_block_count() != 2 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID ||
        mir.insns[7].label != mir.insns[11].label)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
                mir_fortran_add_stmt_opcodes[instruction])
            return 0;
    if (!mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->label_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst, &plan->text_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[3].dst, &plan->unit_stack_offset) ||
        plan->text_stack_offset != plan->label_stack_offset + 2 ||
        plan->unit_stack_offset != plan->text_stack_offset + 2)
        return mir_machine_reject(
            "fortran-statement-append", "parameters");
    plan->statement_count =
        mir_pascal_scan_symbol(4, 2, 0);
    plan->statements =
        mir_pascal_scan_symbol(12, 2, 0);
    plan->failure_function = find_global(mir.insns[10].name);
    plan->duplicate_function =
        mir_interpreter_file_function(24, 0, 1);
    plan->failure_string_id = (int)mir.insns[8].immediate;
    plan->limit = (int)mir.insns[5].immediate;
    plan->stride = (int)mir.insns[14].immediate;
    if (plan->statement_count == NULL ||
        plan->statements == NULL ||
        plan->failure_function == NULL ||
        plan->duplicate_function == NULL ||
        plan->failure_string_id < 0 ||
        plan->limit <= 0 || plan->limit > 32767 ||
        plan->stride <= 0 || plan->stride > 128)
        return mir_machine_reject(
            "fortran-statement-append", "symbols");
    for (member = 0; member < 8; ++member) {
        const struct MirInsn *insn =
            &mir.insns[member_indices[member]];

        if (insn->opcode != MIR_MEMBER_ADDRESS ||
            insn->memory_size != 2 ||
            insn->immediate < 0 ||
            insn->immediate + 1 >= plan->stride)
            return mir_machine_reject(
                "fortran-statement-append", "members");
        plan->offsets[member] = (int)insn->immediate;
    }
    if (!mir_pascal_scan_same_symbol(plan->statements, 18) ||
        !mir_pascal_scan_same_symbol(plan->statements, 26) ||
        !mir_pascal_scan_same_symbol(plan->statements, 32) ||
        !mir_pascal_scan_same_symbol(plan->statement_count, 13) ||
        !mir_pascal_scan_same_symbol(plan->statement_count, 19) ||
        !mir_pascal_scan_same_symbol(plan->statement_count, 27) ||
        !mir_machine_constant_equals(mir.insns[37].dst, 65535) ||
        !mir_machine_constant_equals(mir.insns[44].dst, 65535) ||
        !mir_machine_constant_equals(mir.insns[51].dst, 65535) ||
        !mir_machine_constant_equals(mir.insns[58].dst, 65535) ||
        !mir_machine_constant_equals(mir.insns[65].dst, 65535) ||
        !mir_machine_constant_equals(mir.insns[68].dst, 1))
        return mir_machine_reject(
            "fortran-statement-append", "semantics");
    return 1;
}

static void mir_fortran_statement_address(
    MirStream *out, const struct MirFortranStatementAppendSchedule *plan)
{
    mir_machine_emit_global_word(out, plan->statement_count, 0);
    if (plan->stride == 62) {
        mir_stream_puts("\tld e,l\n\tld d,h\n\tadd hl,hl\n"
              "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
              "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
              "\tadd hl,hl\n", out);
    } else {
        mir_stream_printf(out, "\tld de,%d\n\tcall __mulu\n",
                plan->stride);
    }
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->statements, 0);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_fortran_store_member(
    MirStream *out, const struct MirFortranStatementAppendSchedule *plan,
    int address_label, int member, int stack_offset, int constant)
{
    mir_stream_printf(out, "\tcall L%d\n", address_label);
    mir_machine_emit_hl_offset(out, plan->offsets[member], 0);
    if (stack_offset != 0)
        mir_stream_printf(out,
                "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
                stack_offset, stack_offset + 1);
    else
        mir_stream_printf(out, "\tld de,%d\n", constant);
    mir_stream_puts("\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
}

static void mir_emit_fortran_statement_append_schedule(
    MirStream *out, const struct MirFortranStatementAppendSchedule *plan)
{
    int room = new_label();
    int address_helper = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_global_word(out, plan->statement_count, 0);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp c,L%d\n\tld hl,S%d\n\tpush hl\n",
            plan->limit, room, plan->failure_string_id);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", room);
    mir_fortran_store_member(
        out, plan, address_helper, 0,
        plan->label_stack_offset + 2, 0);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            plan->text_stack_offset + 2,
            plan->text_stack_offset + 3);
    mir_machine_emit_symbol_call(out, plan->duplicate_function);
    mir_stream_puts("\tpop bc\n\tpush hl\n", out);
    mir_stream_printf(out, "\tcall L%d\n", address_helper);
    mir_machine_emit_hl_offset(out, plan->offsets[1], 0);
    mir_stream_puts("\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_fortran_store_member(
        out, plan, address_helper, 2,
        plan->unit_stack_offset + 2, 0);
    mir_fortran_store_member(
        out, plan, address_helper, 3, 0, -1);
    mir_fortran_store_member(
        out, plan, address_helper, 4, 0, -1);
    mir_fortran_store_member(
        out, plan, address_helper, 5, 0, -1);
    mir_fortran_store_member(
        out, plan, address_helper, 6, 0, -1);
    mir_fortran_store_member(
        out, plan, address_helper, 7, 0, -1);
    mir_machine_emit_global_word(out, plan->statement_count, 0);
    mir_stream_puts("\tinc hl\n", out);
    mir_machine_emit_global_word_store(
        out, plan->statement_count, 0);
    mir_stream_puts("\tpop ix\n\tret\n", out);
    mir_stream_printf(out, "L%d:\n", address_helper);
    mir_fortran_statement_address(out, plan);
    mir_stream_puts("\tret\n", out);
}

static int mir_match_fortran_uppercase_schedule(
    struct MirFortranUppercaseSchedule *plan)
{
    static const unsigned char expected_opcodes[55] = {
        MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_CONST, MIR_NOP,
        MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_PHI,
        MIR_NOP, MIR_NOP, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
        MIR_BRANCH_FALSE, MIR_NOP, MIR_NOP, MIR_NOP, MIR_CONST,
        MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
        MIR_JUMP, MIR_LABEL, MIR_CONST, MIR_LABEL, MIR_PHI,
        MIR_BRANCH_FALSE, MIR_NOP, MIR_NOP, MIR_INDEX_ADDRESS,
        MIR_NOP, MIR_NOP, MIR_INDEX_ADDRESS, MIR_LOAD_INDIRECT,
        MIR_UNARY, MIR_UNARY, MIR_ARG, MIR_CALL, MIR_UNARY,
        MIR_STORE_INDIRECT, MIR_LABEL, MIR_NOP, MIR_CONST,
        MIR_BINARY, MIR_STORE, MIR_JUMP, MIR_LABEL, MIR_NOP,
        MIR_NOP, MIR_INDEX_ADDRESS, MIR_NOP, MIR_CONST,
        MIR_STORE_INDIRECT
    };
    struct Sym *uppercase_function;
    int arguments[1];
    int instruction;
    long limit;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 55 || mir.next_value != 38 ||
        mir_cfg_block_count() != 7 || mir.local_bytes != 2 ||
        mir.aggregate_temp_bytes != 0 || mir.has_vla ||
        !mir_has_cfg_backedge() ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
            expected_opcodes[instruction])
            return mir_machine_reject(
                "fortran-uppercase", "opcodes");
    if (!mir_machine_parameter_value_offset(
            mir.insns[1].dst,
            &plan->destination_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst,
            &plan->source_stack_offset) ||
        plan->destination_stack_offset != 2 ||
        plan->source_stack_offset != 4 ||
        type_ptr_depth(mir.insns[1].type) != 1 ||
        type_ptr_depth(mir.insns[2].type) != 1 ||
        (mir.insns[1].type & 15) != TYPE_CHAR ||
        (mir.insns[2].type & 15) != TYPE_CHAR ||
        type_size(mir.insns[1].type) != 2 ||
        type_size(mir.insns[2].type) != 2 ||
        mir_machine_pointee_is_volatile(&mir.insns[1]) ||
        mir_machine_pointee_is_volatile(&mir.insns[2]))
        return mir_machine_reject(
            "fortran-uppercase", "parameters");
    if (!mir_machine_constant_equals(mir.insns[3].dst, 0) ||
        !mir_machine_unobservable_local_store(&mir.insns[5]) ||
        mir.insns[5].src1 != mir.insns[3].dst ||
        mir.insns[9].src1 != mir.insns[3].dst ||
        mir.insns[9].src2 != mir.insns[45].dst ||
        mir.insns[9].phi_pred1 != mir.insns[0].label ||
        mir.insns[9].phi_pred2 != mir.insns[42].label ||
        mir.insns[9].object != mir.insns[5].object ||
        mir.insns[12].src1 != mir.insns[2].dst ||
        mir.insns[12].src2 != mir.insns[9].dst ||
        mir.insns[12].immediate != 1 ||
        mir.insns[12].memory_size != 1 ||
        mir.insns[13].src1 != mir.insns[12].dst ||
        mir.insns[13].memory_size != 1 ||
        mir.insns[14].src1 != mir.insns[13].dst ||
        mir.insns[14].label != mir.insns[24].label ||
        !mir_machine_evaluate_constant(
            mir.insns[18].dst, &limit, 0) ||
        limit <= 0 || limit > 255 ||
        mir.insns[19].src1 != mir.insns[9].dst ||
        mir.insns[19].src2 != mir.insns[18].dst ||
        mir.insns[19].immediate != '<' ||
        mir.insns[20].src1 != mir.insns[19].dst ||
        mir.insns[20].label != mir.insns[24].label ||
        !mir_machine_constant_equals(mir.insns[22].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[25].dst, 0) ||
        mir.insns[27].src1 != mir.insns[22].dst ||
        mir.insns[27].src2 != mir.insns[25].dst ||
        mir.insns[27].phi_pred1 != mir.insns[21].label ||
        mir.insns[27].phi_pred2 != mir.insns[24].label ||
        mir.insns[28].src1 != mir.insns[27].dst ||
        mir.insns[28].label != mir.insns[48].label)
        return mir_machine_reject(
            "fortran-uppercase", "loop");
    if (mir.insns[31].src1 != mir.insns[1].dst ||
        mir.insns[31].src2 != mir.insns[9].dst ||
        mir.insns[31].immediate != 1 ||
        mir.insns[31].memory_size != 1 ||
        mir.insns[34].src1 != mir.insns[2].dst ||
        mir.insns[34].src2 != mir.insns[9].dst ||
        mir.insns[34].immediate != 1 ||
        mir.insns[34].memory_size != 1 ||
        mir.insns[35].src1 != mir.insns[34].dst ||
        mir.insns[35].memory_size != 1 ||
        mir.insns[36].src1 != mir.insns[35].dst ||
        mir.insns[37].src1 != mir.insns[36].dst ||
        !mir_machine_call_arguments(
            &mir.insns[39], 1, arguments) ||
        arguments[0] != mir.insns[37].dst ||
        (uppercase_function =
             mir_interpreter_file_function(39, 0, 1)) == NULL ||
        strcmp(asm_name_for(sym_asm_name(uppercase_function)),
               "__ctu") ||
        mir.insns[40].src1 != mir.insns[39].dst ||
        mir.insns[41].src1 != mir.insns[31].dst ||
        mir.insns[41].src2 != mir.insns[40].dst ||
        mir.insns[41].memory_size != 1)
        return mir_machine_reject(
            "fortran-uppercase", "conversion");
    if (!mir_machine_constant_equals(mir.insns[44].dst, 1) ||
        mir.insns[45].src1 != mir.insns[9].dst ||
        mir.insns[45].src2 != mir.insns[44].dst ||
        mir.insns[45].immediate != '+' ||
        mir.insns[46].src1 != mir.insns[45].dst ||
        mir.insns[47].label != mir.insns[6].label ||
        mir.insns[51].src1 != mir.insns[1].dst ||
        mir.insns[51].src2 != mir.insns[9].dst ||
        mir.insns[51].immediate != 1 ||
        mir.insns[51].memory_size != 1 ||
        !mir_machine_constant_equals(mir.insns[53].dst, 0) ||
        mir.insns[54].src1 != mir.insns[51].dst ||
        mir.insns[54].src2 != mir.insns[53].dst ||
        mir.insns[54].memory_size != 1)
        return mir_machine_reject(
            "fortran-uppercase", "completion");
    plan->limit = (int)limit;
    return 1;
}

static void mir_emit_fortran_uppercase_schedule(
    MirStream *out, const struct MirFortranUppercaseSchedule *plan)
{
    int loop = new_label();
    int store = new_label();
    int done = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tld hl,2\n\tadd hl,sp\n"
          "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tinc hl\n\tld a,(hl)\n\tinc hl\n"
          "\tld h,(hl)\n\tld l,a\n", out);
    mir_stream_printf(out, "\tld b,%d\nL%d:\n", plan->limit, loop);
    mir_stream_puts("\tld a,(hl)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", done);
    mir_stream_puts("\tcp 97\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n", store);
    mir_stream_puts("\tcp 123\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n\tsub 32\nL%d:\n"
            "\tld (de),a\n\tinc de\n\tinc hl\n"
            "\tdjnz L%d\nL%d:\n\txor a\n\tld (de),a\n\tret\n",
            store, store, loop, done);
}

static int mir_match_fortran_declaration_schedule(
    struct MirFortranDeclarationSchedule *plan)
{
    int instruction;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 394 || mir_cfg_block_count() != 68 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode !=
                mir_fortran_parse_decl_opcodes[instruction])
            return 0;
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_flags & (1 | 8)) != 0)
            return 0;
    }
    if (!mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->source_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst, &plan->type_stack_offset) ||
        plan->type_stack_offset != plan->source_stack_offset + 2 ||
        type_ptr_depth(mir.insns[1].type) != 1 ||
        type_size(mir.insns[1].type) != 2 ||
        type_size(mir.insns[2].type) != 2)
        return 0;
    plan->space_function =
        mir_interpreter_file_function(17, 0, 1);
    plan->alnum_function =
        mir_interpreter_file_function(107, 0, 1);
    plan->upper_function =
        mir_interpreter_file_function(159, 0, 1);
    plan->digit_function =
        mir_interpreter_file_function(274, 0, 1);
    plan->find_function =
        mir_interpreter_file_function(345, 0, 1);
    plan->add_function =
        mir_interpreter_file_function(357, 0, 4);
    if (plan->space_function == NULL ||
        plan->alnum_function == NULL ||
        plan->upper_function == NULL ||
        plan->digit_function == NULL ||
        plan->find_function == NULL ||
        plan->add_function == NULL ||
        !mir_interpreter_file_same_function(
            plan->space_function, 49, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->space_function, 201, 0, 1) ||
        !mir_interpreter_file_same_function(
            plan->space_function, 242, 0, 1))
        return 0;
    return 1;
}

static void mir_fortran_declaration_word_address(MirStream *out)
{
    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-24\n\tadd hl,de\n",
          out);
}

static void mir_fortran_declaration_call_character(
    MirStream *out, struct Sym *function)
{
    mir_stream_puts("\tld l,(ix-34)\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, function);
    mir_stream_puts("\tpop bc\n", out);
}

static void mir_emit_fortran_declaration_schedule(
    MirStream *out, const struct MirFortranDeclarationSchedule *plan)
{
    int find_start = new_label();
    int outer = new_label();
    int separators = new_label();
    int separator_accept = new_label();
    int have_entry = new_label();
    int name_loop = new_label();
    int name_done = new_label();
    int name_accept = new_label();
    int name_no_store = new_label();
    int spaces = new_label();
    int no_array = new_label();
    int array_spaces = new_label();
    int digits = new_label();
    int digits_done = new_label();
    int close_scan = new_label();
    int close_done = new_label();
    int array_close_loop = new_label();
    int array_close_found = new_label();
    int no_add = new_label();
    int comma_scan = new_label();
    int done = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-34\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tpush hl\n\tpop iy\nL%d:\n"
            "\tld a,(iy+0)\n\tor a\n\tjp z,L%d\n"
            "\tld (ix-34),a\n",
            plan->source_stack_offset + 4,
            plan->source_stack_offset + 5,
            find_start, outer);
    mir_fortran_declaration_call_character(
        out, plan->space_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tinc iy\n\tjp L%d\nL%d:\n",
            outer, find_start, outer);
    mir_stream_printf(out, "L%d:\n", separators);
    mir_stream_puts("\tld a,(iy+0)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-34),a\n", done);
    mir_fortran_declaration_call_character(
        out, plan->space_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", separator_accept);
    mir_stream_puts("\tld a,(ix-34)\n\tcp ','\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n\tinc iy\n\tjp L%d\n"
            "L%d:\n\txor a\n\tld (ix-26),a\nL%d:\n",
            have_entry, separator_accept, separators,
            have_entry, name_loop);
    mir_stream_puts("\tld a,(iy+0)\n\tld (ix-34),a\n", out);
    mir_fortran_declaration_call_character(
        out, plan->alnum_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", name_accept);
    mir_stream_puts("\tld a,(ix-34)\n\tcp '_'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n",
            name_done, name_accept);
    mir_stream_puts("\tld a,(ix-26)\n\tcp 23\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", name_no_store);
    mir_stream_puts("\tld a,(ix-34)\n", out);
    mir_fortran_declaration_call_character(
        out, plan->upper_function);
    mir_stream_puts("\tld (ix-34),l\n", out);
    mir_fortran_declaration_word_address(out);
    mir_stream_puts("\tld e,(ix-26)\n\tld d,0\n\tadd hl,de\n"
          "\tld a,(ix-34)\n\tld (hl),a\n\tinc (ix-26)\n", out);
    mir_stream_printf(out, "L%d:\n\tinc iy\n\tjp L%d\nL%d:\n",
            name_no_store, name_loop, name_done);
    mir_fortran_declaration_word_address(out);
    mir_stream_puts("\tld e,(ix-26)\n\tld d,0\n\tadd hl,de\n\tld (hl),0\n"
          "\tld a,(ix-24)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld hl,1\n", done);
    mir_interpreter_file_store_ix_word(out, -30);
    mir_stream_puts("\tld hl,0\n", out);
    mir_interpreter_file_store_ix_word(out, -32);
    mir_stream_printf(out, "L%d:\n", spaces);
    mir_stream_puts("\tld a,(iy+0)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-34),a\n", no_array);
    mir_fortran_declaration_call_character(
        out, plan->space_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tinc iy\n\tjp L%d\nL%d:\n"
            "\tld a,(iy+0)\n\tcp '('\n\tjp nz,L%d\n\tinc iy\n"
            "L%d:\n",
            array_spaces, spaces, array_spaces, no_array,
            digits);
    mir_stream_puts("\tld a,(iy+0)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-34),a\n", digits_done);
    mir_fortran_declaration_call_character(
        out, plan->space_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tinc iy\n\tjp L%d\nL%d:\n",
            digits_done, digits, digits_done);
    mir_stream_puts("\tld hl,0\n", out);
    mir_interpreter_file_store_ix_word(out, -28);
    mir_stream_printf(out, "L%d:\n\tld a,(iy+0)\n\tld (ix-34),a\n",
            close_scan);
    mir_fortran_declaration_call_character(
        out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", close_done);
    mir_interpreter_file_load_ix_word(out, -28);
    mir_stream_puts("\tld de,0\n\tld e,l\n\tld d,h\n"
          "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
          "\tadd hl,hl\n\tld a,(ix-34)\n\tsub '0'\n"
          "\tld e,a\n\tld d,0\n\tadd hl,de\n", out);
    mir_interpreter_file_store_ix_word(out, -28);
    mir_stream_puts("\tinc iy\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", close_scan, close_done);
    mir_interpreter_file_load_ix_word(out, -28);
    mir_interpreter_file_store_ix_word(out, -32);
    mir_stream_puts("\tld hl,2\n", out);
    mir_interpreter_file_store_ix_word(out, -30);
    mir_stream_printf(out, "L%d:\n\tld a,(iy+0)\n\tor a\n"
            "\tjp z,L%d\n\tcp ')'\n\tjp z,L%d\n"
            "\tinc iy\n\tjp L%d\nL%d:\n"
            "\tld a,(iy+0)\n\tcp ')'\n\tjp nz,L%d\n\tinc iy\n"
            "L%d:\n",
            array_close_loop, no_array, array_close_found,
            array_close_loop, array_close_found,
            no_array, no_array);
    mir_fortran_declaration_word_address(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->find_function);
    mir_stream_puts("\tpop bc\n\tbit 7,h\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", no_add);
    mir_interpreter_file_load_ix_word(out, -32);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            plan->type_stack_offset + 4,
            plan->type_stack_offset + 5);
    mir_interpreter_file_load_ix_word(out, -30);
    mir_stream_puts("\tpush hl\n", out);
    mir_fortran_declaration_word_address(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->add_function);
    mir_interpreter_file_cleanup(out, 4);
    mir_stream_printf(out, "L%d:\nL%d:\n\tld a,(iy+0)\n\tor a\n"
            "\tjp z,L%d\n\tcp ','\n\tjp z,L%d\n"
            "\tinc iy\n\tjp L%d\n",
            no_add, comma_scan, done, separators, comma_scan);
    mir_stream_printf(out, "L%d:\n\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n",
            done);
}

static int mir_match_fortran_source_schedule(
    struct MirFortranSourceSchedule *plan)
{
    int instruction;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 337 || mir_cfg_block_count() != 51 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
                mir_fortran_parse_source_opcodes[instruction])
            return 0;
    plan->length = mir_pascal_scan_symbol(11, 4, 0);
    plan->source = mir_pascal_scan_symbol(25, 2, 0);
    plan->trim_function =
        mir_interpreter_file_function(218, 0, 1);
    plan->digit_function =
        mir_interpreter_file_function(247, 0, 1);
    plan->space_function =
        mir_interpreter_file_function(288, 0, 1);
    plan->copy_function =
        mir_interpreter_file_function(311, 0, 2);
    plan->prefix_function =
        mir_interpreter_file_function(319, 0, 2);
    plan->append_function =
        mir_interpreter_file_function(332, 0, 3);
    plan->subroutine_string_id = (int)mir.insns[317].immediate;
    if (plan->length == NULL || plan->source == NULL ||
        plan->trim_function == NULL ||
        plan->digit_function == NULL ||
        plan->space_function == NULL ||
        plan->copy_function == NULL ||
        plan->prefix_function == NULL ||
        plan->append_function == NULL ||
        plan->subroutine_string_id < 0 ||
        !mir_pascal_scan_same_symbol(plan->length, 22) ||
        !mir_pascal_scan_same_symbol(plan->length, 69) ||
        !mir_pascal_scan_same_symbol(plan->length, 100) ||
        !mir_pascal_scan_same_symbol(plan->source, 55) ||
        !mir_pascal_scan_same_symbol(plan->source, 103) ||
        !mir_pascal_scan_same_symbol(plan->source, 130))
        return 0;
    return 1;
}

static void mir_fortran_source_line_address(
    MirStream *out, int helper_label)
{
    mir_stream_printf(out, "\tcall L%d\n", helper_label);
}

static void mir_fortran_source_line_address_body(MirStream *out)
{
    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-170\n\tadd hl,de\n",
          out);
}

static void mir_fortran_source_body_address(
    MirStream *out, int helper_label)
{
    mir_stream_printf(out, "\tcall L%d\n", helper_label);
}

static void mir_fortran_source_body_address_body(MirStream *out)
{
    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-330\n\tadd hl,de\n",
          out);
}

static void mir_fortran_source_load_char(
    MirStream *out, const struct MirFortranSourceSchedule *plan)
{
    mir_machine_emit_global_word(out, plan->source, 0);
    mir_stream_puts("\tld e,(ix-2)\n\tld d,(ix-1)\n"
          "\tadd hl,de\n\tld a,(hl)\n", out);
}

static void mir_fortran_source_increment_p(MirStream *out)
{
    mir_stream_puts("\tld l,(ix-2)\n\tld h,(ix-1)\n\tinc hl\n"
          "\tld (ix-2),l\n\tld (ix-1),h\n", out);
}

static void mir_fortran_source_call_char(
    MirStream *out, struct Sym *function)
{
    mir_stream_puts("\tld l,(ix-8)\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, function);
    mir_stream_puts("\tpop bc\n", out);
}

static void mir_emit_fortran_source_schedule(
    MirStream *out, const struct MirFortranSourceSchedule *plan)
{
    int outer = new_label();
    int line_loop = new_label();
    int line_done = new_label();
    int no_marker = new_label();
    int no_store = new_label();
    int skip_rest = new_label();
    int skip_rest_done = new_label();
    int no_newline = new_label();
    int ignore_line = new_label();
    int label_loop = new_label();
    int label_done = new_label();
    int spaces = new_label();
    int spaces_done = new_label();
    int no_subroutine = new_label();
    int done = new_label();
    int line_address_helper = new_label();
    int body_address_helper = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-330\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_puts("\tld hl,0\n\tld (ix-2),l\n\tld (ix-1),h\n"
          "\tld (ix-4),l\n\tld (ix-3),h\n", out);
    mir_stream_printf(out, "L%d:\n", outer);
    mir_stream_puts("\tld l,(ix-2)\n\tld h,(ix-1)\n\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n\tjp z,L%d\n"
            "\txor a\n\tld (ix-6),a\n"
            "\tld (ix-5),a\nL%d:\n",
            done, done, line_loop);
    mir_stream_puts("\tld l,(ix-2)\n\tld h,(ix-1)\n\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n\tjp z,L%d\n",
            line_done, line_done);
    mir_fortran_source_load_char(out, plan);
    mir_stream_puts("\tcp 10\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld a,(ix-6)\n\tcp 159\n"
            "\tjp nc,L%d\n",
            line_done, line_done);
    mir_fortran_source_load_char(out, plan);
    mir_stream_puts("\tld (ix-8),a\n", out);
    mir_fortran_source_increment_p(out);
    mir_stream_puts("\tld a,(ix-8)\n\tcp 26\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_marker);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tld (ix-2),l\n\tld (ix-1),h\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", line_done, no_marker);
    mir_stream_puts("\tld a,(ix-8)\n\tcp 13\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", no_store);
    mir_fortran_source_line_address(out, line_address_helper);
    mir_stream_puts("\tld e,(ix-6)\n\tld d,0\n\tadd hl,de\n"
          "\tld a,(ix-8)\n\tld (hl),a\n\tinc (ix-6)\n", out);
    mir_stream_printf(out, "L%d:\n\tjp L%d\nL%d:\n",
            no_store, line_loop, line_done);
    mir_stream_printf(out, "L%d:\n", skip_rest);
    mir_stream_puts("\tld l,(ix-2)\n\tld h,(ix-1)\n\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n\tjp z,L%d\n",
            skip_rest_done, skip_rest_done);
    mir_fortran_source_load_char(out, plan);
    mir_stream_puts("\tcp 10\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", skip_rest_done);
    mir_fortran_source_increment_p(out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", skip_rest, skip_rest_done);
    mir_stream_puts("\tld l,(ix-2)\n\tld h,(ix-1)\n\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->length, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n\tjp z,L%d\n",
            no_newline, no_newline);
    mir_fortran_source_load_char(out, plan);
    mir_stream_puts("\tcp 10\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", no_newline);
    mir_fortran_source_increment_p(out);
    mir_stream_printf(out, "L%d:\n", no_newline);
    mir_fortran_source_line_address(out, line_address_helper);
    mir_stream_puts("\tld e,(ix-6)\n\tld d,0\n\tadd hl,de\n\tld (hl),0\n",
          out);
    mir_fortran_source_line_address(out, line_address_helper);
    mir_stream_puts("\tld a,(hl)\n\tcp 'C'\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tcp 'c'\n\tjp z,L%d\n"
            "\tcp '#'\n\tjp z,L%d\n",
            ignore_line, ignore_line, ignore_line);
    mir_fortran_source_line_address(out, line_address_helper);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->trim_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_fortran_source_line_address(out, line_address_helper);
    mir_stream_puts("\tld a,(hl)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\txor a\n"
            "\tld (ix-10),a\n\tld (ix-9),a\n"
            "\tld (ix-6),a\n\tld (ix-5),a\nL%d:\n",
            ignore_line, label_loop);
    mir_fortran_source_line_address(out, line_address_helper);
    mir_stream_puts("\tld e,(ix-6)\n\tld d,0\n\tadd hl,de\n"
          "\tld a,(hl)\n\tld (ix-8),a\n", out);
    mir_fortran_source_call_char(out, plan->digit_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", label_done);
    mir_stream_puts("\tld l,(ix-10)\n\tld h,(ix-9)\n"
          "\tld de,0\n\tld e,l\n\tld d,h\n"
          "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n\tadd hl,hl\n"
          "\tld a,(ix-8)\n\tsub '0'\n\tld e,a\n\tld d,0\n"
          "\tadd hl,de\n\tld (ix-10),l\n\tld (ix-9),h\n"
          "\tinc (ix-6)\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", label_loop, label_done);
    mir_stream_printf(out, "L%d:\n", spaces);
    mir_fortran_source_line_address(out, line_address_helper);
    mir_stream_puts("\tld e,(ix-6)\n\tld d,0\n\tadd hl,de\n"
          "\tld a,(hl)\n\tor a\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-8),a\n", spaces_done);
    mir_fortran_source_call_char(out, plan->space_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tinc (ix-6)\n\tjp L%d\nL%d:\n",
            spaces_done, spaces, spaces_done);
    mir_fortran_source_line_address(out, line_address_helper);
    mir_stream_puts("\tld e,(ix-6)\n\tld d,0\n\tadd hl,de\n\tpush hl\n",
          out);
    mir_fortran_source_body_address(out, body_address_helper);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->copy_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_fortran_source_body_address(out, body_address_helper);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->trim_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
            plan->subroutine_string_id);
    mir_fortran_source_body_address(out, body_address_helper);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->prefix_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n"
            "\tld l,(ix-4)\n\tld h,(ix-3)\n\tinc hl\n"
            "\tld (ix-4),l\n\tld (ix-3),h\nL%d:\n",
            no_subroutine, no_subroutine);
    mir_stream_puts("\tld l,(ix-4)\n\tld h,(ix-3)\n\tpush hl\n", out);
    mir_fortran_source_body_address(out, body_address_helper);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_puts("\tld l,(ix-10)\n\tld h,(ix-9)\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->append_function);
    mir_interpreter_file_cleanup(out, 3);
    mir_stream_printf(out, "L%d:\n\tjp L%d\nL%d:\n"
            "\tld sp,ix\n\tpop ix\n\tret\n",
            ignore_line, outer, done);
    mir_stream_printf(out, "L%d:\n", line_address_helper);
    mir_fortran_source_line_address_body(out);
    mir_stream_puts("\tret\n", out);
    mir_stream_printf(out, "L%d:\n", body_address_helper);
    mir_fortran_source_body_address_body(out);
    mir_stream_puts("\tret\n", out);
}

static int mir_match_fortran_lhs_schedule(
    struct MirFortranLhsSchedule *plan)
{
    int instruction;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 158 || mir_cfg_block_count() != 22 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
                mir_fortran_parse_lhs_opcodes[instruction])
            return 0;
    if (!mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->lhs_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst,
            &plan->symbol_pointer_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[3].dst,
            &plan->index_pointer_stack_offset) ||
        plan->symbol_pointer_stack_offset !=
            plan->lhs_stack_offset + 2 ||
        plan->index_pointer_stack_offset !=
            plan->symbol_pointer_stack_offset + 2)
        return 0;
    plan->trim_function =
        mir_interpreter_file_function(9, 0, 1);
    plan->alnum_function =
        mir_interpreter_file_function(23, 0, 1);
    plan->upper_function =
        mir_interpreter_file_function(75, 0, 1);
    plan->ensure_function =
        mir_interpreter_file_function(91, 0, 1);
    plan->space_function =
        mir_interpreter_file_function(110, 0, 1);
    plan->search_function =
        mir_interpreter_file_function(141, 0, 2);
    plan->duplicate_function =
        mir_interpreter_file_function(154, 0, 1);
    return plan->trim_function != NULL &&
           plan->alnum_function != NULL &&
           plan->upper_function != NULL &&
           plan->ensure_function != NULL &&
           plan->space_function != NULL &&
           plan->search_function != NULL &&
           plan->duplicate_function != NULL;
}

static void mir_fortran_lhs_name_address(MirStream *out)
{
    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-24\n\tadd hl,de\n",
          out);
}

static void mir_fortran_lhs_call_char(
    MirStream *out, struct Sym *function)
{
    mir_stream_puts("\tld l,(ix-28)\n\tld h,0\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, function);
    mir_stream_puts("\tpop bc\n", out);
}

static void mir_emit_fortran_lhs_schedule(
    MirStream *out, const struct MirFortranLhsSchedule *plan)
{
    int loop = new_label();
    int accept = new_label();
    int done_name = new_label();
    int spaces = new_label();
    int spaces_done = new_label();
    int no_index = new_label();
    int no_close = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-28\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            plan->lhs_stack_offset + 4,
            plan->lhs_stack_offset + 5);
    mir_machine_emit_symbol_call(out, plan->trim_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tpush hl\n\tpop iy\n\txor a\n\tld (ix-26),a\n"
            "L%d:\n\tld a,(iy+0)\n\tld (ix-28),a\n",
            plan->lhs_stack_offset + 4,
            plan->lhs_stack_offset + 5, loop);
    mir_fortran_lhs_call_char(out, plan->alnum_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", accept);
    mir_stream_puts("\tld a,(ix-28)\n\tcp '_'\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n",
            done_name, accept);
    mir_stream_puts("\tld a,(ix-26)\n\tcp 23\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", done_name);
    mir_stream_puts("\tld a,(ix-28)\n", out);
    mir_fortran_lhs_call_char(out, plan->upper_function);
    mir_stream_puts("\tld (ix-28),l\n", out);
    mir_fortran_lhs_name_address(out);
    mir_stream_puts("\tld e,(ix-26)\n\tld d,0\n\tadd hl,de\n"
          "\tld a,(ix-28)\n\tld (hl),a\n\tinc (ix-26)\n"
          "\tinc iy\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done_name);
    mir_fortran_lhs_name_address(out);
    mir_stream_puts("\tld e,(ix-26)\n\tld d,0\n\tadd hl,de\n\tld (hl),0\n",
          out);
    mir_fortran_lhs_name_address(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->ensure_function);
    mir_stream_puts("\tpop bc\n\tex de,hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (hl),e\n\tinc hl\n\tld (hl),d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\txor a\n\tld (hl),a\n\tinc hl\n\tld (hl),a\n",
            plan->symbol_pointer_stack_offset + 4,
            plan->symbol_pointer_stack_offset + 5,
            plan->index_pointer_stack_offset + 4,
            plan->index_pointer_stack_offset + 5);
    mir_stream_printf(out, "L%d:\n\tld a,(iy+0)\n\tor a\n",
            spaces);
    mir_stream_printf(out, "\tjp z,L%d\n\tld (ix-28),a\n",
            spaces_done);
    mir_fortran_lhs_call_char(out, plan->space_function);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\tinc iy\n\tjp L%d\nL%d:\n"
            "\tld a,(iy+0)\n\tcp '('\n\tjp nz,L%d\n\tinc iy\n"
            "\tld hl,')'\n\tpush hl\n\tpush iy\n",
            spaces_done, spaces, spaces_done, no_index);
    mir_machine_emit_symbol_call(out, plan->search_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n\txor a\n\tld (hl),a\nL%d:\n"
            "\tpush iy\n",
            no_close, no_close);
    mir_machine_emit_symbol_call(out, plan->duplicate_function);
    mir_stream_puts("\tpop bc\n\tex de,hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (hl),e\n\tinc hl\n\tld (hl),d\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n",
            plan->index_pointer_stack_offset + 4,
            plan->index_pointer_stack_offset + 5,
            no_index);
}

static int mir_match_fortran_main_schedule(
    struct MirFortranMainSchedule *plan)
{
    int pipeline;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 96 || mir_cfg_block_count() != 14 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_INT ||
        !mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->argc_stack_offset) ||
        !mir_machine_parameter_value_offset(
            mir.insns[2].dst, &plan->argv_stack_offset) ||
        plan->argv_stack_offset != plan->argc_stack_offset + 2)
        return 0;
    plan->initialize_function =
        mir_interpreter_file_function(3, 0, 0);
    plan->compare_function =
        mir_interpreter_file_function(18, 0, 2);
    plan->verbose = mir_pascal_scan_symbol(57, 2, 0);
    plan->print_function = find_global(mir.insns[73].name);
    plan->load_function =
        mir_interpreter_file_function(83, 0, 1);
    plan->pipeline[0] =
        mir_interpreter_file_function(89, 0, 0);
    plan->pipeline[1] =
        mir_interpreter_file_function(90, 0, 0);
    plan->pipeline[2] =
        mir_interpreter_file_function(91, 0, 0);
    plan->pipeline[3] =
        mir_interpreter_file_function(92, 0, 0);
    plan->pipeline[4] =
        mir_interpreter_file_function(93, 0, 0);
    if (plan->initialize_function == NULL ||
        plan->compare_function == NULL ||
        plan->verbose == NULL ||
        plan->print_function == NULL ||
        plan->load_function == NULL ||
        !mir_interpreter_file_same_function(
            plan->compare_function, 32, 0, 2))
        return 0;
    for (pipeline = 0; pipeline < 5; ++pipeline)
        if (plan->pipeline[pipeline] == NULL)
            return 0;
    plan->strings[0] = (int)mir.insns[16].immediate;
    plan->strings[1] = (int)mir.insns[30].immediate;
    plan->strings[2] = (int)mir.insns[71].immediate;
    if (plan->strings[0] < 0 || plan->strings[1] < 0 ||
        plan->strings[2] < 0)
        return 0;
    snprintf(
        plan->print_name, sizeof(plan->print_name), "%s",
        mir.insns[73].base_name[0] != 0
            ? mir.insns[73].base_name
            : asm_name_for(sym_asm_name(plan->print_function)));
    return plan->print_name[0] != 0;
}

static void mir_fortran_main_argument(
    MirStream *out, const struct MirFortranMainSchedule *plan)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix-2)\n\tld d,(ix-1)\n"
            "\tadd hl,de\n\tadd hl,de\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n",
            plan->argv_stack_offset + 2,
            plan->argv_stack_offset + 3);
}

static void mir_emit_fortran_main_schedule(
    MirStream *out, const struct MirFortranMainSchedule *plan)
{
    int no_option = new_label();
    int option = new_label();
    int have_file = new_label();
    int loaded = new_label();
    int epilogue = new_label();
    int pipeline;

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tdec sp\n\tdec sp\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_symbol_call(out, plan->initialize_function);
    mir_stream_puts("\tld hl,1\n\tld (ix-2),l\n\tld (ix-1),h\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,1\n\tor a\n\tsbc hl,de\n"
            "\tjp c,L%d\n\tjp z,L%d\n",
            plan->argc_stack_offset + 2,
            plan->argc_stack_offset + 3,
            no_option, no_option);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->strings[0]);
    mir_fortran_main_argument(out, plan);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->compare_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", option);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->strings[1]);
    mir_fortran_main_argument(out, plan);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->compare_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\nL%d:\n\tld hl,1\n",
            no_option, option);
    mir_machine_emit_global_word_store(out, plan->verbose, 0);
    mir_stream_puts("\tld hl,2\n\tld (ix-2),l\n\tld (ix-1),h\n", out);
    mir_stream_printf(out, "L%d:\n", no_option);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix-2)\n\tld d,(ix-1)\n"
            "\tor a\n\tsbc hl,de\n\tjp nz,L%d\n",
            plan->argc_stack_offset + 2,
            plan->argc_stack_offset + 3, have_file);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n\tld hl,2\n\tpush hl\n",
            plan->strings[2]);
    mir_emit_runtime_call(out, plan->print_name);
    mir_interpreter_file_cleanup(out, 2);
    mir_stream_puts("\tld hl,1\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", epilogue, have_file);
    mir_fortran_main_argument(out, plan);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->load_function);
    mir_stream_puts("\tpop bc\n\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tld hl,1\n\tjp L%d\nL%d:\n",
            loaded, epilogue, loaded);
    for (pipeline = 0; pipeline < 5; ++pipeline)
        mir_machine_emit_symbol_call(out, plan->pipeline[pipeline]);
    mir_stream_printf(out,
            "\tld hl,0\nL%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            epilogue);
}

static int mir_match_fortran_grow_schedule(
    struct MirFortranGrowSchedule *plan)
{
    long value;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 92 || mir_cfg_block_count() != 11 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID ||
        !mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->need_stack_offset))
        return 0;
    plan->capacity = mir_pascal_scan_symbol(3, 2, 0);
    plan->memory = mir_pascal_scan_symbol(56, 2, 0);
    plan->failure_function = find_global(mir.insns[14].name);
    plan->resize_function =
        mir_interpreter_file_function(62, 0, 2);
    plan->clear_function =
        mir_interpreter_file_function(85, 0, 3);
    plan->full_string_id = (int)mir.insns[12].immediate;
    plan->memory_string_id = (int)mir.insns[69].immediate;
    if (plan->capacity == NULL || plan->memory == NULL ||
        plan->failure_function == NULL ||
        plan->resize_function == NULL ||
        plan->clear_function == NULL ||
        plan->full_string_id < 0 ||
        plan->memory_string_id < 0 ||
        !mir_pascal_scan_same_symbol(plan->capacity, 16) ||
        !mir_pascal_scan_same_symbol(plan->capacity, 74) ||
        !mir_pascal_scan_same_symbol(plan->capacity, 81) ||
        !mir_pascal_scan_same_symbol(plan->capacity, 91) ||
        !mir_pascal_scan_same_symbol(plan->memory, 88))
        return 0;
    if (!mir_machine_evaluate_constant(
            mir.insns[9].dst, &value, 0))
        return 0;
    plan->limit = (int)value;
    if (!mir_machine_evaluate_constant(
            mir.insns[27].dst, &value, 0))
        return 0;
    plan->small_limit = (int)value;
    if (!mir_machine_evaluate_constant(
            mir.insns[32].dst, &value, 0))
        return 0;
    plan->small_step = (int)value;
    if (!mir_machine_evaluate_constant(
            mir.insns[39].dst, &value, 0))
        return 0;
    plan->large_step = (int)value;
    return plan->limit > 0 &&
           plan->small_limit > 0 &&
           plan->small_step > 0 &&
           plan->large_step > 0;
}

static void mir_emit_fortran_grow_schedule(
    MirStream *out, const struct MirFortranGrowSchedule *plan)
{
    int need_growth = new_label();
    int within_limit = new_label();
    int loop = new_label();
    int large_step = new_label();
    int step_done = new_label();
    int loop_done = new_label();
    int clamp_done = new_label();
    int allocated = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-4\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tex de,hl\n",
            plan->need_stack_offset + 2,
            plan->need_stack_offset + 3);
    mir_machine_emit_global_word(out, plan->capacity, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n"
            "\tld sp,ix\n\tpop ix\n\tret\nL%d:\n",
            need_growth, need_growth);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp c,L%d\n\tld hl,S%d\n\tpush hl\n",
            plan->need_stack_offset + 2,
            plan->need_stack_offset + 3,
            plan->limit + 1,
            within_limit, plan->full_string_id);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", within_limit);
    mir_machine_emit_global_word(out, plan->capacity, 0);
    mir_interpreter_file_store_ix_word(out, -2);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc,L%d\n",
            plan->need_stack_offset + 2,
            plan->need_stack_offset + 3, loop_done);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nc,L%d\n",
            plan->small_limit, large_step);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n\tjp L%d\nL%d:\n",
            plan->small_step, step_done, large_step);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\nL%d:\n",
            plan->large_step, step_done);
    mir_interpreter_file_store_ix_word(out, -2);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, loop_done);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp c,L%d\n\tjp z,L%d\n\tld hl,%d\n",
            plan->limit, clamp_done, clamp_done, plan->limit);
    mir_interpreter_file_store_ix_word(out, -2);
    mir_stream_printf(out, "L%d:\n", clamp_done);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_global_word(out, plan->memory, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->resize_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_interpreter_file_store_ix_word(out, -4);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tld hl,S%d\n\tpush hl\n",
            allocated, plan->memory_string_id);
    mir_machine_emit_symbol_call(out, plan->failure_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", allocated);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->capacity, 0);
    mir_stream_puts("\tex de,hl\n\tor a\n\tsbc hl,de\n\tpush hl\n"
          "\tld hl,0\n\tpush hl\n", out);
    mir_machine_emit_global_word(out, plan->capacity, 0);
    mir_stream_puts("\tex de,hl\n", out);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tadd hl,de\n\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->clear_function);
    mir_interpreter_file_cleanup(out, 3);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_machine_emit_global_word_store(out, plan->memory, 0);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_machine_emit_global_word_store(out, plan->capacity, 0);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static int mir_match_fortran_fatal_schedule(
    struct MirFortranFatalSchedule *plan)
{
    const char *exit_name;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 81 || mir_cfg_block_count() != 18 ||
        mir.has_vla || mir.aggregate_temp_bytes != 0 ||
        (mir.return_type & 15) != TYPE_VOID ||
        !mir_machine_parameter_value_offset(
            mir.insns[1].dst, &plan->message_stack_offset))
        return 0;
    plan->statements = mir_pascal_scan_symbol(9, 2, 0);
    plan->program_counter = mir_pascal_scan_symbol(11, 2, 0);
    plan->statement_count = mir_pascal_scan_symbol(52, 2, 0);
    plan->print_function = find_global(mir.insns[77].name);
    plan->statement_stride = (int)mir.insns[24].immediate;
    plan->text_offset = (int)mir.insns[67].immediate;
    plan->format_string_id = (int)mir.insns[5].immediate;
    plan->empty_string_id = (int)mir.insns[72].immediate;
    if (plan->statements == NULL ||
        plan->program_counter == NULL ||
        plan->statement_count == NULL ||
        plan->print_function == NULL ||
        plan->statement_stride <= 0 ||
        plan->text_offset < 0 ||
        plan->text_offset + 1 >= plan->statement_stride ||
        plan->format_string_id < 0 ||
        plan->empty_string_id < 0)
        return 0;
    snprintf(
        plan->print_name, sizeof(plan->print_name), "%s",
        mir.insns[77].base_name[0] != 0
            ? mir.insns[77].base_name
            : asm_name_for(sym_asm_name(plan->print_function)));
    exit_name = mir.insns[80].base_name[0] != 0
        ? mir.insns[80].base_name
        : mir.insns[80].name;
    if (exit_name[0] == '_')
        dcc_copy_str(
            plan->exit_name, sizeof(plan->exit_name), exit_name);
    else if (strlen(exit_name) + 2 <= sizeof(plan->exit_name)) {
        plan->exit_name[0] = '_';
        dcc_copy_str(
            plan->exit_name + 1,
            sizeof(plan->exit_name) - 1, exit_name);
    }
    return plan->print_name[0] != 0 &&
           plan->exit_name[0] != 0;
}

static void mir_emit_fortran_fatal_schedule(
    MirStream *out, const struct MirFortranFatalSchedule *plan)
{
    int no_index = new_label();
    int index_done = new_label();
    int no_text = new_label();
    int text_done = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-4\n\tadd hl,sp\n\tld sp,hl\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_global_word(out, plan->statements, 0);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", no_index);
    mir_machine_emit_global_word(out, plan->program_counter, 0);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", no_index);
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->statements, 0);
    mir_stream_puts("\tex de,hl\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tld de,%d\n", plan->statement_stride);
    mir_emit_runtime_call(out, "__divs");
    mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,-1\nL%d:\n",
            index_done, no_index, index_done);
    mir_interpreter_file_store_ix_word(out, -2);

    mir_machine_emit_global_word(out, plan->statements, 0);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", no_text);
    mir_machine_emit_global_word(out, plan->program_counter, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_global_word(out, plan->statements, 0);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c,L%d\n", no_text);
    mir_machine_emit_global_word(out, plan->statement_count, 0);
    if (plan->statement_stride != 1) {
        mir_stream_printf(out, "\tld de,%d\n", plan->statement_stride);
        mir_emit_runtime_call(out, "__mulu");
    }
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->statements, 0);
    mir_stream_puts("\tadd hl,de\n\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->program_counter, 0);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nc,L%d\n", no_text);
    mir_machine_emit_global_word(out, plan->program_counter, 0);
    mir_machine_emit_hl_offset(out, plan->text_offset, 0);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n",
          out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,S%d\nL%d:\n",
            text_done, no_text, plan->empty_string_id, text_done);
    mir_interpreter_file_store_ix_word(out, -4);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tpush hl\n", out);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            plan->message_stack_offset + 2,
            plan->message_stack_offset + 3);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n\tld hl,2\n\tpush hl\n",
            plan->format_string_id);
    mir_emit_runtime_call(out, plan->print_name);
    mir_interpreter_file_cleanup(out, 5);
    mir_stream_puts("\tld hl,1\n\tpush hl\n", out);
    mir_emit_runtime_call(out, plan->exit_name);
    mir_stream_puts("\tpop bc\n\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static int mir_match_fortran_return_schedule(
    struct MirFortranReturnSchedule *plan)
{
    memset(plan, 0, sizeof(*plan));
    if (mir.count != 21 || mir_cfg_block_count() != 2 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID ||
        mir.insns[4].label != mir.insns[10].label)
        return 0;
    plan->call_count = mir_pascal_scan_symbol(1, 2, 0);
    plan->halted = mir_pascal_scan_symbol(7, 2, 0);
    plan->calls = mir_pascal_scan_symbol(11, 2, 0);
    plan->program_counter = mir_pascal_scan_symbol(20, 2, 0);
    plan->call_stride = (int)mir.insns[16].immediate;
    plan->return_offset = (int)mir.insns[17].immediate;
    return plan->call_count != NULL &&
           plan->halted != NULL &&
           plan->calls != NULL &&
           plan->program_counter != NULL &&
           plan->call_stride > 0 &&
           plan->return_offset >= 0 &&
           mir_machine_constant_equals(mir.insns[2].dst, 0) &&
           mir_machine_constant_equals(mir.insns[5].dst, 1) &&
           mir_machine_constant_equals(mir.insns[13].dst, 1);
}

static void mir_emit_fortran_return_schedule(
    MirStream *out, const struct MirFortranReturnSchedule *plan)
{
    int have_call = new_label();

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n", out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_machine_emit_global_word(out, plan->call_count, 0);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tld hl,1\n", have_call);
    mir_machine_emit_global_word_store(out, plan->halted, 0);
    mir_stream_puts("\tret\n", out);
    mir_stream_printf(out, "L%d:\n\tdec hl\n", have_call);
    mir_machine_emit_global_word_store(out, plan->call_count, 0);
    if (plan->call_stride == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    else {
        mir_stream_printf(out, "\tld de,%d\n", plan->call_stride);
        mir_emit_runtime_call(out, "__mulu");
    }
    mir_stream_puts("\tex de,hl\n", out);
    mir_machine_emit_global_word(out, plan->calls, 0);
    mir_stream_puts("\tadd hl,de\n", out);
    mir_machine_emit_hl_offset(out, plan->return_offset, 0);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n",
          out);
    mir_machine_emit_global_word_store(
        out, plan->program_counter, 0);
    mir_stream_puts("\tret\n", out);
}

static void mir_interpreter_file_cleanup(MirStream *out, int words)
{
    while (words-- > 0)
        mir_stream_puts("\tpop bc\n", out);
}

static void mir_interpreter_file_load_ix_word(
    MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            offset, offset + 1);
}

static void mir_interpreter_file_store_ix_word(
    MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld (ix%+d),l\n\tld (ix%+d),h\n",
            offset, offset + 1);
}

static void mir_interpreter_file_location_address(
    MirStream *out, const struct MirInterpreterFileLocation *location)
{
    if (location->kind == MIR_INTERPRETER_FILE_POINTER_STRUCT) {
        mir_machine_emit_global_word(out, location->root, 0);
        mir_machine_emit_hl_offset(out, location->offset, 0);
        return;
    }
    mir_machine_emit_global_address_de(
        out, location->root, location->offset);
    mir_stream_puts("\tex de,hl\n", out);
}

static void mir_interpreter_file_load_location(
    MirStream *out, const struct MirInterpreterFileLocation *location)
{
    if (location->kind != MIR_INTERPRETER_FILE_POINTER_STRUCT) {
        mir_machine_emit_global_word(
            out, location->root, location->offset);
        return;
    }
    mir_interpreter_file_location_address(out, location);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n",
          out);
}

static void mir_interpreter_file_store_location(
    MirStream *out, const struct MirInterpreterFileLocation *location)
{
    if (location->kind != MIR_INTERPRETER_FILE_POINTER_STRUCT) {
        mir_machine_emit_global_word_store(
            out, location->root, location->offset);
        return;
    }
    mir_stream_puts("\tpush hl\n", out);
    mir_interpreter_file_location_address(out, location);
    mir_stream_puts("\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
}

static void mir_interpreter_file_store_length(
    MirStream *out, const struct MirInterpreterFileLocation *location,
    int value_offset)
{
    mir_interpreter_file_location_address(out, location);
    mir_stream_printf(out,
            "\tld a,(ix%+d)\n\tld (hl),a\n\tinc hl\n"
            "\tld a,(ix%+d)\n\tld (hl),a\n\tinc hl\n"
            "\txor a\n\tld (hl),a\n\tinc hl\n\tld (hl),a\n",
            value_offset, value_offset + 1);
}

static void mir_interpreter_file_push_name(
    MirStream *out, const struct MirInterpreterFileSchedule *plan)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            plan->name_stack_offset + 2,
            plan->name_stack_offset + 3);
}

static void mir_interpreter_file_seek(
    MirStream *out, const struct MirInterpreterFileSchedule *plan,
    int whence)
{
    mir_stream_printf(out, "\tld hl,%d\n\tpush hl\n", whence);
    mir_stream_puts("\tld hl,0\n\tpush hl\n\tpush hl\n", out);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->seek_function);
    mir_interpreter_file_cleanup(out, 4);
}

static void mir_interpreter_file_print(
    MirStream *out, const struct MirInterpreterFileSchedule *plan,
    int string_id, int include_name)
{
    if (include_name)
        mir_interpreter_file_push_name(out, plan);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n\tld hl,2\n\tpush hl\n",
            string_id);
    mir_emit_runtime_call(out, plan->print_name);
    mir_interpreter_file_cleanup(out, include_name ? 3 : 2);
}

static void mir_emit_interpreter_file_schedule(
    MirStream *out, const struct MirInterpreterFileSchedule *plan)
{
    int opened = new_label();
    int first_seek_ok = new_label();
    int bounds_ok = new_label();
    int second_seek_ok = new_label();
    int allocated = new_label();
    int read_ok = new_label();
    int trim_loop = new_label();
    int trim_done = new_label();
    int return_zero = new_label();
    int return_one = new_label();
    int epilogue = new_label();
    int preprocess =
        plan->kind == MIR_INTERPRETER_FILE_PREPROCESS_POINTER ||
        plan->kind == MIR_INTERPRETER_FILE_PREPROCESS_STRUCT;
    int check_seeks =
        plan->kind == MIR_INTERPRETER_FILE_FORTH ||
        plan->kind == MIR_INTERPRETER_FILE_FORTRAN ||
        plan->kind == MIR_INTERPRETER_FILE_PASCAL;
    int seek_before_bounds =
        plan->kind == MIR_INTERPRETER_FILE_COBOL ||
        plan->kind == MIR_INTERPRETER_FILE_BASIC ||
        preprocess;
    int trim_ctrl_z =
        plan->kind == MIR_INTERPRETER_FILE_FORTH ||
        plan->kind == MIR_INTERPRETER_FILE_FORTRAN ||
        plan->kind == MIR_INTERPRETER_FILE_COBOL ||
        plan->kind == MIR_INTERPRETER_FILE_BASIC;

    mir_stream_puts(MIR_EXACT_KERNEL_MARKER "\n"
          "\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
          "\tld hl,-10\n\tadd hl,sp\n\tld sp,hl\n",
          out);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
            plan->mode_string_id);
    mir_interpreter_file_push_name(out, plan);
    mir_machine_emit_symbol_call(out, plan->open_function);
    mir_interpreter_file_cleanup(out, 2);
    mir_interpreter_file_store_ix_word(out, -2);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", opened);
    mir_interpreter_file_push_name(out, plan);
    mir_machine_emit_symbol_call(out, plan->perror_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", return_zero, opened);

    mir_interpreter_file_seek(out, plan, 2);
    if (check_seeks) {
        mir_stream_puts("\tld a,h\n\tor l\n", out);
        mir_stream_printf(out, "\tjp z,L%d\n", first_seek_ok);
        mir_interpreter_file_load_ix_word(out, -2);
        mir_stream_puts("\tpush hl\n", out);
        mir_machine_emit_symbol_call(out, plan->close_function);
        mir_stream_puts("\tpop bc\n", out);
        if (plan->kind == MIR_INTERPRETER_FILE_PASCAL) {
            mir_interpreter_file_push_name(out, plan);
            mir_machine_emit_symbol_call(out, plan->perror_function);
            mir_stream_puts("\tpop bc\n", out);
        }
        mir_stream_printf(out, "\tjp L%d\nL%d:\n",
                return_zero, first_seek_ok);
    }
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->tell_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_interpreter_file_store_ix_word(out, -4);
    mir_stream_puts("\tex de,hl\n", out);
    mir_interpreter_file_store_ix_word(out, -10);

    if (seek_before_bounds)
        mir_interpreter_file_seek(out, plan, 0);
    mir_interpreter_file_load_ix_word(out, -10);
    mir_stream_puts("\tbit 7,h\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tld a,h\n\tor l\n",
            trim_done);
    mir_stream_printf(out, "\tjp nz,L%d\n", trim_done);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp c,L%d\n",
            plan->maximum_source + 1, bounds_ok);
    mir_stream_printf(out, "L%d:\n", trim_done);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->close_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_interpreter_file_print(
        out, plan, plan->too_large_string_id,
        !preprocess);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", return_zero, bounds_ok);

    if (!seek_before_bounds) {
        mir_interpreter_file_seek(out, plan, 0);
        if (check_seeks) {
            mir_stream_puts("\tld a,h\n\tor l\n", out);
            mir_stream_printf(out, "\tjp z,L%d\n", second_seek_ok);
            mir_interpreter_file_load_ix_word(out, -2);
            mir_stream_puts("\tpush hl\n", out);
            mir_machine_emit_symbol_call(out, plan->close_function);
            mir_stream_puts("\tpop bc\n", out);
            if (plan->kind == MIR_INTERPRETER_FILE_PASCAL) {
                mir_interpreter_file_push_name(out, plan);
                mir_machine_emit_symbol_call(
                    out, plan->perror_function);
                mir_stream_puts("\tpop bc\n", out);
            }
            mir_stream_printf(out, "\tjp L%d\nL%d:\n",
                    return_zero, second_seek_ok);
        }
    }

    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts(plan->kind == MIR_INTERPRETER_FILE_BASIC
              ? "\tinc hl\n\tinc hl\n"
              : "\tinc hl\n",
          out);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->allocate_function);
    mir_stream_puts("\tpop bc\n", out);
    if (preprocess) {
        mir_interpreter_file_store_ix_word(out, -8);
    } else {
        mir_interpreter_file_store_location(out, &plan->source);
    }
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n", allocated);
    if (plan->kind == MIR_INTERPRETER_FILE_PASCAL) {
        mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
                plan->allocation_string_id);
        mir_machine_emit_symbol_call(out, plan->perror_function);
        mir_stream_puts("\tpop bc\n", out);
        mir_stream_printf(out, "\tjp L%d\n", return_one);
    } else if (!preprocess) {
        mir_interpreter_file_load_ix_word(out, -2);
        mir_stream_puts("\tpush hl\n", out);
        mir_machine_emit_symbol_call(out, plan->close_function);
        mir_stream_puts("\tpop bc\n", out);
        mir_stream_printf(out, "\tjp L%d\n", return_zero);
    } else {
        mir_stream_printf(out, "\tjp L%d\n", return_zero);
    }
    mir_stream_printf(out, "L%d:\n", allocated);

    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_puts("\tpush hl\n", out);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tpush hl\n\tld hl,1\n\tpush hl\n", out);
    if (preprocess)
        mir_interpreter_file_load_ix_word(out, -8);
    else
        mir_interpreter_file_load_location(out, &plan->source);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->read_function);
    mir_interpreter_file_cleanup(out, 4);
    mir_interpreter_file_store_ix_word(out, -6);
    mir_interpreter_file_load_ix_word(out, -2);
    mir_stream_puts("\tpush hl\n", out);
    mir_machine_emit_symbol_call(out, plan->close_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_interpreter_file_load_ix_word(out, -6);
    mir_stream_puts("\tex de,hl\n", out);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", read_ok);

    if (plan->kind == MIR_INTERPRETER_FILE_PASCAL) {
        mir_interpreter_file_print(
            out, plan, plan->read_string_id, 1);
        mir_interpreter_file_load_location(out, &plan->source);
        mir_stream_puts("\tpush hl\n", out);
        mir_machine_emit_symbol_call(out, plan->free_function);
        mir_stream_puts("\tpop bc\n\tld hl,0\n", out);
        mir_interpreter_file_store_location(out, &plan->source);
    } else if (plan->kind == MIR_INTERPRETER_FILE_FORTH ||
               plan->kind == MIR_INTERPRETER_FILE_BASIC) {
        mir_interpreter_file_load_location(out, &plan->source);
        mir_stream_puts("\tpush hl\n", out);
        mir_machine_emit_symbol_call(out, plan->free_function);
        mir_stream_puts("\tpop bc\n", out);
        if (plan->kind == MIR_INTERPRETER_FILE_FORTH) {
            mir_stream_puts("\tld hl,0\n", out);
            mir_interpreter_file_store_location(
                out, &plan->source);
        }
    }
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", return_zero, read_ok);

    if (preprocess) {
        mir_interpreter_file_load_ix_word(out, -8);
        mir_stream_puts("\tex de,hl\n", out);
        mir_interpreter_file_load_ix_word(out, -4);
        mir_stream_puts("\tadd hl,de\n\txor a\n\tld (hl),a\n", out);
        mir_interpreter_file_load_ix_word(out, -8);
        mir_stream_puts("\tpush hl\n", out);
        mir_machine_emit_symbol_call(
            out, plan->preprocess_function);
        mir_stream_puts("\tpop bc\n", out);
        mir_interpreter_file_store_location(
            out, &plan->source);
        mir_interpreter_file_load_ix_word(out, -8);
        mir_stream_puts("\tpush hl\n", out);
        mir_machine_emit_symbol_call(out, plan->free_function);
        mir_stream_puts("\tpop bc\n", out);
        mir_interpreter_file_load_location(out, &plan->source);
        mir_stream_puts("\tpush hl\n", out);
        mir_machine_emit_symbol_call(out, plan->length_function);
        mir_stream_puts("\tpop bc\n", out);
        mir_interpreter_file_store_ix_word(out, -4);
    } else if (trim_ctrl_z) {
        mir_stream_printf(out, "L%d:\n", trim_loop);
        mir_interpreter_file_load_ix_word(out, -4);
        mir_stream_puts("\tld a,h\n\tor l\n", out);
        mir_stream_printf(out, "\tjp z,L%d\n\tdec hl\n\tpush hl\n",
                trim_done);
        mir_interpreter_file_load_location(out, &plan->source);
        mir_stream_puts("\tpop de\n\tadd hl,de\n\tld a,(hl)\n"
              "\tcp 26\n", out);
        mir_stream_printf(out, "\tjp nz,L%d\n", trim_done);
        mir_interpreter_file_load_ix_word(out, -4);
        mir_stream_puts("\tdec hl\n", out);
        mir_interpreter_file_store_ix_word(out, -4);
        mir_stream_printf(out, "\tjp L%d\nL%d:\n",
                trim_loop, trim_done);
    }

    mir_interpreter_file_load_location(out, &plan->source);
    mir_stream_puts("\tex de,hl\n", out);
    mir_interpreter_file_load_ix_word(out, -4);
    mir_stream_puts("\tadd hl,de\n", out);
    if (plan->kind == MIR_INTERPRETER_FILE_BASIC)
        mir_stream_puts("\tld (hl),10\n\tinc hl\n", out);
    mir_stream_puts("\tld (hl),0\n", out);
    mir_interpreter_file_store_length(
        out, &plan->source_length, -4);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,0\n\tjp L%d\n"
            "L%d:\n\tld hl,1\nL%d:\n"
            "\tld sp,ix\n\tpop ix\n\tret\n",
            return_one, return_zero, epilogue,
            return_one, epilogue);
}

int mir_try_emit_interpreter_runners(MirStream *out)
{
    struct MirInterpreterFileSchedule interpreter_file_plan;
    struct MirForthLexerSchedule forth_lexer_plan;
    struct MirBcdByteMathSchedule bcd_math_plan;
    struct MirPascalScanSchedule pascal_scan_plan;
    struct MirPascalNextSchedule pascal_next_plan;
    struct MirPascalPredicateSchedule pascal_predicate_plan;
    struct MirPascalEmitSchedule pascal_emit_plan;
    struct MirAdaWhitespaceSchedule ada_whitespace_plan;
    struct MirAdaLexerSchedule ada_lexer_plan;
    struct MirStorageOpcodeSchedule storage_opcode_plan;
    struct MirCobAddSubSchedule cob_add_sub_plan;
    struct MirCobAddSubSchedule cob_var_access_plan;
    struct MirCobTokenizerSchedule cob_tokenizer_plan;
    struct MirCobPerformSchedule cob_perform_plan;
    struct MirCobChainSchedule cob_chain_plan;
    struct MirFortranStatementAppendSchedule fortran_append_plan;
    struct MirFortranUppercaseSchedule fortran_uppercase_plan;
    struct MirFortranDeclarationSchedule fortran_decl_plan;
    struct MirFortranSourceSchedule fortran_source_plan;
    struct MirFortranLhsSchedule fortran_lhs_plan;
    struct MirFortranMainSchedule fortran_main_plan;
    struct MirFortranGrowSchedule fortran_grow_plan;
    struct MirFortranFatalSchedule fortran_fatal_plan;
    struct MirFortranReturnSchedule fortran_return_plan;
        if (mir_match_forth_lexer_schedule(&forth_lexer_plan)) {
            mir_emit_forth_lexer_schedule(out, &forth_lexer_plan);
            return 1;
        }
        if (mir_match_bcd_byte_math_schedule(&bcd_math_plan)) {
            mir_emit_bcd_byte_math_schedule(out, &bcd_math_plan);
            return 1;
        }
        if (mir_match_pascal_scan_schedule(&pascal_scan_plan)) {
            mir_emit_pascal_scan_schedule(out, &pascal_scan_plan);
            return 1;
        }
        if (mir_match_pascal_next_schedule(&pascal_next_plan)) {
            mir_emit_pascal_next_schedule(out, &pascal_next_plan);
            return 1;
        }
        if (mir_match_pascal_predicate_schedule(
                &pascal_predicate_plan)) {
            mir_emit_pascal_predicate_schedule(
                out, &pascal_predicate_plan);
            return 1;
        }
        if (mir_match_pascal_emit_schedule(&pascal_emit_plan)) {
            mir_emit_pascal_emit_schedule(out, &pascal_emit_plan);
            return 1;
        }
        if (mir_match_ada_whitespace_schedule(
                &ada_whitespace_plan)) {
            mir_emit_ada_whitespace_schedule(
                out, &ada_whitespace_plan);
            return 1;
        }
        if (mir_match_ada_lexer_schedule(&ada_lexer_plan)) {
            mir_emit_ada_lexer_schedule(out, &ada_lexer_plan);
            return 1;
        }
        if (mir_match_storage_opcode_schedule(
                &storage_opcode_plan)) {
            mir_emit_storage_opcode_schedule(
                out, &storage_opcode_plan);
            return 1;
        }
        if (mir_match_cob_add_sub_schedule(
                &cob_add_sub_plan)) {
            mir_emit_cob_add_sub_schedule(
                out, &cob_add_sub_plan);
            return 1;
        }
        if (mir_match_cob_var_access_schedule(
                &cob_var_access_plan)) {
            mir_emit_cob_var_access_schedule(
                out, &cob_var_access_plan);
            return 1;
        }
        if (mir_match_cob_tokenizer_schedule(
                &cob_tokenizer_plan)) {
            mir_emit_cob_tokenizer_schedule(
                out, &cob_tokenizer_plan);
            return 1;
        }
        if (mir_match_cob_perform_schedule(
                &cob_perform_plan)) {
            mir_emit_cob_perform_schedule(
                out, &cob_perform_plan);
            return 1;
        }
        if (mir_match_cob_chain_schedule(&cob_chain_plan)) {
            mir_emit_cob_chain_schedule(out, &cob_chain_plan);
            return 1;
        }
        if (mir_match_fortran_statement_append_schedule(
                &fortran_append_plan)) {
            mir_emit_fortran_statement_append_schedule(
                out, &fortran_append_plan);
            return 1;
        }
        if (mir_match_fortran_uppercase_schedule(
                &fortran_uppercase_plan)) {
            mir_emit_fortran_uppercase_schedule(
                out, &fortran_uppercase_plan);
            return 1;
        }
        if (mir_match_fortran_declaration_schedule(
                &fortran_decl_plan)) {
            mir_emit_fortran_declaration_schedule(
                out, &fortran_decl_plan);
            return 1;
        }
        if (mir_match_fortran_source_schedule(
                &fortran_source_plan)) {
            mir_emit_fortran_source_schedule(
                out, &fortran_source_plan);
            return 1;
        }
        if (mir_match_fortran_lhs_schedule(
                &fortran_lhs_plan)) {
            mir_emit_fortran_lhs_schedule(
                out, &fortran_lhs_plan);
            return 1;
        }
        if (mir_match_fortran_main_schedule(&fortran_main_plan)) {
            mir_emit_fortran_main_schedule(out, &fortran_main_plan);
            return 1;
        }
        if (mir_match_fortran_grow_schedule(
                &fortran_grow_plan)) {
            mir_emit_fortran_grow_schedule(
                out, &fortran_grow_plan);
            return 1;
        }
        if (mir_match_fortran_fatal_schedule(
                &fortran_fatal_plan)) {
            mir_emit_fortran_fatal_schedule(
                out, &fortran_fatal_plan);
            return 1;
        }
        if (mir_match_fortran_return_schedule(
                &fortran_return_plan)) {
            mir_emit_fortran_return_schedule(
                out, &fortran_return_plan);
            return 1;
        }
        if (mir_match_interpreter_file_schedule(
                &interpreter_file_plan)) {
            mir_emit_interpreter_file_schedule(
                out, &interpreter_file_plan);
            return 1;
        }
    return -1;
}
