/* dcc_mir_spilled_cfg.c - the mir_try_emit_spilled_scalar_cfg selector
 * (the dominant selector, covering most of the corpus) plus its
 * exclusive helpers: virtual IY/offset addressing, HL/stack value
 * forwarding, comparison fusion, fastcall shape detection, and
 * backend-slot preparation.
 *
 * Part of the dcc_mir.c MIR backend split; see dcc_mir_internal.h.
 */

/* fmemopen() (used below for elided-epilogue byte accounting) is a POSIX
 * extension: under strict ISO C mode, glibc's <stdio.h> doesn't declare it
 * unless a feature-test macro asks for it (_POSIX_C_SOURCE 200809L is
 * enough for fmemopen specifically). Without a prototype in scope, the
 * implicit-int rule assumes it returns int, silently truncating the real
 * FILE* pointer it returns - the same undefined-behavior bug class already
 * documented and fixed for realpath()/similar POSIX calls in dcc.c and
 * dcc_func.c. Must be defined before any system header is first included
 * in this translation unit. */
#ifndef _WIN32
#define _POSIX_C_SOURCE 200809L
#endif

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdint.h>
#include "dcc.h"
#include "dcc_ast.h"
#include "dcc_mir.h"
#include "dcc_mir_internal.h"
#include "dcc_mir_machine_internal.h"

struct MirIndirectIncDec {
    int load_instruction;
    int constant_instruction;
    int binary_instruction;
    int store_instruction;
    int return_instruction;
    int address_value;
    int old_value;
    int adjusted_value;
    int memory_size;
    int type;
    int decrement;
    int returns_adjusted;
};

static int mir_match_indirect_incdec(
    int instruction, struct MirIndirectIncDec *plan);
static void mir_emit_indirect_incdec(
    MirStream *out, const struct MirIndirectIncDec *plan);
static int mir_binary_is_fusable_comparison(int i);
static int mir_value_is_nested_truth_comparison_input(int value);
static void mir_emit_symbol_extrn(MirStream *out, struct Sym *symbol);
static void mir_emit_symbol_call(MirStream *out, struct Sym *symbol);
struct MirConstantReturn {
    int value;
};
static int mir_float_madd_match(int add_index, int *multiply_index,
                                int *addend_value);
static int mir_unary_is_fusable_not_branch(int i);
static int mir_forwarded_stack_target_instruction = -1;
static int mir_fused_compare_is_const_zero_rhs(int compare_index);
static int mir_fused_compare_is_const_zero_lhs(int compare_index);
static int mir_fused_compare_is_signed_zero_sign_test(int compare_index);
static const char *mir_wide_runtime_helper(const struct MirInsn *insn);
static int mir_value_is_selfstore_incdec_source(int value);
static int mir_binary_is_selfstore_small_adjust(
    int index, int *store_index, int *amount);
static int mir_binary_is_selfstore_global_predecrement_load(
    int index, int *store_index, int *load_index, int *amount);
/* A loop-carried wide update can reuse the PHI's two-unit slot in place. */
static int mir_binary_is_wide_phi_update(
    int index, int *phi_value, int *store_index);
static int mir_binary_is_wide_phi_increment(
    int index, int *phi_value, int *store_index);
static int mir_value_is_wide_phi_update_result(int value);
static int mir_binary_is_narrow_phi_adjust(
    int index, int *phi_value, int *store_index);
static int mir_value_is_narrow_phi_adjust_result(int value);
/* T511 wide counterpart: update an unaliased four-byte named object in
 * place, then let later SSA uses reload the now-authoritative named home. */
static int mir_binary_is_selfstore_wide_increment(
    int index, int *store_index, int *memory_offset);
static int mir_value_is_selfstore_wide_increment_result(int value);
static int mir_value_only_used_by_selfstore_adjust_amount(int value);
static int mir_value_is_dense_byte_switch_condition(int value);
static int mir_wide_constant_is_signed_relational_immediate(int value);
static int mir_wide_narrow_multiply_match(
    const struct MirInsn *multiply, int *left_source,
    int *right_source, int *is_unsigned);
static int mir_value_is_wide_narrow_multiply_widen(int value);
static int mir_value_is_m1q_bounded(int value, int depth);
static int mir_value_is_m1q_multiply_source(int value);
int mir_wide_operation_is_signed_const_relational(
    const struct MirInsn *insn);
static int mir_single_call_argument(int call_instruction, int *value);
struct MirInlineMemoryStore;
static int mir_call_uses_inline_memory_store(
    int call_instruction, struct MirInlineMemoryStore *helper,
    int arguments[4]);
struct MirInlineTypedIndexedStore;
static int mir_call_uses_inline_typed_indexed_store(
    int call_instruction, struct MirInlineTypedIndexedStore *helper,
    int arguments[3]);
struct MirInlineSimpleIndexedStore;
static int mir_call_uses_inline_simple_indexed_store(
    int call_instruction, struct MirInlineSimpleIndexedStore *helper,
    int arguments[3]);
int mir_store_is_dead(int instruction);
static int mir_divmod_partner(int instruction);
static int mir_call_has_odd_argument_bytes(const struct MirInsn *call);
static int mir_binary_only_constant(int value);
static int mir_stack_forward_target(int value, int *dynamic_index);
static int mir_stack_backend_slot_forwardable(
    int value, int units, int instruction);
static int mir_indirect_load_de_slot_forwardable(
    int value, int units, int instruction);
static int mir_runtime_stride_index_slot_forwardable(
    int value, int units, int instruction);
static int mir_runtime_stride_store_slot_forwardable(
    int value, int units, int instruction);
static int mir_value_has_direct_named_home(int value);
static int mir_bool_call_named_home(
    int value, int *store_instruction_out, int *offset_out);
static int mir_pointer_difference_pow2_shift(
    const struct MirInsn *insn);
static int mir_wide_constant_is_call_comparison_rhs(int value);
static int mir_call_result_direct_reload_indirect_store_target(int value);
static int mir_value_currently_uses_stack_handoff(int value, int instruction);
static int mir_planned_stack_matches_consumer(int value, int instruction);
static int mir_planned_stack_is_emitted(int value);
static int mir_consume_planned_stack(
    MirStream *out, int value, int instruction, const char *destination);
static int mir_value_has_phi_use(int value);
static int mir_pointer_value_has_single_safe_named_home_use(int value);
static void mir_emit_hl_offset_from_ix(MirStream *out, int offset);
static int mir_narrow_store_preserves_hl_value(int instruction);
static int mir_phi_copies_are_empty(int predecessor, int successor);
struct MirLowByteIndexLoop;
static int mir_match_low_byte_index_loop(
    int instruction, struct MirLowByteIndexLoop *plan);
static int mir_low_byte_loop_value_used_outside(
    int value, int first, int last);
static int mir_low_byte_loop_next_effective(int instruction);
struct MirWideByteStore;
static int mir_match_wide_byte_store(
    int instruction, struct MirWideByteStore *plan);
struct MirWideBytePackReturn;
static int mir_match_wide_byte_pack_return(
    int instruction, struct MirWideBytePackReturn *plan);
struct MirWideMaskZeroBranch;
static int mir_match_wide_mask_zero_branch(
    int instruction, struct MirWideMaskZeroBranch *plan);
static int mir_function_has_wide_mask_zero_branch(void);
struct MirByteVerifyLoop;
static int mir_match_byte_verify_loop(
    int instruction, struct MirByteVerifyLoop *plan);
static int mir_match_byte_verify_value_prefix(
    int instruction, struct MirByteVerifyLoop *plan);
struct MirWordScanLoop;
static int mir_match_word_scan_loop(
    int instruction, struct MirWordScanLoop *plan);
static int mir_emit_named_word_load_to_hl(
    MirStream *out, const struct MirInsn *load);
static int mir_same_scalar_memory_location(
    const struct MirInsn *a, const struct MirInsn *b);
static int mir_value_only_used_by_dynamic_absolute_index(int value);
static int mir_forward_skip_last_skipped_dead_store;
static int mir_spilled_cfg_used_dead_store_forwarding;
static int mir_spilled_cfg_used_constant_absolute;
static int mir_spilled_cfg_used_constant_index_absolute;
static int mir_spilled_cfg_used_dynamic_index_base_forwarding;
static int mir_spilled_cfg_used_wide_constant_rematerialization;
static int mir_spilled_cfg_used_unsigned_wide_constant_relational;
static int mir_spilled_cfg_used_signed_wide_constant_relational;
static int mir_spilled_cfg_used_indirect_incdec;
static int mir_spilled_cfg_used_pointer_difference_shift;
static int mir_spilled_cfg_used_wide_call_constant_comparison;
static int mir_spilled_cfg_used_local_constant_byte_store;
static int mir_spilled_cfg_used_unary_not_branch_fusion;
static int mir_spilled_cfg_used_planned_stack_handoff;
static int mir_spilled_cfg_used_planned_index_base_handoff;
static int mir_spilled_cfg_used_stable_pointer_local_home;
static int mir_spilled_cfg_used_stable_pointer_local_slot;
static int mir_spilled_cfg_used_general_rhs_stack_forwarding;
static int mir_spilled_cfg_used_binary_load_pair_forwarding;
static int mir_spilled_cfg_used_dense_byte_switch;
static int mir_spilled_cfg_dense_switch_case_count;
static int mir_spilled_cfg_dense_switch_minimum;
static int mir_spilled_cfg_dense_switch_maximum;
static int mir_spilled_cfg_dense_switch_direct_condition;
static int mir_spilled_cfg_dense_switch_postincrement_index;
static int mir_spilled_cfg_inline_postincrement_use_count;
static int mir_spilled_cfg_inline_indexed_stack_store_use_count;
static int mir_spilled_cfg_inline_simple_indexed_store_use_count;
static int mir_spilled_cfg_small_selfstore_add_use_count;
static int mir_address_rematerialization_enabled;
static int mir_block_cse_address_rematerialization_enabled;
static int mir_spilled_cfg_indirect_store_value_forwarding_count;
static int mir_spilled_cfg_branch_condition_forwarding_count;
static int mir_spilled_cfg_indirect_store_address_forwarding_count;
static int mir_planned_stack_handoffs_enabled;
static int mir_stable_pointer_local_homes_enabled;
static int mir_general_rhs_stack_forwarding_enabled;
static int mir_indirect_store_value_forwarding_enabled;
static int mir_branch_condition_forwarding_enabled;
static int mir_indirect_store_address_forwarding_enabled;
static int mir_wide_binary_lhs_forwarding_enabled;
static int mir_wide_binary_rhs_forwarding_enabled;
static int mir_wide_binary_rhs_forwarding_uses;
static int mir_wide_store_forwarding_enabled;
static int mir_spilled_cfg_used_wide_store_forwarding;
static int mir_spilled_cfg_used_exact_semantic_kernel;

static void mir_numeric_shape_hash_ex(
    int normalize_index_constants,
    unsigned long long *first, unsigned long long *second)
{
    unsigned long long h1 = 1469598103934665603ULL;
    unsigned long long h2 = 0x9e3779b97f4a7c15ULL;
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        /* insn->immediate is `long`, whose width is host-dependent (64
         * bits on Linux/macOS, only 32 on MSVC/Windows and 32-bit Linux).
         * A negative wide (32-bit target) constant like -32768 is stored
         * bit-identically on every host, but widening it straight to
         * unsigned long long sign-extends through whatever the host long
         * happens to be first - giving a small value like
         * 0xFFFFFFFFFFFF8000 on a 32-bit host vs. the intended
         * 0x00000000FFFF8000 on a 64-bit one. That fed a different value
         * into this hash depending on host long width alone, silently
         * failing every exact-shape match (mir_match_long_clamp and
         * friends) on 32-bit hosts even though the MIR shape was
         * identical. Route through uint32_t explicitly, matching every
         * other field below (already effectively 32-bit via unsigned int,
         * made explicit here too), so the hash is byte-for-byte the same
         * on every host regardless of long's width. */
        unsigned long long immediate =
            (unsigned long long)(uint32_t)insn->immediate;
        unsigned long long values[] = {
            (unsigned long long)(uint32_t)insn->opcode,
            (unsigned long long)(uint32_t)insn->dst,
            (unsigned long long)(uint32_t)insn->src1,
            (unsigned long long)(uint32_t)insn->src2,
            (unsigned long long)(uint32_t)insn->type,
            immediate,
            (unsigned long long)(uint32_t)insn->label,
            (unsigned long long)(uint32_t)insn->phi_pred1,
            (unsigned long long)(uint32_t)insn->phi_pred2,
            (unsigned long long)(uint32_t)insn->successors[0],
            (unsigned long long)(uint32_t)insn->successors[1],
            (unsigned long long)(uint32_t)insn->successor_count,
            (unsigned long long)(uint32_t)insn->object,
            (unsigned long long)(uint32_t)insn->memory_size,
            (unsigned long long)(uint32_t)insn->memory_flags,
            (unsigned long long)(uint32_t)insn->bit_width,
            (unsigned long long)(uint32_t)insn->bit_shift,
            (unsigned long long)(uint32_t)insn->bit_mask,
            (unsigned long long)(uint32_t)insn->secondary_offset,
            (unsigned long long)(uint32_t)insn->inline_temp_id
        };
        size_t value;

        if (normalize_index_constants &&
            insn->opcode == MIR_CONST &&
            instruction + 1 < mir.count &&
            mir.insns[instruction + 1].opcode ==
                MIR_INDEX_ADDRESS &&
            mir.insns[instruction + 1].src2 == insn->dst)
            values[5] = 0;

        for (value = 0;
             value < sizeof(values) / sizeof(values[0]); ++value) {
            unsigned long long v = values[value];

            h1 ^= v;
            h1 *= 1099511628211ULL;
            h2 ^= v + 0x9e3779b97f4a7c15ULL +
                  (h2 << 6) + (h2 >> 2);
        }
    }
    if (first != NULL)
        *first = h1;
    if (second != NULL)
        *second = h2;
}

static void mir_numeric_shape_hash(
    unsigned long long *first, unsigned long long *second)
{
    mir_numeric_shape_hash_ex(0, first, second);
}
static int mir_stable_pointer_argument_rematerialization_enabled;
static int mir_global_argument_rematerialization_enabled;
static int mir_wide_first_argument_stack_cache_enabled;
static int mir_narrow_argument_direct_push_enabled;
static int mir_constant_argument_prepacking_enabled;
static int mir_prepacked_call_instruction = -1;
static int mir_prepacked_after_argument = -1;
static int mir_prepacked_result_value = -1;
static int mir_prepacked_skip_through = -1;
static int mir_constant_argument_prepack_count;
static int mir_promoted_local_slot_reuse_enabled;
static int mir_phi_slot_cleanup_enabled;
static int mir_planned_stack_emit_count;
static int mir_planned_stack_consume_count;
static int mir_planned_stack_invalid;
static int mir_forwarded_wide_stack_value = -1;
static int mir_forwarded_wide_stack_consumer = -1;
static int mir_forwarded_de_value = -1;
static int mir_forwarded_de_consumer = -1;
static int mir_runtime_stride_index_base_value = -1;
static int mir_runtime_stride_index_value = -1;
static int mir_runtime_stride_index_consumer = -1;
static unsigned char *mir_backend_slot_accessed;
static int *mir_backend_slot_offsets;
static int mir_backend_slot_offset_capacity;
static int mir_backend_slot_pool_count;
static int mir_backend_frame_slot_count;
static int mir_backend_local_slot_count;
static int mir_spilled_cfg_used_promoted_local_slot;
#define MIR_BACKEND_SLOT_CALL_CACHE (-2)
#define MIR_BACKEND_SLOT_WIDE_ARGUMENT_STACK_CACHE (-3)
#define MIR_BACKEND_SLOT_NARROW_ARGUMENT_DIRECT_PUSH (-4)
#define MIR_BACKEND_SLOT_PHI_ARGUMENT_STACK (-5)

static int mir_runtime_stride_store_match(
    int instruction, int *memory_offset_out, int *store_instruction_out)
{
    const struct MirInsn *increment;
    const struct MirInsn *index;
    const struct MirInsn *indirect;
    const struct MirInsn *load;
    const struct MirInsn *one;
    const struct MirInsn *store;
    int memory_offset;
    int memory_storage;
    int memory_type;

    if (instruction < 0 || instruction + 5 >= mir.count)
        return 0;
    index = &mir.insns[instruction];
    load = &mir.insns[instruction + 1];
    one = &mir.insns[instruction + 2];
    increment = &mir.insns[instruction + 3];
    store = &mir.insns[instruction + 4];
    indirect = &mir.insns[instruction + 5];
    if (index->opcode != MIR_INDEX_ADDRESS ||
        load->opcode != MIR_LOAD ||
        one->opcode != MIR_CONST || one->immediate != 1 ||
        increment->opcode != MIR_BINARY ||
        increment->immediate != '+' ||
        increment->src1 != load->dst ||
        increment->src2 != one->dst ||
        type_size(increment->secondary_offset) != 2 ||
        store->opcode != MIR_STORE ||
        store->src1 != increment->dst ||
        store->object != load->object ||
        indirect->opcode != MIR_STORE_INDIRECT ||
        indirect->src1 != index->dst ||
        indirect->src2 != load->dst ||
        indirect->memory_size != 2 ||
        indirect->bit_width != 0 ||
        !mir_scalar_memory_location(
            load, &memory_type, &memory_storage, &memory_offset) ||
        type_size(memory_type) != 2 ||
        (memory_storage != SC_LOCAL && memory_storage != SC_PARAM) ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    if (memory_offset_out != NULL)
        *memory_offset_out = memory_offset;
    if (store_instruction_out != NULL)
        *store_instruction_out = instruction + 5;
    return 1;
}

static int mir_runtime_stride_schedule_is_active(void)
{
    int instruction;

    if (!mir.has_runtime_stride_param ||
        mir_cfg_block_count() != 7 ||
        mir.count > 64)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_CALL ||
            mir.insns[instruction].opcode == MIR_CALL_AGGREGATE)
            return 0;
    if (type_size(mir.return_type) == 2)
        return 1;
    if ((mir.return_type & 15) == TYPE_VOID)
        for (instruction = 0;
             instruction + 5 < mir.count; ++instruction)
            if (mir_runtime_stride_store_match(
                    instruction, NULL, NULL))
                return 1;
    return 0;
}

static int mir_phi_slot_cleanup_is_active(void)
{
    return mir_phi_slot_cleanup_enabled ||
           mir_runtime_stride_schedule_is_active();
}

static int mir_promoted_local_slot_reuse_is_active(void)
{
    return mir_promoted_local_slot_reuse_enabled ||
           mir_runtime_stride_schedule_is_active();
}

static int mir_phi_argument_stack_handoff_enabled;

static int mir_phi_first_call_argument_candidate(int value)
{
    const struct MirInsn *phi = mir_definition(value);
    int phi_instruction;
    int argument_instruction = -1;
    int call_instruction = -1;
    int call_id = -1;
    int argument_index = -1;
    int argument_count = 0;
    int instruction;

    if (phi == NULL || phi->opcode != MIR_PHI ||
        type_size(phi->type) <= 0 || type_size(phi->type) > 2)
        return -1;
    phi_instruction = (int)(phi - mir.insns);
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode == MIR_ARG && insn->src1 == value) {
            if (argument_instruction >= 0)
                return -1;
            argument_instruction = instruction;
            call_id = insn->secondary_offset;
            argument_index = (int)insn->immediate;
            continue;
        }
        if (insn->src1 == value || insn->src2 == value)
            return -1;
    }
    if (argument_instruction != phi_instruction + 1)
        return -1;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode == MIR_ARG &&
            insn->secondary_offset == call_id)
            ++argument_count;
        if ((insn->opcode == MIR_CALL ||
             insn->opcode == MIR_CALL_AGGREGATE) &&
            insn->secondary_offset == call_id) {
            if (call_instruction >= 0 ||
                insn->opcode != MIR_CALL)
                return -1;
            call_instruction = instruction;
        }
    }
    if (call_instruction != argument_instruction + 1 ||
        argument_count <= 0 ||
        argument_index != argument_count - 1)
        return -1;
    /*
     * A single PHI at this join is load-bearing: each incoming edge may
     * pre-push exactly one first ABI argument without needing simultaneous
     * PHI-copy ordering or a path-sensitive stack plan.
     */
    instruction = phi_instruction - 1;
    while (instruction >= 0 && mir.insns[instruction].opcode == MIR_NOP)
        --instruction;
    if (instruction >= 0 && mir.insns[instruction].opcode == MIR_PHI)
        return -1;
    instruction = phi_instruction + 1;
    while (instruction < mir.count &&
           (mir.insns[instruction].opcode == MIR_NOP ||
            mir.insns[instruction].opcode == MIR_ARG))
        ++instruction;
    if (instruction != call_instruction)
        return -1;
    return call_instruction;
}

static int mir_phi_string_argument_candidate(int value)
{
    const struct MirInsn *phi = mir_definition(value);
    const struct MirInsn *left;
    const struct MirInsn *right;
    int call_instruction =
        mir_phi_first_call_argument_candidate(value);

    if (call_instruction < 0 ||
        phi == NULL || phi->opcode != MIR_PHI)
        return -1;
    left = mir_definition(phi->src1);
    right = mir_definition(phi->src2);
    return left != NULL && right != NULL &&
           left->opcode == MIR_STRING_ADDRESS &&
           right->opcode == MIR_STRING_ADDRESS &&
           (mir.insns[call_instruction].memory_flags &
            MIR_CALL_FLAG_VARIADIC) != 0
        ? call_instruction : -1;
}

static int mir_phi_first_call_argument_target(int value)
{
    int blocks = mir_cfg_block_count();

    if (!mir_phi_argument_stack_handoff_enabled)
        return -1;
    if (blocks >= 50 && blocks <= 96)
        return mir_phi_first_call_argument_candidate(value);
    return mir_phi_string_argument_candidate(value);
}

static int mir_phi_argument_is_prepacked(int value, int call_instruction)
{
    return value >= 0 && value < mir.next_value &&
           mir.backend_slots != NULL &&
           mir.backend_slots[value] ==
               MIR_BACKEND_SLOT_PHI_ARGUMENT_STACK &&
           mir_phi_first_call_argument_target(value) == call_instruction;
}

static int mir_has_phi_first_call_argument_candidate(void)
{
    int blocks = mir_cfg_block_count();
    int value;

    for (value = 0; value < mir.next_value; ++value)
        if (mir_phi_string_argument_candidate(value) >= 0)
            return 1;
    /*
     * Stage the first production rollout on large validation/driver CFGs.
     * The internal baseline race still protects every candidate in this
     * range; smaller and very large CFG strata can be widened independently
     * after their complete populations are measured.
     */
    if (blocks < 50 || blocks > 96)
        return 0;
    for (value = 0; value < mir.next_value; ++value)
        if (mir_phi_first_call_argument_candidate(value) >= 0)
            return 1;
    return 0;
}

static int mir_virtual_offset(int value)
{
    int slot = value;
    if (mir_backend_slot_accessed != NULL &&
        value >= 0 && value < mir.next_value)
        mir_backend_slot_accessed[value] = 1;
    if (value >= 0 && value < mir.next_value && mir.backend_slots != NULL &&
        mir.backend_slots[value] >= 0)
        slot = mir.backend_slots[value];
    if (slot >= 0 && slot < mir_backend_slot_pool_count &&
        mir_backend_slot_offsets != NULL)
        return mir_backend_slot_offsets[slot];
    return -mir_effective_local_bytes() - mir.aggregate_temp_bytes -
           2 * (slot + 1);
}

static int mir_emit_named_address_root_to_hl(MirStream *out,
                                             const struct MirInsn *insn)
{
    int memory_type;
    int memory_storage;
    int memory_offset;
    struct Sym *global;

    if (!mir_scalar_memory_location(insn, &memory_type,
                                    &memory_storage, &memory_offset))
        return 0;
    /* Look up the global/extern symbol table entry only after confirming
     * via memory_storage (mir_scalar_memory_location, which resolves this
     * specific instruction's own object/declared-local binding) that this
     * name is genuinely global/extern/a function here. A local variable
     * whose name happens to match an unrelated library function (e.g. a
     * local named "remove", colliding with stdio.h's remove() prototype)
     * must not take this branch just because find_global() finds that
     * unrelated global by the same name - doing so emits `extrn`/`ld
     * hl,<label>` for the library function's address instead of this
     * local's own frame address, corrupting every read through it. */
    if (memory_storage == SC_GLOBAL || memory_storage == SC_EXTERN ||
        memory_storage == SC_FUNC) {
        global = find_global(insn->name);
        const char *assembly_name = asm_name_for(
            global != NULL ? sym_asm_name(global)
                           : mir_declared_link_name(insn->name));
        if ((memory_storage == SC_EXTERN ||
             (global != NULL && global->storage == SC_FUNC &&
              global->needs_extrn)) &&
            mir_extrn_should_emit(global))
            mir_stream_printf(out, "\textrn %s\n", assembly_name);
        mir_stream_printf(out, "\tld hl,%s\n", assembly_name);
    } else if (mir_declared_is_vla_object(insn->name)) {
        mir_stream_printf(out, "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
                memory_offset, memory_offset + 1);
    } else {
        mir_stream_puts("\tpush ix\n\tpop hl\n", out);
        mir_emit_hl_offset_from_ix(out, memory_offset);
    }
    return 1;
}

static int mir_dynamic_absolute_index_base(
    const struct MirInsn *index, const struct MirInsn **base_out,
    const char **assembly_name_out)
{
    const struct MirInsn *base;
    const struct MirInsn *index_value;
    struct Sym *global;
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (mir_cfg_block_count() < 40 ||
        index == NULL || index->opcode != MIR_INDEX_ADDRESS ||
        index->base_name[0] != 0 || index->src1 < 0 ||
        index->src2 < 0 || index->immediate <= 0)
        return 0;
    base = mir_definition(index->src1);
    index_value = mir_definition(index->src2);
    if (base == NULL || base->opcode != MIR_ADDRESS ||
        base->name[0] == 0 || index_value == NULL ||
        index_value->opcode == MIR_CONST ||
        !mir_scalar_memory_location(
            base, &memory_type, &memory_storage, &memory_offset) ||
        (memory_storage != SC_GLOBAL &&
         memory_storage != SC_EXTERN) ||
        memory_offset != 0)
        return 0;
    global = find_global(base->name);
    if (base_out != NULL)
        *base_out = base;
    if (assembly_name_out != NULL)
        *assembly_name_out = asm_name_for(
            global != NULL ? sym_asm_name(global)
                           : mir_declared_link_name(base->name));
    return 1;
}

static int mir_value_only_used_by_dynamic_absolute_index(int value)
{
    const struct MirInsn *base = mir_definition(value);
    int instruction;
    int uses = 0;

    if (base == NULL || base->opcode != MIR_ADDRESS)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *use = &mir.insns[instruction];

        if (use->src1 != value && use->src2 != value &&
            !mir_call_uses_value(use, value))
            continue;
        if (use->opcode != MIR_INDEX_ADDRESS ||
            use->src1 != value ||
            !mir_dynamic_absolute_index_base(use, NULL, NULL))
            return 0;
        ++uses;
    }
    return uses > 0;
}

static int mir_emit_address_value_to_hl(MirStream *out, int value)
{
    const struct MirInsn *insn = mir_definition(value);

    if (insn == NULL)
        return 0;
    switch (insn->opcode) {
    case MIR_LOAD:
        if (type_size(insn->type) != 2)
            return 0;
        return mir_emit_named_word_load_to_hl(out, insn);
    case MIR_ADDRESS:
        return mir_emit_named_address_root_to_hl(out, insn);
    case MIR_MEMBER_ADDRESS:
        if (!mir_emit_address_value_to_hl(out, insn->src1))
            return 0;
        if (insn->immediate != 0)
            mir_stream_printf(out, "\tld de,%ld\n\tadd hl,de\n",
                    insn->immediate & 0xffffL);
        return 1;
    case MIR_INDEX_ADDRESS:
        {
        const struct MirInsn *index_definition = mir_definition(insn->src2);
        long byte_offset;

        if (insn->base_name[0] != 0 || index_definition == NULL ||
            index_definition->opcode != MIR_CONST)
            return 0;
        if (!mir_emit_address_value_to_hl(out, insn->src1))
            return 0;
        byte_offset = index_definition->immediate * insn->immediate;
        if (byte_offset != 0)
            mir_stream_printf(out, "\tld de,%ld\n\tadd hl,de\n",
                    byte_offset & 0xffffL);
        return 1;
        }
    default:
        return 0;
    }
}

static int mir_virtual_iy_offset(int value)
{
    return mir_virtual_offset(value) + mir_effective_local_bytes() +
           mir.aggregate_temp_bytes;
}

/* Item 15 (mir-migration-plan-100): count exact CFG predecessors of a label
 * instruction using the successors[]/successor_count arrays mir_verify_and_dump
 * already computes for every function (jump targets, branch-false
 * fallthrough+target, plain fallthrough). Reused instead of re-deriving the
 * CFG so this stays consistent with liveness/allocation, which trust the
 * same arrays. */
static int mir_label_predecessor_count(int label_instruction)
{
    int predecessor;
    int successor;
    int count = 0;

    for (predecessor = 0; predecessor < mir.count; ++predecessor) {
        const struct MirInsn *insn = &mir.insns[predecessor];
        for (successor = 0; successor < insn->successor_count; ++successor)
            if (insn->successors[successor] == label_instruction)
                ++count;
    }
    return count;
}

/* Item 15 (mir-migration-plan-100): a value's "next instruction" for
 * HL-forwarding purposes may legitimately sit just past a MIR_LABEL that is
 * textually adjacent but has exactly one CFG predecessor (i.e. it is not a
 * real merge point - the label exists only because some other emitter
 * concern, such as a goto target or loop back-edge landing elsewhere,
 * required a name for this position). Skip at most one such label, in
 * addition to any NOPs, so straight-line code that happens to be split by a
 * label still gets the same forwarding Item 13 already applies across NOPs.
 * Skipping more than one label at a time is deliberately not supported: it
 * would require reasoning about a chain of merges instead of a single,
 * locally-verifiable non-merge point. */

/* Item T30 (mir-text-size-plan.md): a MIR_CONST whose only consumer is the
 * immediately-following MIR_BINARY as a fusable const-zero-RHS (or
 * signed-zero-sign-test) comparison emits no code of its own at all - the
 * binary's own emission (see the "Item 25"/"Item 27" comments further down
 * this file) skips materializing that 0 into DE entirely and tests HL
 * directly. Such a MIR_CONST is therefore just as transparent for
 * forwarding purposes as a MIR_NOP: nothing runs between the forwarded
 * value's own definition and the comparison that actually consumes it. This
 * was previously invisible to mir_forward_skip_target_ex, which only looked
 * through MIR_NOP/single-predecessor MIR_LABEL, so a call result (or any
 * other HL-forwarding candidate) immediately followed by "compare != 0"
 * always fell back to a full backend-slot store/reload round trip even
 * though the constant in between never touches HL. */
static int mir_const_is_transparent_zero_rhs_operand(int instruction)
{
    const struct MirInsn *constant;
    const struct MirInsn *binary;

    if (instruction < 0 || instruction + 1 >= mir.count)
        return 0;
    constant = &mir.insns[instruction];
    binary = &mir.insns[instruction + 1];
    if (constant->opcode != MIR_CONST || binary->opcode != MIR_BINARY ||
        binary->src2 != constant->dst)
        return 0;
    return mir_binary_is_fusable_comparison(instruction + 1) > 0 &&
        (mir_fused_compare_is_const_zero_rhs(instruction + 1) ||
         mir_fused_compare_is_signed_zero_sign_test(instruction + 1));
}

/* Item T74 (mir-text-size-plan.md): a MIR_CONST whose only consumer is the
 * immediately-following MIR_INDEX_ADDRESS, where the constant is the index
 * operand (src2) and folds to a zero byte offset (index value * element
 * stride == 0, i.e. a compile-time-constant `arr[0]`-shaped access), is
 * just as transparent for forwarding purposes as the const-zero-rhs-
 * comparison case above: mir_can_forward_stack_to_index's own const-index
 * emission branch (see the MIR_INDEX_ADDRESS case further down this file)
 * already special-cases this exact shape to skip the `add hl,de` entirely
 * when byte_offset is 0, so the base address's value is never touched by
 * anything between its own definition and the index-address instruction
 * that reuses it unchanged. Before this item, mir_forward_skip_target_ex
 * did not know this, so mir_can_forward_hl_to_next always declined (the
 * intervening real MIR_CONST instruction was never skipped), leaving
 * mir_can_forward_stack_to_index (a strictly more expensive push-then-pop
 * round trip, needed only when a real `add hl,de` follows) to win by
 * default even for this zero-offset case where no register value is ever
 * disturbed at all. Found via tlngfptr.c's main (`operations[0]`, a
 * function-pointer table lookup), newly MIR-reachable once Item T74's own
 * MIR_UNARY forwarding widened mir_can_forward_hl_de_to_next - which
 * exposed this pre-existing, unrelated gap as a real (not just static-
 * metric) regression once the function actually reached MIR emission. */
static int mir_const_is_transparent_zero_index_operand(int instruction)
{
    const struct MirInsn *constant;
    const struct MirInsn *index;

    if (instruction < 0 || instruction + 1 >= mir.count)
        return 0;
    constant = &mir.insns[instruction];
    index = &mir.insns[instruction + 1];
    if (constant->opcode != MIR_CONST || index->opcode != MIR_INDEX_ADDRESS ||
        index->src2 != constant->dst || index->base_name[0] != 0)
        return 0;
    return constant->immediate * index->immediate == 0;
}

/* Item T83 (mir-text-size-plan.md): a MIR_STORE this selector itself
 * proves dead (mir_store_is_dead, or a fully-promoted object needing no
 * physical storage at all) emits zero machine instructions - exactly
 * the same "safe to look through, nothing here" property MIR_NOP
 * already has for forwarding-adjacency purposes. Without this, a value
 * whose immediately-following use (through the intervening dead store)
 * is a single-operand consumer (return, the next binary/unary operand,
 * a call argument, ...) never gets to forward at all: the dead store
 * still occupies a real instruction slot in mir.insns, so every existing
 * adjacency check anchored on it saw a non-NOP, non-transparent
 * instruction and gave up, forcing a genuinely redundant slot reload
 * even though nothing at all executes between the value's own
 * definition and this later read. Found via tests/tc89decl.c's timpreg
 * (`register c; c = a + b; return c;` - "c"'s own store is dead since
 * nothing ever loads "c" as an object again, every later use reads the
 * binary's own SSA value directly) and tests/tmirslot.c's cross_call
 * (`saved = a + b; return scale(saved) - saved;`, the same shape with an
 * intervening call). */
static int mir_instruction_is_transparent_dead_store(int instruction)
{
    int transparent =
        mir.insns[instruction].opcode == MIR_STORE &&
        (mir_object_is_fully_promoted(mir.insns[instruction].object) ||
         mir_store_is_dead(instruction));

    if (transparent)
        mir_forward_skip_last_skipped_dead_store = 1;
    return transparent;
}

static int mir_forward_skip_target_ex(int instruction, int *out_skipped_label)
{
    int next_instruction = instruction + 1;
    int skipped_label = 0;

    mir_forward_skip_last_skipped_dead_store = 0;
    for (;;) {
        while (next_instruction < mir.count &&
               (mir.insns[next_instruction].opcode == MIR_NOP ||
                mir_const_is_transparent_zero_rhs_operand(next_instruction) ||
                mir_const_is_transparent_zero_index_operand(
                    next_instruction) ||
                mir_instruction_is_transparent_dead_store(next_instruction)))
            ++next_instruction;
        if (!skipped_label && next_instruction < mir.count &&
            mir.insns[next_instruction].opcode == MIR_LABEL &&
            mir_label_predecessor_count(next_instruction) == 1) {
            skipped_label = 1;
            ++next_instruction;
            continue;
        }
        break;
    }
    if (out_skipped_label != NULL)
        *out_skipped_label = skipped_label;
    return next_instruction;
}

static int mir_forward_skip_target(int instruction)
{
    return mir_forward_skip_target_ex(instruction, NULL);
}

static int mir_forward_note_success(void)
{
    if (mir_forward_skip_last_skipped_dead_store)
        mir_spilled_cfg_used_dead_store_forwarding = 1;
    return 1;
}

static int mir_is_general_rhs_stack_forward(int value, int consumer)
{
    const struct MirInsn *binary;

    if (consumer < 0 || consumer >= mir.count)
        return 0;
    binary = &mir.insns[consumer];
    return binary->opcode == MIR_BINARY && binary->src2 == value &&
           !mir_binary_only_constant(binary->src1);
}

static int mir_is_indirect_store_value_stack_forward(int value, int consumer)
{
    const struct MirInsn *store;

    if (consumer < 0 || consumer >= mir.count)
        return 0;
    store = &mir.insns[consumer];
    return store->opcode == MIR_STORE_INDIRECT && store->src2 == value &&
           store->bit_width == 0 && store->memory_size > 0 &&
           store->memory_size <= 2;
}

static int mir_is_branch_condition_forward(int value, int consumer)
{
    return consumer >= 0 && consumer < mir.count &&
           mir.insns[consumer].opcode == MIR_BRANCH_FALSE &&
           mir.insns[consumer].src1 == value;
}

/* Like the wide T402 helper below, but kept narrow for the current
 * call-result/storeind slice: a 1/2-byte storeind can consume a directly
 * forwarded value only when its address can be reloaded at the store site
 * without relying on an in-flight stack handoff. */
static int mir_indirect_store_address_has_direct_reload(
    const struct MirInsn *store)
{
    if (store == NULL || store->opcode != MIR_STORE_INDIRECT ||
        store->bit_width > 0 || store->memory_size <= 0 ||
        store->memory_size > 2 ||
        store->src1 < 0 || store->src1 >= mir.next_value)
        return 0;
    if (mir_constant_absolute_access_supported(store) ||
        mir_value_has_direct_named_home(store->src1))
        return 1;
    return mir.backend_slots != NULL && mir.backend_slots[store->src1] >= 0;
}

/* A wide storeind can consume a forwarded DE:HL value without first forcing
 * that value through a dedicated slot only when its address can be reloaded
 * directly at the store site. Constant-absolute addresses, stable named homes,
 * and already-assigned backend slots all satisfy that property. Address values
 * that themselves depend on a stack handoff do not. */
static int mir_wide_indirect_store_address_has_direct_reload(
    const struct MirInsn *store)
{
    if (store == NULL || store->opcode != MIR_STORE_INDIRECT ||
        store->bit_width > 0 || store->memory_size != 4 ||
        store->src1 < 0 || store->src1 >= mir.next_value)
        return 0;
    if (mir_constant_absolute_access_supported(store) ||
        mir_value_has_direct_named_home(store->src1))
        return 1;
    return mir.backend_slots != NULL && mir.backend_slots[store->src1] >= 0;
}

/* Item T40 (mir-text-size-plan.md): the wide (32-bit, HL:DE) analog of
 * mir_can_forward_hl_to_next below. Items 1-32 built a rich forwarding
 * predicate for 16-bit scalar values (this function plus its
 * MIR_STORE/call-argument/stack-index siblings), but mir_emit_virtual_
 * store_wide had no equivalent at all until this item - every wide value
 * with an assigned backend slot was unconditionally spilled and reloaded,
 * even when its single next use could consume it directly from HL:DE.
 * Deliberately started with only the single narrowest, most-certain
 * consumer shape (MIR_RETURN, mirroring this function's own MIR_RETURN
 * case and VLA guard exactly) rather than the full consumer switch below -
 * per SKILL.md's staging discipline, generalize to MIR_STORE/MIR_BINARY
 * consumers only after this narrow slice is validated end to end.
 *
 * Item T74 (mir-text-size-plan.md): add MIR_UNARY as a second recognized
 * immediate consumer, mirroring mir_can_forward_hl_to_next's own
 * already-proven-safe MIR_UNARY whitelist entry for the narrow (16-bit)
 * case (see the `case MIR_INDEX_ADDRESS: case MIR_MEMBER_ADDRESS: case
 * MIR_LOAD_INDIRECT: case MIR_UNARY: break;` group below). Found via a
 * forced-accept diff on tlongreg.c's use_after_long_return: a wide call
 * result (`x = ret_high_only();`) whose sole use is an immediately
 * following identity-cast MIR_UNARY (the implicit assignment-conversion
 * dcc inserts even when the declared and source types already match) was
 * always spilled to its own backend slot and reloaded one instruction
 * later, purely because this predicate only recognized MIR_RETURN -
 * duplicating work mir_emit_virtual_store_wide's own MIR_CALL result
 * homing had already covered. Kept to a plain adjacency shape identical
 * to MIR_RETURN's own (no additional skipped-label allowance - the
 * existing `skipped_label && next->opcode != MIR_RETURN` guard just
 * below already rejects a label-crossed MIR_UNARY, so this stays exactly
 * as conservative as the existing VLA-guarded MIR_RETURN path). */
static int mir_wide_binary_rhs_is_commutative(
    const struct MirInsn *binary)
{
    const struct MirInsn *left;

    if (binary == NULL || binary->opcode != MIR_BINARY ||
        type_size(binary->secondary_offset) != 4 ||
        type_is_float(binary->secondary_offset))
        return 0;
    switch ((int)binary->immediate) {
    case '+':
    case '|':
    case '^':
    case TOK_EQ:
    case TOK_NE:
        return 1;
    case '&':
    case '*':
        left = mir_definition(binary->src1);
        return left == NULL || left->opcode != MIR_CONST;
    }
    return 0;
}

static int mir_wide_binary_rhs_pair_supported(
    int value, const struct MirInsn *binary)
{
    const struct MirInsn *definition = mir_definition(value);

    if (definition == NULL || !mir_wide_binary_rhs_is_commutative(binary))
        return 0;
    if (definition->opcode == MIR_UNARY)
        return 1;
    return definition->opcode == MIR_BINARY && binary->immediate == '*';
}

static int mir_can_forward_hl_de_to_next(int value)
{
    const struct MirInsn *next;
    int next_instruction;
    int instruction;
    int skipped_label;

    if (mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 1 >= mir.count)
        return 0;
    next_instruction = mir_forward_skip_target_ex(mir_emit_instruction_index,
                                                   &skipped_label);
    if (next_instruction >= mir.count)
        return 0;
    for (instruction = mir_emit_instruction_index + 1;
         instruction < next_instruction; ++instruction)
        if (mir.insns[instruction].opcode != MIR_NOP &&
            !mir_instruction_is_transparent_dead_store(instruction))
            return 0;
    next = &mir.insns[next_instruction];
    if (skipped_label && next->opcode != MIR_RETURN)
        return 0;
    if (next->opcode == MIR_RETURN) {
        if (next->src1 != value)
            return 0;
        if (mir.has_vla)
            return 0;
    } else if (next->opcode == MIR_UNARY) {
        if (next->src1 != value)
            return 0;
        if (mir_value_is_wide_narrow_multiply_widen(next->dst))
            return 0;
    } else if (mir_wide_binary_lhs_forwarding_enabled &&
               next->opcode == MIR_BINARY &&
               next->src1 == value &&
               type_size(next->secondary_offset) == 4) {
        /* The wide binary emitter consumes src1 first and immediately
         * pushes DE:HL before materializing src2, so an adjacent producer
         * can remain resident exactly as it can for MIR_UNARY/RETURN. */
    } else if (mir_wide_store_forwarding_enabled &&
               next->opcode == MIR_STORE && next->src1 == value &&
               (next->memory_size == 4 || type_size(next->type) == 4)) {
        /* The named store consumes DE:HL before any other value is formed. */
    } else if (next->opcode == MIR_STORE_INDIRECT && next->src2 == value &&
               mir_wide_indirect_store_address_has_direct_reload(next)) {
        /* The widened storeind path saves DE:HL first, reloads the address
         * afterward, and then writes the full four-byte value directly. */
    } else if (mir_wide_binary_rhs_forwarding_enabled &&
               next_instruction == mir_emit_instruction_index + 1 &&
               mir_wide_binary_rhs_pair_supported(value, next) &&
               next->src2 == value) {
        /* The producer pushes src2 once. The binary then loads src1 into
         * DE:HL and uses the same stack/current-register convention with
         * physically swapped operands. */
    } else {
        return 0;
    }
    /* A loop-header phi can consume this value on a backedge even when the
     * phi instruction textually appears earlier in the stream than the
     * immediate next-use candidate. The linear later-use scan below would miss
     * that edge use and incorrectly treat the value as single-use forwardable,
     * leaving later wide reloads to read from a slot that was never assigned
     * (tvapinit.join's loop-carried `pos` update). */
    if (mir_value_has_phi_use(value))
        return 0;
    for (instruction = next_instruction + 1;
         instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 == value || insn->src2 == value)
            return 0;
        if ((insn->opcode == MIR_CALL ||
             insn->opcode == MIR_CALL_AGGREGATE) &&
            mir_call_uses_value(insn, value))
            return 0;
    }
    return mir_forward_note_success();
}

static int mir_can_forward_hl_to_next(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    const struct MirInsn *next;
    int next_instruction;
    int chained_use = -1;
    int instruction;
    int skipped_label;

    if (mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 1 >= mir.count)
        return 0;
    if (definition != NULL && definition->opcode == MIR_CALL_AGGREGATE)
        return 0;
    next_instruction = mir_forward_skip_target_ex(mir_emit_instruction_index,
                                                   &skipped_label);
    if (next_instruction >= mir.count)
        return 0;
    next = &mir.insns[next_instruction];
    /* A pure MIR_NOP skip (skipped_label == 0) never crosses a CFG edge -
     * MIR_NOP is a same-block rename/metadata marker that emits no code and
     * has no live-range implications, so it is safe to look through for
     * every consuming opcode, not just MIR_RETURN. Only a skipped LABEL
     * (a real block boundary) still needs the narrower MIR_RETURN-only
     * carve-out below, since that is the one hazard actually analyzed
     * (VLA frame reuse across a skipped-to return). Item T29
     * (mir-text-size-plan.md): the old check required exact physical
     * adjacency for every opcode except MIR_RETURN, so a single
     * intervening MIR_NOP (e.g. a global/local rename marker emitted
     * immediately before a MIR_STORE) defeated forwarding even though
     * mir_forward_skip_target already looked straight through it. */
    if (skipped_label && next->opcode != MIR_RETURN)
        return 0;
    if (next->opcode == MIR_LOAD_INDIRECT) {
        struct MirIndirectIncDec incdec;

        if (mir_match_indirect_incdec(next_instruction, &incdec) &&
            incdec.address_value == value)
            return mir_forward_note_success();
    }
    /* MIR_RETURN may be reached via a non-adjacent skip (see
     * mir_forward_skip_target above), so the forwarded value's home must
     * still be live at that point. VLA frames can reuse/shrink stack space
     * between the definition and a skipped-to return, so forwarding across
     * a VLA-bearing function is unsafe; that is the only known hazard.
     * (Item T13 / mir-text-size-plan.md: this used to also require
     * !mir_function_has_any_call(), a broad whole-function gate with no
     * call-adjacency link to the forwarded value; removing it, validated
     * against the whole corpus plus full-mode runs, uncovered no
     * correctness issue and only unlocked more RETURN-adjacent forwards.) */
    if (next->opcode == MIR_RETURN && mir.has_vla)
        return 0;
    if (next->opcode == MIR_INDEX_ADDRESS) {
        if (next->src2 == value) {
            /* Existing case: forward the index operand. */
        } else if (next->src1 == value && next->base_name[0] == 0) {
            /* Item T74 (mir-text-size-plan.md): forward the base operand
             * too, but only for the fixed-stride (base_name[0] == 0)
             * const-index shape, where mir_emit_virtual_load(out,
             * insn->src1) runs first (loading the base, consuming this
             * exact forward) and any byte-offset add is a separate,
             * independent step afterward - see the const-index emission
             * branch further down this file. The runtime-stride/non-const-
             * index shapes both load the index operand into HL first (to
             * multiply/combine it with the base), which would clobber a
             * forwarded base value before it is ever used, so this branch
             * must not accept those. Restricting to a MIR_CONST index
             * definition mirrors mir_const_is_transparent_zero_index_
             * operand's own scope (found via tlngfptr.c's main,
             * `operations[0]`, a function-pointer table lookup newly
             * MIR-reachable once this item's own MIR_UNARY wide-forwarding
             * fix widened coverage elsewhere in the same function). */
            const struct MirInsn *index_definition =
                mir_definition(next->src2);
            if (index_definition == NULL ||
                index_definition->opcode != MIR_CONST)
                return 0;
        } else {
            return 0;
        }
    } else if (next->opcode == MIR_STORE_INDIRECT &&
               next->src2 == value &&
               mir_constant_absolute_access_supported(next)) {
        /* A direct absolute store consumes its value without first loading
         * the address into HL, so an immediately preceding producer can
         * forward HL exactly like it can for MIR_STORE. */
    } else if (next->opcode == MIR_BINARY && next->src2 == value &&
               mir_forwarded_stack_value == next->src1 &&
               mir_forwarded_stack_target_instruction ==
                   next_instruction) {
        /* The binary's left operand is already below SP. Keep this
         * adjacent right operand in HL; the binary emitter moves it to DE
         * before popping the left operand back into HL. */
    } else if (next->src1 != value)
            return 0;
    switch (next->opcode) {
    case MIR_INDEX_ADDRESS:
    case MIR_MEMBER_ADDRESS: case MIR_LOAD_INDIRECT:
        break;
    case MIR_UNARY:
        if (mir_value_is_m1q_multiply_source(value))
            return 0;
        break;
    case MIR_BRANCH_FALSE:
        {
            int target;
            int first_phi;
            if (!mir_branch_condition_forwarding_enabled)
                return 0;
            if (next->label < 0 || next->label >= mir.next_label)
                return 0;
            target = mir_find_label(next->label);
            if (target < 0)
                return 0;
            first_phi = mir_first_phi_or_block_end(target);
            if (first_phi < mir.count &&
                mir.insns[first_phi].opcode == MIR_PHI)
                return 0;
        }
        break;
    case MIR_BINARY:
        if (type_size(next->secondary_offset) == 4)
            return 0;
        break;
    case MIR_STORE_INDIRECT:
        if (next->bit_width > 0 || next->memory_size > 2)
            return 0;
        break;
    case MIR_RETURN:
        break;
    case MIR_STORE:
        {
            int memory_type;
            int memory_storage;
            int memory_offset;
            int producer_opcode = mir.insns[mir_emit_instruction_index].opcode;
            /* Item T31 (mir-text-size-plan.md): MIR_CALL was never in this
             * whitelist because, until Item T30, mir_can_forward_hl_to_next
             * unconditionally excluded every call result before reaching
             * this switch at all - the omission was never reachable, not a
             * deliberate safety exclusion (see Items 6/7/8, which added this
             * whitelist for binary/unary/const producers only, the set that
             * *was* reachable at the time). A call result forwarded directly
             * into its destination's store needs nothing beyond what any
             * other producer here needs: the value sits in HL right after
             * the call returns, and the store's own address computation
             * below is a fixed ix-relative offset unaffected by whatever
             * else the call clobbered.
             *
             * Item T37 (mir-text-size-plan.md): MIR_ADDRESS was missing
             * from this same whitelist for the same reason - not a
             * deliberate exclusion, just never added. A MIR_ADDRESS result
             * (a local/global's own address, e.g. `int *p = &x;`) is a pure,
             * side-effect-free computation exactly like MIR_CONST - its
             * value sits in HL right after computing it, and nothing about
             * the following store's own fixed ix-relative destination
             * offset depends on how the stored value was produced. Found
             * via a forced-accept diff on tmirfast.c's inc_observe (`int
             * *p = &x;`), which crossed the text-size acceptance threshold
             * as a side effect of Item T36 and exposed a real (not just
             * static-metric) cycle-count regression from this exact dead
             * round trip - store-to-temp-slot, reload, store-to-p's-real-
             * slot - for &x's address.
             *
             * Item T38 (mir-text-size-plan.md): MIR_LOAD (a plain,
             * non-indirect load of a named object/global, e.g. `y = x;`)
             * was missing too, even though MIR_LOAD_INDIRECT (`y = *p;`)
             * was already whitelisted - an inconsistency with no safety
             * rationale, since a plain load is if anything simpler than an
             * indirect one (no pointer dereference at all). Found via a
             * synthetic `g2 = g1;` (both plain globals) test after auditing
             * every MIR_* opcode against this whitelist post-T37: the load
             * still spilled to a temporary slot and reloaded before the
             * following store, an identical dead round trip to the T31/T37
             * cases. The value sits in HL right after the load completes,
             * exactly like every other whitelisted producer here.
             *
             * Item T39 (mir-text-size-plan.md): the rest of the "address"
             * family - MIR_STRING_ADDRESS (a string literal's address, e.g.
             * `gp = "hello";`), MIR_MEMBER_ADDRESS (`gp = &s.field;`), and
             * MIR_INDEX_ADDRESS (`gp = &arr[i];`) - were missing for the
             * exact same reason MIR_ADDRESS was (Item T37): none are a
             * deliberate exclusion, all are pure, side-effect-free address
             * computations whose value sits in HL right after computing it,
             * with no dependency on the following store's own fixed
             * ix-relative destination offset. Confirmed via three synthetic
             * tests (mirroring T38's methodology), each showing the
             * identical dead round trip. MIR_COMPOUND_ADDRESS was checked
             * too but a synthetic trigger for it was not found this
             * session - left out until a concrete motivating case
             * confirms it needs the same treatment. */
            if (mir_object_is_fully_promoted(next->object) ||
                (producer_opcode != MIR_LOAD_INDIRECT &&
                 producer_opcode != MIR_LOAD &&
                 producer_opcode != MIR_BINARY &&
                 producer_opcode != MIR_UNARY &&
                 producer_opcode != MIR_CONST &&
                 producer_opcode != MIR_CALL &&
                 producer_opcode != MIR_ADDRESS &&
                 producer_opcode != MIR_STRING_ADDRESS &&
                 producer_opcode != MIR_MEMBER_ADDRESS &&
                 producer_opcode != MIR_INDEX_ADDRESS) ||
                !mir_scalar_memory_location(next, &memory_type,
                                            &memory_storage, &memory_offset) ||
                type_is_struct_object(memory_type) ||
                type_size(memory_type) > 2)
                return 0;
        }
        break;
    default:
        return 0;
    }
    if (next->opcode == MIR_STORE &&
        mir_narrow_store_preserves_hl_value(next_instruction)) {
        int after = next_instruction + 1;

        while (after < mir.count &&
               mir.insns[after].opcode == MIR_NOP)
            ++after;
        if (after < mir.count &&
            mir.insns[after].opcode == MIR_LOAD_INDIRECT &&
            mir.insns[after].src1 == value &&
            mir.insns[after].memory_size <= 2 &&
            mir.insns[after].memory_flags == 0)
            chained_use = after;
    }
    for (instruction = next_instruction + 1;
         instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (instruction == chained_use)
            continue;
        if (insn->src1 == value || insn->src2 == value)
            return 0;
        if ((insn->opcode == MIR_CALL ||
             insn->opcode == MIR_CALL_AGGREGATE) &&
            mir_call_uses_value(insn, value))
            return 0;
    }
    return mir_forward_note_success();
}

static int mir_narrow_store_preserves_hl_value(int instruction)
{
    const struct MirInsn *store;
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (instruction < 0 || instruction >= mir.count)
        return 0;
    store = &mir.insns[instruction];
    if (store->opcode != MIR_STORE ||
        mir_object_is_fully_promoted(store->object) ||
        mir_store_is_dead(instruction) ||
        !mir_scalar_memory_location(
            store, &memory_type, &memory_storage, &memory_offset))
        return 0;
    if (store->memory_size > 0 &&
        !type_is_struct_object(store->type))
        memory_type = store->type;
    if (type_is_struct_object(memory_type) ||
        type_size(memory_type) < 1 ||
        type_size(memory_type) > 2)
        return 0;
    return memory_storage == SC_GLOBAL ||
           memory_storage == SC_EXTERN ||
           ((memory_storage == SC_LOCAL ||
             memory_storage == SC_PARAM) &&
            memory_offset >= -128 &&
            memory_offset + type_size(memory_type) - 1 <= 127);
}

static int mir_dynamic_index_base_forward_target(int value)
{
    const struct MirInsn *index;
    const struct MirInsn *index_definition;
    int target;
    int instruction;

    if (mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 1 >= mir.count ||
        mir_value_use_count(value) != 1)
        return -1;
    target = mir_emit_instruction_index + 1;
    while (target < mir.count && mir.insns[target].opcode == MIR_NOP)
        ++target;
    if (target >= mir.count)
        return -1;
    index = &mir.insns[target];
    index_definition = mir_definition(index->src2);
    if (index->opcode != MIR_INDEX_ADDRESS ||
        index->base_name[0] != 0 || index->src1 != value ||
        (index_definition != NULL &&
         index_definition->opcode == MIR_CONST))
        return -1;
    for (instruction = target + 1;
         instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 == value || insn->src2 == value)
            return -1;
        if ((insn->opcode == MIR_CALL ||
             insn->opcode == MIR_CALL_AGGREGATE) &&
            mir_call_uses_value(insn, value))
            return -1;
    }
    return target;
}

static int mir_stack_index_forward_target(int value)
{
    const struct MirInsn *middle;
    const struct MirInsn *index;
    int instruction;
    int target;

    /* Item T4 (mir-text-size-plan.md): mir_virtual_iy_base was a constant
     * 0 (dead scaffolding) when this helper and its gate were introduced
     * in 938c45b, so this optimization was dead on arrival - it only
     * started firing, for large frames only, once a later session gave
     * mir_virtual_iy_base a real value. The push/pop handoff below is a
     * self-contained physical-stack round-trip over a fixed 2-instruction
     * window with no intervening branch or call, unrelated to whether the
     * eventual store destination would use ix- or iy-relative addressing,
     * so the gate is removed the same way Item T3 removed the analogous
     * dead gate from mir_can_forward_hl_to_next. */
    if (mir_emit_instruction_index >= 0 &&
        mir_emit_instruction_index + 2 < mir.count) {
        middle = &mir.insns[mir_emit_instruction_index + 1];
        index = &mir.insns[mir_emit_instruction_index + 2];
        if (middle->opcode == MIR_CONST &&
            index->opcode == MIR_INDEX_ADDRESS &&
            index->base_name[0] == 0 &&
            index->src1 == value && index->src2 == middle->dst) {
            target = mir_emit_instruction_index + 2;
            for (instruction = target + 1;
                 instruction < mir.count; ++instruction) {
                const struct MirInsn *insn = &mir.insns[instruction];
                if (insn->src1 == value || insn->src2 == value ||
                    ((insn->opcode == MIR_CALL ||
                      insn->opcode == MIR_CALL_AGGREGATE) &&
                     mir_call_uses_value(insn, value)))
                    return -1;
            }
            return target;
        }
    }
    return mir_dynamic_index_base_forward_target(value);
}

static int mir_runtime_stride_index_forward_match(
    int instruction, int *index_value_out, int *consumer_out)
{
    const struct MirInsn *base;
    const struct MirInsn *index;
    const struct MirInsn *middle;
    int memory_offset;
    int memory_storage;
    int memory_type;

    if (instruction < 0 || instruction + 2 >= mir.count)
        return 0;
    base = &mir.insns[instruction];
    middle = &mir.insns[instruction + 1];
    index = &mir.insns[instruction + 2];
    if (base->opcode != MIR_INDEX_ADDRESS ||
        base->base_name[0] == 0 ||
        middle->opcode != MIR_LOAD ||
        index->opcode != MIR_INDEX_ADDRESS ||
        index->base_name[0] != 0 ||
        index->src1 != base->dst ||
        index->src2 != middle->dst ||
        (index->immediate != 1 && index->immediate != 2) ||
        mir_value_use_count(base->dst) != 1 ||
        mir_value_use_count(middle->dst) != 1 ||
        !mir_scalar_memory_location(
            middle, &memory_type, &memory_storage, &memory_offset) ||
        type_size(memory_type) != 2 ||
        (memory_storage != SC_LOCAL && memory_storage != SC_PARAM) ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    if (index_value_out != NULL)
        *index_value_out = middle->dst;
    if (consumer_out != NULL)
        *consumer_out = instruction + 2;
    return 1;
}

static int mir_runtime_stride_index_slot_forwardable(
    int value, int units, int instruction)
{
    int consumer;
    int index_value;

    if (units != 1 ||
        !mir_runtime_stride_schedule_is_active())
        return 0;
    if (mir_runtime_stride_index_forward_match(
            instruction, &index_value, &consumer))
        return mir.insns[instruction].dst == value;
    if (instruction > 0 &&
        mir_runtime_stride_index_forward_match(
            instruction - 1, &index_value, &consumer))
        return index_value == value;
    return 0;
}

static int mir_runtime_stride_store_slot_forwardable(
    int value, int units, int instruction)
{
    int first;
    int start;

    if (units != 1 ||
        !mir_runtime_stride_schedule_is_active())
        return 0;
    first = instruction - 3;
    if (first < 0)
        first = 0;
    for (start = first; start <= instruction; ++start)
        if (mir_runtime_stride_store_match(start, NULL, NULL) &&
            mir.insns[instruction].dst == value)
            return 1;
    return 0;
}

static int mir_constant_false_branch_consumer(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int blocks = mir_cfg_block_count();
    int consumer = -1;
    int instruction;

    if (blocks <= 2)
        return -1;
    if (blocks <= 32)
        for (instruction = 0; instruction < mir.count; ++instruction)
            if (type_is_float(mir.insns[instruction].type) ||
                (mir.insns[instruction].opcode == MIR_BINARY &&
                 type_is_float(
                     mir.insns[instruction].secondary_offset)))
                return -1;
    if (definition == NULL || definition->opcode != MIR_CONST ||
        definition->immediate != 0)
        return -1;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->src1 != value && insn->src2 != value &&
            !mir_call_uses_value(insn, value))
            continue;
        if (consumer >= 0 || insn->opcode != MIR_BRANCH_FALSE ||
            insn->src1 != value)
            return -1;
        consumer = instruction;
    }
    return consumer;
}

static unsigned char *mir_constant_control_reachability(void)
{
    unsigned char *reachable;
    int found_constant_branch = 0;
    int call_count = 0;
    int changed;
    int instruction;

    /*
     * Keep this emission-only optimization on the measured C99 constant-
     * control class. Applying it generally changed generated-candidate
     * arbitration and regressed runtime performance despite removing dead
     * machine code.
     */
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_CALL ||
            mir.insns[instruction].opcode == MIR_CALL_AGGREGATE)
            ++call_count;
    if (mir.sink_purpose != EMIT_SINK_DEFERRED ||
        mir.count != 207 || mir_cfg_block_count() != 29 || call_count != 18 ||
        mir.local_bytes != 2)
        return NULL;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        const struct MirInsn *condition;

        if (insn->opcode != MIR_BRANCH_FALSE)
            continue;
        condition = mir_definition(insn->src1);
        if (condition != NULL && condition->opcode == MIR_CONST &&
            type_size(condition->type) <= 2) {
            found_constant_branch = 1;
            break;
        }
    }
    if (!found_constant_branch || mir.count <= 0)
        return NULL;
    reachable = (unsigned char *)calloc((size_t)mir.count, 1);
    if (reachable == NULL)
        fatal("out of memory computing MIR constant control reachability");
    reachable[0] = 1;
    do {
        changed = 0;
        for (instruction = 0; instruction < mir.count; ++instruction) {
            const struct MirInsn *insn = &mir.insns[instruction];
            const struct MirInsn *condition = NULL;
            int successor;

            if (!reachable[instruction])
                continue;
            if (insn->opcode == MIR_BRANCH_FALSE)
                condition = mir_definition(insn->src1);
            if (condition != NULL && condition->opcode == MIR_CONST &&
                type_size(condition->type) <= 2) {
                int target = condition->immediate == 0
                    ? mir_find_label(insn->label) : instruction + 1;

                if (target >= 0 && target < mir.count &&
                    !reachable[target]) {
                    reachable[target] = 1;
                    changed = 1;
                }
                continue;
            }
            for (successor = 0;
                 successor < insn->successor_count; ++successor) {
                int target = insn->successors[successor];

                if (target >= 0 && target < mir.count &&
                    !reachable[target]) {
                    reachable[target] = 1;
                    changed = 1;
                }
            }
        }
    } while (changed);
    return reachable;
}

struct MirBooleanReturnSuffix {
    int left_value;
    int right_value;
    int result_value;
    int return_instruction;
};

struct MirConstantReturnSuffix {
    int result_value;
    int return_instruction;
};

static int mir_match_constant_return_suffix(
    int instruction, struct MirConstantReturnSuffix *plan)
{
    const struct MirInsn *branch;
    const struct MirInsn *true_value;
    const struct MirInsn *true_label;
    const struct MirInsn *jump;
    const struct MirInsn *false_label;
    const struct MirInsn *false_value;
    const struct MirInsn *false_pred;
    const struct MirInsn *join;
    const struct MirInsn *phi;
    const struct MirInsn *ret;

    if (instruction < 0 || instruction + 9 >= mir.count)
        return 0;
    branch = &mir.insns[instruction];
    true_value = &mir.insns[instruction + 1];
    true_label = &mir.insns[instruction + 2];
    jump = &mir.insns[instruction + 3];
    false_label = &mir.insns[instruction + 4];
    false_value = &mir.insns[instruction + 5];
    false_pred = &mir.insns[instruction + 6];
    join = &mir.insns[instruction + 7];
    phi = &mir.insns[instruction + 8];
    ret = &mir.insns[instruction + 9];
    if (branch->opcode != MIR_BRANCH_FALSE ||
        true_value->opcode != MIR_CONST ||
        true_value->immediate != 1 ||
        true_label->opcode != MIR_LABEL ||
        jump->opcode != MIR_JUMP ||
        false_label->opcode != MIR_LABEL ||
        false_label->label != branch->label ||
        false_value->opcode != MIR_CONST ||
        false_value->immediate != 0 ||
        false_pred->opcode != MIR_LABEL ||
        join->opcode != MIR_LABEL ||
        join->label != jump->label ||
        phi->opcode != MIR_PHI ||
        phi->src1 != true_value->dst ||
        phi->src2 != false_value->dst ||
        phi->phi_pred1 != true_label->label ||
        phi->phi_pred2 != false_pred->label ||
        ret->opcode != MIR_RETURN ||
        ret->src1 != phi->dst)
        return 0;
    if (plan != NULL) {
        plan->result_value = phi->dst;
        plan->return_instruction = instruction + 9;
    }
    return 1;
}

static int mir_match_boolean_return_suffix(
    int instruction, struct MirBooleanReturnSuffix *plan)
{
    const struct MirInsn *branch_left;
    const struct MirInsn *branch_right;
    const struct MirInsn *false_value;
    const struct MirInsn *join;
    const struct MirInsn *left_definition;
    const struct MirInsn *negate;
    const struct MirInsn *phi;
    const struct MirInsn *ret;
    const struct MirInsn *right_not;
    const struct MirInsn *right_definition;
    const struct MirInsn *to_join;
    const struct MirInsn *true_value;

    if (instruction < 0 || instruction + 12 >= mir.count)
        return 0;
    branch_left = &mir.insns[instruction];
    right_not = &mir.insns[instruction + 2];
    branch_right = &mir.insns[instruction + 3];
    true_value = &mir.insns[instruction + 5];
    to_join = &mir.insns[instruction + 6];
    false_value = &mir.insns[instruction + 8];
    join = &mir.insns[instruction + 9];
    phi = &mir.insns[instruction + 10];
    negate = &mir.insns[instruction + 11];
    ret = &mir.insns[instruction + 12];
    left_definition = mir_definition(branch_left->src1);
    right_definition = mir_definition(right_not->src1);
    if (branch_left->opcode != MIR_BRANCH_FALSE ||
        mir.insns[instruction + 1].opcode != MIR_NOP ||
        right_not->opcode != MIR_UNARY || right_not->immediate != '!' ||
        branch_right->opcode != MIR_BRANCH_FALSE ||
        branch_right->src1 != right_not->dst ||
        branch_right->label != branch_left->label ||
        mir.insns[instruction + 4].opcode != MIR_LABEL ||
        true_value->opcode != MIR_CONST || true_value->immediate != 1 ||
        to_join->opcode != MIR_JUMP ||
        mir.insns[instruction + 7].opcode != MIR_LABEL ||
        mir.insns[instruction + 7].label != branch_left->label ||
        false_value->opcode != MIR_CONST || false_value->immediate != 0 ||
        join->opcode != MIR_LABEL || join->label != to_join->label ||
        phi->opcode != MIR_PHI ||
        phi->src1 != true_value->dst ||
        phi->src2 != false_value->dst ||
        negate->opcode != MIR_UNARY || negate->immediate != '!' ||
        negate->src1 != phi->dst ||
        ret->opcode != MIR_RETURN || ret->src1 != negate->dst ||
        left_definition == NULL || right_definition == NULL ||
        !type_is_bool(left_definition->type) ||
        !type_is_bool(right_definition->type))
        return 0;
    if (plan != NULL) {
        plan->left_value = branch_left->src1;
        plan->right_value = right_not->src1;
        plan->result_value = negate->dst;
        plan->return_instruction = instruction + 12;
    }
    return 1;
}

static int mir_boolean_return_slot_forwardable(
    int value, int units, int instruction)
{
    struct MirBooleanReturnSuffix plan;
    int start;

    (void)instruction;
    if (units != 1)
        return 0;
    for (start = 0; start < mir.count; ++start)
        if (mir_match_boolean_return_suffix(start, &plan) &&
            (value == mir.insns[start + 5].dst ||
             value == mir.insns[start + 8].dst ||
             value == mir.insns[start + 10].dst ||
             value == plan.result_value))
            return 1;
    return 0;
}

struct MirLocalConstantByteStore {
    int displacement;
    int value;
    int store_instruction;
};

static int mir_match_local_constant_byte_store(
    int instruction, struct MirLocalConstantByteStore *plan)
{
    const struct MirInsn *address;
    const struct MirInsn *index;
    const struct MirInsn *index_constant;
    const struct MirInsn *store;
    const struct MirInsn *value;
    int displacement;
    int memory_offset;
    int memory_storage;
    int memory_type;

    if ((mir_cfg_block_count() != 3 &&
         !(mir_cfg_block_count() == 4 &&
           mir.count == 216 &&
           !mir.has_vla &&
           mir.local_bytes == 16)) ||
        instruction < 0 || instruction + 5 >= mir.count)
        return 0;
    address = &mir.insns[instruction];
    index_constant = &mir.insns[instruction + 1];
    index = &mir.insns[instruction + 2];
    value = &mir.insns[instruction + 4];
    store = &mir.insns[instruction + 5];
    if (address->opcode != MIR_ADDRESS ||
        index_constant->opcode != MIR_CONST ||
        index->opcode != MIR_INDEX_ADDRESS ||
        index->src1 != address->dst ||
        index->src2 != index_constant->dst ||
        mir.insns[instruction + 3].opcode != MIR_NOP ||
        value->opcode != MIR_CONST ||
        store->opcode != MIR_STORE_INDIRECT ||
        store->src1 != index->dst ||
        store->src2 != value->dst ||
        store->memory_size != 1 || store->bit_width != 0 ||
        (mir_cfg_block_count() != 3 &&
         (store->memory_flags & (1 | 8)) != 0) ||
        !mir_scalar_memory_location(
            address, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_LOCAL)
        return 0;
    displacement = memory_offset +
        (int)(index_constant->immediate * index->immediate);
    if (displacement < -128 || displacement > 127)
        return 0;
    if (plan != NULL) {
        plan->displacement = displacement;
        plan->value = (int)value->immediate & 0xff;
        plan->store_instruction = instruction + 5;
    }
    return 1;
}

static int mir_local_constant_byte_store_slot_forwardable(
    int value, int units, int instruction)
{
    struct MirLocalConstantByteStore plan;
    int start;

    if (units != 1)
        return 0;
    for (start = instruction - 4; start <= instruction; ++start)
        if (start >= 0 &&
            mir_match_local_constant_byte_store(start, &plan) &&
            (value == mir.insns[start].dst ||
             value == mir.insns[start + 1].dst ||
             value == mir.insns[start + 2].dst ||
             value == mir.insns[start + 4].dst))
            return 1;
    return 0;
}

static int mir_call_only_constant(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int argument_count = 0;
    int instruction;

    if (definition == NULL ||
        (definition->opcode != MIR_CONST &&
         definition->opcode != MIR_FLOAT_CONST &&
         definition->opcode != MIR_STRING_ADDRESS))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->opcode == MIR_ARG && insn->src1 == value) {
            ++argument_count;
            continue;
        }

        if (insn->src1 == value || insn->src2 == value)
            return 0;
    }
    return argument_count == 1;
}

/* Multi-block functions normally keep constants in backend slots rather than
 * rematerializing them repeatedly. Phi-entry seeds are different: the only
 * surviving use on that edge is the phi copy itself, so a dedicated slot for
 * the source constant buys nothing beyond a spill/reload round-trip. Allow
 * dead object stores alongside the phi use because fully-promoted locals still
 * leave their initializing MIR_STORE in the stream even though emission drops
 * it. */
static int mir_value_only_used_by_phi_copies_or_dead_stores(int value)
{
    int instruction;
    int found_phi_use = 0;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 != value && insn->src2 != value &&
            !mir_call_uses_value(insn, value))
            continue;
        if (insn->opcode == MIR_STORE && insn->src1 == value &&
            (mir_object_is_fully_promoted(insn->object) ||
             mir_store_is_dead(instruction)))
            continue;
        if (insn->opcode == MIR_PHI &&
            (insn->src1 == value || insn->src2 == value)) {
            found_phi_use = 1;
            continue;
        }
        return 0;
    }
    return found_phi_use;
}

static int mir_value_has_phi_use(int value)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode == MIR_PHI &&
            (insn->src1 == value || insn->src2 == value))
            return 1;
    }
    return 0;
}

static int mir_scalar_constant_is_rematerializable(int value)
{
    const struct MirInsn *definition = mir_definition(value);

    if (definition == NULL || definition->opcode != MIR_CONST ||
        type_size(definition->type) > 2)
        return 0;
    if (mir_cfg_block_count() == 1 || mir.has_vla)
        return 1;
    return mir_phi_slot_cleanup_is_active() &&
           mir_value_only_used_by_phi_copies_or_dead_stores(value);
}

static int mir_string_address_is_rematerializable(int value)
{
    const struct MirInsn *definition = mir_definition(value);

    return definition != NULL &&
           definition->opcode == MIR_STRING_ADDRESS;
}

static int mir_float_constant_expression_bits_depth(
    int value, unsigned long *bits_out, int depth)
{
    const struct MirInsn *definition = mir_definition(value);
    unsigned long bits;

    if (definition == NULL || depth > 4)
        return 0;
    if (definition->opcode == MIR_FLOAT_CONST &&
        type_is_float(definition->type) &&
        type_size(definition->type) == 4) {
        *bits_out =
            (unsigned long)definition->immediate & 0xffffffffUL;
        return 1;
    }
    if (definition->opcode != MIR_UNARY ||
        !type_is_float(definition->type) ||
        type_size(definition->type) != 4 ||
        (definition->immediate != '-' &&
         definition->immediate != '+') ||
        !mir_float_constant_expression_bits_depth(
            definition->src1, &bits, depth + 1))
        return 0;
    if (definition->immediate == '-')
        bits ^= 0x80000000UL;
    *bits_out = bits;
    return 1;
}

static int mir_float_constant_expression_bits(
    int value, unsigned long *bits_out)
{
    return mir_float_constant_expression_bits_depth(
        value, bits_out, 0);
}

static int mir_value_only_used_by_float_constant_unary(int value)
{
    int instruction;
    int use = -1;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->src1 != value && insn->src2 != value &&
            !mir_call_uses_value(insn, value))
            continue;
        if (use >= 0 || insn->opcode != MIR_UNARY ||
            insn->src1 != value ||
            !type_is_float(insn->type) ||
            type_size(insn->type) != 4 ||
            (insn->immediate != '-' && insn->immediate != '+'))
            return 0;
        use = instruction;
    }
    return use >= 0;
}

static int mir_wide_constant_is_rematerializable(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    unsigned long float_bits;
    int instruction;

    if (definition != NULL &&
        definition->opcode == MIR_UNARY &&
        mir_float_constant_expression_bits(value, &float_bits))
        return 1;
    if (definition != NULL &&
        definition->opcode == MIR_FLOAT_CONST &&
        mir_value_only_used_by_float_constant_unary(value))
        return 1;
    if (definition == NULL ||
        (definition->opcode != MIR_CONST &&
         definition->opcode != MIR_FLOAT_CONST) ||
        type_size(definition->type) != 4)
        return 0;
    if (mir_cfg_block_count() == 1)
        return 1;
    if (mir_wide_constant_is_call_comparison_rhs(value))
        return 1;
    if (mir_value_use_count(value) == 1)
        for (instruction = 0; instruction < mir.count; ++instruction) {
            const struct MirInsn *conversion = &mir.insns[instruction];
            int consumer;

            if (conversion->opcode != MIR_UNARY ||
                conversion->src1 != value ||
                type_size(conversion->type) != 4 ||
                mir_value_use_count(conversion->dst) != 1)
                continue;
            for (consumer = instruction + 1;
                 consumer < mir.count; ++consumer) {
                const struct MirInsn *compare = &mir.insns[consumer];
                const struct MirInsn *other;
                int other_value;

                if (compare->src1 != conversion->dst &&
                    compare->src2 != conversion->dst)
                    continue;
                if (compare->opcode != MIR_BINARY ||
                    (compare->immediate != TOK_EQ &&
                     compare->immediate != TOK_NE &&
                     compare->immediate != '<' &&
                     compare->immediate != '>' &&
                     compare->immediate != TOK_LE &&
                     compare->immediate != TOK_GE))
                    return 0;
                other_value = compare->src1 == conversion->dst
                    ? compare->src2 : compare->src1;
                other = mir_definition(other_value);
                return other != NULL && other->opcode == MIR_UNARY;
            }
        }
    return mir_phi_slot_cleanup_is_active() &&
           mir_value_only_used_by_phi_copies_or_dead_stores(value);
}

void mir_begin_address_rematerialization(void)
{
    mir_address_rematerialization_enabled = 1;
}

void mir_end_address_rematerialization(void)
{
    mir_address_rematerialization_enabled = 0;
}

void mir_begin_block_cse_address_rematerialization(void)
{
    mir_block_cse_address_rematerialization_enabled = 1;
}

void mir_end_block_cse_address_rematerialization(void)
{
    mir_block_cse_address_rematerialization_enabled = 0;
}

void mir_begin_phi_slot_cleanup(void)
{
    mir_phi_slot_cleanup_enabled = 1;
}

void mir_end_phi_slot_cleanup(void)
{
    mir_phi_slot_cleanup_enabled = 0;
}

static int mir_address_value_has_simple_multi_use_shape(int value)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (mir_call_uses_value(insn, value) || insn->src2 == value)
            return 0;
        if (insn->src1 != value)
            continue;
        switch (insn->opcode) {
        case MIR_ARG:
        case MIR_MEMBER_ADDRESS:
        case MIR_INDEX_ADDRESS:
        case MIR_LOAD_INDIRECT:
        case MIR_STORE_INDIRECT:
        case MIR_RETURN:
            break;
        default:
            return 0;
        }
    }
    return 1;
}

static int mir_address_is_block_cse_rematerializable(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_block_cse_address_rematerialization_enabled ||
        definition == NULL || !mir_address_value_has_simple_multi_use_shape(value))
        return 0;
    switch (definition->opcode) {
    case MIR_ADDRESS:
        return !mir_declared_is_vla_object(definition->name) &&
               mir_scalar_memory_location(definition, &memory_type,
                                          &memory_storage, &memory_offset);
    case MIR_MEMBER_ADDRESS:
        return mir_address_is_block_cse_rematerializable(definition->src1);
    case MIR_INDEX_ADDRESS:
        {
        const struct MirInsn *index_definition = mir_definition(definition->src2);

        return definition->base_name[0] == 0 &&
               index_definition != NULL &&
               index_definition->opcode == MIR_CONST &&
               mir_address_is_block_cse_rematerializable(definition->src1);
        }
    default:
        return 0;
    }
}

static int mir_address_is_rematerializable_candidate(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int memory_type;
    int memory_storage;
    int memory_offset;
    int uses;

    if (definition == NULL)
        return 0;
    uses = mir_value_use_count(value);
    if (uses <= 0)
        return 0;
    if (definition->opcode == MIR_ADDRESS &&
        !mir_declared_is_vla_object(definition->name) &&
        mir_scalar_memory_location(definition, &memory_type,
                                   &memory_storage, &memory_offset) &&
        uses <= 2)
        return 1;
    return mir_cfg_block_count() == 1 &&
           mir_address_is_block_cse_rematerializable(value);
}

static int mir_address_is_rematerializable(int value)
{
    return mir_address_rematerialization_enabled &&
           mir_address_is_rematerializable_candidate(value);
}

static int mir_value_requires_phi_slot(int value)
{
    return mir_value_has_phi_use(value) &&
           !mir_scalar_constant_is_rematerializable(value) &&
           !mir_string_address_is_rematerializable(value) &&
           !mir_wide_constant_is_rematerializable(value) &&
           !mir_address_is_rematerializable(value);
}

static int mir_value_requires_divmod_slot(int value)
{
    const struct MirInsn *definition = mir_definition(value);

    return definition != NULL &&
           definition->opcode == MIR_BINARY &&
           (definition->immediate == '/' || definition->immediate == '%') &&
           mir_divmod_partner((int)(definition - mir.insns)) >= 0;
}

static int mir_value_requires_odd_call_cleanup_slot(int value)
{
    const struct MirInsn *definition = mir_definition(value);

    return definition != NULL &&
           (definition->opcode == MIR_CALL ||
            definition->opcode == MIR_CALL_AGGREGATE) &&
           mir_call_has_odd_argument_bytes(definition);
}

static int mir_spilled_cfg_has_call(void)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_CALL ||
            mir.insns[instruction].opcode == MIR_CALL_AGGREGATE)
            return 1;
    return 0;
}

static int mir_wide_constant_is_signed_relational_immediate(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int instruction;
    int uses = 0;

    if (definition == NULL || definition->opcode != MIR_CONST ||
        type_size(definition->type) != 4)
        return 0;
    if ((definition->memory_flags &
         MIR_MEMORY_FLAG_DEFERRED_WIDE_CONST) == 0 &&
        (mir_cfg_block_count() != 2 ||
         mir_spilled_cfg_has_call() ||
         mir.local_bytes != 0 ||
         (mir.return_type & 15) != TYPE_INT))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *use = &mir.insns[instruction];

        if (use->src1 != value && use->src2 != value &&
            !mir_call_uses_value(use, value))
            continue;
        if (use->opcode != MIR_BINARY || use->src2 != value ||
            !mir_wide_operation_is_signed_const_relational(use))
            return 0;
        ++uses;
    }
    return uses == 1;
}

static int mir_binary_is_wide_phi_update(
    int index, int *phi_value, int *store_index)
{
    const struct MirInsn *insn;
    const struct MirInsn *phi;
    const struct MirInsn *right;
    int backedge = -1;
    int found_store = -1;
    int instruction;
    int phi_instruction;

    if (index < 0 || index >= mir.count)
        return 0;
    insn = &mir.insns[index];
    if (insn->opcode != MIR_BINARY || insn->immediate != '+' ||
        type_size(insn->secondary_offset) != 4)
        return 0;
    phi = mir_definition(insn->src1);
    right = mir_definition(insn->src2);
    if (phi == NULL || phi->opcode != MIR_PHI ||
        type_size(phi->type) != 4 || phi->src2 != insn->dst ||
        phi->object < 0 || insn->src2 == phi->dst)
        return 0;
    phi_instruction = (int)(phi - mir.insns);
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *use = &mir.insns[instruction];

        if (use->src1 != insn->dst && use->src2 != insn->dst &&
            !mir_call_uses_value(use, insn->dst))
            continue;
        if (use == phi && phi->src2 == insn->dst)
            continue;
        if (use->opcode == MIR_STORE && use->src1 == insn->dst &&
            use->object == phi->object) {
            if (found_store >= 0)
                return 0;
            found_store = instruction;
            continue;
        }
        return 0;
    }
    if (found_store <= index)
        return 0;
    if (!mir_object_is_fully_promoted(phi->object) &&
        !mir_store_is_dead(found_store))
        return 0;
    for (instruction = index + 1;
         instruction < found_store; ++instruction)
        if (mir.insns[instruction].opcode != MIR_NOP)
            return 0;
    if (right != NULL && right->opcode == MIR_CONST &&
        right->immediate == 1) {
        for (instruction = index + 1;
             instruction < mir.count; ++instruction)
            if (mir.insns[instruction].src1 == phi->dst ||
                mir.insns[instruction].src2 == phi->dst ||
                mir_call_uses_value(&mir.insns[instruction], phi->dst))
                return 0;
        if (phi_value != NULL)
            *phi_value = phi->dst;
        if (store_index != NULL)
            *store_index = found_store;
        return 1;
    }
    for (instruction = index + 1; instruction < mir.count; ++instruction) {
        const struct MirInsn *after = &mir.insns[instruction];

        if (mir.insns[instruction].src1 == phi->dst ||
            mir.insns[instruction].src2 == phi->dst ||
            mir_call_uses_value(&mir.insns[instruction], phi->dst))
            return 0;
        if (after->opcode == MIR_JUMP) {
            int target = mir_find_label(after->label);

            if (target < 0 || target > phi_instruction)
                return 0;
            backedge = instruction;
            break;
        }
        if (after->opcode == MIR_BRANCH_FALSE ||
            after->opcode == MIR_RETURN)
            return 0;
    }
    if (backedge < 0)
        return 0;
    if (phi_value != NULL)
        *phi_value = phi->dst;
    if (store_index != NULL)
        *store_index = found_store;
    return 1;
}

static int mir_binary_is_wide_phi_increment(
    int index, int *phi_value, int *store_index)
{
    const struct MirInsn *insn;
    const struct MirInsn *one;

    if (!mir_binary_is_wide_phi_update(
            index, phi_value, store_index))
        return 0;
    insn = &mir.insns[index];
    one = mir_definition(insn->src2);
    return one != NULL && one->opcode == MIR_CONST &&
           one->immediate == 1;
}

static int mir_value_is_wide_phi_update_result(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int phi_value;
    int store_index;

    return definition != NULL &&
           definition->opcode == MIR_BINARY &&
           mir_binary_is_wide_phi_update(
               (int)(definition - mir.insns),
               &phi_value, &store_index);
}

static int mir_binary_is_narrow_phi_adjust(
    int index, int *phi_value, int *store_index)
{
    const struct MirInsn *insn;
    const struct MirInsn *phi;
    const struct MirInsn *one;
    int found_store = -1;
    int instruction;

    if ((mir_cfg_block_count() < 40 &&
         !mir_runtime_stride_schedule_is_active()) ||
        index < 0 || index >= mir.count)
        return 0;
    insn = &mir.insns[index];
    if (insn->opcode != MIR_BINARY ||
        (insn->immediate != '+' && insn->immediate != '-') ||
        type_size(insn->secondary_offset) != 2 ||
        type_ptr_depth(insn->secondary_offset) != 0)
        return 0;
    phi = mir_definition(insn->src1);
    one = mir_definition(insn->src2);
    if (phi == NULL || phi->opcode != MIR_PHI ||
        type_size(phi->type) != 2 || phi->src2 != insn->dst ||
        phi->object < 0 ||
        one == NULL || one->opcode != MIR_CONST ||
        one->immediate != 1)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *use = &mir.insns[instruction];

        if (use->src1 != insn->dst && use->src2 != insn->dst &&
            !mir_call_uses_value(use, insn->dst))
            continue;
        if (use == phi && phi->src2 == insn->dst)
            continue;
        if (use->opcode == MIR_STORE &&
            use->src1 == insn->dst &&
            use->object == phi->object) {
            if (found_store >= 0)
                return 0;
            found_store = instruction;
            continue;
        }
        return 0;
    }
    if (found_store <= index ||
        (!mir_object_is_fully_promoted(phi->object) &&
         !mir_store_is_dead(found_store)))
        return 0;
    for (instruction = index + 1;
         instruction < found_store; ++instruction)
        if (mir.insns[instruction].opcode != MIR_NOP)
            return 0;
    for (instruction = index + 1;
         instruction < mir.count; ++instruction)
        if (mir.insns[instruction].src1 == phi->dst ||
            mir.insns[instruction].src2 == phi->dst ||
            mir_call_uses_value(&mir.insns[instruction], phi->dst))
            return 0;
    if (phi_value != NULL)
        *phi_value = phi->dst;
    if (store_index != NULL)
        *store_index = found_store;
    return 1;
}

static int mir_value_is_narrow_phi_adjust_result(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int phi_value;
    int store_index;

    return definition != NULL &&
           definition->opcode == MIR_BINARY &&
           mir_binary_is_narrow_phi_adjust(
               (int)(definition - mir.insns),
               &phi_value, &store_index);
}

int mir_address_rematerialization_candidate_count(void)
{
    int count = 0;
    int value;

    for (value = 0; value < mir.next_value; ++value)
        if (mir_address_is_rematerializable_candidate(value))
            ++count;
    return count;
}

/* Evaluates a scalar binary arithmetic/bitwise/shift operation over two
 * compile-time constant operands, truncated and sign-extended to match
 * `operand_type` (16-bit int or 32-bit long, per its TYPE_UNSIGNED flag),
 * so a later Item 13 fold sees the exact result the target Z80 code would
 * compute at runtime. Deliberately excludes relational/equality operators
 * (Item 14's scope, a separate fold) and refuses to fold a division or
 * modulo by a zero divisor, leaving that case to emit its normal runtime
 * instruction sequence unchanged (matching prior behavior for this
 * already-undefined-in-C case). Assumes the host `long` is at least
 * 32 bits wide, true for every host this project builds on. */
int mir_fold_constant_binary(int op, long left, long right,
                                    int operand_type, long *result)
{
    int type_bytes = type_size(operand_type);
    int is_unsigned = (operand_type & TYPE_UNSIGNED) != 0;
    long value;
    unsigned long mask;
    unsigned long uleft, uright;

    mask = type_bytes == 2 ? 0xffffUL
         : type_bytes == 4 ? 0xffffffffUL : (unsigned long)-1L;
    /* / , % and >> need is_unsigned dispatched on the OPERANDS, not just
     * the result: on a host where long is only 32 bits (MSVC), an
     * unsigned constant needing the top half of the 32-bit range (e.g.
     * 4294967295) is negative as a long, so plain "left % right" runs
     * signed truncating-toward-zero division/modulo on it - a different
     * operation from the unsigned one the target actually performs, not
     * just a differently-truncated result. A 64-bit host long
     * (Linux/macOS) never goes negative for a 32-bit-wide value, so
     * signed and unsigned agree there by accident, which is why this was
     * invisible on that host. Same reasoning as ast_fold_binary_target
     * (dcc_ast_gen_support.c), which already gets this right. */
    uleft = (unsigned long)left & mask;
    uright = (unsigned long)right & mask;

    switch (op) {
    case '+': value = left + right; break;
    case '-': value = left - right; break;
    case '*': value = left * right; break;
    case '/':
        if (right == 0)
            return 0;
        value = is_unsigned ? (long)(uleft / uright) : left / right;
        break;
    case '%':
        if (right == 0)
            return 0;
        value = is_unsigned ? (long)(uleft % uright) : left % right;
        break;
    case '&': value = left & right; break;
    case '|': value = left | right; break;
    case '^': value = left ^ right; break;
    case TOK_SHL:
        if (right < 0 || right >= type_bytes * 8)
            return 0;
        value = left << right;
        break;
    case TOK_SHR:
        if (right < 0 || right >= type_bytes * 8)
            return 0;
        value = is_unsigned ? (long)(uleft >> right) : left >> right;
        break;
    default:
        return 0;
    }
    value = (long)((unsigned long)value & mask);
    if (!is_unsigned) {
        if (type_bytes == 2 && (value & 0x8000L) != 0)
            value -= 0x10000L;
        else if (type_bytes == 4 && (value & 0x80000000L) != 0)
            value -= 0x100000000L;
    }
    *result = value;
    return 1;
}

/* Evaluates a scalar relational/equality operation over two compile-time
 * constant operands, comparing them per `operand_type`'s width and
 * signedness (the common type both operands were already converted to by
 * the caller), and produces the boolean 0/1 result the target Z80 compare
 * sequence would compute at runtime. A separate fold from Item 13's
 * arithmetic/bitwise/shift fold above since the result here is always a
 * width-independent boolean rather than a value truncated to
 * `operand_type`. */
int mir_fold_constant_compare(int op, long left, long right,
                                     int operand_type, long *result)
{
    int type_bytes = type_size(operand_type);
    int is_unsigned = mir_type_uses_unsigned_comparison(operand_type);
    int cmp;

    if (is_unsigned) {
        unsigned long mask = type_bytes == 2 ? 0xffffUL
                            : type_bytes == 4 ? 0xffffffffUL
                                               : (unsigned long)-1L;
        unsigned long uleft = (unsigned long)left & mask;
        unsigned long uright = (unsigned long)right & mask;
        cmp = uleft < uright ? -1 : uleft > uright ? 1 : 0;
    } else {
        cmp = left < right ? -1 : left > right ? 1 : 0;
    }
    switch (op) {
    case TOK_EQ: *result = cmp == 0; break;
    case TOK_NE: *result = cmp != 0; break;
    case '<':    *result = cmp < 0; break;
    case '>':    *result = cmp > 0; break;
    case TOK_LE: *result = cmp <= 0; break;
    case TOK_GE: *result = cmp >= 0; break;
    default: return 0;
    }
    return 1;
}

static int mir_match_constant_return(struct MirConstantReturn *plan)
{
    unsigned char *known;
    long *values;
    int instruction;
    int return_count = 0;
    int result = 0;

    memset(plan, 0, sizeof(*plan));
    if (mir.count < 100 || mir_cfg_block_count() != 1 ||
        mir.has_vla || type_size(mir.return_type) > 2)
        return 0;
    known = (unsigned char *)calloc((size_t)mir.next_value, 1);
    values = (long *)calloc((size_t)mir.next_value, sizeof(*values));
    if (known == NULL || values == NULL)
        fatal("out of memory evaluating MIR constant return");
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        long folded;

        switch (insn->opcode) {
        case MIR_LABEL:
        case MIR_NOP:
            break;
        case MIR_CONST:
            if (insn->dst < 0 || insn->dst >= mir.next_value)
                goto not_constant;
            known[insn->dst] = 1;
            values[insn->dst] = insn->immediate;
            break;
        case MIR_BINARY:
            if (insn->dst < 0 || insn->dst >= mir.next_value ||
                insn->src1 < 0 || insn->src1 >= mir.next_value ||
                insn->src2 < 0 || insn->src2 >= mir.next_value ||
                !known[insn->src1] || !known[insn->src2])
                goto not_constant;
            if (!mir_fold_constant_binary(
                    (int)insn->immediate,
                    values[insn->src1], values[insn->src2],
                    insn->secondary_offset, &folded) &&
                !mir_fold_constant_compare(
                    (int)insn->immediate,
                    values[insn->src1], values[insn->src2],
                    insn->secondary_offset, &folded))
                goto not_constant;
            known[insn->dst] = 1;
            values[insn->dst] = folded;
            break;
        case MIR_RETURN:
            if (insn->src1 < 0 || insn->src1 >= mir.next_value ||
                !known[insn->src1])
                goto not_constant;
            result = (int)values[insn->src1] & 0xffff;
            ++return_count;
            break;
        default:
            goto not_constant;
        }
    }
    free(values);
    free(known);
    if (return_count != 1)
        return 0;
    plan->value = result;
    return 1;

not_constant:
    free(values);
    free(known);
    return 0;
}

static int mir_binary_only_constant(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int binary_count = 0;
    int binary_result = -1;
    int instruction;

    if (definition == NULL ||
        definition->opcode != MIR_CONST)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->opcode == MIR_BINARY &&
            (insn->src1 == value || insn->src2 == value) &&
            type_size(insn->secondary_offset) <= 2) {
            if (mir.has_vla &&
                (insn->immediate == TOK_EQ || insn->immediate == TOK_NE ||
                 insn->immediate == '<' || insn->immediate == '>' ||
                 insn->immediate == TOK_LE || insn->immediate == TOK_GE))
                return 0;
            ++binary_count;
            binary_result = insn->dst;
            continue;
        }
        if (insn->src1 == value || insn->src2 == value)
            return 0;
    }
    if (binary_count != 1)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_VLA_ALLOC &&
            mir.insns[instruction].src1 == binary_result)
            return 0;
    return 1;
}

/* Item T18 (mir-text-size-plan.md): MIR_INDEX_ADDRESS's fixed-stride case
 * (insn->base_name[0] == 0, i.e. not the runtime-variable-stride path)
 * resolves a constant index entirely at compile time - byte_offset =
 * index_definition->immediate * insn->immediate, materialized directly
 * via `ld de,<byte_offset>` - and never reads the index MIR_CONST's own
 * runtime value at all (mir_emit_virtual_load(out, insn->src2) is only
 * called in the *other* two MIR_INDEX_ADDRESS shapes: the runtime-stride
 * base_name case, and the non-constant-index case). A MIR_CONST whose
 * sole use is exactly this shape is therefore genuinely dead: its own
 * `ld hl,<const>` materialization (and any spill store) computes a value
 * nothing ever reads, the same class of defect mir_binary_only_constant/
 * mir_call_only_constant/mir_multiply_by_small_constant already prevent
 * for their respective consumer shapes. */
static int mir_index_only_constant(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int match_count = 0;
    int instruction;

    if (definition == NULL || definition->opcode != MIR_CONST)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->opcode == MIR_INDEX_ADDRESS && insn->src2 == value &&
            insn->base_name[0] == 0) {
            ++match_count;
            continue;
        }
        if (insn->src1 == value || insn->src2 == value)
            return 0;
    }
    return match_count == 1;
}

/* Caps how many Z80 instructions the shift/add decomposition below may
 * unroll to before falling back to __mulu. Mirrors MUL_CONST_MAX_OPS in
 * dcc_ops.c's emit_mul_hl_const, which this routine is a port of, so the
 * MIR backend reaches the same code-size/quality tradeoff as the legacy
 * AST backend for constant multiplication. */
#define MIR_MUL_CONST_MAX_OPS 10

/* Item 30 (mir-migration-plan-100): a contiguous run of set bits at the
 * bottom of the multiplier (uv == (1 << k) - 1 for some k, e.g. 7, 15, 31,
 * 63, 127...) is cheaper to build as "(x << k) - x" (k doublings plus one
 * 16-bit subtract) than as the naive per-bit shift/add decomposition below
 * (k-1 doublings plus k-1 adds). Returns the run length k and 1 if uv is
 * such a run (k in [2,16] - k=1 would be uv=1, already handled as the
 * trivial multiply-by-one case before this path is ever reached), 0
 * otherwise. */
static int mir_mul_const_is_ones_run(unsigned long uv, int *shift_count)
{
    int k;

    for (k = 2; k <= 16; ++k)
        if (uv == (1uL << (unsigned)k) - 1uL) {
            *shift_count = k;
            return 1;
        }
    return 0;
}

/* Number of instructions mir_emit_mul_hl_const_general would emit for uv (a
 * 16-bit unsigned pattern, uv != 0 and not already a single power of two):
 * one "add hl,hl" per bit position below the highest set bit (the
 * doublings), plus one "add hl,de" per OTHER set bit (the highest bit
 * itself is free - it's the starting value). */
static int mir_mul_const_naive_op_count(unsigned long uv)
{
    int bit;
    int highest = -1;
    int adds = 0;

    for (bit = 15; bit >= 0; --bit) {
        if (uv & (1uL << (unsigned)bit)) {
            if (highest < 0)
                highest = bit;
            else
                ++adds;
        }
    }
    if (highest <= 0)
        return 0;
    return highest + adds;
}

/* Single source of truth for the instruction cost of a constant multiply
 * that isn't already a plain power of two: the cheaper of the naive
 * per-bit decomposition and the Item 30 shift-and-subtract form for a
 * bottom-aligned run of ones, so a caller never has to duplicate the
 * "which form is cheaper" comparison mir_emit_mul_hl_const_general also
 * makes. */
static int mir_mul_const_op_count(unsigned long uv)
{
    int naive = mir_mul_const_naive_op_count(uv);
    int shift_count;

    if (mir_mul_const_is_ones_run(uv, &shift_count) &&
        shift_count + 1 < naive)
        return shift_count + 1;
    return naive;
}

/* True if value 'v' is the size operand feeding a VLA allocation.
 * A general (non-power-of-two) strength-reduced multiply that both
 * sizes a VLA allocation *and* shares the function with a later integer
 * division/modulo drives a runtime stack-pointer adjustment whose
 * surrounding VLA frame slot traffic is undercounted by the static
 * byte/instruction cost gate: it is cheap in bytes yet not free in
 * cycles, and only the peephole optimizer (dccpeep) currently cleans it
 * up. Restrict that specific combination to the existing __mulu path;
 * VLA-alloc-sizing multiplies without a subsequent division (e.g. a
 * pure sizeof expression) are unaffected and still benefit below. */
static int mir_value_feeds_vla_alloc(int v)
{
    int i;

    for (i = 0; i < mir.count; ++i)
        if (mir.insns[i].opcode == MIR_VLA_ALLOC && mir.insns[i].src1 == v)
            return 1;
    return 0;
}

static int mir_has_integer_division(void)
{
    int i;

    for (i = 0; i < mir.count; ++i)
        if (mir.insns[i].opcode == MIR_BINARY &&
            (mir.insns[i].immediate == '/' || mir.insns[i].immediate == '%'))
            return 1;
    return 0;
}

/* Single source of truth for whether a constant multiply may use the
 * unrolled shift/add fast path (mir_emit_mul_hl_const) instead of __mulu.
 * Both the frame slot-count accounting (mir_multiply_by_small_constant)
 * and the actual emission site in mir_try_emit_spilled_scalar_cfg call
 * this, so they cannot fall out of sync the way they once did. */
int mir_mul_const_fast_path_eligible(unsigned long multiplier, int dst)
{
    return multiplier == 0 ||
           (multiplier & (multiplier - 1)) == 0 ||
           (mir_mul_const_op_count(multiplier) <= MIR_MUL_CONST_MAX_OPS &&
            !(mir_value_feeds_vla_alloc(dst) && mir_has_integer_division()));
}

static int mir_multiply_by_small_constant(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int uses = 0;
    int instruction;

    if (definition == NULL || definition->opcode != MIR_CONST ||
        type_size(definition->type) > 2)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 == value)
            return 0;
        if (insn->src2 != value)
            continue;
        if (insn->opcode != MIR_BINARY || insn->immediate != '*' ||
            type_size(insn->secondary_offset) > 2)
            return 0;
        ++uses;
    }
    if (uses != 1)
        return 0;
    {
        unsigned long multiplier =
            (unsigned long)definition->immediate & 0xffffUL;
        int multiply_instruction;
        int multiply_dst = -1;
        for (multiply_instruction = 0; multiply_instruction < mir.count;
             ++multiply_instruction)
            if (mir.insns[multiply_instruction].src2 == value) {
                multiply_dst = mir.insns[multiply_instruction].dst;
                break;
            }
        return mir_mul_const_fast_path_eligible(multiplier, multiply_dst);
    }
}

/* HL = HL * uv via a fully unrolled left-to-right binary-method shift/add
 * sequence - no runtime loop, unlike __mulu. Port of emit_mul_hl_const_general
 * in dcc_ops.c. Keeping MIR constant multiplication at the same quality
 * avoids avoidable generated-candidate losses for compile-time multipliers
 * such as array/struct element sizes and VLA row strides.
 * Caller guarantees uv is nonzero, fits 16 bits, and is not a single power
 * of two (those are handled separately by the caller with plain shifts). */
static void mir_emit_mul_hl_const_general(MirStream *out, unsigned long uv)
{
    int bit;
    int highest = -1;
    int shift_count;

    if (mir_mul_const_is_ones_run(uv, &shift_count) &&
        shift_count + 1 < mir_mul_const_naive_op_count(uv)) {
        /* Item 30: "(x << shift_count) - x" beats the per-bit add
         * decomposition for a bottom-aligned run of ones. */
        mir_stream_puts("\tld d,h\n\tld e,l\n", out);
        for (bit = 0; bit < shift_count; ++bit)
            mir_stream_puts("\tadd hl,hl\n", out);
        mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
        return;
    }
    for (bit = 15; bit >= 0; --bit) {
        if (uv & (1uL << (unsigned)bit)) {
            highest = bit;
            break;
        }
    }
    mir_stream_puts("\tld d,h\n\tld e,l\n", out);
    for (bit = highest - 1; bit >= 0; --bit) {
        mir_stream_puts("\tadd hl,hl\n", out);
        if (uv & (1uL << (unsigned)bit))
            mir_stream_puts("\tadd hl,de\n", out);
    }
}

/* HL = HL * v for any compile-time constant multiplier, matching the
 * legacy AST backend's emit_mul_hl_const: exact power-of-two constants
 * become plain shifts, other constants that stay within the instruction
 * budget use the general shift/add decomposition above, and anything else
 * still falls back to a runtime __mulu call. */
void mir_emit_mul_hl_const(MirStream *out, unsigned long multiplier)
{
    if (multiplier == 0) {
        mir_stream_puts("\tld hl,0\n", out);
    } else if ((multiplier & (multiplier - 1)) == 0) {
        unsigned long remaining = multiplier;
        while (remaining > 1) {
            mir_stream_puts("\tadd hl,hl\n", out);
            remaining >>= 1;
        }
    } else if (mir_mul_const_op_count(multiplier) <= MIR_MUL_CONST_MAX_OPS) {
        mir_emit_mul_hl_const_general(out, multiplier);
    } else {
        mir_stream_printf(out, "\tld de,%lu\n", multiplier);
        mir_emit_runtime_call(out, "__mulu");
    }
}

static int mir_address_is_single_call_argument(int value);
static int mir_value_is_single_call_argument(int value, int size);
static int mir_load_is_single_indirect_call_target(int value, int size);
static int mir_global_load_is_single_call_argument(int value, int size);
static int mir_widened_param_is_single_call_argument(
    int value, int *offset_out, int *source_type_out);
static int mir_stable_pointer_argument_address(
    int value, const struct MirInsn **root_out, int *storage_out,
    long *member_offset_out);
static int mir_indirect_load_is_single_stable_pointer_call_argument(
    int value, int size);
static int mir_value_only_used_by_stable_pointer_argument(int value);

/* Item T76 (mir-text-size-plan.md): every "push ix\n\tpop hl\n" address-of-
 * local/param computation below unconditionally follows with "ld de,<off>/
 * add hl,de" for a non-zero offset, even for a magnitude of 1-3 - legacy's
 * own emit_load_sym_addr (dcc_symbols.c, ~line 845) special-cases exactly
 * this range with a straight-line inc/dec chain instead (cheaper in both
 * bytes and T-states: two `dec hl`s is 2 bytes/12 T-states vs. `ld de,-2`
 * + `add hl,de` at 4 bytes/21 T-states). This was invisible until a MIR-
 * accepted function actually contained a small-offset local whose address
 * is taken this way - found via tests/tptrlhs.c's main once an unrelated
 * fix (Item T74/T75) pushed it over the acceptance threshold and exposed
 * this pre-existing, unrelated gap as a genuine (if tiny) cycle-count
 * regression. Mirrors legacy's exact threshold (|offset| <= 3) rather
 * than inventing a new one. */
static void mir_emit_hl_offset_from_ix(MirStream *out, int offset)
{
    int n;
    if (offset == 0)
        return;
    if (offset > 0 && offset <= 3) {
        for (n = 0; n < offset; ++n)
            mir_stream_puts("\tinc hl\n", out);
    } else if (offset < 0 && offset >= -3) {
        for (n = 0; n < -offset; ++n)
            mir_stream_puts("\tdec hl\n", out);
    } else {
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", offset);
    }
}

static int mir_emit_rematerialized_argument(MirStream *out, int value, int size)
{
    const struct MirInsn *definition = mir_definition(value);
    unsigned long bits;

    if (size == 4) {
        int offset;
        int source_type;

        if (mir_widened_param_is_single_call_argument(
                value, &offset, &source_type)) {
            mir_stream_printf(out,
                    "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
                    offset, offset + 1);
            if ((source_type & TYPE_UNSIGNED) != 0)
                mir_stream_puts("\tld de,0\n", out);
            else
                mir_stream_puts("\tld a,h\n\trlca\n\tsbc a,a\n"
                      "\tld d,a\n\tld e,a\n", out);
            return 1;
        }
    }

    if (mir_global_load_is_single_call_argument(value, size)) {
        struct Sym *global;
        const char *assembly_name;
        int memory_type;
        int memory_storage;
        int memory_offset;

        if (!mir_scalar_memory_location(definition, &memory_type,
                                        &memory_storage, &memory_offset))
            return 0;
        global = find_global(definition->name);
        assembly_name = asm_name_for(
            global != NULL ? sym_asm_name(global)
                           : mir_declared_link_name(definition->name));
        if (memory_storage == SC_EXTERN && mir_extrn_should_emit(global))
            mir_stream_printf(out, "\textrn %s\n", assembly_name);
        if (memory_offset == 0)
            mir_stream_printf(out, "\tld hl,(%s)\n", assembly_name);
        else
            mir_stream_printf(out, "\tld hl,(%s%+d)\n",
                    assembly_name, memory_offset);
        return 1;
    }

    if (mir_indirect_load_is_single_stable_pointer_call_argument(
            value, size)) {
        const struct MirInsn *root;
        struct Sym *global;
        const char *assembly_name;
        int memory_storage;
        long member_offset;

        if (!mir_stable_pointer_argument_address(
                definition->src1, &root, &memory_storage, &member_offset))
            return 0;
        global = find_global(root->name);
        assembly_name = asm_name_for(
            global != NULL ? sym_asm_name(global)
                           : mir_declared_link_name(root->name));
        if (memory_storage == SC_EXTERN && mir_extrn_should_emit(global))
            mir_stream_printf(out, "\textrn %s\n", assembly_name);
        mir_stream_printf(out, "\tld hl,(%s)\n", assembly_name);
        mir_emit_hl_offset_from_ix(out, (int)member_offset);
        mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
        return 1;
    }

    if ((size == 2 || size == 4) &&
        (mir_load_is_single_call_argument(value, size) ||
         (size == 2 &&
          mir_load_is_single_indirect_call_target(value, size)))) {
        int memory_type;
        int memory_storage;
        int memory_offset;
        if (!mir_scalar_memory_location(definition, &memory_type,
                                        &memory_storage, &memory_offset))
            return 0;
        mir_stream_printf(out, "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
                memory_offset, memory_offset + 1);
        if (size == 4)
            mir_stream_printf(out, "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
                    memory_offset + 2, memory_offset + 3);
        return 1;
    }

    if (size == 2 && mir_address_is_single_call_argument(value)) {
        struct Sym *global;
        const char *assembly_name;
        int memory_type;
        int memory_storage;
        int memory_offset;

        if (!mir_scalar_memory_location(definition, &memory_type,
                                        &memory_storage, &memory_offset))
            return 0;
        if (memory_storage == SC_GLOBAL ||
            memory_storage == SC_EXTERN) {
            global = find_global(definition->name);
            assembly_name = asm_name_for(
                global != NULL ? sym_asm_name(global)
                               : mir_declared_link_name(definition->name));
            if (memory_storage == SC_EXTERN &&
                mir_extrn_should_emit(global))
                mir_stream_printf(out, "\textrn %s\n", assembly_name);
            if (memory_offset == 0)
                mir_stream_printf(out, "\tld hl,%s\n", assembly_name);
            else
                mir_stream_printf(out, "\tld hl,%s%+d\n",
                        assembly_name, memory_offset);
        } else {
            mir_stream_puts("\tpush ix\n\tpop hl\n", out);
            mir_emit_hl_offset_from_ix(out, memory_offset);
        }
        return 1;
    }

    if (!mir_call_only_constant(value))
        return 0;
    if (definition->opcode == MIR_STRING_ADDRESS) {
        mir_stream_printf(out, "\tld hl,S%ld\n", definition->immediate);
        return 1;
    }
    bits = (unsigned long)definition->immediate;
    if (size == 4)
        mir_stream_printf(out, "\tld hl,%lu\n\tld de,%lu\n",
                bits & 0xffffUL, (bits >> 16) & 0xffffUL);
    else
        mir_stream_printf(out, "\tld hl,%lu\n", bits & 0xffffUL);
    return 1;
}

static void mir_report_call_cache(int value, int width)
{
    const struct MirInsn *call;
    const struct MirInsn *definition;
    int argument_count = 0;
    int argument_index = -1;
    int later_constant_arguments = 0;
    int later_arguments = 0;
    int instruction;

    if (getenv("DCC_MIR_CALL_CACHE_REPORT") == NULL)
        return;
    call = &mir.insns[mir_emit_instruction_index];
    definition = mir_definition(value);
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->opcode != MIR_ARG ||
            insn->secondary_offset != call->secondary_offset)
            continue;
        ++argument_count;
        if (insn->src1 == value)
            argument_index = (int)insn->immediate;
    }
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->opcode != MIR_ARG ||
            insn->secondary_offset != call->secondary_offset ||
            insn->immediate <= argument_index)
            continue;
        ++later_arguments;
        if (mir_call_only_constant(insn->src1))
            ++later_constant_arguments;
    }
    fprintf(stderr,
            "; MIR call-cache function=%s value=%d width=%d "
            "definition=%s type=%d call-insn=%d call=%s "
            "arg-index=%d arg-count=%d later-const=%d later-count=%d\n",
            mir.name, value, width,
            definition != NULL
                ? mir_opcode_name(definition->opcode) : "none",
            definition != NULL ? definition->type : 0,
            mir_emit_instruction_index, call->name,
            argument_index, argument_count,
            later_constant_arguments, later_arguments);
}

static int mir_emit_cached_call_argument(MirStream *out, int value)
{
    if (mir_cached_call_value != value ||
        mir_cached_call_instruction != mir_emit_instruction_index)
        return 0;
    mir_report_call_cache(value, 2);
    mir_stream_puts("\tld l,c\n\tld h,b\n", out);
    mir_cached_call_value = -1;
    mir_cached_call_instruction = -1;
    mir_cached_wide_call_value = -1;
    mir_cached_wide_call_instruction = -1;
    return 1;
}

static int mir_emit_cached_call_argument_to_stack(MirStream *out, int value)
{
    if (mir_cached_call_value != value ||
        mir_cached_call_instruction != mir_emit_instruction_index ||
        mir.backend_slots == NULL ||
        mir.backend_slots[value] !=
            MIR_BACKEND_SLOT_NARROW_ARGUMENT_DIRECT_PUSH)
        return 0;
    mir_report_call_cache(value, 2);
    mir_stream_puts("\tpush bc\n", out);
    mir_cached_call_value = -1;
    mir_cached_call_instruction = -1;
    mir_cached_wide_call_value = -1;
    mir_cached_wide_call_instruction = -1;
    return 1;
}

/* Item 17 (mir-migration-plan-to-100pct.md): load a narrow argument value
 * into HL for spilled-scalar-cfg's fastcall emission, using the exact
 * same cached/rematerialized/virtual-load fallback chain the generic
 * MIR_CALL argument loop below already uses per argument - factored out
 * since the strlen/strchr/memcmp/bdos-family fastcalls each need this
 * several times. */
static void mir_emit_spilled_arg_to_hl(MirStream *out, int value)
{
    if (!mir_emit_cached_call_argument(out, value) &&
        !mir_emit_rematerialized_argument(out, value, 2))
        mir_emit_virtual_load(out, value);
}

/* A final fastcall argument may be forwarded directly from its definition in
 * HL. Multi-argument fastcall setup must preserve that value before loading
 * earlier arguments, or the later virtual load will consume a stale forwarding
 * marker and incorrectly assume the original value is still in HL. */
static int mir_take_forwarded_hl_call_argument(int value)
{
    if (mir_forwarded_hl_value != value ||
        mir_forwarded_hl_instruction + 1 != mir_emit_instruction_index)
        return 0;
    mir_forwarded_hl_value = -1;
    mir_forwarded_hl_instruction = -1;
    return 1;
}

static int mir_emit_cached_wide_call_argument(MirStream *out, int value)
{
    int stack_cached;

    if (mir_cached_wide_call_value != value ||
        mir_cached_wide_call_instruction != mir_emit_instruction_index)
        return 0;
    mir_report_call_cache(value, 4);
    stack_cached = mir.backend_slots != NULL &&
        mir.backend_slots[value] ==
            MIR_BACKEND_SLOT_WIDE_ARGUMENT_STACK_CACHE;
    if (!stack_cached)
        mir_stream_puts("\texx\n", out);
    mir_cached_wide_call_value = -1;
    mir_cached_wide_call_instruction = -1;
    return stack_cached ? 2 : 1;
}

/* Item 15 (mir-migration-plan-to-100pct.md): mirrors dcc_ast_gen_expr.c's
 * legacy AST "fastcall" recognition of memset(dest,c,count) - DCCRTL's
 * __msf takes dest in HL, the fill byte in E, count in BC directly,
 * skipping both the general push-3-args/call/pop-3 convention MIR_CALL's
 * generic emission otherwise always uses and __mset's own ~10-instruction
 * stack-marshaling prologue. Item 14 found this gap costs a genuine
 * both-mode performance regression once a memset call becomes MIR-
 * reachable (mir_try_emit_homed_scalar_cfg's MIR_ADDRESS support unlocked
 * trw's clear_buf, a `memset` trampoline, but its MIR_CALL emission had no
 * knowledge of the specialized runtime convention the legacy backend
 * already exploits). This is shared, selector-independent detection logic
 * so it benefits every MIR_CALL emitter (spilled- and homed-scalar-cfg
 * alike), not just newly-unlocked functions - closing the gap for any
 * memset call already reachable via MIR today too. Returns 1 and fills
 * `*dest_value`/`*fill_value`/`*count_value` with the three arguments'
 * value indices if `call_index` is a MIR_CALL to memset with exactly
 * three non-struct, non-4-byte arguments (the same shape the legacy
 * fastcall requires); returns 0 otherwise (any struct/wide/int-promoted
 * argument, or an argument count other than 3, falls back to the generic
 * convention rather than risk a mismatched register width). */
/* Item 17 (mir-migration-plan-to-100pct.md): shared core for every
 * MIR_CALL fastcall detector below. Matches a call named exactly `name`
 * with exactly `argc` narrow (non-struct, non-4-byte) arguments, and
 * fills `values[0..argc-1]` with each argument's defining value index in
 * source-argument order (not push order). This is the same matching
 * shape Item 15's mir_call_is_memset_fastcall originally used, factored
 * out so strlen/strchr/memcmp/bdos-family don't each re-derive it. */
static int mir_call_matches_fastcall_shape(int call_index, const char *name,
                                          int argc, int *values)
{
    const struct MirInsn *call = &mir.insns[call_index];
    int scan;

    for (scan = 0; scan < argc; ++scan)
        values[scan] = -1;
    if (strcmp(call->name, name) != 0)
        return 0;
    for (scan = 0; scan < call_index; ++scan) {
        const struct MirInsn *arg = &mir.insns[scan];
        int index;
        if (arg->opcode != MIR_ARG ||
            arg->secondary_offset != call->secondary_offset)
            continue;
        index = (int)arg->immediate;
        if (index < 0 || index >= argc)
            return 0;
        if (type_is_struct_object(arg->type) || type_size(arg->type) == 4)
            return 0;
        values[index] = arg->src1;
    }
    for (scan = 0; scan < argc; ++scan)
        if (values[scan] < 0)
            return 0;
    return 1;
}

int mir_call_is_memset_fastcall(int call_index, int *dest_value,
                                       int *fill_value, int *count_value)
{
    int values[3];
    if (!mir_call_matches_fastcall_shape(call_index, "memset", 3, values))
        return 0;
    *dest_value = values[0];
    *fill_value = values[1];
    *count_value = values[2];
    return 1;
}

/* dcc_ast_gen_expr.c's legacy fastcall for strlen(s): DCCRTL's __slf takes
 * s directly in HL and returns the length in HL. */
int mir_call_is_strlen_fastcall(int call_index, int *s_value)
{
    int values[1];
    if (!mir_call_matches_fastcall_shape(call_index, "strlen", 1, values))
        return 0;
    *s_value = values[0];
    return 1;
}

/* Legacy fastcall for strchr(s,c): DCCRTL's __chf takes s in HL and c's
 * low byte in A, returning the match (or 0) in HL. */
int mir_call_is_strchr_fastcall(int call_index, int *s_value,
                                      int *c_value)
{
    int values[2];
    if (!mir_call_matches_fastcall_shape(call_index, "strchr", 2, values))
        return 0;
    *s_value = values[0];
    *c_value = values[1];
    return 1;
}

/* Item 18 (mir-migration-plan-to-100pct.md): strrchr(s,c) has the exact
 * same DCCRTL argument shape as strchr above (HL=s, A=low byte of c),
 * differing only in the entry point (__rcf vs __chf). */
int mir_call_is_strrchr_fastcall(int call_index, int *s_value,
                                       int *c_value)
{
    int values[2];
    if (!mir_call_matches_fastcall_shape(call_index, "strrchr", 2, values))
        return 0;
    *s_value = values[0];
    *c_value = values[1];
    return 1;
}

/* Item 18: memchr(s,c,n) has the exact same DCCRTL argument shape as
 * memset above (HL=s, E=low byte of c, BC=n), differing only in the
 * entry point (__mhf vs __msf). */
int mir_call_is_memchr_fastcall(int call_index, int *s_value,
                                      int *c_value, int *n_value)
{
    int values[3];
    if (!mir_call_matches_fastcall_shape(call_index, "memchr", 3, values))
        return 0;
    *s_value = values[0];
    *c_value = values[1];
    *n_value = values[2];
    return 1;
}

/* Item 18: memcpy(dst,src,n) has the exact same DCCRTL argument shape as
 * memcmp above (DE=arg0, HL=arg1, BC=n), differing only in the entry
 * point (__mcf vs __cmpf) and copy-not-compare semantics. */
int mir_call_is_memcpy_fastcall(int call_index, int *dst_value,
                                      int *src_value, int *n_value)
{
    int values[3];
    if (!mir_call_matches_fastcall_shape(call_index, "memcpy", 3, values))
        return 0;
    *dst_value = values[0];
    *src_value = values[1];
    *n_value = values[2];
    return 1;
}

/* Item 18: strcpy(dst,src)->__scf, strstr(haystack,needle)->__ssf, and
 * stricmp(s1,s2)->__icf all share one 2-argument shape: arg1 ends up in
 * HL (the last-evaluated value needs no move) and arg0 is pushed then
 * popped into DE. */
int mir_call_is_de_hl_fastcall(int call_index, const char **rtl_name,
                                     int *arg0_value, int *arg1_value)
{
    static const struct { const char *name; const char *rtl; } family[] = {
        { "strcpy", "__scf" }, { "strstr", "__ssf" }, { "stricmp", "__icf" }
    };
    size_t member;
    int values[2];
    for (member = 0; member < sizeof(family) / sizeof(family[0]); ++member) {
        if (mir_call_matches_fastcall_shape(call_index, family[member].name,
                                            2, values)) {
            *rtl_name = family[member].rtl;
            *arg0_value = values[0];
            *arg1_value = values[1];
            return 1;
        }
    }
    return 0;
}

/* Legacy fastcall for memcmp(s1,s2,n): DCCRTL's __cmpf takes s1 in DE,
 * s2 in HL, n in BC directly. */
int mir_call_is_memcmp_fastcall(int call_index, int *s1_value,
                                       int *s2_value, int *n_value)
{
    int values[3];
    if (!mir_call_matches_fastcall_shape(call_index, "memcmp", 3, values))
        return 0;
    *s1_value = values[0];
    *s2_value = values[1];
    *n_value = values[2];
    return 1;
}

/* Legacy fastcall for bdos(fn,dearg)/bdoshl(fn,dearg)/bios(fn,dearg)/
 * bioshl(fn,dearg): all four share the same DE=dearg, C=fn-low-byte
 * argument convention, differing only in which DCCRTL entry point is
 * called (__bdosf/__bhlf/__biosf/__bhf respectively - see
 * dcc_ast_gen_expr.c). */
int mir_call_is_bdos_family_fastcall(int call_index,
                                           const char **rtl_name,
                                           int *fn_value, int *dearg_value)
{
    static const struct { const char *name; const char *rtl; } family[] = {
        { "bdos", "__bdosf" }, { "bdoshl", "__bhlf" }, { "bios", "__biosf" },
        { "bioshl", "__bhf" }
    };
    size_t i;
    int values[2];
    for (i = 0; i < sizeof(family) / sizeof(family[0]); ++i) {
        if (mir_call_matches_fastcall_shape(call_index, family[i].name, 2,
                                            values)) {
            *rtl_name = family[i].rtl;
            *fn_value = values[0];
            *dearg_value = values[1];
            return 1;
        }
    }
    return 0;
}

int mir_object_is_fully_promoted(int object)
{
    int instruction;

    if (object < 0 || object >= mir.object_count)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_LOAD &&
            mir.insns[instruction].object == object)
            return 0;
    return 1;
}

int mir_object_address_taken(int object)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_ADDRESS &&
            mir.insns[instruction].object == object)
            return 1;
    return 0;
}

/* True if the value written by the MIR_STORE at `instruction` can never be
 * observed: no real MIR_LOAD of the same object is reachable along any
 * successor path before the object is next stored or the function ends.
 * A backward liveness fixed point mirrors the per-value computation in
 * mir_verify_and_dump, specialised so a MIR_LOAD of the object is a "use"
 * (forces liveness) and a MIR_STORE of the object is a "kill" (the store's
 * own liveness does not depend on anything reachable before it). This
 * single mechanism covers both a dead initialisation store (never read
 * before the function ends) and a store overwritten by a later store
 * before any intervening read. Bails conservatively if the object's
 * address is ever taken, since a store could then be observed through an
 * escaped pointer that a static scan of MIR_LOAD instructions cannot see. */
int mir_store_is_dead(int instruction)
{
    int object;
    unsigned char *live_in;
    unsigned char *live_out;
    int i;
    int changed;
    int dead;

    if (mir.insns[instruction].opcode != MIR_STORE)
        return 0;
    object = mir.insns[instruction].object;
    if (object < 0 || object >= mir.object_count ||
        mir_object_address_taken(object))
        return 0;
    /* mir.count is always a small non-negative instruction count, but
     * without an explicit check GCC's range analysis can't rule out the
     * full negative int range folding into a huge size_t here, and warns
     * accordingly (-Walloc-size-larger-than=). */
    if (mir.count < 0)
        fatal("negative MIR instruction count");
    live_in = (unsigned char *)calloc((size_t)mir.count, 1);
    live_out = (unsigned char *)calloc((size_t)mir.count, 1);
    if (mir.count && (live_in == NULL || live_out == NULL))
        fatal("out of memory computing MIR store liveness");
    do {
        changed = 0;
        for (i = mir.count - 1; i >= 0; --i) {
            const struct MirInsn *insn = &mir.insns[i];
            int successor;
            int next_out = 0;
            int next_in;

            for (successor = 0; successor < insn->successor_count; ++successor)
                next_out |= live_in[insn->successors[successor]];
            if (insn->opcode == MIR_LOAD && insn->object == object)
                next_in = 1;
            else if (insn->opcode == MIR_STORE && insn->object == object)
                next_in = 0;
            else
                next_in = next_out;
            if (live_out[i] != next_out || live_in[i] != next_in) {
                live_out[i] = (unsigned char)next_out;
                live_in[i] = (unsigned char)next_in;
                changed = 1;
            }
        }
    } while (changed);
    dead = !live_out[instruction];
    free(live_in);
    free(live_out);
    return dead;
}

static int mir_ranges_overlap(int offset1, int size1, int offset2, int size2)
{
    return offset1 < offset2 + size2 && offset2 < offset1 + size1;
}

static int mir_dead_local_candidate(int object, int *store_instruction,
                                    int *source_value)
{
    const struct MirObject *candidate = &mir.objects[object];
    int size = type_size(candidate->type);
    int store = -1;
    int instruction;

    if (candidate->storage != SC_LOCAL || (size != 2 && size != 4) ||
        candidate->offset >= 0)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode == MIR_STORE && insn->object == object) {
            if (store >= 0)
                return 0;
            store = instruction;
        }
    }
    if (store < 0 || type_size(mir.insns[store].type) != size ||
        !mir_object_is_fully_promoted(object))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        int memory_type;
        int memory_storage;
        int memory_offset;
        int memory_size;

        if (insn->opcode != MIR_PARAM && insn->opcode != MIR_LOAD &&
            insn->opcode != MIR_STORE && insn->opcode != MIR_ADDRESS)
            continue;
        if (!mir_scalar_memory_location(insn, &memory_type, &memory_storage,
                                        &memory_offset))
            return 0;
        memory_size = type_size(memory_type);
        if (memory_storage == SC_LOCAL && memory_size > 0 &&
            mir_ranges_overlap(candidate->offset, size, memory_offset,
                               memory_size) &&
            instruction != store)
            return 0;
    }
    *source_value = mir.insns[store].src1;
    if (*source_value < 0)
        return 0;
    *store_instruction = store;
    return 1;
}

static int mir_dead_local_suffix_allowed(void)
{
    int instruction;

    if (opt_debug || mir.has_vla || mir.aggregate_temp_bytes != 0)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        switch (mir.insns[instruction].opcode) {
        case MIR_ADDRESS:
        case MIR_COMPOUND_ADDRESS:
        case MIR_COPY_AGGREGATE:
        case MIR_VLA_SIZE:
        case MIR_VLA_SAVE:
        case MIR_VLA_ALLOC:
        case MIR_VLA_RESTORE:
        case MIR_CALL_AGGREGATE:
        case MIR_VA_START:
        case MIR_VA_END:
        case MIR_VA_ARG:
        case MIR_OPAQUE:
            return 0;
        default:
            break;
        }
    }
    return 1;
}

static int mir_dead_local_range_is_unique(int candidate)
{
    int candidate_size = type_size(mir.objects[candidate].type);
    int object;

    for (object = 0; object < mir.object_count; ++object) {
        int size;

        if (object == candidate || mir.objects[object].storage != SC_LOCAL)
            continue;
        size = type_size(mir.objects[object].type);
        if (size > 0 &&
            mir_ranges_overlap(mir.objects[candidate].offset, candidate_size,
                               mir.objects[object].offset, size))
            return 0;
    }
    return 1;
}

void mir_compute_dead_local_suffix(void)
{
    int reclaimed = 0;

    mir.dead_local_suffix_bytes = 0;
    if (!mir_dead_local_suffix_allowed())
        return;
    for (;;) {
        int boundary = -mir.local_bytes + reclaimed;
        int found = -1;
        int store;
        int value;
        int object;

        for (object = 0; object < mir.object_count; ++object)
            if (mir.objects[object].offset == boundary &&
                mir_dead_local_candidate(object, &store, &value) &&
                mir_dead_local_range_is_unique(object)) {
                if (found >= 0)
                    return;
                found = object;
            }
        if (found < 0)
            break;
        reclaimed += type_size(mir.objects[found].type);
    }
    mir.dead_local_suffix_bytes = reclaimed;
}

int mir_effective_local_bytes(void)
{
    return mir.local_bytes - mir.dead_local_suffix_bytes;
}

void mir_report_dead_local_suffix(void)
{
    int effective = mir_effective_local_bytes();
    int object;

    if (getenv("DCC_MIR_DEAD_LOCAL_REPORT") == NULL ||
        mir.dead_local_suffix_bytes == 0)
        return;
    for (object = 0; object < mir.object_count; ++object) {
        const struct MirObject *candidate = &mir.objects[object];
        int size = type_size(candidate->type);
        int store;
        int value;

        if (candidate->storage != SC_LOCAL ||
            candidate->offset < -mir.local_bytes ||
            candidate->offset + size > -effective ||
            !mir_dead_local_candidate(object, &store, &value))
            continue;
        fprintf(stderr,
                "; MIR dead-local function=%s object=%s offset=%d size=%d"
                " store=%d value=%d locals=%d effective=%d"
                " reclaimable=%d\n",
                mir.name, candidate->name, candidate->offset,
                size, store, value, mir.local_bytes, effective,
                mir.dead_local_suffix_bytes);
    }
}

/* Item T10 (mir-text-size-plan.md): a value-defining instruction (const,
 * float const, address materialisation, ...) unconditionally checks
 * mir_value_has_use(dst) before deciding whether it needs to write its
 * result into dst's own backend home slot at all - but "has a use" only
 * asks whether some later instruction's src operand names this value, not
 * whether that later instruction will actually emit any code. A MIR_STORE
 * consuming this value can itself turn out to be fully dead (see
 * mir_object_is_fully_promoted/mir_store_is_dead above) and emit nothing,
 * in which case materialising the value into its home was pure waste - a
 * write that is now provably never read by any surviving instruction.
 * This mirrors the exact two conditions the MIR_STORE case itself already
 * uses to decide it will emit nothing, so a value is only ever treated as
 * "unused" here when every one of its uses is a store this selector has
 * independently already proven dead - never a broader guess. */
int mir_value_only_used_by_dead_stores(int value)
{
    int instruction;
    int found_use = 0;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 != value && insn->src2 != value &&
            !mir_call_uses_value(insn, value))
            continue;
        found_use = 1;
        if (insn->opcode != MIR_STORE || insn->src1 != value)
            return 0;
        if (!(mir_object_is_fully_promoted(insn->object) ||
              mir_store_is_dead(instruction)))
            return 0;
    }
    return found_use;
}

/* Item T12 (mir-text-size-plan.md): analogous to
 * mir_value_only_used_by_dead_stores above, but for a value whose only
 * use is as the operand of a MIR_UNARY instruction (cast, +, -, ~, !)
 * whose own destination in turn has no use - the common "(void)param;"
 * idiom used to silence unused-parameter warnings in callback/visitor
 * signatures. The MIR_UNARY emission case already skips itself entirely
 * once its own dst is unused, so loading a value that only ever feeds
 * such a dead unary is pure waste: it is loaded into hl and immediately
 * discarded, never observed by anything. */
static int mir_value_only_used_by_dead_unary(int value)
{
    int instruction;
    int found_use = 0;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 != value && insn->src2 != value &&
            !mir_call_uses_value(insn, value))
            continue;
        found_use = 1;
        if (insn->opcode != MIR_UNARY || insn->src1 != value ||
            mir_value_has_use(insn->dst))
            return 0;
    }
    return found_use;
}

/* mir-text-size Item T20: when a value's own definition is immediately
 * followed by its sole use as a MIR_ARG, and that ARG is in turn
 * immediately followed by its own MIR_CALL/MIR_CALL_AGGREGATE (nothing
 * at all in between - not even another argument's own value
 * definition), this is provably the *last*-defined argument for that
 * call in MIR-stream order. The generic MIR_CALL argument loop below
 * always walks a call's own MIR_ARG instructions *backward* (matching
 * strictly decreasing positional index starting from
 * call_arg_count - 1), so the last-MIR-stream-order argument is always
 * the *first* one physically processed/pushed - nothing can touch HL
 * between this value's own computation and the moment it is pushed.
 * mir_call_argument_cache_target's more general BC-cache mechanism
 * (below) exists precisely to preserve a value across such a gap; when
 * there is provably no gap at all, caching (store to bc, reload from
 * bc) is pure overhead - the value can flow straight through HL via
 * the same mir_forwarded_hl_value/_instruction mechanism
 * mir_can_forward_hl_to_next already uses for its own recognized
 * consumer opcodes, anchored to the ARG instruction's own index: since
 * MIR_ARG's own emission case is a no-op (it contributes zero machine
 * instructions), mir_emit_virtual_load's existing forwarded-hl check
 * naturally fires when the emit loop later reaches the CALL
 * instruction one position after the ARG, exactly the position this
 * predicate requires be adjacent. Found via tests/pint.c's
 * while_stmt: `patch(jz, cp)`'s second argument (a fresh global read of
 * `cp`) was round-tripped through bc for no reason - nothing runs
 * between the load and the call at all. */
/* Item T82 (mir-text-size-plan.md): the value's own MIR_ARG use can be
 * separated from its definition by an intervening MIR_NOP (a same-block
 * rename/metadata marker that emits no code - see mir_forward_skip_target's
 * own comment on why these are always safe to look through), the same gap
 * Item T29 already closed for mir_can_forward_hl_to_next. This predicate
 * used a raw "+1" index instead, so a value like tests/tmirfast.c's
 * dec_dead/inc_dead `x` parameter (whose MIR_PARAM definition is followed
 * by one such NOP before its ARG use) never matched at all, regardless of
 * how many other uses it had. Skipping only plain MIR_NOP (never a LABEL -
 * an ARG/CALL pair can never legitimately cross a block boundary) keeps
 * this exactly as safe as the original adjacent-index check. */
static int mir_call_argument_after_nops(int instruction)
{
    int index = instruction + 1;

    mir_forward_skip_last_skipped_dead_store = 0;
    while (index < mir.count &&
           (mir.insns[index].opcode == MIR_NOP ||
            mir_instruction_is_transparent_dead_store(index)))
        ++index;
    return index;
}

static int mir_can_forward_hl_to_call_argument(int value)
{
    const struct MirInsn *arg_insn;
    const struct MirInsn *call_insn;
    int instruction;
    int arg_instruction;

    if (mir_emit_instruction_index < 0)
        return 0;
    arg_instruction = mir_call_argument_after_nops(mir_emit_instruction_index);
    if (arg_instruction + 1 >= mir.count)
        return 0;
    arg_insn = &mir.insns[arg_instruction];
    call_insn = &mir.insns[arg_instruction + 1];
    if (arg_insn->opcode != MIR_ARG || arg_insn->src1 != value ||
        type_size(arg_insn->type) > 2)
        return 0;
    if ((call_insn->opcode != MIR_CALL &&
         call_insn->opcode != MIR_CALL_AGGREGATE) ||
        call_insn->secondary_offset != arg_insn->secondary_offset)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        if (instruction == arg_instruction)
            continue;
        if (mir.insns[instruction].src1 == value ||
            mir.insns[instruction].src2 == value)
            return 0;
    }
    return mir_forward_note_success();
}

/* Item T82 (mir-text-size-plan.md): sibling to mir_can_forward_hl_to_call_
 * argument above, but for a value that also has a *later* use elsewhere
 * (so it still genuinely needs a real backend slot - the sole-use
 * predicate above correctly declines it). The store into that slot still
 * runs unchanged so every later reload keeps working; this only proves
 * that the value's very first consumer, immediately following its own
 * definition (through any intervening MIR_NOP), is this exact ARG+CALL
 * adjacency with nothing at all between the store and that first use -
 * so the immediately-following reload of the bytes just written is pure,
 * provably redundant round-tripping and can reuse HL directly instead.
 * Found via tests/tmirfast.c's dec_dead/inc_dead: `x` is used once as
 * side_effect's sole call argument and again afterward by `x--`/`x++`,
 * so it needs a slot, but its first use directly follows its own
 * MIR_PARAM definition (through one MIR_NOP) with nothing in between. */
static int mir_can_forward_hl_to_call_argument_first_use(int value)
{
    const struct MirInsn *arg_insn;
    const struct MirInsn *call_insn;
    int arg_instruction;

    if (mir_emit_instruction_index < 0)
        return 0;
    arg_instruction = mir_call_argument_after_nops(mir_emit_instruction_index);
    if (arg_instruction + 1 >= mir.count)
        return 0;
    arg_insn = &mir.insns[arg_instruction];
    call_insn = &mir.insns[arg_instruction + 1];
    if (arg_insn->opcode != MIR_ARG || arg_insn->src1 != value ||
        type_size(arg_insn->type) > 2)
        return 0;
    if ((call_insn->opcode != MIR_CALL &&
         call_insn->opcode != MIR_CALL_AGGREGATE) ||
        call_insn->secondary_offset != arg_insn->secondary_offset)
        return 0;
    return mir_forward_note_success();
}

static int mir_call_has_odd_argument_bytes(const struct MirInsn *call)
{
    int argument_bytes = 0;
    int instruction;

    if (call == NULL || call->opcode != MIR_CALL)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *arg = &mir.insns[instruction];
        int size;
        if (arg->opcode != MIR_ARG ||
            arg->secondary_offset != call->secondary_offset)
            continue;
        size = type_size(arg->type);
        argument_bytes += type_is_struct_object(arg->type)
            ? size : (size == 4 ? 4 : 2);
    }
    return (argument_bytes & 1) != 0;
}

static int mir_call_argument_cache_target_for_state(
    int value, int definition_instruction,
    int narrow_cache_occupied, int wide_cache_occupied)
{
    int argument_instruction = -1;
    int call_id = -1;
    int call_instruction = -1;
    int instruction;

    if ((mir_value_is_wide(value) && wide_cache_occupied) ||
        (!mir_value_is_wide(value) && narrow_cache_occupied))
        return -1;
    {
        const struct MirInsn *definition = mir_definition(value);
        if (definition == NULL || definition->opcode == MIR_PHI ||
            definition->opcode == MIR_PARAM ||
            mir_divmod_partner((int)(definition - mir.insns)) >= 0)
            return -1;
        /*
         * Generic calls with an odd aggregate-argument byte count store a
         * scalar result once around SP cleanup and again afterward. Such a
         * result cannot be cache-only because its second store is real.
         */
        if (mir_call_has_odd_argument_bytes(definition))
            return -1;
    }
    for (instruction = definition_instruction + 1;
         instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->opcode == MIR_ARG && insn->src1 == value) {
            if (argument_instruction >= 0)
                return -1;
            argument_instruction = instruction;
            call_id = insn->secondary_offset;
        } else if (insn->src1 == value || insn->src2 == value) {
            if (!(insn->opcode == MIR_STORE &&
                  mir_object_is_fully_promoted(insn->object)))
                return -1;
        }
    }
    if (argument_instruction < 0)
        return -1;
    if ((type_size(mir.insns[argument_instruction].type) > 2) !=
        mir_value_is_wide(value))
        return -1;
    for (instruction = argument_instruction + 1;
         instruction < mir.count; ++instruction)
        if ((mir.insns[instruction].opcode == MIR_CALL ||
             mir.insns[instruction].opcode == MIR_CALL_AGGREGATE) &&
            mir.insns[instruction].secondary_offset == call_id) {
            call_instruction = instruction;
            break;
        }
    if (call_instruction < 0)
        return -1;
    for (instruction = definition_instruction + 1;
         instruction < call_instruction; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->opcode == MIR_NOP || insn->opcode == MIR_ARG)
            continue;
        if ((insn->opcode == MIR_CONST ||
             insn->opcode == MIR_FLOAT_CONST ||
             insn->opcode == MIR_STRING_ADDRESS) &&
            mir_call_only_constant(insn->dst))
            continue;
        if (insn->opcode == MIR_STORE && insn->src1 == value &&
            mir_object_is_fully_promoted(insn->object))
            continue;
        return -1;
    }
    return call_instruction;
}

static int mir_call_argument_cache_target(int value)
{
    return mir_call_argument_cache_target_for_state(
        value, mir_emit_instruction_index,
        mir_cached_call_value >= 0,
        mir_cached_wide_call_value >= 0);
}

static int mir_wide_call_argument_is_first_pushed(
    int value, int call_instruction)
{
    const struct MirInsn *call;
    int argument_count = 0;
    int argument_index = -1;
    int instruction;

    if (!mir_wide_first_argument_stack_cache_enabled ||
        !mir_value_is_wide(value) || call_instruction < 0 ||
        call_instruction >= mir.count)
        return 0;
    call = &mir.insns[call_instruction];
    if (call->opcode != MIR_CALL &&
        call->opcode != MIR_CALL_AGGREGATE)
        return 0;
    for (instruction = 0; instruction < call_instruction; ++instruction) {
        const struct MirInsn *arg = &mir.insns[instruction];
        if (arg->opcode != MIR_ARG ||
            arg->secondary_offset != call->secondary_offset)
            continue;
        ++argument_count;
        if (arg->src1 == value)
            argument_index = (int)arg->immediate;
    }
    return argument_count > 0 &&
           argument_index == argument_count - 1;
}

static int mir_planned_call_argument_cache_target(int value, int wide)
{
    int call_instruction = mir_call_argument_cache_target(value);

    if (call_instruction < 0) {
        char message[256];
        const struct MirInsn *definition = mir_definition(value);
        snprintf(message, sizeof(message),
                 "planned %sMIR call-argument cache unavailable in %s "
                 "for value %d (%s) at instruction %d (cached value %d)",
                 wide ? "wide " : "", mir.name, value,
                 definition != NULL
                    ? mir_opcode_name(definition->opcode) : "none",
                 mir_emit_instruction_index,
                 wide ? mir_cached_wide_call_value : mir_cached_call_value);
        fatal(message);
    }
    return call_instruction;
}

static int mir_call_uses_generic_stack_arguments(int instruction)
{
    const char *rtl_name;
    int a, b, c;

    return !mir_call_is_memset_fastcall(instruction, &a, &b, &c) &&
           !mir_call_uses_inline_memory_store(instruction, NULL, NULL) &&
           !mir_call_uses_inline_simple_indexed_store(
               instruction, NULL, NULL) &&
           !mir_call_uses_inline_typed_indexed_store(
               instruction, NULL, NULL) &&
           !mir_call_is_strlen_fastcall(instruction, &a) &&
           !mir_call_is_strchr_fastcall(instruction, &a, &b) &&
           !mir_call_is_strrchr_fastcall(instruction, &a, &b) &&
           !mir_call_is_memchr_fastcall(instruction, &a, &b, &c) &&
           !mir_call_is_memcmp_fastcall(instruction, &a, &b, &c) &&
           !mir_call_is_memcpy_fastcall(instruction, &a, &b, &c) &&
           !mir_call_is_de_hl_fastcall(instruction, &rtl_name, &a, &b) &&
           !mir_call_is_bdos_family_fastcall(
               instruction, &rtl_name, &a, &b);
}

static void mir_emit_prepacked_constant_arguments(
    MirStream *out, int trigger_instruction)
{
    const struct MirInsn *outer_call = NULL;
    int argument_count = 0;
    int cached_argument = -1;
    int definition_instruction;
    int outer_instruction = -1;
    int argument;
    int instruction;
    int value = -1;

    if (!mir_constant_argument_prepacking_enabled || mir.has_vla ||
        mir.backend_slots == NULL || mir_prepacked_call_instruction >= 0)
        return;
    for (definition_instruction = trigger_instruction;
         definition_instruction < mir.count; ++definition_instruction) {
        const struct MirInsn *definition =
            &mir.insns[definition_instruction];
        int previous_argument_instruction = -1;

        value = definition->dst;
        if (definition->opcode != MIR_CALL || value < 0 ||
            value >= mir.next_value ||
            (mir.backend_slots[value] != MIR_BACKEND_SLOT_CALL_CACHE &&
             mir.backend_slots[value] !=
                 MIR_BACKEND_SLOT_NARROW_ARGUMENT_DIRECT_PUSH))
            continue;
        outer_instruction = mir_call_argument_cache_target_for_state(
            value, definition_instruction, 0, 0);
        if (outer_instruction < 0 ||
            !mir_call_uses_generic_stack_arguments(outer_instruction))
            continue;
        outer_call = &mir.insns[outer_instruction];
        if (outer_call->opcode != MIR_CALL)
            continue;
        argument_count = 0;
        cached_argument = -1;
        for (instruction = 0;
             instruction < outer_instruction; ++instruction) {
            const struct MirInsn *arg = &mir.insns[instruction];
            if (arg->opcode != MIR_ARG ||
                arg->secondary_offset != outer_call->secondary_offset)
                continue;
            ++argument_count;
            if (arg->src1 == value)
                cached_argument = (int)arg->immediate;
        }
        if (cached_argument <= 0 ||
            cached_argument + 1 >= argument_count)
            continue;
        for (instruction = 0;
             instruction < definition_instruction; ++instruction) {
            const struct MirInsn *arg = &mir.insns[instruction];
            if (arg->opcode == MIR_ARG &&
                arg->secondary_offset == outer_call->secondary_offset &&
                arg->immediate == cached_argument - 1)
                previous_argument_instruction = instruction;
        }
        if (previous_argument_instruction + 1 != trigger_instruction)
            continue;
        break;
    }
    if (definition_instruction >= mir.count || outer_call == NULL ||
        outer_instruction < 0)
        return;
    for (argument = cached_argument + 1;
         argument < argument_count; ++argument) {
        int found = 0;
        for (instruction = 0;
             instruction < outer_instruction; ++instruction) {
            const struct MirInsn *arg = &mir.insns[instruction];
            if (arg->opcode != MIR_ARG ||
                arg->secondary_offset != outer_call->secondary_offset ||
                arg->immediate != argument)
                continue;
            if (!mir_call_only_constant(arg->src1))
                return;
            found = 1;
            break;
        }
        if (!found)
            return;
    }
    /*
     * The ABI pushes arguments in descending source-index order. Emit the
     * constant suffix before evaluating the nested call, then push that
     * call's result. The outer call later emits only the lower-index prefix,
     * producing exactly the same stack order as its ordinary reverse scan.
     * Triggering before the nested call's own arguments is essential: they
     * may use the same forwarding state, but their call restores SP before
     * the staged outer arguments are consumed.
     */
    for (argument = argument_count - 1;
         argument > cached_argument; --argument) {
        for (instruction = outer_instruction - 1;
             instruction >= 0; --instruction) {
            const struct MirInsn *arg = &mir.insns[instruction];
            int size;
            if (arg->opcode != MIR_ARG ||
                arg->secondary_offset != outer_call->secondary_offset ||
                arg->immediate != argument)
                continue;
            size = type_size(arg->type);
            if (!mir_emit_rematerialized_argument(
                    out, arg->src1, size))
                fatal("cannot prepack constant MIR call argument");
            if (size == 4)
                mir_stream_puts("\tpush de\n\tpush hl\n", out);
            else
                mir_stream_puts("\tpush hl\n", out);
            break;
        }
    }
    mir_prepacked_call_instruction = outer_instruction;
    mir_prepacked_after_argument = cached_argument;
    mir_prepacked_result_value = value;
    ++mir_constant_argument_prepack_count;
}

static void mir_emit_prepacked_byte_check_argument(
    MirStream *out, int trigger_instruction)
{
    const struct MirInsn *actual;
    const struct MirInsn *actual_arg;
    const struct MirInsn *base;
    const struct MirInsn *call;
    const struct MirInsn *expected;
    const struct MirInsn *expected_arg;
    const struct MirInsn *index;
    const struct MirInsn *index_constant;
    const struct MirInsn *prefix_arg;
    const struct MirInsn *root = NULL;
    int memory_offset;
    int memory_storage;
    int memory_type;
    int scan;

    if (mir_prepacked_call_instruction >= 0 ||
        mir_cfg_block_count() != 1 ||
        trigger_instruction <= 0 ||
        trigger_instruction + 7 >= mir.count)
        return;
    prefix_arg = &mir.insns[trigger_instruction - 1];
    base = &mir.insns[trigger_instruction];
    index_constant = &mir.insns[trigger_instruction + 1];
    index = &mir.insns[trigger_instruction + 2];
    actual = &mir.insns[trigger_instruction + 3];
    actual_arg = &mir.insns[trigger_instruction + 4];
    expected_arg = &mir.insns[trigger_instruction + 6];
    call = &mir.insns[trigger_instruction + 7];
    expected = mir_definition(expected_arg->src1);
    if (prefix_arg->opcode != MIR_ARG || prefix_arg->immediate != 0 ||
        base->opcode != MIR_LOAD ||
        index_constant->opcode != MIR_CONST ||
        index->opcode != MIR_INDEX_ADDRESS ||
        index->src1 != base->dst ||
        index->src2 != index_constant->dst ||
        actual->opcode != MIR_LOAD_INDIRECT ||
        actual->src1 != index->dst ||
        actual->memory_size != 1 ||
        actual_arg->opcode != MIR_ARG ||
        actual_arg->src1 != actual->dst ||
        actual_arg->immediate != 1 ||
        mir.insns[trigger_instruction + 5].opcode != MIR_NOP ||
        expected_arg->opcode != MIR_ARG ||
        expected_arg->immediate != 2 ||
        call->opcode != MIR_CALL ||
        prefix_arg->secondary_offset != call->secondary_offset ||
        actual_arg->secondary_offset != call->secondary_offset ||
        expected_arg->secondary_offset != call->secondary_offset ||
        !mir_call_uses_generic_stack_arguments(
            trigger_instruction + 7) ||
        expected == NULL || expected->opcode != MIR_PARAM ||
        type_size(expected->type) != 1 ||
        mir_value_use_count(expected->dst) != 2 ||
        mir_value_use_count(actual->dst) != 2 ||
        !mir_scalar_memory_location(
            expected, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || memory_offset < -128)
        return;
    for (scan = 0; scan < trigger_instruction; ++scan) {
        const struct MirInsn *store = &mir.insns[scan];
        const struct MirInsn *alias;

        if (store->opcode != MIR_STORE ||
            !mir_same_scalar_memory_location(store, base))
            continue;
        alias = mir_definition(store->src1);
        if (alias != NULL && alias->opcode == MIR_UNARY &&
            alias->immediate == 0)
            root = mir_definition(alias->src1);
    }
    if (root == NULL || root->opcode != MIR_LOAD ||
        type_ptr_depth(root->type) == 0 ||
        !mir_scalar_memory_location(
            root, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || memory_offset < -128 ||
        memory_offset + 1 > 127)
        return;
    mir_emit_virtual_load(out, expected->dst);
    mir_stream_puts("\tpush hl\n", out);
    if (!mir_emit_named_word_load_to_hl(out, root))
        fatal("cannot reload prepacked byte-check root");
    memory_offset =
        (int)(index_constant->immediate * index->immediate);
    while (memory_offset-- > 0)
        mir_stream_puts("\tinc hl\n", out);
    mir_stream_puts("\tld l,(hl)\n", out);
    if ((actual->type & TYPE_UNSIGNED) != 0)
        mir_stream_puts("\tld h,0\n", out);
    else
        mir_emit_signed_byte_extend(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_prepacked_call_instruction = trigger_instruction + 7;
    mir_prepacked_after_argument = 0;
    mir_prepacked_result_value = -1;
    mir_prepacked_skip_through = trigger_instruction + 4;
}

static void mir_emit_prepacked_bool_arguments(
    MirStream *out, int trigger_instruction)
{
    const struct MirInsn *arg_a;
    const struct MirInsn *arg_b;
    const struct MirInsn *call;
    const struct MirInsn *convert_a;
    const struct MirInsn *convert_b;
    const struct MirInsn *prefix_arg;
    int bool_offset;
    int bool_store;

    if (mir_prepacked_call_instruction >= 0 ||
        trigger_instruction < 2 ||
        trigger_instruction + 5 >= mir.count)
        return;
    prefix_arg = &mir.insns[trigger_instruction - 2];
    convert_a = &mir.insns[trigger_instruction];
    arg_a = &mir.insns[trigger_instruction + 1];
    convert_b = &mir.insns[trigger_instruction + 3];
    arg_b = &mir.insns[trigger_instruction + 4];
    call = &mir.insns[trigger_instruction + 5];
    if (prefix_arg->opcode != MIR_ARG || prefix_arg->immediate != 0 ||
        mir.insns[trigger_instruction - 1].opcode != MIR_NOP ||
        convert_a->opcode != MIR_UNARY || convert_a->immediate != 0 ||
        type_size(convert_a->type) != 2 ||
        arg_a->opcode != MIR_ARG || arg_a->src1 != convert_a->dst ||
        arg_a->immediate != 1 ||
        mir.insns[trigger_instruction + 2].opcode != MIR_NOP ||
        convert_b->opcode != MIR_UNARY || convert_b->immediate != 0 ||
        type_size(convert_b->type) != 2 ||
        arg_b->opcode != MIR_ARG || arg_b->src1 != convert_b->dst ||
        arg_b->immediate != 2 ||
        call->opcode != MIR_CALL ||
        prefix_arg->secondary_offset != call->secondary_offset ||
        arg_a->secondary_offset != call->secondary_offset ||
        arg_b->secondary_offset != call->secondary_offset ||
        !mir_call_uses_generic_stack_arguments(trigger_instruction + 5) ||
        !mir_bool_call_named_home(
            convert_a->src1, &bool_store, &bool_offset) ||
        bool_store >= trigger_instruction ||
        !mir_bool_call_named_home(
            convert_b->src1, &bool_store, &bool_offset) ||
        bool_store >= trigger_instruction)
        return;
    mir_emit_virtual_load(out, convert_b->src1);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_virtual_load(out, convert_a->src1);
    mir_stream_puts("\tpush hl\n", out);
    mir_prepacked_call_instruction = trigger_instruction + 5;
    mir_prepacked_after_argument = 0;
    mir_prepacked_result_value = -1;
    mir_prepacked_skip_through = trigger_instruction + 4;
}

static int mir_definition_is_wide(const struct MirInsn *definition)
{
    if (definition == NULL)
        return 0;
    if (definition->opcode == MIR_ADDRESS ||
        definition->opcode == MIR_COMPOUND_ADDRESS ||
        definition->opcode == MIR_INDEX_ADDRESS ||
        definition->opcode == MIR_MEMBER_ADDRESS ||
        definition->opcode == MIR_CALL_AGGREGATE)
        return 0;
    if ((definition->opcode == MIR_LOAD_INDIRECT ||
         definition->opcode == MIR_INDEX_LOAD) &&
        definition->memory_size > 2)
        return 1;
    return type_size(definition->type) > 2;
}

static int mir_value_is_single_call_argument(int value, int size)
{
    int argument_count = 0;
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src2 == value)
            return 0;
        if (insn->src1 != value)
            continue;
        if (insn->opcode != MIR_ARG || type_size(insn->type) != size ||
            ++argument_count > 1)
            return 0;
    }
    /* Item T42 (mir-text-size-plan.md): this used to also require the
     * call's *total* argument count to be <=3 (a second scan counting
     * every MIR_ARG sharing this value's own call_id) - a restriction
     * this function's own sibling, mir_address_is_single_call_argument
     * just below, never had (it only ever checks this *value's* own use
     * count, never the call's total arity). No comment anywhere
     * explained the cap, and nothing about recomputing a value fresh
     * from its own fixed ix-relative offset at push time depends on how
     * many *other* arguments the same call has - IX never moves during
     * argument evaluation regardless of arity. Found via
     * tests/t2darr.c's check() (`printf("FAIL %s got %d expected %d\n",
     * name, got, expected)`, a 4-argument call): `name`'s value,
     * otherwise eligible for this exact rematerialization, was falling
     * through to mir_call_argument_cache_target's costlier BC-cache
     * round trip purely because the call had one argument more than
     * this arbitrary cap allowed. */
    return argument_count == 1;
}

static int mir_widened_param_is_single_call_argument(
    int value, int *offset_out, int *source_type_out)
{
    const struct MirInsn *definition = mir_definition(value);
    const struct MirInsn *source;
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (definition == NULL || definition->opcode != MIR_UNARY ||
        definition->immediate != 0 || type_size(definition->type) != 4 ||
        type_is_float(definition->type) ||
        !mir_value_is_single_call_argument(value, 4))
        return 0;
    source = mir_definition(definition->src1);
    if (source == NULL || source->opcode != MIR_PARAM ||
        type_ptr_depth(source->type) != 0 || type_size(source->type) != 2 ||
        mir_value_use_count(source->dst) != 1 ||
        !mir_scalar_memory_location(source, &memory_type,
                                    &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    if (offset_out != NULL)
        *offset_out = memory_offset;
    if (source_type_out != NULL)
        *source_type_out = source->type;
    return 1;
}

int mir_load_is_single_call_argument(int value, int size)
{
    const struct MirInsn *definition = mir_definition(value);
    int memory_type;
    int memory_storage;
    int memory_offset;

    return definition != NULL && definition->opcode == MIR_LOAD &&
        mir_scalar_memory_location(definition, &memory_type,
                                   &memory_storage, &memory_offset) &&
        type_size(memory_type) == size &&
        (memory_storage == SC_LOCAL || memory_storage == SC_PARAM) &&
        memory_offset >= -128 && memory_offset + size - 1 <= 127 &&
        mir_value_is_single_call_argument(value, size);
}

static int mir_global_load_is_single_call_argument(int value, int size)
{
    const struct MirInsn *definition = mir_definition(value);
    int memory_type;
    int memory_storage;
    int memory_offset;

    return mir_global_argument_rematerialization_enabled &&
        size == 2 && definition != NULL &&
        definition->opcode == MIR_LOAD &&
        mir_scalar_memory_location(definition, &memory_type,
                                   &memory_storage, &memory_offset) &&
        type_size(memory_type) == size &&
        (memory_storage == SC_GLOBAL || memory_storage == SC_EXTERN) &&
        (memory_storage != SC_EXTERN || memory_offset == 0) &&
        mir_value_is_single_call_argument(value, size);
}

static int mir_stable_pointer_argument_address(
    int value, const struct MirInsn **root_out, int *storage_out,
    long *member_offset_out)
{
    const struct MirInsn *definition = mir_definition(value);
    int memory_type;
    int memory_storage;
    int memory_offset;
    long member_offset = 0;

    while (definition != NULL &&
           definition->opcode == MIR_MEMBER_ADDRESS) {
        member_offset += definition->immediate;
        definition = mir_definition(definition->src1);
    }
    if (definition == NULL || definition->opcode != MIR_LOAD ||
        !mir_scalar_memory_location(definition, &memory_type,
                                    &memory_storage, &memory_offset) ||
        (memory_storage != SC_GLOBAL && memory_storage != SC_EXTERN) ||
        type_size(memory_type) != 2 || type_ptr_depth(memory_type) == 0)
        return 0;
    if (root_out != NULL)
        *root_out = definition;
    if (storage_out != NULL)
        *storage_out = memory_storage;
    if (member_offset_out != NULL)
        *member_offset_out = member_offset;
    return 1;
}

static int mir_indirect_load_is_single_stable_pointer_call_argument(
    int value, int size)
{
    const struct MirInsn *definition = mir_definition(value);
    int argument_count = 0;
    int instruction;

    if (!mir_stable_pointer_argument_rematerialization_enabled ||
        size != 2 || definition == NULL ||
        definition->opcode != MIR_LOAD_INDIRECT ||
        definition->memory_size != 2 || definition->bit_width != 0 ||
        !mir_stable_pointer_argument_address(
            definition->src1, NULL, NULL, NULL))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src2 == value)
            return 0;
        if (insn->src1 != value)
            continue;
        if (insn->opcode != MIR_ARG || type_size(insn->type) != size ||
            ++argument_count > 1)
            return 0;
    }
    return argument_count == 1;
}

static int mir_value_only_used_by_stable_pointer_argument(int value)
{
    int uses = 0;
    int instruction;

    if (!mir_stable_pointer_argument_rematerialization_enabled)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src2 == value || mir_call_uses_value(insn, value))
            return 0;
        if (insn->src1 != value)
            continue;
        if (++uses > 1)
            return 0;
        if (insn->opcode == MIR_MEMBER_ADDRESS &&
            mir_value_only_used_by_stable_pointer_argument(insn->dst))
            continue;
        if (insn->opcode == MIR_LOAD_INDIRECT &&
            mir_indirect_load_is_single_stable_pointer_call_argument(
                insn->dst, 2))
            continue;
        return 0;
    }
    return uses == 1;
}

static int mir_global_address_is_single_indirect_call_argument(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int argument_count = 0;
    int call_id = -1;
    int call_count = 0;
    int memory_type;
    int memory_storage;
    int memory_offset;
    int instruction;

    if (definition == NULL || definition->opcode != MIR_ADDRESS ||
        !mir_scalar_memory_location(definition, &memory_type,
                                    &memory_storage, &memory_offset) ||
        (memory_storage != SC_GLOBAL &&
         memory_storage != SC_EXTERN))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->src2 == value)
            return 0;
        if (insn->src1 != value)
            continue;
        if (insn->opcode != MIR_ARG ||
            type_size(insn->type) != 2 ||
            ++argument_count > 1)
            return 0;
        call_id = insn->secondary_offset;
    }
    if (argument_count != 1)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != MIR_CALL ||
            insn->secondary_offset != call_id)
            continue;
        if (strcmp(insn->name, "<indirect>") != 0 ||
            ++call_count > 1)
            return 0;
    }
    return call_count == 1;
}

static int mir_global_indirect_address_argument_count(void)
{
    int count = 0;
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_ADDRESS &&
            mir_global_address_is_single_indirect_call_argument(
                mir.insns[instruction].dst) &&
            ++count >= 3)
            return count;
    return count;
}

/* A MIR_ADDRESS whose sole use is exactly one MIR_ARG is stable and
 * rematerializable. Local/parameter addresses are fixed functions of IX.
 * Global addresses use this path only for call-heavy indirect orchestration
 * with at least three independent global-address arguments: a single
 * rematerialization can lose a profitable BC cache or peephole shape, while
 * the repeated class removes frame/cache traffic overall. */
static int mir_address_is_single_call_argument(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int argument_count = 0;
    int memory_type;
    int memory_storage;
    int memory_offset;
    int instruction;

    if (definition == NULL || definition->opcode != MIR_ADDRESS ||
        mir_declared_is_vla_object(definition->name) ||
        !mir_scalar_memory_location(definition, &memory_type,
                                    &memory_storage, &memory_offset) ||
        ((memory_storage == SC_LOCAL || memory_storage == SC_PARAM)
             ? (memory_offset < -128 || memory_offset > 127)
             : (memory_storage != SC_GLOBAL &&
                memory_storage != SC_EXTERN)))
        return 0;
    if ((memory_storage == SC_GLOBAL ||
         memory_storage == SC_EXTERN) &&
        (!mir_global_address_is_single_indirect_call_argument(value) ||
         mir_global_indirect_address_argument_count() < 3))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src2 == value)
            return 0;
        if (insn->src1 != value)
            continue;
        if (insn->opcode != MIR_ARG || type_size(insn->type) != 2 ||
            ++argument_count > 1)
            return 0;
    }
    return argument_count == 1;
}

/* mir-text-size Item T25 (mir-text-size-plan.md): sibling to
 * mir_load_is_single_call_argument above, but recognizing a completely
 * different use position - a value whose sole use is the *callee*
 * operand of an indirect MIR_CALL (`insn->src1` when the call's own
 * `name` is the sentinel string "<indirect>"), not an ordinary
 * MIR_ARG. Neither mir_load_is_single_call_argument nor
 * mir_address_is_single_call_argument recognize this use at all - a
 * function pointer read once and called through immediately still
 * takes a full spill-and-reload round-trip through its backend slot
 * even though nothing else in the function ever touches it, exactly
 * the same wasted-slot-traffic shape those two predicates already
 * fixed for the MIR_ARG position. Found via tests/tc89core.c's
 * main(): `fp(41)` (fp a local function-pointer variable) reloads fp
 * from its own backend slot instead of reading it fresh from its
 * stable ix-relative home the way legacy does. */
static int mir_load_is_single_indirect_call_target(int value, int size)
{
    const struct MirInsn *definition = mir_definition(value);
    int use_count = 0;
    int memory_type;
    int memory_storage;
    int memory_offset;
    int instruction;

    if (definition == NULL || definition->opcode != MIR_LOAD ||
        !mir_scalar_memory_location(definition, &memory_type,
                                    &memory_storage, &memory_offset) ||
        type_size(memory_type) != size ||
        (memory_storage != SC_LOCAL && memory_storage != SC_PARAM) ||
        memory_offset < -128 || memory_offset + size - 1 > 127)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src2 == value)
            return 0;
        if (insn->src1 != value)
            continue;
        if (insn->opcode != MIR_CALL ||
            strcmp(insn->name, "<indirect>") != 0 || ++use_count > 1)
            return 0;
    }
    return use_count == 1;
}

/* Item 9 (mir-migration-plan-100): DCC_MIR_FUSE_REPORT=1 prints, per function,
 * how many scalar comparisons the Item 1/4 fusion caught versus how many
 * still fell through to mir_emit_scalar_compare's materialize-then-store
 * path, so future regressions in fusion coverage are visible without
 * re-reading assembly. */
static int mir_fuse_report_fused_count;
static int mir_fuse_report_materialized_count;

/* Item 8 (mir-migration-plan-100): when set, mir_prepare_backend_slots must
 * not allocate a frame slot for a comparison result (or intervening '!'
 * result) that mir_try_emit_spilled_scalar_cfg's Items 1/4 fusion consumes
 * entirely in registers - only that selector actually skips the store/load
 * for such values, so this stays off for every other caller. */
static int mir_backend_slots_skip_fused_comparisons = 0;

/* Item 20 (mir-migration-plan-100): DCC_MIR_SLOT_REPORT=1 prints, per
 * function, how many values mir_prepare_backend_slots considered for a
 * frame slot ("requested" - every value reaching its own first-definition
 * point that was not already unconditionally excluded, i.e. still a live
 * candidate) versus how many actually received one ("assigned" - the rest
 * were elided by Items 13-18's dead-value/forwarding/fusion/reuse skip
 * predicates). A large requested/assigned gap on a function flags it as a
 * good candidate for further slot-elision work; a small one means most of
 * that function's slot traffic is already necessary. */
static int mir_slot_report_requested_count;
static int mir_slot_report_assigned_count;
/* mir-migration-plan-next10 parameter-rehoming investigation: how many
 * assigned slots are for a value whose sole definition is MIR_PARAM (i.e.
 * a function parameter re-copied into a fresh backend slot instead of
 * being re-read from its own stable incoming ix+N offset each time it is
 * needed again). Diagnostic-only counter; not read by any acceptance
 * gate. */
static int mir_slot_report_param_assigned_count;

/* Item 13 (mir-migration-plan-100): a definition whose single use is the
 * immediately following instruction (no intervening label/call/aliasing
 * store) never needs a backend slot at all - mir_emit_virtual_store already
 * skips storing it via mir_can_forward_hl_to_next (kept in HL across the
 * one-instruction gap). Reuse mir_can_forward_hl_to_next itself (rather than
 * a second copy of the same forwarding predicate) so the accounting pass and
 * the real emission-time skip can never drift apart - the repo's own Item 19
 * caution. Only 1-unit (narrow, HL-sized) values ever reach
 * mir_emit_virtual_store/mir_can_forward_hl_to_next; wide values use the
 * separate _wide store path and must still get a slot.
 *
 * mir-text-size Item T28 (mir-text-size-plan.md): this elision used to be
 * withheld whenever the forward target was itself a MIR_STORE (via a
 * now-removed mir_backend_slot_forward_target_is_store helper), forcing
 * mir_prepare_backend_slots to still hand out a real slot for such values.
 * But mir_can_forward_hl_to_next's own MIR_STORE case (above) already fully
 * validates that forwarding into a store is safe - resolvable non-struct
 * <=2-byte memory location, no other use anywhere in the function - and
 * mir_emit_virtual_store's has_slot branch already forwards the value via
 * mir_forwarded_hl_value for exactly this case (see forward_to_store
 * there). The only effect of the withheld elision was that the value's own
 * slot got persisted (via an unconditional ld (ix+d),l/h pair, or
 * (iy+d),l/h) and then never read back by anyone, since the real consuming
 * MIR_STORE already takes the value straight from HL. Found via
 * tests/tclit.c's pick_pair(), whose compound-literal field constants were
 * each stored twice: once into this now-dead slot, once into the real
 * per-field destination the closing ldir reads from. */

/* mir-text-size Item T22 (mir-text-size-plan.md): when a value's sole use
 * is an immediately-following 1-byte MIR_STORE (mir_can_forward_hl_to_next's
 * MIR_STORE case, e.g. an int/char constant initializing one element of a
 * byte array), mir_emit_virtual_store still persists BOTH bytes of the
 * value's own backend slot even though only L is ever read back for a
 * 1-byte store - H's persisted byte is never read by anyone, proven by the
 * same mir_can_forward_hl_to_next scan that already showed nothing
 * references the value after the forwarded store. Skipping just that
 * H-byte store (kept narrowly scoped to the forward-to-1-byte-store case,
 * not the general slot-elision Item 13 left alone for stores) closes this
 * gap safely. Found via tests/tc89core.c's main(): its `char msg[] =
 * "core"` initializer stored a wasted high byte for every one of its five
 * constant elements. */
static int mir_forward_store_target_is_narrow(int forward_instruction)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (forward_instruction < 0 || forward_instruction >= mir.count)
        return 0;
    return mir_scalar_memory_location(&mir.insns[forward_instruction],
                                      &memory_type, &memory_storage,
                                      &memory_offset) &&
           type_size(memory_type) == 1;
}

static int mir_backend_slot_forwardable(int value, int units, int instruction)
{
    int saved_index;
    int forwardable;

    if (units != 1)
        return 0;
    /* A MIR_PHI destination is never written by the normal linear emission
     * loop at its own instruction index - mir_emit_spilled_phi_copies writes
     * it from each predecessor's jump/branch instruction instead, with
     * mir_emit_instruction_index left at that unrelated predecessor index.
     * Evaluating mir_can_forward_hl_to_next() there would check the wrong
     * "next instruction" entirely (found live during Item 13 validation via
     * tvla.c's fixed_cast_bounds regression), so phi destinations must always
     * keep a real slot. */
    if (mir.insns[instruction].opcode == MIR_PHI)
        return 0;
    saved_index = mir_emit_instruction_index;
    mir_emit_instruction_index = instruction;
    forwardable = mir_can_forward_hl_to_next(value);
    mir_emit_instruction_index = saved_index;
    return forwardable;
}

/* Item T79 (mir-text-size-plan.md): mir_prepare_backend_slots' reservation
 * pass only ever consulted mir_can_forward_hl_to_next (via
 * mir_backend_slot_forwardable above, which explicitly requires
 * units == 1, i.e. narrow/16-bit values only) to decide whether a value
 * can skip a real slot. Item T40 built the wide (32-bit HL:DE) equivalent,
 * mir_can_forward_hl_de_to_next, and wired it into mir_emit_virtual_
 * store_wide's emission-time decision - but never added a matching
 * reservation-time check, so a wide value that emission will always
 * forward directly (e.g. a wide binary result whose sole use is the very
 * next MIR_RETURN, the plain `return a + b;` shape) still gets a real,
 * dead backend slot reserved for it: 2 units/4 bytes of frame space that
 * the emitted body never once references. Found via tests/tlong.c's
 * lsum (`long lsum(long a, long b) { return a + b; }`): its only gap vs.
 * legacy is exactly this unused `ld hl,-4/add hl,sp/ld sp,hl` prologue
 * pair - once it is skipped, the rest of the body (already using
 * mir_can_forward_hl_de_to_next's push/pop handoff to carry the first
 * wide operand across evaluating the second, matching legacy instruction-
 * for-instruction) already matches legacy byte-for-byte. This is the
 * exact same reservation/emission mismatch class as Item T59's
 * mir_call_argument_slot_forwardable fix, one level up for wide values.
 *
 * Item T86 narrows the reservation skip to measured-profitable direct
 * returns: every integer-long producer, plus float MIR_BINARY results.
 * Float conversions and wide intermediates consumed by MIR_UNARY were
 * correct but slower under the checked emulator, so they retain a slot
 * and stay behind the cost gate. */
static int mir_wide_backend_slot_forwardable(int value, int units,
                                              int instruction)
{
    const struct MirInsn *definition;
    const struct MirInsn *next;
    int next_instruction;
    int saved_index;
    int forwardable;

    if (units != 2)
        return 0;
    if (mir.insns[instruction].opcode == MIR_PHI)
        return 0;
    next_instruction = mir_forward_skip_target(instruction);
    if (next_instruction >= mir.count)
        return 0;
    next = &mir.insns[next_instruction];
    definition = mir_definition(value);
    if (definition == NULL)
        return 0;
    if (!((mir_wide_binary_lhs_forwarding_enabled &&
           next->opcode == MIR_BINARY && next->src1 == value &&
           type_size(next->secondary_offset) == 4) ||
          (mir_wide_store_forwarding_enabled &&
           next->opcode == MIR_STORE && next->src1 == value &&
           (next->memory_size == 4 || type_size(next->type) == 4)) ||
          (next->opcode == MIR_STORE_INDIRECT && next->src2 == value &&
           mir_wide_indirect_store_address_has_direct_reload(next)) ||
          (mir_wide_binary_rhs_forwarding_enabled &&
           mir_wide_binary_rhs_pair_supported(value, next) &&
           next->src2 == value))) {
        if ((!type_is_long(definition->type) &&
             !(type_is_float(definition->type) &&
               definition->opcode == MIR_BINARY)) ||
            next->opcode != MIR_RETURN || next->src1 != value)
            return 0;
    }
    saved_index = mir_emit_instruction_index;
    mir_emit_instruction_index = instruction;
    forwardable = mir_can_forward_hl_de_to_next(value);
    mir_emit_instruction_index = saved_index;
    return forwardable;
}

static int mir_wide_helper_lhs_consumer(int value, int instruction,
                                        int *consumer_out)
{
    const struct MirInsn *definition;
    int consumer_index;

    if (value < 0 || instruction < 0 || instruction >= mir.count ||
        mir_value_use_count(value) != 1)
        return 0;
    definition = mir_definition(value);
    if (definition == NULL || definition->opcode == MIR_PHI ||
        !mir_definition_is_wide(definition))
        return 0;
    for (consumer_index = instruction + 1;
         consumer_index < mir.count; ++consumer_index) {
        const struct MirInsn *consumer = &mir.insns[consumer_index];
        int operation;

        if (consumer->src1 != value && consumer->src2 != value &&
            !mir_call_uses_value(consumer, value))
            continue;
        operation = (int)consumer->immediate;
        if (consumer->opcode != MIR_BINARY ||
            (consumer->src1 != value &&
             !(consumer->src2 == value &&
               (operation == TOK_EQ || operation == TOK_NE ||
                operation == '<' || operation == '>' ||
                operation == TOK_LE || operation == TOK_GE))) ||
            type_size(consumer->secondary_offset) != 4 ||
            mir_wide_runtime_helper(consumer) == NULL)
            return 0;
        break;
    }
    if (consumer_index >= mir.count)
        return 0;
    if (consumer_out != NULL)
        *consumer_out = consumer_index;
    return 1;
}

static int mir_wide_helper_lhs_span_is_safe(int producer, int consumer)
{
    int instruction;

    for (instruction = producer + 1;
         instruction < consumer; ++instruction)
        switch (mir.insns[instruction].opcode) {
        case MIR_NOP:
        case MIR_CONST:
        case MIR_LOAD:
        case MIR_UNARY:
            break;
        default:
            return 0;
        }
    return 1;
}

static int mir_wide_helper_handoff_supported(const struct MirInsn *consumer)
{
    const struct MirInsn *left;
    const struct MirInsn *right;
    int operation;

    if (consumer == NULL || consumer->opcode != MIR_BINARY)
        return 0;
    operation = (int)consumer->immediate;
    if (operation == TOK_EQ || operation == TOK_NE ||
        operation == '<' || operation == '>' ||
        operation == TOK_LE || operation == TOK_GE) {
        left = mir_definition(consumer->src1);
        right = mir_definition(consumer->src2);
        return left != NULL && left->opcode == MIR_UNARY &&
               right != NULL &&
               (right->opcode == MIR_UNARY ||
                (right->opcode == MIR_BINARY &&
                 type_is_float(right->type)));
    }

    if (type_is_float(consumer->secondary_offset))
        return operation == '+' || operation == '-' ||
               operation == '*' || operation == '/';
    return operation == '*' || operation == '/' || operation == '%';
}

static int mir_swap_wide_comparison_operator(int operation)
{
    switch (operation) {
    case '<': return '>';
    case '>': return '<';
    case TOK_LE: return TOK_GE;
    case TOK_GE: return TOK_LE;
    default: return operation;
    }
}

static int mir_wide_helper_rhs_pair_consumer(
    int value, int instruction, int *consumer_out)
{
    const struct MirInsn *consumer;
    const struct MirInsn *left;
    const struct MirInsn *outer_consumer;
    int left_consumer;
    int left_instruction;
    int outer_consumer_index;

    if (value < 0 || mir_value_use_count(value) != 1 ||
        instruction < 0 || instruction + 1 >= mir.count)
        return 0;
    consumer = &mir.insns[instruction + 1];
    if (consumer->opcode != MIR_BINARY ||
        consumer->src2 != value ||
        !mir_wide_helper_handoff_supported(consumer))
        return 0;
    left = mir_definition(consumer->src1);
    if (left == NULL)
        return 0;
    left_instruction = (int)(left - mir.insns);
    if (!mir_wide_helper_lhs_consumer(
            consumer->src1, left_instruction, &left_consumer) ||
        left_consumer != instruction + 1 ||
        !mir_wide_helper_lhs_span_is_safe(
            left_instruction, instruction + 1))
        return 0;
    if (!mir_wide_helper_lhs_consumer(
            consumer->dst, instruction + 1, &outer_consumer_index))
        return 0;
    outer_consumer = &mir.insns[outer_consumer_index];
    if (outer_consumer->opcode != MIR_BINARY ||
        (outer_consumer->immediate != TOK_EQ &&
         outer_consumer->immediate != TOK_NE &&
         outer_consumer->immediate != '<' &&
         outer_consumer->immediate != '>' &&
         outer_consumer->immediate != TOK_LE &&
         outer_consumer->immediate != TOK_GE) ||
        outer_consumer->src2 != consumer->dst)
        return 0;
    if (consumer_out != NULL)
        *consumer_out = instruction + 1;
    return 1;
}

static int mir_wide_helper_lhs_slot_forwardable(int value, int units,
                                                 int instruction)
{
    int consumer;

    return units == 2 &&
        mir_wide_helper_lhs_consumer(value, instruction, &consumer) &&
        mir_wide_helper_handoff_supported(&mir.insns[consumer]) &&
        mir_wide_helper_lhs_span_is_safe(instruction, consumer);
}

static int mir_wide_helper_rhs_pair_slot_forwardable(
    int value, int units, int instruction)
{
    return units == 2 &&
           mir_wide_helper_rhs_pair_consumer(
               value, instruction, NULL);
}

static int mir_wide_call_constant_comparison_consumer(
    int value, int instruction, int *consumer_out)
{
    const struct MirInsn *constant;
    const struct MirInsn *consumer;
    const struct MirInsn *definition = mir_definition(value);
    int consumer_index;
    int operation;
    int scan;

    if (definition == NULL || definition->opcode != MIR_CALL ||
        type_size(definition->type) != 4 ||
        mir_value_use_count(value) != 1)
        return 0;
    consumer_index = -1;
    for (scan = instruction + 1; scan < mir.count; ++scan) {
        const struct MirInsn *insn = &mir.insns[scan];

        if (insn->src1 == value || insn->src2 == value ||
            mir_call_uses_value(insn, value)) {
            consumer_index = scan;
            break;
        }
        if (insn->opcode != MIR_NOP && insn->opcode != MIR_CONST &&
            insn->opcode != MIR_FLOAT_CONST)
            return 0;
    }
    if (consumer_index < 0)
        return 0;
    consumer = &mir.insns[consumer_index];
    operation = (int)consumer->immediate;
    constant = mir_definition(consumer->src2);
    if (consumer->opcode != MIR_BINARY ||
        consumer->src1 != value ||
        type_size(consumer->secondary_offset) != 4 ||
        (operation != TOK_EQ && operation != TOK_NE &&
         operation != '<' && operation != '>' &&
         operation != TOK_LE && operation != TOK_GE) ||
        constant == NULL ||
        (constant->opcode != MIR_CONST &&
         constant->opcode != MIR_FLOAT_CONST) ||
        type_size(constant->type) != 4)
        return 0;
    if (consumer_out != NULL)
        *consumer_out = consumer_index;
    return 1;
}

static int mir_wide_constant_is_call_comparison_rhs(int value)
{
    int instruction;

    if (mir_value_use_count(value) != 1)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *consumer = &mir.insns[instruction];
        const struct MirInsn *left;

        if (consumer->src2 != value)
            continue;
        left = mir_definition(consumer->src1);
        return left != NULL &&
               mir_wide_call_constant_comparison_consumer(
                   consumer->src1,
                   (int)(left - mir.insns), NULL);
    }
    return 0;
}

struct MirNestedWideAdd {
    int narrow_source;
    int outer_lhs;
    int wide_rhs;
    int result;
    int inner_instruction;
    int outer_instruction;
    int return_instruction;
};

static int mir_match_nested_wide_add(
    int instruction, struct MirNestedWideAdd *plan)
{
    const struct MirInsn *inner;
    const struct MirInsn *narrow;
    const struct MirInsn *outer;
    const struct MirInsn *outer_lhs;
    const struct MirInsn *return_insn;
    const struct MirInsn *store;
    const struct MirInsn *wide_rhs;
    int memory_offset;
    int memory_storage;
    int memory_type;

    if (mir_cfg_block_count() != 1 ||
        instruction < 0 || instruction + 6 >= mir.count)
        return 0;
    narrow = &mir.insns[instruction];
    inner = &mir.insns[instruction + 1];
    outer = &mir.insns[instruction + 2];
    store = &mir.insns[instruction + 4];
    return_insn = &mir.insns[instruction + 6];
    if (narrow->opcode != MIR_UNARY || narrow->immediate != 0 ||
        type_size(narrow->type) != 4 ||
        inner->opcode != MIR_BINARY || inner->immediate != '+' ||
        inner->src1 != narrow->dst ||
        type_size(inner->secondary_offset) != 4 ||
        outer->opcode != MIR_BINARY ||
        (outer->immediate != '+' && outer->immediate != '&' &&
         outer->immediate != '/' && outer->immediate != '%') ||
        outer->src2 != inner->dst ||
        type_size(outer->secondary_offset) != 4 ||
        mir.insns[instruction + 3].opcode != MIR_NOP ||
        store->opcode != MIR_STORE || store->src1 != outer->dst ||
        mir.insns[instruction + 5].opcode != MIR_NOP ||
        return_insn->opcode != MIR_RETURN ||
        return_insn->src1 != outer->dst)
        return 0;
    outer_lhs = mir_definition(outer->src1);
    wide_rhs = mir_definition(inner->src2);
    if (mir_definition(narrow->src1) == NULL ||
        mir_definition(narrow->src1)->opcode != MIR_PARAM ||
        type_size(mir_definition(narrow->src1)->type) > 2 ||
        outer_lhs == NULL || outer_lhs->opcode != MIR_PARAM ||
        type_size(outer_lhs->type) != 4 ||
        wide_rhs == NULL || wide_rhs->opcode != MIR_PARAM ||
        type_size(wide_rhs->type) != 4 ||
        store->object != outer_lhs->object ||
        !mir_scalar_memory_location(
            outer_lhs, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || memory_offset < -128 ||
        memory_offset + 3 > 127)
        return 0;
    if (plan != NULL) {
        plan->narrow_source = narrow->src1;
        plan->outer_lhs = outer->src1;
        plan->wide_rhs = inner->src2;
        plan->result = outer->dst;
        plan->inner_instruction = instruction + 1;
        plan->outer_instruction = instruction + 2;
        plan->return_instruction = instruction + 6;
    }
    return 1;
}

static int mir_nested_wide_add_slot_forwardable(
    int value, int units, int instruction)
{
    struct MirNestedWideAdd plan;
    int candidate;

    (void)instruction;
    if (units != 2)
        return 0;
    for (candidate = 0; candidate < mir.count; ++candidate)
        if (mir_match_nested_wide_add(candidate, &plan) &&
            (value == plan.outer_lhs ||
             value == mir.insns[candidate].dst ||
             value == mir.insns[plan.inner_instruction].dst ||
             value == plan.result))
            return 1;
    return 0;
}

static int mir_nested_wide_add_source_parameter(int value)
{
    struct MirNestedWideAdd plan;
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir_match_nested_wide_add(instruction, &plan) &&
            value == plan.outer_lhs)
            return 1;
    return 0;
}

static int mir_emit_nested_wide_add_parameter(
    MirStream *out, int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int memory_offset;
    int memory_storage;
    int memory_type;

    if (definition == NULL || definition->opcode != MIR_PARAM ||
        !mir_scalar_memory_location(
            definition, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || type_size(memory_type) != 4 ||
        memory_offset < -128 || memory_offset + 3 > 127)
        return 0;
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
            memory_offset, memory_offset + 1,
            memory_offset + 2, memory_offset + 3);
    return 1;
}

static int mir_wide_signed_const_lhs_slot_forwardable(
    int value, int units, int instruction)
{
    const struct MirInsn *consumer;

    if (units != 2 || mir_value_use_count(value) != 1 ||
        instruction < 0 || instruction + 1 >= mir.count)
        return 0;
    consumer = &mir.insns[instruction + 1];
    return consumer->opcode == MIR_BINARY &&
           consumer->src1 == value &&
           mir_wide_operation_is_signed_const_relational(consumer);
}

/* Item T59 (mir-text-size-plan.md): mir_prepare_backend_slots' own
 * reservation pass only recognized mir_load_is_single_call_argument (a
 * MIR_LOAD whose sole use is exactly one call argument, restricted to
 * SC_LOCAL/SC_PARAM memory so re-reading it at push time is a cheap
 * ix-relative reload) as grounds to skip a slot for a would-be call
 * argument. mir_emit_virtual_store's own emission-time logic is strictly
 * more capable: mir_can_forward_hl_to_call_argument accepts *any*
 * single-use, adjacent-to-call value regardless of how it was defined
 * (a MIR_LOAD from a global, a MIR_BINARY result, etc.) and simply
 * leaves it resident in HL/queues it for direct forwarding, so it is
 * never actually stored to the slot mir_prepare_backend_slots reserved
 * for it. This mismatch reserves genuinely dead frame space - 2 bytes
 * nothing ever writes to or reads from - for any such value whose
 * defining opcode is not itself a MIR_LOAD-from-local/param (e.g. a
 * MIR_LOAD of a global like `errno`). Found via tests/terrno.c's
 * expect_ok_fd (`printf(..., errno)` on its else-branch): the reserved
 * slot forces an entire IX-relative frame lifecycle
 * (push ix/ld ix,0/add ix,sp/... teardown) for a function whose actual
 * emitted body never references any (ix-N) backend-slot offset at all.
 * Reusing the exact same predicate the emitter already trusts to elide
 * the store closes this gap with no new mechanism. */
static int mir_call_argument_slot_forwardable(int value, int units,
                                               int instruction)
{
    int saved_index;
    int forwardable;

    if (units != 1)
        return 0;
    if (mir.insns[instruction].opcode == MIR_PHI)
        return 0;
    saved_index = mir_emit_instruction_index;
    mir_emit_instruction_index = instruction;
    forwardable = mir_can_forward_hl_to_call_argument(value);
    mir_emit_instruction_index = saved_index;
    return forwardable;
}

static int mir_direct_named_home_location(const struct MirInsn *definition,
                                          int *storage, int *offset)
{
    int memory_type;

    if (definition == NULL)
        return 0;
    if (definition->object >= 0 && definition->object < mir.object_count) {
        const struct MirObject *object = &mir.objects[definition->object];
        *storage = object->storage;
        *offset = object->offset;
        return 1;
    }
    return mir_scalar_memory_location(definition, &memory_type,
                                      storage, offset) &&
        (*storage == SC_PARAM || *storage == SC_LOCAL) &&
        type_size(memory_type) == type_size(definition->type);
}

/*
 * Large frames use IY for spill slots. In the measured masked-wide loop
 * class, snapshotting an adjacent named RHS into that spill area and loading
 * it back at the next instruction is pure overhead; defer the load to the
 * binary consumer without reordering its operands.
 */
static int mir_wide_load_is_immediate_binary_rhs(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    const struct MirInsn *consumer;
    int definition_index;
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (mir_effective_local_bytes() + mir.aggregate_temp_bytes <= 140 ||
        !mir_function_has_wide_mask_zero_branch() ||
        definition == NULL || definition->opcode != MIR_LOAD ||
        type_size(definition->type) != 4 ||
        (definition->memory_flags & 1) != 0 ||
        mir_value_use_count(value) != 1 ||
        !mir_scalar_memory_location(definition, &memory_type,
                                    &memory_storage, &memory_offset) ||
        (memory_storage != SC_LOCAL && memory_storage != SC_PARAM) ||
        type_size(memory_type) != 4)
        return 0;
    definition_index = (int)(definition - mir.insns);
    if (definition_index + 1 >= mir.count)
        return 0;
    consumer = &mir.insns[definition_index + 1];
    return consumer->opcode == MIR_BINARY &&
           consumer->src2 == value &&
           type_size(consumer->secondary_offset) == 4;
}

static int mir_pointer_value_is_aggregate_address(int value)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode == MIR_COPY_AGGREGATE &&
            (insn->src1 == value || insn->src2 == value))
            return 1;
        if (insn->opcode == MIR_CALL_AGGREGATE && insn->src1 == value)
            return 1;
    }
    return 0;
}

/* Item T425: the T424/T55 classifier work proved that some single-use pointer
 * parameters are semantically safe, but forcing them through full object
 * promotion is still too expensive for trivial `return p;` / `*p` shapes and
 * wider than needed for `*p = bool_expr`, where the only real win is avoiding
 * the redundant "load parameter -> spill backend slot -> reload later as the
 * address operand" round trip. For an objectless pointer parameter whose sole
 * use is exactly one direct dereference/address formation/return, treat the
 * incoming named `ix+N` home as stable and let later uses reload from there
 * on demand instead of manufacturing a backend slot. This recovers
 * `tbool.set_bool`'s win without re-admitting the broader object-promotion
 * path that cost T424 its `+1/-3` experiment. */
static int mir_pointer_value_has_single_safe_named_home_use_recursive(
    int value, int *use_count, unsigned char *visiting)
{
    int instruction;

    if (value < 0 || value >= mir.next_value || visiting[value])
        return 0;
    visiting[value] = 1;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        int uses_src1 = insn->src1 == value;
        int uses_src2 = insn->src2 == value;

        if (mir_call_uses_value(insn, value)) {
            visiting[value] = 0;
            return 0;
        }
        if (!uses_src1 && !uses_src2)
            continue;
        if (insn->opcode == MIR_ARG && uses_src1) {
            visiting[value] = 0;
            return 0;
        }
        if (insn->opcode == MIR_UNARY && uses_src1 &&
            insn->immediate == 0 && insn->dst >= 0 &&
            type_ptr_depth(insn->type) > 0) {
            if (!mir_pointer_value_has_single_safe_named_home_use_recursive(
                    insn->dst, use_count, visiting)) {
                visiting[value] = 0;
                return 0;
            }
            continue;
        }
        ++*use_count;
        if (*use_count > 1) {
            visiting[value] = 0;
            return 0;
        }
        if ((insn->opcode == MIR_LOAD_INDIRECT ||
             insn->opcode == MIR_INDEX_LOAD ||
             insn->opcode == MIR_INDEX_ADDRESS ||
             insn->opcode == MIR_MEMBER_ADDRESS ||
             insn->opcode == MIR_STORE_INDIRECT ||
             insn->opcode == MIR_RETURN) &&
            uses_src1)
            continue;
        visiting[value] = 0;
        return 0;
    }
    visiting[value] = 0;
    return *use_count == 1;
}

static int mir_pointer_value_has_single_safe_named_home_use(int value)
{
    const struct MirInsn *definition;
    unsigned char *visiting;
    int use_count = 0;
    int eligible;

    if (value < 0 || value >= mir.next_value)
        return 0;
    definition = mir_definition(value);
    if (definition == NULL ||
        (definition->opcode != MIR_PARAM && definition->opcode != MIR_LOAD) ||
        type_ptr_depth(definition->type) == 0)
        return 0;
    visiting = (unsigned char *)calloc((size_t)mir.next_value, 1);
    if (mir.next_value > 0 && visiting == NULL)
        fatal("out of memory planning pointer direct homes");
    eligible = mir_pointer_value_has_single_safe_named_home_use_recursive(
        value, &use_count, visiting);
    free(visiting);
    return eligible;
}

/* A value does not need a backend slot while its named IX-relative location
 * remains unchanged. Parameters are stable for the complete function after
 * the no-reassignment proof. A pointer local is stable after its MIR_LOAD when
 * no later store, alias, or backedge can change its slot. Scalar locals remain
 * slot-based: the measured prototype added only one further function and does
 * not justify widening this first local slice. Item T425 extends the same
 * "stable named home" idea to objectless single-use pointer parameters whose
 * sole direct use can simply reload the original incoming `ix+N` address. */
static int mir_phi_uses_byte_object_home(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    const struct MirObject *object;
    int effective_local_bytes;

    if (definition == NULL || definition->opcode != MIR_PHI ||
        type_size(definition->type) != 1 ||
        definition->object < 0 ||
        definition->object >= mir.object_count ||
        mir.has_vla)
        return 0;
    object = &mir.objects[definition->object];
    effective_local_bytes = mir_effective_local_bytes();
    return object->storage == SC_LOCAL &&
           type_size(object->type) == 1 &&
           object->offset >= -effective_local_bytes &&
           object->offset < 0;
}

static int mir_value_has_direct_named_home(int value)
{
    const struct MirInsn *definition;
    int has_object;
    int object;
    int home_storage;
    int home_offset;
    int definition_index;
    int local_home;
    int i;

    if (value < 0 || value >= mir.next_value)
        return 0;
    definition = mir_definition(value);
    if (mir_phi_uses_byte_object_home(value))
        return 1;
    if (definition == NULL ||
        (definition->opcode != MIR_PARAM && definition->opcode != MIR_LOAD))
        return mir_value_is_selfstore_wide_increment_result(value);
    if (mir_wide_load_is_immediate_binary_rhs(value))
        return 1;
    if (type_is_struct_object(definition->type) ||
        (type_size(definition->type) != 1 &&
         type_size(definition->type) != 2 &&
         type_size(definition->type) != 4))
        return 0;
    object = definition->object;
    has_object = object >= 0 && object < mir.object_count;
    if (!mir_direct_named_home_location(definition, &home_storage,
                                        &home_offset))
        return 0;
    local_home = home_storage == SC_LOCAL;
    definition_index = (int)(definition - mir.insns);
    if (local_home &&
        (!mir_stable_pointer_local_homes_enabled ||
         definition->opcode != MIR_LOAD ||
         (definition->memory_flags & 1) != 0 ||
         type_ptr_depth(definition->type) == 0 ||
         local_name_address_taken_in_function(definition->name) ||
         mir_has_cfg_backedge()))
        return 0;
    if (!local_home && !has_object &&
        !mir_pointer_value_is_aggregate_address(value) &&
        !mir_pointer_value_has_single_safe_named_home_use(value))
        return 0;
    if (mir.has_vla)
        return 0;
    for (i = 0; i < mir.count; ++i) {
        const struct MirInsn *insn = &mir.insns[i];
        int memory_type;
        int memory_storage;
        int memory_offset;

        if (insn->opcode != MIR_STORE)
            continue;
        if (has_object && insn->object == object) {
            if (local_home && i < definition_index)
                continue;
            return 0;
        }
        if (!has_object &&
            mir_scalar_memory_location(insn, &memory_type,
                                       &memory_storage, &memory_offset) &&
            memory_storage == home_storage &&
            memory_offset == home_offset) {
            if (local_home && i < definition_index)
                continue;
            return 0;
        }
    }
    if (!local_home && definition->opcode == MIR_LOAD) {
        int has_param = 0;
        for (i = 0; i < mir.count; ++i) {
            const struct MirInsn *insn = &mir.insns[i];
            int memory_type;
            int memory_storage;
            int memory_offset;

            if (insn->opcode != MIR_PARAM)
                continue;
            if ((has_object && insn->object == object) ||
                (!has_object &&
                 mir_scalar_memory_location(insn, &memory_type,
                                            &memory_storage,
                                            &memory_offset) &&
                 memory_storage == SC_PARAM &&
                 memory_offset == home_offset)) {
                has_param = 1;
                break;
            }
        }
        if (!has_param)
            return 0;
    }
    /* A fused divmod pair
     * (mir_divmod_partner) must eagerly materialize BOTH its quotient and
     * remainder results into two simultaneous backend slots at whichever
     * operator is encountered first. Direct-object-forwarding one operand can
     * hide that simultaneous frame pressure and select slower stack-checked
     * code. Keep pair operands on the ordinary backend-slot path.
     */
    for (i = 0; i < mir.count; ++i) {
        const struct MirInsn *insn = &mir.insns[i];
        if (insn->opcode == MIR_BINARY &&
            (insn->immediate == '/' || insn->immediate == '%') &&
            (insn->src1 == value || insn->src2 == value) &&
            mir_divmod_partner(i) >= 0)
            return 0;
    }
    if (local_home)
        mir_spilled_cfg_used_stable_pointer_local_home = 1;
    return 1;
}

static int mir_bool_call_named_home(
    int value, int *store_instruction_out, int *offset_out)
{
    const struct MirInsn *definition = mir_definition(value);
    const struct MirInsn *store;
    int definition_instruction;
    int memory_offset;
    int memory_storage;
    int memory_type;
    int store_instruction;
    int instruction;

    if (definition == NULL || definition->opcode != MIR_CALL ||
        !type_is_bool(definition->type) ||
        mir_cfg_block_count() != 4)
        return 0;
    definition_instruction = (int)(definition - mir.insns);
    store_instruction = definition_instruction + 1;
    while (store_instruction < mir.count &&
           mir.insns[store_instruction].opcode == MIR_NOP)
        ++store_instruction;
    if (store_instruction < 0 || store_instruction >= mir.count)
        return 0;
    store = &mir.insns[store_instruction];
    if (store->opcode != MIR_STORE || store->src1 != value ||
        store->object < 0 || (store->memory_flags & 1) != 0 ||
        !mir_scalar_memory_location(
            store, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_LOCAL || type_size(memory_type) != 1 ||
        memory_offset < -128 || local_name_address_taken_in_function(
            store->name))
        return 0;
    for (instruction = store_instruction + 1;
         instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_STORE &&
            mir.insns[instruction].object == store->object)
            return 0;
    if (store_instruction_out != NULL)
        *store_instruction_out = store_instruction;
    if (offset_out != NULL)
        *offset_out = memory_offset;
    return 1;
}

void mir_begin_stable_pointer_local_homes(void)
{
    mir_stable_pointer_local_homes_enabled = 1;
}

void mir_end_stable_pointer_local_homes(void)
{
    mir_stable_pointer_local_homes_enabled = 0;
}

void mir_begin_general_rhs_stack_forwarding(void)
{
    mir_general_rhs_stack_forwarding_enabled = 1;
}

void mir_end_general_rhs_stack_forwarding(void)
{
    mir_general_rhs_stack_forwarding_enabled = 0;
}

void mir_begin_indirect_store_value_forwarding(void)
{
    mir_indirect_store_value_forwarding_enabled = 1;
}

void mir_end_indirect_store_value_forwarding(void)
{
    mir_indirect_store_value_forwarding_enabled = 0;
}

void mir_begin_branch_condition_forwarding(void)
{
    mir_branch_condition_forwarding_enabled = 1;
}

void mir_end_branch_condition_forwarding(void)
{
    mir_branch_condition_forwarding_enabled = 0;
}

void mir_begin_indirect_store_address_forwarding(void)
{
    mir_indirect_store_address_forwarding_enabled = 1;
}

void mir_end_indirect_store_address_forwarding(void)
{
    mir_indirect_store_address_forwarding_enabled = 0;
}

void mir_begin_wide_binary_lhs_forwarding(void)
{
    mir_wide_binary_lhs_forwarding_enabled = 1;
}

void mir_end_wide_binary_lhs_forwarding(void)
{
    mir_wide_binary_lhs_forwarding_enabled = 0;
}

void mir_begin_wide_binary_rhs_forwarding(void)
{
    mir_wide_binary_rhs_forwarding_enabled = 1;
    mir_wide_binary_rhs_forwarding_uses = 0;
}

void mir_end_wide_binary_rhs_forwarding(void)
{
    mir_wide_binary_rhs_forwarding_enabled = 0;
}

int mir_wide_binary_rhs_forwarding_use_count(void)
{
    return mir_wide_binary_rhs_forwarding_uses;
}

void mir_begin_wide_store_forwarding(void)
{
    mir_wide_store_forwarding_enabled = 1;
}

void mir_end_wide_store_forwarding(void)
{
    mir_wide_store_forwarding_enabled = 0;
}

void mir_begin_stable_pointer_argument_rematerialization(void)
{
    mir_stable_pointer_argument_rematerialization_enabled = 1;
}

void mir_end_stable_pointer_argument_rematerialization(void)
{
    mir_stable_pointer_argument_rematerialization_enabled = 0;
}

void mir_begin_global_argument_rematerialization(void)
{
    mir_global_argument_rematerialization_enabled = 1;
}

void mir_end_global_argument_rematerialization(void)
{
    mir_global_argument_rematerialization_enabled = 0;
}

void mir_begin_wide_first_argument_stack_cache(void)
{
    mir_wide_first_argument_stack_cache_enabled = 1;
}

void mir_end_wide_first_argument_stack_cache(void)
{
    mir_wide_first_argument_stack_cache_enabled = 0;
}

void mir_begin_narrow_argument_direct_push(void)
{
    mir_narrow_argument_direct_push_enabled = 1;
}

void mir_end_narrow_argument_direct_push(void)
{
    mir_narrow_argument_direct_push_enabled = 0;
}

void mir_begin_constant_argument_prepacking(void)
{
    mir_constant_argument_prepacking_enabled = 1;
}

void mir_end_constant_argument_prepacking(void)
{
    mir_constant_argument_prepacking_enabled = 0;
}

void mir_begin_promoted_local_slot_reuse(void)
{
    mir_promoted_local_slot_reuse_enabled = 1;
}

void mir_end_promoted_local_slot_reuse(void)
{
    mir_promoted_local_slot_reuse_enabled = 0;
}

static int mir_planned_stack_interval_opcode_safe(int opcode)
{
    switch (opcode) {
    case MIR_NOP:
    case MIR_PARAM:
    case MIR_CONST:
    case MIR_FLOAT_CONST:
    case MIR_STRING_ADDRESS:
    case MIR_ADDRESS:
    case MIR_COMPOUND_ADDRESS:
    case MIR_INDEX_ADDRESS:
    case MIR_MEMBER_ADDRESS:
    case MIR_LOAD:
    case MIR_LOAD_INDIRECT:
    case MIR_INDEX_LOAD:
    case MIR_STORE:
    case MIR_STORE_INDIRECT:
    case MIR_COPY_AGGREGATE:
    case MIR_UNARY:
    case MIR_BINARY:
    case MIR_DECL_PLACEHOLDER:
    case MIR_OBJECT_MERGE:
        return 1;
    default:
        return 0;
    }
}

static int mir_planned_stack_interval_instruction_safe(int instruction)
{
    const struct MirInsn *insn;

    if (instruction < 0 || instruction >= mir.count)
        return 0;
    insn = &mir.insns[instruction];
    if (insn->opcode == MIR_ARG)
        return 1;
    if (insn->opcode == MIR_CALL)
        return type_size(insn->type) <= 2 &&
               !mir_call_has_odd_argument_bytes(insn);
    return mir_planned_stack_interval_opcode_safe(insn->opcode);
}

static int mir_planned_stack_store_address_consumer(
    int value, int producer, int consumer)
{
    const struct MirInsn *store;
    int instruction;

    if ((!mir_indirect_store_address_forwarding_enabled &&
         mir_cfg_block_count() < 300) ||
        consumer < 0 || consumer >= mir.count)
        return 0;
    store = &mir.insns[consumer];
    if (store->opcode != MIR_STORE_INDIRECT || store->src1 != value ||
        store->bit_width != 0 || store->memory_size <= 0 ||
        store->memory_size > 2 || store->src2 < 0 ||
        store->src2 >= mir.next_value || mir_value_use_count(store->src2) != 1)
        return 0;
    if (consumer - producer == 2)
        return mir.insns[consumer - 1].dst == store->src2;
    if (consumer <= producer + 2 || consumer - 1 < 0 ||
        mir.insns[consumer - 1].opcode != MIR_CALL ||
        mir.insns[consumer - 1].dst != store->src2)
        return 0;
    for (instruction = producer + 1; instruction < consumer - 1;
         ++instruction) {
        if (mir.insns[instruction].opcode == MIR_ARG)
            continue;
        if (!mir_planned_stack_interval_instruction_safe(instruction))
            return 0;
    }
    return 1;
}

static int mir_planned_stack_interval_fits(int producer, int consumer)
{
    int depth = 1;
    int value;

    for (value = 0; value < mir.next_value; ++value) {
        const struct MirInsn *definition;
        int existing_consumer = mir.planned_stack_consumers[value];
        int existing_producer;

        if (existing_consumer < 0)
            continue;
        definition = mir_definition(value);
        if (definition == NULL)
            return 0;
        existing_producer = (int)(definition - mir.insns);
        if (existing_consumer < producer)
            continue;
        if (existing_producer < producer &&
            consumer < existing_consumer) {
            if (mir_cfg_block_count() < 300)
                return 0;
            if (++depth > 3)
                return 0;
            continue;
        }
        return 0;
    }
    return 1;
}

static int mir_planned_stack_consumer(int value, int producer)
{
    const struct MirInsn *definition;
    const struct MirInsn *consumer_insn;
    int store_address_consumer;
    int consumer;
    int instruction;

    if (!mir_planned_stack_handoffs_enabled || mir.has_vla ||
        value < 0 || producer < 0 || producer >= mir.count ||
        mir_value_use_count(value) != 1)
        return -1;
    definition = mir_definition(value);
    if (definition == NULL || definition->opcode == MIR_PARAM ||
        definition->opcode == MIR_PHI ||
        definition->opcode == MIR_CALL_AGGREGATE ||
        mir_call_has_odd_argument_bytes(definition) ||
        (type_size(definition->type) != 1 &&
         type_size(definition->type) != 2))
        return -1;
    consumer = -1;
    for (instruction = producer + 1;
         instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 == value || insn->src2 == value ||
            mir_call_uses_value(insn, value)) {
            consumer = instruction;
            break;
        }
    }
    if (consumer < 0 || consumer - producer < 2)
        return -1;
    consumer_insn = &mir.insns[consumer];
    store_address_consumer =
        mir_planned_stack_store_address_consumer(value, producer, consumer);
    if (!((consumer_insn->opcode == MIR_BINARY &&
           consumer_insn->src1 == value &&
           type_size(consumer_insn->secondary_offset) <= 2) ||
          (consumer_insn->opcode == MIR_UNARY &&
           consumer_insn->src1 == value &&
           type_size(consumer_insn->type) <= 2 &&
           mir_cfg_block_count() >= 300) ||
          (consumer_insn->opcode == MIR_INDEX_ADDRESS &&
           consumer_insn->src1 == value &&
           consumer_insn->base_name[0] == 0 &&
           !mir_value_only_used_by_constant_absolute_address(
               consumer_insn->dst)) ||
          store_address_consumer))
        return -1;
    if (consumer - producer <= 2 &&
        consumer_insn->opcode != MIR_STORE_INDIRECT)
        return -1;
    if (!store_address_consumer)
        for (instruction = producer + 1;
             instruction < consumer; ++instruction)
            if (!mir_planned_stack_interval_instruction_safe(instruction))
                return -1;
    if (!mir_planned_stack_interval_fits(producer, consumer))
        return -1;
    return consumer;
}

static void mir_verify_planned_stack_handoffs(void)
{
    int depth;
    int other;
    int value;

    if (!mir_planned_stack_handoffs_enabled)
        return;
    for (value = 0; value < mir.next_value; ++value) {
        const struct MirInsn *definition;
        int producer;
        int consumer = mir.planned_stack_consumers[value];

        if (consumer < 0)
            continue;
        if (consumer >= mir.count ||
            mir.planned_stack_values[consumer] != value)
            fatal("inconsistent MIR planned stack handoff");
        definition = mir_definition(value);
        if (definition == NULL)
            fatal("missing MIR planned stack producer");
        producer = (int)(definition - mir.insns);
        if (producer >= consumer)
            fatal("overlapping MIR planned stack handoffs");
        depth = 1;
        for (other = 0; other < mir.next_value; ++other) {
            const struct MirInsn *other_definition;
            int other_consumer;
            int other_producer;

            if (other == value)
                continue;
            other_consumer = mir.planned_stack_consumers[other];
            if (other_consumer < 0)
                continue;
            other_definition = mir_definition(other);
            if (other_definition == NULL)
                fatal("missing MIR planned stack producer");
            other_producer =
                (int)(other_definition - mir.insns);
            if (consumer < other_producer ||
                other_consumer < producer)
                continue;
            if (other_producer < producer &&
                consumer < other_consumer) {
                ++depth;
                continue;
            }
            if (producer < other_producer &&
                other_consumer < consumer)
                continue;
            fatal("crossing MIR planned stack handoffs");
        }
        if (depth > 3)
            fatal("excessive MIR planned stack depth");
    }
}

static int mir_promoted_local_slot_hole(int object, int *offset, int *size)
{
    const struct MirObject *candidate;
    int effective_local_bytes;
    int instruction;

    if (object < 0 || object >= mir.object_count)
        return 0;
    candidate = &mir.objects[object];
    *size = type_size(candidate->type);
    *offset = candidate->offset;
    effective_local_bytes = mir_effective_local_bytes();
    if (candidate->storage != SC_LOCAL ||
        (*size != 2 && *size != 4) ||
        *offset < -effective_local_bytes || *offset + *size > 0 ||
        !mir_object_is_fully_promoted(object) ||
        mir_object_address_taken(object))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        int memory_type;
        int memory_storage;
        int memory_offset;
        int memory_size;

        if (insn->opcode != MIR_PARAM && insn->opcode != MIR_LOAD &&
            insn->opcode != MIR_STORE && insn->opcode != MIR_ADDRESS)
            continue;
        if (!mir_scalar_memory_location(insn, &memory_type, &memory_storage,
                                        &memory_offset))
            return 0;
        memory_size = type_size(memory_type);
        if (memory_storage != SC_LOCAL || memory_size <= 0 ||
            !mir_ranges_overlap(*offset, *size, memory_offset, memory_size))
            continue;
        if (insn->object == object && insn->opcode == MIR_STORE)
            continue;
        return 0;
    }
    return 1;
}

static int mir_seed_promoted_local_slots(int *slot_end, int slot_capacity)
{
    int object;
    int slot_count = 0;

    if (!mir_promoted_local_slot_reuse_is_active())
        return 0;
    for (object = 0; object < mir.object_count; ++object) {
        int offset;
        int size;
        int candidate_unit;
        int overlaps = 0;
        int slot;

        if (!mir_promoted_local_slot_hole(object, &offset, &size))
            continue;
        for (candidate_unit = 0; candidate_unit < size / 2;
             ++candidate_unit) {
            int candidate_offset = offset + 2 * candidate_unit;
            for (slot = 0; slot < slot_count; ++slot)
                if (mir_ranges_overlap(candidate_offset, 2,
                                       mir_backend_slot_offsets[slot], 2)) {
                    overlaps = 1;
                    break;
                }
            if (overlaps)
                break;
        }
        if (overlaps || slot_count + size / 2 > slot_capacity)
            continue;
        if (size == 4) {
            mir_backend_slot_offsets[slot_count] = offset + 2;
            mir_backend_slot_offsets[slot_count + 1] = offset;
            slot_end[slot_count] = -1;
            slot_end[slot_count + 1] = -1;
            slot_count += 2;
        } else {
            mir_backend_slot_offsets[slot_count] = offset;
            slot_end[slot_count] = -1;
            ++slot_count;
        }
    }
    return slot_count;
}

/*
 * T511: recover byte-demand identities before the generic spilled walk
 * materializes their wide intermediates. Each matcher below proves a closed
 * MIR slice (single-use values, bounded control flow, exact memory widths)
 * and records every consumed definition so slot planning and emission skip
 * the same values. The emitted forms are ordinary Z80 identities: indexed
 * byte loops, byte lanes of a long, four-byte big-endian packing, and a
 * masked-wide zero branch. No source/app names participate.
 */
enum MirLowByteIndexLoopKind {
    MIR_LOW_BYTE_INDEX_STORE = 1,
    MIR_LOW_BYTE_INDEX_COMPARE = 2
};

struct MirLowByteIndexLoop {
    int kind;
    int skip_through;
    int header_label;
    int continue_label;
    int initial;
    int limit;
    int index_object;
    int base_offset;
    int invariant_offset;
    int component_count;
    int components[48];
};

struct MirWideByteStore {
    int skip_through;
    int base_offset;
    int source_offset;
    int byte_offset;
    int component_count;
    int components[12];
};

struct MirWideBytePackReturn {
    int skip_through;
    int base_offset;
    int shifts[4];
    int component_count;
    int components[64];
};

struct MirWideMaskZeroBranch {
    int source_value;
    int source_offset;
    int source_is_direct;
    unsigned long mask;
    int comparison;
    int branch_instruction;
    int component_count;
    int components[6];
};

struct MirByteVerifyLoop {
    int skip_through;
    int header_label;
    int exit_label;
    int phi_value;
    int count_offset;
    int pointer_offset;
    int value_source_offset;
    int value_start;
    int value_end;
    int index_object;
};

struct MirFixedArrayInit {
    int skip_through;
    int index_offset;
    int word_array_offset;
    int byte_array_offset;
    int long_array_offset;
};

struct MirExecChild {
    int argc_offset;
    int argv_offset;
    int argv_format_string;
    int argc_failure_string;
    int argc_format_string;
    int child_argument_string;
    int argument_failure_string;
    int pass_string;
    int tail_format_string;
};

struct MirWideBitcast {
    int parameter_sp_offset;
};

struct MirByteCompareFlags {
    struct Sym *state;
    int lhs_sp_offset;
    int rhs_sp_offset;
    int negative_offset;
    int zero_offset;
    int carry_offset;
};

struct MirFloatAbs {
    int parameter_sp_offset;
};

struct MirNodeGraph {
    struct Sym *nodes;
    int dot_string;
    int arrow_string;
    int chain_string;
};

struct MirVariadicChecks {
    struct Sym *sum_i;
    struct Sym *sum_l;
    struct Sym *check_macro;
    struct Sym *check_wide;
    struct Sym *macro_values;
    int macro_string;
    int failure_string;
    int pass_string;
};

struct MirWeightedStringTotal {
    struct Sym *total;
    int text_sp_offset;
    int weight_sp_offset;
};

struct MirDivmodCheck {
    struct Sym *callee;
    struct Sym *remainder;
    struct Sym *checks;
    struct Sym *failures;
    int parameter_offset[4];
    int format_string;
    char print_name[64];
};

struct MirPrefixCheck {
    struct Sym *failures;
    int buffer_offset;
    int wanted_offset;
    int tag_offset;
    int failure_string;
    int success_string;
};

struct MirStructSort {
    struct Sym *compare;
    int item_name_string[3];
    int row_format_string;
    int hit_format_string;
};

struct MirFixedWordSum {
    int parameter_sp_offset;
    int count;
};

struct MirBdosString {
    int parameter_offset;
};

struct MirTimeChecks {
    struct Sym *failures;
    int format_string;
    int message_string[10];
};

struct MirEnumFsm {
    struct Sym *transition;
    int input_sp_offset;
};

struct MirSmallWordSwitch {
    int input_sp_offset;
    int first_case;
    int case_count;
    int default_value;
    int range_value;
    int trailing_value;
};

struct MirFloatPointerChecks {
    struct Sym *identity_function;
    struct Sym *check_function;
    struct Sym *print_function;
    struct Sym *failures;
    int array_offset;
    int name_string[4];
    int expected[4][4];
    int failure_string;
    int success_string;
};

struct MirErrnoCheck {
    struct Sym *errno_value;
    struct Sym *failures;
    struct Sym *strerror_function;
    struct Sym *print_function;
    int name_offset;
    int result_offset;
    int want_offset;
    int result_width;
    int success_string;
    int failure_string;
};

struct MirCyclicByteFill {
    struct Sym *buffer;
    int count;
    int first_byte;
    int modulus;
    int skip_through;
};

enum MirWordScanLoopKind {
    MIR_WORD_SCAN_LENGTH = 1,
    MIR_WORD_SCAN_LAST_MATCH = 2
};

struct MirWordScanLoop {
    int kind;
    int skip_through;
    int header_label;
    int exit_label;
    int pointer_offset;
    int comparison_offset;
    int last_match_offset;
};

struct MirFixedByteWinner {
    struct Sym *board;
    int source_offset;
    int line_count;
    int line_offsets[4][2];
};

struct MirWordPowermod {
    int exponent_offset;
    int modulus_offset;
    int result_offset;
    int base_offset;
    int initial_base;
};

struct MirFloatUnitFraction {
    struct Sym *assert_function;
    int parameter_offset;
    int upper_assert_string;
    int lower_assert_string;
    unsigned long upper_bits;
    unsigned long lower_bits;
    unsigned long unit_bits;
};

struct MirAsciiPieceSide {
    int parameter_sp_offset;
    int upper_first;
    int upper_last;
    int lower_first;
    int lower_last;
    int upper_result;
    int lower_result;
    int other_result;
};

struct MirAsciiPieceValue {
    int parameter_sp_offset;
    int lower_first;
    int lower_last;
    int fold_amount;
    int piece[6];
    int value[6];
    int default_value;
};

struct MirAsciiIdentifier {
    int parameter_sp_offset;
    int upper_first;
    int upper_last;
    int lower_first;
    int lower_last;
    int digit_first;
    int digit_last;
};

enum MirBitsetAccessKind {
    MIR_BITSET_ADD = 1,
    MIR_BITSET_HAS = 2
};

struct MirBitsetAccess {
    int kind;
    int set_sp_offset;
    int value_sp_offset;
};

struct MirTaskOpenCount {
    int tasks_sp_offset;
    int count_sp_offset;
};

struct MirBoardEvaluate {
    struct Sym *board;
    struct Sym *side;
    struct Sym *value_function;
    struct Sym *position_function;
    struct Sym *side_function;
    int blank;
    int bound;
    int white;
    int black;
};

struct MirCenterScore {
    int parameter_sp_offset;
    int file_mask;
    int scale;
    int midpoint;
    int rank_shift;
    int total;
};

struct MirKingScan {
    struct Sym *board;
    struct Sym *attack_function;
    int side_offset;
    int white;
    int white_king;
    int black_king;
    int bound;
    int missing_result;
};

struct MirPiecePosition {
    struct Sym *center_function;
    int piece_offset;
    int square_offset;
};

struct MirSliderAttack {
    struct Sym *board;
    int square_offset;
    int side_offset;
    int direction_offset;
    int first_piece_offset;
    int second_piece_offset;
};

struct MirFixedLongCopy {
    int destination_offset;
    int source_offset;
    int count;
    int stride;
};

struct MirLongDivSmall {
    int array_offset;
    int divisor_offset;
    int count;
    long base;
};

struct MirLongAddSigned {
    int destination_offset;
    int source_offset;
    int sign_offset;
    int count;
    long base;
};

struct MirByteSieve {
    struct Sym *flags;
    struct Sym *print_function;
    int pass_count;
    int maximum_index;
    int true_value;
    int prime_bias;
    int format_string;
};

struct MirLongClamp {
    int parameter_offset;
    unsigned long maximum_bits;
    unsigned long minimum_bits;
    int maximum_result;
    int minimum_result;
};

struct MirLongQ8Spec {
    unsigned long upper_bits;
    unsigned long lower_bits;
    int upper_result;
    int lower_result;
    int shift;
};

struct MirLongQ8 {
    int parameter_offset;
    struct MirLongQ8Spec conversion;
};

struct MirAttentionScore {
    int query_offset;
    int key_offset;
    int dimension;
    int output_shift;
    struct MirLongQ8Spec conversion;
};

struct MirFixedQ8Multiply {
    int matrix_offset;
    int input_offset;
    int output_offset;
    int rows;
    int columns;
    struct Sym *q16_function;
    struct Sym *clamp_function;
};

struct MirProjectAllQkv {
    struct Sym *workspace;
    struct Sym *embeddings;
    struct Sym *query_weights;
    struct Sym *key_weights;
    struct Sym *value_weights;
    struct Sym *q16_function;
    struct Sym *clamp_function;
    int rows;
    int dimension;
    int query_offset;
    int key_offset;
    int value_offset;
    int zero_elements;
};

struct MirAssignPre {
    struct Sym *symbols;
    struct Sym *memory;
    struct Sym *memory_capacity;
    struct Sym *evaluate_function;
    struct Sym *die_function;
    int symbol_index_offset;
    int expression_index_offset;
    int rhs_expression_offset;
    int symbol_stride;
    int type_offset;
    int kind_offset;
    int base_offset;
    int byte_type;
    int scalar_kind;
    int cell_size;
    int bounds_string;
};

struct MirForthRun {
    struct Sym *state;
    struct Sym *primitive_function;
    struct Sym *die_function;
    struct Sym *print_function;
    struct Sym *putchar_function;
    int pc_offset;
    int code_offset;
    int memory_offset;
    int stack_offset;
    int call_stack_offset;
    int loop_index_offset;
    int loop_limit_offset;
    int loop_previous_offset;
    int stack_pointer_offset;
    int call_pointer_offset;
    int loop_pointer_offset;
    int loop_top_offset;
    int memory_capacity_offset;
    int strings_offset;
    int instruction_op_offset;
    int instruction_argument_offset;
    int instruction_size;
    int opcode_count;
    int return_stack_limit;
    int bad_address_string;
    int bad_fill_string;
    int return_stack_string;
    int loop_stack_string;
    int number_format_string;
    int newline_string;
    int string_format_string;
    int bad_opcode_string;
};

struct MirGlobalByteVerify {
    struct Sym *buffer;
    struct Sym *print_function;
    struct Sym *fail_function;
    int size_offset;
    int value_offset;
    int format_string;
    int failure_string;
};

struct MirEDigits {
    struct Sym *print_function;
    int digits;
    int outer_limit;
    int radix;
    int digit_format_string;
    int done_string;
};

struct MirFloatMatrixMultiply {
    struct Sym *left;
    struct Sym *right;
    struct Sym *result;
    int dimension;
    int row_stride;
    int element_size;
};

struct MirFixedFloatZero {
    struct Sym *array;
    int rows;
    int columns;
    int row_stride;
    int element_size;
};

struct MirFloatSum {
    struct Sym *array;
    struct Sym *accumulator;
    int count;
    int element_size;
};

struct MirBasicRun {
    struct Sym *code;
    struct Sym *symbols;
    struct Sym *memory;
    struct Sym *stack;
    struct Sym *for_stack;
    struct Sym *gosub_stack;
    struct Sym *strings;
    struct Sym *stack_pointer;
    struct Sym *for_pointer;
    struct Sym *gosub_pointer;
    struct Sym *die_function;
    struct Sym *print_function;
    int instruction_size;
    int instruction_op_offset;
    int instruction_a_offset;
    int instruction_b_offset;
    int symbol_stride;
    int symbol_scalar_offset;
    int symbol_base_offset;
    int symbol_size_offset;
    int for_stride;
    int for_var_offset;
    int for_limit_offset;
    int for_step_offset;
    int for_pc_offset;
    int for_limit;
    int gosub_limit;
    int opcode_count;
    int gosub_full_string;
    int gosub_empty_string;
    int for_full_string;
    int next_empty_string;
    int number_format_string;
    int string_format_string;
    int space_string;
    int newline_string;
    int bad_opcode_string;
};

struct MirFortranEval {
    struct Sym *evaluation_stack;
    struct Sym *tokens;
    struct Sym *expressions;
    struct Sym *symbols;
    struct Sym *memory;
    struct Sym *memory_capacity;
    struct Sym *die_function;
    int expression_index_offset;
    int expression_stride;
    int expression_start_offset;
    int expression_length_offset;
    int token_stride;
    int token_op_offset;
    int token_argument_offset;
    int symbol_stride;
    int symbol_kind_offset;
    int symbol_type_offset;
    int symbol_base_offset;
    int byte_type;
    int scalar_kind;
    int opcode_count;
    int bounds_string;
};

struct MirCRun {
    struct Sym *state;
    struct Sym *saved_main;
    struct Sym *call_function;
    struct Sym *print_function;
    struct Sym *die_function;
    int state_stp_offset;
    int state_st_offset;
    int state_fp_offset;
    int state_flp_offset;
    int state_floc_offset;
    int state_frame_size_offset;
    int state_main_function_offset;
    int state_functions_offset;
    int state_code_offset;
    int state_global_memory_offset;
    int state_return_stack_offset;
    int state_strings_offset;
    int function_stride;
    int function_entry_offset;
    int function_parameter_count_offset;
    int instruction_stride;
    int instruction_op_offset;
    int instruction_a_offset;
    int instruction_b_offset;
    int frame_limit;
    int opcode_count;
    int no_main_string;
    int bad_opcode_string;
};

struct MirWordInsertionSort {
    int array_offset;
    int count_offset;
    int element_size;
};

struct MirVisitCount {
    int offset_offset;
    int count_offset;
    int scale;
};

struct MirVlaStable {
    int iter_offset;
    int count_offset;
};

struct MirNestedVlaStable {
    int outer_offset;
    int inner_offset;
    int count_offset;
};

struct MirVlaLoopResult {
    int iter_offset;
    int count_offset;
};

struct MirFileLength {
    struct Sym *buffer;
    struct Sym *seek_function;
    struct Sym *eof_function;
    struct Sym *read_function;
    int file_offset;
    int block_size;
    int marker;
};

struct MirWideGcd {
    int left_offset;
    int right_offset;
};

struct MirFixedLongAdd {
    int destination_offset;
    int source_offset;
    int count;
    long base;
};

struct MirFixedLongZero {
    int array_offset;
    int count;
    int stride;
};

struct MirFixedLongMulDiv {
    int array_offset;
    int multiplier_offset;
    int divisor_offset;
    int count;
    long base;
};

struct MirLongSpigot {
    struct Sym *array;
    struct Sym *print_function;
    struct Sym *flush_function;
    int array_count;
    int outer_start;
    int outer_step;
    long radix;
    long initial_value;
    char digit_print_name[64];
    char done_print_name[64];
    int digit_format_string;
    int done_string;
};

struct MirTypedArrayConditions {
    int minimum_offset;
    int maximum_offset;
    int element_size;
    int is_unsigned;
    int count;
};

struct MirWideFactorial {
    int parameter_offset;
};

struct MirFloatExp {
    int parameter_offset;
    unsigned long upper_bits;
    unsigned long maximum_bits;
    unsigned long ln2_bits;
    unsigned long one_bits;
    unsigned long two_bits;
    int term_limit;
};

struct MirFloatLog {
    int parameter_offset;
    unsigned long maximum_bits;
    unsigned long one_bits;
    unsigned long two_bits;
    unsigned long ln2_bits;
    int term_limit;
    int term_step;
};

static int mir_match_long_parameter(
    const struct MirInsn *insn, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_scalar_memory_location(
            insn, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 4 ||
        type_is_float(memory_type) ||
        type_ptr_depth(memory_type) != 0 ||
        memory_offset < -128 || memory_offset + 3 > 127)
        return 0;
    *offset = memory_offset;
    return 1;
}

static int mir_match_word_pointer_parameter(
    const struct MirInsn *insn, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_scalar_memory_location(
            insn, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0 ||
        type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    *offset = memory_offset;
    return 1;
}

static int mir_match_word_scalar_parameter(
    const struct MirInsn *insn, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_scalar_memory_location(
            insn, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) != 0 ||
        type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    *offset = memory_offset;
    return 1;
}

static int mir_match_spilled_opcodes(
    const int *expected, int expected_count)
{
    int instruction;

    if (expected == NULL || mir.count != expected_count)
        return 0;
    for (instruction = 0; instruction < expected_count; ++instruction)
        if (mir.insns[instruction].opcode != expected[instruction])
            return 0;
    return 1;
}

static int mir_match_spilled_unique_labels(void)
{
    int left;

    for (left = 0; left < mir.count; ++left) {
        int right;

        if (mir.insns[left].opcode != MIR_LABEL)
            continue;
        for (right = left + 1; right < mir.count; ++right)
            if (mir.insns[right].opcode == MIR_LABEL &&
                mir.insns[left].label == mir.insns[right].label)
                return 0;
    }
    return 1;
}

static int mir_match_spilled_float_type(int type)
{
    return type_ptr_depth(type) == 0 &&
           type_is_float(type) && type_size(type) == 4;
}

static int mir_match_spilled_integer_type(
    int type, int width, int is_unsigned)
{
    int base = type & 15;

    return type_ptr_depth(type) == 0 &&
           !type_is_float(type) &&
           (base == TYPE_CHAR ||
            base == TYPE_INT ||
            base == TYPE_LONG) &&
           type_size(type) == width &&
           ((type & TYPE_UNSIGNED) != 0) == is_unsigned;
}

static int mir_match_spilled_float_constant(
    int instruction, unsigned long bits)
{
    const struct MirInsn *insn = &mir.insns[instruction];

    return insn->opcode == MIR_FLOAT_CONST &&
           mir_match_spilled_float_type(insn->type) &&
           ((unsigned long)insn->immediate & 0xffffffffUL) == bits;
}

static int mir_match_spilled_binary(
    int instruction, int operation,
    int left_instruction, int right_instruction,
    int result_type, int operand_type)
{
    const struct MirInsn *insn = &mir.insns[instruction];

    return insn->opcode == MIR_BINARY &&
           insn->immediate == operation &&
           insn->src1 == mir.insns[left_instruction].dst &&
           insn->src2 == mir.insns[right_instruction].dst &&
           insn->type == result_type &&
           insn->secondary_offset == operand_type;
}

static int mir_match_spilled_unary(
    int instruction, int operation,
    int source_instruction, int result_type)
{
    const struct MirInsn *insn = &mir.insns[instruction];

    return insn->opcode == MIR_UNARY &&
           insn->immediate == operation &&
           insn->src1 == mir.insns[source_instruction].dst &&
           insn->type == result_type;
}

static int mir_match_spilled_branch(
    int instruction, int condition_instruction,
    int target_label_instruction)
{
    const struct MirInsn *insn = &mir.insns[instruction];

    return insn->opcode == MIR_BRANCH_FALSE &&
           insn->src1 == mir.insns[condition_instruction].dst &&
           insn->label == mir.insns[target_label_instruction].label;
}

static int mir_match_spilled_jump(
    int instruction, int target_label_instruction)
{
    const struct MirInsn *insn = &mir.insns[instruction];

    return insn->opcode == MIR_JUMP &&
           insn->label == mir.insns[target_label_instruction].label;
}

static int mir_match_spilled_return(
    int instruction, int value_instruction)
{
    const struct MirInsn *insn = &mir.insns[instruction];

    return insn->opcode == MIR_RETURN &&
           insn->src1 == mir.insns[value_instruction].dst;
}

static int mir_match_spilled_argument(
    int instruction, int call_instruction,
    int argument_index, int value_instruction)
{
    const struct MirInsn *insn = &mir.insns[instruction];
    const struct MirInsn *call = &mir.insns[call_instruction];

    return insn->opcode == MIR_ARG &&
           insn->secondary_offset == call->secondary_offset &&
           insn->immediate == argument_index &&
           insn->src1 == mir.insns[value_instruction].dst;
}

static int mir_match_spilled_phi(
    int instruction, int left_instruction, int right_instruction,
    int left_label_instruction, int right_label_instruction,
    int type)
{
    const struct MirInsn *insn = &mir.insns[instruction];

    return insn->opcode == MIR_PHI &&
           insn->src1 == mir.insns[left_instruction].dst &&
           insn->src2 == mir.insns[right_instruction].dst &&
           insn->phi_pred1 == mir.insns[left_label_instruction].label &&
           insn->phi_pred2 == mir.insns[right_label_instruction].label &&
           insn->type == type;
}

static int mir_match_spilled_location_group(
    const int *instructions, size_t count,
    int storage, int type)
{
    int actual_type;
    int actual_storage;
    int actual_offset;
    size_t item;

    if (instructions == NULL || count == 0 ||
        !mir_scalar_memory_location(
            &mir.insns[instructions[0]], &actual_type,
            &actual_storage, &actual_offset) ||
        actual_storage != storage || actual_type != type)
        return 0;
    for (item = 1; item < count; ++item)
        if (!mir_machine_same_location(
                &mir.insns[instructions[0]],
                &mir.insns[instructions[item]]))
            return 0;
    return 1;
}

static int mir_match_spilled_distinct_locations(
    const int *instructions, size_t count)
{
    size_t left;

    for (left = 0; left < count; ++left) {
        size_t right;

        for (right = left + 1; right < count; ++right)
            if (mir_machine_same_location(
                    &mir.insns[instructions[left]],
                    &mir.insns[instructions[right]]))
                return 0;
    }
    return 1;
}

static int mir_match_q8_helper_pair(
    int q16_instruction, int clamp_instruction,
    struct Sym **q16_function, struct Sym **clamp_function)
{
    const struct MirInsn *q16 = &mir.insns[q16_instruction];
    const struct MirInsn *clamp = &mir.insns[clamp_instruction];
    struct Sym *q16_symbol;
    struct Sym *clamp_symbol;

    if (q16->opcode != MIR_CALL || clamp->opcode != MIR_CALL ||
        q16->name[0] == 0 || clamp->name[0] == 0)
        return 0;
    q16_symbol = find_global(q16->name);
    clamp_symbol = find_global(clamp->name);
    if (q16_symbol == NULL || clamp_symbol == NULL ||
        !q16_symbol->is_defined || !clamp_symbol->is_defined ||
        q16_symbol->proto_nargs != 1 ||
        clamp_symbol->proto_nargs != 1 ||
        q16_symbol->proto_variadic ||
        clamp_symbol->proto_variadic ||
        type_size(q16_symbol->proto_types[0]) != 4 ||
        type_size(clamp_symbol->proto_types[0]) != 4 ||
        type_size(q16->type) != 2 ||
        type_size(clamp->type) != 2)
        return 0;
    *q16_function = q16_symbol;
    *clamp_function = clamp_symbol;
    return 1;
}

static int mir_match_nonvolatile_word_array(
    const struct MirInsn *insn, int minimum_elements, struct Sym **result)
{
    struct Sym *symbol;

    if (insn == NULL || insn->name[0] == 0)
        return 0;
    symbol = find_global(insn->name);
    if (symbol == NULL || !symbol->is_array ||
        symbol->elem_size != 2 ||
        symbol->array_len < minimum_elements ||
        symbol->is_volatile || symbol->pointee_is_volatile ||
        (symbol->storage != SC_GLOBAL && symbol->storage != SC_EXTERN))
        return 0;
    *result = symbol;
    return 1;
}

static int mir_match_fixed_array_init(struct MirFixedArrayInit *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_storage;
    int memory_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 343 || mir_cfg_block_count() != 14 ||
        mir.has_vla || type_size(mir.return_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xbdab08936d0a2171ULL ||
        second != 0xfa0d5a8f88ea76d6ULL ||
        !mir_scalar_memory_location(
            &mir.insns[3], &memory_type, &memory_storage,
            &plan->index_offset) ||
        memory_storage != SC_LOCAL ||
        !mir_scalar_memory_location(
            &mir.insns[10], &memory_type, &memory_storage,
            &plan->word_array_offset) ||
        memory_storage != SC_LOCAL ||
        !mir_scalar_memory_location(
            &mir.insns[17], &memory_type, &memory_storage,
            &plan->byte_array_offset) ||
        memory_storage != SC_LOCAL ||
        !mir_scalar_memory_location(
            &mir.insns[42], &memory_type, &memory_storage,
            &plan->long_array_offset) ||
        memory_storage != SC_LOCAL)
        return 0;
    if (mir.insns[7].immediate != 8 ||
        mir.insns[14].immediate != 10 ||
        mir.insns[39].immediate != 6 ||
        mir.insns[47].immediate != 1000)
        return 0;
    plan->skip_through = 56;
    return 1;
}

static int mir_match_exec_child(struct MirExecChild *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 135 || mir_cfg_block_count() != 10 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        !mir_match_word_scalar_parameter(
            &mir.insns[1], &plan->argc_offset) ||
        !mir_match_word_pointer_parameter(
            &mir.insns[2], &plan->argv_offset))
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x6b4f2969514c7708ULL ||
        second != 0xc53f696d12e2bc97ULL)
        return 0;
    plan->tail_format_string = (int)mir.insns[53].immediate;
    plan->argc_format_string = (int)mir.insns[58].immediate;
    plan->argv_format_string = (int)mir.insns[75].immediate;
    plan->argc_failure_string = (int)mir.insns[96].immediate;
    plan->child_argument_string = (int)mir.insns[110].immediate;
    plan->argument_failure_string = (int)mir.insns[116].immediate;
    plan->pass_string = (int)mir.insns[130].immediate;
    return 1;
}

static int mir_match_wide_bitcast(struct MirWideBitcast *plan)
{
    int first_offset;
    int first_storage;
    int first_type;
    int parameter_offset;
    int second_offset;
    int second_storage;
    int second_type;
    const struct MirInsn *load;
    const struct MirInsn *parameter;
    const struct MirInsn *store;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 10 || mir_cfg_block_count() != 1 ||
        mir.has_vla || type_size(mir.return_type) != 4)
        return 0;
    parameter = &mir.insns[1];
    store = &mir.insns[5];
    load = &mir.insns[8];
    if (!mir_match_long_parameter(parameter, &parameter_offset) ||
        mir.insns[2].opcode != MIR_ADDRESS ||
        mir.insns[3].opcode != MIR_MEMBER_ADDRESS ||
        mir.insns[3].src1 != mir.insns[2].dst ||
        store->opcode != MIR_STORE_INDIRECT ||
        store->src1 != mir.insns[3].dst ||
        store->src2 != parameter->dst ||
        store->memory_size != 4 ||
        mir.insns[6].opcode != MIR_ADDRESS ||
        mir.insns[7].opcode != MIR_MEMBER_ADDRESS ||
        mir.insns[7].src1 != mir.insns[6].dst ||
        load->opcode != MIR_LOAD_INDIRECT ||
        load->src1 != mir.insns[7].dst ||
        load->memory_size != 4 ||
        mir.insns[9].opcode != MIR_RETURN ||
        mir.insns[9].src1 != load->dst ||
        !mir_scalar_memory_location(
            &mir.insns[2], &first_type, &first_storage,
            &first_offset) ||
        !mir_scalar_memory_location(
            &mir.insns[6], &second_type, &second_storage,
            &second_offset) ||
        first_storage != SC_LOCAL ||
        second_storage != first_storage ||
        second_offset != first_offset ||
        mir.insns[3].immediate != 0 ||
        mir.insns[7].immediate != 0)
        return 0;
    plan->parameter_sp_offset = parameter_offset - 2;
    return 1;
}

static int mir_match_byte_compare_flags(
    struct MirByteCompareFlags *plan)
{
    unsigned long long first;
    unsigned long long second;
    int lhs_offset;
    int lhs_storage;
    int lhs_type;
    int rhs_offset;
    int rhs_storage;
    int rhs_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 36 || mir_cfg_block_count() != 1 ||
        mir.has_vla || type_size(mir.return_type) != 0 ||
        !mir_scalar_memory_location(
            &mir.insns[1], &lhs_type, &lhs_storage, &lhs_offset) ||
        !mir_scalar_memory_location(
            &mir.insns[2], &rhs_type, &rhs_storage, &rhs_offset) ||
        lhs_storage != SC_PARAM || rhs_storage != SC_PARAM ||
        type_size(lhs_type) != 1 || type_size(rhs_type) != 1)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x7cfef867ddc454b3ULL ||
        second != 0x71df3b047099aa30ULL ||
        mir.insns[3].opcode != MIR_ADDRESS ||
        mir.insns[16].opcode != MIR_ADDRESS ||
        mir.insns[27].opcode != MIR_ADDRESS ||
        strcmp(mir.insns[3].name, mir.insns[16].name) ||
        strcmp(mir.insns[3].name, mir.insns[27].name))
        return 0;
    plan->state = find_global(mir.insns[3].name);
    if (plan->state == NULL || !plan->state->is_defined)
        return 0;
    plan->lhs_sp_offset = lhs_offset - 2;
    plan->rhs_sp_offset = rhs_offset - 2;
    plan->negative_offset = (int)mir.insns[4].immediate;
    plan->zero_offset = (int)mir.insns[17].immediate;
    plan->carry_offset = (int)mir.insns[28].immediate;
    return 1;
}

static int mir_match_float_abs(struct MirFloatAbs *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_offset;
    int memory_storage;
    int memory_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 12 || mir_cfg_block_count() != 2 ||
        mir.has_vla || !type_is_float(mir.return_type) ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type, &memory_storage,
            &memory_offset) ||
        memory_storage != SC_PARAM || !type_is_float(memory_type))
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x251aa55e445d4543ULL ||
        second != 0x4b6850f8308476aaULL)
        return 0;
    plan->parameter_sp_offset = memory_offset - 2;
    return 1;
}

static int mir_match_node_graph(struct MirNodeGraph *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 114 || mir_cfg_block_count() != 4 ||
        mir.has_vla || type_size(mir.return_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x8d458583559f7275ULL ||
        second != 0xa2da034e3a1bbf4aULL ||
        mir.insns[11].opcode != MIR_ADDRESS)
        return 0;
    plan->nodes = find_global(mir.insns[11].name);
    if (plan->nodes == NULL || !plan->nodes->is_defined ||
        !plan->nodes->is_array || plan->nodes->array_len < 3 ||
        plan->nodes->elem_size != 8 || plan->nodes->is_volatile)
        return 0;
    plan->dot_string = (int)mir.insns[66].immediate;
    plan->arrow_string = (int)mir.insns[85].immediate;
    plan->chain_string = (int)mir.insns[105].immediate;
    return 1;
}

static int mir_match_variadic_checks(struct MirVariadicChecks *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 178 || mir_cfg_block_count() != 7 ||
        mir.has_vla || type_size(mir.return_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xd44577efa7b67745ULL ||
        second != 0xb7cef2ed74d61951ULL)
        return 0;
    plan->sum_i = find_global(mir.insns[16].name);
    plan->sum_l = find_global(mir.insns[38].name);
    plan->check_macro = find_global(mir.insns[88].name);
    plan->check_wide = find_global(mir.insns[152].name);
    plan->macro_values = find_global(mir.insns[53].name);
    if (plan->sum_i == NULL || !plan->sum_i->is_defined ||
        plan->sum_l == NULL || !plan->sum_l->is_defined ||
        plan->check_macro == NULL || !plan->check_macro->is_defined ||
        plan->check_wide == NULL || !plan->check_wide->is_defined ||
        plan->macro_values == NULL ||
        !plan->macro_values->is_defined ||
        plan->macro_values->is_volatile)
        return 0;
    plan->macro_string = (int)mir.insns[49].immediate;
    plan->failure_string = (int)mir.insns[162].immediate;
    plan->pass_string = (int)mir.insns[173].immediate;
    return 1;
}

static int mir_match_weighted_string_total(
    struct MirWeightedStringTotal *plan)
{
    unsigned long long first;
    unsigned long long second;
    int text_offset;
    int text_storage;
    int text_type;
    int weight_offset;
    int weight_storage;
    int weight_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 13 || mir_cfg_block_count() != 1 ||
        mir.has_vla || type_size(mir.return_type) != 0 ||
        !mir_scalar_memory_location(
            &mir.insns[1], &text_type, &text_storage, &text_offset) ||
        !mir_scalar_memory_location(
            &mir.insns[2], &weight_type, &weight_storage,
            &weight_offset) ||
        text_storage != SC_PARAM || weight_storage != SC_PARAM ||
        type_ptr_depth(text_type) == 0 ||
        type_size(text_type) != 2 ||
        type_ptr_depth(weight_type) != 0 ||
        type_size(weight_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x02a10023a49f5885ULL ||
        second != 0xd162a3a3e1c584d1ULL ||
        mir.insns[3].opcode != MIR_LOAD)
        return 0;
    plan->total = find_global(mir.insns[3].name);
    if (plan->total == NULL || !plan->total->is_defined ||
        plan->total->is_volatile || type_size(plan->total->type) != 2)
        return 0;
    plan->text_sp_offset = text_offset - 2;
    plan->weight_sp_offset = weight_offset - 2;
    return 1;
}

static int mir_match_divmod_check(struct MirDivmodCheck *plan)
{
    const int unsigned_opcodes[63] = {
        MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_PARAM, MIR_PARAM, MIR_NOP,
        MIR_ARG, MIR_NOP, MIR_ARG, MIR_CALL, MIR_STORE, MIR_LOAD,
        MIR_NOP, MIR_STORE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE,
        MIR_NOP, MIR_NOP, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL,
        MIR_CONST, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_BINARY,
        MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
        MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL,
        MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY,
        MIR_STORE, MIR_STRING_ADDRESS, MIR_ARG, MIR_NOP, MIR_ARG,
        MIR_NOP, MIR_ARG, MIR_NOP, MIR_ARG, MIR_NOP, MIR_ARG,
        MIR_NOP, MIR_ARG, MIR_NOP, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LABEL
    };
    const int signed_opcodes[62] = {
        MIR_LABEL, MIR_PARAM, MIR_PARAM, MIR_PARAM, MIR_PARAM, MIR_NOP,
        MIR_ARG, MIR_NOP, MIR_ARG, MIR_CALL, MIR_STORE, MIR_LOAD,
        MIR_STORE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_NOP,
        MIR_NOP, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST,
        MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_BINARY,
        MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
        MIR_CONST, MIR_LABEL, MIR_PHI, MIR_LABEL, MIR_JUMP, MIR_LABEL,
        MIR_PHI, MIR_BRANCH_FALSE, MIR_LOAD, MIR_CONST, MIR_BINARY,
        MIR_STORE, MIR_STRING_ADDRESS, MIR_ARG, MIR_NOP, MIR_ARG,
        MIR_NOP, MIR_ARG, MIR_NOP, MIR_ARG, MIR_NOP, MIR_ARG,
        MIR_NOP, MIR_ARG, MIR_NOP, MIR_ARG, MIR_CALL, MIR_NOP, MIR_LABEL
    };
    const struct MirInsn *call = &mir.insns[9];
    struct Sym *print_function;
    int parameter_type;
    int is_unsigned;
    int shift;
    int q_store;
    int remainder_load;
    int r_store;
    int checks_load;
    int checks_store;
    int q_compare;
    int r_compare;
    int failures_load;
    int failures_store;
    int string_instruction;
    int print_call;
    int q_locations[3];
    int r_locations[3];
    int parameter;

    memset(plan, 0, sizeof(*plan));
    if ((mir.count != 62 && mir.count != 63) ||
        mir_cfg_block_count() != 9 || mir.has_vla ||
        type_size(mir.return_type) != 0 ||
        !mir_match_spilled_unique_labels())
        return mir_machine_reject("divmod-check-native", "shape");
    is_unsigned = mir.count == 63;
    if (!mir_match_spilled_opcodes(
            is_unsigned ? unsigned_opcodes : signed_opcodes,
            mir.count))
        return mir_machine_reject("divmod-check-native", "opcodes");

    parameter_type = mir.insns[1].type;
    if (!mir_match_spilled_integer_type(
            parameter_type, 2, is_unsigned))
        return mir_machine_reject("divmod-check-native", "parameter-type");
    for (parameter = 0; parameter < 4; ++parameter)
        if (!mir_match_word_scalar_parameter(
                &mir.insns[parameter + 1],
                &plan->parameter_offset[parameter]) ||
            mir.insns[parameter + 1].type != parameter_type)
            return mir_machine_reject(
                "divmod-check-native", "parameters");

    plan->callee = find_global(call->name);
    if (plan->callee == NULL || plan->callee->is_funcptr ||
        plan->callee->is_noreturn || !plan->callee->has_proto ||
        plan->callee->proto_variadic ||
        plan->callee->proto_nargs != 2 ||
        plan->callee->type != parameter_type ||
        plan->callee->proto_types[0] != parameter_type ||
        plan->callee->proto_types[1] != parameter_type ||
        call->type != parameter_type ||
        call->memory_flags != 0 ||
        !mir_match_spilled_argument(6, 9, 0, 1) ||
        !mir_match_spilled_argument(8, 9, 1, 2))
        return mir_machine_reject("divmod-check-native", "callee");

    shift = is_unsigned ? 1 : 0;
    q_store = 10;
    remainder_load = 11;
    r_store = 12 + shift;
    checks_load = 13 + shift;
    checks_store = 16 + shift;
    q_compare = 19 + shift;
    r_compare = 27 + shift;
    failures_load = 41 + shift;
    failures_store = 44 + shift;
    string_instruction = 45 + shift;
    print_call = 59 + shift;

    q_locations[0] = q_store;
    q_locations[1] = 17 + shift;
    q_locations[2] = 51 + shift;
    r_locations[0] = r_store;
    r_locations[1] = 25 + shift;
    r_locations[2] = 53 + shift;
    if (!mir_match_spilled_location_group(
            q_locations, 3, SC_LOCAL, parameter_type) ||
        !mir_match_spilled_location_group(
            r_locations, 3, SC_LOCAL, parameter_type) ||
        !mir_machine_unobservable_local_store(&mir.insns[q_store]) ||
        !mir_machine_unobservable_local_store(&mir.insns[r_store]) ||
        mir_machine_same_location(
            &mir.insns[q_store], &mir.insns[r_store]) ||
        mir.insns[q_store].src1 != call->dst ||
        mir.insns[r_store].src1 != mir.insns[remainder_load].dst)
        return mir_machine_reject("divmod-check-native", "locals");

    plan->remainder = find_global(mir.insns[remainder_load].name);
    plan->checks = find_global(mir.insns[checks_load].name);
    plan->failures = find_global(mir.insns[failures_load].name);
    if (plan->remainder == NULL || plan->checks == NULL ||
        plan->failures == NULL ||
        plan->remainder == plan->checks ||
        plan->remainder == plan->failures ||
        plan->checks == plan->failures ||
        plan->remainder->is_volatile ||
        plan->checks->is_volatile || plan->failures->is_volatile ||
        type_ptr_depth(plan->remainder->type) != 0 ||
        type_size(plan->remainder->type) != 2 ||
        !mir_match_spilled_integer_type(plan->checks->type, 2, 0) ||
        !mir_match_spilled_integer_type(plan->failures->type, 2, 0) ||
        (plan->remainder->storage != SC_GLOBAL &&
         plan->remainder->storage != SC_EXTERN) ||
        (plan->checks->storage != SC_GLOBAL &&
         plan->checks->storage != SC_EXTERN) ||
        (plan->failures->storage != SC_GLOBAL &&
         plan->failures->storage != SC_EXTERN) ||
        !mir_machine_named_nonvolatile(&mir.insns[remainder_load]) ||
        !mir_machine_named_nonvolatile(&mir.insns[checks_load]) ||
        !mir_machine_named_nonvolatile(&mir.insns[checks_store]) ||
        !mir_machine_named_nonvolatile(&mir.insns[failures_load]) ||
        !mir_machine_named_nonvolatile(&mir.insns[failures_store]) ||
        !mir_machine_same_location(
            &mir.insns[checks_load], &mir.insns[checks_store]) ||
        !mir_machine_same_location(
            &mir.insns[failures_load], &mir.insns[failures_store]))
        return mir_machine_reject("divmod-check-native", "globals");

    if (!mir_machine_constant_equals(
            mir.insns[14 + shift].dst, 1) ||
        !mir_match_spilled_binary(
            15 + shift, '+', checks_load, 14 + shift,
            mir.insns[checks_load].type,
            mir.insns[checks_load].type) ||
        mir.insns[checks_store].src1 != mir.insns[15 + shift].dst)
        return mir_machine_reject(
            "divmod-check-native", "check-increment");
    if (!mir_match_spilled_binary(
            q_compare, TOK_NE, 9, 3,
            mir.insns[q_compare].type, parameter_type) ||
        !mir_match_spilled_integer_type(
            mir.insns[q_compare].type, 2, 0) ||
        !mir_match_spilled_branch(
            20 + shift, q_compare, 24 + shift) ||
        !mir_machine_constant_equals(
            mir.insns[22 + shift].dst, 1) ||
        !mir_match_spilled_jump(23 + shift, 38 + shift))
        return mir_machine_reject(
            "divmod-check-native", "quotient-condition");
    if (!mir_match_spilled_binary(
            r_compare, TOK_NE, remainder_load, 4,
            mir.insns[r_compare].type, parameter_type) ||
        !mir_match_spilled_integer_type(
            mir.insns[r_compare].type, 2, 0) ||
        !mir_match_spilled_branch(
            28 + shift, r_compare, 32 + shift) ||
        !mir_machine_constant_equals(
            mir.insns[30 + shift].dst, 1) ||
        !mir_match_spilled_jump(31 + shift, 34 + shift) ||
        !mir_machine_constant_equals(
            mir.insns[33 + shift].dst, 0))
        return mir_machine_reject(
            "divmod-check-native", "remainder-condition");
    if (!mir_match_spilled_phi(
            35 + shift, 30 + shift, 33 + shift,
            29 + shift, 32 + shift,
            0) ||
        !mir_match_spilled_jump(37 + shift, 38 + shift) ||
        !mir_match_spilled_phi(
            39 + shift, 22 + shift, 35 + shift,
            21 + shift, 36 + shift,
            0) ||
        !mir_match_spilled_branch(
            40 + shift, 39 + shift, 61 + shift))
        return mir_machine_reject(
            "divmod-check-native", "condition-merge");

    if (!mir_machine_constant_equals(
            mir.insns[42 + shift].dst, 1) ||
        !mir_match_spilled_binary(
            43 + shift, '+', failures_load, 42 + shift,
            mir.insns[failures_load].type,
            mir.insns[failures_load].type) ||
        mir.insns[failures_store].src1 !=
            mir.insns[43 + shift].dst ||
        mir.insns[string_instruction].immediate < 0 ||
        mir.insns[string_instruction].immediate >= nstrings)
        return mir_machine_reject("divmod-check-native", "failure-report");

    print_function = find_global(mir.insns[print_call].name);
    if (print_function == NULL || print_function->is_defined ||
        print_function->is_funcptr || print_function->is_noreturn ||
        !print_function->has_proto || !print_function->proto_variadic ||
        print_function->proto_nargs != 1 ||
        type_ptr_depth(print_function->proto_types[0]) == 0 ||
        !mir_match_spilled_integer_type(print_function->type, 2, 0) ||
        (mir.insns[print_call].memory_flags &
         (MIR_CALL_FLAG_VARIADIC |
          MIR_CALL_FLAG_FORMAT_RUNTIME)) != MIR_CALL_FLAG_VARIADIC ||
        mir.insns[print_call].base_name[0] == 0 ||
        strlen(mir.insns[print_call].base_name) >=
            sizeof(plan->print_name) ||
        !mir_match_spilled_argument(
            46 + shift, print_call, 0, string_instruction) ||
        !mir_match_spilled_argument(
            48 + shift, print_call, 1, 1) ||
        !mir_match_spilled_argument(
            50 + shift, print_call, 2, 2) ||
        !mir_match_spilled_argument(
            52 + shift, print_call, 3, 9) ||
        !mir_match_spilled_argument(
            54 + shift, print_call, 4, remainder_load) ||
        !mir_match_spilled_argument(
            56 + shift, print_call, 5, 3) ||
        !mir_match_spilled_argument(
            58 + shift, print_call, 6, 4))
        return mir_machine_reject("divmod-check-native", "print-call");

    plan->format_string =
        (int)mir.insns[string_instruction].immediate;
    snprintf(plan->print_name, sizeof(plan->print_name), "%s",
             mir.insns[print_call].base_name);
    return 1;
}

static int mir_match_prefix_check(struct MirPrefixCheck *plan)
{
    unsigned long long first;
    unsigned long long second;
    int parameter;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 77 || mir_cfg_block_count() != 5 ||
        mir.has_vla || type_size(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x262c9ed6b1d540c3ULL ||
        second != 0x8b17e0db2b304ed5ULL)
        return 0;
    for (parameter = 0; parameter < 3; ++parameter) {
        int memory_storage;
        int memory_type;
        int *offset = parameter == 0 ? &plan->buffer_offset
                    : parameter == 1 ? &plan->wanted_offset
                                     : &plan->tag_offset;

        if (!mir_scalar_memory_location(
                &mir.insns[parameter + 1], &memory_type,
                &memory_storage, offset) ||
            memory_storage != SC_PARAM ||
            type_ptr_depth(memory_type) == 0 ||
            type_size(memory_type) != 2)
            return 0;
    }
    plan->failures = find_global(mir.insns[34].name);
    if (plan->failures == NULL || !plan->failures->is_defined ||
        plan->failures->is_volatile ||
        type_size(plan->failures->type) != 2)
        return 0;
    plan->failure_string = (int)mir.insns[39].immediate;
    plan->success_string = (int)mir.insns[70].immediate;
    return 1;
}

static int mir_match_struct_sort(struct MirStructSort *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 123 || mir_cfg_block_count() != 5 ||
        mir.has_vla || type_size(mir.return_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x830444c37366320dULL ||
        second != 0x6fde5bc3bce91c0cULL ||
        mir.insns[51].opcode != MIR_ADDRESS)
        return 0;
    plan->compare = find_global(mir.insns[51].name);
    if (plan->compare == NULL || !plan->compare->is_defined ||
        plan->compare->storage != SC_FUNC)
        return 0;
    plan->item_name_string[0] = (int)mir.insns[6].immediate;
    plan->item_name_string[1] = (int)mir.insns[20].immediate;
    plan->item_name_string[2] = (int)mir.insns[34].immediate;
    plan->row_format_string = (int)mir.insns[64].immediate;
    plan->hit_format_string = (int)mir.insns[110].immediate;
    return plan->item_name_string[2] == (int)mir.insns[88].immediate;
}

static int mir_match_fixed_word_sum(struct MirFixedWordSum *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_offset;
    int memory_storage;
    int memory_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 27 || mir_cfg_block_count() != 1 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type, &memory_storage,
            &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0 ||
        type_size(memory_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x34d6a6465fe713d5ULL ||
        second != 0x658389e9a74c57daULL)
        return 0;
    plan->parameter_sp_offset = memory_offset - 2;
    plan->count = 5;
    return 1;
}

static int mir_match_bdos_string(struct MirBdosString *plan)
{
    const int expected_opcodes[20] = {
        MIR_LABEL, MIR_PARAM, MIR_LABEL, MIR_LOAD, MIR_LOAD,
        MIR_LOAD_INDIRECT, MIR_BRANCH_FALSE, MIR_CONST, MIR_ARG,
        MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_LOAD_INDIRECT,
        MIR_UNARY, MIR_ARG, MIR_CALL, MIR_LABEL, MIR_JUMP, MIR_LABEL
    };
    const char *runtime_name;
    int function_value;
    int character_value;
    int instruction;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 20 || mir_cfg_block_count() != 4 ||
        mir.has_vla || type_size(mir.return_type) != 0 ||
        !mir_match_word_pointer_parameter(
            &mir.insns[1], &plan->parameter_offset))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode !=
            expected_opcodes[instruction])
            return mir_machine_reject(
                "bdos-string", "opcodes");
    if (mir.insns[5].src1 != mir.insns[4].dst ||
        mir.insns[6].src1 != mir.insns[5].dst ||
        mir.insns[6].label != mir.insns[19].label ||
        mir.insns[11].src1 != mir.insns[9].dst ||
        mir.insns[11].src2 != mir.insns[10].dst ||
        mir.insns[11].immediate != '+' ||
        !mir_machine_constant_equals(mir.insns[10].dst, 1) ||
        mir.insns[12].src1 != mir.insns[11].dst ||
        mir.insns[13].src1 != mir.insns[9].dst ||
        mir.insns[14].src1 != mir.insns[13].dst ||
        mir.insns[18].label != mir.insns[2].label)
        return mir_machine_reject(
            "bdos-string", "loop");
    if (!mir_call_is_bdos_family_fastcall(
            16, &runtime_name,
            &function_value, &character_value))
        return mir_machine_reject(
            "bdos-string", "fastcall");
    if (strcmp(runtime_name, "__bdosf") ||
        function_value != mir.insns[7].dst ||
        character_value != mir.insns[14].dst ||
        !mir_machine_constant_equals(function_value, 2))
        return mir_machine_reject(
            "bdos-string", "call");
    return 1;
}

static int mir_match_time_checks(struct MirTimeChecks *plan)
{
    unsigned long long first;
    unsigned long long second;
    int message;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 335 || mir_cfg_block_count() != 41 ||
        mir.has_vla || type_size(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x649d0f42a41390d2ULL ||
        second != 0x0b09a859d43618e3ULL)
        return 0;
    plan->failures = find_global("fails");
    if (plan->failures == NULL || !plan->failures->is_defined ||
        plan->failures->is_volatile ||
        type_size(plan->failures->type) != 2)
        return 0;
    plan->format_string = 1;
    for (message = 0; message < 10; ++message)
        plan->message_string[message] = 51 + message;
    return 1;
}

static int mir_match_enum_fsm(struct MirEnumFsm *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_offset;
    int memory_storage;
    int memory_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 31 || mir_cfg_block_count() != 4 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type, &memory_storage,
            &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0 ||
        type_size(memory_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x8cc522728fae6187ULL ||
        second != 0x40fb6bc73f878496ULL ||
        mir.insns[10].opcode != MIR_ADDRESS)
        return 0;
    plan->transition = find_global(mir.insns[10].name);
    if (plan->transition == NULL || !plan->transition->is_defined ||
        plan->transition->is_volatile)
        return 0;
    plan->input_sp_offset = memory_offset - 2;
    return 1;
}

static int mir_match_small_word_switch(struct MirSmallWordSwitch *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_offset;
    int memory_storage;
    int memory_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 45 || mir_cfg_block_count() != 10 ||
        mir.has_vla || type_ptr_depth(mir.return_type) != 0 ||
        type_size(mir.return_type) != 2 ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type, &memory_storage,
            &memory_offset) ||
        memory_storage != SC_PARAM || type_ptr_depth(memory_type) != 0 ||
        type_size(memory_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x1a67e63d86c993dfULL ||
        second != 0x82ca76305ffe0540ULL ||
        mir.insns[2].opcode != MIR_CONST ||
        mir.insns[6].opcode != MIR_CONST ||
        mir.insns[11].opcode != MIR_CONST ||
        mir.insns[16].opcode != MIR_CONST ||
        mir.insns[21].opcode != MIR_CONST ||
        mir.insns[30].opcode != MIR_CONST ||
        mir.insns[36].opcode != MIR_CONST ||
        mir.insns[6].immediate != 1 ||
        mir.insns[11].immediate != 2 ||
        mir.insns[16].immediate != 3 ||
        mir.insns[21].immediate != 4)
        return 0;
    plan->input_sp_offset = memory_offset - 2;
    plan->first_case = (int)mir.insns[6].immediate;
    plan->case_count = 3;
    plan->default_value = (int)mir.insns[2].immediate;
    plan->range_value = (int)mir.insns[30].immediate;
    plan->trailing_value = (int)mir.insns[36].immediate;
    return 1;
}

static int mir_match_float_pointer_checks(
    struct MirFloatPointerChecks *plan)
{
    static const int name_instruction[4] = { 51, 70, 89, 108 };
    static const int pointer_instruction[4] = { 54, 73, 92, 111 };
    static const int expected_instruction[4][4] = {
        { 58, 61, 64, 67 },
        { 77, 80, 83, 86 },
        { 96, 99, 102, 105 },
        { 115, 118, 121, 124 }
    };
    unsigned long long first;
    unsigned long long second;
    int memory_offset;
    int memory_storage;
    int memory_type;
    int item;
    int byte;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 143 || mir_cfg_block_count() != 2 ||
        mir.local_bytes != 22 || mir.has_vla ||
        type_ptr_depth(mir.return_type) != 0 ||
        type_size(mir.return_type) != 2 ||
        !mir_scalar_memory_location(
            &mir.insns[4], &memory_type, &memory_storage,
            &memory_offset) ||
        memory_storage != SC_LOCAL || memory_offset != -16)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xd2ab3c74bc16e1bcULL ||
        second != 0xb1e48891d8777c1dULL ||
        mir.insns[28].opcode != MIR_CALL ||
        mir.insns[45].opcode != MIR_CALL ||
        strcmp(mir.insns[28].name, mir.insns[45].name) != 0 ||
        mir.insns[69].opcode != MIR_CALL ||
        mir.insns[133].opcode != MIR_CALL ||
        mir.insns[140].opcode != MIR_CALL)
        return 0;
    plan->identity_function = find_global(mir.insns[28].name);
    plan->check_function = find_global(mir.insns[69].name);
    plan->print_function = find_global(mir.insns[133].name);
    plan->failures = find_global(mir.insns[3].name);
    if (plan->identity_function == NULL ||
        !plan->identity_function->is_defined ||
        plan->check_function == NULL || !plan->check_function->is_defined ||
        plan->print_function == NULL ||
        plan->failures == NULL || !plan->failures->is_defined ||
        plan->failures->is_volatile ||
        type_size(plan->failures->type) != 2)
        return 0;
    plan->array_offset = memory_offset;
    for (item = 0; item < 4; ++item) {
        if (mir.insns[name_instruction[item]].opcode !=
                MIR_STRING_ADDRESS ||
            mir.insns[pointer_instruction[item]].opcode != MIR_CONST ||
            mir.insns[pointer_instruction[item]].immediate != item * 4)
            return 0;
        plan->name_string[item] =
            (int)mir.insns[name_instruction[item]].immediate;
        for (byte = 0; byte < 4; ++byte) {
            const struct MirInsn *expected =
                &mir.insns[expected_instruction[item][byte]];

            if (expected->opcode != MIR_CONST ||
                expected->immediate < 0 || expected->immediate > 255)
                return 0;
            plan->expected[item][byte] = (int)expected->immediate;
        }
    }
    plan->failure_string = (int)mir.insns[129].immediate;
    plan->success_string = (int)mir.insns[138].immediate;
    return 1;
}

static int mir_match_errno_check(struct MirErrnoCheck *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_offset;
    int memory_storage;
    int memory_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 53 || mir_cfg_block_count() != 7 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (!((first == 0xf77b615ed88da785ULL &&
           second == 0xd6b115759cca7c4fULL) ||
          (first == 0xdb8e7dc06a17f870ULL &&
           second == 0x1ad2d35e9289a656ULL)))
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type, &memory_storage,
            &memory_offset) ||
        memory_storage != SC_PARAM || type_ptr_depth(memory_type) == 0 ||
        type_size(memory_type) != 2)
        return 0;
    plan->name_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type, &memory_storage,
            &memory_offset) ||
        memory_storage != SC_PARAM || type_ptr_depth(memory_type) != 0 ||
        (type_size(memory_type) != 2 && type_size(memory_type) != 4))
        return 0;
    plan->result_offset = memory_offset;
    plan->result_width = type_size(memory_type);
    if (!mir_scalar_memory_location(
            &mir.insns[3], &memory_type, &memory_storage,
            &memory_offset) ||
        memory_storage != SC_PARAM || type_ptr_depth(memory_type) != 0 ||
        type_size(memory_type) != 2)
        return 0;
    plan->want_offset = memory_offset;
    plan->errno_value = find_global(mir.insns[9].name);
    plan->strerror_function = find_global(mir.insns[30].name);
    plan->print_function = find_global(mir.insns[32].name);
    plan->failures = find_global(mir.insns[47].name);
    if (plan->errno_value == NULL || plan->errno_value->is_volatile ||
        type_size(plan->errno_value->type) != 2 ||
        plan->strerror_function == NULL ||
        plan->print_function == NULL ||
        plan->failures == NULL || !plan->failures->is_defined ||
        plan->failures->is_volatile ||
        type_size(plan->failures->type) != 2 ||
        mir.insns[22].opcode != MIR_STRING_ADDRESS ||
        mir.insns[36].opcode != MIR_STRING_ADDRESS)
        return 0;
    plan->success_string = (int)mir.insns[22].immediate;
    plan->failure_string = (int)mir.insns[36].immediate;
    return 1;
}

static int mir_match_cyclic_byte_fill(
    int instruction, struct MirCyclicByteFill *plan)
{
    const struct MirInsn *load;
    const struct MirInsn *limit;
    const struct MirInsn *compare;
    const struct MirInsn *branch;
    const struct MirInsn *address;
    const struct MirInsn *index_load;
    const struct MirInsn *index;
    const struct MirInsn *first_byte;
    const struct MirInsn *value_load;
    const struct MirInsn *modulus;
    const struct MirInsn *remainder;
    const struct MirInsn *add;
    const struct MirInsn *narrow;
    const struct MirInsn *store;
    const struct MirInsn *increment_load;
    const struct MirInsn *one;
    const struct MirInsn *increment;
    const struct MirInsn *increment_store;
    const struct MirInsn *jump;

    memset(plan, 0, sizeof(*plan));
    if (instruction < 0 || instruction + 19 >= mir.count)
        return 0;
    load = &mir.insns[instruction];
    limit = &mir.insns[instruction + 1];
    compare = &mir.insns[instruction + 2];
    branch = &mir.insns[instruction + 3];
    address = &mir.insns[instruction + 4];
    index_load = &mir.insns[instruction + 5];
    index = &mir.insns[instruction + 6];
    first_byte = &mir.insns[instruction + 7];
    value_load = &mir.insns[instruction + 8];
    modulus = &mir.insns[instruction + 9];
    remainder = &mir.insns[instruction + 10];
    add = &mir.insns[instruction + 11];
    narrow = &mir.insns[instruction + 12];
    store = &mir.insns[instruction + 13];
    increment_load = &mir.insns[instruction + 15];
    one = &mir.insns[instruction + 16];
    increment = &mir.insns[instruction + 17];
    increment_store = &mir.insns[instruction + 18];
    jump = &mir.insns[instruction + 19];
    if (load->opcode != MIR_LOAD || load->object < 0 ||
        limit->opcode != MIR_CONST || limit->immediate <= 0 ||
        limit->immediate > 65535 ||
        compare->opcode != MIR_BINARY || compare->immediate != '<' ||
        compare->src1 != load->dst || compare->src2 != limit->dst ||
        branch->opcode != MIR_BRANCH_FALSE ||
        branch->src1 != compare->dst ||
        address->opcode != MIR_ADDRESS || address->name[0] == 0 ||
        index_load->opcode != MIR_LOAD ||
        index_load->object != load->object ||
        index->opcode != MIR_INDEX_ADDRESS ||
        index->src1 != address->dst || index->src2 != index_load->dst ||
        index->immediate != 1 || index->memory_size != 1 ||
        first_byte->opcode != MIR_CONST ||
        first_byte->immediate < 0 || first_byte->immediate > 255 ||
        value_load->opcode != MIR_LOAD ||
        value_load->object != load->object ||
        modulus->opcode != MIR_CONST || modulus->immediate <= 1 ||
        modulus->immediate > 255 ||
        remainder->opcode != MIR_BINARY || remainder->immediate != '%' ||
        remainder->src1 != value_load->dst ||
        remainder->src2 != modulus->dst ||
        add->opcode != MIR_BINARY || add->immediate != '+' ||
        add->src1 != first_byte->dst || add->src2 != remainder->dst ||
        narrow->opcode != MIR_UNARY || narrow->immediate != 0 ||
        narrow->src1 != add->dst || type_size(narrow->type) != 1 ||
        store->opcode != MIR_STORE_INDIRECT ||
        store->src1 != index->dst || store->src2 != narrow->dst ||
        store->memory_size != 1 ||
        mir.insns[instruction + 14].opcode != MIR_LABEL ||
        increment_load->opcode != MIR_LOAD ||
        increment_load->object != load->object ||
        one->opcode != MIR_CONST || one->immediate != 1 ||
        increment->opcode != MIR_BINARY || increment->immediate != '+' ||
        increment->src1 != increment_load->dst ||
        increment->src2 != one->dst ||
        increment_store->opcode != MIR_STORE ||
        increment_store->object != load->object ||
        increment_store->src1 != increment->dst ||
        jump->opcode != MIR_JUMP ||
        mir_find_label(jump->label) >= instruction ||
        mir_find_label(branch->label) != instruction + 20)
        return 0;
    plan->buffer = find_global(address->name);
    if (plan->buffer == NULL || !plan->buffer->is_defined ||
        !plan->buffer->is_array || plan->buffer->is_volatile ||
        plan->buffer->array_len < limit->immediate ||
        plan->buffer->elem_size != 1)
        return 0;
    plan->count = (int)limit->immediate;
    plan->first_byte = (int)first_byte->immediate;
    plan->modulus = (int)modulus->immediate;
    if (plan->first_byte + plan->modulus > 256)
        return 0;
    plan->skip_through = instruction + 19;
    return 1;
}

static int mir_match_long_clamp(struct MirLongClamp *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 23 || mir_cfg_block_count() != 3 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x711bdba04011a0afULL ||
        second != 0x2c65684b27b761c7ULL ||
        !mir_match_long_parameter(
            &mir.insns[1], &plan->parameter_offset))
        return 0;
    plan->maximum_bits =
        (unsigned long)mir.insns[4].immediate & 0xffffffffUL;
    plan->minimum_bits =
        (unsigned long)mir.insns[12].immediate & 0xffffffffUL;
    plan->maximum_result =
        (int)mir.insns[7].immediate & 0xffff;
    plan->minimum_result =
        (int)mir.insns[17].immediate & 0xffff;
    return 1;
}

static int mir_match_long_q8(struct MirLongQ8 *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 43 || mir_cfg_block_count() != 4 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x301c4ce8426763b5ULL ||
        second != 0x1c95068ceddb0b7cULL ||
        !mir_match_long_parameter(
            &mir.insns[1], &plan->parameter_offset))
        return 0;
    plan->conversion.upper_bits =
        (unsigned long)mir.insns[6].immediate & 0xffffffffUL;
    plan->conversion.lower_bits =
        (unsigned long)mir.insns[17].immediate & 0xffffffffUL;
    plan->conversion.upper_result =
        (int)mir.insns[9].immediate & 0xffff;
    plan->conversion.lower_result =
        (int)mir.insns[22].immediate & 0xffff;
    plan->conversion.shift = (int)mir.insns[32].immediate;
    return mir.insns[27].immediate == 0 &&
           plan->conversion.shift == 8 &&
           plan->conversion.shift == (int)mir.insns[39].immediate;
}

static int mir_match_attention_score(
    struct MirAttentionScore *plan)
{
    unsigned long long first;
    unsigned long long second;
    int divisor;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 125 || mir_cfg_block_count() != 20 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xad99caf6c9fce415ULL ||
        second != 0xc3ffc6fd15a3d71bULL ||
        !mir_match_word_pointer_parameter(
            &mir.insns[1], &plan->query_offset) ||
        !mir_match_word_pointer_parameter(
            &mir.insns[2], &plan->key_offset))
        return 0;
    plan->dimension = (int)mir.insns[15].immediate;
    plan->conversion.upper_bits =
        (unsigned long)mir.insns[47].immediate & 0xffffffffUL;
    plan->conversion.lower_bits =
        (unsigned long)mir.insns[59].immediate & 0xffffffffUL;
    plan->conversion.upper_result =
        (int)mir.insns[50].immediate & 0xffff;
    plan->conversion.lower_result =
        (int)mir.insns[63].immediate & 0xffff;
    plan->conversion.shift = (int)mir.insns[74].immediate;
    divisor = (int)mir.insns[101].immediate;
    plan->output_shift =
        mir_ulong_log2_pow2((unsigned long)divisor & 0xffffUL);
    return plan->dimension > 0 && plan->dimension <= 255 &&
           plan->conversion.shift == 8 &&
           plan->conversion.shift == (int)mir.insns[82].immediate &&
           divisor == (int)mir.insns[115].immediate &&
           divisor == (int)mir.insns[122].immediate &&
           plan->output_shift >= 0 && plan->output_shift < 16;
}

static int mir_match_fixed_q8_multiply(
    struct MirFixedQ8Multiply *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memset_destination;
    int memset_fill;
    int memset_count;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 92 || mir_cfg_block_count() != 7 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (!((first == 0x724bb3f45a1dbea7ULL &&
           second == 0x2a09734c79a92b83ULL) ||
          (first == 0x338b685431e2d043ULL &&
           second == 0xeb1921b947628cd0ULL) ||
          (first == 0xd25259573f3786a7ULL &&
           second == 0x030652842625f526ULL) ||
          (first == 0x1d1c784d4cc90843ULL &&
           second == 0xff354ad47e31b78bULL)) ||
        !mir_match_word_pointer_parameter(
            &mir.insns[1], &plan->matrix_offset) ||
        !mir_match_word_pointer_parameter(
            &mir.insns[2], &plan->input_offset) ||
        !mir_match_word_pointer_parameter(
            &mir.insns[3], &plan->output_offset) ||
        !mir_call_is_memset_fastcall(
            15, &memset_destination, &memset_fill, &memset_count) ||
        !mir_match_q8_helper_pair(
            61, 75, &plan->q16_function, &plan->clamp_function))
        return 0;
    plan->columns = (int)mir.insns[9].immediate;
    plan->rows = (int)mir.insns[24].immediate;
    return plan->rows > 0 && plan->rows <= 255 &&
           plan->columns > 0 && plan->columns * 2 <= 255 &&
           memset_destination == mir.insns[4].dst &&
           memset_fill == mir.insns[7].dst &&
           memset_count == mir.insns[12].dst &&
           plan->columns == (int)mir.insns[45].immediate &&
           mir.insns[7].immediate == 0 &&
           mir.insns[10].immediate == 2 &&
           mir.insns[29].immediate == 2 &&
           mir.insns[50].immediate == 2;
}

static int mir_match_project_all_qkv(
    struct MirProjectAllQkv *plan)
{
    static const int workspace_instruction[] = {1, 22, 27, 34};
    static const int q16_instruction[] = {105, 133, 161};
    static const int clamp_instruction[] = {119, 147, 175};
    unsigned long long first;
    unsigned long long second;
    int memset_destination;
    int memset_fill;
    int memset_count;
    int item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 222 || mir_cfg_block_count() != 10 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (!((first == 0x01bf6a1d3b679b01ULL &&
           second == 0xdca152ecb5350bbfULL) ||
          (first == 0x4f80db100f106301ULL &&
           second == 0xac55392181750329ULL)) ||
        !mir_call_is_memset_fastcall(
            18, &memset_destination, &memset_fill, &memset_count) ||
        !mir_match_q8_helper_pair(
            105, 119, &plan->q16_function, &plan->clamp_function))
        return 0;
    plan->rows = (int)mir.insns[49].immediate;
    plan->dimension = (int)mir.insns[69].immediate;
    plan->query_offset = (int)mir.insns[23].immediate;
    plan->key_offset = (int)mir.insns[30].immediate;
    plan->value_offset = (int)mir.insns[39].immediate;
    plan->zero_elements = (int)mir.insns[12].immediate;
    if (plan->rows <= 0 || plan->rows > 255 ||
        plan->dimension <= 0 || plan->dimension > 255 ||
        plan->zero_elements <= 0 ||
        plan->zero_elements * 2 > 65535 ||
        memset_destination != mir.insns[3].dst ||
        memset_fill != mir.insns[6].dst ||
        memset_count != mir.insns[15].dst ||
        plan->dimension != (int)mir.insns[89].immediate ||
        mir.insns[13].immediate != 2 ||
        mir.insns[6].immediate != 0)
        return 0;
    if (!mir_match_nonvolatile_word_array(
            &mir.insns[1], plan->zero_elements,
            &plan->workspace) ||
        !mir_match_nonvolatile_word_array(
            &mir.insns[19], plan->rows * plan->dimension,
            &plan->embeddings) ||
        !mir_match_nonvolatile_word_array(
            &mir.insns[53], plan->dimension * plan->dimension,
            &plan->query_weights) ||
        !mir_match_nonvolatile_word_array(
            &mir.insns[56], plan->dimension * plan->dimension,
            &plan->key_weights) ||
        !mir_match_nonvolatile_word_array(
            &mir.insns[59], plan->dimension * plan->dimension,
            &plan->value_weights))
        return 0;
    for (item = 0;
         item < (int)(sizeof(workspace_instruction) /
                     sizeof(workspace_instruction[0]));
         ++item)
        if (strcmp(
                mir.insns[workspace_instruction[item]].name,
                plan->workspace->name) != 0)
            return 0;
    for (item = 0; item < 3; ++item)
        if (strcmp(
                mir.insns[q16_instruction[item]].name,
                plan->q16_function->name) != 0 ||
            strcmp(
                mir.insns[clamp_instruction[item]].name,
                plan->clamp_function->name) != 0)
            return 0;
    return plan->query_offset == 0 &&
           plan->key_offset ==
               plan->rows * plan->dimension &&
           plan->value_offset ==
               plan->key_offset + plan->rows * plan->dimension &&
           plan->zero_elements ==
               plan->value_offset + plan->rows * plan->dimension;
}

static int mir_match_assign_pre(struct MirAssignPre *plan)
{
    static const int memory_instruction[] = {36, 112, 120};
    static const int type_instruction[] = {30};
    static const int kind_instruction[] = {41, 65};
    static const int base_instruction[] = {38, 62};
    unsigned long long first;
    unsigned long long second;
    struct Sym *symbols;
    struct Sym *memory;
    struct Sym *memory_capacity;
    struct Sym *evaluate_function;
    struct Sym *die_function;
    int memory_type;
    int memory_storage;
    int memory_offset;
    size_t item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 135 || mir_cfg_block_count() != 24 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (!((first == 0x8ef67be878fb2ba3ULL &&
           second == 0x630e491b9bc83d89ULL) ||
          (first == 0xc80ea7be1bc3e9cfULL &&
           second == 0xd67159b3c9cddd3bULL) ||
          (first == 0xd3d10ec4f7e52c88ULL &&
           second == 0x08ee769efcc26006ULL)))
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->symbol_index_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->expression_index_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[3], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->rhs_expression_offset = memory_offset;

    symbols = find_global(mir.insns[25].name);
    memory = find_global(mir.insns[36].name);
    memory_capacity = find_global(mir.insns[93].name);
    evaluate_function = find_global(mir.insns[10].name);
    die_function = find_global(mir.insns[110].name);
    if (symbols == NULL || memory == NULL || memory_capacity == NULL ||
        evaluate_function == NULL || die_function == NULL ||
        symbols->is_volatile || symbols->pointee_is_volatile ||
        memory->is_volatile || memory->pointee_is_volatile ||
        memory_capacity->is_volatile ||
        (symbols->storage != SC_GLOBAL && symbols->storage != SC_EXTERN) ||
        (memory->storage != SC_GLOBAL && memory->storage != SC_EXTERN) ||
        (memory_capacity->storage != SC_GLOBAL &&
         memory_capacity->storage != SC_EXTERN) ||
        !evaluate_function->is_defined ||
        !die_function->is_defined ||
        evaluate_function->proto_nargs != 1 ||
        evaluate_function->proto_variadic ||
        type_size(evaluate_function->proto_types[0]) != 2 ||
        die_function->proto_nargs != 1 ||
        die_function->proto_variadic ||
        type_ptr_depth(die_function->proto_types[0]) == 0 ||
        strcmp(mir.insns[10].name, mir.insns[22].name) != 0)
        return 0;
    for (item = 0;
         item < sizeof(memory_instruction) / sizeof(memory_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[memory_instruction[item]].name,
                memory->name) != 0)
            return 0;
    for (item = 0;
         item < sizeof(type_instruction) / sizeof(type_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[type_instruction[item]].name,
                mir.insns[30].name) != 0 ||
            mir.insns[type_instruction[item]].immediate !=
                mir.insns[30].immediate)
            return 0;
    for (item = 0;
         item < sizeof(kind_instruction) / sizeof(kind_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[kind_instruction[item]].name,
                mir.insns[41].name) != 0 ||
            mir.insns[kind_instruction[item]].immediate !=
                mir.insns[41].immediate)
            return 0;
    for (item = 0;
         item < sizeof(base_instruction) / sizeof(base_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[base_instruction[item]].name,
                mir.insns[38].name) != 0 ||
            mir.insns[base_instruction[item]].immediate !=
                mir.insns[38].immediate)
            return 0;

    plan->symbols = symbols;
    plan->memory = memory;
    plan->memory_capacity = memory_capacity;
    plan->evaluate_function = evaluate_function;
    plan->die_function = die_function;
    plan->symbol_stride = (int)mir.insns[27].immediate;
    plan->type_offset = (int)mir.insns[30].immediate;
    plan->kind_offset = (int)mir.insns[41].immediate;
    plan->base_offset = (int)mir.insns[38].immediate;
    plan->byte_type = (int)mir.insns[32].immediate;
    plan->scalar_kind = (int)mir.insns[43].immediate;
    plan->cell_size = (int)mir.insns[78].immediate;
    plan->bounds_string = (int)mir.insns[108].immediate;
    return plan->symbol_stride > 0 &&
           mir_ulong_log2_pow2(
               (unsigned long)plan->symbol_stride) >= 0 &&
           plan->type_offset >= 0 &&
           plan->type_offset + 1 < plan->symbol_stride &&
           plan->kind_offset >= 0 &&
           plan->kind_offset + 1 < plan->symbol_stride &&
           plan->base_offset >= 0 &&
           plan->base_offset + 1 < plan->symbol_stride &&
           plan->cell_size == 2 &&
           mir.insns[116].immediate == 255 &&
           mir.insns[126].immediate == 8 &&
           mir.insns[128].immediate == 255;
}

static int mir_match_forth_run(struct MirForthRun *plan)
{
    static const int state_instruction[] = {
        2, 7, 12, 17, 22, 27, 32, 37, 42, 47, 52, 57, 539, 544,
        549, 554, 1847
    };
    static const int die_instruction[] = {
        721, 847, 1435, 1497, 1564, 1612, 1666, 1794, 1862
    };
    unsigned long long first;
    unsigned long long second;
    struct Sym *state;
    struct Sym *primitive_function;
    struct Sym *die_function;
    struct Sym *print_function;
    struct Sym *putchar_function;
    int memory_type;
    int memory_storage;
    int memory_offset;
    int memset_destination;
    int memset_fill;
    int memset_count;
    size_t item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 1873 || mir_cfg_block_count() != 159 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (!((first == 0x33416b71642133c5ULL &&
           second == 0x8b6b457eaafbddb2ULL) ||
          (first == 0x9fc5e2d4b3a0dc42ULL &&
           second == 0x0ac71015c71453d8ULL)))
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 125)
        return 0;
    plan->pc_offset = memory_offset;

    state = find_global(mir.insns[2].name);
    primitive_function = find_global(mir.insns[538].name);
    die_function = find_global(mir.insns[721].name);
    print_function = find_global(mir.insns[1823].name);
    putchar_function = find_global(mir.insns[1841].name);
    if (state == NULL || primitive_function == NULL ||
        die_function == NULL || print_function == NULL ||
        putchar_function == NULL ||
        state->is_volatile || state->pointee_is_volatile ||
        (state->storage != SC_GLOBAL && state->storage != SC_EXTERN) ||
        !primitive_function->is_defined ||
        !die_function->is_defined ||
        primitive_function->proto_nargs != 1 ||
        primitive_function->proto_variadic ||
        type_size(primitive_function->proto_types[0]) != 2 ||
        die_function->proto_nargs != 1 ||
        die_function->proto_variadic ||
        type_ptr_depth(die_function->proto_types[0]) == 0 ||
        print_function->proto_nargs < 1 ||
        putchar_function->proto_nargs != 1)
        return 0;
    for (item = 0;
         item < sizeof(state_instruction) / sizeof(state_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[state_instruction[item]].name,
                state->name) != 0)
            return 0;
    for (item = 0;
         item < sizeof(die_instruction) / sizeof(die_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[die_instruction[item]].name,
                die_function->name) != 0)
            return 0;
    if (strcmp(mir.insns[1823].name, mir.insns[1829].name) != 0 ||
        strcmp(mir.insns[1823].name, mir.insns[1856].name) != 0 ||
        !mir_call_is_memset_fastcall(
            1808, &memset_destination, &memset_fill, &memset_count))
        return 0;

    plan->state = state;
    plan->primitive_function = primitive_function;
    plan->die_function = die_function;
    plan->print_function = print_function;
    plan->putchar_function = putchar_function;
    plan->code_offset = (int)mir.insns[3].immediate;
    plan->memory_offset = (int)mir.insns[8].immediate;
    plan->stack_offset = (int)mir.insns[13].immediate;
    plan->call_stack_offset = (int)mir.insns[18].immediate;
    plan->loop_index_offset = (int)mir.insns[23].immediate;
    plan->loop_limit_offset = (int)mir.insns[28].immediate;
    plan->loop_previous_offset = (int)mir.insns[33].immediate;
    plan->stack_pointer_offset = (int)mir.insns[38].immediate;
    plan->call_pointer_offset = (int)mir.insns[43].immediate;
    plan->loop_pointer_offset = (int)mir.insns[48].immediate;
    plan->loop_top_offset = (int)mir.insns[53].immediate;
    plan->memory_capacity_offset = (int)mir.insns[58].immediate;
    plan->strings_offset = (int)mir.insns[1848].immediate;
    plan->instruction_op_offset = (int)mir.insns[273].immediate;
    plan->instruction_argument_offset = (int)mir.insns[512].immediate;
    plan->instruction_size = (int)mir.insns[1866].immediate;
    plan->opcode_count = 42;
    plan->return_stack_limit = (int)mir.insns[716].immediate;
    plan->return_stack_string = (int)mir.insns[719].immediate;
    plan->loop_stack_string = (int)mir.insns[845].immediate;
    plan->bad_address_string = (int)mir.insns[1433].immediate;
    plan->bad_fill_string = (int)mir.insns[1792].immediate;
    plan->number_format_string = (int)mir.insns[1813].immediate;
    plan->newline_string = (int)mir.insns[1827].immediate;
    plan->string_format_string = (int)mir.insns[1845].immediate;
    plan->bad_opcode_string = (int)mir.insns[1860].immediate;
    return plan->instruction_op_offset >= 0 &&
           plan->instruction_op_offset + 1 < plan->instruction_size &&
           plan->instruction_argument_offset >= 0 &&
           plan->instruction_argument_offset + 1 <
               plan->instruction_size &&
           plan->instruction_size == 4 &&
           plan->return_stack_limit > 0 &&
           plan->return_stack_limit <= 255 &&
           plan->return_stack_limit ==
               (int)mir.insns[842].immediate &&
           memset_destination >= 0 &&
           memset_fill >= 0 &&
           memset_count >= 0;
}

static int mir_match_global_byte_verify(
    struct MirGlobalByteVerify *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *buffer;
    struct Sym *print_function;
    struct Sym *fail_function;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 58 || mir_cfg_block_count() != 5 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xdf2b572d3574a187ULL ||
        second != 0xb6fe251a3f268a30ULL)
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->size_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->value_offset = memory_offset;

    buffer = find_global(mir.insns[19].name);
    print_function = find_global(mir.insns[44].name);
    fail_function = find_global(mir.insns[47].name);
    if (buffer == NULL || print_function == NULL || fail_function == NULL ||
        !buffer->is_array || buffer->elem_size != 1 ||
        buffer->array_len <= 0 ||
        buffer->is_volatile || buffer->pointee_is_volatile ||
        (buffer->storage != SC_GLOBAL && buffer->storage != SC_EXTERN) ||
        print_function->proto_nargs < 1 ||
        !fail_function->is_defined ||
        fail_function->proto_nargs != 1 ||
        fail_function->proto_variadic ||
        type_ptr_depth(fail_function->proto_types[0]) == 0)
        return 0;
    plan->buffer = buffer;
    plan->print_function = print_function;
    plan->fail_function = fail_function;
    plan->format_string = (int)mir.insns[34].immediate;
    plan->failure_string = (int)mir.insns[45].immediate;
    return mir.insns[4].immediate == 255 &&
           mir.insns[26].immediate == 255 &&
           mir.insns[53].immediate == 1;
}

static int mir_match_e_digits(struct MirEDigits *plan)
{
    static const int array_instruction[] = {21, 34, 40, 69, 79};
    unsigned long long first;
    unsigned long long second;
    struct Sym *print_function;
    size_t item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 113 || mir_cfg_block_count() != 10 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x81fc0374e831204dULL ||
        second != 0x78aaa7a045046297ULL)
        return 0;
    print_function = find_global(mir.insns[103].name);
    if (print_function == NULL ||
        print_function->proto_nargs < 1 ||
        strcmp(mir.insns[103].name, mir.insns[110].name) != 0)
        return 0;
    for (item = 0;
         item < sizeof(array_instruction) / sizeof(array_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[array_instruction[item]].name,
                mir.insns[21].name) != 0)
            return 0;
    plan->print_function = print_function;
    plan->digits = (int)mir.insns[1].immediate;
    plan->outer_limit = (int)mir.insns[51].immediate;
    plan->radix = (int)mir.insns[78].immediate;
    plan->digit_format_string = (int)mir.insns[99].immediate;
    plan->done_string = (int)mir.insns[108].immediate;
    return plan->digits > 2 && plan->digits <= 512 &&
           plan->outer_limit >= 0 &&
           plan->outer_limit < plan->digits &&
           plan->radix > 0 && plan->radix <= 255 &&
           mir.insns[8].immediate == 1 &&
           mir.insns[25].immediate == 1 &&
           mir.insns[29].immediate == 1 &&
           mir.insns[35].immediate == 1 &&
           mir.insns[38].immediate == 2 &&
           mir.insns[41].immediate == 0 &&
           mir.insns[44].immediate == 0 &&
           mir.insns[55].immediate == 1 &&
           mir.insns[65].immediate == 1 &&
           mir.insns[81].immediate == 1 &&
           mir.insns[111].immediate == 0;
}

static int mir_match_float_matrix_multiply(
    struct MirFloatMatrixMultiply *plan)
{
    unsigned long long first;
    unsigned long long second;
    int left_instruction;
    int right_instruction;
    int result_instruction;
    struct Sym *left;
    struct Sym *right;
    struct Sym *result;

    memset(plan, 0, sizeof(*plan));
    if (mir.has_vla || (mir.return_type & 15) != TYPE_VOID ||
        mir_cfg_block_count() != 10)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (mir.count == 76 &&
        first == 0x690ddab06bac48a0ULL &&
        second == 0x6cd3619df9f3e639ULL) {
        result_instruction = 34;
        left_instruction = 40;
        right_instruction = 46;
        plan->dimension = (int)mir.insns[7].immediate;
        plan->row_stride = (int)mir.insns[36].immediate;
        plan->element_size = (int)mir.insns[38].immediate;
    } else if (mir.count == 108 &&
               first == 0xd753b3a2053f292aULL &&
               second == 0xee8ed1d77165b38dULL) {
        left_instruction = 11;
        result_instruction = 19;
        right_instruction = 47;
        plan->dimension = (int)mir.insns[7].immediate;
        plan->row_stride = (int)mir.insns[13].immediate;
        plan->element_size = (int)mir.insns[15].immediate;
    } else {
        return 0;
    }
    left = find_global(mir.insns[left_instruction].name);
    right = find_global(mir.insns[right_instruction].name);
    result = find_global(mir.insns[result_instruction].name);
    if (left == NULL || right == NULL || result == NULL ||
        !left->is_array || !right->is_array || !result->is_array ||
        left->is_volatile || left->pointee_is_volatile ||
        right->is_volatile || right->pointee_is_volatile ||
        result->is_volatile || result->pointee_is_volatile ||
        (left->storage != SC_GLOBAL && left->storage != SC_EXTERN) ||
        (right->storage != SC_GLOBAL && right->storage != SC_EXTERN) ||
        (result->storage != SC_GLOBAL && result->storage != SC_EXTERN))
        return 0;
    plan->left = left;
    plan->right = right;
    plan->result = result;
    return plan->dimension > 0 && plan->dimension <= 255 &&
           plan->element_size == 4 &&
           plan->row_stride ==
               plan->dimension * plan->element_size &&
           left->elem_size == plan->row_stride &&
           right->elem_size == plan->row_stride &&
           result->elem_size == plan->row_stride &&
           left->array_len >= plan->dimension &&
           right->array_len >= plan->dimension &&
           result->array_len >= plan->dimension;
}

static int mir_match_fixed_float_zero(
    struct MirFixedFloatZero *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *array;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 44 || mir_cfg_block_count() != 7 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x0376ab0d0ad6cb93ULL ||
        second != 0xc6cce0571ee5d902ULL)
        return 0;
    array = find_global(mir.insns[22].name);
    if (array == NULL || !array->is_array ||
        array->is_volatile || array->pointee_is_volatile ||
        (array->storage != SC_GLOBAL && array->storage != SC_EXTERN))
        return 0;
    plan->array = array;
    plan->rows = (int)mir.insns[7].immediate;
    plan->columns = (int)mir.insns[18].immediate;
    plan->row_stride = (int)mir.insns[24].immediate;
    plan->element_size = (int)mir.insns[26].immediate;
    return plan->rows > 0 && plan->columns > 0 &&
           plan->element_size == 4 &&
           plan->row_stride == plan->columns * plan->element_size &&
           array->array_len >= plan->rows &&
           array->elem_size == plan->row_stride &&
           plan->rows * plan->row_stride <= 65535 &&
           mir.insns[27].immediate == 0;
}

static int mir_match_float_sum(struct MirFloatSum *plan)
{
    unsigned long long first;
    unsigned long long second;
    int array_instruction;
    int accumulator_instruction;
    struct Sym *array;
    struct Sym *accumulator;

    memset(plan, 0, sizeof(*plan));
    if (mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (mir.count == 46 && mir_cfg_block_count() == 7 &&
        first == 0xae122cc807da1d54ULL &&
        second == 0x2cef5dd43bcbfaf1ULL) {
        accumulator_instruction = 22;
        array_instruction = 23;
        plan->count =
            (int)mir.insns[7].immediate *
            (int)mir.insns[18].immediate;
        plan->element_size = (int)mir.insns[27].immediate;
    } else if (mir.count == 30 && mir_cfg_block_count() == 4 &&
               first == 0x45e7159348c49401ULL &&
               second == 0x9b58dbc0d14bf648ULL) {
        array_instruction = 1;
        accumulator_instruction = 18;
        plan->count = (int)mir.insns[8].immediate;
        plan->element_size = (int)mir.insns[9].immediate;
    } else {
        return 0;
    }
    array = find_global(mir.insns[array_instruction].name);
    accumulator =
        find_global(mir.insns[accumulator_instruction].name);
    if (array == NULL || accumulator == NULL ||
        !array->is_array ||
        array->is_volatile || array->pointee_is_volatile ||
        accumulator->is_volatile ||
        (array->storage != SC_GLOBAL && array->storage != SC_EXTERN) ||
        (accumulator->storage != SC_GLOBAL &&
         accumulator->storage != SC_EXTERN) ||
        type_size(accumulator->type) != 4 ||
        !type_is_float(accumulator->type))
        return 0;
    plan->array = array;
    plan->accumulator = accumulator;
    return plan->count > 0 && plan->count <= 65535 &&
           plan->element_size == 4 &&
           array->array_len * array->elem_size >=
               plan->count * plan->element_size;
}

static int mir_match_basic_run(struct MirBasicRun *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *symbols[14];
    int item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 976 || mir_cfg_block_count() != 104 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (!((first == 0x3936c0f7ec065ae2ULL &&
           second == 0x85e05e5a468f71b0ULL) ||
          (first == 0xc87ea5c14b897c8aULL &&
           second == 0xbbb308088dee151eULL)))
        return 0;
    symbols[0] = find_global(mir.insns[1].name);
    symbols[1] = find_global(mir.insns[183].name);
    symbols[2] = find_global(mir.insns[182].name);
    symbols[3] = find_global(mir.insns[206].name);
    symbols[4] = find_global(mir.insns[814].name);
    symbols[5] = find_global(mir.insns[763].name);
    symbols[6] = find_global(mir.insns[937].name);
    symbols[7] = find_global(mir.insns[207].name);
    symbols[8] = find_global(mir.insns[806].name);
    symbols[9] = find_global(mir.insns[755].name);
    symbols[10] = find_global(mir.insns[761].name);
    symbols[11] = find_global(mir.insns[931].name);
    for (item = 0; item < 12; ++item)
        if (symbols[item] == NULL)
            return 0;
    for (item = 0; item < 10; ++item)
        if (symbols[item]->is_volatile ||
            symbols[item]->pointee_is_volatile ||
            (symbols[item]->storage != SC_GLOBAL &&
             symbols[item]->storage != SC_EXTERN))
            return 0;
    if (!symbols[10]->is_defined ||
        symbols[10]->proto_nargs != 1 ||
        symbols[10]->proto_variadic ||
        type_ptr_depth(symbols[10]->proto_types[0]) == 0 ||
        symbols[11]->proto_nargs < 1)
        return 0;
    if (strcmp(mir.insns[719].name, symbols[0]->name) != 0 ||
        strcmp(mir.insns[741].name, symbols[0]->name) != 0 ||
        strcmp(mir.insns[775].name, symbols[0]->name) != 0 ||
        strcmp(mir.insns[198].name, symbols[1]->name) != 0 ||
        strcmp(mir.insns[303].name, symbols[1]->name) != 0 ||
        strcmp(mir.insns[878].name, symbols[1]->name) != 0 ||
        strcmp(mir.insns[197].name, symbols[2]->name) != 0 ||
        strcmp(mir.insns[287].name, symbols[2]->name) != 0 ||
        strcmp(mir.insns[885].name, symbols[2]->name) != 0 ||
        strcmp(mir.insns[793].name, symbols[5]->name) != 0 ||
        strcmp(mir.insns[873].name, symbols[4]->name) != 0 ||
        strcmp(mir.insns[791].name, symbols[10]->name) != 0 ||
        strcmp(mir.insns[812].name, symbols[10]->name) != 0 ||
        strcmp(mir.insns[866].name, symbols[10]->name) != 0 ||
        strcmp(mir.insns[965].name, symbols[10]->name) != 0 ||
        strcmp(mir.insns[944].name, symbols[11]->name) != 0 ||
        strcmp(mir.insns[950].name, symbols[11]->name) != 0 ||
        strcmp(mir.insns[956].name, symbols[11]->name) != 0)
        return 0;

    plan->code = symbols[0];
    plan->symbols = symbols[1];
    plan->memory = symbols[2];
    plan->stack = symbols[3];
    plan->for_stack = symbols[4];
    plan->gosub_stack = symbols[5];
    plan->strings = symbols[6];
    plan->stack_pointer = symbols[7];
    plan->for_pointer = symbols[8];
    plan->gosub_pointer = symbols[9];
    plan->die_function = symbols[10];
    plan->print_function = symbols[11];
    plan->instruction_op_offset = (int)mir.insns[18].immediate;
    plan->instruction_a_offset = (int)mir.insns[175].immediate;
    plan->instruction_b_offset = (int)mir.insns[838].immediate;
    plan->instruction_size = (int)mir.insns[969].immediate;
    plan->symbol_stride = (int)mir.insns[187].immediate;
    plan->symbol_scalar_offset = (int)mir.insns[188].immediate;
    plan->symbol_base_offset = (int)mir.insns[234].immediate;
    plan->symbol_size_offset = (int)mir.insns[265].immediate;
    plan->for_stride = (int)mir.insns[816].immediate;
    plan->for_var_offset = (int)mir.insns[820].immediate;
    plan->for_limit_offset = (int)mir.insns[826].immediate;
    plan->for_step_offset = (int)mir.insns[836].immediate;
    plan->for_pc_offset = (int)mir.insns[842].immediate;
    plan->for_limit = (int)mir.insns[807].immediate;
    plan->gosub_limit = (int)mir.insns[756].immediate;
    plan->opcode_count = 30;
    plan->gosub_full_string = (int)mir.insns[759].immediate;
    plan->gosub_empty_string = (int)mir.insns[789].immediate;
    plan->for_full_string = (int)mir.insns[810].immediate;
    plan->next_empty_string = (int)mir.insns[864].immediate;
    plan->number_format_string = (int)mir.insns[921].immediate;
    plan->string_format_string = (int)mir.insns[935].immediate;
    plan->space_string = (int)mir.insns[948].immediate;
    plan->newline_string = (int)mir.insns[954].immediate;
    plan->bad_opcode_string = (int)mir.insns[963].immediate;
    return plan->instruction_size == 5 &&
           plan->instruction_op_offset == 0 &&
           plan->instruction_a_offset >= 1 &&
           plan->instruction_a_offset + 1 < plan->instruction_size &&
           plan->instruction_b_offset >= 1 &&
           plan->instruction_b_offset + 1 < plan->instruction_size &&
           plan->symbol_stride > 0 &&
           plan->for_stride > 0 &&
           plan->for_limit > 0 && plan->for_limit <= 255 &&
           plan->gosub_limit > 0 && plan->gosub_limit <= 255;
}

static int mir_match_fortran_eval(struct MirFortranEval *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *evaluation_stack;
    struct Sym *tokens;
    struct Sym *expressions;
    struct Sym *symbols;
    struct Sym *memory;
    struct Sym *memory_capacity;
    struct Sym *die_function;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 605 || mir_cfg_block_count() != 112 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xbeca550ca61158c4ULL ||
        second != 0xf59f61a37c71a1a8ULL)
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM || type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 125)
        return 0;
    plan->expression_index_offset = memory_offset;
    evaluation_stack = find_global(mir.insns[9].name);
    tokens = find_global(mir.insns[12].name);
    expressions = find_global(mir.insns[13].name);
    symbols = find_global(mir.insns[71].name);
    memory = find_global(mir.insns[81].name);
    memory_capacity = find_global(mir.insns[139].name);
    die_function = find_global(mir.insns[156].name);
    if (evaluation_stack == NULL || tokens == NULL ||
        expressions == NULL || symbols == NULL || memory == NULL ||
        memory_capacity == NULL || die_function == NULL ||
        !evaluation_stack->is_array ||
        evaluation_stack->elem_size != 2 ||
        evaluation_stack->array_len < 2 ||
        evaluation_stack->is_volatile ||
        evaluation_stack->pointee_is_volatile ||
        tokens->is_volatile || tokens->pointee_is_volatile ||
        expressions->is_volatile || expressions->pointee_is_volatile ||
        symbols->is_volatile || symbols->pointee_is_volatile ||
        memory->is_volatile || memory->pointee_is_volatile ||
        memory_capacity->is_volatile ||
        !die_function->is_defined ||
        die_function->proto_nargs != 1 ||
        die_function->proto_variadic ||
        type_ptr_depth(die_function->proto_types[0]) == 0)
        return 0;
    if (strcmp(mir.insns[22].name, expressions->name) != 0 ||
        strcmp(mir.insns[206].name, symbols->name) != 0 ||
        strcmp(mir.insns[164].name, memory->name) != 0 ||
        strcmp(mir.insns[299].name, memory->name) != 0 ||
        strcmp(mir.insns[274].name, memory_capacity->name) != 0 ||
        strcmp(mir.insns[291].name, die_function->name) != 0)
        return 0;
    plan->evaluation_stack = evaluation_stack;
    plan->tokens = tokens;
    plan->expressions = expressions;
    plan->symbols = symbols;
    plan->memory = memory;
    plan->memory_capacity = memory_capacity;
    plan->die_function = die_function;
    plan->expression_stride = (int)mir.insns[15].immediate;
    plan->expression_start_offset = (int)mir.insns[16].immediate;
    plan->expression_length_offset = (int)mir.insns[25].immediate;
    plan->token_stride = (int)mir.insns[18].immediate;
    plan->token_op_offset = (int)mir.insns[39].immediate;
    plan->token_argument_offset = (int)mir.insns[53].immediate;
    plan->symbol_stride = (int)mir.insns[73].immediate;
    plan->symbol_type_offset = (int)mir.insns[76].immediate;
    plan->symbol_base_offset = (int)mir.insns[83].immediate;
    plan->symbol_kind_offset = (int)mir.insns[86].immediate;
    plan->byte_type = (int)mir.insns[78].immediate;
    plan->scalar_kind = (int)mir.insns[88].immediate;
    plan->opcode_count = 17;
    plan->bounds_string = (int)mir.insns[154].immediate;
    return plan->expression_stride == 4 &&
           plan->token_stride == 4 &&
           plan->token_op_offset >= 0 &&
           plan->token_op_offset + 1 < plan->token_stride &&
           plan->token_argument_offset >= 0 &&
           plan->token_argument_offset + 1 < plan->token_stride &&
           plan->symbol_stride == 32 &&
           plan->symbol_kind_offset >= 0 &&
           plan->symbol_type_offset >= 0 &&
           plan->symbol_base_offset >= 0;
}

static int mir_match_c_run(struct MirCRun *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *state;
    struct Sym *saved_main;
    struct Sym *call_function;
    struct Sym *print_function;
    struct Sym *die_function;
    int memset_destination;
    int memset_fill;
    int memset_count;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 1854 || mir_cfg_block_count() != 118 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x61bd50a8d93257caULL ||
        second != 0x38aa2257752ad49eULL)
        return 0;
    state = find_global(mir.insns[1].name);
    saved_main = find_global(mir.insns[32].name);
    die_function = find_global(mir.insns[38].name);
    call_function = find_global(mir.insns[82].name);
    print_function = find_global(mir.insns[1838].name);
    if (state == NULL || saved_main == NULL || die_function == NULL ||
        call_function == NULL || print_function == NULL ||
        state->is_volatile || saved_main->is_volatile ||
        (state->storage != SC_GLOBAL && state->storage != SC_EXTERN) ||
        (saved_main->storage != SC_GLOBAL &&
         saved_main->storage != SC_EXTERN) ||
        !call_function->is_defined ||
        call_function->proto_nargs != 3 ||
        call_function->proto_variadic ||
        !print_function->is_defined ||
        print_function->proto_nargs != 3 ||
        print_function->proto_variadic ||
        !die_function->is_defined ||
        die_function->proto_nargs != 1 ||
        die_function->proto_variadic ||
        type_ptr_depth(die_function->proto_types[0]) == 0 ||
        !mir_call_is_memset_fastcall(
            31, &memset_destination, &memset_fill, &memset_count) ||
        strcmp(mir.insns[1653].name, call_function->name) != 0 ||
        strcmp(mir.insns[1690].name, call_function->name) != 0 ||
        strcmp(mir.insns[1847].name, die_function->name) != 0)
        return 0;
    plan->state = state;
    plan->saved_main = saved_main;
    plan->call_function = call_function;
    plan->print_function = print_function;
    plan->die_function = die_function;
    plan->state_stp_offset = (int)mir.insns[2].immediate;
    plan->state_st_offset = (int)mir.insns[4].immediate;
    plan->state_fp_offset = (int)mir.insns[8].immediate;
    plan->state_flp_offset = (int)mir.insns[12].immediate;
    plan->state_floc_offset = (int)mir.insns[14].immediate;
    plan->state_frame_size_offset = (int)mir.insns[26].immediate;
    plan->state_main_function_offset = (int)mir.insns[41].immediate;
    plan->state_functions_offset = (int)mir.insns[45].immediate;
    plan->state_code_offset = (int)mir.insns[98].immediate;
    plan->state_global_memory_offset = (int)mir.insns[333].immediate;
    plan->state_return_stack_offset = (int)mir.insns[1720].immediate;
    plan->state_strings_offset = (int)mir.insns[1824].immediate;
    plan->function_stride = (int)mir.insns[50].immediate;
    plan->function_parameter_count_offset =
        (int)mir.insns[51].immediate;
    plan->function_entry_offset = (int)mir.insns[90].immediate;
    plan->instruction_stride = (int)mir.insns[104].immediate;
    plan->instruction_op_offset = (int)mir.insns[108].immediate;
    plan->instruction_a_offset = (int)mir.insns[325].immediate;
    plan->instruction_b_offset = (int)mir.insns[1650].immediate;
    plan->frame_limit = (int)mir.insns[24].immediate;
    plan->opcode_count = 42;
    plan->no_main_string = (int)mir.insns[36].immediate;
    plan->bad_opcode_string = (int)mir.insns[1845].immediate;
    return plan->instruction_stride == 5 &&
           plan->instruction_op_offset == 0 &&
           plan->instruction_a_offset > 0 &&
           plan->instruction_a_offset + 1 < plan->instruction_stride &&
           plan->instruction_b_offset > 0 &&
           plan->instruction_b_offset + 1 < plan->instruction_stride &&
           plan->function_stride > 0 &&
           plan->function_entry_offset >= 0 &&
           plan->function_parameter_count_offset >= 0 &&
           plan->frame_limit > 0 && plan->frame_limit <= 255 &&
           memset_destination >= 0 &&
           memset_fill >= 0 &&
           memset_count >= 0;
}

static int mir_match_word_insertion_sort(
    struct MirWordInsertionSort *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 83 || mir_cfg_block_count() != 10 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x3f77f575d5fa04d3ULL ||
        second != 0x2d0d471389f6722eULL)
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->array_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->count_offset = memory_offset;
    plan->element_size = (int)mir.insns[16].immediate;
    return plan->element_size == 2 &&
           (int)mir.insns[37].immediate == plan->element_size &&
           (int)mir.insns[54].immediate == plan->element_size &&
           (int)mir.insns[57].immediate == plan->element_size &&
           (int)mir.insns[72].immediate == plan->element_size;
}

static int mir_match_visit_count(struct MirVisitCount *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 150 || mir_cfg_block_count() != 15 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        (mir.return_type & TYPE_UNSIGNED) == 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x23615dd0a2405a69ULL ||
        second != 0x2b3026fb5c17cc54ULL)
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        (memory_type & TYPE_UNSIGNED) == 0 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->offset_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        (memory_type & TYPE_UNSIGNED) == 0 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    plan->count_offset = memory_offset;
    plan->scale = (int)mir.insns[9].immediate;
    return plan->scale == 128 &&
           mir_ulong_log2_pow2((unsigned long)plan->scale) == 7 &&
           (int)mir.insns[4].immediate == plan->scale &&
           (int)mir.insns[14].immediate == 32 &&
           (int)mir.insns[20].immediate == 32 &&
           (int)mir.insns[26].immediate == 2048 &&
           (int)mir.insns[113].immediate == 2048;
}

static int mir_match_signed_word_parameter(
    int instruction, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_scalar_memory_location(
            &mir.insns[instruction], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        (memory_type & TYPE_UNSIGNED) != 0 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    *offset = memory_offset;
    return 1;
}

static int mir_match_vla_stable(struct MirVlaStable *plan)
{
    unsigned long long first;
    unsigned long long second;
    int shape_matches;

    memset(plan, 0, sizeof(*plan));
    if (!mir.has_vla || type_size(mir.return_type) != 2 ||
        (mir.return_type & TYPE_UNSIGNED) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    shape_matches =
        (mir.count == 117 && mir_cfg_block_count() == 11 &&
         first == 0xb59469e79e19fa2bULL &&
         second == 0x7240f38a761ad649ULL) ||
        (mir.count == 124 && mir_cfg_block_count() == 12 &&
         first == 0x66870004159feaa3ULL &&
         second == 0x4c802fcc0dd9d226ULL);
    if (!shape_matches ||
        !mir_match_signed_word_parameter(1, &plan->iter_offset) ||
        !mir_match_signed_word_parameter(2, &plan->count_offset))
        return 0;
    return mir.insns[32].opcode == MIR_CONST &&
           mir.insns[32].immediate == 2 &&
           mir.insns[34].opcode == MIR_VLA_ALLOC &&
           mir.insns[34].memory_size == 2;
}

static int mir_match_nested_vla_stable(
    struct MirNestedVlaStable *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 263 || mir_cfg_block_count() != 27 ||
        !mir.has_vla || type_size(mir.return_type) != 2 ||
        (mir.return_type & TYPE_UNSIGNED) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (!((first == 0x9b1d9d55314a498fULL &&
           second == 0xf8cc992b72f9401cULL) ||
          (first == 0xfa2ea213bf43f54cULL &&
           second == 0x65b23499c2ca83ecULL)) ||
        !mir_match_signed_word_parameter(1, &plan->outer_offset) ||
        !mir_match_signed_word_parameter(2, &plan->inner_offset) ||
        !mir_match_signed_word_parameter(3, &plan->count_offset))
        return 0;
    return mir.insns[46].opcode == MIR_CONST &&
           mir.insns[46].immediate == 2 &&
           mir.insns[48].opcode == MIR_VLA_ALLOC &&
           mir.insns[48].memory_size == 2 &&
           mir.insns[134].opcode == MIR_CONST &&
           mir.insns[134].immediate == 2 &&
           mir.insns[136].opcode == MIR_VLA_ALLOC &&
           mir.insns[136].memory_size == 2;
}

static int mir_match_vla_loop_result(
    struct MirVlaLoopResult *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 92 || mir_cfg_block_count() != 8 ||
        !mir.has_vla || type_size(mir.return_type) != 2 ||
        (mir.return_type & TYPE_UNSIGNED) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xb1ed346d3e5e08c4ULL ||
        second != 0x016f21e6f762d38aULL ||
        !mir_match_signed_word_parameter(1, &plan->iter_offset) ||
        !mir_match_signed_word_parameter(2, &plan->count_offset))
        return 0;
    return mir.insns[25].opcode == MIR_CONST &&
           mir.insns[25].immediate == 2 &&
           mir.insns[27].opcode == MIR_VLA_ALLOC &&
           mir.insns[27].memory_size == 2;
}

static int mir_match_file_length(struct MirFileLength *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *buffer;
    struct Sym *seek_function;
    struct Sym *eof_function;
    struct Sym *read_function;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 100 || mir_cfg_block_count() != 11 ||
        mir.has_vla || type_size(mir.return_type) != 4 ||
        (mir.return_type & TYPE_UNSIGNED) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x48b5d4537cd1df2bULL ||
        second != 0x1abe8726c6191e53ULL ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    buffer = find_global(mir.insns[27].name);
    seek_function = find_global(mir.insns[16].name);
    eof_function = find_global(mir.insns[24].name);
    read_function = find_global(mir.insns[38].name);
    if (buffer == NULL || seek_function == NULL ||
        eof_function == NULL || read_function == NULL ||
        !buffer->is_array || buffer->elem_size != 1 ||
        buffer->is_volatile || buffer->pointee_is_volatile ||
        (buffer->storage != SC_GLOBAL && buffer->storage != SC_EXTERN) ||
        strcmp(mir.insns[27].name, mir.insns[62].name) != 0 ||
        strcmp(mir.insns[16].name, mir.insns[97].name) != 0 ||
        seek_function->proto_nargs != 3 ||
        eof_function->proto_nargs != 1 ||
        read_function->proto_nargs != 4)
        return 0;
    plan->buffer = buffer;
    plan->seek_function = seek_function;
    plan->eof_function = eof_function;
    plan->read_function = read_function;
    plan->file_offset = memory_offset;
    plan->block_size = (int)mir.insns[33].immediate;
    plan->marker = (int)mir.insns[66].immediate;
    return plan->block_size > 0 && plan->block_size <= 255 &&
           buffer->array_len >= plan->block_size &&
           mir.insns[14].immediate == 0 &&
           mir.insns[30].immediate == 1 &&
           mir.insns[41].immediate == 0 &&
           mir.insns[49].immediate == 0 &&
           mir.insns[59].immediate == plan->block_size &&
           mir.insns[76].immediate == 1 &&
           mir.insns[82].immediate == 1 &&
           mir.insns[95].immediate == 0;
}

static int mir_match_wide_gcd(struct MirWideGcd *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 60 || mir_cfg_block_count() != 12 ||
        mir.has_vla || type_size(mir.return_type) != 4 ||
        (mir.return_type & TYPE_UNSIGNED) == 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x4a7040a635b8488fULL ||
        second != 0x5098ea5af2652b6aULL ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 4 ||
        (memory_type & TYPE_UNSIGNED) == 0 ||
        memory_offset < -128 || memory_offset + 3 > 127)
        return 0;
    plan->left_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 4 ||
        (memory_type & TYPE_UNSIGNED) == 0 ||
        memory_offset < -128 || memory_offset + 3 > 127)
        return 0;
    plan->right_offset = memory_offset;
    return mir.insns[4].opcode == MIR_CONST &&
           mir.insns[4].immediate == 0 &&
           mir.insns[40].opcode == MIR_CONST &&
           mir.insns[40].immediate == 0 &&
           mir.insns[51].opcode == MIR_BINARY &&
           mir.insns[51].immediate == '%';
}

static int mir_match_pointer_parameter(
    int instruction, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_scalar_memory_location(
            &mir.insns[instruction], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    *offset = memory_offset;
    return 1;
}

static int mir_match_long_parameter_at(
    int instruction, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_scalar_memory_location(
            &mir.insns[instruction], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 4 ||
        (memory_type & TYPE_UNSIGNED) != 0 ||
        memory_offset < -128 || memory_offset + 3 > 127)
        return 0;
    *offset = memory_offset;
    return 1;
}

static int mir_match_fixed_long_add(
    struct MirFixedLongAdd *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 88 || mir_cfg_block_count() != 7 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x31a2b2bb4426458cULL ||
        second != 0x2f7df92e539a5d7dULL ||
        !mir_match_pointer_parameter(1, &plan->destination_offset) ||
        !mir_match_pointer_parameter(2, &plan->source_offset))
        return 0;
    plan->count = (int)mir.insns[28].immediate + 1;
    plan->base = mir.insns[55].immediate;
    return plan->count > 0 && plan->count <= 255 &&
           plan->base > 0 && plan->base <= 0x7fffffffL &&
           mir.insns[4].immediate == 0 &&
           mir.insns[38].immediate == 0 &&
           mir.insns[44].memory_size == 4 &&
           mir.insns[44].immediate == 4 &&
           mir.insns[48].memory_size == 4 &&
           mir.insns[48].immediate == 4 &&
           mir.insns[60].immediate == plan->base &&
           mir.insns[65].immediate == 1 &&
           mir.insns[71].immediate == 0 &&
           mir.insns[77].memory_size == 4 &&
           mir.insns[77].immediate == 4 &&
           mir.insns[83].immediate == 1;
}

static int mir_match_fixed_long_zero(
    struct MirFixedLongZero *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 40 || mir_cfg_block_count() != 5 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        (mir.return_type & TYPE_UNSIGNED) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x41bd34110fffb51dULL ||
        second != 0xe0ae1271279f341fULL ||
        !mir_match_pointer_parameter(1, &plan->array_offset))
        return 0;
    plan->count = (int)mir.insns[17].immediate;
    plan->stride = (int)mir.insns[22].immediate;
    return plan->count > 0 && plan->count <= 255 &&
           plan->stride == 4 &&
           mir.insns[2].immediate == 0 &&
           mir.insns[23].memory_size == 4 &&
           mir.insns[25].immediate == 0 &&
           mir.insns[28].immediate == 0 &&
           mir.insns[33].immediate == 1 &&
           mir.insns[38].immediate == 1;
}

static int mir_match_fixed_long_mul_div(
    struct MirFixedLongMulDiv *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 136 || mir_cfg_block_count() != 7 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xc30709100963e52bULL ||
        second != 0x671af59e92df8930ULL ||
        !mir_match_pointer_parameter(1, &plan->array_offset) ||
        !mir_match_long_parameter_at(2, &plan->multiplier_offset) ||
        !mir_match_long_parameter_at(3, &plan->divisor_offset))
        return 0;
    plan->count = (int)mir.insns[29].immediate + 1;
    plan->base = mir.insns[58].immediate;
    return plan->count > 0 && plan->count <= 255 &&
           plan->base > 0 && plan->base <= 0x7fffffffL &&
           mir.insns[5].immediate == 0 &&
           mir.insns[8].immediate == 0 &&
           mir.insns[41].immediate == 0 &&
           mir.insns[47].memory_size == 4 &&
           mir.insns[47].immediate == 4 &&
           mir.insns[50].immediate == '*' &&
           mir.insns[52].immediate == '+' &&
           mir.insns[56].memory_size == 4 &&
           mir.insns[56].immediate == 4 &&
           mir.insns[59].immediate == '%' &&
           mir.insns[62].immediate == plan->base &&
           mir.insns[63].immediate == '/' &&
           mir.insns[69].immediate == 1 &&
           mir.insns[83].immediate == 0 &&
           mir.insns[103].immediate == plan->count &&
           mir.insns[108].immediate == plan->base &&
           mir.insns[112].memory_size == 4 &&
           mir.insns[112].immediate == 4 &&
           mir.insns[114].immediate == '+' &&
           mir.insns[118].memory_size == 4 &&
           mir.insns[118].immediate == 4 &&
           mir.insns[121].immediate == '/' &&
           mir.insns[125].immediate == '%' &&
           mir.insns[131].immediate == 1;
}

static int mir_match_long_spigot(struct MirLongSpigot *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *array;
    struct Sym *print_function;
    struct Sym *flush_function;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 139 || mir_cfg_block_count() != 11 ||
        mir.has_vla || type_size(mir.return_type) != 2)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xdc160e68cf90d6ceULL ||
        second != 0xe8b2a0fc5a155383ULL)
        return 0;
    array = find_global(mir.insns[15].name);
    print_function = find_global(mir.insns[113].name);
    flush_function = find_global(mir.insns[117].name);
    if (array == NULL || print_function == NULL || flush_function == NULL ||
        !array->is_array || array->elem_size != 4 ||
        array->is_volatile || array->pointee_is_volatile ||
        (array->storage != SC_GLOBAL && array->storage != SC_EXTERN) ||
        strcmp(mir.insns[15].name, mir.insns[52].name) != 0 ||
        strcmp(mir.insns[15].name, mir.insns[71].name) != 0 ||
        strcmp(mir.insns[113].name, mir.insns[136].name) != 0 ||
        print_function->proto_nargs < 1 ||
        flush_function->proto_nargs != 1)
        return 0;
    plan->array = array;
    plan->print_function = print_function;
    plan->flush_function = flush_function;
    plan->outer_start = (int)mir.insns[29].immediate;
    plan->array_count = (int)mir.insns[12].immediate + 1;
    plan->initial_value = mir.insns[19].immediate;
    plan->radix = mir.insns[57].immediate;
    plan->outer_step = (int)mir.insns[128].immediate;
    plan->digit_format_string = (int)mir.insns[104].immediate;
    plan->done_string = (int)mir.insns[134].immediate;
    dcc_copy_str(
        plan->digit_print_name, sizeof(plan->digit_print_name),
        mir.insns[113].base_name[0] != 0
            ? mir.insns[113].base_name
            : asm_name_for(sym_asm_name(print_function)));
    dcc_copy_str(
        plan->done_print_name, sizeof(plan->done_print_name),
        mir.insns[136].base_name[0] != 0
            ? mir.insns[136].base_name
            : asm_name_for(sym_asm_name(print_function)));
    return plan->array_count > 1 &&
           plan->array_count <= 16384 &&
           array->array_len >= plan->array_count &&
           plan->outer_start == plan->array_count - 1 &&
           plan->outer_step > 0 &&
           plan->radix > 1 &&
           plan->radix <= 0x7fffffffL &&
           mir.insns[2].immediate == 0 &&
           mir.insns[5].immediate == 0 &&
           mir.insns[17].memory_size == 4 &&
           mir.insns[17].immediate == 4 &&
           mir.insns[23].immediate == 1 &&
           mir.insns[37].immediate == 0 &&
           mir.insns[41].immediate == 0 &&
           mir.insns[64].immediate == 2 &&
           mir.insns[67].immediate == 1 &&
           mir.insns[73].memory_size == 4 &&
           mir.insns[73].immediate == 4 &&
           mir.insns[76].immediate == '%' &&
           mir.insns[80].immediate == '/' &&
           mir.insns[84].immediate == 1 &&
           mir.insns[89].immediate == 0 &&
           mir.insns[97].immediate == '*' &&
           mir.insns[109].immediate == plan->radix &&
           mir.insns[114].immediate == 1 &&
           mir.insns[120].immediate == plan->radix &&
           mir.insns[137].immediate == 0;
}

static int mir_match_typed_array_conditions(
    struct MirTypedArrayConditions *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_type;
    int memory_storage;
    int memory_offset;
    int expected_count;
    int instruction;

    memset(plan, 0, sizeof(*plan));
    if (mir.has_vla || mir_cfg_block_count() != 72)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first == 0xde96db7af2c0c929ULL &&
        second == 0x1ae578ceb26eb4ceULL) {
        plan->element_size = 1;
        plan->is_unsigned = 0;
        expected_count = 495;
    } else if (first == 0xfe136c3a42288da3ULL &&
               second == 0xeceef943e903e83fULL) {
        plan->element_size = 1;
        plan->is_unsigned = 1;
        expected_count = 496;
    } else if (first == 0x997eba211f21dd6cULL &&
               second == 0x9e768ddbeece5b89ULL) {
        plan->element_size = 2;
        plan->is_unsigned = 0;
        expected_count = 476;
    } else if (first == 0x0216bd0b2eeeaf34ULL &&
               second == 0x8742078c3f216010ULL) {
        plan->element_size = 2;
        plan->is_unsigned = 1;
        expected_count = 481;
    } else if (first == 0x2da786f06e6d53a4ULL &&
               second == 0xa6da5bad3c16d329ULL) {
        plan->element_size = 4;
        plan->is_unsigned = 0;
        expected_count = 480;
    } else if (first == 0x69e46b7825d8fa2cULL &&
               second == 0xb045f9da575f8a64ULL) {
        plan->element_size = 4;
        plan->is_unsigned = 1;
        expected_count = 481;
    } else {
        return 0;
    }
    if (mir.count != expected_count ||
        type_size(mir.return_type) != plan->element_size ||
        ((mir.return_type & TYPE_UNSIGNED) != 0) != plan->is_unsigned ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != plan->element_size ||
        ((memory_type & TYPE_UNSIGNED) != 0) != plan->is_unsigned ||
        memory_offset < -128 ||
        memory_offset + plan->element_size - 1 > 127)
        return 0;
    plan->minimum_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != plan->element_size ||
        ((memory_type & TYPE_UNSIGNED) != 0) != plan->is_unsigned ||
        memory_offset < -128 ||
        memory_offset + plan->element_size - 1 > 127)
        return 0;
    plan->maximum_offset = memory_offset;
    plan->count = 50;
    if (mir.insns[4].opcode != MIR_CONST ||
        mir.insns[4].immediate != 0 ||
        mir.insns[11].opcode != MIR_CONST ||
        mir.insns[11].immediate != plan->count * plan->element_size ||
        mir.insns[12].opcode != MIR_CONST ||
        mir.insns[12].immediate != plan->element_size ||
        mir.insns[mir.count - 4].opcode != MIR_CONST ||
        mir.insns[mir.count - 4].immediate != 10 ||
        mir.insns[mir.count - 3].opcode != MIR_INDEX_ADDRESS ||
        mir.insns[mir.count - 3].memory_size != plan->element_size ||
        mir.insns[mir.count - 3].immediate != plan->element_size ||
        mir.insns[mir.count - 2].opcode != MIR_LOAD_INDIRECT ||
        mir.insns[mir.count - 2].memory_size != plan->element_size ||
        mir.insns[mir.count - 1].opcode != MIR_RETURN)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_INDEX_ADDRESS &&
            (mir.insns[instruction].memory_size != plan->element_size ||
             mir.insns[instruction].immediate != plan->element_size))
            return 0;
    return 1;
}

static int mir_match_wide_factorial(struct MirWideFactorial *plan)
{
    const int expected_opcodes[20] = {
        MIR_LABEL, MIR_PARAM, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY,
        MIR_BRANCH_FALSE, MIR_NOP, MIR_CONST, MIR_RETURN, MIR_LABEL,
        MIR_NOP, MIR_NOP, MIR_NOP, MIR_CONST, MIR_BINARY, MIR_ARG,
        MIR_CALL, MIR_BINARY, MIR_RETURN
    };
    const int parameter_locations[4] = {1, 3, 11, 12};
    const struct MirInsn *call = &mir.insns[17];
    struct Sym *callee;
    int memory_type;
    int memory_storage;
    int memory_offset;
    int comparison_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 20 || mir_cfg_block_count() != 2 ||
        mir.has_vla || type_size(mir.return_type) != 4 ||
        (mir.return_type & TYPE_UNSIGNED) == 0 ||
        !mir_match_spilled_opcodes(expected_opcodes, 20) ||
        !mir_match_spilled_unique_labels())
        return mir_machine_reject("wide-factorial-native", "shape");
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset))
        return mir_machine_reject(
            "wide-factorial-native", "parameter-location");
    if (memory_storage != SC_PARAM ||
        memory_offset < -128 || memory_offset + 3 > 127)
        return mir_machine_reject(
            "wide-factorial-native", "parameter-storage");
    if (memory_type != mir.return_type ||
        !mir_match_spilled_integer_type(memory_type, 4, 1))
        return mir_machine_reject(
            "wide-factorial-native", "parameter-type");
    if (!mir_match_spilled_location_group(
            parameter_locations, 4, SC_PARAM, memory_type))
        return mir_machine_reject(
            "wide-factorial-native", "parameter-uses");

    comparison_type = mir.insns[5].type;
    if (!mir_match_spilled_integer_type(comparison_type, 2, 0) ||
        !mir_machine_constant_equals(mir.insns[4].dst, 0) ||
        mir.insns[4].type != memory_type ||
        !mir_match_spilled_binary(
            5, TOK_EQ, 4, 1, comparison_type, memory_type) ||
        !mir_match_spilled_branch(6, 5, 10) ||
        !mir_machine_constant_equals(mir.insns[8].dst, 1) ||
        mir.insns[8].type != memory_type ||
        !mir_match_spilled_return(9, 8) ||
        !mir_machine_constant_equals(mir.insns[14].dst, 1) ||
        mir.insns[14].type != memory_type ||
        !mir_match_spilled_binary(
            15, '-', 1, 14, memory_type, memory_type) ||
        !mir_match_spilled_argument(16, 17, 0, 15))
        return mir_machine_reject("wide-factorial-native", "prefix");

    callee = find_global(call->name);
    if (callee == NULL || callee->is_funcptr ||
        callee->is_noreturn || !callee->is_defined ||
        !callee->has_proto || callee->proto_variadic ||
        callee->proto_nargs != 1 ||
        callee->type != memory_type ||
        callee->proto_types[0] != memory_type ||
        call->type != memory_type ||
        call->memory_flags != 0 ||
        strcmp(call->name, mir.name) != 0 ||
        !mir_match_spilled_binary(
            18, '*', 1, 17, memory_type, memory_type) ||
        !mir_match_spilled_return(19, 18))
        return mir_machine_reject("wide-factorial-native", "recursion");
    plan->parameter_offset = memory_offset;
    return 1;
}

static int mir_match_float_parameter_at(
    int instruction, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_scalar_memory_location(
            &mir.insns[instruction], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        !type_is_float(memory_type) ||
        type_size(memory_type) != 4 ||
        memory_offset < -128 || memory_offset + 3 > 127)
        return 0;
    *offset = memory_offset;
    return 1;
}

static int mir_match_float_exp(struct MirFloatExp *plan);

static int mir_match_float_exp_opcodes(int *promoted_n)
{
    const int expected[160] = {
        MIR_LABEL, MIR_PARAM, MIR_NOP, MIR_FLOAT_CONST, MIR_BINARY,
        MIR_BRANCH_FALSE, MIR_FLOAT_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL,
        MIR_LOAD, MIR_FLOAT_CONST, MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE,
        MIR_FLOAT_CONST, MIR_RETURN, MIR_NOP, MIR_LABEL, MIR_LOAD,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_UNARY, MIR_NOP, MIR_STORE, MIR_LOAD,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_LOAD,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_NOP, MIR_UNARY, MIR_BINARY,
        MIR_BRANCH_FALSE, MIR_LABEL, MIR_CONST, MIR_JUMP, MIR_LABEL,
        MIR_CONST, MIR_LABEL, MIR_PHI, MIR_BRANCH_FALSE, MIR_NOP, MIR_CONST,
        MIR_BINARY, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_LOAD,
        MIR_UNARY, MIR_FLOAT_CONST, MIR_BINARY, MIR_BINARY, MIR_NOP,
        MIR_STORE, MIR_FLOAT_CONST, MIR_NOP, MIR_STORE, MIR_FLOAT_CONST,
        MIR_NOP, MIR_STORE, MIR_NOP, MIR_CONST, MIR_STORE, MIR_LABEL, MIR_NOP,
        MIR_NOP, MIR_NOP, MIR_PHI, MIR_PHI, MIR_PHI, MIR_NOP, MIR_CONST,
        MIR_UNARY, MIR_BINARY, MIR_BRANCH_FALSE, MIR_NOP, MIR_NOP, MIR_BINARY,
        MIR_NOP, MIR_UNARY, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_NOP, MIR_NOP,
        MIR_BINARY, MIR_NOP, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_NOP, MIR_CONST,
        MIR_BINARY, MIR_STORE, MIR_JUMP, MIR_LABEL, MIR_LOAD, MIR_CONST,
        MIR_BINARY, MIR_BRANCH_FALSE, MIR_LABEL, MIR_LABEL, MIR_NOP, MIR_NOP,
        MIR_NOP, MIR_PHI, MIR_NOP, MIR_NOP, MIR_LOAD, MIR_CONST, MIR_BINARY,
        MIR_BRANCH_FALSE, MIR_NOP, MIR_FLOAT_CONST, MIR_BINARY, MIR_NOP,
        MIR_STORE, MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_STORE, MIR_NOP,
        MIR_LABEL, MIR_JUMP, MIR_LABEL, MIR_NOP, MIR_JUMP, MIR_LABEL,
        MIR_LABEL, MIR_NOP, MIR_NOP, MIR_NOP, MIR_PHI, MIR_NOP, MIR_NOP,
        MIR_LOAD, MIR_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_NOP,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_CONST,
        MIR_BINARY, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL,
        MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_RETURN
    };
    int promoted;
    int instruction;

    if (mir.count != 160)
        return 0;
    promoted = mir.insns[37].opcode == MIR_NOP;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        int opcode = expected[instruction];

        if (promoted) {
            switch (instruction) {
            case 37: case 42: case 43: case 100:
            case 121: case 148:
                opcode = MIR_NOP;
                break;
            case 40:
                opcode = MIR_JUMP;
                break;
            case 51: case 112: case 139:
                opcode = MIR_PHI;
                break;
            default:
                break;
            }
        }
        if (mir.insns[instruction].opcode != opcode)
            return 0;
    }
    *promoted_n = promoted;
    return 1;
}

static int mir_match_float_exp_locations(
    int float_type, int int_type, int byte_type)
{
    const int parameter_locations[] = {
        1, 2, 10, 19, 25, 29, 50, 68, 106, 133
    };
    const int n_locations[] = {
        24, 32, 44, 47, 51, 69, 100, 107,
        112, 121, 124, 134, 139, 148, 151
    };
    const int r_locations[] = {57, 70, 80, 108, 135};
    const int sum_locations[] = {
        60, 71, 87, 90, 91, 109, 116,
        119, 120, 136, 143, 146, 147, 158
    };
    const int term_locations[] = {
        63, 72, 79, 85, 86, 88, 110, 137
    };
    const int i_locations[] = {
        66, 73, 74, 82, 94, 97, 111, 138
    };
    const int distinct_locations[] = {24, 57, 60, 63, 66};

    return mir_match_spilled_location_group(
               parameter_locations,
               sizeof(parameter_locations) / sizeof(parameter_locations[0]),
               SC_PARAM, float_type) &&
           mir_match_spilled_location_group(
               n_locations,
               sizeof(n_locations) / sizeof(n_locations[0]),
               SC_LOCAL, int_type) &&
           mir_match_spilled_location_group(
               r_locations,
               sizeof(r_locations) / sizeof(r_locations[0]),
               SC_LOCAL, float_type) &&
           mir_match_spilled_location_group(
               sum_locations,
               sizeof(sum_locations) / sizeof(sum_locations[0]),
               SC_LOCAL, float_type) &&
           mir_match_spilled_location_group(
               term_locations,
               sizeof(term_locations) / sizeof(term_locations[0]),
               SC_LOCAL, float_type) &&
           mir_match_spilled_location_group(
               i_locations,
               sizeof(i_locations) / sizeof(i_locations[0]),
               SC_LOCAL, byte_type) &&
           mir_match_spilled_distinct_locations(
               distinct_locations,
               sizeof(distinct_locations) /
                   sizeof(distinct_locations[0])) &&
           mir_machine_unobservable_local_store(&mir.insns[24]) &&
           mir_machine_unobservable_local_store(&mir.insns[57]) &&
           mir_machine_unobservable_local_store(&mir.insns[60]) &&
           mir_machine_unobservable_local_store(&mir.insns[63]) &&
           mir_machine_unobservable_local_store(&mir.insns[66]);
}

static int mir_match_float_exp_constants(
    struct MirFloatExp *plan, int promoted_n)
{
    plan->upper_bits = (unsigned long)mir.insns[3].immediate;
    plan->maximum_bits = (unsigned long)mir.insns[6].immediate;
    plan->ln2_bits = (unsigned long)mir.insns[20].immediate;
    plan->one_bits = (unsigned long)mir.insns[58].immediate;
    plan->two_bits = (unsigned long)mir.insns[117].immediate;
    plan->term_limit = (int)mir.insns[75].immediate;
    if (plan->upper_bits == 0 || plan->maximum_bits == 0 ||
        plan->ln2_bits == 0 || plan->one_bits == 0 ||
        plan->two_bits == 0 ||
        plan->term_limit <= 0 || plan->term_limit > 255 ||
        !mir_match_spilled_float_constant(3, plan->upper_bits) ||
        !mir_match_spilled_float_constant(6, plan->maximum_bits) ||
        !mir_match_spilled_float_constant(11, plan->upper_bits) ||
        !mir_match_spilled_float_constant(15, 0) ||
        !mir_match_spilled_float_constant(20, plan->ln2_bits) ||
        !mir_match_spilled_float_constant(26, 0) ||
        !mir_match_spilled_float_constant(30, plan->ln2_bits) ||
        !mir_match_spilled_float_constant(53, plan->ln2_bits) ||
        !mir_match_spilled_float_constant(58, plan->one_bits) ||
        !mir_match_spilled_float_constant(61, plan->one_bits) ||
        !mir_match_spilled_float_constant(117, plan->two_bits) ||
        !mir_match_spilled_float_constant(144, plan->two_bits) ||
        !mir_machine_constant_equals(mir.insns[45].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[65].dst, 1) ||
        !mir_machine_constant_equals(
            mir.insns[75].dst, plan->term_limit) ||
        !mir_machine_constant_equals(mir.insns[95].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[101].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[113].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[122].dst, 1) ||
        !mir_machine_constant_equals(mir.insns[140].dst, 0) ||
        !mir_machine_constant_equals(mir.insns[149].dst, 1))
        return 0;
    return promoted_n ||
           (mir_machine_constant_equals(mir.insns[37].dst, 1) &&
            mir_machine_constant_equals(mir.insns[40].dst, 0));
}

static int mir_match_float_exp_graph(
    int promoted_n, int float_type, int int_type,
    int byte_type, int comparison_type)
{
    int n_after_rounding = 51;

    if (!mir_match_spilled_binary(
            4, '>', 1, 3, comparison_type, float_type) ||
        !mir_match_spilled_branch(5, 4, 9) ||
        !mir_match_spilled_return(7, 6) ||
        !mir_match_spilled_unary(12, '-', 11, float_type) ||
        !mir_match_spilled_binary(
            13, '<', 10, 12, comparison_type, float_type) ||
        !mir_match_spilled_branch(14, 13, 18) ||
        !mir_match_spilled_return(16, 15) ||
        !mir_match_spilled_binary(
            21, '/', 19, 20, float_type, float_type) ||
        !mir_match_spilled_unary(22, 0, 21, int_type) ||
        mir.insns[24].src1 != mir.insns[22].dst ||
        !mir_match_spilled_binary(
            27, '<', 25, 26, comparison_type, float_type) ||
        !mir_match_spilled_branch(28, 27, 39) ||
        !mir_match_spilled_binary(
            31, '/', 29, 30, float_type, float_type) ||
        !mir_match_spilled_unary(33, 0, 22, float_type) ||
        !mir_match_spilled_binary(
            34, TOK_NE, 31, 33, comparison_type, float_type) ||
        !mir_match_spilled_branch(35, 34, 39) ||
        !mir_match_spilled_binary(
            46, '-', 22, 45, int_type, int_type) ||
        mir.insns[47].src1 != mir.insns[46].dst)
        return mir_machine_reject(
            "float-exp-native-graph", "prefix");

    if (promoted_n) {
        if (!mir_match_spilled_jump(38, 41) ||
            !mir_match_spilled_jump(40, 49) ||
            !mir_match_spilled_phi(
                51, 22, 46, 39, 41, int_type))
            return mir_machine_reject(
                "float-exp-native-graph", "promoted-rounding");
    } else if (!mir_match_spilled_jump(38, 41) ||
               !mir_match_spilled_phi(
                   42, 37, 40, 36, 39, 0) ||
               !mir_match_spilled_branch(43, 42, 49)) {
        return mir_machine_reject(
            "float-exp-native-graph", "rounding");
    }

    if (!mir_match_spilled_unary(
            52, 0, n_after_rounding, float_type) ||
        !mir_match_spilled_binary(
            54, '*', 52, 53, float_type, float_type) ||
        !mir_match_spilled_binary(
            55, '-', 50, 54, float_type, float_type) ||
        mir.insns[57].src1 != mir.insns[55].dst ||
        mir.insns[60].src1 != mir.insns[58].dst ||
        mir.insns[63].src1 != mir.insns[61].dst ||
        mir.insns[66].src1 != mir.insns[65].dst ||
        !mir_match_spilled_phi(
            71, 58, 89, 49, 93, float_type) ||
        !mir_match_spilled_phi(
            72, 61, 84, 49, 93, float_type) ||
        !mir_match_spilled_phi(
            73, 65, 96, 49, 93, byte_type) ||
        !mir_match_spilled_unary(76, 0, 73, int_type) ||
        !mir_match_spilled_binary(
            77, TOK_LE, 76, 75, comparison_type, int_type) ||
        !mir_match_spilled_branch(78, 77, 99) ||
        !mir_match_spilled_binary(
            81, '*', 72, 55, float_type, float_type) ||
        !mir_match_spilled_unary(83, 0, 73, float_type) ||
        !mir_match_spilled_binary(
            84, '/', 81, 83, float_type, float_type) ||
        mir.insns[86].src1 != mir.insns[84].dst ||
        !mir_match_spilled_binary(
            89, '+', 71, 84, float_type, float_type) ||
        mir.insns[91].src1 != mir.insns[89].dst ||
        !mir_match_spilled_binary(
            96, '+', 73, 95, byte_type, byte_type) ||
        mir.insns[97].src1 != mir.insns[96].dst ||
        !mir_match_spilled_jump(98, 67))
        return mir_machine_reject(
            "float-exp-native-graph", "series");

    if (promoted_n) {
        if (!mir_match_spilled_binary(
                102, TOK_GE, n_after_rounding, 101,
                comparison_type, int_type) ||
            !mir_match_spilled_phi(
                112, n_after_rounding, 123, 104, 126, int_type) ||
            !mir_match_spilled_binary(
                114, '>', 112, 113, comparison_type, int_type) ||
            !mir_match_spilled_binary(
                123, '-', 112, 122, int_type, int_type) ||
            !mir_match_spilled_phi(
                139, n_after_rounding, 150, 131, 153, int_type) ||
            !mir_match_spilled_binary(
                141, '<', 139, 140, comparison_type, int_type) ||
            !mir_match_spilled_binary(
                150, '+', 139, 149, int_type, int_type))
            return mir_machine_reject(
                "float-exp-native-graph", "promoted-scaling");
    } else if (!mir_match_spilled_binary(
                   102, TOK_GE, 100, 101,
                   comparison_type, int_type) ||
               !mir_match_spilled_binary(
                   114, '>', 112, 113,
                   comparison_type, int_type) ||
               !mir_match_spilled_binary(
                   123, '-', 121, 122, int_type, int_type) ||
               !mir_match_spilled_binary(
                   141, '<', 139, 140,
                   comparison_type, int_type) ||
               !mir_match_spilled_binary(
                   150, '+', 148, 149, int_type, int_type)) {
        return mir_machine_reject(
            "float-exp-native-graph", "scaling-values");
    }

    if (!mir_match_spilled_branch(103, 102, 131) ||
        !mir_match_spilled_phi(
            109, 71, 118, 104, 126, float_type) ||
        !mir_match_spilled_branch(115, 114, 157) ||
        !mir_match_spilled_binary(
            118, '*', 109, 117, float_type, float_type) ||
        mir.insns[120].src1 != mir.insns[118].dst ||
        mir.insns[124].src1 != mir.insns[123].dst ||
        !mir_match_spilled_jump(127, 105) ||
        !mir_match_spilled_jump(130, 157) ||
        !mir_match_spilled_phi(
            136, 71, 145, 131, 153, float_type) ||
        !mir_match_spilled_branch(142, 141, 155) ||
        !mir_match_spilled_binary(
            145, '/', 136, 144, float_type, float_type) ||
        mir.insns[147].src1 != mir.insns[145].dst ||
        mir.insns[151].src1 != mir.insns[150].dst ||
        !mir_match_spilled_jump(154, 132) ||
        !mir_match_spilled_return(159, 158))
        return mir_machine_reject(
            "float-exp-native-graph", "scaling-graph");
    return 1;
}

static int mir_match_float_exp(struct MirFloatExp *plan)
{
    int promoted_n;
    int float_type;
    int int_type;
    int byte_type;
    int comparison_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 160 || mir_cfg_block_count() != 19 ||
        mir.has_vla ||
        !mir_match_spilled_float_type(mir.return_type) ||
        !mir_match_spilled_unique_labels() ||
        !mir_match_float_exp_opcodes(&promoted_n) ||
        !mir_match_float_parameter_at(
            1, &plan->parameter_offset))
        return mir_machine_reject("float-exp-native", "shape");
    float_type = mir.return_type;
    int_type = mir.insns[22].type;
    byte_type = mir.insns[65].type;
    comparison_type = mir.insns[4].type;
    if (mir.insns[1].type != float_type ||
        !mir_match_spilled_integer_type(int_type, 2, 0) ||
        !mir_match_spilled_integer_type(byte_type, 1, 1) ||
        !mir_match_spilled_integer_type(comparison_type, 2, 0))
        return mir_machine_reject("float-exp-native", "types");
    if (!mir_match_float_exp_locations(
            float_type, int_type, byte_type))
        return mir_machine_reject("float-exp-native", "locations");
    if (!mir_match_float_exp_constants(plan, promoted_n))
        return mir_machine_reject("float-exp-native", "constants");
    if (!mir_match_float_exp_graph(
            promoted_n, float_type, int_type,
            byte_type, comparison_type))
        return mir_machine_reject("float-exp-native", "graph");
    return 1;
}

static int mir_match_float_log(struct MirFloatLog *plan);

static int mir_match_float_log_opcodes(void)
{
    const int expected[139] = {
        MIR_LABEL, MIR_PARAM, MIR_NOP, MIR_FLOAT_CONST, MIR_BINARY,
        MIR_BRANCH_FALSE, MIR_FLOAT_CONST, MIR_UNARY, MIR_RETURN, MIR_NOP,
        MIR_LABEL, MIR_CONST, MIR_NOP, MIR_STORE, MIR_LOAD, MIR_NOP,
        MIR_STORE, MIR_NOP, MIR_FLOAT_CONST, MIR_BINARY, MIR_BRANCH_FALSE,
        MIR_LABEL, MIR_LABEL, MIR_NOP, MIR_PHI, MIR_PHI, MIR_NOP,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_NOP,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_NOP, MIR_CONST,
        MIR_BINARY, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL,
        MIR_NOP, MIR_JUMP, MIR_LABEL, MIR_LABEL, MIR_NOP, MIR_PHI, MIR_PHI,
        MIR_NOP, MIR_FLOAT_CONST, MIR_BINARY, MIR_BRANCH_FALSE, MIR_NOP,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_NOP, MIR_CONST,
        MIR_BINARY, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_JUMP, MIR_LABEL,
        MIR_NOP, MIR_LABEL, MIR_LOAD, MIR_FLOAT_CONST, MIR_BINARY, MIR_LOAD,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_NOP,
        MIR_NOP, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_FLOAT_CONST, MIR_NOP,
        MIR_STORE, MIR_NOP, MIR_NOP, MIR_STORE, MIR_CONST, MIR_NOP,
        MIR_STORE, MIR_LABEL, MIR_NOP, MIR_NOP, MIR_NOP, MIR_NOP, MIR_NOP,
        MIR_PHI, MIR_PHI, MIR_PHI, MIR_NOP, MIR_CONST, MIR_BINARY,
        MIR_BRANCH_FALSE, MIR_NOP, MIR_NOP, MIR_NOP, MIR_UNARY, MIR_BINARY,
        MIR_BINARY, MIR_NOP, MIR_STORE, MIR_NOP, MIR_NOP, MIR_BINARY,
        MIR_NOP, MIR_STORE, MIR_NOP, MIR_LABEL, MIR_NOP, MIR_CONST,
        MIR_BINARY, MIR_NOP, MIR_STORE, MIR_JUMP, MIR_LABEL, MIR_NOP,
        MIR_FLOAT_CONST, MIR_BINARY, MIR_NOP, MIR_STORE, MIR_NOP, MIR_LOAD,
        MIR_UNARY, MIR_FLOAT_CONST, MIR_BINARY, MIR_BINARY, MIR_RETURN
    };

    return mir_match_spilled_opcodes(expected, 139);
}

static int mir_match_float_log_locations(
    int float_type, int int_type)
{
    const int parameter_locations[] = {1, 2, 14, 23, 47, 93};
    const int n_locations[] = {
        13, 12, 24, 35, 38, 48, 59, 62, 94, 133
    };
    const int m_locations[] = {
        16, 15, 17, 25, 26, 30, 33, 34,
        49, 50, 54, 57, 58, 69, 72, 95
    };
    const int y_locations[] = {77, 76, 78, 79, 86, 96};
    const int y2_locations[] = {82, 81, 97, 114};
    const int sum_locations[] = {
        85, 84, 98, 105, 111, 112, 127, 130, 131, 132
    };
    const int term_locations[] = {88, 87, 99, 106, 113, 116, 117};
    const int i_locations[] = {91, 90, 100, 101, 107, 120, 123, 124};
    const int distinct_locations[] = {13, 16, 77, 82, 85, 88, 91};

    return mir_match_spilled_location_group(
               parameter_locations,
               sizeof(parameter_locations) / sizeof(parameter_locations[0]),
               SC_PARAM, float_type) &&
           mir_match_spilled_location_group(
               n_locations, sizeof(n_locations) / sizeof(n_locations[0]),
               SC_LOCAL, int_type) &&
           mir_match_spilled_location_group(
               m_locations, sizeof(m_locations) / sizeof(m_locations[0]),
               SC_LOCAL, float_type) &&
           mir_match_spilled_location_group(
               y_locations, sizeof(y_locations) / sizeof(y_locations[0]),
               SC_LOCAL, float_type) &&
           mir_match_spilled_location_group(
               y2_locations, sizeof(y2_locations) / sizeof(y2_locations[0]),
               SC_LOCAL, float_type) &&
           mir_match_spilled_location_group(
               sum_locations,
               sizeof(sum_locations) / sizeof(sum_locations[0]),
               SC_LOCAL, float_type) &&
           mir_match_spilled_location_group(
               term_locations,
               sizeof(term_locations) / sizeof(term_locations[0]),
               SC_LOCAL, float_type) &&
           mir_match_spilled_location_group(
               i_locations, sizeof(i_locations) / sizeof(i_locations[0]),
               SC_LOCAL, int_type) &&
           mir_match_spilled_distinct_locations(
               distinct_locations,
               sizeof(distinct_locations) /
                   sizeof(distinct_locations[0])) &&
           mir_machine_unobservable_local_store(&mir.insns[13]) &&
           mir_machine_unobservable_local_store(&mir.insns[16]) &&
           mir_machine_unobservable_local_store(&mir.insns[77]) &&
           mir_machine_unobservable_local_store(&mir.insns[82]) &&
           mir_machine_unobservable_local_store(&mir.insns[85]) &&
           mir_machine_unobservable_local_store(&mir.insns[88]) &&
           mir_machine_unobservable_local_store(&mir.insns[91]);
}

static int mir_match_float_log_constants(struct MirFloatLog *plan)
{
    plan->maximum_bits = (unsigned long)mir.insns[6].immediate;
    plan->one_bits = (unsigned long)mir.insns[18].immediate;
    plan->two_bits = (unsigned long)mir.insns[27].immediate;
    plan->ln2_bits = (unsigned long)mir.insns[135].immediate;
    plan->term_limit = (int)mir.insns[102].immediate;
    plan->term_step = (int)mir.insns[121].immediate;
    return plan->maximum_bits != 0 &&
           plan->one_bits != 0 &&
           plan->two_bits != 0 &&
           plan->ln2_bits != 0 &&
           plan->term_limit > 0 && plan->term_limit <= 255 &&
           plan->term_step > 0 && plan->term_step <= 255 &&
           mir_match_spilled_float_constant(3, 0) &&
           mir_match_spilled_float_constant(6, plan->maximum_bits) &&
           mir_match_spilled_float_constant(18, plan->one_bits) &&
           mir_match_spilled_float_constant(27, plan->two_bits) &&
           mir_match_spilled_float_constant(31, plan->two_bits) &&
           mir_match_spilled_float_constant(51, plan->one_bits) &&
           mir_match_spilled_float_constant(55, plan->two_bits) &&
           mir_match_spilled_float_constant(70, plan->one_bits) &&
           mir_match_spilled_float_constant(73, plan->one_bits) &&
           mir_match_spilled_float_constant(83, 0) &&
           mir_match_spilled_float_constant(128, plan->two_bits) &&
           mir_match_spilled_float_constant(135, plan->ln2_bits) &&
           mir_machine_constant_equals(mir.insns[11].dst, 0) &&
           mir_machine_constant_equals(mir.insns[36].dst, 1) &&
           mir_machine_constant_equals(mir.insns[60].dst, 1) &&
           mir_machine_constant_equals(mir.insns[89].dst, 1) &&
           mir_machine_constant_equals(
               mir.insns[102].dst, plan->term_limit) &&
           mir_machine_constant_equals(
               mir.insns[121].dst, plan->term_step);
}

static int mir_match_float_log_graph(
    int float_type, int int_type, int comparison_type)
{
    return
        mir_match_spilled_binary(
            4, TOK_LE, 1, 3, comparison_type, float_type) &&
        mir_match_spilled_branch(5, 4, 10) &&
        mir_match_spilled_unary(7, '-', 6, float_type) &&
        mir_match_spilled_return(8, 7) &&
        mir.insns[13].src1 == mir.insns[11].dst &&
        mir.insns[16].src1 == mir.insns[14].dst &&
        mir_match_spilled_binary(
            19, TOK_GE, 14, 18, comparison_type, float_type) &&
        mir_match_spilled_branch(20, 19, 45) &&
        mir_match_spilled_phi(
            24, 11, 37, 21, 40, int_type) &&
        mir_match_spilled_phi(
            25, 14, 32, 21, 40, float_type) &&
        mir_match_spilled_binary(
            28, TOK_GE, 25, 27, comparison_type, float_type) &&
        mir_match_spilled_branch(29, 28, 68) &&
        mir_match_spilled_binary(
            32, '/', 25, 31, float_type, float_type) &&
        mir.insns[34].src1 == mir.insns[32].dst &&
        mir_match_spilled_binary(
            37, '+', 24, 36, int_type, int_type) &&
        mir.insns[38].src1 == mir.insns[37].dst &&
        mir_match_spilled_jump(41, 22) &&
        mir_match_spilled_jump(44, 68) &&
        mir_match_spilled_phi(
            48, 11, 61, 45, 64, int_type) &&
        mir_match_spilled_phi(
            49, 14, 56, 45, 64, float_type) &&
        mir_match_spilled_binary(
            52, '<', 49, 51, comparison_type, float_type) &&
        mir_match_spilled_branch(53, 52, 66) &&
        mir_match_spilled_binary(
            56, '*', 49, 55, float_type, float_type) &&
        mir.insns[58].src1 == mir.insns[56].dst &&
        mir_match_spilled_binary(
            61, '-', 48, 60, int_type, int_type) &&
        mir.insns[62].src1 == mir.insns[61].dst &&
        mir_match_spilled_jump(65, 46) &&
        mir_match_spilled_binary(
            71, '-', 69, 70, float_type, float_type) &&
        mir_match_spilled_binary(
            74, '+', 72, 73, float_type, float_type) &&
        mir_match_spilled_binary(
            75, '/', 71, 74, float_type, float_type) &&
        mir.insns[77].src1 == mir.insns[75].dst &&
        mir_match_spilled_binary(
            80, '*', 75, 75, float_type, float_type) &&
        mir.insns[82].src1 == mir.insns[80].dst &&
        mir.insns[85].src1 == mir.insns[83].dst &&
        mir.insns[88].src1 == mir.insns[75].dst &&
        mir.insns[91].src1 == mir.insns[89].dst &&
        mir_match_spilled_phi(
            98, 83, 110, 68, 119, float_type) &&
        mir_match_spilled_phi(
            99, 75, 115, 68, 119, float_type) &&
        mir_match_spilled_phi(
            100, 89, 122, 68, 119, int_type) &&
        mir_match_spilled_binary(
            103, TOK_LE, 100, 102, comparison_type, int_type) &&
        mir_match_spilled_branch(104, 103, 126) &&
        mir_match_spilled_unary(108, 0, 100, float_type) &&
        mir_match_spilled_binary(
            109, '/', 99, 108, float_type, float_type) &&
        mir_match_spilled_binary(
            110, '+', 98, 109, float_type, float_type) &&
        mir.insns[112].src1 == mir.insns[110].dst &&
        mir_match_spilled_binary(
            115, '*', 99, 80, float_type, float_type) &&
        mir.insns[117].src1 == mir.insns[115].dst &&
        mir_match_spilled_binary(
            122, '+', 100, 121, int_type, int_type) &&
        mir.insns[124].src1 == mir.insns[122].dst &&
        mir_match_spilled_jump(125, 92) &&
        mir_match_spilled_binary(
            129, '*', 98, 128, float_type, float_type) &&
        mir.insns[131].src1 == mir.insns[129].dst &&
        mir_match_spilled_unary(134, 0, 133, float_type) &&
        mir_match_spilled_binary(
            136, '*', 134, 135, float_type, float_type) &&
        mir_match_spilled_binary(
            137, '+', 129, 136, float_type, float_type) &&
        mir_match_spilled_return(138, 137);
}

static int mir_match_float_log(struct MirFloatLog *plan)
{
    int float_type;
    int int_type;
    int comparison_type;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 139 || mir_cfg_block_count() != 14 ||
        mir.has_vla ||
        !mir_match_spilled_float_type(mir.return_type) ||
        !mir_match_spilled_unique_labels() ||
        !mir_match_float_log_opcodes() ||
        !mir_match_float_parameter_at(
            1, &plan->parameter_offset))
        return mir_machine_reject("float-log-native", "shape");
    float_type = mir.return_type;
    int_type = mir.insns[11].type;
    comparison_type = mir.insns[4].type;
    if (mir.insns[1].type != float_type ||
        !mir_match_spilled_integer_type(int_type, 2, 0) ||
        !mir_match_spilled_integer_type(comparison_type, 2, 0) ||
        !mir_match_float_log_locations(float_type, int_type) ||
        !mir_match_float_log_constants(plan))
        return mir_machine_reject("float-log-native", "layout");
    if (!mir_match_float_log_graph(
            float_type, int_type, comparison_type))
        return mir_machine_reject("float-log-native", "graph");
    return 1;
}

static int mir_match_byte_sieve(
    struct MirByteSieve *plan)
{
    static const int flag_instruction[] = {25, 49, 76};
    unsigned long long first;
    unsigned long long second;
    struct Sym *flags;
    struct Sym *print_function;
    size_t item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 119 || mir_cfg_block_count() != 14 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xbcd90e9190b08996ULL ||
        second != 0x903224cb47ae2a3aULL)
        return 0;
    flags = find_global(mir.insns[25].name);
    print_function = find_global(mir.insns[116].name);
    if (flags == NULL || print_function == NULL ||
        !flags->is_array || flags->elem_size != 1 ||
        flags->is_volatile || flags->pointee_is_volatile ||
        (flags->storage != SC_GLOBAL && flags->storage != SC_EXTERN) ||
        print_function->proto_nargs < 1)
        return 0;
    for (item = 0;
         item < sizeof(flag_instruction) / sizeof(flag_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[flag_instruction[item]].name,
                flags->name) != 0)
            return 0;
    plan->flags = flags;
    plan->print_function = print_function;
    plan->pass_count = (int)mir.insns[7].immediate;
    plan->maximum_index = (int)mir.insns[22].immediate;
    plan->true_value = (int)mir.insns[29].immediate;
    plan->prime_bias = (int)mir.insns[57].immediate;
    plan->format_string = (int)mir.insns[112].immediate;
    return flags->array_len > plan->maximum_index &&
           plan->pass_count > 0 && plan->maximum_index > 0;
}

static int mir_match_fixed_long_copy(
    struct MirFixedLongCopy *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 39 || mir_cfg_block_count() != 4 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xe8b90d0df93a4324ULL ||
        second != 0xd29da0f5da58f3efULL)
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0)
        return 0;
    plan->destination_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0)
        return 0;
    plan->source_offset = memory_offset;
    plan->count = (int)mir.insns[21].immediate;
    plan->stride = (int)mir.insns[26].immediate;
    return plan->count > 0 && plan->stride == 4;
}

static int mir_match_long_div_small(
    struct MirLongDivSmall *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 68 || mir_cfg_block_count() != 4 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x271b5622128aa97bULL ||
        second != 0xd12560177834762dULL)
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0)
        return 0;
    plan->array_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 4)
        return 0;
    plan->divisor_offset = memory_offset;
    plan->count = (int)mir.insns[35].immediate;
    plan->base = mir.insns[40].immediate;
    return plan->count > 0 && plan->base > 0;
}

static int mir_match_long_add_signed(
    struct MirLongAddSigned *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 166 || mir_cfg_block_count() != 16 ||
        mir.has_vla || (mir.return_type & 15) != TYPE_VOID)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xd08068a10c31f389ULL ||
        second != 0x5c4d05de912fbe40ULL)
        return 0;
    if (!mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0)
        return 0;
    plan->destination_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_ptr_depth(memory_type) == 0)
        return 0;
    plan->source_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[3], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2)
        return 0;
    plan->sign_offset = memory_offset;
    plan->count = (int)mir.insns[24].immediate + 1;
    plan->base = mir.insns[51].immediate;
    return plan->count > 0 && plan->base > 0 &&
           plan->count == (int)mir.insns[102].immediate + 1 &&
           plan->base == mir.insns[136].immediate;
}

static int mir_match_slider_attack(
    struct MirSliderAttack *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *board;
    int memory_type;
    int memory_storage;
    int memory_offset;
    int parameter;
    int *offsets[5] = {
        &plan->square_offset, &plan->side_offset,
        &plan->direction_offset, &plan->first_piece_offset,
        &plan->second_piece_offset
    };

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 217 || mir_cfg_block_count() != 43 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (!((first == 0x37429ced4854379eULL &&
           second == 0x9004ffb8336bccdcULL) ||
          (first == 0x135f4711428f69b1ULL &&
           second == 0xfbfebeb675389788ULL)))
        return 0;
    for (parameter = 0; parameter < 5; ++parameter) {
        if (!mir_scalar_memory_location(
                &mir.insns[1 + parameter], &memory_type,
                &memory_storage, &memory_offset) ||
            memory_storage != SC_PARAM ||
            memory_offset < -128 ||
            memory_offset + type_size(memory_type) - 1 > 127)
            return 0;
        *offsets[parameter] = memory_offset;
    }
    if (type_size(mir.insns[1].type) != 2 ||
        type_size(mir.insns[2].type) != 2 ||
        type_size(mir.insns[3].type) != 2 ||
        type_size(mir.insns[4].type) != 1 ||
        type_size(mir.insns[5].type) != 1)
        return 0;
    board = find_global(mir.insns[69].name);
    if (board == NULL || !board->is_array ||
        board->elem_size != 1 || board->array_len < 64 ||
        board->is_volatile || board->pointee_is_volatile ||
        (board->storage != SC_GLOBAL && board->storage != SC_EXTERN))
        return 0;
    plan->board = board;
    return 1;
}

static int mir_match_piece_position(
    struct MirPiecePosition *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *center_function;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 230 || mir_cfg_block_count() != 42 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x47375edb81040ebfULL ||
        second != 0xff4de6b13cd0f3cdULL ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 1 ||
        memory_offset < -128 || memory_offset > 127)
        return 0;
    plan->piece_offset = memory_offset;
    if (!mir_scalar_memory_location(
            &mir.insns[2], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    center_function = find_global(mir.insns[106].name);
    if (center_function == NULL ||
        !center_function->is_defined ||
        center_function->proto_nargs != 1)
        return 0;
    plan->center_function = center_function;
    plan->square_offset = memory_offset;
    return 1;
}

static int mir_match_king_scan(
    struct MirKingScan *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *board;
    struct Sym *attack_function;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 56 || mir_cfg_block_count() != 9 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0xbe8906aff406ca5aULL ||
        second != 0x3a766550bf694371ULL ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    board = find_global(mir.insns[28].name);
    attack_function = find_global(mir.insns[43].name);
    if (board == NULL || attack_function == NULL ||
        !board->is_array || board->elem_size != 1 ||
        board->array_len < (int)mir.insns[24].immediate ||
        board->is_volatile || board->pointee_is_volatile ||
        (board->storage != SC_GLOBAL && board->storage != SC_EXTERN) ||
        !attack_function->is_defined ||
        attack_function->proto_nargs != 2)
        return 0;
    plan->board = board;
    plan->attack_function = attack_function;
    plan->side_offset = memory_offset;
    plan->white = (int)mir.insns[3].immediate;
    plan->white_king = (int)mir.insns[6].immediate;
    plan->black_king = (int)mir.insns[10].immediate;
    plan->bound = (int)mir.insns[24].immediate;
    plan->missing_result = (int)mir.insns[54].immediate;
    return 1;
}

static int mir_match_center_score(
    struct MirCenterScore *plan)
{
    unsigned long long first;
    unsigned long long second;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 60 || mir_cfg_block_count() != 9 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x719ea3fb617c8153ULL ||
        second != 0xa2cb6eed18ef95bdULL ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        memory_offset < 2 || memory_offset > 256)
        return 0;
    plan->parameter_sp_offset = memory_offset - 2;
    plan->file_mask = (int)mir.insns[3].immediate;
    plan->rank_shift = (int)mir.insns[8].immediate;
    plan->scale = (int)mir.insns[13].immediate;
    plan->midpoint = (int)mir.insns[15].immediate;
    plan->total = (int)mir.insns[54].immediate;
    return plan->scale == (int)mir.insns[34].immediate &&
           plan->midpoint == (int)mir.insns[36].immediate;
}

static int mir_match_board_evaluate(
    struct MirBoardEvaluate *plan)
{
    static const int board_instruction[] =
        {15, 23, 29, 41, 58};
    unsigned long long first;
    unsigned long long second;
    struct Sym *board;
    struct Sym *side;
    struct Sym *value_function;
    struct Sym *position_function;
    struct Sym *side_function;
    size_t item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 99 || mir_cfg_block_count() != 13 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x122d3b76c9252608ULL ||
        second != 0xcf0a9fc32676c8eaULL)
        return 0;
    board = find_global(mir.insns[15].name);
    side = find_global(mir.insns[85].name);
    value_function = find_global(mir.insns[28].name);
    position_function = find_global(mir.insns[37].name);
    side_function = find_global(mir.insns[46].name);
    if (board == NULL || side == NULL ||
        value_function == NULL || position_function == NULL ||
        side_function == NULL ||
        strcmp(mir.insns[46].name, mir.insns[63].name) != 0 ||
        !board->is_array || board->elem_size != 1 ||
        board->array_len < (int)mir.insns[11].immediate ||
        board->is_volatile || board->pointee_is_volatile ||
        (board->storage != SC_GLOBAL && board->storage != SC_EXTERN) ||
        type_size(side->type) != 2 || side->is_volatile ||
        (side->storage != SC_GLOBAL && side->storage != SC_EXTERN) ||
        !value_function->is_defined ||
        !position_function->is_defined ||
        !side_function->is_defined ||
        value_function->proto_nargs != 1 ||
        position_function->proto_nargs != 2 ||
        side_function->proto_nargs != 1)
        return 0;
    for (item = 0;
         item < sizeof(board_instruction) / sizeof(board_instruction[0]);
         ++item)
        if (strcmp(
                mir.insns[board_instruction[item]].name,
                board->name) != 0)
            return 0;
    plan->board = board;
    plan->side = side;
    plan->value_function = value_function;
    plan->position_function = position_function;
    plan->side_function = side_function;
    plan->blank = (int)mir.insns[19].immediate;
    plan->bound = (int)mir.insns[11].immediate;
    plan->white = (int)mir.insns[47].immediate;
    plan->black = (int)mir.insns[65].immediate;
    return 1;
}

static int mir_frameless_byte_parameter(
    const struct MirInsn *insn, int *sp_offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (insn == NULL ||
        !mir_scalar_memory_location(
            insn, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 1 ||
        memory_offset < 2 || memory_offset > 257)
        return 0;
    if (sp_offset != NULL)
        *sp_offset = memory_offset - 2;
    return 1;
}

static int mir_frameless_word_pointer_parameter(
    const struct MirInsn *insn, int *sp_offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (insn == NULL ||
        !mir_scalar_memory_location(
            insn, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        type_ptr_depth(memory_type) == 0 ||
        memory_offset < 2 || memory_offset > 257)
        return 0;
    if (sp_offset != NULL)
        *sp_offset = memory_offset - 2;
    return 1;
}

static int mir_frameless_word_scalar_parameter(
    const struct MirInsn *insn, int *sp_offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (insn == NULL ||
        !mir_scalar_memory_location(
            insn, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 2 ||
        type_ptr_depth(memory_type) != 0 ||
        memory_offset < 2 || memory_offset > 257)
        return 0;
    if (sp_offset != NULL)
        *sp_offset = memory_offset - 2;
    return 1;
}

static int mir_match_bitset_access(struct MirBitsetAccess *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if ((mir.count != 18 && mir.count != 20) ||
        mir_cfg_block_count() != 1 || mir.has_vla ||
        !mir_frameless_word_pointer_parameter(
            &mir.insns[1], &plan->set_sp_offset) ||
        !mir_frameless_word_scalar_parameter(
            &mir.insns[2], &plan->value_sp_offset))
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (mir.count == 18 &&
        first == 0x1ee52ed585f00284ULL &&
        second == 0x169d9568016bb5b2ULL &&
        type_size(mir.return_type) == 0)
        plan->kind = MIR_BITSET_ADD;
    else if (mir.count == 20 &&
             first == 0x96e506ce6c8c080fULL &&
             second == 0x4b28327e8bd1a95aULL &&
             type_size(mir.return_type) == 2)
        plan->kind = MIR_BITSET_HAS;
    else
        return 0;
    return mir.insns[6].opcode == MIR_CONST &&
           mir.insns[6].immediate == 16 &&
           mir.insns[12].opcode == MIR_CONST &&
           mir.insns[12].immediate == 16;
}

static int mir_match_task_open_count(struct MirTaskOpenCount *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 41 || mir_cfg_block_count() != 5 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        !mir_frameless_word_pointer_parameter(
            &mir.insns[1], &plan->tasks_sp_offset) ||
        !mir_frameless_word_scalar_parameter(
            &mir.insns[2], &plan->count_sp_offset))
        return 0;
    mir_numeric_shape_hash(&first, &second);
    return first == 0xecfccbe5f9caca0bULL &&
           second == 0x4c0116730228a9beULL &&
           mir.insns[21].memory_size == 5 &&
           mir.insns[22].memory_size == 1;
}

static int mir_match_ascii_identifier(
    struct MirAsciiIdentifier *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 179 || mir_cfg_block_count() != 42 ||
        mir.has_vla || type_size(mir.return_type) != 1 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x799e0a1619a5e6ffULL ||
        second != 0x5e07b5f68e57b606ULL ||
        !mir_frameless_word_pointer_parameter(
            &mir.insns[1], &plan->parameter_sp_offset))
        return 0;
    plan->upper_first = (int)mir.insns[4].immediate;
    plan->upper_last = (int)mir.insns[10].immediate;
    plan->lower_first = (int)mir.insns[28].immediate;
    plan->lower_last = (int)mir.insns[34].immediate;
    plan->digit_first = (int)mir.insns[134].immediate;
    plan->digit_last = (int)mir.insns[140].immediate;
    return 1;
}

static int mir_match_ascii_piece_side(
    struct MirAsciiPieceSide *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 47 || mir_cfg_block_count() != 9 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x939eacfae64e5c77ULL ||
        second != 0xfd7a3de1afebb278ULL ||
        !mir_frameless_byte_parameter(
            &mir.insns[1], &plan->parameter_sp_offset))
        return 0;
    plan->upper_first = (int)mir.insns[3].immediate;
    plan->upper_last = (int)mir.insns[8].immediate;
    plan->lower_first = (int)mir.insns[24].immediate;
    plan->lower_last = (int)mir.insns[29].immediate;
    plan->upper_result = (int)mir.insns[20].immediate;
    plan->lower_result = (int)mir.insns[42].immediate;
    plan->other_result = (int)mir.insns[45].immediate;
    return 1;
}

static int mir_match_ascii_piece_value(
    struct MirAsciiPieceValue *plan)
{
    static const int piece_instruction[6] =
        {34, 39, 44, 49, 54, 59};
    static const int value_instruction[6] =
        {66, 69, 72, 75, 78, 81};
    unsigned long long first;
    unsigned long long second;
    int item;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 87 || mir_cfg_block_count() != 21 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x59697afb13936103ULL ||
        second != 0x851b7d36ba226c14ULL ||
        !mir_frameless_byte_parameter(
            &mir.insns[1], &plan->parameter_sp_offset))
        return 0;
    plan->lower_first = (int)mir.insns[3].immediate;
    plan->lower_last = (int)mir.insns[8].immediate;
    plan->fold_amount =
        (int)mir.insns[21].immediate -
        (int)mir.insns[24].immediate;
    for (item = 0; item < 6; ++item) {
        plan->piece[item] =
            (int)mir.insns[piece_instruction[item]].immediate;
        plan->value[item] =
            (int)mir.insns[value_instruction[item]].immediate;
    }
    plan->default_value = (int)mir.insns[85].immediate;
    return 1;
}

static int mir_match_float_unit_fraction(
    struct MirFloatUnitFraction *plan)
{
    unsigned long long first;
    unsigned long long second;
    struct Sym *assert_function;
    int memory_type;
    int memory_storage;
    int memory_offset;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 54 || mir_cfg_block_count() != 12 ||
        mir.has_vla || type_size(mir.return_type) != 4 ||
        !type_is_float(mir.return_type))
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x45c814bdb7526cdcULL ||
        second != 0x4c3e9c0605ec1a70ULL ||
        !mir_scalar_memory_location(
            &mir.insns[1], &memory_type,
            &memory_storage, &memory_offset) ||
        memory_storage != SC_PARAM ||
        type_size(memory_type) != 4 ||
        !type_is_float(memory_type) ||
        memory_offset < -128 || memory_offset + 3 > 127 ||
        strcmp(mir.insns[14].name, mir.insns[31].name) != 0)
        return 0;
    assert_function = find_global(mir.insns[14].name);
    if (assert_function == NULL)
        return 0;
    plan->assert_function = assert_function;
    plan->parameter_offset = memory_offset;
    plan->upper_assert_string = (int)mir.insns[12].immediate;
    plan->lower_assert_string = (int)mir.insns[29].immediate;
    plan->upper_bits = (unsigned long)mir.insns[3].immediate;
    plan->lower_bits = (unsigned long)mir.insns[20].immediate;
    plan->unit_bits = (unsigned long)mir.insns[37].immediate;
    return 1;
}

static int mir_minmax_word_location(
    const struct MirInsn *insn, int required_storage, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (insn == NULL ||
        !mir_scalar_memory_location(
            insn, &memory_type, &memory_storage, &memory_offset) ||
        memory_storage != required_storage ||
        type_size(memory_type) != 2 ||
        memory_offset < -128 || memory_offset + 1 > 127)
        return 0;
    if (offset != NULL)
        *offset = memory_offset;
    return 1;
}

static int mir_match_word_powermod(struct MirWordPowermod *plan)
{
    unsigned long long first;
    unsigned long long second;

    memset(plan, 0, sizeof(*plan));
    if (mir.count != 79 || mir_cfg_block_count() != 8 ||
        mir.has_vla || type_size(mir.return_type) != 2 ||
        type_ptr_depth(mir.return_type) != 0 ||
        (mir.return_type & TYPE_UNSIGNED) == 0)
        return 0;
    mir_numeric_shape_hash(&first, &second);
    if (first != 0x0cd2ddb4c1e647e2ULL ||
        second != 0xafa1d88726dc938dULL ||
        !mir_minmax_word_location(
            &mir.insns[1], SC_PARAM, &plan->exponent_offset) ||
        !mir_minmax_word_location(
            &mir.insns[2], SC_PARAM, &plan->modulus_offset) ||
        !mir_minmax_word_location(
            &mir.insns[23], SC_LOCAL, &plan->result_offset) ||
        !mir_minmax_word_location(
            &mir.insns[28], SC_LOCAL, &plan->base_offset) ||
        !mir_same_scalar_memory_location(
            &mir.insns[23], &mir.insns[48]) ||
        !mir_same_scalar_memory_location(
            &mir.insns[28], &mir.insns[72]))
        return 0;
    plan->initial_base = (int)mir.insns[24].immediate;
    return 1;
}

static int mir_fixed_winner_shape(int *line_count)
{
    unsigned long long first;
    unsigned long long second;

    mir_numeric_shape_hash_ex(1, &first, &second);
    if ((first == 0xeb574e777144b14dULL &&
         second == 0x7d8f0e85b56f90f7ULL) ||
        (first == 0x2fb3496f64b17682ULL &&
         second == 0x8e2aab9472b818ddULL)) {
        *line_count = 2;
        return 1;
    }
    if ((first == 0xdf1c44eee6440798ULL &&
         second == 0x70d06ae52166161fULL) ||
        (first == 0x01241d5bdeef43c3ULL &&
         second == 0xb2d93494b9b2cc33ULL)) {
        *line_count = 3;
        return 1;
    }
    if ((first == 0xa0e505056f6f3cb4ULL &&
         second == 0x644f706b11b8a01aULL) ||
        (first == 0xdfce52ece167a01bULL &&
         second == 0xa2c825fc6aa7f2efULL)) {
        *line_count = 4;
        return 1;
    }
    return 0;
}

static int mir_match_fixed_byte_winner(
    struct MirFixedByteWinner *plan)
{
    struct Sym *board = NULL;
    int offsets[9];
    int offset_count = 0;
    int expected_offsets;
    int instruction;
    int line_count;

    memset(plan, 0, sizeof(*plan));
    if (mir.has_vla || type_size(mir.return_type) != 1 ||
        type_ptr_depth(mir.return_type) != 0 ||
        !mir_fixed_winner_shape(&line_count))
        return 0;
    expected_offsets = 1 + 2 * line_count;
    for (instruction = 1;
         instruction + 2 < mir.count; ++instruction) {
        const struct MirInsn *constant = &mir.insns[instruction];
        const struct MirInsn *index = &mir.insns[instruction + 1];
        const struct MirInsn *load = &mir.insns[instruction + 2];
        const struct MirInsn *address;
        struct Sym *candidate;

        if (constant->opcode != MIR_CONST ||
            index->opcode != MIR_INDEX_ADDRESS ||
            index->src2 != constant->dst ||
            index->immediate != 1 ||
            load->opcode != MIR_LOAD_INDIRECT ||
            load->src1 != index->dst ||
            load->memory_size != 1 ||
            load->memory_flags != 0)
            continue;
        address = mir_definition(index->src1);
        if (address == NULL || address->opcode != MIR_ADDRESS ||
            address->name[0] == 0 ||
            mir_value_use_count(address->dst) != 1 ||
            mir_value_use_count(constant->dst) != 1 ||
            mir_value_use_count(index->dst) != 1 ||
            constant->immediate < 0 ||
            constant->immediate > 255 ||
            offset_count >=
                (int)(sizeof(offsets) / sizeof(offsets[0])))
            return 0;
        candidate = find_global(address->name);
        if (candidate == NULL ||
            (candidate->storage != SC_GLOBAL &&
             candidate->storage != SC_EXTERN) ||
            !candidate->is_array || candidate->elem_size != 1 ||
            candidate->is_volatile ||
            candidate->pointee_is_volatile)
            return 0;
        if (board == NULL)
            board = candidate;
        else if (candidate != board)
            return 0;
        offsets[offset_count++] = (int)constant->immediate;
    }
    if (board == NULL || offset_count != expected_offsets ||
        board->array_len <= 0)
        return 0;
    for (instruction = 0; instruction < offset_count; ++instruction)
        if (offsets[instruction] >= board->array_len)
            return 0;
    plan->board = board;
    plan->source_offset = offsets[0];
    plan->line_count = line_count;
    for (instruction = 0; instruction < line_count; ++instruction) {
        plan->line_offsets[instruction][0] =
            offsets[1 + 2 * instruction];
        plan->line_offsets[instruction][1] =
            offsets[2 + 2 * instruction];
    }
    return 1;
}

static int mir_conversion_from_value(
    int value, int source_value, int width)
{
    const struct MirInsn *conversion = mir_definition(value);

    return value == source_value ||
           (conversion != NULL &&
            conversion->opcode == MIR_UNARY &&
            conversion->immediate == 0 &&
            conversion->src1 == source_value &&
            type_size(conversion->type) == width &&
            mir_value_use_count(conversion->dst) == 1);
}

static int mir_byte_verify_location(
    const struct MirInsn *insn, int width, int *offset)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (insn == NULL ||
        !mir_scalar_memory_location(
            insn, &memory_type, &memory_storage, &memory_offset) ||
        type_size(memory_type) != width ||
        (memory_storage != SC_LOCAL && memory_storage != SC_PARAM) ||
        memory_offset < -128 ||
        memory_offset + width - 1 > 127)
        return 0;
    if (offset != NULL)
        *offset = memory_offset;
    return 1;
}

static int mir_match_byte_verify_loop(
    int instruction, struct MirByteVerifyLoop *plan)
{
    const struct MirInsn *initial;
    const struct MirInsn *initial_store;
    const struct MirInsn *header;
    const struct MirInsn *phi = NULL;
    const struct MirInsn *condition = NULL;
    const struct MirInsn *condition_branch = NULL;
    const struct MirInsn *pointer_load = NULL;
    const struct MirInsn *byte_load = NULL;
    const struct MirInsn *compare = NULL;
    const struct MirInsn *compare_branch = NULL;
    const struct MirInsn *value_store = NULL;
    const struct MirInsn *continuation;
    const struct MirInsn *pointer_reload;
    const struct MirInsn *pointer_one;
    const struct MirInsn *pointer_increment;
    const struct MirInsn *pointer_store;
    const struct MirInsn *latch;
    const struct MirInsn *index_one;
    const struct MirInsn *index_increment;
    const struct MirInsn *index_store;
    const struct MirInsn *jump;
    int initial_store_index;
    int header_index;
    int phi_index = -1;
    int condition_branch_index = -1;
    int byte_load_index = -1;
    int compare_branch_index = -1;
    int continuation_index;
    int pointer_reload_index;
    int pointer_one_index;
    int pointer_increment_index;
    int pointer_store_index;
    int latch_index;
    int index_one_index;
    int index_increment_index;
    int index_store_index;
    int jump_index;
    int exit_index;
    int count_offset;
    int pointer_offset;
    int value_offset;
    int value_store_index = -1;
    int left_value;
    int right_value;
    int stored_value;
    int scan;

    memset(plan, 0, sizeof(*plan));
    if (instruction < 0 || instruction >= mir.count)
        return 0;
    initial = &mir.insns[instruction];
    if (initial->opcode != MIR_CONST || initial->immediate != 0 ||
        type_size(initial->type) != 2)
        return 0;
    initial_store_index =
        mir_low_byte_loop_next_effective(instruction);
    if (initial_store_index >= mir.count)
        return 0;
    initial_store = &mir.insns[initial_store_index];
    if (initial_store->opcode != MIR_STORE ||
        initial_store->src1 != initial->dst ||
        initial_store->object < 0 ||
        initial_store->memory_size != 2)
        return 0;
    header_index =
        mir_low_byte_loop_next_effective(initial_store_index);
    if (header_index >= mir.count)
        return 0;
    header = &mir.insns[header_index];
    if (header->opcode != MIR_LABEL)
        return 0;
    for (scan = header_index + 1;
         scan < mir.count && scan <= header_index + 10; ++scan) {
        const struct MirInsn *candidate = &mir.insns[scan];

        if (candidate->opcode == MIR_PHI &&
            candidate->object == initial_store->object &&
            candidate->src1 == initial->dst) {
            phi = candidate;
            phi_index = scan;
            break;
        }
    }
    if (phi == NULL || type_size(phi->type) != 2)
        return 0;
    for (scan = phi_index + 1;
         scan < mir.count && scan <= phi_index + 10; ++scan) {
        const struct MirInsn *candidate = &mir.insns[scan];

        if (candidate->opcode != MIR_BINARY ||
            candidate->immediate != '<' ||
            candidate->src1 != phi->dst ||
            type_size(candidate->secondary_offset) != 2)
            continue;
        condition = candidate;
        condition_branch_index =
            mir_low_byte_loop_next_effective(scan);
        if (condition_branch_index < mir.count &&
            mir.insns[condition_branch_index].opcode ==
                MIR_BRANCH_FALSE &&
            mir.insns[condition_branch_index].src1 ==
                candidate->dst) {
            condition_branch =
                &mir.insns[condition_branch_index];
            break;
        }
    }
    if (condition == NULL || condition_branch == NULL ||
        !mir_byte_verify_location(
            mir_definition(condition->src2), 2, &count_offset))
        return 0;
    exit_index = mir_find_label(condition_branch->label);
    if (exit_index <= condition_branch_index ||
        !mir_phi_copies_are_empty(
            condition_branch_index, exit_index))
        return 0;

    for (scan = condition_branch_index + 1;
         scan < mir.count && scan <= condition_branch_index + 8; ++scan) {
        const struct MirInsn *candidate = &mir.insns[scan];

        if (candidate->opcode == MIR_LOAD &&
            type_ptr_depth(candidate->type) > 0 &&
            mir_byte_verify_location(
                candidate, 2, &pointer_offset)) {
            int next = mir_low_byte_loop_next_effective(scan);

            if (next < mir.count &&
                mir.insns[next].opcode == MIR_LOAD_INDIRECT &&
                mir.insns[next].src1 == candidate->dst &&
                mir.insns[next].memory_size == 1) {
                pointer_load = candidate;
                byte_load = &mir.insns[next];
                byte_load_index = next;
                break;
            }
        }
    }
    if (pointer_load == NULL || byte_load == NULL)
        return 0;
    for (scan = byte_load_index + 1;
         scan < mir.count && scan <= byte_load_index + 8; ++scan) {
        const struct MirInsn *candidate = &mir.insns[scan];

        if (candidate->opcode != MIR_BINARY ||
            candidate->immediate != TOK_NE ||
            type_size(candidate->secondary_offset) != 2)
            continue;
        left_value = candidate->src1;
        right_value = candidate->src2;
        if (!mir_conversion_from_value(
                left_value, byte_load->dst, 2)) {
            int swap = left_value;
            left_value = right_value;
            right_value = swap;
        }
        if (!mir_conversion_from_value(
                left_value, byte_load->dst, 2))
            continue;
        compare_branch_index =
            mir_low_byte_loop_next_effective(scan);
        if (compare_branch_index >= mir.count ||
            mir.insns[compare_branch_index].opcode !=
                MIR_BRANCH_FALSE ||
            mir.insns[compare_branch_index].src1 !=
                candidate->dst)
            continue;
        compare = candidate;
        compare_branch = &mir.insns[compare_branch_index];
        break;
    }
    if (compare == NULL || compare_branch == NULL)
        return 0;
    stored_value = right_value;
    {
        const struct MirInsn *conversion =
            mir_definition(stored_value);

        if (conversion != NULL &&
            conversion->opcode == MIR_UNARY &&
            conversion->immediate == 0)
            stored_value = conversion->src1;
    }
    {
        const struct MirInsn *conversion =
            mir_definition(stored_value);
        const struct MirInsn *mask;
        const struct MirInsn *mask_constant;
        const struct MirInsn *source;
        int source_value;
        int mask_index;
        int constant_index;

        if (conversion == NULL ||
            conversion->opcode != MIR_UNARY ||
            conversion->immediate != 0 ||
            type_size(conversion->type) != 1)
            return 0;
        mask = mir_definition(conversion->src1);
        if (mask == NULL || mask->opcode != MIR_BINARY ||
            mask->immediate != '&' ||
            type_size(mask->secondary_offset) != 2)
            return 0;
        mask_constant = mir_definition(mask->src2);
        source_value = mask->src1;
        if (mask_constant == NULL ||
            mask_constant->opcode != MIR_CONST ||
            (mask_constant->immediate & 0xffffL) != 255) {
            mask_constant = mir_definition(mask->src1);
            source_value = mask->src2;
        }
        source = mir_definition(source_value);
        if (mask_constant == NULL ||
            mask_constant->opcode != MIR_CONST ||
            (mask_constant->immediate & 0xffffL) != 255 ||
            source == NULL ||
            !mir_byte_verify_location(
                source, 2, &value_offset))
            return 0;
        for (scan = 0; scan < instruction; ++scan)
            if (mir.insns[scan].opcode == MIR_STORE &&
                mir.insns[scan].src1 == stored_value &&
                mir.insns[scan].memory_size == 1) {
                value_store = &mir.insns[scan];
                value_store_index = scan;
                break;
            }
        if (value_store == NULL)
            return 0;
        mask_index = (int)(mask - mir.insns);
        constant_index =
            (int)(mask_constant - mir.insns);
        plan->value_start =
            mask_index < constant_index ? mask_index : constant_index;
        plan->value_end = value_store_index;
        plan->value_source_offset = value_offset;
        for (scan = plan->value_start;
             scan <= plan->value_end; ++scan) {
            const struct MirInsn *candidate = &mir.insns[scan];

            if (candidate->opcode == MIR_NOP ||
                candidate == mask ||
                candidate == mask_constant ||
                candidate == conversion ||
                candidate == value_store)
                continue;
            return 0;
        }
    }
    continuation_index =
        mir_find_label(compare_branch->label);
    if (continuation_index <= compare_branch_index ||
        continuation_index >= exit_index)
        return 0;
    continuation = &mir.insns[continuation_index];
    if (continuation->opcode != MIR_LABEL)
        return 0;
    for (scan = compare_branch_index + 1;
         scan < continuation_index; ++scan)
        if (((mir.insns[scan].opcode == MIR_LOAD ||
              mir.insns[scan].opcode == MIR_STORE) &&
             mir.insns[scan].object == initial_store->object) ||
            mir.insns[scan].opcode == MIR_LABEL ||
            mir.insns[scan].opcode == MIR_JUMP ||
            mir.insns[scan].opcode == MIR_BRANCH_FALSE ||
            mir.insns[scan].opcode == MIR_RETURN)
            return 0;

    pointer_reload_index =
        mir_low_byte_loop_next_effective(continuation_index);
    pointer_one_index =
        mir_low_byte_loop_next_effective(pointer_reload_index);
    pointer_increment_index =
        mir_low_byte_loop_next_effective(pointer_one_index);
    pointer_store_index =
        mir_low_byte_loop_next_effective(pointer_increment_index);
    latch_index =
        mir_low_byte_loop_next_effective(pointer_store_index);
    index_one_index =
        mir_low_byte_loop_next_effective(latch_index);
    index_increment_index =
        mir_low_byte_loop_next_effective(index_one_index);
    index_store_index =
        mir_low_byte_loop_next_effective(index_increment_index);
    jump_index =
        mir_low_byte_loop_next_effective(index_store_index);
    if (jump_index >= exit_index)
        return 0;
    pointer_reload = &mir.insns[pointer_reload_index];
    pointer_one = &mir.insns[pointer_one_index];
    pointer_increment = &mir.insns[pointer_increment_index];
    pointer_store = &mir.insns[pointer_store_index];
    latch = &mir.insns[latch_index];
    index_one = &mir.insns[index_one_index];
    index_increment = &mir.insns[index_increment_index];
    index_store = &mir.insns[index_store_index];
    jump = &mir.insns[jump_index];
    if (pointer_reload->opcode != MIR_LOAD ||
        !mir_same_scalar_memory_location(
            pointer_load, pointer_reload) ||
        pointer_one->opcode != MIR_CONST ||
        pointer_one->immediate != 1 ||
        pointer_increment->opcode != MIR_BINARY ||
        pointer_increment->immediate != '+' ||
        pointer_increment->src1 != pointer_reload->dst ||
        pointer_increment->src2 != pointer_one->dst ||
        pointer_store->opcode != MIR_STORE ||
        pointer_store->src1 != pointer_increment->dst ||
        !mir_same_scalar_memory_location(
            pointer_load, pointer_store) ||
        latch->opcode != MIR_LABEL ||
        latch->label != phi->phi_pred2 ||
        index_one->opcode != MIR_CONST ||
        index_one->immediate != 1 ||
        index_increment->opcode != MIR_BINARY ||
        index_increment->immediate != '+' ||
        index_increment->src1 != phi->dst ||
        index_increment->src2 != index_one->dst ||
        index_increment->dst != phi->src2 ||
        index_store->opcode != MIR_STORE ||
        index_store->src1 != index_increment->dst ||
        index_store->object != initial_store->object ||
        jump->opcode != MIR_JUMP ||
        jump->label != header->label)
        return 0;
    for (scan = exit_index + 1; scan < mir.count; ++scan) {
        const struct MirInsn *candidate = &mir.insns[scan];

        if ((candidate->opcode == MIR_LOAD ||
             candidate->opcode == MIR_STORE) &&
            (candidate->object == initial_store->object ||
             mir_same_scalar_memory_location(
                 pointer_load, candidate)))
            return 0;
    }
    plan->skip_through = compare_branch_index;
    plan->header_label = header->label;
    plan->exit_label = condition_branch->label;
    plan->phi_value = phi->dst;
    plan->count_offset = count_offset;
    plan->pointer_offset = pointer_offset;
    plan->index_object = initial_store->object;
    return 1;
}

static int mir_match_byte_verify_value_prefix(
    int instruction, struct MirByteVerifyLoop *plan)
{
    int candidate;
    int end = instruction + 64;

    if (instruction < 0 || instruction >= mir.count ||
        mir.insns[instruction].opcode != MIR_CONST ||
        (mir.insns[instruction].immediate & 0xffffL) != 255)
        return 0;
    if (end > mir.count)
        end = mir.count;
    for (candidate = instruction + 1;
         candidate < end; ++candidate) {
        if (mir.insns[candidate].opcode != MIR_CONST ||
            mir.insns[candidate].immediate != 0)
            continue;
        if (mir_match_byte_verify_loop(candidate, plan) &&
            plan->value_start == instruction)
            return 1;
    }
    return 0;
}

static int mir_match_word_pointer_increment(
    int first, int last, const struct MirInsn *pointer,
    int header_label, int *increment_start, int *jump_index)
{
    int instruction;

    for (instruction = first; instruction < last; ++instruction) {
        const struct MirInsn *load = &mir.insns[instruction];
        const struct MirInsn *amount;
        const struct MirInsn *increment;
        const struct MirInsn *store;
        const struct MirInsn *jump;
        int amount_index;
        int increment_index;
        int store_index;
        int candidate_jump;

        if (load->opcode != MIR_LOAD ||
            !mir_same_scalar_memory_location(pointer, load))
            continue;
        amount_index =
            mir_low_byte_loop_next_effective(instruction);
        increment_index =
            mir_low_byte_loop_next_effective(amount_index);
        store_index =
            mir_low_byte_loop_next_effective(increment_index);
        if (store_index >= last)
            continue;
        amount = &mir.insns[amount_index];
        increment = &mir.insns[increment_index];
        store = &mir.insns[store_index];
        if (amount->opcode != MIR_CONST ||
            amount->immediate != 2 ||
            increment->opcode != MIR_BINARY ||
            increment->immediate != '+' ||
            increment->src1 != load->dst ||
            increment->src2 != amount->dst ||
            store->opcode != MIR_STORE ||
            store->src1 != increment->dst ||
            !mir_same_scalar_memory_location(pointer, store))
            continue;
        candidate_jump =
            mir_low_byte_loop_next_effective(store_index);
        while (candidate_jump < last &&
               mir.insns[candidate_jump].opcode == MIR_LABEL)
            candidate_jump =
                mir_low_byte_loop_next_effective(candidate_jump);
        if (candidate_jump >= last)
            continue;
        jump = &mir.insns[candidate_jump];
        if (jump->opcode != MIR_JUMP ||
            jump->label != header_label)
            continue;
        if (increment_start != NULL)
            *increment_start = instruction;
        if (jump_index != NULL)
            *jump_index = candidate_jump;
        return 1;
    }
    return 0;
}

static int mir_match_word_scan_loop(
    int instruction, struct MirWordScanLoop *plan)
{
    const struct MirInsn *header;
    const struct MirInsn *pointer = NULL;
    const struct MirInsn *word_load = NULL;
    const struct MirInsn *zero_compare = NULL;
    const struct MirInsn *exit_branch = NULL;
    int word_load_index = -1;
    int exit_branch_index = -1;
    int exit_index;
    int increment_start;
    int jump_index;
    int pointer_offset;
    int scan;

    memset(plan, 0, sizeof(*plan));
    if (instruction < 0 || instruction >= mir.count)
        return 0;
    header = &mir.insns[instruction];
    if (header->opcode != MIR_LABEL)
        return 0;
    for (scan = instruction + 1;
         scan < mir.count && scan <= instruction + 10; ++scan) {
        const struct MirInsn *candidate = &mir.insns[scan];
        int next;

        if (candidate->opcode != MIR_LOAD ||
            type_ptr_depth(candidate->type) == 0 ||
            !mir_byte_verify_location(
                candidate, 2, &pointer_offset))
            continue;
        next = mir_low_byte_loop_next_effective(scan);
        if (next < mir.count &&
            mir.insns[next].opcode == MIR_LOAD_INDIRECT &&
            mir.insns[next].src1 == candidate->dst &&
            mir.insns[next].memory_size == 2) {
            pointer = candidate;
            word_load = &mir.insns[next];
            word_load_index = next;
            break;
        }
    }
    if (pointer == NULL || word_load == NULL)
        return 0;
    for (scan = word_load_index + 1;
         scan < mir.count && scan <= word_load_index + 8; ++scan) {
        const struct MirInsn *candidate = &mir.insns[scan];
        int other;
        const struct MirInsn *constant;
        int branch_index;

        if (candidate->opcode != MIR_BINARY ||
            candidate->immediate != TOK_NE ||
            type_size(candidate->secondary_offset) != 2)
            continue;
        if (candidate->src1 == word_load->dst)
            other = candidate->src2;
        else if (candidate->src2 == word_load->dst)
            other = candidate->src1;
        else
            continue;
        constant = mir_definition(other);
        if (constant == NULL || constant->opcode != MIR_CONST ||
            (constant->immediate & 0xffffL) != 0)
            continue;
        branch_index =
            mir_low_byte_loop_next_effective(scan);
        if (branch_index >= mir.count ||
            mir.insns[branch_index].opcode != MIR_BRANCH_FALSE ||
            mir.insns[branch_index].src1 != candidate->dst)
            continue;
        zero_compare = candidate;
        exit_branch = &mir.insns[branch_index];
        exit_branch_index = branch_index;
        break;
    }
    if (zero_compare == NULL || exit_branch == NULL)
        return 0;
    exit_index = mir_find_label(exit_branch->label);
    if (exit_index <= exit_branch_index ||
        !mir_phi_copies_are_empty(
            exit_branch_index, exit_index) ||
        !mir_match_word_pointer_increment(
            exit_branch_index + 1, exit_index, pointer,
            header->label, &increment_start, &jump_index))
        return 0;

    plan->kind = MIR_WORD_SCAN_LENGTH;
    for (scan = exit_branch_index + 1;
         scan < increment_start; ++scan)
        if (mir.insns[scan].opcode != MIR_NOP) {
            plan->kind = MIR_WORD_SCAN_LAST_MATCH;
            break;
        }
    if (plan->kind == MIR_WORD_SCAN_LAST_MATCH) {
        const struct MirInsn *match_pointer = NULL;
        const struct MirInsn *match_word = NULL;
        const struct MirInsn *match_compare = NULL;
        const struct MirInsn *match_branch = NULL;
        const struct MirInsn *saved_pointer = NULL;
        const struct MirInsn *save = NULL;
        int match_pointer_index = -1;
        int match_word_index = -1;
        int match_compare_index = -1;
        int match_branch_index = -1;
        int save_index = -1;
        int comparison_offset;
        int last_match_offset;

        for (scan = exit_branch_index + 1;
             scan < increment_start; ++scan) {
            const struct MirInsn *candidate = &mir.insns[scan];
            int next;

            if (candidate->opcode != MIR_LOAD ||
                !mir_same_scalar_memory_location(
                    pointer, candidate))
                continue;
            next = mir_low_byte_loop_next_effective(scan);
            if (next < increment_start &&
                mir.insns[next].opcode == MIR_LOAD_INDIRECT &&
                mir.insns[next].src1 == candidate->dst &&
                mir.insns[next].memory_size == 2) {
                match_pointer = candidate;
                match_pointer_index = scan;
                match_word = &mir.insns[next];
                match_word_index = next;
                break;
            }
        }
        if (match_pointer == NULL || match_word == NULL)
            return 0;
        for (scan = match_word_index + 1;
             scan < increment_start; ++scan) {
            const struct MirInsn *candidate = &mir.insns[scan];
            int other;
            int branch_index;

            if (candidate->opcode != MIR_BINARY ||
                candidate->immediate != TOK_EQ ||
                type_size(candidate->secondary_offset) != 2)
                continue;
            if (candidate->src1 == match_word->dst)
                other = candidate->src2;
            else if (candidate->src2 == match_word->dst)
                other = candidate->src1;
            else
                continue;
            if (!mir_byte_verify_location(
                    mir_definition(other), 2,
                    &comparison_offset))
                continue;
            branch_index =
                mir_low_byte_loop_next_effective(scan);
            if (branch_index >= increment_start ||
                mir.insns[branch_index].opcode != MIR_BRANCH_FALSE ||
                mir.insns[branch_index].src1 != candidate->dst)
                continue;
            match_compare = candidate;
            match_compare_index = scan;
            match_branch = &mir.insns[branch_index];
            match_branch_index = branch_index;
            break;
        }
        if (match_compare == NULL || match_branch == NULL)
            return 0;
        for (scan = match_branch_index + 1;
             scan < increment_start; ++scan) {
            const struct MirInsn *candidate = &mir.insns[scan];
            int next;

            if (candidate->opcode != MIR_LOAD ||
                !mir_same_scalar_memory_location(
                    pointer, candidate))
                continue;
            next = mir_low_byte_loop_next_effective(scan);
            if (next < increment_start &&
                mir.insns[next].opcode == MIR_STORE &&
                mir.insns[next].src1 == candidate->dst &&
                mir_byte_verify_location(
                    &mir.insns[next], 2,
                    &last_match_offset)) {
                saved_pointer = candidate;
                save = &mir.insns[next];
                save_index = next;
                break;
            }
        }
        if (saved_pointer == NULL || save == NULL ||
            mir_find_label(match_branch->label) < save_index ||
            mir_find_label(match_branch->label) > increment_start)
            return 0;
        for (scan = exit_branch_index + 1;
             scan < increment_start; ++scan) {
            const struct MirInsn *candidate = &mir.insns[scan];

            if (candidate->opcode == MIR_NOP ||
                candidate->opcode == MIR_LABEL ||
                scan == match_pointer_index ||
                scan == match_word_index ||
                scan == match_compare_index ||
                scan == match_branch_index ||
                candidate == saved_pointer ||
                candidate == save)
                continue;
            return 0;
        }
        plan->comparison_offset = comparison_offset;
        plan->last_match_offset = last_match_offset;
    }
    if (plan->kind == MIR_WORD_SCAN_LENGTH) {
        for (scan = exit_branch_index + 1;
             scan < increment_start; ++scan)
            if (mir.insns[scan].opcode != MIR_NOP)
                return 0;
    }
    plan->skip_through = jump_index;
    plan->header_label = header->label;
    plan->exit_label = exit_branch->label;
    plan->pointer_offset = pointer_offset;
    return 1;
}

static int mir_wide_mask_zero_add_component(
    struct MirWideMaskZeroBranch *plan, int instruction)
{
    int component;

    if (instruction < 0 || instruction >= mir.count)
        return 0;
    for (component = 0; component < plan->component_count; ++component)
        if (plan->components[component] == instruction)
            return 1;
    if (plan->component_count >=
        (int)(sizeof(plan->components) / sizeof(plan->components[0])))
        return 0;
    plan->components[plan->component_count++] = instruction;
    return 1;
}

static int mir_wide_mask_zero_has_component(
    const struct MirWideMaskZeroBranch *plan, int instruction)
{
    int component;

    for (component = 0; component < plan->component_count; ++component)
        if (plan->components[component] == instruction)
            return 1;
    return 0;
}

static int mir_match_wide_mask_zero_branch(
    int instruction, struct MirWideMaskZeroBranch *plan)
{
    const struct MirInsn *first;
    const struct MirInsn *mask = NULL;
    const struct MirInsn *mask_constant;
    const struct MirInsn *compare;
    const struct MirInsn *zero;
    const struct MirInsn *branch;
    const struct MirInsn *source;
    int mask_instruction = -1;
    int compare_instruction;
    int branch_instruction;
    int source_value;
    int scan;
    int first_instruction;

    memset(plan, 0, sizeof(*plan));
    if (instruction < 0 || instruction >= mir.count)
        return 0;
    first = &mir.insns[instruction];
    if (first->opcode != MIR_CONST || type_size(first->type) != 4)
        return 0;
    for (scan = instruction + 1;
         scan < mir.count && scan <= instruction + 8; ++scan)
        if (mir.insns[scan].opcode == MIR_BINARY &&
            mir.insns[scan].immediate == '&' &&
            type_size(mir.insns[scan].secondary_offset) == 4) {
            mask = &mir.insns[scan];
            mask_instruction = scan;
            break;
        }
    if (mask == NULL || mir_value_use_count(mask->dst) != 1)
        return 0;
    mask_constant = mir_definition(mask->src2);
    source_value = mask->src1;
    if (mask_constant == NULL ||
        mask_constant->opcode != MIR_CONST) {
        mask_constant = mir_definition(mask->src1);
        source_value = mask->src2;
    }
    if (mask_constant == NULL ||
        mask_constant->opcode != MIR_CONST ||
        type_size(mask_constant->type) != 4 ||
        ((unsigned long)mask_constant->immediate & 0xffffffffUL) == 0 ||
        mir_value_use_count(mask_constant->dst) != 1 ||
        mir_definition(source_value) == NULL ||
        type_size(mir_definition(source_value)->type) != 4)
        return 0;
    compare_instruction =
        mir_low_byte_loop_next_effective(mask_instruction);
    if (compare_instruction >= mir.count)
        return 0;
    compare = &mir.insns[compare_instruction];
    if (compare->opcode != MIR_BINARY ||
        (compare->immediate != TOK_EQ &&
         compare->immediate != TOK_NE) ||
        mir_value_use_count(compare->dst) != 1)
        return 0;
    zero = mir_definition(compare->src1);
    if (compare->src2 == mask->dst) {
        /* zero remains src1 */
    } else if (compare->src1 == mask->dst) {
        zero = mir_definition(compare->src2);
    } else {
        return 0;
    }
    if (zero == NULL || zero->opcode != MIR_CONST ||
        type_size(zero->type) != 4 ||
        ((unsigned long)zero->immediate & 0xffffffffUL) != 0 ||
        mir_value_use_count(zero->dst) != 1)
        return 0;
    branch_instruction =
        mir_low_byte_loop_next_effective(compare_instruction);
    if (branch_instruction >= mir.count)
        return 0;
    branch = &mir.insns[branch_instruction];
    if (branch->opcode != MIR_BRANCH_FALSE ||
        branch->src1 != compare->dst ||
        mir_find_label(branch->label) < 0)
        return 0;
    first_instruction = (int)(mask_constant - mir.insns);
    if ((int)(zero - mir.insns) < first_instruction)
        first_instruction = (int)(zero - mir.insns);
    if (first_instruction != instruction)
        return 0;

    plan->source_value = source_value;
    source = mir_definition(source_value);
    if (source != NULL &&
        (int)(source - mir.insns) >= instruction &&
        (int)(source - mir.insns) <= branch_instruction) {
        int source_type;
        int source_storage;
        int source_offset;

        if (source->opcode != MIR_LOAD ||
            mir_value_use_count(source->dst) != 1 ||
            !mir_scalar_memory_location(
                source, &source_type, &source_storage, &source_offset) ||
            type_size(source_type) != 4 ||
            (source_storage != SC_LOCAL &&
             source_storage != SC_PARAM) ||
            source_offset < -128 || source_offset + 3 > 127 ||
            !mir_wide_mask_zero_add_component(
                plan, (int)(source - mir.insns)))
            return 0;
        plan->source_is_direct = 1;
        plan->source_offset = source_offset;
    }
    plan->mask =
        (unsigned long)mask_constant->immediate & 0xffffffffUL;
    plan->comparison = (int)compare->immediate;
    plan->branch_instruction = branch_instruction;
    if (!mir_wide_mask_zero_add_component(
            plan, (int)(mask_constant - mir.insns)) ||
        !mir_wide_mask_zero_add_component(
            plan, (int)(zero - mir.insns)) ||
        !mir_wide_mask_zero_add_component(plan, mask_instruction) ||
        !mir_wide_mask_zero_add_component(plan, compare_instruction) ||
        !mir_wide_mask_zero_add_component(plan, branch_instruction))
        return 0;
    for (scan = instruction; scan <= branch_instruction; ++scan)
        if (!mir_wide_mask_zero_has_component(plan, scan) &&
            mir.insns[scan].opcode != MIR_NOP)
            return 0;
    return 1;
}

static int mir_function_has_wide_mask_zero_branch(void)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        struct MirWideMaskZeroBranch plan;

        if (mir_match_wide_mask_zero_branch(instruction, &plan))
            return 1;
    }
    return 0;
}

static int mir_wide_byte_pack_add_component(
    struct MirWideBytePackReturn *plan, int instruction)
{
    int component;

    if (instruction < 0 || instruction >= mir.count)
        return 0;
    for (component = 0; component < plan->component_count; ++component)
        if (plan->components[component] == instruction)
            return 1;
    if (plan->component_count >=
        (int)(sizeof(plan->components) / sizeof(plan->components[0])))
        return 0;
    plan->components[plan->component_count++] = instruction;
    return 1;
}

static int mir_wide_byte_pack_has_component(
    const struct MirWideBytePackReturn *plan, int instruction)
{
    int component;

    for (component = 0; component < plan->component_count; ++component)
        if (plan->components[component] == instruction)
            return 1;
    return 0;
}

static int mir_collect_wide_byte_pack_terms(
    int value, struct MirWideBytePackReturn *plan, int depth)
{
    const struct MirInsn *definition = mir_definition(value);
    const struct MirInsn *shift_count = NULL;
    const struct MirInsn *mask;
    const struct MirInsn *mask_constant;
    const struct MirInsn *widen;
    const struct MirInsn *load;
    const struct MirInsn *index;
    const struct MirInsn *index_constant;
    const struct MirInsn *base;
    int shift = 0;
    int masked_value;
    int byte_offset;
    int base_type;
    int base_storage;
    int base_offset;

    if (definition == NULL || depth > 8)
        return 0;
    if (definition->opcode == MIR_BINARY &&
        definition->immediate == '|' &&
        type_size(definition->secondary_offset) == 4) {
        if (!mir_wide_byte_pack_add_component(
                plan, (int)(definition - mir.insns)))
            return 0;
        return mir_collect_wide_byte_pack_terms(
                   definition->src1, plan, depth + 1) &&
               mir_collect_wide_byte_pack_terms(
                   definition->src2, plan, depth + 1);
    }
    if (definition->opcode == MIR_BINARY &&
        definition->immediate == TOK_SHL &&
        type_size(definition->secondary_offset) == 4) {
        shift_count = mir_definition(definition->src2);
        if (shift_count == NULL || shift_count->opcode != MIR_CONST ||
            (shift_count->immediate != 8 &&
             shift_count->immediate != 16 &&
             shift_count->immediate != 24))
            return 0;
        shift = (int)shift_count->immediate;
        masked_value = definition->src1;
        if (!mir_wide_byte_pack_add_component(
                plan, (int)(definition - mir.insns)) ||
            !mir_wide_byte_pack_add_component(
                plan, (int)(shift_count - mir.insns)))
            return 0;
    } else {
        masked_value = value;
    }
    mask = mir_definition(masked_value);
    if (mask == NULL || mask->opcode != MIR_BINARY ||
        mask->immediate != '&' || type_size(mask->secondary_offset) != 4)
        return 0;
    mask_constant = mir_definition(mask->src1);
    value = mask->src2;
    if (mask_constant == NULL || mask_constant->opcode != MIR_CONST ||
        ((unsigned long)mask_constant->immediate & 0xffffffffUL) != 255UL) {
        mask_constant = mir_definition(mask->src2);
        value = mask->src1;
    }
    if (mask_constant == NULL || mask_constant->opcode != MIR_CONST ||
        ((unsigned long)mask_constant->immediate & 0xffffffffUL) != 255UL)
        return 0;
    widen = mir_definition(value);
    if (widen == NULL || widen->opcode != MIR_UNARY ||
        widen->immediate != 0 || type_size(widen->type) != 4)
        return 0;
    load = mir_definition(widen->src1);
    if (load == NULL || load->opcode != MIR_LOAD_INDIRECT ||
        load->memory_size != 1 || load->bit_width != 0)
        return 0;
    index = mir_definition(load->src1);
    if (index == NULL || index->opcode != MIR_INDEX_ADDRESS ||
        index->immediate != 1)
        return 0;
    index_constant = mir_definition(index->src2);
    base = mir_definition(index->src1);
    if (index_constant == NULL ||
        index_constant->opcode != MIR_CONST ||
        index_constant->immediate < 0 ||
        index_constant->immediate > 3 ||
        base == NULL ||
        !mir_scalar_memory_location(base, &base_type, &base_storage,
                                    &base_offset) ||
        type_ptr_depth(base_type) == 0 || type_size(base_type) != 2 ||
        (base_storage != SC_LOCAL && base_storage != SC_PARAM) ||
        base_offset < -128 || base_offset + 1 > 127)
        return 0;
    byte_offset = (int)index_constant->immediate;
    if (plan->shifts[byte_offset] >= 0 ||
        (plan->base_offset != 32767 &&
         plan->base_offset != base_offset))
        return 0;
    plan->base_offset = base_offset;
    plan->shifts[byte_offset] = shift;
    return mir_wide_byte_pack_add_component(
               plan, (int)(mask - mir.insns)) &&
           mir_wide_byte_pack_add_component(
               plan, (int)(mask_constant - mir.insns)) &&
           mir_wide_byte_pack_add_component(
               plan, (int)(widen - mir.insns)) &&
           mir_wide_byte_pack_add_component(
               plan, (int)(load - mir.insns)) &&
           mir_wide_byte_pack_add_component(
               plan, (int)(index - mir.insns)) &&
           mir_wide_byte_pack_add_component(
               plan, (int)(index_constant - mir.insns));
}

static int mir_match_wide_byte_pack_return(
    int instruction, struct MirWideBytePackReturn *plan)
{
    const struct MirInsn *first;
    const struct MirInsn *ret = NULL;
    int return_instruction = -1;
    int scan;
    int offset;

    memset(plan, 0, sizeof(*plan));
    plan->base_offset = 32767;
    for (offset = 0; offset < 4; ++offset)
        plan->shifts[offset] = -1;
    if (instruction < 0 || instruction >= mir.count)
        return 0;
    first = &mir.insns[instruction];
    if (first->opcode != MIR_INDEX_ADDRESS)
        return 0;
    for (scan = instruction + 1; scan < mir.count; ++scan) {
        if (mir.insns[scan].opcode == MIR_LABEL ||
            mir.insns[scan].opcode == MIR_JUMP ||
            mir.insns[scan].opcode == MIR_BRANCH_FALSE)
            return 0;
        if (mir.insns[scan].opcode == MIR_RETURN) {
            ret = &mir.insns[scan];
            return_instruction = scan;
            break;
        }
    }
    if (ret == NULL || ret->src1 < 0 ||
        !mir_collect_wide_byte_pack_terms(ret->src1, plan, 0))
        return 0;
    for (offset = 0; offset < 4; ++offset)
        if (plan->shifts[offset] != 24 - 8 * offset)
            return 0;
    if (!mir_wide_byte_pack_has_component(plan, instruction) ||
        !mir_wide_byte_pack_add_component(plan, return_instruction))
        return 0;
    plan->skip_through = return_instruction;
    for (scan = instruction; scan <= return_instruction; ++scan) {
        const struct MirInsn *insn = &mir.insns[scan];

        if (mir_wide_byte_pack_has_component(plan, scan) ||
            insn->opcode == MIR_NOP)
            continue;
        if (insn->opcode == MIR_STORE &&
            insn->src1 >= 0 &&
            mir_object_is_fully_promoted(insn->object)) {
            const struct MirInsn *source = mir_definition(insn->src1);

            if (source != NULL &&
                mir_wide_byte_pack_has_component(
                    plan, (int)(source - mir.insns))) {
                if (!mir_wide_byte_pack_add_component(plan, scan))
                    return 0;
                continue;
            }
        }
        return 0;
    }
    for (scan = 0; scan < plan->component_count; ++scan) {
        const struct MirInsn *component =
            &mir.insns[plan->components[scan]];

        if (component->dst >= 0 &&
            mir_low_byte_loop_value_used_outside(
                component->dst, instruction, return_instruction))
            return 0;
    }
    return 1;
}

static int mir_wide_byte_store_add_component(
    struct MirWideByteStore *plan, int instruction)
{
    int component;

    if (instruction < 0 || instruction >= mir.count)
        return 0;
    for (component = 0; component < plan->component_count; ++component)
        if (plan->components[component] == instruction)
            return 1;
    if (plan->component_count >=
        (int)(sizeof(plan->components) / sizeof(plan->components[0])))
        return 0;
    plan->components[plan->component_count++] = instruction;
    return 1;
}

static int mir_wide_byte_store_has_component(
    const struct MirWideByteStore *plan, int instruction)
{
    int component;

    for (component = 0; component < plan->component_count; ++component)
        if (plan->components[component] == instruction)
            return 1;
    return 0;
}

static int mir_match_wide_byte_store(
    int instruction, struct MirWideByteStore *plan)
{
    const struct MirInsn *index;
    const struct MirInsn *index_constant;
    const struct MirInsn *base;
    const struct MirInsn *store = NULL;
    const struct MirInsn *conversion;
    const struct MirInsn *mask;
    const struct MirInsn *mask_constant;
    const struct MirInsn *lane;
    const struct MirInsn *shift_count = NULL;
    const struct MirInsn *source;
    int base_type;
    int base_storage;
    int base_offset;
    int source_type;
    int source_storage;
    int source_offset;
    int store_instruction = -1;
    int lane_value;
    int source_value;
    int shift = 0;
    int scan;

    memset(plan, 0, sizeof(*plan));
    if (instruction < 0 || instruction >= mir.count)
        return 0;
    index = &mir.insns[instruction];
    if (index->opcode != MIR_INDEX_ADDRESS || index->immediate != 1 ||
        mir_value_use_count(index->dst) != 1)
        return 0;
    index_constant = mir_definition(index->src2);
    base = mir_definition(index->src1);
    if (index_constant == NULL ||
        index_constant->opcode != MIR_CONST ||
        index_constant->immediate < 0 ||
        index_constant->immediate > 3 ||
        mir_value_use_count(index_constant->dst) != 1 ||
        base == NULL ||
        !mir_scalar_memory_location(base, &base_type, &base_storage,
                                    &base_offset) ||
        type_ptr_depth(base_type) == 0 || type_size(base_type) != 2 ||
        (base_storage != SC_LOCAL && base_storage != SC_PARAM) ||
        base_offset < -128 || base_offset + 1 > 127)
        return 0;
    for (scan = instruction + 1; scan < mir.count; ++scan) {
        if (mir.insns[scan].opcode == MIR_LABEL ||
            mir.insns[scan].opcode == MIR_JUMP ||
            mir.insns[scan].opcode == MIR_BRANCH_FALSE ||
            mir.insns[scan].opcode == MIR_RETURN)
            break;
        if (mir.insns[scan].opcode == MIR_STORE_INDIRECT &&
            mir.insns[scan].src1 == index->dst) {
            store = &mir.insns[scan];
            store_instruction = scan;
            break;
        }
    }
    if (store == NULL || store->memory_size != 1 ||
        store->bit_width != 0)
        return 0;
    conversion = mir_definition(store->src2);
    if (conversion == NULL || conversion->opcode != MIR_UNARY ||
        conversion->immediate != 0 || type_size(conversion->type) != 1 ||
        mir_value_use_count(conversion->dst) != 1)
        return 0;
    mask = mir_definition(conversion->src1);
    if (mask == NULL || mask->opcode != MIR_BINARY ||
        mask->immediate != '&' || type_size(mask->secondary_offset) != 4 ||
        mir_value_use_count(mask->dst) != 1)
        return 0;
    mask_constant = mir_definition(mask->src1);
    lane_value = mask->src2;
    if (mask_constant == NULL || mask_constant->opcode != MIR_CONST ||
        ((unsigned long)mask_constant->immediate & 0xffffffffUL) != 255UL) {
        mask_constant = mir_definition(mask->src2);
        lane_value = mask->src1;
    }
    if (mask_constant == NULL || mask_constant->opcode != MIR_CONST ||
        ((unsigned long)mask_constant->immediate & 0xffffffffUL) != 255UL ||
        mir_value_use_count(mask_constant->dst) != 1)
        return 0;
    lane = mir_definition(lane_value);
    source_value = lane_value;
    if (lane != NULL && lane->opcode == MIR_BINARY &&
        lane->immediate == TOK_SHR &&
        type_size(lane->secondary_offset) == 4 &&
        mir_value_use_count(lane->dst) == 1) {
        shift_count = mir_definition(lane->src2);
        if (shift_count == NULL || shift_count->opcode != MIR_CONST ||
            (shift_count->immediate != 8 &&
             shift_count->immediate != 16 &&
             shift_count->immediate != 24) ||
            mir_value_use_count(shift_count->dst) != 1)
            return 0;
        shift = (int)shift_count->immediate;
        source_value = lane->src1;
    } else {
        lane = NULL;
    }
    source = mir_definition(source_value);
    if (source == NULL || type_size(source->type) != 4 ||
        !mir_scalar_memory_location(source, &source_type, &source_storage,
                                    &source_offset) ||
        type_size(source_type) != 4 ||
        (source_storage != SC_LOCAL && source_storage != SC_PARAM) ||
        source_offset < -128 || source_offset + shift / 8 > 127)
        return 0;

    plan->skip_through = store_instruction;
    plan->base_offset = base_offset;
    plan->source_offset = source_offset + shift / 8;
    plan->byte_offset = (int)index_constant->immediate;
    if (!mir_wide_byte_store_add_component(plan, instruction) ||
        !mir_wide_byte_store_add_component(
            plan, (int)(conversion - mir.insns)) ||
        !mir_wide_byte_store_add_component(
            plan, (int)(mask - mir.insns)) ||
        !mir_wide_byte_store_add_component(
            plan, (int)(mask_constant - mir.insns)) ||
        !mir_wide_byte_store_add_component(plan, store_instruction))
        return 0;
    if (lane != NULL &&
        (!mir_wide_byte_store_add_component(
             plan, (int)(lane - mir.insns)) ||
         !mir_wide_byte_store_add_component(
             plan, (int)(shift_count - mir.insns))))
        return 0;
    for (scan = instruction; scan <= store_instruction; ++scan) {
        const struct MirInsn *insn = &mir.insns[scan];

        if (mir_wide_byte_store_has_component(plan, scan) ||
            insn->opcode == MIR_NOP)
            continue;
        return 0;
    }
    for (scan = 0; scan < plan->component_count; ++scan) {
        const struct MirInsn *component =
            &mir.insns[plan->components[scan]];

        if (component->dst >= 0 &&
            mir_low_byte_loop_value_used_outside(
                component->dst, instruction, store_instruction))
            return 0;
    }
    return 1;
}

static int mir_low_byte_loop_add_component(
    struct MirLowByteIndexLoop *plan, int instruction)
{
    int component;

    if (instruction < 0 || instruction >= mir.count)
        return 0;
    for (component = 0; component < plan->component_count; ++component)
        if (plan->components[component] == instruction)
            return 1;
    if (plan->component_count >=
        (int)(sizeof(plan->components) / sizeof(plan->components[0])))
        return 0;
    plan->components[plan->component_count++] = instruction;
    return 1;
}

static int mir_low_byte_loop_has_component(
    const struct MirLowByteIndexLoop *plan, int instruction)
{
    int component;

    for (component = 0; component < plan->component_count; ++component)
        if (plan->components[component] == instruction)
            return 1;
    return 0;
}

static int mir_low_byte_loop_next_effective(int instruction)
{
    int next = instruction + 1;

    while (next < mir.count) {
        const struct MirInsn *insn = &mir.insns[next];

        if (insn->opcode == MIR_NOP) {
            ++next;
            continue;
        }
        if (insn->opcode == MIR_LOAD && insn->memory_flags == 0 &&
            insn->dst >= 0 && !mir_value_has_use(insn->dst)) {
            ++next;
            continue;
        }
        break;
    }
    return next;
}

static int mir_low_byte_loop_value_used_outside(
    int value, int first, int last)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (instruction >= first && instruction <= last)
            continue;
        if (insn->src1 == value || insn->src2 == value)
            return 1;
        if ((insn->opcode == MIR_CALL ||
             insn->opcode == MIR_CALL_AGGREGATE) &&
            mir_call_uses_value(insn, value))
            return 1;
    }
    return 0;
}

static int mir_low_byte_loop_add_projection(
    int value, int index_value, struct MirLowByteIndexLoop *plan)
{
    const struct MirInsn *conversion = mir_definition(value);
    const struct MirInsn *byte_conversion;
    const struct MirInsn *mask;
    const struct MirInsn *mask_constant;
    const struct MirInsn *addition;
    const struct MirInsn *widen;
    const struct MirInsn *invariant;
    int conversion_index = -1;
    int byte_conversion_index;
    int mask_index;
    int constant_index;
    int addition_index;
    int widen_index;
    int invariant_type;
    int invariant_storage;
    int invariant_offset;
    int addition_value;
    int widen_value;
    int invariant_value;

    if (conversion != NULL && conversion->opcode == MIR_UNARY &&
        conversion->immediate == 0 && type_size(conversion->type) == 2) {
        const struct MirInsn *source = mir_definition(conversion->src1);

        if (source == NULL || type_size(source->type) != 1 ||
            mir_value_use_count(conversion->dst) != 1)
            return 0;
        conversion_index = (int)(conversion - mir.insns);
        value = conversion->src1;
        conversion = source;
    }
    byte_conversion = conversion;
    if (byte_conversion == NULL ||
        byte_conversion->opcode != MIR_UNARY ||
        byte_conversion->immediate != 0 ||
        type_size(byte_conversion->type) != 1 ||
        mir_value_use_count(byte_conversion->dst) != 1)
        return 0;
    mask = mir_definition(byte_conversion->src1);
    if (mask == NULL || mask->opcode != MIR_BINARY ||
        mask->immediate != '&' || type_size(mask->secondary_offset) != 4 ||
        mir_value_use_count(mask->dst) != 1)
        return 0;
    mask_constant = mir_definition(mask->src1);
    addition_value = mask->src2;
    if (mask_constant == NULL || mask_constant->opcode != MIR_CONST ||
        ((unsigned long)mask_constant->immediate & 0xffffffffUL) != 255UL) {
        mask_constant = mir_definition(mask->src2);
        addition_value = mask->src1;
    }
    if (mask_constant == NULL || mask_constant->opcode != MIR_CONST ||
        ((unsigned long)mask_constant->immediate & 0xffffffffUL) != 255UL ||
        mir_value_use_count(mask_constant->dst) != 1)
        return 0;
    addition = mir_definition(addition_value);
    if (addition == NULL || addition->opcode != MIR_BINARY ||
        addition->immediate != '+' ||
        type_size(addition->secondary_offset) != 4 ||
        mir_value_use_count(addition->dst) != 1)
        return 0;
    widen_value = addition->src1;
    invariant_value = addition->src2;
    widen = mir_definition(widen_value);
    if (widen == NULL || widen->opcode != MIR_UNARY ||
        widen->immediate != 0 || type_size(widen->type) != 4 ||
        widen->src1 != index_value) {
        widen_value = addition->src2;
        invariant_value = addition->src1;
        widen = mir_definition(widen_value);
    }
    if (widen == NULL || widen->opcode != MIR_UNARY ||
        widen->immediate != 0 || type_size(widen->type) != 4 ||
        widen->src1 != index_value ||
        mir_value_use_count(widen->dst) != 1)
        return 0;
    invariant = mir_definition(invariant_value);
    if (invariant == NULL || type_size(invariant->type) != 4 ||
        !mir_scalar_memory_location(invariant, &invariant_type,
                                    &invariant_storage,
                                    &invariant_offset) ||
        type_size(invariant_type) != 4 ||
        (invariant_storage != SC_LOCAL &&
         invariant_storage != SC_PARAM) ||
        invariant_offset < -128 || invariant_offset > 127)
        return 0;

    byte_conversion_index = (int)(byte_conversion - mir.insns);
    mask_index = (int)(mask - mir.insns);
    constant_index = (int)(mask_constant - mir.insns);
    addition_index = (int)(addition - mir.insns);
    widen_index = (int)(widen - mir.insns);
    if ((conversion_index >= 0 &&
         !mir_low_byte_loop_add_component(plan, conversion_index)) ||
        !mir_low_byte_loop_add_component(plan, byte_conversion_index) ||
        !mir_low_byte_loop_add_component(plan, mask_index) ||
        !mir_low_byte_loop_add_component(plan, constant_index) ||
        !mir_low_byte_loop_add_component(plan, addition_index) ||
        !mir_low_byte_loop_add_component(plan, widen_index))
        return 0;
    plan->invariant_offset = invariant_offset;
    return 1;
}

static int mir_low_byte_loop_promotes_value(
    int value, int source_value, struct MirLowByteIndexLoop *plan)
{
    const struct MirInsn *conversion;

    if (value == source_value)
        return 1;
    conversion = mir_definition(value);
    if (conversion == NULL || conversion->opcode != MIR_UNARY ||
        conversion->immediate != 0 || conversion->src1 != source_value ||
        type_size(conversion->type) != 2 ||
        mir_value_use_count(conversion->dst) != 1)
        return 0;
    return mir_low_byte_loop_add_component(
        plan, (int)(conversion - mir.insns));
}

static int mir_low_byte_loop_match_body(
    int first, int latch, int index_value, struct MirLowByteIndexLoop *plan)
{
    int instruction;

    for (instruction = first; instruction < latch; ++instruction) {
        const struct MirInsn *index = &mir.insns[instruction];
        const struct MirInsn *base;
        int base_type;
        int base_storage;
        int base_offset;
        int use;

        if (index->opcode != MIR_INDEX_ADDRESS ||
            index->src2 != index_value || index->immediate != 1 ||
            mir_value_use_count(index->dst) != 1)
            continue;
        base = mir_definition(index->src1);
        if (base == NULL ||
            !mir_scalar_memory_location(base, &base_type, &base_storage,
                                        &base_offset) ||
            type_ptr_depth(base_type) == 0 ||
            type_size(base_type) != 2 ||
            (base_storage != SC_LOCAL && base_storage != SC_PARAM) ||
            base_offset < -128 || base_offset + 1 > 127)
            continue;

        use = instruction + 1;
        while (use < latch && mir.insns[use].opcode == MIR_NOP)
            ++use;
        if (use >= latch ||
            mir.insns[use].opcode != MIR_STORE_INDIRECT ||
            mir.insns[use].src1 != index->dst)
            for (use = instruction + 1; use < latch; ++use)
                if (mir.insns[use].opcode == MIR_STORE_INDIRECT &&
                    mir.insns[use].src1 == index->dst)
                    break;
        if (use < latch && mir.insns[use].opcode == MIR_STORE_INDIRECT &&
            mir.insns[use].src1 == index->dst &&
            mir.insns[use].memory_size == 1 &&
            mir.insns[use].bit_width == 0) {
            struct MirLowByteIndexLoop candidate = *plan;

            if (!mir_low_byte_loop_add_projection(
                    mir.insns[use].src2, index_value, &candidate))
                continue;
            candidate.kind = MIR_LOW_BYTE_INDEX_STORE;
            candidate.base_offset = base_offset;
            if (!mir_low_byte_loop_add_component(&candidate, instruction) ||
                !mir_low_byte_loop_add_component(&candidate, use))
                continue;
            if ((int)(base - mir.insns) >= first &&
                !mir_low_byte_loop_add_component(
                    &candidate, (int)(base - mir.insns)))
                continue;
            *plan = candidate;
            return 1;
        }
        use = instruction + 1;
        while (use < latch && mir.insns[use].opcode == MIR_NOP)
            ++use;
        if (use >= latch ||
            mir.insns[use].opcode != MIR_LOAD_INDIRECT ||
            mir.insns[use].src1 != index->dst)
            for (use = instruction + 1; use < latch; ++use)
                if (mir.insns[use].opcode == MIR_LOAD_INDIRECT &&
                    mir.insns[use].src1 == index->dst)
                    break;
        if (use < latch && mir.insns[use].opcode == MIR_LOAD_INDIRECT &&
            mir.insns[use].src1 == index->dst &&
            mir.insns[use].memory_size == 1 &&
            mir.insns[use].bit_width == 0 &&
            mir_value_use_count(mir.insns[use].dst) == 1) {
            int compare_index;

            for (compare_index = use + 1;
                 compare_index < latch; ++compare_index) {
                const struct MirInsn *compare =
                    &mir.insns[compare_index];
                int projection_value;
                int loaded_value;
                int branch_index;
                int continue_instruction;
                int mismatch_const;
                int mismatch_return;
                struct MirLowByteIndexLoop candidate = *plan;

                if (compare->opcode != MIR_BINARY ||
                    compare->immediate != TOK_NE ||
                    mir_value_use_count(compare->dst) != 1)
                    continue;
                loaded_value = compare->src1;
                projection_value = compare->src2;
                if (!mir_low_byte_loop_promotes_value(
                        loaded_value, mir.insns[use].dst, &candidate)) {
                    loaded_value = compare->src2;
                    projection_value = compare->src1;
                    candidate = *plan;
                    if (!mir_low_byte_loop_promotes_value(
                            loaded_value, mir.insns[use].dst, &candidate))
                        continue;
                }
                if (!mir_low_byte_loop_add_projection(
                        projection_value, index_value, &candidate))
                    continue;
                branch_index = mir_low_byte_loop_next_effective(
                    compare_index);
                if (branch_index >= latch ||
                    mir.insns[branch_index].opcode != MIR_BRANCH_FALSE ||
                    mir.insns[branch_index].src1 != compare->dst)
                    continue;
                continue_instruction =
                    mir_find_label(mir.insns[branch_index].label);
                if (continue_instruction <= branch_index ||
                    continue_instruction > latch ||
                    !mir_phi_copies_are_empty(
                        branch_index, continue_instruction))
                    continue;
                mismatch_const =
                    mir_low_byte_loop_next_effective(branch_index);
                mismatch_return =
                    mir_low_byte_loop_next_effective(mismatch_const);
                if (mismatch_const >= continue_instruction ||
                    mismatch_return >= continue_instruction ||
                    type_size(mir.return_type) != 2 ||
                    mir.insns[mismatch_const].opcode != MIR_CONST ||
                    type_size(mir.insns[mismatch_const].type) != 2 ||
                    mir.insns[mismatch_const].immediate != 0 ||
                    mir.insns[mismatch_return].opcode != MIR_RETURN ||
                    mir.insns[mismatch_return].src1 !=
                        mir.insns[mismatch_const].dst)
                    continue;
                while (continue_instruction < latch &&
                       (mir.insns[continue_instruction].opcode == MIR_LABEL ||
                        mir.insns[continue_instruction].opcode == MIR_NOP)) {
                    if (mir.insns[continue_instruction].opcode == MIR_LABEL &&
                        !mir_low_byte_loop_add_component(
                            &candidate, continue_instruction))
                        break;
                    ++continue_instruction;
                }
                if (continue_instruction != latch)
                    continue;
                candidate.kind = MIR_LOW_BYTE_INDEX_COMPARE;
                candidate.base_offset = base_offset;
                candidate.continue_label =
                    mir.insns[branch_index].label;
                if (!mir_low_byte_loop_add_component(
                        &candidate, instruction) ||
                    !mir_low_byte_loop_add_component(&candidate, use) ||
                    !mir_low_byte_loop_add_component(
                        &candidate, compare_index) ||
                    !mir_low_byte_loop_add_component(
                        &candidate, branch_index) ||
                    !mir_low_byte_loop_add_component(
                        &candidate, mismatch_const) ||
                    !mir_low_byte_loop_add_component(
                        &candidate, mismatch_return))
                    continue;
                if ((int)(base - mir.insns) >= first &&
                    !mir_low_byte_loop_add_component(
                        &candidate, (int)(base - mir.insns)))
                    continue;
                *plan = candidate;
                return 1;
            }
        }
    }
    return 0;
}

static int mir_match_low_byte_index_loop(
    int instruction, struct MirLowByteIndexLoop *plan)
{
    const struct MirInsn *initial;
    const struct MirInsn *store;
    const struct MirInsn *header;
    const struct MirInsn *phi = NULL;
    const struct MirInsn *condition;
    const struct MirInsn *limit;
    const struct MirInsn *promote;
    const struct MirInsn *branch;
    const struct MirInsn *one;
    const struct MirInsn *increment;
    const struct MirInsn *update_store;
    const struct MirInsn *jump;
    int store_instruction;
    int header_instruction;
    int phi_instruction = -1;
    int condition_instruction;
    int branch_instruction;
    int latch_instruction;
    int one_instruction;
    int increment_instruction;
    int update_store_instruction;
    int jump_instruction;
    int exit_instruction;
    int index_type;
    int index_storage;
    int index_offset;
    int scan;

    memset(plan, 0, sizeof(*plan));
    plan->continue_label = -1;
    plan->invariant_offset = 0;
    if (instruction < 0 || instruction >= mir.count)
        return 0;
    initial = &mir.insns[instruction];
    if (initial->opcode != MIR_CONST || initial->dst < 0 ||
        mir_value_use_count(initial->dst) != 2 ||
        initial->immediate < 0 || initial->immediate > 254)
        return 0;
    store_instruction = mir_low_byte_loop_next_effective(instruction);
    if (store_instruction >= mir.count)
        return 0;
    store = &mir.insns[store_instruction];
    if (store->opcode != MIR_STORE || store->src1 != initial->dst ||
        store->object < 0 || store->memory_size != 1)
        return 0;
    header_instruction =
        mir_low_byte_loop_next_effective(store_instruction);
    if (header_instruction >= mir.count)
        return 0;
    header = &mir.insns[header_instruction];
    if (header->opcode != MIR_LABEL)
        return 0;
    for (scan = header_instruction + 1;
         scan < mir.count && scan <= header_instruction + 8; ++scan)
        if (mir.insns[scan].opcode == MIR_PHI &&
            mir.insns[scan].object == store->object &&
            mir.insns[scan].src1 == initial->dst) {
            phi = &mir.insns[scan];
            phi_instruction = scan;
            break;
        }
    if (phi == NULL || type_size(phi->type) != 1 ||
        !mir_scalar_memory_location(phi, &index_type, &index_storage,
                                    &index_offset) ||
        type_size(index_type) != 1 ||
        (index_storage != SC_LOCAL && index_storage != SC_PARAM) ||
        index_offset < -128 || index_offset > 127)
        return 0;

    branch_instruction = -1;
    for (scan = phi_instruction + 1;
         scan < mir.count && scan <= phi_instruction + 8; ++scan)
        if (mir.insns[scan].opcode == MIR_BRANCH_FALSE) {
            branch_instruction = scan;
            break;
        }
    if (branch_instruction < 0)
        return 0;
    branch = &mir.insns[branch_instruction];
    condition = mir_definition(branch->src1);
    if (condition == NULL || condition->opcode != MIR_BINARY ||
        condition->immediate != '<' ||
        mir_value_use_count(condition->dst) != 1)
        return 0;
    condition_instruction = (int)(condition - mir.insns);
    promote = mir_definition(condition->src1);
    limit = mir_definition(condition->src2);
    if (promote == NULL || promote->opcode != MIR_UNARY ||
        promote->immediate != 0 || promote->src1 != phi->dst ||
        type_size(promote->type) != 2 ||
        mir_value_use_count(promote->dst) != 1 ||
        limit == NULL || limit->opcode != MIR_CONST ||
        limit->immediate <= initial->immediate ||
        limit->immediate > 255 ||
        (((index_type & TYPE_UNSIGNED) == 0) &&
         limit->immediate > 127) ||
        mir_value_use_count(limit->dst) != 1)
        return 0;
    exit_instruction = mir_find_label(branch->label);
    latch_instruction = mir_find_label(phi->phi_pred2);
    if (exit_instruction <= branch_instruction ||
        latch_instruction <= branch_instruction ||
        latch_instruction >= exit_instruction ||
        !mir_phi_copies_are_empty(branch_instruction, exit_instruction))
        return 0;

    one_instruction = mir_low_byte_loop_next_effective(latch_instruction);
    increment_instruction =
        mir_low_byte_loop_next_effective(one_instruction);
    update_store_instruction =
        mir_low_byte_loop_next_effective(increment_instruction);
    jump_instruction =
        mir_low_byte_loop_next_effective(update_store_instruction);
    if (jump_instruction >= exit_instruction)
        return 0;
    one = &mir.insns[one_instruction];
    increment = &mir.insns[increment_instruction];
    update_store = &mir.insns[update_store_instruction];
    jump = &mir.insns[jump_instruction];
    if (one->opcode != MIR_CONST || one->immediate != 1 ||
        mir_value_use_count(one->dst) != 1 ||
        increment->opcode != MIR_BINARY || increment->immediate != '+' ||
        increment->src1 != phi->dst || increment->src2 != one->dst ||
        increment->dst != phi->src2 ||
        type_size(increment->secondary_offset) != 1 ||
        update_store->opcode != MIR_STORE ||
        update_store->src1 != increment->dst ||
        update_store->object != store->object ||
        jump->opcode != MIR_JUMP || jump->label != header->label)
        return 0;
    for (scan = phi_instruction + 1;
         scan < branch_instruction; ++scan)
        if (mir.insns[scan].opcode == MIR_PHI)
            return 0;

    plan->skip_through = jump_instruction;
    plan->header_label = header->label;
    plan->initial = (int)initial->immediate;
    plan->limit = (int)limit->immediate;
    plan->index_object = store->object;
    if (!mir_low_byte_loop_add_component(plan, instruction) ||
        !mir_low_byte_loop_add_component(plan, store_instruction) ||
        !mir_low_byte_loop_add_component(plan, header_instruction) ||
        !mir_low_byte_loop_add_component(plan, phi_instruction) ||
        !mir_low_byte_loop_add_component(
            plan, (int)(promote - mir.insns)) ||
        !mir_low_byte_loop_add_component(
            plan, (int)(limit - mir.insns)) ||
        !mir_low_byte_loop_add_component(plan, condition_instruction) ||
        !mir_low_byte_loop_add_component(plan, branch_instruction) ||
        !mir_low_byte_loop_add_component(plan, latch_instruction) ||
        !mir_low_byte_loop_add_component(plan, one_instruction) ||
        !mir_low_byte_loop_add_component(plan, increment_instruction) ||
        !mir_low_byte_loop_add_component(plan, update_store_instruction) ||
        !mir_low_byte_loop_add_component(plan, jump_instruction) ||
        !mir_low_byte_loop_match_body(
            branch_instruction + 1, latch_instruction, phi->dst, plan))
        return 0;

    for (scan = instruction; scan <= plan->skip_through; ++scan) {
        const struct MirInsn *insn = &mir.insns[scan];

        if (mir_low_byte_loop_has_component(plan, scan) ||
            insn->opcode == MIR_NOP)
            continue;
        if (insn->opcode == MIR_LOAD && insn->memory_flags == 0 &&
            insn->dst >= 0 && !mir_value_has_use(insn->dst))
            continue;
        return 0;
    }
    for (scan = 0; scan < mir.count; ++scan) {
        const struct MirInsn *insn = &mir.insns[scan];

        if (scan >= instruction && scan <= plan->skip_through)
            continue;
        if (insn->object == plan->index_object &&
            (insn->opcode == MIR_LOAD || insn->opcode == MIR_STORE ||
             insn->opcode == MIR_PARAM ||
             insn->opcode == MIR_OBJECT_MERGE ||
             insn->opcode == MIR_PHI))
            return 0;
    }
    for (scan = 0; scan < plan->component_count; ++scan) {
        const struct MirInsn *component =
            &mir.insns[plan->components[scan]];

        if (component->dst >= 0 &&
            mir_low_byte_loop_value_used_outside(
                component->dst, instruction, plan->skip_through))
            return 0;
    }
    return 1;
}

static int mir_match_indirect_incdec(
    int instruction, struct MirIndirectIncDec *plan)
{
    const struct MirInsn *load;
    const struct MirInsn *constant;
    const struct MirInsn *binary;
    const struct MirInsn *store;
    const struct MirInsn *ret;
    int returns_adjusted;
    int scan;

    /*
     * First rollout: isolated update helpers only. Multi-block functions can
     * execute this shape in very hot paths where the surrounding MIR register
     * schedule outweighs its local instruction saving (cint/cobint emit).
     */
    if (mir_cfg_block_count() != 1 ||
        instruction < 0 || instruction + 4 >= mir.count)
        return 0;
    load = &mir.insns[instruction];
    constant = &mir.insns[instruction + 1];
    binary = &mir.insns[instruction + 2];
    store = &mir.insns[instruction + 3];
    ret = &mir.insns[instruction + 4];
    if (load->opcode != MIR_LOAD_INDIRECT ||
        load->bit_width != 0 ||
        (load->memory_size != 1 &&
         load->memory_size != 2 &&
         load->memory_size != 4) ||
        type_size(load->type) != load->memory_size ||
        type_is_bool(load->type) ||
        constant->opcode != MIR_CONST ||
        constant->immediate != 1 ||
        constant->dst < 0 ||
        binary->opcode != MIR_BINARY ||
        (binary->immediate != '+' && binary->immediate != '-') ||
        binary->src1 != load->dst ||
        binary->src2 != constant->dst ||
        (binary->secondary_offset != 0 &&
         type_size(binary->secondary_offset) != load->memory_size) ||
        store->opcode != MIR_STORE_INDIRECT ||
        store->src1 != load->src1 ||
        store->src2 != binary->dst ||
        store->bit_width != 0 ||
        store->memory_size != load->memory_size ||
        ret->opcode != MIR_RETURN ||
        (ret->src1 != load->dst && ret->src1 != binary->dst))
        return 0;
    returns_adjusted = ret->src1 == binary->dst;
    if (load->memory_size == 4 && returns_adjusted)
        return 0;
    for (scan = 0; scan < mir.count; ++scan) {
        const struct MirInsn *use = &mir.insns[scan];

        if (use->src1 == load->dst || use->src2 == load->dst ||
            mir_call_uses_value(use, load->dst)) {
            if (!((scan == instruction + 2 &&
                   use->src1 == load->dst) ||
                  (!returns_adjusted &&
                   scan == instruction + 4 &&
                   use->src1 == load->dst)))
                return 0;
        }
        if (use->src1 == constant->dst ||
            use->src2 == constant->dst ||
            mir_call_uses_value(use, constant->dst))
            if (!(scan == instruction + 2 &&
                  use->src2 == constant->dst))
                return 0;
        if (use->src1 == binary->dst || use->src2 == binary->dst ||
            mir_call_uses_value(use, binary->dst)) {
            if (!((scan == instruction + 3 &&
                   use->src2 == binary->dst) ||
                  (returns_adjusted &&
                   scan == instruction + 4 &&
                   use->src1 == binary->dst)))
                return 0;
        }
        if (use->src1 == load->src1 || use->src2 == load->src1 ||
            mir_call_uses_value(use, load->src1)) {
            if (!((scan == instruction &&
                   use->src1 == load->src1) ||
                  (scan == instruction + 3 &&
                   use->src1 == load->src1)))
                return 0;
        }
    }
    plan->load_instruction = instruction;
    plan->constant_instruction = instruction + 1;
    plan->binary_instruction = instruction + 2;
    plan->store_instruction = instruction + 3;
    plan->return_instruction = instruction + 4;
    plan->address_value = load->src1;
    plan->old_value = load->dst;
    plan->adjusted_value = binary->dst;
    plan->memory_size = load->memory_size;
    plan->type = load->type;
    plan->decrement = binary->immediate == '-';
    plan->returns_adjusted = returns_adjusted;
    return 1;
}

static void mir_emit_indirect_incdec_normalize(MirStream *out, int type)
{
    if (type_size(type) != 1)
        return;
    if ((type & TYPE_UNSIGNED) != 0)
        mir_stream_puts("\tld h,0\n", out);
    else
        mir_emit_signed_byte_extend(out);
}

static void mir_emit_indirect_incdec(
    MirStream *out, const struct MirIndirectIncDec *plan)
{
    int adjust_label;

    mir_spilled_cfg_used_indirect_incdec = 1;
    mir_emit_virtual_load(out, plan->address_value);
    if (plan->memory_size == 4) {
        mir_stream_puts("\tld b,h\n\tld c,l\n"
              "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
              "\tinc hl\n\tld a,(hl)\n\tinc hl\n"
              "\tld h,(hl)\n\tld l,a\n\tex de,hl\n",
              out);
        if (!plan->returns_adjusted)
            mir_stream_puts("\tpush de\n\tpush hl\n", out);
        adjust_label = new_label();
        if (plan->decrement)
            mir_stream_printf(out,
                    "\tld a,h\n\tor l\n\tdec hl\n"
                    "\tjp nz, L%d\n\tdec de\nL%d:\n",
                    adjust_label, adjust_label);
        else
            mir_stream_printf(out,
                    "\tinc hl\n\tld a,h\n\tor l\n"
                    "\tjp nz, L%d\n\tinc de\nL%d:\n",
                    adjust_label, adjust_label);
        mir_stream_puts("\tpush hl\n\tld h,b\n\tld l,c\n\tpop bc\n"
              "\tld (hl),c\n\tinc hl\n\tld (hl),b\n"
              "\tinc hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
              out);
        if (!plan->returns_adjusted)
            mir_stream_puts("\tpop hl\n\tpop de\n", out);
        else
            mir_stream_puts("\tld h,b\n\tld l,c\n", out);
        mir_emit_instruction_index = plan->store_instruction;
        mir_forwarded_wide_value = plan->returns_adjusted
            ? plan->adjusted_value : plan->old_value;
        mir_forwarded_wide_instruction = plan->store_instruction;
        return;
    }
    mir_stream_puts("\tpush hl\n", out);
    if (plan->memory_size == 1) {
        mir_stream_puts("\tld l,(hl)\n", out);
        mir_emit_indirect_incdec_normalize(out, plan->type);
    } else {
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n",
              out);
    }
    if (!plan->returns_adjusted)
        mir_stream_puts("\tpush hl\n", out);
    mir_stream_puts(plan->decrement ? "\tdec hl\n" : "\tinc hl\n", out);
    if (plan->returns_adjusted)
        mir_emit_indirect_incdec_normalize(out, plan->type);
    mir_stream_puts("\tex de,hl\n", out);
    if (!plan->returns_adjusted)
        mir_stream_puts("\tpop hl\n\tex (sp),hl\n", out);
    else
        mir_stream_puts("\tpop hl\n", out);
    if (plan->memory_size == 1)
        mir_stream_puts("\tld (hl),e\n", out);
    else
        mir_stream_puts("\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    if (!plan->returns_adjusted)
        mir_stream_puts("\tpop hl\n", out);
    else
        mir_stream_puts("\tex de,hl\n", out);
    mir_emit_instruction_index = plan->store_instruction;
    mir_forwarded_hl_value = plan->returns_adjusted
        ? plan->adjusted_value : plan->old_value;
    mir_forwarded_hl_instruction = plan->store_instruction;
}

static int mir_prepare_backend_slots(void)
{
    int *first;
    int *last;
    int *slot_end;
    char *fused_away = NULL;
    int planned_narrow_cache_call = -1;
    int planned_wide_cache_call = -1;
    int slot_capacity;
    int value;
    int i;

    if (mir.next_value <= 0) {
        mir.backend_slot_count = 0;
        return 0;
    }

    if (mir.backend_slot_capacity < mir.next_value) {
        int *new_slots = (int *)realloc(
            mir.backend_slots, (size_t)mir.next_value * sizeof(*new_slots));
        if (new_slots == NULL)
            fatal("out of memory allocating MIR backend slots");
        mir.backend_slots = new_slots;
        mir.backend_slot_capacity = mir.next_value;
    }
    if (mir.planned_stack_consumer_capacity < mir.next_value) {
        int *new_consumers = (int *)realloc(
            mir.planned_stack_consumers,
            (size_t)mir.next_value * sizeof(*new_consumers));
        if (new_consumers == NULL)
            fatal("out of memory allocating MIR planned stack consumers");
        mir.planned_stack_consumers = new_consumers;
        mir.planned_stack_consumer_capacity = mir.next_value;
    }
    if (mir.planned_stack_value_capacity < mir.count) {
        int *new_values = (int *)realloc(
            mir.planned_stack_values,
            (size_t)mir.count * sizeof(*new_values));
        if (new_values == NULL)
            fatal("out of memory allocating MIR planned stack values");
        mir.planned_stack_values = new_values;
        mir.planned_stack_value_capacity = mir.count;
    }
    if (mir.planned_stack_emitted_capacity < mir.next_value) {
        unsigned char *new_emitted = (unsigned char *)realloc(
            mir.planned_stack_emitted,
            (size_t)mir.next_value * sizeof(*new_emitted));
        if (new_emitted == NULL)
            fatal("out of memory allocating MIR planned stack state");
        mir.planned_stack_emitted = new_emitted;
        mir.planned_stack_emitted_capacity = mir.next_value;
    }
    for (value = 0; value < mir.next_value; ++value)
        mir.planned_stack_consumers[value] = -1;
    memset(mir.planned_stack_emitted, 0,
           (size_t)mir.next_value * sizeof(*mir.planned_stack_emitted));
    for (i = 0; i < mir.count; ++i)
        mir.planned_stack_values[i] = -1;
    first = (int *)malloc((size_t)mir.next_value * sizeof(*first));
    last = (int *)malloc((size_t)mir.next_value * sizeof(*last));
    slot_capacity = mir.next_value * 2 + mir.object_count * 2;
    slot_end = (int *)malloc((size_t)slot_capacity * sizeof(*slot_end));
    if (first == NULL || last == NULL || slot_end == NULL)
        fatal("out of memory computing MIR backend intervals");
    if (mir_backend_slot_offset_capacity < slot_capacity) {
        int *new_offsets = (int *)realloc(
            mir_backend_slot_offsets,
            (size_t)slot_capacity * sizeof(*new_offsets));
        if (new_offsets == NULL)
            fatal("out of memory computing MIR backend slot offsets");
        mir_backend_slot_offsets = new_offsets;
        mir_backend_slot_offset_capacity = slot_capacity;
    }
    fused_away = (char *)calloc((size_t)mir.next_value, 1);
    if (fused_away == NULL)
        fatal("out of memory computing MIR fused-value set");
    if (mir_backend_slots_skip_fused_comparisons) {
        for (i = 0; i < mir.count; ++i) {
            int skip = mir_binary_is_fusable_comparison(i);
            if (skip > 0)
                fused_away[mir.insns[i].dst] = 1;
            if (skip == 2)
                fused_away[mir.insns[i + 1].dst] = 1;
            if (mir_unary_is_fusable_not_branch(i))
                fused_away[mir.insns[i].dst] = 1;
            {
                int multiply_index;
                if (mir_float_madd_match(i, &multiply_index, NULL))
                    fused_away[mir.insns[multiply_index].dst] = 1;
            }
        }
    }
    for (i = 0; i < mir.count; ++i) {
        struct MirLowByteIndexLoop loop;
        int component;

        if (!mir_match_low_byte_index_loop(i, &loop))
            continue;
        for (component = 0; component < loop.component_count; ++component) {
            int value = mir.insns[loop.components[component]].dst;

            if (value >= 0)
                fused_away[value] = 2;
        }
    }
    for (i = 0; i < mir.count; ++i) {
        struct MirWideByteStore store;
        int component;

        if (!mir_match_wide_byte_store(i, &store))
            continue;
        for (component = 0; component < store.component_count; ++component) {
            int value = mir.insns[store.components[component]].dst;

            if (value >= 0)
                fused_away[value] = 2;
        }
    }
    for (i = 0; i < mir.count; ++i) {
        struct MirWideBytePackReturn pack;
        int component;

        if (!mir_match_wide_byte_pack_return(i, &pack))
            continue;
        for (component = 0; component < pack.component_count; ++component) {
            int value = mir.insns[pack.components[component]].dst;

            if (value >= 0)
                fused_away[value] = 2;
        }
    }
    for (i = 0; i < mir.count; ++i) {
        struct MirWideMaskZeroBranch branch;
        int component;

        if (!mir_match_wide_mask_zero_branch(i, &branch))
            continue;
        for (component = 0; component < branch.component_count;
             ++component) {
            int value = mir.insns[branch.components[component]].dst;

            if (value >= 0)
                fused_away[value] = 2;
        }
    }
    for (i = 0; i < mir.count; ++i) {
        struct MirIndirectIncDec incdec;

        if (!mir_match_indirect_incdec(i, &incdec))
            continue;
        fused_away[incdec.old_value] = 2;
        fused_away[mir.insns[incdec.constant_instruction].dst] = 2;
        fused_away[incdec.adjusted_value] = 2;
        fused_away[incdec.address_value] = 2;
    }
    for (value = 0; value < mir.next_value; ++value) {
        first[value] = mir.count;
        last[value] = -1;
        mir.backend_slots[value] = -1;
    }
    for (value = 0; value < slot_capacity; ++value)
        slot_end[value] = -1;
    for (i = 0; i < mir.count; ++i) {
        const struct MirInsn *insn = &mir.insns[i];
        if (insn->dst >= 0) {
            first[insn->dst] =
                insn->opcode == MIR_PHI
                    ? mir_phi_physical_start(i)
                    : i;
            last[insn->dst] = i;
        }
        if (insn->src1 >= 0 && last[insn->src1] < i)
            last[insn->src1] = i;
        if (insn->src2 >= 0 && last[insn->src2] < i)
            last[insn->src2] = i;
        if (insn->opcode == MIR_CALL ||
            insn->opcode == MIR_CALL_AGGREGATE) {
            int argument;
            for (argument = 0; argument < mir.next_value; ++argument)
                if (mir_call_uses_value(insn, argument) &&
                    last[argument] < i)
                    last[argument] = i;
        }
    }
    for (i = 0; i < mir.count; ++i)
        if (mir.insns[i].opcode == MIR_PHI) {
            int predecessor;
            for (predecessor = 0; predecessor < mir.count; ++predecessor) {
                int successor;
                int predecessor_label = mir_block_label_before(predecessor);
                for (successor = 0;
                     successor < mir.insns[predecessor].successor_count;
                     ++successor) {
                    int block_start =
                        mir.insns[predecessor].successors[successor];
                    int target =
                        mir_next_phi_in_block(block_start, block_start);
                    while (target >= 0 && target < i)
                        target =
                            mir_next_phi_in_block(block_start, target + 1);
                    if (target != i)
                        continue;
                    if (predecessor_label == mir.insns[i].phi_pred1 &&
                        last[mir.insns[i].src1] < predecessor)
                        last[mir.insns[i].src1] = predecessor;
                    if (predecessor_label == mir.insns[i].phi_pred2 &&
                        last[mir.insns[i].src2] < predecessor)
                        last[mir.insns[i].src2] = predecessor;
                }
            }
        }
    for (i = 0; i < mir.count; ++i)
        if (mir.insns[i].opcode == MIR_JUMP) {
            int target = mir_find_label(mir.insns[i].label);
            if (target >= 0 && target < i)
                for (value = 0; value < mir.next_value; ++value)
                    if (first[value] < target && last[value] >= target &&
                        last[value] < i)
                        last[value] = i;
        }
    /* mir-migration-plan-next10 (param-direct follow-up): a fused
     * divmod pair (mir_divmod_partner, emission ~9000) writes BOTH the
     * modulo and division result values eagerly at whichever of the two
     * MIR_BINARY instructions is encountered first in program order, not
     * at each result's own defining instruction - the second instruction
     * only "break"s without emitting anything, trusting the earlier
     * store. first[]/last[] computed above only account for each value's
     * own instruction index, so a result written early this way was
     * still reported as first-live only at its own (later) instruction,
     * understating its true live range by the gap between the two
     * instructions. Any other value's slot reused across exactly that
     * gap can then alias and clobber the early-written result before its
     * real use - a latent hazard nothing forced into the open until the
     * parameter-rehoming change (below) perturbed slot numbering enough
     * for two such values to collide (surfaced as tmuldiv's ui16_test
     * modulo/division mixups). Extend both partners' first[] to the
     * earlier instruction so slot reuse across that gap is impossible. */
    for (i = 0; i < mir.count; ++i) {
        const struct MirInsn *insn = &mir.insns[i];
        int partner;
        if (insn->opcode != MIR_BINARY ||
            (insn->immediate != '/' && insn->immediate != '%'))
            continue;
        partner = mir_divmod_partner(i);
        if (partner > i) {
            if (first[insn->dst] > i)
                first[insn->dst] = i;
            if (first[mir.insns[partner].dst] > i)
                first[mir.insns[partner].dst] = i;
        }
    }
    /* Item T51 (mir-text-size-plan.md): mir_binary_only_constant and
     * mir_index_only_constant already make the MIR_CONST case (above,
     * where the value is defined) and the MIR_BINARY/MIR_INDEX_ADDRESS
     * consumer sites (below, in the emitter) both emit zero code for a
     * constant whose sole use qualifies - the definition site skips its
     * own "ld hl,<const>"/store entirely, and the consumer materializes
     * the constant directly as an immediate (e.g. "ld de,<const>")
     * instead of ever calling mir_emit_virtual_load/mir_emit_virtual_store
     * for it. Despite this, mir_prepare_backend_slots never excluded
     * these two predicates from slot assignment - unlike the structurally
     * identical mir_call_only_constant (a sibling predicate for the
     * MIR_CALL-argument case, already excluded here) - so such a value
     * still got a live backend slot reserved for it: 2 bytes of dead
     * frame space nothing ever stores to or reads from. Found via
     * tests/t2darr.c's check() (`failures = failures + 1;`): the
     * constant 1's sole use is the ADD's right-hand operand, qualifying
     * for mir_binary_only_constant, yet a slot was still reserved. This
     * pattern recurs across the whole `check`/`chk`/`okb`/`fail`
     * assertion-helper family (50+ functions sharing the same shape) and
     * is the same class of dead-slot waste as the already-known,
     * narrower Item T33 (wumpus.c's rndix). */
    /* Batch 11 extends the same one-predicate discipline to two more
     * emission/reservation mismatches. Deferred metadata can leave values
     * defined by analysis-only MIR_NOP instructions, which emit no access,
     * and mir_address_is_single_call_argument makes both the MIR_ADDRESS
     * definition and its MIR_ARG consumer rematerialize without a slot.
     * Reuse those existing structural facts here rather than reserving
     * frame space that neither emission path can reference. */
    mir.backend_slot_count =
        mir_seed_promoted_local_slots(slot_end, slot_capacity);
    mir_backend_local_slot_count = mir.backend_slot_count;
    mir_backend_slot_pool_count = mir.backend_slot_count;
    mir_backend_frame_slot_count = 0;
    mir_slot_report_requested_count = 0;
    mir_slot_report_assigned_count = 0;
    mir_slot_report_param_assigned_count = 0;
    for (i = 0; i < mir.count; ++i) {
        if (planned_narrow_cache_call == i)
            planned_narrow_cache_call = -1;
        if (planned_wide_cache_call == i)
            planned_wide_cache_call = -1;
        for (value = 0; value < mir.next_value; ++value)
            if (first[value] == i) {
                int cache_target;
                int stack_consumer;
                int slot;
                const struct MirInsn *definition = mir_definition(value);
                int units = mir_definition_is_wide(definition) ? 2 : 1;
                int reusable_source = -1;
                int wide_phi_source = -1;
                int narrow_phi_source = -1;
                int phi_argument_call =
                    mir_phi_first_call_argument_target(value);
                int force_slot =
                    mir_value_requires_phi_slot(value) ||
                    mir_value_requires_divmod_slot(value) ||
                    mir_value_requires_odd_call_cleanup_slot(value);
                ++mir_slot_report_requested_count;
                if (fused_away[value] == 2)
                    continue;
                if (phi_argument_call >= 0) {
                    mir.backend_slots[value] =
                        MIR_BACKEND_SLOT_PHI_ARGUMENT_STACK;
                    continue;
                }
                if (units == 2 && definition != NULL &&
                    definition->opcode == MIR_BINARY &&
                    mir_binary_is_wide_phi_update(
                        i, &wide_phi_source, NULL) &&
                    wide_phi_source >= 0 &&
                    mir.backend_slots[wide_phi_source] >= 0) {
                    int unit;

                    slot = mir.backend_slots[wide_phi_source];
                    mir.backend_slots[value] = slot;
                    for (unit = 0; unit < units; ++unit)
                        if (slot_end[slot + unit] < last[value])
                            slot_end[slot + unit] = last[value];
                    continue;
                }
                if (units == 1 && definition != NULL &&
                    definition->opcode == MIR_BINARY &&
                    mir_binary_is_narrow_phi_adjust(
                        i, &narrow_phi_source, NULL) &&
                    narrow_phi_source >= 0 &&
                    mir.backend_slots[narrow_phi_source] >= 0) {
                    slot = mir.backend_slots[narrow_phi_source];
                    mir.backend_slots[value] = slot;
                    slot_end[slot] = last[value];
                    continue;
                }
                /* A genuine MIR_PHI (as opposed to the MIR_BINARY-
                 * implementing-a-phi-update shapes just above) gets no
                 * special treatment here otherwise - its destination
                 * just falls through to a fresh slot below, distinct
                 * from either source's own, even when a source's only
                 * use in the entire function is feeding this one phi.
                 * mir_coalesce_solitary_phi_sources (dccpeep's post-hoc
                 * retrofit attempt, reverted) and a direct liveness
                 * narrowing (also reverted) both tried to close this
                 * from the outside and each regressed dozens of
                 * unrelated apps - see their own removed comments in
                 * git history - by perturbing decisions already made
                 * (or about to be made) for OTHER values from the same
                 * shared first[]/last[] numbers or from an
                 * already-consulted slot. Deciding it right here instead
                 * touches neither: the phi destination has not been
                 * assigned anything yet (nothing downstream has
                 * consulted its slot), and reusing an eligible source's
                 * slot changes that source's own entry not at all - only
                 * this one new value's assignment is affected, exactly
                 * like the two narrower MIR_BINARY cases just above already
                 * establish this pattern is safe for. Confirmed via
                 * tests/cobint.c and tests/adaint.c's shared interpreter-
                 * dispatch shape (a ternary whose result flows into
                 * exactly one later use), which motivated this in the
                 * first place. */
                if (definition != NULL && definition->opcode == MIR_PHI) {
                    int phi_source = definition->src1;
                    int phi_source_slot = -1;

                    if (phi_source >= 0 && phi_source != value &&
                        mir.backend_slots[phi_source] >= 0 &&
                        mir_value_use_count(phi_source) == 1 &&
                        mir_value_is_wide(phi_source) == (units == 2))
                        phi_source_slot = mir.backend_slots[phi_source];
                    if (phi_source_slot < 0) {
                        phi_source = definition->src2;
                        if (phi_source >= 0 && phi_source != value &&
                            mir.backend_slots[phi_source] >= 0 &&
                            mir_value_use_count(phi_source) == 1 &&
                            mir_value_is_wide(phi_source) == (units == 2))
                            phi_source_slot = mir.backend_slots[phi_source];
                    }
                    if (phi_source_slot >= 0) {
                        int unit;

                        mir.backend_slots[value] = phi_source_slot;
                        for (unit = 0; unit < units; ++unit)
                            if (slot_end[phi_source_slot + unit] < last[value])
                                slot_end[phi_source_slot + unit] = last[value];
                        continue;
                    }
                }
                if (!force_slot &&
                    (last[value] <= first[value] ||
                                        (definition != NULL &&
                                         definition->opcode == MIR_NOP) ||
                                        mir_scalar_constant_is_rematerializable(
                                            value) ||
                                        mir_string_address_is_rematerializable(
                                            value) ||
                                        mir_wide_constant_is_rematerializable(
                                            value) ||
                                        mir_wide_constant_is_signed_relational_immediate(
                                            value) ||
                                        mir_widened_param_is_single_call_argument(
                                            value, NULL, NULL) ||
                                        mir_value_is_wide_narrow_multiply_widen(
                                            value) ||
                                        mir_address_is_rematerializable(value) ||
                                        mir_call_only_constant(value) ||
                                        mir_binary_only_constant(value) ||
                                        mir_index_only_constant(value) ||
                                        mir_value_only_used_by_constant_absolute_address(value) ||
                                        mir_value_only_used_by_dynamic_absolute_index(value) ||
                                        mir_multiply_by_small_constant(value) ||
                                        fused_away[value] == 1 ||
                                        mir_value_is_selfstore_incdec(value) ||
                                        mir_value_is_selfstore_incdec_source(value) ||
                                        mir_value_only_used_by_selfstore_adjust_amount(
                                            value) ||
                                        mir_value_is_dense_byte_switch_condition(
                                            value) ||
                                        ((type_size(definition->type) == 2 ||
                                            type_size(definition->type) == 4) &&
                                         mir_load_is_single_call_argument(value,
                                                                                                            type_size(definition->type))) ||
                                        mir_value_only_used_by_stable_pointer_argument(
                                            value) ||
                                        (type_size(definition->type) == 2 &&
                                         mir_indirect_load_is_single_stable_pointer_call_argument(
                                             value, 2)) ||
                                        (type_size(definition->type) == 2 &&
                                         mir_global_load_is_single_call_argument(
                                             value, 2)) ||
                                        (type_size(definition->type) == 2 &&
                                         mir_load_is_single_indirect_call_target(value, 2)) ||
                                        mir_address_is_single_call_argument(value) ||
                                        mir_backend_slot_forwardable(value, units, i) ||
                                        mir_wide_backend_slot_forwardable(value, units, i) ||
                                        mir_wide_signed_const_lhs_slot_forwardable(
                                            value, units, i) ||
                                        mir_wide_helper_lhs_slot_forwardable(value, units, i) ||
                                        mir_wide_helper_rhs_pair_slot_forwardable(
                                            value, units, i) ||
                                        (units == 2 &&
                                         mir_wide_call_constant_comparison_consumer(
                                             value, i, NULL)) ||
                                        mir_nested_wide_add_slot_forwardable(
                                            value, units, i) ||
                                        mir_call_argument_slot_forwardable(value, units, i) ||
                                        mir_indirect_load_de_slot_forwardable(
                                            value, units, i) ||
                                        mir_runtime_stride_index_slot_forwardable(
                                            value, units, i) ||
                                        mir_runtime_stride_store_slot_forwardable(
                                            value, units, i) ||
                                        mir_constant_false_branch_consumer(
                                            value) >= 0 ||
                                        mir_bool_call_named_home(
                                            value, NULL, NULL) ||
                                        mir_boolean_return_slot_forwardable(
                                            value, units, i) ||
                                        mir_local_constant_byte_store_slot_forwardable(
                                            value, units, i) ||
                                        mir_stack_backend_slot_forwardable(value, units, i) ||
                                        mir_value_is_nested_truth_comparison_input(value) ||
                                        mir_value_only_used_by_dead_stores(value) ||
                                        mir_value_only_used_by_dead_unary(value)))
                    continue;
                if (!force_slot &&
                    mir_value_has_direct_named_home(value)) {
                    int home_storage;
                    int home_offset;
                    if (mir_direct_named_home_location(
                            definition, &home_storage, &home_offset) &&
                        home_storage == SC_LOCAL) {
                        mir_spilled_cfg_used_stable_pointer_local_home = 1;
                        mir_spilled_cfg_used_stable_pointer_local_slot = 1;
                    }
                    continue;
                }
                stack_consumer = force_slot ? -1 :
                    mir_planned_stack_consumer(value, i);
                if (stack_consumer >= 0) {
                    mir.planned_stack_consumers[value] = stack_consumer;
                    mir.planned_stack_values[stack_consumer] = value;
                    continue;
                }
                /*
                 * The emitters can retain one pending call argument in BC
                 * or the alternate register set instead of touching its
                 * assigned slot. Plan that choice in the same definition
                 * order as emission so cache occupancy is exact. A
                 * cacheable definition-to-call span
                 * permits no other value-producing instruction, so narrow
                 * and wide cache lifetimes cannot overlap.
                 */
                cache_target = force_slot ? -1 :
                    mir_call_argument_cache_target_for_state(
                        value, i, planned_narrow_cache_call >= 0,
                        planned_wide_cache_call >= 0);
                if (cache_target >= 0) {
                    if (units == 2 &&
                        mir_wide_call_argument_is_first_pushed(
                            value, cache_target))
                        mir.backend_slots[value] =
                            MIR_BACKEND_SLOT_WIDE_ARGUMENT_STACK_CACHE;
                    else if (units == 1 &&
                             mir_narrow_argument_direct_push_enabled)
                        mir.backend_slots[value] =
                            MIR_BACKEND_SLOT_NARROW_ARGUMENT_DIRECT_PUSH;
                    else
                        mir.backend_slots[value] =
                            MIR_BACKEND_SLOT_CALL_CACHE;
                    if (units == 2)
                        planned_wide_cache_call = cache_target;
                    else
                        planned_narrow_cache_call = cache_target;
                    continue;
                }
                ++mir_slot_report_assigned_count;
                if (definition != NULL && definition->opcode == MIR_PARAM)
                    ++mir_slot_report_param_assigned_count;
                if (definition != NULL && definition->opcode == MIR_BINARY &&
                    ((units == 1 && type_size(definition->secondary_offset) == 2) ||
                     (units == 2 && type_size(definition->secondary_offset) == 4) ||
                     (units == 1 && type_size(definition->secondary_offset) == 4))) {
                    /* A narrow (16-bit) boolean result from a wide (32-bit)
                     * comparison still has two dying 32-bit operand units
                     * available; match reuse against the operand width, not
                     * the (narrower) result width, and take only the first
                     * unit of a wide operand's slot for the result. */
                    int operand_units =
                        type_size(definition->secondary_offset) == 4 ? 2 : 1;
                    if (definition->src1 >= 0 && last[definition->src1] == i &&
                        mir.backend_slots[definition->src1] >= 0 &&
                        (mir_definition_is_wide(mir_definition(
                             definition->src1)) ? 2 : 1) == operand_units)
                        reusable_source = definition->src1;
                    else if (definition->src2 >= 0 && last[definition->src2] == i &&
                             mir.backend_slots[definition->src2] >= 0 &&
                             (mir_definition_is_wide(mir_definition(
                                  definition->src2)) ? 2 : 1) == operand_units)
                        reusable_source = definition->src2;
                } else if (definition != NULL && definition->opcode == MIR_UNARY &&
                    definition->src1 >= 0 &&
                    (mir_definition_is_wide(mir_definition(
                         definition->src1)) ? 2 : 1) == units &&
                    last[definition->src1] == i &&
                    mir.backend_slots[definition->src1] >= 0) {
                    reusable_source = definition->src1;
                }
                if (reusable_source >= 0) {
                    int unit;
                    slot = mir.backend_slots[reusable_source];
                    mir.backend_slots[value] = slot;
                    if (slot < mir_backend_local_slot_count)
                        mir_spilled_cfg_used_promoted_local_slot = 1;
                    for (unit = 0; unit < units; ++unit)
                        slot_end[slot + unit] = last[value];
                    continue;
                }
                for (slot = 0; slot + units <= mir.backend_slot_count; ++slot) {
                    int unit;
                    int available = 1;
                    if (units == 2 &&
                        mir_backend_slot_offsets[slot + 1] !=
                            mir_backend_slot_offsets[slot] - 2)
                        continue;
                    for (unit = 0; unit < units; ++unit)
                        if (slot_end[slot + unit] >= i) {
                            available = 0;
                            break;
                        }
                    if (available)
                        break;
                }
                if (slot + units > mir.backend_slot_count) {
                    int unit;
                    slot = mir.backend_slot_count;
                    for (unit = 0; unit < units; ++unit)
                        mir_backend_slot_offsets[slot + unit] =
                            -mir_effective_local_bytes() -
                            mir.aggregate_temp_bytes -
                            2 * (mir_backend_frame_slot_count + unit + 1);
                    mir.backend_slot_count += units;
                    mir_backend_frame_slot_count += units;
                    mir_backend_slot_pool_count = mir.backend_slot_count;
                }
                mir.backend_slots[value] = slot;
                if (slot < mir_backend_local_slot_count)
                    mir_spilled_cfg_used_promoted_local_slot = 1;
                {
                    int unit;
                    for (unit = 0; unit < units; ++unit)
                        slot_end[slot + unit] = last[value];
                }
            }
    }
    mir_verify_planned_stack_handoffs();
    if (getenv("DCC_MIR_WIDE_HELPER_REPORT") != NULL)
        for (value = 0; value < mir.next_value; ++value) {
            const struct MirInsn *definition = mir_definition(value);
            int definition_index;
            int consumer;
            const char *helper;

            if (definition == NULL)
                continue;
            definition_index = (int)(definition - mir.insns);
            if (!mir_wide_helper_lhs_consumer(
                    value, definition_index, &consumer))
                continue;
            helper = mir_wide_runtime_helper(&mir.insns[consumer]);
            fprintf(stderr,
                    "; MIR wide-helper function=%s value=%d producer=%s "
                    "consumer=%d helper=%s distance=%d safe=%d "
                    "handoff=%d slot=%d slots=%d\n",
                    mir.name, value, mir_opcode_name(definition->opcode),
                    consumer, helper != NULL ? helper : "none",
                    consumer - definition_index,
                    mir_wide_helper_lhs_span_is_safe(
                        definition_index, consumer),
                    mir_wide_helper_handoff_supported(
                        &mir.insns[consumer]),
                    mir.backend_slots[value], mir.backend_slot_count);
        }
    if (getenv("DCC_MIR_BACKEND_SLOT_REPORT") != NULL)
        for (value = 0; value < mir.next_value; ++value)
            if (mir.backend_slots[value] >= mir_backend_local_slot_count) {
                const struct MirInsn *definition = mir_definition(value);

                fprintf(stderr,
                        "; MIR backend-slot function=%s value=%d opcode=%s "
                        "type=%d first=%d last=%d slot=%d uses=%d\n",
                        mir.name, value,
                        definition != NULL
                            ? mir_opcode_name(definition->opcode) : "none",
                        definition != NULL ? definition->type : 0,
                        first[value], last[value], mir.backend_slots[value],
                        mir_value_use_count(value));
            }
    free(fused_away);
    free(slot_end);
    free(last);
    free(first);
    if (getenv("DCC_MIR_SLOT_REPORT") != NULL &&
        mir_slot_report_requested_count > 0)
        fprintf(stderr,
                "; MIR slot-report function=%s requested=%d assigned=%d "
                "param_assigned=%d\n",
                mir.name, mir_slot_report_requested_count,
                mir_slot_report_assigned_count,
                mir_slot_report_param_assigned_count);
    return mir_backend_frame_slot_count;
}

void mir_emit_virtual_load(MirStream *out, int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int compact_byte_slot =
        mir_cfg_block_count() >= 300 && definition != NULL &&
        type_size(definition->type) == 1;
    int bool_offset;
    int bool_store_instruction;
    int offset;
    int iy_offset;
    if (mir_planned_stack_matches_consumer(
            value, mir_emit_instruction_index) &&
        mir_planned_stack_is_emitted(value)) {
        if (!mir_consume_planned_stack(
                out, value, mir_emit_instruction_index, "hl"))
            mir_planned_stack_invalid = 1;
        return;
    }
    if (mir_forwarded_hl_value == value &&
        mir_forwarded_hl_instruction + 1 == mir_emit_instruction_index) {
        /* This check must precede the param-direct branch below: the
         * register-forwarding optimization elides the reload entirely
         * when the value is already resident in HL from the immediately
         * preceding instruction, regardless of whether this value is
         * slot-based or param-direct. Skipping it here regressed
         * tvla's vla_sizeof_c99_type_bytes (a param read many times in
         * a loop) by re-emitting a full ix-relative reload on every use
         * instead of reusing HL when available. */
        mir_forwarded_hl_value = -1;
        mir_forwarded_hl_instruction = -1;
        return;
    }
    if (mir_bool_call_named_home(
            value, &bool_store_instruction, &bool_offset) &&
        mir_emit_instruction_index > bool_store_instruction) {
        mir_stream_printf(out, "\tld l,(ix%+d)\n\tld h,0\n", bool_offset);
        return;
    }
    if (mir_scalar_constant_is_rematerializable(value)) {
        mir_stream_printf(out, "\tld hl,%ld\n", definition->immediate & 0xffffL);
        return;
    }
    if (mir_string_address_is_rematerializable(value)) {
        mir_stream_printf(out, "\tld hl,S%ld\n", definition->immediate);
        return;
    }
    if (mir_address_is_rematerializable(value) &&
        !(mir_runtime_stride_schedule_is_active() &&
          definition != NULL &&
          definition->opcode == MIR_PARAM &&
          type_ptr_depth(definition->type) > 0 &&
          mir_value_has_direct_named_home(value))) {
        if (!mir_emit_address_value_to_hl(out, value))
            fatal("cannot rematerialize MIR address");
        return;
    }
    if (mir_value_has_direct_named_home(value)) {
        /* Read straight from the value's stable named IX-relative home instead
         * of a duplicated backend slot. Mirrors exactly the
         * in-range/out-of-range forms the original MIR_PARAM binding site
         * uses for a narrow scalar, including the IY-relative fast path
         * hot loops rely on (skipping it caused a measurable, if tiny,
         * cycle regression in tsnprtf's call_vsnprintf). */
        const struct MirInsn *definition = mir_definition(value);
        int value_type = definition->type;
        int value_size = type_size(value_type);
        int object_offset;
        int object_storage;
        int object_iy_offset;
        if (!mir_direct_named_home_location(
                definition, &object_storage, &object_offset))
            fatal("missing direct MIR named-home offset");
        object_iy_offset = object_offset + mir_effective_local_bytes() +
                           mir.aggregate_temp_bytes;
        if (mir_virtual_iy_base && object_iy_offset >= -128 &&
            object_iy_offset + value_size - 1 <= 127) {
            mir_stream_printf(out, "\tld l,(iy%+d)\n", object_iy_offset);
            if (value_size == 2)
                mir_stream_printf(out, "\tld h,(iy%+d)\n", object_iy_offset + 1);
        } else if (object_offset >= -128 &&
                   object_offset + value_size - 1 <= 127) {
            mir_stream_printf(out, "\tld l,(ix%+d)\n", object_offset);
            if (value_size == 2)
                mir_stream_printf(out, "\tld h,(ix%+d)\n", object_offset + 1);
        } else {
            mir_stream_puts("\tpush ix\n\tpop hl\n", out);
            mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n\tld a,(hl)\n",
                    object_offset);
            if (value_size == 2)
                mir_stream_puts("\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
            else
                mir_stream_puts("\tld l,a\n", out);
        }
        if (value_size == 1) {
            if (type_is_bool(value_type)) {
                int bool_label = new_label();
                mir_stream_puts("\tld a,l\n\tor a\n\tld hl,0\n", out);
                mir_stream_printf(out, "\tjp z, L%d\n\tinc hl\nL%d:\n",
                        bool_label, bool_label);
            } else if ((value_type & TYPE_UNSIGNED) != 0) {
                mir_stream_puts("\tld h,0\n", out);
            } else {
                mir_emit_signed_byte_extend(out);
            }
        }
        return;
    }
    offset = mir_virtual_offset(value);
    iy_offset = mir_virtual_iy_offset(value);
    if (mir_virtual_iy_base && iy_offset >= -128 && iy_offset + 1 <= 127) {
        mir_stream_printf(out, "\tld l,(iy%+d)\n", iy_offset);
        if (compact_byte_slot) {
            if ((definition->type & TYPE_UNSIGNED) != 0 ||
                type_is_bool(definition->type))
                mir_stream_puts("\tld h,0\n", out);
            else
                mir_emit_signed_byte_extend(out);
        } else
            mir_stream_printf(out, "\tld h,(iy%+d)\n", iy_offset + 1);
        return;
    }
    if (offset >= -128 && offset + 1 <= 127) {
        mir_stream_printf(out, "\tld l,(ix%+d)\n", offset);
        if (compact_byte_slot) {
            if ((definition->type & TYPE_UNSIGNED) != 0 ||
                type_is_bool(definition->type))
                mir_stream_puts("\tld h,0\n", out);
            else
                mir_emit_signed_byte_extend(out);
        } else
            mir_stream_printf(out, "\tld h,(ix%+d)\n", offset + 1);
    } else {
        mir_stream_puts("\tpush ix\n\tpop hl\n", out);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n\tld a,(hl)\n",
                offset);
        if (compact_byte_slot) {
            mir_stream_puts("\tld l,a\n", out);
            if ((definition->type & TYPE_UNSIGNED) != 0 ||
                type_is_bool(definition->type))
                mir_stream_puts("\tld h,0\n", out);
            else
                mir_emit_signed_byte_extend(out);
        } else
            mir_stream_puts("\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    }
}

/* Item T15 (mir-text-size-plan.md): a value's definition immediately
 * followed by a MIR_CONST that itself feeds the very next MIR_BINARY as
 * that binary's right-hand operand (value; const; binary(value, const))
 * currently loses mir_can_forward_hl_to_next's forwarding entirely,
 * because the CONST sits between the value's definition and its one real
 * use - mir_can_forward_hl_to_next only recognizes a handful of consumer
 * opcodes as the literal next instruction, and MIR_CONST isn't one of
 * them, so the value is unconditionally stored to its backend slot and
 * immediately reloaded one "logical" instruction later even though
 * nothing between the store and reload could have clobbered it (a
 * MIR_CONST used only as an immediate operand emits no code of its own
 * at that point - see mir_binary_only_constant's callers below). This
 * mirrors mir_can_forward_stack_to_index's existing const-in-the-middle
 * shape (push the value, let the consumer pop it back after evaluating
 * the constant) but for a binary operator's constant right-hand operand
 * instead of an array index's constant stride.
 *
 * Deliberately excludes any shape that takes a different code path for
 * the constant: divmod pairing and the multiply-by-constant fast path
 * bypass the plain "push left, evaluate right, pop, combine" sequence
 * this optimization targets, so forwarding across them is out of scope
 * here (a separate item, not folded in).
 *
 * Item T17 (mir-text-size-plan.md): originally also excluded fused-
 * comparison branches, on the assumption they took a different code
 * path entirely - investigation showed mir_emit_fused_comparison_branch
 * only consumes HL/DE however they got there (the src1/src2-loading
 * code above, including this forwarding mechanism, is shared with the
 * fused-comparison case; only the final action after loading - branch
 * directly vs. materialize+store - differs). The exclusion is removed:
 * a fusable comparison's left operand can be forwarded exactly the same
 * as any other binary operator's. */
static int mir_can_forward_stack_to_binary_const(int value)
{
    const struct MirInsn *middle;
    const struct MirInsn *binary;
    int binary_instruction;
    int instruction;

    if (mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 2 >= mir.count)
        return 0;
    middle = &mir.insns[mir_emit_instruction_index + 1];
    binary_instruction = mir_emit_instruction_index + 2;
    binary = &mir.insns[binary_instruction];
    if (middle->opcode != MIR_CONST || binary->opcode != MIR_BINARY ||
        binary->src1 != value || binary->src2 != middle->dst)
        return 0;
    if (type_size(binary->secondary_offset) == 4)
        return 0;
    if (mir_divmod_partner(binary_instruction) >= 0)
        return 0;
    if (binary->immediate == '*' &&
        mir_mul_const_fast_path_eligible(
            (unsigned long)middle->immediate & 0xffffUL, binary->dst))
        return 0;
    /* A branch-fused zero comparison skips the pop that ordinary binary
     * emission performs. Materialized equality may still use this handoff:
     * framed functions discard it in their SP-restoring epilogue, while a
     * zero-frame function consumes it explicitly at the comparison. */
    if (mir_binary_is_fusable_comparison(binary_instruction) > 0 &&
        (mir_fused_compare_is_const_zero_rhs(binary_instruction) ||
         mir_fused_compare_is_signed_zero_sign_test(binary_instruction)))
        return 0;
    for (instruction = binary_instruction + 1;
         instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 == value || insn->src2 == value)
            return 0;
        if ((insn->opcode == MIR_CALL ||
             insn->opcode == MIR_CALL_AGGREGATE) &&
            mir_call_uses_value(insn, value))
            return 0;
    }
    return 1;
}

static int mir_can_forward_stack_to_binary_lhs(int value)
{
    const struct MirInsn *middle;
    const struct MirInsn *binary;
    int binary_instruction;

    if (!mir_general_rhs_stack_forwarding_enabled ||
        mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 2 >= mir.count ||
        mir_value_use_count(value) != 1)
        return 0;
    middle = &mir.insns[mir_emit_instruction_index + 1];
    binary_instruction = mir_emit_instruction_index + 2;
    binary = &mir.insns[binary_instruction];
    if ((middle->opcode != MIR_LOAD && middle->opcode != MIR_PARAM) ||
        type_size(middle->type) > 2 ||
        binary->opcode != MIR_BINARY || binary->src1 != value ||
        binary->src2 != middle->dst ||
        type_size(binary->secondary_offset) == 4 ||
        mir_value_use_count(middle->dst) != 1 ||
        mir_divmod_partner(binary_instruction) >= 0)
        return 0;
    mir_spilled_cfg_used_binary_load_pair_forwarding = 1;
    return 1;
}

/* Item T16 (mir-text-size-plan.md): mir_can_forward_hl_to_next's
 * MIR_BINARY case only ever matches `value` against the binary's src1
 * (left operand) - a value that is instead the very next MIR_BINARY's
* *right*-hand operand (src2) gets no forwarding at all. Push the value
* immediately after computing it and let the binary's emission pop it
* into DE after loading the left operand.
*
* T203 generalizes the original constant-left implementation to any
* single-use narrow right operand. Loading src1 is stack-balanced, so it
* cannot disturb the pending right value. If src1 is itself a planned
* stack handoff, the right value is newer and is popped first, followed by
* the planned left value. The fix is symmetric to
* mir_can_forward_stack_to_index/_binary_const above. It remains
* restricted to the immediately-following instruction only (no intervening
* MIR_CONST skip - that shape is a distinct, not yet needed case) and
* excludes divmod pairing, which uses a different code path entirely.
 *
 * Item T17 (mir-text-size-plan.md): like
 * mir_can_forward_stack_to_binary_const above, this originally also
 * excluded fused-comparison branches - removed for the same reason:
 * mir_emit_fused_comparison_branch only consumes whatever HL/DE already
 * hold, regardless of how they got there. */
static int mir_can_forward_stack_to_binary_rhs(int value)
{
    const struct MirInsn *binary;
    int binary_instruction;
    int instruction;

    if (mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 1 >= mir.count)
        return 0;
    binary_instruction = mir_emit_instruction_index + 1;
    binary = &mir.insns[binary_instruction];
    if (binary->opcode != MIR_BINARY || binary->src2 != value)
        return 0;
    if (type_size(binary->secondary_offset) == 4)
        return 0;
    if (mir_value_use_count(value) != 1)
        return 0;
    if (!mir_binary_only_constant(binary->src1) &&
        !mir_general_rhs_stack_forwarding_enabled)
        return 0;
    if (mir_divmod_partner(binary_instruction) >= 0)
        return 0;
    /* Defensive: mirrors the same Item T17 zero-RHS guard as
     * mir_can_forward_stack_to_binary_const above. src2 (the forwarded
     * value here) is not expected to itself be a plain constant in
     * practice, but if it ever were, the Item 25/27 shortcuts would
     * skip the pop this predicate promises the caller will perform. */
    if (mir_binary_is_fusable_comparison(binary_instruction) > 0 &&
        (mir_fused_compare_is_const_zero_rhs(binary_instruction) ||
         mir_fused_compare_is_signed_zero_sign_test(binary_instruction)))
        return 0;
    for (instruction = binary_instruction + 1;
         instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 == value || insn->src2 == value)
            return 0;
        if ((insn->opcode == MIR_CALL ||
             insn->opcode == MIR_CALL_AGGREGATE) &&
            mir_call_uses_value(insn, value))
            return 0;
    }
    return 1;
}

static int mir_can_forward_stack_to_indirect_store_value(int value)
{
    const struct MirInsn *store;

    if (!mir_indirect_store_value_forwarding_enabled ||
        mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 1 >= mir.count ||
        mir_value_use_count(value) != 1)
        return 0;
    store = &mir.insns[mir_emit_instruction_index + 1];
    return store->opcode == MIR_STORE_INDIRECT &&
           store->src2 == value && store->bit_width == 0 &&
           store->memory_size > 0 && store->memory_size <= 2 &&
           !mir_constant_absolute_access_supported(store);
}

static int mir_stack_forward_target(int value, int *dynamic_index)
{
    int target;

    if (dynamic_index != NULL)
        *dynamic_index = 0;
    if (mir_emit_instruction_index >= 0 &&
        mir_emit_instruction_index < mir.count &&
        mir.insns[mir_emit_instruction_index].opcode == MIR_PHI)
        return -1;
    target = mir_dynamic_index_base_forward_target(value);
    if (target >= 0) {
        if (dynamic_index != NULL)
            *dynamic_index = 1;
        return target;
    }
    target = mir_stack_index_forward_target(value);
    if (target >= 0)
        return target;
    if (mir_can_forward_stack_to_indirect_store_value(value))
        return mir_emit_instruction_index + 1;
    if (mir_can_forward_stack_to_binary_lhs(value))
        return mir_emit_instruction_index + 2;
    if (mir_can_forward_stack_to_binary_const(value))
        return mir_emit_instruction_index + 2;
    if (mir_can_forward_stack_to_binary_rhs(value))
        return mir_emit_instruction_index + 1;
    return -1;
}

static int mir_stack_backend_slot_forwardable(
    int value, int units, int instruction)
{
    int saved_index;
    int forwardable;

    if (units != 1)
        return 0;
    saved_index = mir_emit_instruction_index;
    mir_emit_instruction_index = instruction;
    forwardable = mir_stack_forward_target(value, NULL) >= 0;
    mir_emit_instruction_index = saved_index;
    return forwardable;
}

static int mir_planned_stack_target(int value)
{
    if (value < 0 || value >= mir.next_value ||
        mir.planned_stack_consumers == NULL)
        return -1;
    return mir.planned_stack_consumers[value];
}

static int mir_planned_stack_matches_consumer(int value, int instruction)
{
    return mir_planned_stack_target(value) == instruction &&
           instruction >= 0 && instruction < mir.count &&
           mir.planned_stack_values != NULL &&
           mir.planned_stack_values[instruction] == value;
}

static int mir_planned_stack_is_emitted(int value)
{
    return value >= 0 && value < mir.next_value &&
           mir.planned_stack_emitted != NULL &&
           mir.planned_stack_emitted[value] != 0;
}

static int mir_pending_planned_stack_consumer(void)
{
    int consumer = -1;
    int value;

    for (value = 0; value < mir.next_value; ++value)
        if (mir_planned_stack_is_emitted(value) &&
            (consumer < 0 ||
             mir_planned_stack_target(value) < consumer))
            consumer = mir_planned_stack_target(value);
    return consumer;
}

static int mir_stack_forward_nests_with_planned_left(
    int value, int consumer)
{
    const struct MirInsn *insn;

    if (consumer < 0 || consumer >= mir.count)
        return 0;
    insn = &mir.insns[consumer];
    if (!mir_planned_stack_matches_consumer(insn->src1, consumer))
        return 0;
    return mir_is_general_rhs_stack_forward(value, consumer) ||
           mir_is_indirect_store_value_stack_forward(value, consumer);
}

static int mir_value_currently_uses_stack_handoff(
    int value, int instruction)
{
    return (mir_planned_stack_matches_consumer(value, instruction) &&
            mir_planned_stack_is_emitted(value)) ||
           (mir_forwarded_stack_value == value &&
            mir_forwarded_stack_target_instruction == instruction);
}

static int mir_scaled_index_rhs_can_stay_in_hl(int value)
{
    struct MirIndirectIncDec incdec;
    const struct MirInsn *consumer;
    const struct MirInsn *definition;
    int consumer_index;

    if (mir_cfg_block_count() != 1 ||
        mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 1 >= mir.count)
        return 0;
    definition = mir_definition(value);
    consumer_index = mir_emit_instruction_index + 1;
    consumer = &mir.insns[consumer_index];
    return definition != NULL &&
           definition->opcode == MIR_BINARY &&
           consumer->opcode == MIR_BINARY &&
           consumer->immediate == '+' &&
           type_ptr_depth(consumer->type) > 0 &&
           consumer->src2 == value &&
           mir_match_indirect_incdec(
               consumer_index + 1, &incdec) &&
           incdec.address_value == consumer->dst &&
           mir_planned_stack_matches_consumer(
               consumer->src1, consumer_index) &&
           mir_planned_stack_is_emitted(consumer->src1);
}

static int mir_pointer_difference_can_stay_in_hl(
    int value, int *consumer_out)
{
    const struct MirInsn *consumer;
    const struct MirInsn *constant;
    int consumer_index;

    if (mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 2 >= mir.count)
        return 0;
    constant = &mir.insns[mir_emit_instruction_index + 1];
    consumer_index = mir_emit_instruction_index + 2;
    consumer = &mir.insns[consumer_index];
    if (constant->opcode != MIR_CONST ||
        consumer->src1 != value ||
        mir_pointer_difference_pow2_shift(consumer) <= 0)
        return 0;
    if (consumer_out != NULL)
        *consumer_out = consumer_index;
    return 1;
}

static int mir_indirect_load_de_consumer(
    int instruction, int *forwarded_value_out)
{
    const struct MirInsn *address;
    const struct MirInsn *base;
    const struct MirInsn *load;
    const struct MirInsn *consumer;
    const struct MirInsn *conversion;
    int consumer_index;

    if (!mir_runtime_stride_schedule_is_active() ||
        instruction < 0 || instruction >= mir.count)
        return -1;
    load = &mir.insns[instruction];
    address = mir_definition(load->src1);
    base = address != NULL && address->opcode == MIR_INDEX_ADDRESS
        ? mir_definition(address->src1) : NULL;
    if (load->opcode != MIR_LOAD_INDIRECT ||
        load->bit_width != 0 ||
        (load->memory_size != 1 && load->memory_size != 2) ||
        type_is_bool(load->type) ||
        mir_value_use_count(load->dst) != 1 ||
        address == NULL || address->opcode != MIR_INDEX_ADDRESS ||
        (address->base_name[0] == 0 &&
         (base == NULL || base->opcode != MIR_INDEX_ADDRESS ||
          base->base_name[0] == 0)))
        return -1;
    consumer_index = mir_forward_skip_target(instruction);
    if (consumer_index < 0 || consumer_index >= mir.count)
        return -1;
    consumer = &mir.insns[consumer_index];
    if (consumer->opcode == MIR_UNARY &&
        consumer->immediate == 0 &&
        consumer->src1 == load->dst &&
        type_size(consumer->type) == 2 &&
        load->memory_size == 1) {
        conversion = consumer;
        consumer_index = mir_forward_skip_target(consumer_index);
        if (consumer_index < 0 || consumer_index >= mir.count)
            return -1;
        consumer = &mir.insns[consumer_index];
    } else {
        conversion = NULL;
    }
    if (consumer->opcode != MIR_BINARY ||
        consumer->immediate != '+' ||
        consumer->src2 != (conversion != NULL
                            ? conversion->dst : load->dst) ||
        type_size(consumer->secondary_offset) != 2 ||
        (!mir_value_currently_uses_stack_handoff(
             consumer->src1, consumer_index) &&
         !mir_planned_stack_matches_consumer(
             consumer->src1, consumer_index)))
        return -1;
    if (forwarded_value_out != NULL)
        *forwarded_value_out = conversion != NULL
            ? conversion->dst : load->dst;
    return consumer_index;
}

static int mir_indirect_load_de_slot_forwardable(
    int value, int units, int instruction)
{
    const struct MirInsn *insn;
    const struct MirInsn *source;
    int consumer;
    int forwarded_value;

    if (units != 1 || instruction < 0 || instruction >= mir.count)
        return 0;
    insn = &mir.insns[instruction];
    if (insn->opcode == MIR_LOAD_INDIRECT)
        return mir_indirect_load_de_consumer(
            instruction, &forwarded_value) >= 0;
    if (insn->opcode != MIR_UNARY || insn->immediate != 0 ||
        insn->dst != value)
        return 0;
    source = mir_definition(insn->src1);
    if (source == NULL || source->opcode != MIR_LOAD_INDIRECT)
        return 0;
    consumer = mir_indirect_load_de_consumer(
        (int)(source - mir.insns), &forwarded_value);
    return consumer >= 0 && forwarded_value == value;
}

static void mir_emit_indirect_load_to_de(
    MirStream *out, const struct MirInsn *load)
{
    if (load->memory_size == 2) {
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n", out);
    } else if ((load->type & TYPE_UNSIGNED) != 0) {
        mir_stream_puts("\tld e,(hl)\n\tld d,0\n", out);
    } else {
        mir_stream_puts("\tld e,(hl)\n\tld a,e\n\trlca\n\tsbc a,a\n\tld d,a\n",
              out);
    }
}

static int mir_consume_planned_stack(MirStream *out, int value, int instruction,
                                     const char *destination)
{
    if (!mir_planned_stack_matches_consumer(value, instruction) ||
        !mir_planned_stack_is_emitted(value) ||
        mir_pending_planned_stack_consumer() != instruction)
        return 0;
    mir_stream_printf(out, "\tpop %s\n", destination);
    mir.planned_stack_emitted[value] = 0;
    ++mir_planned_stack_consume_count;
    return 1;
}

static int mir_compact_byte_unary_first_use(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int instruction = mir_emit_instruction_index + 1;

    if (mir_cfg_block_count() < 300 || definition == NULL ||
        type_size(definition->type) != 1)
        return -1;
    while (instruction < mir.count) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode == MIR_NOP) {
            ++instruction;
            continue;
        }
        if (insn->opcode == MIR_CONST &&
            mir_binary_only_constant(insn->dst)) {
            ++instruction;
            continue;
        }
        break;
    }
    if (instruction >= mir.count ||
        mir.insns[instruction].opcode != MIR_UNARY ||
        mir.insns[instruction].src1 != value ||
        type_size(mir.insns[instruction].type) > 2)
        return -1;
    return instruction;
}

static void mir_emit_virtual_store(MirStream *out, int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int compact_byte_slot =
        mir_cfg_block_count() >= 300 && definition != NULL &&
        type_size(definition->type) == 1;
    int force_slot_store = definition != NULL &&
                           definition->opcode == MIR_PHI;
    int dynamic_index_forward;
    int direct_reload_storeind_target;
    int bool_store_instruction;
    int forward_to_store;
    int has_slot;
    int forward_instruction;
    int compact_unary_consumer;
    int runtime_stride_consumer;
    int runtime_stride_index_value;
    int pointer_difference_consumer;
    int offset;
    int iy_offset;
    int pending_planned_consumer;
    if (mir_phi_uses_byte_object_home(value)) {
        int home_storage;
        int home_offset;

        if (!mir_direct_named_home_location(
                definition, &home_storage, &home_offset) ||
            home_storage != SC_LOCAL)
            fatal("missing MIR byte-phi object home");
        if (home_offset >= -128 && home_offset <= 127)
            mir_stream_printf(out, "\tld (ix%+d),l\n", home_offset);
        else {
            mir_stream_puts("\tld a,l\n\tpush ix\n\tpop hl\n", out);
            mir_stream_printf(out,
                    "\tld de,%d\n\tadd hl,de\n\tld (hl),a\n",
                    home_offset);
        }
        return;
    }
    if (mir_value_requires_phi_slot(value))
        force_slot_store = 1;
    if (!force_slot_store &&
        mir_bool_call_named_home(
            value, &bool_store_instruction, NULL) &&
        definition != NULL &&
        mir_emit_instruction_index ==
            (int)(definition - mir.insns)) {
        mir_forwarded_hl_value = value;
        mir_forwarded_hl_instruction = bool_store_instruction - 1;
        return;
    }
    if (mir_value_has_direct_named_home(value))
        /* Nothing to store: later uses re-read the stable named home (see
         * mir_emit_virtual_load); the loaded HL is simply not persisted. */
        return;
    if (!force_slot_store &&
        mir_runtime_stride_index_forward_match(
            mir_emit_instruction_index, &runtime_stride_index_value,
            &runtime_stride_consumer) &&
        definition != NULL && definition->dst == value) {
        mir_runtime_stride_index_base_value = value;
        mir_runtime_stride_index_value = runtime_stride_index_value;
        mir_runtime_stride_index_consumer = runtime_stride_consumer;
        return;
    }
    if (!force_slot_store &&
        mir_scaled_index_rhs_can_stay_in_hl(value)) {
        mir_forwarded_hl_value = value;
        mir_forwarded_hl_instruction = mir_emit_instruction_index;
        return;
    }
    if (!force_slot_store &&
        mir_pointer_difference_can_stay_in_hl(
            value, &pointer_difference_consumer)) {
        mir_forwarded_hl_value = value;
        mir_forwarded_hl_instruction =
            pointer_difference_consumer - 1;
        return;
    }
    if (value == mir_prepacked_result_value) {
        mir_stream_puts("\tpush hl\n", out);
        return;
    }
    if (value >= 0 && value < mir.next_value &&
        mir.backend_slots != NULL &&
        (mir.backend_slots[value] == MIR_BACKEND_SLOT_CALL_CACHE ||
         mir.backend_slots[value] ==
             MIR_BACKEND_SLOT_NARROW_ARGUMENT_DIRECT_PUSH)) {
        int call_instruction =
            mir_planned_call_argument_cache_target(value, 0);
        mir_stream_puts("\tld c,l\n\tld b,h\n", out);
        mir_cached_call_value = value;
        mir_cached_call_instruction = call_instruction;
        return;
    }
    direct_reload_storeind_target =
        mir_call_result_direct_reload_indirect_store_target(value);
    if (!force_slot_store &&
        direct_reload_storeind_target >= 0) {
        mir_forwarded_hl_value = value;
        mir_forwarded_hl_instruction = direct_reload_storeind_target - 1;
        return;
    }
    forward_instruction = mir_planned_stack_target(value);
    if (!force_slot_store && forward_instruction >= 0) {
        if (mir_forwarded_stack_value >= 0 &&
            mir_forwarded_stack_target_instruction <
                mir_emit_instruction_index) {
            mir_forwarded_stack_value = -1;
            mir_forwarded_stack_instruction = -1;
            mir_forwarded_stack_target_instruction = -1;
        }
        if (mir_forwarded_stack_value >= 0) {
            if (getenv("DCC_MIR_SELECT_REPORT") != NULL)
                fprintf(stderr,
                        "; MIR planned-stack overlap function=%s "
                        "existing-value=%d existing-consumer=%d "
                        "value=%d producer=%d consumer=%d\n",
                        mir.name, mir_forwarded_stack_value,
                        mir_forwarded_stack_target_instruction,
                        value, mir_emit_instruction_index,
                        forward_instruction);
            mir_planned_stack_invalid = 1;
        }
        if (mir_forwarded_wide_stack_value >= 0 &&
            forward_instruction >= mir_forwarded_wide_stack_consumer)
            mir_planned_stack_invalid = 1;
        mir_stream_puts("\tpush hl\n", out);
        mir_spilled_cfg_used_planned_stack_handoff = 1;
        if (mir.insns[forward_instruction].opcode == MIR_INDEX_ADDRESS)
            mir_spilled_cfg_used_planned_index_base_handoff = 1;
        mir.planned_stack_emitted[value] = 1;
        ++mir_planned_stack_emit_count;
        return;
    }
    compact_unary_consumer =
        mir_compact_byte_unary_first_use(value);
    if (!force_slot_store && compact_unary_consumer >= 0 &&
        mir_value_use_count(value) == 1) {
        mir_forwarded_hl_value = value;
        mir_forwarded_hl_instruction =
            compact_unary_consumer - 1;
        return;
    }
    has_slot = value >= 0 && value < mir.next_value &&
                   mir.backend_slots != NULL && mir.backend_slots[value] >= 0;
    forward_instruction = mir_forward_skip_target(mir_emit_instruction_index);
    if (!has_slot) {
        /* Item 13 (mir-migration-plan-100): mir_prepare_backend_slots skips
         * allocating a slot for a value it already proved via
         * mir_backend_slot_forwardable() can only ever be consumed through
         * this same HL-forwarding path (never stored to memory) - still set
         * up the forwarding handoff so the immediately-following use skips
         * its own reload; anything else with no slot is genuinely dead. */
        if (!force_slot_store && mir_can_forward_hl_to_next(value)) {
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction = forward_instruction - 1;
            if (mir_is_branch_condition_forward(value, forward_instruction))
                ++mir_spilled_cfg_branch_condition_forwarding_count;
        } else if (!force_slot_store &&
                   mir_can_forward_hl_to_call_argument(value)) {
            /* Item T59 (mir-text-size-plan.md): mir_prepare_backend_slots'
             * reservation pass now also skips allocating a slot for values
             * mir_can_forward_hl_to_call_argument alone proves (the
             * has_slot branch below already relied on this same predicate
             * to avoid ever *writing* to an already-reserved slot; this
             * mirrors that for the no-slot case). Without this branch the
             * value has has_slot == 0, mir_can_forward_hl_to_next doesn't
             * recognize an ARG+CALL consumer, no forwarding handoff is
             * armed at all, and the later load falls through to reading
             * mir.backend_slots[value] - an address that was never
             * reserved for this value. Found via tests/tstrcmpi.c's main
             * (nested `sgn(stricmp(...))` calls) once mir_prepare_backend_
             * slots' own skip-list started recognizing this predicate. */
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction =
                mir_call_argument_after_nops(mir_emit_instruction_index);
        } else if (!force_slot_store &&
                   (forward_instruction =
                    mir_stack_forward_target(
                        value, &dynamic_index_forward)) >= 0) {
            pending_planned_consumer =
                mir_pending_planned_stack_consumer();
            if (pending_planned_consumer >= 0 &&
                forward_instruction >= pending_planned_consumer &&
                !mir_stack_forward_nests_with_planned_left(
                    value, forward_instruction))
                mir_planned_stack_invalid = 1;
            mir_stream_puts("\tpush hl\n", out);
            mir_forwarded_stack_value = value;
            mir_forwarded_stack_instruction = mir_emit_instruction_index;
            mir_forwarded_stack_target_instruction = forward_instruction;
            if (mir_is_general_rhs_stack_forward(value, forward_instruction))
                mir_spilled_cfg_used_general_rhs_stack_forwarding = 1;
            if (mir_is_indirect_store_value_stack_forward(
                    value, forward_instruction))
                ++mir_spilled_cfg_indirect_store_value_forwarding_count;
            if (dynamic_index_forward)
                mir_spilled_cfg_used_dynamic_index_base_forwarding = 1;
        }
        return;
    }
    forward_to_store = !force_slot_store &&
        mir_can_forward_hl_to_next(value) &&
        forward_instruction < mir.count &&
        mir.insns[forward_instruction].opcode == MIR_STORE;
    if (!force_slot_store &&
        !forward_to_store && mir_can_forward_hl_to_next(value)) {
        mir_forwarded_hl_value = value;
        mir_forwarded_hl_instruction = forward_instruction - 1;
        if (mir_is_branch_condition_forward(value, forward_instruction))
            ++mir_spilled_cfg_branch_condition_forwarding_count;
        return;
    }
    if (!force_slot_store &&
        mir_can_forward_hl_to_call_argument(value)) {
        mir_forwarded_hl_value = value;
        mir_forwarded_hl_instruction =
            mir_call_argument_after_nops(mir_emit_instruction_index);
        return;
    }
    if (!force_slot_store &&
        (forward_instruction = mir_stack_forward_target(
             value, &dynamic_index_forward)) >= 0) {
        pending_planned_consumer = mir_pending_planned_stack_consumer();
        if (pending_planned_consumer >= 0 &&
            forward_instruction >= pending_planned_consumer &&
            !mir_stack_forward_nests_with_planned_left(
                value, forward_instruction))
            mir_planned_stack_invalid = 1;
        mir_stream_puts("\tpush hl\n", out);
        mir_forwarded_stack_value = value;
        mir_forwarded_stack_instruction = mir_emit_instruction_index;
        mir_forwarded_stack_target_instruction = forward_instruction;
        if (mir_is_general_rhs_stack_forward(value, forward_instruction))
            mir_spilled_cfg_used_general_rhs_stack_forwarding = 1;
        if (mir_is_indirect_store_value_stack_forward(
                value, forward_instruction))
            ++mir_spilled_cfg_indirect_store_value_forwarding_count;
        if (dynamic_index_forward)
            mir_spilled_cfg_used_dynamic_index_base_forwarding = 1;
        return;
    }
    if (!force_slot_store) {
        int call_instruction = mir_call_argument_cache_target(value);
        if (call_instruction >= 0) {
            mir_stream_puts("\tld c,l\n\tld b,h\n", out);
            mir_cached_call_value = value;
            mir_cached_call_instruction = call_instruction;
            return;
        }
    }
    offset = mir_virtual_offset(value);
    iy_offset = mir_virtual_iy_offset(value);
    mir_forwarded_hl_value = -1;
    mir_forwarded_hl_instruction = -1;
    {
    int forward_to_narrow_store = forward_to_store &&
        mir_forward_store_target_is_narrow(forward_instruction);
    if (mir_virtual_iy_base && iy_offset >= -128 && iy_offset + 1 <= 127) {
        mir_stream_printf(out, "\tld (iy%+d),l\n", iy_offset);
        if (!compact_byte_slot && !forward_to_narrow_store)
            mir_stream_printf(out, "\tld (iy%+d),h\n", iy_offset + 1);
        if (forward_to_store) {
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction = forward_instruction - 1;
        } else if (compact_unary_consumer >= 0) {
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction =
                compact_unary_consumer - 1;
        } else if (mir_can_forward_hl_to_call_argument_first_use(value)) {
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction =
                mir_call_argument_after_nops(mir_emit_instruction_index);
        }
        return;
    }
    if (offset >= -128 && offset + 1 <= 127) {
        mir_stream_printf(out, compact_byte_slot
                    ? "\tld (ix%+d),l ;@dcc.mir byte-slot\n"
                    : "\tld (ix%+d),l\n",
                offset);
        if (!compact_byte_slot && !forward_to_narrow_store)
            mir_stream_printf(out, "\tld (ix%+d),h\n", offset + 1);
        if (forward_to_store) {
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction = forward_instruction - 1;
        } else if (compact_unary_consumer >= 0) {
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction =
                compact_unary_consumer - 1;
        } else if (mir_can_forward_hl_to_call_argument_first_use(value)) {
            /* Item T82 (mir-text-size-plan.md): safe only for this
             * in-range form - the out-of-range branch below moves the
             * value out of HL (into DE via `ex de,hl`) to compute the
             * store address, so HL no longer holds it afterward. */
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction =
                mir_call_argument_after_nops(mir_emit_instruction_index);
        }
    } else {
        mir_stream_puts("\tex de,hl\n\tpush ix\n\tpop hl\n", out);
        mir_stream_printf(out, "\tld bc,%d\n\tadd hl,bc\n\tld (hl),e\n",
                offset);
        if (!compact_byte_slot)
            mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
        if (forward_to_store) {
            mir_forwarded_hl_value = value;
            mir_forwarded_hl_instruction = forward_instruction - 1;
        }
    }
    }
}

int mir_value_is_wide(int value)
{
    return mir_definition_is_wide(mir_definition(value));
}

static int mir_wide_constant_uses_new_rematerialization(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int instruction = definition != NULL
        ? (int)(definition - mir.insns) : -1;

    return mir_wide_constant_is_rematerializable(value) &&
           !mir_call_only_constant(value) &&
           !mir_binary_only_constant(value) &&
           !mir_index_only_constant(value) &&
           !mir_backend_slot_forwardable(value, 2, instruction) &&
           !mir_wide_backend_slot_forwardable(value, 2, instruction) &&
           !mir_wide_helper_lhs_slot_forwardable(value, 2, instruction) &&
           !mir_call_argument_slot_forwardable(value, 2, instruction) &&
           !mir_stack_backend_slot_forwardable(value, 2, instruction) &&
           !mir_value_only_used_by_dead_stores(value) &&
           !mir_value_only_used_by_dead_unary(value);
}

static int mir_call_result_direct_reload_indirect_store_target(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    const struct MirInsn *store;
    int skipped_label;
    int target;
    int instruction;

    if (definition == NULL || definition->opcode != MIR_CALL ||
        mir_emit_instruction_index < 0 ||
        mir_emit_instruction_index + 1 >= mir.count)
        return -1;
    target = mir_forward_skip_target_ex(mir_emit_instruction_index,
                                        &skipped_label);
    if (skipped_label || target < 0 || target >= mir.count)
        return -1;
    store = &mir.insns[target];
    if (store->opcode != MIR_STORE_INDIRECT || store->src2 != value ||
        mir_constant_absolute_access_supported(store) ||
        (!mir_indirect_store_address_has_direct_reload(store) &&
         !(mir_planned_stack_matches_consumer(store->src1, target) &&
           mir_planned_stack_is_emitted(store->src1))) ||
        (mir_value_currently_uses_stack_handoff(store->src1, target) &&
         !(mir_planned_stack_matches_consumer(store->src1, target) &&
           mir_planned_stack_is_emitted(store->src1))))
        return -1;
    for (instruction = target + 1;
         instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 == value || insn->src2 == value)
            return -1;
        if ((insn->opcode == MIR_CALL ||
             insn->opcode == MIR_CALL_AGGREGATE) &&
            mir_call_uses_value(insn, value))
            return -1;
    }
    return target;
}

static int mir_divmod_partner(int instruction)
{
    const struct MirInsn *candidate;
    int partner;

    if (instruction < 0 || instruction >= mir.count)
        return -1;
    candidate = &mir.insns[instruction];
    if (candidate->opcode != MIR_BINARY ||
        (candidate->immediate != '/' && candidate->immediate != '%') ||
        type_size(candidate->secondary_offset) > 2)
        return -1;
    for (partner = 0; partner < mir.count; ++partner) {
        const struct MirInsn *other = &mir.insns[partner];
        if (partner == instruction || other->opcode != MIR_BINARY ||
            other->src1 != candidate->src1 || other->src2 != candidate->src2 ||
            other->secondary_offset != candidate->secondary_offset)
            continue;
        if ((candidate->immediate == '/' && other->immediate == '%') ||
            (candidate->immediate == '%' && other->immediate == '/'))
            return partner;
    }
    return -1;
}

int mir_spilled_cfg_has_divmod_pair(void)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir_divmod_partner(instruction) > instruction)
            return 1;
    return 0;
}

int mir_spilled_cfg_divmod_has_dead_result(void)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        int partner = mir_divmod_partner(instruction);

        if (partner > instruction &&
            (mir_value_only_used_by_dead_stores(
                 mir.insns[instruction].dst) ||
             mir_value_only_used_by_dead_stores(
                 mir.insns[partner].dst)))
            return 1;
    }
    return 0;
}

static void mir_emit_virtual_load_wide(MirStream *out, int value)
{
    const struct MirInsn *definition = mir_definition(value);
    unsigned long float_bits;
    int offset;
    int iy_offset;

    /* Item T40 (mir-text-size-plan.md): consume a value mir_emit_virtual_
     * store_wide already forwarded straight from its producing
     * instruction, exactly mirroring mir_emit_virtual_load's own
     * mir_forwarded_hl_value check - the value is already resident in
     * HL:DE from the immediately preceding instruction, so no reload is
     * needed at all. Must precede every other case below (matching the
     * scalar version's own ordering) since this applies regardless of
     * whether the value is slot-based, param-direct, or anything else. */
    if (mir_forwarded_wide_value == value &&
        mir_forwarded_wide_instruction + 1 == mir_emit_instruction_index) {
        mir_forwarded_wide_value = -1;
        mir_forwarded_wide_instruction = -1;
        return;
    }
    if (definition != NULL &&
        definition->opcode == MIR_UNARY &&
        mir_float_constant_expression_bits(value, &float_bits)) {
        mir_stream_printf(out, "\tld hl,%lu\n\tld de,%lu\n",
                float_bits & 0xffffUL,
                (float_bits >> 16) & 0xffffUL);
        return;
    }
    if (mir_wide_constant_is_rematerializable(value) ||
        (definition != NULL &&
         (definition->opcode == MIR_CONST ||
          definition->opcode == MIR_FLOAT_CONST) &&
         type_size(definition->type) == 4 &&
         mir.backend_slots != NULL &&
         mir.backend_slots[value] < 0)) {
        /*
         * Slot planning can classify a constant as binary-only before
         * multiply-add fusion turns it into a direct helper operand. Such a
         * value intentionally has no slot; materialize it here rather than
         * deriving an IX offset from its SSA number.
         */
        if (mir_wide_constant_uses_new_rematerialization(value)) {
            mir_spilled_cfg_used_wide_constant_rematerialization = 1;
        }
        mir_stream_printf(out, "\tld hl,%lu\n\tld de,%lu\n",
                (unsigned long)definition->immediate & 0xffffUL,
                ((unsigned long)definition->immediate >> 16) & 0xffffUL);
        return;
    }
    if (!mir_definition_is_wide(definition)) {
        mir_emit_virtual_load(out, value);
        if (definition != NULL &&
            ((definition->type & TYPE_UNSIGNED) != 0 ||
             type_ptr_depth(definition->type) > 0 ||
             type_is_bool(definition->type)))
            mir_stream_puts("\tld de,0\n", out);
        else
            mir_stream_puts("\tld a,h\n\trlca\n\tsbc a,a\n\tld d,a\n\tld e,a\n",
                  out);
        return;
    }
    if (mir_value_has_direct_named_home(value)) {
        /* mir-migration-plan-next10: wide (4-byte) parameter counterpart
         * of mir_emit_virtual_load - object storage for a wide parameter is
         * ascending (offset..offset+3), unlike the descending backend-slot
         * convention below, so this mirrors the original MIR_PARAM binding
         * site's load form exactly rather than reusing mir_virtual_offset.
         * Also mirrors the IY-relative fast path hot loops rely on. */
        int object_offset;
        int object_storage;
        int selfstore_store_index;
        if (getenv("DCC_MIR_PERF_RECOVERY_REPORT") != NULL &&
            mir_wide_load_is_immediate_binary_rhs(value))
            fprintf(stderr,
                    "; MIR perf-recovery function=%s feature=wide-rhs-load\n",
                    mir.name);
        if (!mir_direct_named_home_location(
                definition, &object_storage, &object_offset) &&
            !mir_binary_is_selfstore_wide_increment(
                (int)(definition - mir.insns),
                &selfstore_store_index, &object_offset))
            fatal("missing direct wide MIR named-home offset");
        int object_iy_offset = object_offset + mir_effective_local_bytes() +
                                mir.aggregate_temp_bytes;
        if (mir_virtual_iy_base && object_iy_offset >= -128 &&
            object_iy_offset + 3 <= 127) {
            mir_stream_printf(out,
                    "\tld l,(iy%+d)\n\tld h,(iy%+d)\n"
                    "\tld e,(iy%+d)\n\tld d,(iy%+d)\n",
                    object_iy_offset, object_iy_offset + 1,
                    object_iy_offset + 2, object_iy_offset + 3);
        } else if (object_offset >= -128 && object_offset + 3 <= 127) {
            mir_stream_printf(out,
                    "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
                    "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
                    object_offset, object_offset + 1,
                    object_offset + 2, object_offset + 3);
        } else {
            mir_stream_puts("\tpush ix\n\tpop hl\n", out);
            mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", object_offset);
            mir_stream_puts("\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
                  "\tinc hl\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
                  "\tld h,b\n\tld l,c\n", out);
        }
        return;
    }
    offset = mir_virtual_offset(value);
    iy_offset = mir_virtual_iy_offset(value);
    if (mir_virtual_iy_base && iy_offset - 2 >= -128 &&
        iy_offset + 1 <= 127) {
        mir_stream_printf(out,
                "\tld l,(iy%+d)\n\tld h,(iy%+d)\n"
                "\tld e,(iy%+d)\n\tld d,(iy%+d)\n",
                iy_offset, iy_offset + 1, iy_offset - 2, iy_offset - 1);
        return;
    }
    if (offset - 2 >= -128 && offset + 1 <= 127) {
        mir_stream_printf(out,
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
                "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
                offset, offset + 1, offset - 2, offset - 1);
    } else {
        mir_stream_puts("\tpush ix\n\tpop hl\n", out);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", offset);
        mir_stream_puts("\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
              "\tdec hl\n\tdec hl\n\tdec hl\n"
              "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
              "\tld h,b\n\tld l,c\n", out);
    }
}

static void mir_emit_virtual_store_wide(MirStream *out, int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int force_slot_store = definition != NULL &&
                           definition->opcode == MIR_PHI;
    int has_slot;
    int helper_consumer;
    int helper_rhs_pair;
    int offset;
    int iy_offset;
    /* Item T35 (mir-text-size-plan.md): mirrors mir_emit_virtual_store's
     * own first-line check, now that Item T35's mir_object_eligible
     * relaxation lets a wide (4-byte) parameter actually have an object
     * at all - nothing to store here, later reads simply re-load
     * directly from the parameter's own stable home (see
     * mir_emit_virtual_load_wide, which already checks this same
     * predicate on the load side). Without this check a direct-eligible
     * wide parameter would still pay a full spill even though nothing
     * downstream would ever read the slot this store writes. */
    if (mir_value_has_direct_named_home(value))
        return;
    if (value == mir_prepacked_result_value) {
        mir_stream_puts("\tpush de\n\tpush hl\n", out);
        return;
    }
    if (value >= 0 && value < mir.next_value &&
        mir.backend_slots != NULL &&
        (mir.backend_slots[value] == MIR_BACKEND_SLOT_CALL_CACHE ||
         mir.backend_slots[value] ==
             MIR_BACKEND_SLOT_WIDE_ARGUMENT_STACK_CACHE)) {
        int call_instruction =
            mir_planned_call_argument_cache_target(value, 1);
        if (mir.backend_slots[value] ==
            MIR_BACKEND_SLOT_WIDE_ARGUMENT_STACK_CACHE)
            mir_stream_puts("\tpush de\n\tpush hl\n", out);
        else
            mir_stream_puts("\texx\n", out);
        mir_cached_wide_call_value = value;
        mir_cached_wide_call_instruction = call_instruction;
        return;
    }
    if (!force_slot_store &&
        mir_wide_signed_const_lhs_slot_forwardable(
            value, 2, mir_emit_instruction_index)) {
        mir_forwarded_wide_value = value;
        mir_forwarded_wide_instruction = mir_emit_instruction_index;
        return;
    }
    if (!force_slot_store &&
        mir_wide_call_constant_comparison_consumer(
            value, mir_emit_instruction_index, &helper_consumer)) {
        if (mir_forwarded_wide_stack_value >= 0 ||
            mir_pending_planned_stack_consumer() >= 0) {
            mir_planned_stack_invalid = 1;
            return;
        }
        mir_spilled_cfg_used_wide_call_constant_comparison = 1;
        mir_stream_puts("\tpush de\n\tpush hl\n", out);
        mir_forwarded_wide_stack_value = value;
        mir_forwarded_wide_stack_consumer = helper_consumer;
        return;
    }
    helper_rhs_pair = mir_wide_helper_rhs_pair_consumer(
        value, mir_emit_instruction_index, &helper_consumer);
    if (!force_slot_store &&
        (helper_rhs_pair ||
         (mir_wide_helper_lhs_consumer(
              value, mir_emit_instruction_index, &helper_consumer) &&
          mir_wide_helper_handoff_supported(&mir.insns[helper_consumer]) &&
          mir_wide_helper_lhs_span_is_safe(
              mir_emit_instruction_index, helper_consumer)))) {
        if (mir_forwarded_wide_stack_value >= 0) {
            const struct MirInsn *consumer = &mir.insns[helper_consumer];

            if (helper_rhs_pair &&
                mir_forwarded_wide_stack_consumer == helper_consumer &&
                consumer->src1 == mir_forwarded_wide_stack_value &&
                consumer->src2 == value) {
                mir_forwarded_wide_value = value;
                mir_forwarded_wide_instruction = helper_consumer - 1;
                return;
            }
            if (!helper_rhs_pair &&
                mir_forwarded_wide_stack_consumer == helper_consumer &&
                consumer->src1 == mir_forwarded_wide_stack_value &&
                consumer->src2 == value &&
                (consumer->immediate == TOK_EQ ||
                 consumer->immediate == TOK_NE ||
                 consumer->immediate == '<' ||
                 consumer->immediate == '>' ||
                 consumer->immediate == TOK_LE ||
                 consumer->immediate == TOK_GE) &&
                mir_forward_skip_target(mir_emit_instruction_index) ==
                    helper_consumer) {
                mir_forwarded_wide_value = value;
                mir_forwarded_wide_instruction = helper_consumer - 1;
                return;
            }
            if (!helper_rhs_pair &&
                mir_forwarded_wide_stack_consumer == helper_consumer &&
                consumer->src1 == value &&
                consumer->src2 == mir_forwarded_wide_stack_value &&
                mir_forward_skip_target(mir_emit_instruction_index) ==
                    helper_consumer) {
                mir_forwarded_wide_value = value;
                mir_forwarded_wide_instruction = helper_consumer - 1;
                return;
            }
            mir_planned_stack_invalid = 1;
            return;
        }
        if (helper_rhs_pair) {
            mir_planned_stack_invalid = 1;
            return;
        }
        int pending_consumer = mir_pending_planned_stack_consumer();
        if (pending_consumer >= 0 && helper_consumer >= pending_consumer)
            mir_planned_stack_invalid = 1;
        mir_stream_puts("\tpush de\n\tpush hl\n", out);
        mir_forwarded_wide_stack_value = value;
        mir_forwarded_wide_stack_consumer = helper_consumer;
        return;
    }
    if (!force_slot_store &&
        mir_wide_binary_rhs_forwarding_enabled &&
        mir_emit_instruction_index + 1 < mir.count &&
        mir_wide_binary_rhs_pair_supported(
            value, &mir.insns[mir_emit_instruction_index + 1]) &&
        mir.insns[mir_emit_instruction_index + 1].src2 == value &&
        mir_value_use_count(value) == 1) {
        if (mir_pending_planned_stack_consumer() >= 0 ||
            mir_forwarded_wide_stack_value >= 0) {
            mir_planned_stack_invalid = 1;
            return;
        }
        mir_stream_puts("\tpush de\n\tpush hl\n", out);
        mir_forwarded_wide_stack_value = value;
        mir_forwarded_wide_stack_consumer = mir_emit_instruction_index + 1;
        ++mir_wide_binary_rhs_forwarding_uses;
        return;
    }
    /* Item T40 (mir-text-size-plan.md): forward a computed wide value
     * straight to its sole next use (currently only a MIR_RETURN, see
     * mir_can_forward_hl_de_to_next) instead of always spilling it to a
     * backend slot first - mirrors mir_emit_virtual_store's own
     * forwarding check, just for HL:DE instead of HL alone. The slot
     * mir_prepare_backend_slots already assigned (it has no wide-
     * forwarding awareness yet) is simply left unused in this case,
     * the same acceptable tradeoff Item 13 documented for the scalar
     * path before mir_prepare_backend_slots grew its own forwarding
     * awareness. */
    if (!force_slot_store &&
        mir_can_forward_hl_de_to_next(value)) {
        int forward_instruction =
            mir_forward_skip_target(mir_emit_instruction_index);
        mir_forwarded_wide_value = value;
        mir_forwarded_wide_instruction = forward_instruction - 1;
        return;
    }
    has_slot = value >= 0 && value < mir.next_value &&
                   mir.backend_slots != NULL && mir.backend_slots[value] >= 0;
    if (!has_slot)
        return;
    int call_instruction = mir_call_argument_cache_target(value);
    if (!force_slot_store && call_instruction >= 0) {
        mir_stream_puts("\texx\n", out);
        mir_cached_wide_call_value = value;
        mir_cached_wide_call_instruction = call_instruction;
        return;
    }
    offset = mir_virtual_offset(value);
    iy_offset = mir_virtual_iy_offset(value);
    mir_forwarded_hl_value = -1;
    mir_forwarded_hl_instruction = -1;
    mir_forwarded_wide_value = -1;
    mir_forwarded_wide_instruction = -1;
    if (mir_virtual_iy_base && iy_offset - 2 >= -128 &&
        iy_offset + 1 <= 127) {
        mir_stream_printf(out,
                "\tld (iy%+d),l\n\tld (iy%+d),h\n"
                "\tld (iy%+d),e\n\tld (iy%+d),d\n",
                iy_offset, iy_offset + 1, iy_offset - 2, iy_offset - 1);
        return;
    }
    if (offset - 2 >= -128 && offset + 1 <= 127) {
        mir_stream_printf(out,
                "\tld (ix%+d),l\n\tld (ix%+d),h\n"
                "\tld (ix%+d),e\n\tld (ix%+d),d\n",
                offset, offset + 1, offset - 2, offset - 1);
    } else {
        mir_stream_puts("\tpush de\n\tpush hl\n\tpush ix\n\tpop hl\n", out);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", offset);
        mir_stream_puts("\tpop bc\n\tld (hl),c\n\tinc hl\n\tld (hl),b\n"
              "\tdec hl\n\tdec hl\n\tdec hl\n"
              "\tpop bc\n\tld (hl),c\n\tinc hl\n\tld (hl),b\n", out);
    }
}

static void mir_emit_virtual_iy_epilogue(MirStream *out)
{
    if (!mir_virtual_iy_base) {
        if (mir_virtual_iy_frame_bytes == 0)
            mir_stream_puts("\tpop ix\n\tret\n", out);
        else
            mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
        return;
    }
    mir_stream_puts("\texx\n\tpush ix\n\tpop hl\n", out);
    mir_stream_printf(out, "\tld de,-%d\n\tadd hl,de\n\tld sp,hl\n",
            mir_virtual_iy_frame_bytes + 2);
    mir_stream_puts("\tpop iy\n\tld sp,ix\n\tpop ix\n\texx\n\tret\n", out);
}

static void mir_emit_low_byte_index_loop(
    MirStream *out, const int *labels, const struct MirLowByteIndexLoop *loop)
{
    if (getenv("DCC_MIR_LOW_BYTE_INDEX_LOOP_REPORT") != NULL)
        fprintf(stderr,
                "; MIR low-byte-index-loop function=%s kind=%s "
                "initial=%d limit=%d\n",
                mir.name,
                loop->kind == MIR_LOW_BYTE_INDEX_STORE
                    ? "store" : "compare",
                loop->initial, loop->limit);
    mir_stream_printf(out,
            "\tld c,(ix%+d)\n\tld b,(ix%+d)\n",
            loop->base_offset, loop->base_offset + 1);
    if (loop->initial != 0)
        mir_stream_printf(out,
                "\tld hl,%d\n\tadd hl,bc\n\tld b,h\n\tld c,l\n",
                loop->initial);
    mir_stream_printf(out,
            "\tld e,%d\n\tld h,(ix%+d)\nL%d:\n"
            "\tld a,h\n\tadd a,e\n",
            loop->initial, loop->invariant_offset,
            labels[loop->header_label]);
    if (loop->kind == MIR_LOW_BYTE_INDEX_STORE) {
        mir_stream_puts("\tld (bc),a\n\tinc bc\n", out);
    } else {
        mir_stream_printf(out,
                "\tld d,a\n\tld a,(bc)\n\tcp d\n\tinc bc\n"
                "\tjp z, L%d\n\tld hl,0\n",
                labels[loop->continue_label]);
        mir_emit_virtual_iy_epilogue(out);
        mir_stream_printf(out, "L%d:\n", labels[loop->continue_label]);
    }
    mir_stream_printf(out,
            "\tinc e\n\tld a,e\n\tcp %d\n\tjp c, L%d\n",
            loop->limit, labels[loop->header_label]);
}

static void mir_emit_phi_value_store(MirStream *out, int value)
{
    int saved_instruction = mir_emit_instruction_index;

    mir_emit_instruction_index = -1;
    mir_emit_virtual_store(out, value);
    mir_emit_instruction_index = saved_instruction;
}

static void mir_emit_phi_value_load(MirStream *out, int value)
{
    int saved_instruction = mir_emit_instruction_index;

    mir_emit_instruction_index = -1;
    mir_emit_virtual_load(out, value);
    mir_emit_instruction_index = saved_instruction;
}

static void mir_emit_byte_verify_loop(
    MirStream *out, const int *labels, const struct MirByteVerifyLoop *loop)
{
    int body_label = new_label();
    int mismatch_label = new_label();
    int mismatch_fallthrough_label = new_label();

    mir_stream_printf(out,
            "\tld c,(ix%+d)\n\tld b,(ix%+d)\n"
            "\tld a,b\n\tor c\n\tjp z, L%d\n",
            loop->count_offset, loop->count_offset + 1,
            labels[loop->exit_label]);
    mir_stream_printf(out,
            "\tld a,(ix%+d)\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "L%d:\n\tcpi\n\tjp nz, L%d\n"
            "\tjp pe, L%d\n\tjp L%d\n"
            "L%d:\n\tdec hl\n"
            "\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tor a\n\tsbc hl,bc\n\tdec hl\n",
            loop->value_source_offset,
            loop->pointer_offset, loop->pointer_offset + 1,
            body_label, mismatch_label, body_label,
            labels[loop->exit_label],
            mismatch_label,
            loop->pointer_offset, loop->pointer_offset + 1,
            loop->count_offset, loop->count_offset + 1);
    mir_emit_phi_value_store(out, loop->phi_value);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n",
            mismatch_fallthrough_label,
            labels[loop->header_label]);
    mir_emit_phi_value_load(out, loop->phi_value);
    mir_stream_puts("\tex de,hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tor a\n\tsbc hl,de\n\tld c,l\n\tld b,h\n"
            "\tld a,b\n\tor c\n\tjp z, L%d\n",
            loop->count_offset, loop->count_offset + 1,
            labels[loop->exit_label]);
    mir_stream_printf(out,
            "\tld a,(ix%+d)\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tjp L%d\nL%d:\n",
            loop->value_source_offset,
            loop->pointer_offset, loop->pointer_offset + 1,
            body_label, mismatch_fallthrough_label);
}

static void mir_emit_word_scan_loop(
    MirStream *out, const int *labels, const struct MirWordScanLoop *loop)
{
    int done_label = new_label();
    int next_label = new_label();

    mir_stream_printf(out,
            "\tld c,(ix%+d)\n\tld b,(ix%+d)\n"
            "L%d:\n"
            "\tld l,c\n\tld h,b\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tex de,hl\n\tld a,h\n\tor l\n"
            "\tjp z, L%d\n",
            loop->pointer_offset, loop->pointer_offset + 1,
            labels[loop->header_label], done_label);
    if (loop->kind == MIR_WORD_SCAN_LAST_MATCH) {
        mir_stream_printf(out,
                "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
                "\tor a\n\tsbc hl,de\n\tjp nz, L%d\n"
                "\tld (ix%+d),c\n\tld (ix%+d),b\n"
                "L%d:\n",
                loop->comparison_offset,
                loop->comparison_offset + 1,
                next_label,
                loop->last_match_offset,
                loop->last_match_offset + 1,
                next_label);
    }
    mir_stream_printf(out,
            "\tinc bc\n\tinc bc\n\tjp L%d\n"
            "L%d:\n",
            labels[loop->header_label], done_label);
    if (loop->kind == MIR_WORD_SCAN_LENGTH)
        mir_stream_printf(out,
                "\tld (ix%+d),c\n\tld (ix%+d),b\n",
                loop->pointer_offset, loop->pointer_offset + 1);
    mir_stream_printf(out, "\tjp L%d\n", labels[loop->exit_label]);
}

static void mir_emit_fixed_byte_winner(
    MirStream *out, const struct MirFixedByteWinner *plan)
{
    const char *board_name =
        asm_name_for(sym_asm_name(plan->board));
    int success = new_label();
    int line;

    if ((plan->board->storage == SC_EXTERN ||
         plan->board->needs_extrn) &&
        mir_extrn_should_emit(plan->board))
        mir_stream_printf(out, "\textrn %s\n", board_name);
    mir_stream_printf(out, "\tld a,(%s%+d)\n\tld b,a\n",
            board_name, plan->source_offset);
    for (line = 0; line < plan->line_count; ++line) {
        int next = new_label();

        mir_stream_printf(out,
                "\tld a,(%s%+d)\n\tcp b\n\tjp nz, L%d\n"
                "\tld a,(%s%+d)\n\tcp b\n\tjp z, L%d\n"
                "L%d:\n",
                board_name, plan->line_offsets[line][0], next,
                board_name, plan->line_offsets[line][1], success,
                next);
    }
    mir_stream_printf(out,
            "\tld hl,0\n\tret\n"
            "L%d:\n\tld l,b\n\tld h,0\n\tret\n",
            success);
}

static void mir_emit_word_powermod(
    MirStream *out, const struct MirWordPowermod *plan)
{
    int exponent_nonzero = new_label();
    int initialize = new_label();
    int loop = new_label();
    int after_product = new_label();
    int return_result = new_label();
    int exit = new_label();

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tdec hl\n"
            "\tld a,h\n\tor l\n\tjp nz, L%d\n"
            "\tld hl,0\n\tjp L%d\n"
            "L%d:\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld a,h\n\tor l\n\tjp nz, L%d\n"
            "\tld hl,1\n\tjp L%d\n"
            "L%d:\n"
            "\tld hl,1\n\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "\tld hl,%d\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
            plan->modulus_offset, plan->modulus_offset + 1,
            exponent_nonzero,
            exit,
            exponent_nonzero,
            plan->exponent_offset, plan->exponent_offset + 1,
            initialize,
            exit,
            initialize,
            plan->result_offset, plan->result_offset + 1,
            plan->initial_base,
            plan->modulus_offset, plan->modulus_offset + 1);
    mir_emit_runtime_call(out, "__modu");
    mir_stream_printf(out,
            "\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "L%d:\n"
            "\tbit 0,(ix%+d)\n\tjp z, L%d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tld c,(ix%+d)\n\tld b,(ix%+d)\n",
            plan->base_offset, plan->base_offset + 1,
            loop,
            plan->exponent_offset, after_product,
            plan->result_offset, plan->result_offset + 1,
            plan->base_offset, plan->base_offset + 1,
            plan->modulus_offset, plan->modulus_offset + 1);
    mir_emit_runtime_call(out, "__m1mu");
    mir_stream_printf(out,
            "\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "L%d:\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tsrl h\n\trr l\n"
            "\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "\tld a,h\n\tor l\n\tjp z, L%d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,l\n\tld d,h\n"
            "\tld c,(ix%+d)\n\tld b,(ix%+d)\n",
            plan->result_offset, plan->result_offset + 1,
            after_product,
            plan->exponent_offset, plan->exponent_offset + 1,
            plan->exponent_offset, plan->exponent_offset + 1,
            return_result,
            plan->base_offset, plan->base_offset + 1,
            plan->modulus_offset, plan->modulus_offset + 1);
    mir_emit_runtime_call(out, "__m1mu");
    mir_stream_printf(out,
            "\tld (ix%+d),l\n\tld (ix%+d),h\n\tjp L%d\n"
            "L%d:\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "L%d:\n"
            "\tld sp,ix\n\tpop ix\n\tret\n",
            plan->base_offset, plan->base_offset + 1, loop,
            return_result,
            plan->result_offset, plan->result_offset + 1,
            exit);
}

static void mir_emit_float_parameter(
    MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
            offset, offset + 1, offset + 2, offset + 3);
}

static void mir_emit_float_bits(
    MirStream *out, unsigned long bits)
{
    mir_stream_printf(out, "\tld hl,%lu\n\tld de,%lu\n",
            bits & 0xffffUL, (bits >> 16) & 0xffffUL);
}

static void mir_emit_float_parameter_left(
    MirStream *out, int offset, unsigned long right_bits,
    const char *helper)
{
    mir_emit_float_parameter(out, offset);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_float_bits(out, right_bits);
    mir_emit_runtime_call(out, helper);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
}

static void mir_emit_float_unit_fraction(
    MirStream *out, const struct MirFloatUnitFraction *plan)
{
    const char *assert_name =
        asm_name_for(sym_asm_name(plan->assert_function));
    int upper_ok = new_label();
    int lower_ok = new_label();
    int return_parameter = new_label();
    int exit = new_label();

    if ((plan->assert_function->storage == SC_EXTERN ||
         plan->assert_function->needs_extrn) &&
        mir_extrn_should_emit(plan->assert_function))
        mir_stream_printf(out, "\textrn %s\n", assert_name);
    mir_emit_float_parameter_left(
        out, plan->parameter_offset,
        plan->upper_bits, "__fgtf");
    mir_stream_printf(out,
            "\tld a,h\n\tor l\n\tjp nz, L%d\n"
            "\tld hl,S%d\n\tpush hl\n\tcall %s\n\tpop bc\n"
            "L%d:\n",
            upper_ok, plan->upper_assert_string,
            assert_name, upper_ok);
    mir_emit_float_parameter_left(
        out, plan->parameter_offset,
        plan->lower_bits, "__flef");
    mir_stream_printf(out,
            "\tld a,h\n\tor l\n\tjp nz, L%d\n"
            "\tld hl,S%d\n\tpush hl\n\tcall %s\n\tpop bc\n"
            "L%d:\n",
            lower_ok, plan->lower_assert_string,
            assert_name, lower_ok);
    mir_emit_float_parameter_left(
        out, plan->parameter_offset,
        plan->unit_bits, "__flef");
    mir_stream_printf(out,
            "\tld a,h\n\tor l\n\tjp z, L%d\n",
            return_parameter);
    mir_emit_float_parameter(out, plan->parameter_offset);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_float_bits(out, plan->unit_bits);
    mir_emit_runtime_call(out, "__fsf");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out,
            "\tjp L%d\n"
            "L%d:\n",
            exit, return_parameter);
    mir_emit_float_parameter(out, plan->parameter_offset);
    mir_stream_printf(out,
            "L%d:\n"
            "\tld sp,ix\n\tpop ix\n\tret\n",
            exit);
}

static void mir_emit_frameless_byte_parameter(
    MirStream *out, int sp_offset)
{
    mir_stream_printf(out, "\tld hl,%d\n\tadd hl,sp\n\tld a,(hl)\n",
            sp_offset);
}

static void mir_emit_fixed_array_init(
    MirStream *out, const struct MirFixedArrayInit *plan)
{
    int byte_word_loop = new_label();
    int long_loop = new_label();

    mir_stream_printf(out,
            "\tpush ix\n\tpop hl\n\tld de,%d\n\tadd hl,de\n"
            "\tex de,hl\n"
            "\tpush ix\n\tpop hl\n\tld bc,%d\n\tadd hl,bc\n"
            "\tld c,0\n"
            "L%d:\n"
            "\tld a,c\n\tadd a,a\n\tld b,a\n"
            "\tadd a,a\n\tadd a,a\n\tadd a,b\n"
            "\tld (hl),a\n\tinc hl\n\tld (hl),0\n\tinc hl\n"
            "\tld a,c\n\tinc a\n\tld (de),a\n\tinc de\n"
            "\tinc c\n\tld a,c\n\tcp 8\n\tjp c, L%d\n"
            "\tpush ix\n\tpop hl\n\tld de,%d\n\tadd hl,de\n"
            "\tld bc,0\n\tld e,6\n"
            "L%d:\n"
            "\tld (hl),c\n\tinc hl\n\tld (hl),b\n\tinc hl\n"
            "\tld (hl),0\n\tinc hl\n\tld (hl),0\n\tinc hl\n"
            "\tld a,c\n\tadd a,232\n\tld c,a\n"
            "\tld a,b\n\tadc a,3\n\tld b,a\n"
            "\tdec e\n\tjp nz, L%d\n"
            "\tld (ix%+d),6\n\tld (ix%+d),0\n",
            plan->byte_array_offset,
            plan->word_array_offset,
            byte_word_loop, byte_word_loop,
            plan->long_array_offset,
            long_loop, long_loop,
            plan->index_offset, plan->index_offset + 1);
}

static void mir_emit_exec_child(
    MirStream *out, const struct MirExecChild *plan)
{
    int argc_ok = new_label();
    int argv_done = new_label();
    int argv_loop = new_label();
    int copy_done = new_label();
    int copy_loop = new_label();
    int epilogue = new_label();
    int length_ready = new_label();
    int next_index = new_label();
    int pass = new_label();

    mir_stream_puts("\tld hl,128\n\tld a,(hl)\n"
          "\tcp 127\n", out);
    mir_stream_printf(out,
            "\tjp c, L%d\n\tld a,126\nL%d:\n"
            "\tld c,a\n"
            "\tpush ix\n\tpop hl\n\tld de,-130\n\tadd hl,de\n"
            "\tld de,129\n\tld b,c\n\tld a,b\n\tor a\n"
            "\tjp z, L%d\n"
            "L%d:\n\tld a,(de)\n\tld (hl),a\n"
            "\tinc de\n\tinc hl\n\tdjnz L%d\n"
            "L%d:\n\tld (hl),0\n",
            length_ready, length_ready,
            copy_done,
            copy_loop, copy_loop,
            copy_done);
    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-130\n\tadd hl,de\n"
          "\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
            plan->tail_format_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            plan->argc_offset, plan->argc_offset + 1,
            plan->argc_format_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n"
            "\tld (ix-2),0\n\tld (ix-1),0\n"
            "L%d:\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tld a,h\n\txor 128\n\tld h,a\n"
            "\tld a,d\n\txor 128\n\tld d,a\n"
            "\tor a\n\tsbc hl,de\n\tjp nc, L%d\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n\tadd hl,hl\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n\tadd hl,de\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tpush hl\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            argv_loop,
            plan->argc_offset, plan->argc_offset + 1,
            argv_done,
            plan->argv_offset, plan->argv_offset + 1,
            plan->argv_format_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tinc (ix-2)\n\tjp nz, L%d\n\tinc (ix-1)\n"
            "L%d:\n\tjp L%d\n"
            "L%d:\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,2\n"
            "\tld a,h\n\txor 128\n\tld h,a\n"
            "\tld a,d\n\txor 128\n\tld d,a\n"
            "\tor a\n\tsbc hl,de\n\tjp nc, L%d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            next_index, next_index, argv_loop,
            argv_done,
            plan->argc_offset, plan->argc_offset + 1,
            argc_ok,
            plan->argc_offset, plan->argc_offset + 1,
            plan->argc_failure_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tld hl,1\n\tjp L%d\n"
            "L%d:\n"
            "\tld hl,S%d\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tinc hl\n\tinc hl\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tpush hl\n",
            epilogue,
            argc_ok,
            plan->child_argument_string,
            plan->argv_offset, plan->argv_offset + 1);
    mir_emit_runtime_call(out, "__scmp");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tld a,h\n\tor l\n"
            "\tjp z, L%d\n"
            "\tld hl,S%d\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tinc hl\n\tinc hl\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            pass,
            plan->child_argument_string,
            plan->argv_offset, plan->argv_offset + 1,
            plan->argument_failure_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tld hl,1\n\tjp L%d\n"
            "L%d:\n\tld hl,S%d\n\tpush hl\n",
            epilogue,
            pass, plan->pass_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tld hl,0\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            epilogue);
}

static void mir_emit_frameless_wide_parameter(
    MirStream *out, int parameter_sp_offset)
{
    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n\tinc hl\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld l,c\n\tld h,b\n",
            parameter_sp_offset);
}

static void mir_emit_wide_bitcast(
    MirStream *out, const struct MirWideBitcast *plan)
{
    mir_emit_frameless_wide_parameter(
        out, plan->parameter_sp_offset);
    mir_stream_puts("\tret\n", out);
}

static void mir_emit_float_abs(
    MirStream *out, const struct MirFloatAbs *plan)
{
    mir_emit_frameless_wide_parameter(
        out, plan->parameter_sp_offset);
    mir_stream_puts("\tpush de\n\tpush hl\n\tpush de\n\tpush hl\n"
          "\tld hl,0\n\tld de,0\n", out);
    mir_emit_runtime_call(out, "__fltf");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tld a,h\n\tor l\n"
          "\tpop hl\n\tpop de\n\tret z\n"
          "\tld a,d\n\txor 128\n\tld d,a\n\tret\n", out);
}

static void mir_emit_node_graph(
    MirStream *out, const struct MirNodeGraph *plan)
{
    const char *nodes_name = asm_name_for(plan->nodes->name);

    mir_stream_printf(out,
            "\tld hl,0\n\tld (%s),hl\n"
            "\tld hl,10\n\tld (%s+8),hl\n"
            "\tld hl,20\n\tld (%s+16),hl\n"
            "\tld hl,%s+8\n\tld (%s+2),hl\n"
            "\tld hl,%s+16\n\tld (%s+4),hl\n"
            "\tld hl,%s\n\tld (%s+6),hl\n"
            "\tld hl,(%s+8)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            nodes_name,
            nodes_name,
            nodes_name,
            nodes_name, nodes_name,
            nodes_name, nodes_name,
            nodes_name, nodes_name,
            nodes_name,
            plan->dot_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n"
            "\tld hl,(%s+4)\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tpush hl\n\tld hl,S%d\n\tpush hl\n",
            nodes_name, plan->arrow_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n"
            "\tld hl,(%s+6)\n\tinc hl\n\tinc hl\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tpush hl\n\tld hl,S%d\n\tpush hl\n",
            nodes_name, plan->chain_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tld hl,0\n\tret\n", out);
}

static void mir_emit_variadic_checks(
    MirStream *out, const struct MirVariadicChecks *plan)
{
    const char *check_macro = asm_name_for(plan->check_macro->name);
    const char *check_wide = asm_name_for(plan->check_wide->name);
    const char *macro_values = asm_name_for(plan->macro_values->name);
    const char *sum_i = asm_name_for(plan->sum_i->name);
    const char *sum_l = asm_name_for(plan->sum_l->name);
    int epilogue = new_label();
    int macro0_ok = new_label();
    int macro1_ok = new_label();
    int pass_label = new_label();
    int si_ok = new_label();
    int sl_ok = new_label();
    int wide_ok = new_label();

    mir_stream_puts("\tld (ix-8),1\n\tld (ix-7),0\n"
          "\tld hl,5\n\tpush hl\n"
          "\tld hl,4\n\tpush hl\n"
          "\tld hl,3\n\tpush hl\n"
          "\tld hl,2\n\tpush hl\n"
          "\tld hl,1\n\tpush hl\n"
          "\tld hl,5\n\tpush hl\n", out);
    mir_stream_printf(out, "\tcall %s\n", sum_i);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tld (ix-2),l\n\tld (ix-1),h\n"
          "\tld de,15\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out,
            "\tjp z, L%d\n\tld (ix-8),0\n\tld (ix-7),0\n"
            "L%d:\n",
            si_ok, si_ok);

    mir_stream_puts("\tld hl,7\n\tld de,0\n\tpush de\n\tpush hl\n"
          "\tld hl,35536\n\tld de,65535\n\tpush de\n\tpush hl\n"
          "\tld hl,3392\n\tld de,3\n\tpush de\n\tpush hl\n"
          "\tld hl,34464\n\tld de,1\n\tpush de\n\tpush hl\n"
          "\tld hl,4\n\tpush hl\n", out);
    mir_stream_printf(out, "\tcall %s\n", sum_l);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tld (ix-4),l\n\tld (ix-3),h\n"
          "\tld (ix-6),e\n\tld (ix-5),d\n"
          "\tld a,l\n\txor 183\n\tld c,a\n"
          "\tld a,h\n\txor 30\n\tor c\n\tld c,a\n"
          "\tld a,e\n\txor 4\n\tor c\n\tld c,a\n"
          "\tld a,d\n\tor c\n", out);
    mir_stream_printf(out,
            "\tjp z, L%d\n\tld (ix-8),0\n\tld (ix-7),0\n"
            "L%d:\n",
            sl_ok, sl_ok);

    mir_stream_printf(out,
            "\tld hl,(%s+8)\n\tpush hl\n"
            "\tld hl,(%s+6)\n\tpush hl\n"
            "\tld hl,(%s+4)\n\tpush hl\n"
            "\tld hl,(%s+2)\n\tpush hl\n"
            "\tld hl,(%s)\n\tpush hl\n"
            "\tld hl,0\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n"
            "\tcall %s\n"
            "\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tld a,h\n\tor l\n\tjp nz, L%d\n"
            "\tld (ix-8),0\n\tld (ix-7),0\n"
            "L%d:\n",
            macro_values, macro_values, macro_values,
            macro_values, macro_values,
            plan->macro_string, check_macro,
            macro0_ok, macro0_ok);
    mir_stream_printf(out,
            "\tld hl,(%s+18)\n\tpush hl\n"
            "\tld hl,(%s+16)\n\tpush hl\n"
            "\tld hl,(%s+14)\n\tpush hl\n"
            "\tld hl,(%s+12)\n\tpush hl\n"
            "\tld hl,(%s+10)\n\tpush hl\n"
            "\tld hl,1\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n"
            "\tcall %s\n"
            "\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tld a,h\n\tor l\n\tjp nz, L%d\n"
            "\tld (ix-8),0\n\tld (ix-7),0\n"
            "L%d:\n",
            macro_values, macro_values, macro_values,
            macro_values, macro_values,
            plan->macro_string, check_macro,
            macro1_ok, macro1_ok);

    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-8\n\tadd hl,de\n"
          "\tpush hl\n\tld hl,2\n\tpush hl\n"
          "\tld hl,17253\n\tld de,30241\n\tpush de\n\tpush hl\n"
          "\tld hl,1\n\tpush hl\n"
          "\tpush ix\n\tpop hl\n\tld de,-8\n\tadd hl,de\n"
          "\tpush hl\n", out);
    mir_stream_printf(out, "\tcall %s\n", check_wide);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tld a,h\n\tor l\n", out);
    mir_stream_printf(out,
            "\tjp nz, L%d\n\tld (ix-8),0\n\tld (ix-7),0\n"
            "L%d:\n"
            "\tld l,(ix-8)\n\tld h,(ix-7)\n"
            "\tld a,h\n\tor l\n\tjp nz, L%d\n"
            "\tld e,(ix-6)\n\tld d,(ix-5)\n"
            "\tld l,(ix-4)\n\tld h,(ix-3)\n"
            "\tpush de\n\tpush hl\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            wide_ok, wide_ok,
            pass_label,
            plan->failure_string);
    mir_emit_runtime_call(out, "_pflng");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tld hl,1\n\tjp L%d\n"
            "L%d:\n\tld hl,S%d\n\tpush hl\n",
            epilogue,
            pass_label, plan->pass_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tld hl,0\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            epilogue);
}

static void mir_emit_weighted_string_total(
    MirStream *out, const struct MirWeightedStringTotal *plan)
{
    const char *total_name = asm_name_for(plan->total->name);

    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tpush hl\n",
            plan->text_sp_offset);
    mir_emit_runtime_call(out, "__slen");
    mir_stream_printf(out,
            "\tpop bc\n\tld d,h\n\tld e,l\n"
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n",
            plan->weight_sp_offset);
    mir_emit_runtime_call(out, "__mulu");
    mir_stream_printf(out,
            "\tld de,(%s)\n\tadd hl,de\n\tld (%s),hl\n\tret\n",
            total_name, total_name);
}

static void mir_emit_divmod_check(
    MirStream *out, const struct MirDivmodCheck *plan)
{
    const char *callee_name = asm_name_for(plan->callee->name);
    const char *checks_name = asm_name_for(plan->checks->name);
    const char *failures_name = asm_name_for(plan->failures->name);
    const char *remainder_name = asm_name_for(plan->remainder->name);
    int done = new_label();
    int failure = new_label();

    mir_emit_symbol_extrn(out, plan->callee);
    mir_emit_symbol_extrn(out, plan->remainder);
    mir_emit_symbol_extrn(out, plan->checks);
    mir_emit_symbol_extrn(out, plan->failures);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tcall %s\n\tpop bc\n\tpop bc\n"
            "\tld (ix-2),l\n\tld (ix-1),h\n"
            "\tld hl,(%s)\n\tld (ix-4),l\n\tld (ix-3),h\n"
            "\tld hl,(%s)\n\tinc hl\n\tld (%s),hl\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nz, L%d\n"
            "\tld l,(ix-4)\n\tld h,(ix-3)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nz, L%d\n"
            "\tjp L%d\n"
            "L%d:\n"
            "\tld hl,(%s)\n\tinc hl\n\tld (%s),hl\n",
            plan->parameter_offset[1], plan->parameter_offset[1] + 1,
            plan->parameter_offset[0], plan->parameter_offset[0] + 1,
            callee_name,
            remainder_name,
            checks_name, checks_name,
            plan->parameter_offset[2], plan->parameter_offset[2] + 1,
            failure,
            plan->parameter_offset[3], plan->parameter_offset[3] + 1,
            failure,
            done,
            failure,
            failures_name, failures_name);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld l,(ix-4)\n\tld h,(ix-3)\n\tpush hl\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            plan->parameter_offset[3], plan->parameter_offset[3] + 1,
            plan->parameter_offset[2], plan->parameter_offset[2] + 1,
            plan->parameter_offset[1], plan->parameter_offset[1] + 1,
            plan->parameter_offset[0], plan->parameter_offset[0] + 1,
            plan->format_string);
    mir_emit_runtime_call(out, plan->print_name);
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tpop bc\n\tpop bc\n\tpop bc\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            done);
}

static void mir_emit_prefix_check(
    MirStream *out, const struct MirPrefixCheck *plan)
{
    const char *failures_name = asm_name_for(plan->failures->name);
    int failure = new_label();
    int loop = new_label();
    int success = new_label();

    mir_emit_symbol_extrn(out, plan->failures);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tld bc,0\n"
            "L%d:\n\tld a,(de)\n\tor a\n\tjp z, L%d\n"
            "\tcp (hl)\n\tjp nz, L%d\n"
            "\tinc hl\n\tinc de\n\tinc bc\n\tjp L%d\n"
            "L%d:\n"
            "\tld a,(de)\n\tld e,a\n\tld d,0\n\tpush de\n"
            "\tld a,(hl)\n\tld e,a\n\tld d,0\n\tpush de\n"
            "\tpush bc\n"
            "\tld hl,(%s)\n\tinc hl\n\tld (%s),hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            plan->buffer_offset, plan->buffer_offset + 1,
            plan->wanted_offset, plan->wanted_offset + 1,
            loop, success,
            failure,
            loop,
            failure,
            failures_name, failures_name,
            plan->tag_offset, plan->tag_offset + 1,
            plan->failure_string);
    mir_emit_runtime_call(out, "__pfehx");
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
            "\tld sp,ix\n\tpop ix\n\tret\n"
            "L%d:\n\tpush bc\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            success,
            plan->tag_offset, plan->tag_offset + 1,
            plan->success_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_struct_sort(
    MirStream *out, const struct MirStructSort *plan)
{
    const char *compare_name = asm_name_for(plan->compare->name);
    int done = new_label();
    int item;

    for (item = 0; item < 3; ++item) {
        mir_stream_printf(out,
                "\tpush ix\n\tpop hl\n\tld de,%d\n\tadd hl,de\n"
                "\tex de,hl\n\tld hl,S%d\n",
                -30 + item * 10,
                plan->item_name_string[item]);
        mir_emit_runtime_call(out, "__scf");
        mir_stream_printf(out,
                "\tld (ix%+d),%d\n\tld (ix%+d),0\n",
                -22 + item * 10, item == 0 ? 4 : item == 1 ? 9 : 2,
                -21 + item * 10);
    }
    mir_stream_printf(out,
            "\tld hl,%s\n\tpush hl\n"
            "\tld hl,10\n\tpush hl\n"
            "\tld hl,3\n\tpush hl\n"
            "\tpush ix\n\tpop hl\n\tld de,-30\n\tadd hl,de\n"
            "\tpush hl\n",
            compare_name);
    mir_emit_runtime_call(out, "_qsort");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n", out);
    for (item = 0; item < 3; ++item) {
        mir_stream_printf(out,
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
                "\tpush ix\n\tpop hl\n\tld de,%d\n\tadd hl,de\n"
                "\tpush hl\n\tld hl,S%d\n\tpush hl\n",
                -22 + item * 10, -21 + item * 10,
                -30 + item * 10,
                plan->row_format_string);
        mir_emit_runtime_call(out, "_printf");
        mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n", out);
    }
    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-40\n\tadd hl,de\n"
          "\tex de,hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n", plan->item_name_string[2]);
    mir_emit_runtime_call(out, "__scf");
    mir_stream_printf(out,
            "\tld hl,%s\n\tpush hl\n"
            "\tld hl,10\n\tpush hl\n"
            "\tld hl,3\n\tpush hl\n"
            "\tpush ix\n\tpop hl\n\tld de,-30\n\tadd hl,de\n"
            "\tpush hl\n"
            "\tpush ix\n\tpop hl\n\tld de,-40\n\tadd hl,de\n"
            "\tpush hl\n",
            compare_name);
    mir_emit_runtime_call(out, "_bsearch");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n", done);
    mir_stream_puts("\tld d,h\n\tld e,l\n\tld bc,8\n\tadd hl,bc\n"
          "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
          "\tpush hl\n\tpush de\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->hit_format_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tpop bc\n"
            "L%d:\n\tld hl,0\n\tld sp,ix\n\tpop ix\n\tret\n",
            done);
}

static void mir_emit_fixed_word_sum(
    MirStream *out, const struct MirFixedWordSum *plan)
{
    int element;

    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
            "\tld de,0\n",
            plan->parameter_sp_offset);
    for (element = 0; element < plan->count; ++element)
        mir_stream_puts("\tld a,(bc)\n\tld l,a\n\tinc bc\n"
              "\tld a,(bc)\n\tld h,a\n\tinc bc\n"
              "\tadd hl,de\n\tex de,hl\n", out);
    mir_stream_puts("\tex de,hl\n\tret\n", out);
}

static void mir_emit_bdos_string(
    MirStream *out, const struct MirBdosString *plan)
{
    int done = new_label();
    int loop = new_label();

    mir_stream_printf(out,
            "L%d:\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld a,(hl)\n\tor a\n\tjp z, L%d\n"
            "\tinc hl\n\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "\tld e,a\n\trlca\n\tsbc a,a\n\tld d,a\n"
            "\tld c,2\n",
            loop,
            plan->parameter_offset, plan->parameter_offset + 1,
            done,
            plan->parameter_offset, plan->parameter_offset + 1);
    mir_emit_runtime_call(out, "__bdosf");
    mir_stream_printf(out,
            "\tjp L%d\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            loop, done);
}

static void mir_emit_time_failure(
    MirStream *out, const struct MirTimeChecks *plan,
    int message, int success_label)
{
    const char *failures_name = asm_name_for(plan->failures->name);

    mir_stream_printf(out,
            "\tld hl,S%d\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            plan->message_string[message],
            plan->format_string);
    mir_emit_runtime_call(out, "_printf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n"
            "\tld hl,(%s)\n\tinc hl\n\tld (%s),hl\n"
            "L%d:\n",
            failures_name, failures_name, success_label);
}

static void mir_emit_time_checks(
    MirStream *out, const struct MirTimeChecks *plan)
{
    static const char *const null_pointer_call[4] = {
        "_asctime", "_ctime", "_gmtime", "__ltim"
    };
    int check;
    int success;

    mir_emit_symbol_extrn(out, plan->failures);
    mir_emit_runtime_call(out, "_clock");
    mir_stream_puts("\tld a,l\n\tand h\n\tand e\n\tand d\n\tinc a\n", out);
    success = new_label();
    mir_stream_printf(out, "\tjp z, L%d\n", success);
    mir_emit_time_failure(out, plan, 0, success);

    mir_stream_puts("\tld hl,0\n\tpush hl\n", out);
    mir_emit_runtime_call(out, "_time");
    mir_stream_puts("\tpop bc\n\tld a,l\n\tand h\n\tand e\n\tand d\n\tinc a\n", out);
    success = new_label();
    mir_stream_printf(out, "\tjp z, L%d\n", success);
    mir_emit_time_failure(out, plan, 1, success);

    mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-4\n\tadd hl,de\n\tpush hl\n", out);
    mir_emit_runtime_call(out, "_time");
    mir_stream_puts("\tpop bc\n\tld a,l\n\tand h\n\tand e\n\tand d\n\tinc a\n", out);
    success = new_label();
    mir_stream_printf(out, "\tjp z, L%d\n", success);
    mir_emit_time_failure(out, plan, 2, success);

    mir_stream_puts("\tld hl,0\n\tpush hl\n", out);
    mir_emit_runtime_call(out, "_mktime");
    mir_stream_puts("\tpop bc\n\tld a,l\n\tand h\n\tand e\n\tand d\n\tinc a\n", out);
    success = new_label();
    mir_stream_printf(out, "\tjp z, L%d\n", success);
    mir_emit_time_failure(out, plan, 3, success);

    for (check = 0; check < 4; ++check) {
        mir_stream_puts("\tld hl,0\n\tpush hl\n", out);
        mir_emit_runtime_call(out, null_pointer_call[check]);
        mir_stream_puts("\tpop bc\n\tld a,h\n\tor l\n", out);
        success = new_label();
        mir_stream_printf(out, "\tjp z, L%d\n", success);
        mir_emit_time_failure(out, plan, 4 + check, success);
    }

    mir_stream_puts("\tld hl,0\n\tpush hl\n\tpush hl\n\tpush hl\n\tpush hl\n", out);
    mir_emit_runtime_call(out, "_strftime");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tld a,h\n\tor l\n", out);
    success = new_label();
    mir_stream_printf(out, "\tjp z, L%d\n", success);
    mir_emit_time_failure(out, plan, 8, success);

    mir_stream_puts("\tld hl,65535\n\tld de,65535\n"
          "\tpush de\n\tpush hl\n\tpush de\n\tpush hl\n", out);
    mir_emit_runtime_call(out, "_difftime");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tpush de\n\tpush hl\n\tld hl,0\n\tld de,0\n", out);
    mir_emit_runtime_call(out, "__feqf");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tld a,h\n\tor l\n", out);
    success = new_label();
    mir_stream_printf(out, "\tjp nz, L%d\n", success);
    mir_emit_time_failure(out, plan, 9, success);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_enum_fsm(
    MirStream *out, const struct MirEnumFsm *plan)
{
    const char *transition_name = asm_name_for(plan->transition->name);
    int done = new_label();
    int loop = new_label();

    mir_emit_symbol_extrn(out, plan->transition);
    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld bc,0\n"
            "L%d:\n\tld a,(de)\n\tor a\n\tjp z, L%d\n"
            "\tinc de\n\tsub 48\n\tadd a,a\n"
            "\tld l,c\n\tld h,b\n\tadd hl,hl\n\tadd hl,hl\n"
            "\tadd a,l\n\tld l,a\n\tld a,h\n\tadc a,0\n\tld h,a\n"
            "\tld bc,%s\n\tadd hl,bc\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n\tjp L%d\n"
            "L%d:\n\tld l,c\n\tld h,b\n\tret\n",
            plan->input_sp_offset,
            loop, done,
            transition_name,
            loop,
            done);
}

static void mir_emit_small_word_switch(
    MirStream *out, const struct MirSmallWordSwitch *plan)
{
    int default_label = new_label();
    int range_label = new_label();

    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
            "\tld l,c\n\tld h,b\n",
            plan->input_sp_offset);
    if (plan->first_case == 1)
        mir_stream_puts("\tdec hl\n", out);
    else
        mir_stream_printf(out, "\tld de,%d\n\tor a\n\tsbc hl,de\n",
                plan->first_case);
    mir_stream_printf(out,
            "\tld a,h\n\tor a\n\tjp nz, L%d\n"
            "\tld a,l\n\tcp %d\n\tjp c, L%d\n\tjp nz, L%d\n"
            "\tld hl,%d\n\tret\n"
            "L%d:\n\tld hl,%d\n\tret\n"
            "L%d:\n\tld hl,%d\n\tret\n",
            default_label,
            plan->case_count, range_label, default_label,
            plan->trailing_value,
            range_label, plan->range_value,
            default_label, plan->default_value);
}

static void mir_emit_float_identity_store(
    MirStream *out, const struct MirFloatPointerChecks *plan,
    int source_offset, int destination_offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tpush de\n\tpush hl\n",
            source_offset, source_offset + 1,
            source_offset + 2, source_offset + 3);
    mir_emit_symbol_call(out, plan->identity_function);
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n"
            "\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "\tld (ix%+d),e\n\tld (ix%+d),d\n",
            destination_offset, destination_offset + 1,
            destination_offset + 2, destination_offset + 3);
}

static void mir_emit_float_pointer_check(
    MirStream *out, const struct MirFloatPointerChecks *plan, int item)
{
    int byte;

    for (byte = 3; byte >= 0; --byte)
        mir_stream_printf(out, "\tld hl,%d\n\tpush hl\n",
                plan->expected[item][byte]);
    mir_stream_puts("\tpush ix\n\tpop hl\n", out);
    mir_emit_hl_offset_from_ix(out, plan->array_offset + item * 4);
    mir_stream_printf(out, "\tpush hl\n\tld hl,S%d\n\tpush hl\n",
            plan->name_string[item]);
    mir_emit_symbol_call(out, plan->check_function);
    mir_stream_puts("\tld hl,12\n\tadd hl,sp\n\tld sp,hl\n", out);
}

static void mir_emit_float_pointer_checks(
    MirStream *out, const struct MirFloatPointerChecks *plan)
{
    const char *failures_name = asm_name_for(plan->failures->name);
    static const unsigned char initial_bytes[3][4] = {
        { 0, 0, 128, 63 },
        { 0, 0, 32, 64 },
        { 0, 0, 64, 192 }
    };
    int done = new_label();
    int success = new_label();
    int item;
    int byte;

    mir_emit_symbol_extrn(out, plan->failures);
    mir_stream_printf(out, "\tld hl,0\n\tld (%s),hl\n",
            failures_name);
    for (item = 0; item < 3; ++item)
        for (byte = 0; byte < 4; ++byte)
            mir_stream_printf(out, "\tld (ix%+d),%u\n",
                    plan->array_offset + item * 4 + byte,
                    (unsigned)initial_bytes[item][byte]);
    mir_emit_float_identity_store(
        out, plan, plan->array_offset + 4, plan->array_offset + 12);
    mir_emit_float_identity_store(
        out, plan, plan->array_offset + 8, plan->array_offset + 8);
    for (item = 0; item < 4; ++item)
        mir_emit_float_pointer_check(out, plan, item);
    mir_stream_printf(out,
            "\tld hl,(%s)\n\tld a,h\n\tor l\n\tjp z, L%d\n"
            "\tpush hl\n\tld hl,S%d\n\tpush hl\n",
            failures_name, success, plan->failure_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n\tld hl,1\n\tjp L%d\n"
            "L%d:\n\tld hl,S%d\n\tpush hl\n",
            done, success, plan->success_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_printf(out,
            "\tpop bc\n\tld hl,0\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            done);
}

static void mir_emit_errno_check(
    MirStream *out, const struct MirErrnoCheck *plan)
{
    const char *errno_name = asm_name_for(plan->errno_value->name);
    const char *failures_name = asm_name_for(plan->failures->name);
    int byte;
    int done = new_label();
    int failure = new_label();

    mir_emit_symbol_extrn(out, plan->errno_value);
    mir_emit_symbol_extrn(out, plan->failures);
    if (plan->result_width == 2) {
        mir_stream_printf(out,
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tinc hl\n"
                "\tld a,h\n\tor l\n\tjp nz, L%d\n",
                plan->result_offset, plan->result_offset + 1,
                failure);
    } else {
        for (byte = 0; byte < 4; ++byte)
            mir_stream_printf(out,
                    "\tld a,(ix%+d)\n\tinc a\n\tjp nz, L%d\n",
                    plan->result_offset + byte, failure);
    }
    mir_stream_printf(out,
            "\tld hl,(%s)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nz, L%d\n"
            "\tld hl,(%s)\n\tpush hl\n",
            errno_name,
            plan->want_offset, plan->want_offset + 1,
            failure,
            errno_name);
    mir_emit_symbol_call(out, plan->strerror_function);
    mir_stream_printf(out,
            "\tpop bc\n\tpush hl\n"
            "\tld hl,(%s)\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            errno_name,
            plan->name_offset, plan->name_offset + 1,
            plan->success_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_printf(out,
            "\tld hl,8\n\tadd hl,sp\n\tld sp,hl\n\tjp L%d\n"
            "L%d:\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,(%s)\n\tpush hl\n",
            done, failure,
            plan->want_offset, plan->want_offset + 1,
            errno_name);
    if (plan->result_width == 4)
        mir_stream_printf(out,
                "\tld e,(ix%+d)\n\tld d,(ix%+d)\n\tpush de\n"
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
                plan->result_offset + 2, plan->result_offset + 3,
                plan->result_offset, plan->result_offset + 1);
    else
        mir_stream_printf(out,
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
                plan->result_offset, plan->result_offset + 1);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            plan->name_offset, plan->name_offset + 1,
            plan->failure_string);
    if (plan->result_width == 4)
        mir_emit_runtime_call(out, "_pflng");
    else
        mir_emit_symbol_call(out, plan->print_function);
    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n\tld sp,hl\n"
            "\tld hl,(%s)\n\tinc hl\n\tld (%s),hl\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            plan->result_width == 4 ? 12 : 10,
            failures_name, failures_name,
            done);
}

static void mir_emit_cyclic_byte_fill(
    MirStream *out, const struct MirCyclicByteFill *plan)
{
    const char *buffer_name = asm_name_for(sym_asm_name(plan->buffer));
    int loop = new_label();
    int next = new_label();

    mir_emit_symbol_extrn(out, plan->buffer);
    mir_stream_printf(out,
            "\tld hl,%s\n\tld bc,%d\n\tld e,%d\n"
            "L%d:\n\tld a,e\n\tld (hl),a\n\tinc hl\n\tinc e\n"
            "\tld a,e\n\tcp %d\n\tjp nz, L%d\n\tld e,%d\n"
            "L%d:\n\tdec bc\n\tld a,b\n\tor c\n\tjp nz, L%d\n",
            buffer_name, plan->count, plan->first_byte,
            loop, plan->first_byte + plan->modulus,
            next, plan->first_byte,
            next, loop);
}

static void mir_emit_byte_compare_flags(
    MirStream *out, const struct MirByteCompareFlags *plan)
{
    const char *state_name = asm_name_for(plan->state->name);
    int carry_done = new_label();
    int nonzero = new_label();

    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n\tld b,(hl)\n"
            "\tld hl,%d\n\tadd hl,sp\n\tld c,(hl)\n"
            "\tld a,b\n\tsub c\n\tld d,a\n"
            "\trlca\n\tand 1\n\tld (%s+%d),a\n"
            "\tld a,d\n\tor a\n\tld a,0\n"
            "\tjp nz, L%d\n\tinc a\n"
            "L%d:\n\tld (%s+%d),a\n"
            "\tld a,b\n\tcp c\n\tld a,0\n"
            "\tjp c, L%d\n\tinc a\n"
            "L%d:\n\tld (%s+%d),a\n\tret\n",
            plan->lhs_sp_offset,
            plan->rhs_sp_offset,
            state_name, plan->negative_offset,
            nonzero, nonzero,
            state_name, plan->zero_offset,
            carry_done, carry_done,
            state_name, plan->carry_offset);
}

static void mir_emit_bitset_access(
    MirStream *out, const struct MirBitsetAccess *plan)
{
    int mask_loop = new_label();
    int mask_ready = new_label();

    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld a,e\n\tand 15\n\tld b,a\n"
            "\tld a,e\n\tand 240\n\tld e,a\n"
            "\tsra d\n\trr e\n"
            "\tsra d\n\trr e\n"
            "\tsra d\n\trr e\n"
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tadd hl,de\n\tex de,hl\n"
            "\tld hl,1\n\tld a,b\n\tor a\n\tjp z, L%d\n"
            "L%d:\n\tadd hl,hl\n\tdjnz L%d\n"
            "L%d:\n\tex de,hl\n",
            plan->value_sp_offset,
            plan->set_sp_offset,
            mask_ready,
            mask_loop, mask_loop,
            mask_ready);
    if (plan->kind == MIR_BITSET_ADD) {
        mir_stream_puts("\tld a,(hl)\n\tor e\n\tld (hl),a\n"
              "\tinc hl\n\tld a,(hl)\n\tor d\n\tld (hl),a\n"
              "\tret\n", out);
    } else {
        mir_stream_puts("\tld a,(hl)\n\tand e\n\tld e,a\n"
              "\tinc hl\n\tld a,(hl)\n\tand d\n\tor e\n"
              "\tld hl,0\n\tret z\n\tinc l\n\tret\n", out);
    }
}

static void mir_emit_task_open_count(
    MirStream *out, const struct MirTaskOpenCount *plan)
{
    int loop = new_label();
    int next = new_label();
    int zero = new_label();

    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
            "\tbit 7,b\n\tjp nz, L%d\n"
            "\tld a,b\n\tor c\n\tjp z, L%d\n"
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n"
            "\tld de,0\n"
            "L%d:\n"
            "\tinc hl\n\tinc hl\n\tinc hl\n\tinc hl\n"
            "\tld a,(hl)\n\tinc hl\n\tor a\n\tjp nz, L%d\n"
            "\tinc de\n"
            "L%d:\n"
            "\tdec bc\n\tld a,b\n\tor c\n\tjp nz, L%d\n"
            "\tex de,hl\n\tret\n"
            "L%d:\n\tld hl,0\n\tret\n",
            plan->count_sp_offset,
            zero, zero,
            plan->tasks_sp_offset,
            loop,
            next,
            next,
            loop,
            zero);
}

static void mir_emit_ascii_identifier(
    MirStream *out, const struct MirAsciiIdentifier *plan)
{
    int digit = new_label();
    int failure = new_label();
    int first_lower = new_label();
    int loop = new_label();
    int loop_lower = new_label();
    int next = new_label();
    int success = new_label();

    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tld a,(hl)\n"
            "\tcp %d\n\tjp c, L%d\n"
            "\tcp %d\n\tjp c, L%d\n"
            "L%d:\n"
            "\tcp %d\n\tjp c, L%d\n"
            "\tcp %d\n\tjp nc, L%d\n"
            "\tinc hl\n"
            "L%d:\n"
            "\tld a,(hl)\n\tor a\n\tjp z, L%d\n"
            "\tcp %d\n\tjp c, L%d\n"
            "\tcp %d\n\tjp c, L%d\n"
            "L%d:\n"
            "\tcp %d\n\tjp c, L%d\n"
            "\tcp %d\n\tjp c, L%d\n"
            "L%d:\n"
            "\tcp %d\n\tjp c, L%d\n"
            "\tcp %d\n\tjp nc, L%d\n"
            "L%d:\n"
            "\tinc hl\n\tjp L%d\n"
            "L%d:\n"
            "\tld hl,0\n\tret\n"
            "L%d:\n"
            "\tld hl,1\n\tret\n",
            plan->parameter_sp_offset,
            plan->upper_first, first_lower,
            plan->upper_last + 1, next,
            first_lower,
            plan->lower_first, failure,
            plan->lower_last + 1, failure,
            loop,
            success,
            plan->upper_first, loop_lower,
            plan->upper_last + 1, next,
            loop_lower,
            plan->lower_first, digit,
            plan->lower_last + 1, next,
            digit,
            plan->digit_first, failure,
            plan->digit_last + 1, failure,
            next,
            loop,
            failure,
            success);
}

static void mir_emit_ascii_piece_side(
    MirStream *out, const struct MirAsciiPieceSide *plan)
{
    int lower = new_label();
    int upper_result = new_label();
    int lower_result = new_label();
    int other_result = new_label();

    mir_emit_frameless_byte_parameter(out, plan->parameter_sp_offset);
    mir_stream_printf(out,
            "\tcp %d\n\tjp c, L%d\n"
            "\tcp %d\n\tjp c, L%d\n"
            "L%d:\n"
            "\tcp %d\n\tjp c, L%d\n"
            "\tcp %d\n\tjp c, L%d\n"
            "\tjp L%d\n"
            "L%d:\n\tld hl,%d\n\tret\n"
            "L%d:\n\tld hl,%d\n\tret\n"
            "L%d:\n\tld hl,%d\n\tret\n",
            plan->upper_first, lower,
            plan->upper_last + 1, upper_result,
            lower,
            plan->lower_first, other_result,
            plan->lower_last + 1, lower_result,
            other_result,
            upper_result, plan->upper_result & 0xffff,
            lower_result, plan->lower_result & 0xffff,
            other_result, plan->other_result & 0xffff);
}

static void mir_emit_ascii_piece_value(
    MirStream *out, const struct MirAsciiPieceValue *plan)
{
    int folded = new_label();
    int labels[6];
    int item;

    mir_emit_frameless_byte_parameter(out, plan->parameter_sp_offset);
    mir_stream_printf(out,
            "\tcp %d\n\tjp c, L%d\n"
            "\tcp %d\n\tjp nc, L%d\n"
            "\tsub %d\n"
            "L%d:\n",
            plan->lower_first, folded,
            plan->lower_last + 1, folded,
            plan->fold_amount, folded);
    for (item = 0; item < 6; ++item) {
        labels[item] = new_label();
        mir_stream_printf(out, "\tcp %d\n\tjp z, L%d\n",
                plan->piece[item], labels[item]);
    }
    mir_stream_printf(out, "\tld hl,%d\n\tret\n",
            plan->default_value & 0xffff);
    for (item = 0; item < 6; ++item)
        mir_stream_printf(out, "L%d:\n\tld hl,%d\n\tret\n",
                labels[item], plan->value[item] & 0xffff);
}

static void mir_emit_sign_extend_a(MirStream *out)
{
    mir_stream_puts("\tld l,a\n\trlca\n\tsbc a,a\n\tld h,a\n", out);
}

static void mir_emit_evaluate_board_reload(
    MirStream *out, const char *board_name)
{
    mir_stream_printf(out,
            "\tld l,(ix-3)\n\tld h,0\n"
            "\tld de,%s\n\tadd hl,de\n"
            "\tld a,(hl)\n\tld (ix-4),a\n",
            board_name);
}

static void mir_emit_board_evaluate(
    MirStream *out, const struct MirBoardEvaluate *plan)
{
    const char *board_name =
        asm_name_for(sym_asm_name(plan->board));
    const char *side_name =
        asm_name_for(sym_asm_name(plan->side));
    const char *value_name =
        asm_name_for(sym_asm_name(plan->value_function));
    const char *position_name =
        asm_name_for(sym_asm_name(plan->position_function));
    const char *piece_side_name =
        asm_name_for(sym_asm_name(plan->side_function));
    int loop = new_label();
    int tail = new_label();
    int black = new_label();
    int update_done = new_label();
    int positive = new_label();
    int exit = new_label();

    if ((plan->board->storage == SC_EXTERN ||
         plan->board->needs_extrn) &&
        mir_extrn_should_emit(plan->board))
        mir_stream_printf(out, "\textrn %s\n", board_name);
    if ((plan->side->storage == SC_EXTERN ||
         plan->side->needs_extrn) &&
        mir_extrn_should_emit(plan->side))
        mir_stream_printf(out, "\textrn %s\n", side_name);
    mir_stream_puts("\tld hl,0\n\tld (ix-2),l\n\tld (ix-1),h\n"
          "\tld (ix-3),0\n", out);
    mir_stream_printf(out,
            "L%d:\n"
            "\tld l,(ix-3)\n\tld h,0\n\tld de,%s\n\tadd hl,de\n"
            "\tld a,(hl)\n\tcp %d\n\tjp z, L%d\n"
            "\tld (ix-4),a\n",
            loop, board_name, plan->blank, tail);

    mir_emit_evaluate_board_reload(out, board_name);
    mir_emit_sign_extend_a(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tcall %s\n\tpop bc\n", value_name);
    mir_stream_puts("\tld (ix-6),l\n\tld (ix-5),h\n", out);

    mir_stream_puts("\tld l,(ix-3)\n\tld h,0\n\tpush hl\n", out);
    mir_emit_evaluate_board_reload(out, board_name);
    mir_stream_puts("\tld a,(ix-4)\n", out);
    mir_emit_sign_extend_a(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tcall %s\n\tpop bc\n\tpop bc\n",
            position_name);
    mir_stream_puts("\tld e,(ix-6)\n\tld d,(ix-5)\n\tadd hl,de\n"
          "\tld (ix-6),l\n\tld (ix-5),h\n", out);

    mir_emit_evaluate_board_reload(out, board_name);
    mir_stream_puts("\tld a,(ix-4)\n", out);
    mir_emit_sign_extend_a(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tcall %s\n\tpop bc\n", piece_side_name);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n\tjp nz, L%d\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n"
            "\tld e,(ix-6)\n\tld d,(ix-5)\n\tadd hl,de\n"
            "\tld (ix-2),l\n\tld (ix-1),h\n\tjp L%d\n"
            "L%d:\n",
            plan->white, black,
            update_done,
            black);
    mir_emit_evaluate_board_reload(out, board_name);
    mir_stream_puts("\tld a,(ix-4)\n", out);
    mir_emit_sign_extend_a(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tcall %s\n\tpop bc\n", piece_side_name);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n\tjp nz, L%d\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n"
            "\tld e,(ix-6)\n\tld d,(ix-5)\n"
            "\tor a\n\tsbc hl,de\n"
            "\tld (ix-2),l\n\tld (ix-1),h\n"
            "L%d:\n",
            plan->black, update_done,
            update_done);
    mir_stream_printf(out,
            "L%d:\n"
            "\tinc (ix-3)\n\tld a,(ix-3)\n\tcp %d\n\tjp c, L%d\n"
            "\tld hl,(%s)\n\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp z, L%d\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n"
            "\txor a\n\tsub l\n\tld l,a\n"
            "\tld a,0\n\tsbc a,h\n\tld h,a\n\tjp L%d\n"
            "L%d:\n\tld l,(ix-2)\n\tld h,(ix-1)\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            tail, plan->bound, loop,
            side_name, plan->white, positive,
            exit,
            positive,
            exit);
}

static void mir_emit_center_score(
    MirStream *out, const struct MirCenterScore *plan)
{
    int file_nonnegative = new_label();
    int rank_nonnegative = new_label();

    mir_stream_printf(out,
            "\tld hl,%d\n\tadd hl,sp\n"
            "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n"
            "\tpush hl\n"
            "\tld a,l\n\tand %d\n",
            plan->parameter_sp_offset, plan->file_mask);
    if (plan->scale == 2)
        mir_stream_puts("\tadd a,a\n", out);
    else
        mir_stream_printf(out, "\tld e,%d\n\tcall __mulu\n\tld a,l\n",
                plan->scale);
    mir_stream_printf(out,
            "\tsub %d\n\tjp nc, L%d\n\tneg\n"
            "L%d:\n\tld b,a\n"
            "\tpop hl\n",
            plan->midpoint, file_nonnegative, file_nonnegative);
    if (plan->rank_shift > 0)
        mir_emit_scalar_shift_by_constant(
            out, TOK_SHR, 0, plan->rank_shift);
    if (plan->scale == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    else
        mir_emit_mul_hl_const(out, (unsigned long)plan->scale);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tbit 7,h\n\tjp z, L%d\n"
            "\txor a\n\tsub l\n\tld l,a\n"
            "\tld a,0\n\tsbc a,h\n\tld h,a\n"
            "L%d:\n"
            "\tex de,hl\n\tld hl,%d\n\tld a,l\n\tsub b\n\tld l,a\n"
            "\tor a\n\tsbc hl,de\n\tret\n",
            plan->midpoint, rank_nonnegative, rank_nonnegative,
            plan->total);
}

static void mir_emit_king_scan(
    MirStream *out, const struct MirKingScan *plan)
{
    const char *board_name =
        asm_name_for(sym_asm_name(plan->board));
    const char *attack_name =
        asm_name_for(sym_asm_name(plan->attack_function));
    int black = new_label();
    int initialized = new_label();
    int loop = new_label();
    int found = new_label();
    int exit = new_label();

    if ((plan->board->storage == SC_EXTERN ||
         plan->board->needs_extrn) &&
        mir_extrn_should_emit(plan->board))
        mir_stream_printf(out, "\textrn %s\n", board_name);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,%d\n\tor a\n\tsbc hl,de\n\tjp nz, L%d\n"
            "\tld c,%d\n\tjp L%d\n"
            "L%d:\n\tld c,%d\n"
            "L%d:\n\tld b,0\n\tld hl,%s\n"
            "L%d:\n\tld a,(hl)\n\tcp c\n\tjp z, L%d\n"
            "\tinc hl\n\tinc b\n\tld a,b\n\tcp %d\n\tjp c, L%d\n"
            "\tld hl,%d\n\tjp L%d\n"
            "L%d:\n",
            plan->side_offset, plan->side_offset + 1,
            plan->white, black,
            plan->white_king, initialized,
            black, plan->black_king,
            initialized, board_name,
            loop, found,
            plan->bound, loop,
            plan->missing_result, exit,
            found);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\txor a\n\tsub l\n\tld l,a\n"
            "\tld a,0\n\tsbc a,h\n\tld h,a\n\tpush hl\n"
            "\tld l,b\n\tld h,0\n\tpush hl\n"
            "\tcall %s\n\tpop bc\n\tpop bc\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            plan->side_offset, plan->side_offset + 1,
            attack_name, exit);
}

static void mir_emit_piece_position(
    MirStream *out, const struct MirPiecePosition *plan)
{
    const char *center_name =
        asm_name_for(sym_asm_name(plan->center_function));
    int folded = new_label();
    int rank_ready = new_label();
    int pawn = new_label();
    int knight = new_label();
    int bishop = new_label();
    int queen = new_label();
    int rook = new_label();
    int king = new_label();
    int knight_done = new_label();
    int bishop_done = new_label();
    int king_done = new_label();
    int exit = new_label();

    mir_stream_printf(out,
            "\tld a,(ix%+d)\n"
            "\tcp 97\n\tjp c, L%d\n\tcp 123\n\tjp nc, L%d\n"
            "\tsub 32\n"
            "L%d:\n\tld (ix-1),a\n",
            plan->piece_offset, folded, folded, folded);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            plan->square_offset, plan->square_offset + 1);
    mir_emit_scalar_shift_by_constant(out, TOK_SHR, 0, 3);
    mir_stream_puts("\tld (ix-3),l\n\tld (ix-2),h\n", out);
    mir_stream_printf(out,
            "\tld a,(ix%+d)\n\tcp 97\n\tjp c, L%d\n"
            "\tcp 123\n\tjp nc, L%d\n"
            "\tld hl,7\n\tld e,(ix-3)\n\tld d,(ix-2)\n"
            "\tor a\n\tsbc hl,de\n"
            "\tld (ix-3),l\n\tld (ix-2),h\n"
            "L%d:\n",
            plan->piece_offset, rank_ready,
            rank_ready, rank_ready);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tcall %s\n\tpop bc\n"
            "\tld (ix-5),l\n\tld (ix-4),h\n"
            "\tld a,(ix-1)\n"
            "\tcp 80\n\tjp z, L%d\n"
            "\tcp 78\n\tjp z, L%d\n"
            "\tcp 66\n\tjp z, L%d\n"
            "\tcp 81\n\tjp z, L%d\n"
            "\tcp 82\n\tjp z, L%d\n"
            "\tcp 75\n\tjp z, L%d\n"
            "\tld hl,0\n\tjp L%d\n",
            plan->square_offset, plan->square_offset + 1,
            center_name,
            pawn, knight, bishop, queen, rook, king, exit);

    mir_stream_printf(out,
            "L%d:\n"
            "\tld l,(ix-3)\n\tld h,(ix-2)\n"
            "\tld e,l\n\tld d,h\n\tadd hl,hl\n\tadd hl,hl\n\tadd hl,de\n"
            "\tld e,(ix-5)\n\tld d,(ix-4)\n\tadd hl,de\n\tjp L%d\n",
            pawn, exit);

    mir_stream_printf(out,
            "L%d:\n"
            "\tld l,(ix-5)\n\tld h,(ix-4)\n"
            "\tadd hl,hl\n\tadd hl,hl\n\tadd hl,hl\n"
            "\tbit 7,(ix-2)\n\tjp nz, L%d\n"
            "\tld a,(ix-3)\n\tor (ix-2)\n\tjp z, L%d\n"
            "\tld de,20\n\tadd hl,de\n"
            "L%d:\n\tjp L%d\n",
            knight, knight_done, knight_done, knight_done, exit);

    mir_stream_printf(out,
            "L%d:\n"
            "\tld l,(ix-5)\n\tld h,(ix-4)\n"
            "\tadd hl,hl\n\tadd hl,hl\n"
            "\tbit 7,(ix-2)\n\tjp nz, L%d\n"
            "\tld a,(ix-3)\n\tor (ix-2)\n\tjp z, L%d\n"
            "\tld de,15\n\tadd hl,de\n"
            "L%d:\n\tjp L%d\n",
            bishop, bishop_done, bishop_done, bishop_done, exit);

    mir_stream_printf(out,
            "L%d:\n\tld l,(ix-5)\n\tld h,(ix-4)\n\tjp L%d\n"
            "L%d:\n\tld hl,0\n\tjp L%d\n",
            queen, exit, rook, exit);

    mir_stream_printf(out,
            "L%d:\n"
            "\tld l,(ix-5)\n\tld h,(ix-4)\n"
            "\tadd hl,hl\n\tadd hl,hl\n"
            "\txor a\n\tsub l\n\tld l,a\n"
            "\tld a,0\n\tsbc a,h\n\tld h,a\n"
            "\tbit 7,(ix-2)\n\tjp nz, L%d\n"
            "\tld a,(ix-3)\n\tor (ix-2)\n\tjp z, L%d\n"
            "\tld de,80\n\tor a\n\tsbc hl,de\n"
            "L%d:\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            king, king_done, king_done, king_done, exit);
}

static void mir_emit_slider_attack(
    MirStream *out, const struct MirSliderAttack *plan)
{
    const char *board_name =
        asm_name_for(sym_asm_name(plan->board));
    int loop = new_label();
    int distance_ok = new_label();
    int occupied = new_label();
    int lowercase = new_label();
    int compare_side = new_label();
    int side_ready = new_label();
    int matching_side = new_label();
    int folded = new_label();
    int hit = new_label();
    int advance = new_label();
    int miss = new_label();
    int exit = new_label();

    if ((plan->board->storage == SC_EXTERN ||
         plan->board->needs_extrn) &&
        mir_extrn_should_emit(plan->board))
        mir_stream_printf(out, "\textrn %s\n", board_name);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n\tadd hl,de\n"
            "\tld c,l\n\tld b,h\n"
            "L%d:\n"
            "\tbit 7,b\n\tjp nz, L%d\n"
            "\tld a,b\n\tor a\n\tjp nz, L%d\n"
            "\tld a,c\n\tcp 64\n\tjp nc, L%d\n"
            "\tld l,c\n\tld h,b\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tor a\n\tsbc hl,de\n"
            "\tld a,l\n\tand 7\n\tld (ix-1),a\n"
            "\tld a,c\n\tand 7\n\tsub (ix-1)\n"
            "\tjp nc, L%d\n\tneg\n"
            "L%d:\n\tcp 2\n\tjp nc, L%d\n"
            "\tld hl,%s\n\tld e,c\n\tld d,b\n\tadd hl,de\n"
            "\tld a,(hl)\n\tcp 46\n\tjp nz, L%d\n\tjp L%d\n",
            plan->square_offset, plan->square_offset + 1,
            plan->direction_offset, plan->direction_offset + 1,
            loop,
            miss, miss, miss,
            plan->direction_offset, plan->direction_offset + 1,
            distance_ok, distance_ok, miss,
            board_name,
            occupied, advance);
    mir_stream_printf(out,
            "L%d:\n\tld (ix-2),a\n"
            "\tcp 65\n\tjp c, L%d\n\tcp 91\n\tjp nc, L%d\n"
            "\tld hl,1\n\tjp L%d\n"
            "L%d:\n\tcp 97\n\tjp c, L%d\n"
            "\tcp 123\n\tjp nc, L%d\n"
            "\tld hl,65535\n\tjp L%d\n"
            "L%d:\n\tld hl,0\n"
            "L%d:\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tor a\n\tsbc hl,de\n\tjp nz, L%d\n"
            "L%d:\n"
            "\tld a,(ix-2)\n\tcp 97\n\tjp c, L%d\n"
            "\tcp 123\n\tjp nc, L%d\n\tsub 32\n"
            "L%d:\n"
            "\tcp (ix%+d)\n\tjp z, L%d\n"
            "\tcp (ix%+d)\n\tjp z, L%d\n\tjp L%d\n",
            occupied,
            lowercase, lowercase,
            side_ready,
            lowercase, compare_side, compare_side,
            side_ready,
            compare_side,
            side_ready,
            plan->side_offset, plan->side_offset + 1,
            miss,
            matching_side,
            folded, folded, folded,
            plan->first_piece_offset, hit,
            plan->second_piece_offset, hit, miss);
    mir_stream_printf(out,
            "L%d:\n\tld hl,1\n\tjp L%d\n"
            "L%d:\n"
            "\tld l,c\n\tld h,b\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n\tadd hl,de\n"
            "\tld c,l\n\tld b,h\n\tjp L%d\n"
            "L%d:\n\tld hl,0\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            hit, exit,
            advance,
            plan->direction_offset, plan->direction_offset + 1,
            loop,
            miss,
            exit);
}

static void mir_emit_frame_long_load(
    MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
            offset, offset + 1, offset + 2, offset + 3);
}

static void mir_emit_frame_long_store(
    MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "\tld (ix%+d),e\n\tld (ix%+d),d\n",
            offset, offset + 1, offset + 2, offset + 3);
}

static void mir_emit_pointer_long_load(
    MirStream *out, int pointer_offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n\tinc hl\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld l,c\n\tld h,b\n",
            pointer_offset, pointer_offset + 1);
}

static void mir_emit_pointer_long_store(
    MirStream *out, int pointer_offset)
{
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            pointer_offset, pointer_offset + 1);
    mir_stream_puts("\tpop bc\n\tld (hl),c\n\tinc hl\n\tld (hl),b\n\tinc hl\n"
          "\tpop bc\n\tld (hl),c\n\tinc hl\n\tld (hl),b\n", out);
}

static void mir_emit_frame_pointer_adjust(
    MirStream *out, int offset, int amount)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tld de,%d\n",
            offset, offset + 1,
            amount < 0 ? -amount : amount);
    if (amount < 0)
        mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    else
        mir_stream_puts("\tadd hl,de\n", out);
    mir_stream_printf(out, "\tld (ix%+d),l\n\tld (ix%+d),h\n",
            offset, offset + 1);
}

static void mir_emit_symbol_extrn(MirStream *out, struct Sym *symbol)
{
    const char *name = asm_name_for(sym_asm_name(symbol));

    if ((symbol->storage == SC_EXTERN || symbol->needs_extrn) &&
        mir_extrn_should_emit(symbol))
        mir_stream_printf(out, "\textrn %s\n", name);
}

static void mir_emit_symbol_call(MirStream *out, struct Sym *symbol)
{
    const char *name = asm_name_for(sym_asm_name(symbol));

    mir_emit_symbol_extrn(out, symbol);
    mir_stream_printf(out, "\tcall %s\n", name);
}

static void mir_emit_resolved_symbol_call(
    MirStream *out, struct Sym *symbol, const char *name)
{
    if ((symbol == NULL || symbol->needs_extrn) &&
        mir_extrn_should_emit_name(name))
        mir_stream_printf(out, "\textrn %s\n", name);
    mir_stream_printf(out, "\tcall %s\n", name);
}

static void mir_emit_copy_frame_word(
    MirStream *out, int destination_offset, int source_offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (ix%+d),l\n\tld (ix%+d),h\n",
            source_offset, source_offset + 1,
            destination_offset, destination_offset + 1);
}

static void mir_emit_sign_extend_hl_to_de(MirStream *out)
{
    mir_stream_puts("\tld a,h\n\trlca\n\tsbc a,a\n\tld d,a\n\tld e,a\n",
          out);
}

static void mir_emit_signed_long_constant_branch(
    MirStream *out, int offset, unsigned long constant_bits,
    int branch_if_less, int true_label, int false_label)
{
    int byte;

    mir_stream_printf(out,
            "\tld a,(ix%+d)\n\txor 128\n\tcp %lu\n",
            offset + 3,
            ((constant_bits >> 24) & 0xffUL) ^ 0x80UL);
    if (branch_if_less)
        mir_stream_printf(out, "\tjp c, L%d\n\tjp nz, L%d\n",
                true_label, false_label);
    else
        mir_stream_printf(out, "\tjp c, L%d\n\tjp nz, L%d\n",
                false_label, true_label);
    for (byte = 2; byte >= 0; --byte) {
        mir_stream_printf(out,
                "\tld a,(ix%+d)\n\tcp %lu\n",
                offset + byte,
                (constant_bits >> (byte * 8)) & 0xffUL);
        if (byte != 0) {
            if (branch_if_less)
                mir_stream_printf(out, "\tjp c, L%d\n\tjp nz, L%d\n",
                        true_label, false_label);
            else
                mir_stream_printf(out, "\tjp c, L%d\n\tjp nz, L%d\n",
                        false_label, true_label);
        } else if (branch_if_less) {
            mir_stream_printf(out, "\tjp c, L%d\n\tjp L%d\n",
                    true_label, false_label);
        } else {
            mir_stream_printf(out, "\tjp c, L%d\n\tjp nz, L%d\n\tjp L%d\n",
                    false_label, true_label, false_label);
        }
    }
}

static void mir_emit_long_clamp(
    MirStream *out, const struct MirLongClamp *plan)
{
    int below_maximum = new_label();
    int above_minimum = new_label();
    int maximum = new_label();
    int minimum = new_label();
    int done = new_label();

    mir_emit_signed_long_constant_branch(
        out, plan->parameter_offset, plan->maximum_bits,
        0, maximum, below_maximum);
    mir_stream_printf(out, "L%d:\n", below_maximum);
    mir_emit_signed_long_constant_branch(
        out, plan->parameter_offset, plan->minimum_bits,
        1, minimum, above_minimum);
    mir_stream_printf(out,
            "L%d:\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            above_minimum,
            plan->parameter_offset, plan->parameter_offset + 1,
            done,
            maximum, plan->maximum_result, done,
            minimum, plan->minimum_result,
            done);
}

static void mir_emit_long_q8_value(
    MirStream *out, int offset, const struct MirLongQ8Spec *conversion)
{
    int below_upper = new_label();
    int above_lower = new_label();
    int nonnegative = new_label();
    int no_carry = new_label();
    int upper = new_label();
    int lower = new_label();
    int done = new_label();

    mir_emit_signed_long_constant_branch(
        out, offset, conversion->upper_bits,
        0, upper, below_upper);
    mir_stream_printf(out, "L%d:\n", below_upper);
    mir_emit_signed_long_constant_branch(
        out, offset, conversion->lower_bits,
        1, lower, above_lower);
    mir_stream_printf(out,
            "L%d:\n\tbit 7,(ix%+d)\n\tjp z, L%d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld e,(ix%+d)\n"
            "\tld bc,%d\n\tadd hl,bc\n\tjp nc, L%d\n\tinc e\n"
            "L%d:\n\tld l,h\n\tld h,e\n\tjp L%d\n"
            "L%d:\n\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n"
            "L%d:\n",
            above_lower, offset + 3, nonnegative,
            offset, offset + 1, offset + 2,
            (1 << conversion->shift) - 1, no_carry,
            no_carry, done,
            nonnegative, offset + conversion->shift / 8,
            offset + conversion->shift / 8 + 1, done,
            upper, conversion->upper_result, done,
            lower, conversion->lower_result,
            done);
}

static void mir_emit_long_q8(
    MirStream *out, const struct MirLongQ8 *plan)
{
    mir_emit_long_q8_value(
        out, plan->parameter_offset, &plan->conversion);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_pointer_word_and_advance(
    MirStream *out, int pointer_offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n\tinc hl\n"
            "\tld (ix%+d),l\n\tld (ix%+d),h\n"
            "\tld l,c\n\tld h,b\n",
            pointer_offset, pointer_offset + 1,
            pointer_offset, pointer_offset + 1);
}

static void mir_emit_signed_pointer_product(
    MirStream *out, int left_pointer_offset, int right_pointer_offset)
{
    mir_emit_pointer_word_and_advance(out, left_pointer_offset);
    mir_emit_sign_extend_hl_to_de(out);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_pointer_word_and_advance(out, right_pointer_offset);
    mir_emit_sign_extend_hl_to_de(out);
    mir_stream_puts("\tpop bc\n\tpop de\n", out);
    mir_emit_runtime_call(out, "__m1s");
}

static void mir_emit_q8_accumulate(
    MirStream *out, int matrix_pointer_offset, int scalar_offset,
    int output_pointer_offset, struct Sym *q16_function,
    struct Sym *clamp_function)
{
    mir_emit_pointer_word_and_advance(out, matrix_pointer_offset);
    mir_emit_sign_extend_hl_to_de(out);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            scalar_offset, scalar_offset + 1);
    mir_emit_sign_extend_hl_to_de(out);
    mir_stream_puts("\tpop bc\n\tpop de\n", out);
    mir_emit_runtime_call(out, "__m1q");
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_symbol_call(out, q16_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);

    mir_emit_sign_extend_hl_to_de(out);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
            "\tld l,c\n\tld h,b\n",
            output_pointer_offset, output_pointer_offset + 1);
    mir_emit_sign_extend_hl_to_de(out);
    mir_stream_puts("\tpop bc\n\tadd hl,bc\n\tex de,hl\n"
          "\tpop bc\n\tadc hl,bc\n\tex de,hl\n"
          "\tpush de\n\tpush hl\n", out);
    mir_emit_symbol_call(out, clamp_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tex de,hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (hl),e\n\tinc hl\n\tld (hl),d\n\tinc hl\n"
            "\tld (ix%+d),l\n\tld (ix%+d),h\n",
            output_pointer_offset, output_pointer_offset + 1,
            output_pointer_offset, output_pointer_offset + 1);
}

static void mir_emit_fixed_q8_multiply(
    MirStream *out, const struct MirFixedQ8Multiply *plan)
{
    enum {
        MATRIX_POINTER = -2,
        INPUT_POINTER = -4,
        OUTPUT_BASE = -6,
        SCALAR_VALUE = -8,
        OUTPUT_POINTER = -10,
        OUTER_COUNT = -11,
        INNER_COUNT = -12
    };
    int zero_loop = new_label();
    int outer_loop = new_label();
    int inner_loop = new_label();

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n",
            plan->matrix_offset, plan->matrix_offset + 1,
            MATRIX_POINTER, MATRIX_POINTER + 1);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n",
            plan->input_offset, plan->input_offset + 1,
            INPUT_POINTER, INPUT_POINTER + 1);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tld b,%d\n\txor a\n"
            "L%d:\n\tld (hl),a\n\tinc hl\n\tdjnz L%d\n"
            "\tld (ix%d),%d\n"
            "L%d:\n",
            plan->output_offset, plan->output_offset + 1,
            OUTPUT_BASE, OUTPUT_BASE + 1,
            plan->columns * 2,
            zero_loop, zero_loop,
            OUTER_COUNT, plan->rows,
            outer_loop);
    mir_emit_pointer_word_and_advance(out, INPUT_POINTER);
    mir_stream_printf(out,
            "\tld (ix%d),l\n\tld (ix%d),h\n",
            SCALAR_VALUE, SCALAR_VALUE + 1);
    mir_emit_copy_frame_word(out, OUTPUT_POINTER, OUTPUT_BASE);
    mir_stream_printf(out,
            "\tld (ix%d),%d\n"
            "L%d:\n",
            INNER_COUNT, plan->columns,
            inner_loop);
    mir_emit_q8_accumulate(
        out, MATRIX_POINTER, SCALAR_VALUE, OUTPUT_POINTER,
        plan->q16_function, plan->clamp_function);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n"
            "\tdec (ix%d)\n\tjp nz, L%d\n"
            "\tld sp,ix\n\tpop ix\n\tret\n",
            INNER_COUNT, inner_loop,
            OUTER_COUNT, outer_loop);
}

static void mir_emit_attention_score(
    MirStream *out, const struct MirAttentionScore *plan)
{
    enum {
        QUERY_POINTER = -2,
        KEY_POINTER = -4,
        ACCUMULATOR = -8,
        LOOP_COUNT = -9
    };
    int loop = new_label();

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n",
            plan->query_offset, plan->query_offset + 1,
            QUERY_POINTER, QUERY_POINTER + 1);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tld hl,0\n\tld de,0\n",
            plan->key_offset, plan->key_offset + 1,
            KEY_POINTER, KEY_POINTER + 1);
    mir_emit_frame_long_store(out, ACCUMULATOR);
    mir_stream_printf(out,
            "\tld (ix%d),%d\n"
            "L%d:\n",
            LOOP_COUNT, plan->dimension, loop);
    mir_emit_signed_pointer_product(
        out, QUERY_POINTER, KEY_POINTER);
    mir_stream_printf(out,
            "\tld c,(ix%d)\n\tld b,(ix%d)\n\tadd hl,bc\n"
            "\tex de,hl\n"
            "\tld c,(ix%d)\n\tld b,(ix%d)\n\tadc hl,bc\n"
            "\tex de,hl\n",
            ACCUMULATOR, ACCUMULATOR + 1,
            ACCUMULATOR + 2, ACCUMULATOR + 3);
    mir_emit_frame_long_store(out, ACCUMULATOR);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n",
            LOOP_COUNT, loop);
    mir_emit_long_q8_value(
        out, ACCUMULATOR, &plan->conversion);
    mir_emit_scalar_shift_by_constant(
        out, TOK_SHR, 0, plan->output_shift);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_symbol_address_to_frame(
    MirStream *out, struct Sym *symbol, int byte_offset, int frame_offset)
{
    const char *name = asm_name_for(sym_asm_name(symbol));

    mir_emit_symbol_extrn(out, symbol);
    if (byte_offset == 0)
        mir_stream_printf(out, "\tld hl,%s\n", name);
    else
        mir_stream_printf(out, "\tld hl,%s+%d\n", name, byte_offset);
    mir_stream_printf(out,
            "\tld (ix%+d),l\n\tld (ix%+d),h\n",
            frame_offset, frame_offset + 1);
}

static void mir_emit_project_all_qkv(
    MirStream *out, const struct MirProjectAllQkv *plan)
{
    enum {
        INPUT_POINTER = -2,
        QUERY_OUTPUT = -4,
        KEY_OUTPUT = -6,
        VALUE_OUTPUT = -8,
        QUERY_MATRIX = -10,
        KEY_MATRIX = -12,
        VALUE_MATRIX = -14,
        SCALAR_VALUE = -16,
        QUERY_CURSOR = -18,
        KEY_CURSOR = -20,
        VALUE_CURSOR = -22,
        ROW_COUNT = -23,
        INPUT_COUNT = -24,
        OUTPUT_COUNT = -25
    };
    const char *workspace_name =
        asm_name_for(sym_asm_name(plan->workspace));
    int zero_bytes = plan->zero_elements * 2;
    int row_loop = new_label();
    int input_loop = new_label();
    int output_loop = new_label();

    mir_emit_symbol_extrn(out, plan->workspace);
    mir_stream_printf(out, "\tld hl,%s\n\tld (hl),0\n", workspace_name);
    if (zero_bytes > 1)
        mir_stream_printf(out,
                "\tld de,%s+1\n\tld bc,%d\n\tldir\n",
                workspace_name, zero_bytes - 1);

    mir_emit_symbol_address_to_frame(
        out, plan->embeddings, 0, INPUT_POINTER);
    mir_emit_symbol_address_to_frame(
        out, plan->workspace, plan->query_offset * 2, QUERY_OUTPUT);
    mir_emit_symbol_address_to_frame(
        out, plan->workspace, plan->key_offset * 2, KEY_OUTPUT);
    mir_emit_symbol_address_to_frame(
        out, plan->workspace, plan->value_offset * 2, VALUE_OUTPUT);
    mir_stream_printf(out,
            "\tld (ix%d),%d\n"
            "L%d:\n",
            ROW_COUNT, plan->rows, row_loop);
    mir_emit_symbol_address_to_frame(
        out, plan->query_weights, 0, QUERY_MATRIX);
    mir_emit_symbol_address_to_frame(
        out, plan->key_weights, 0, KEY_MATRIX);
    mir_emit_symbol_address_to_frame(
        out, plan->value_weights, 0, VALUE_MATRIX);
    mir_stream_printf(out,
            "\tld (ix%d),%d\n"
            "L%d:\n",
            INPUT_COUNT, plan->dimension, input_loop);
    mir_emit_pointer_word_and_advance(out, INPUT_POINTER);
    mir_stream_printf(out,
            "\tld (ix%d),l\n\tld (ix%d),h\n",
            SCALAR_VALUE, SCALAR_VALUE + 1);
    mir_emit_copy_frame_word(out, QUERY_CURSOR, QUERY_OUTPUT);
    mir_emit_copy_frame_word(out, KEY_CURSOR, KEY_OUTPUT);
    mir_emit_copy_frame_word(out, VALUE_CURSOR, VALUE_OUTPUT);
    mir_stream_printf(out,
            "\tld (ix%d),%d\n"
            "L%d:\n",
            OUTPUT_COUNT, plan->dimension, output_loop);
    mir_emit_q8_accumulate(
        out, QUERY_MATRIX, SCALAR_VALUE, QUERY_CURSOR,
        plan->q16_function, plan->clamp_function);
    mir_emit_q8_accumulate(
        out, KEY_MATRIX, SCALAR_VALUE, KEY_CURSOR,
        plan->q16_function, plan->clamp_function);
    mir_emit_q8_accumulate(
        out, VALUE_MATRIX, SCALAR_VALUE, VALUE_CURSOR,
        plan->q16_function, plan->clamp_function);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n"
            "\tdec (ix%d)\n\tjp nz, L%d\n",
            OUTPUT_COUNT, output_loop,
            INPUT_COUNT, input_loop);
    mir_emit_frame_pointer_adjust(
        out, QUERY_OUTPUT, plan->dimension * 2);
    mir_emit_frame_pointer_adjust(
        out, KEY_OUTPUT, plan->dimension * 2);
    mir_emit_frame_pointer_adjust(
        out, VALUE_OUTPUT, plan->dimension * 2);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n"
            "\tld sp,ix\n\tpop ix\n\tret\n",
            ROW_COUNT, row_loop);
}

static void mir_emit_assign_pre_offset(
    MirStream *out, const struct MirAssignPre *plan,
    int symbol_pointer_offset, int index_offset, int scale)
{
    int indexed = new_label();
    int ready = new_label();

    mir_stream_printf(out,
            "\tld l,(ix%d)\n\tld h,(ix%d)\n"
            "\tld de,%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld hl,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nz, L%d\n"
            "\tld hl,0\n\tjp L%d\n"
            "L%d:\n\tld l,(ix%d)\n\tld h,(ix%d)\n"
            "L%d:\n",
            symbol_pointer_offset, symbol_pointer_offset + 1,
            plan->kind_offset,
            plan->scalar_kind,
            indexed,
            ready,
            indexed, index_offset, index_offset + 1,
            ready);
    if (scale == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    mir_stream_puts("\tex de,hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%d)\n\tld h,(ix%d)\n"
            "\tld bc,%d\n\tadd hl,bc\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
            "\tld h,b\n\tld l,c\n\tadd hl,de\n",
            symbol_pointer_offset, symbol_pointer_offset + 1,
            plan->base_offset);
}

static void mir_emit_assign_pre(
    MirStream *out, const struct MirAssignPre *plan)
{
    enum {
        SYMBOL_INDEX = -2,
        RESOLVED_INDEX = -4,
        VALUE = -6,
        SYMBOL_POINTER = -8,
        MEMORY_OFFSET = -10
    };
    const char *symbols_name =
        asm_name_for(sym_asm_name(plan->symbols));
    const char *memory_name =
        asm_name_for(sym_asm_name(plan->memory));
    const char *capacity_name =
        asm_name_for(sym_asm_name(plan->memory_capacity));
    int zero_index = new_label();
    int index_ready = new_label();
    int word_store = new_label();
    int bounds_error = new_label();
    int done = new_label();

    mir_emit_symbol_extrn(out, plan->symbols);
    mir_emit_symbol_extrn(out, plan->memory);
    mir_emit_symbol_extrn(out, plan->memory_capacity);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tbit 7,h\n\tjp nz, L%d\n\tpush hl\n",
            plan->symbol_index_offset,
            plan->symbol_index_offset + 1,
            SYMBOL_INDEX, SYMBOL_INDEX + 1,
            plan->expression_index_offset,
            plan->expression_index_offset + 1,
            zero_index);
    mir_emit_symbol_call(out, plan->evaluate_function);
    mir_stream_printf(out,
            "\tpop bc\n\tjp L%d\n"
            "L%d:\n\tld hl,0\n"
            "L%d:\n\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            index_ready,
            zero_index,
            index_ready, RESOLVED_INDEX, RESOLVED_INDEX + 1,
            plan->rhs_expression_offset,
            plan->rhs_expression_offset + 1);
    mir_emit_symbol_call(out, plan->evaluate_function);
    mir_stream_printf(out,
            "\tpop bc\n\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tld l,(ix%d)\n\tld h,(ix%d)\n",
            VALUE, VALUE + 1,
            SYMBOL_INDEX, SYMBOL_INDEX + 1);
    mir_emit_mul_hl_const(out, (unsigned long)plan->symbol_stride);
    mir_stream_printf(out,
            "\tex de,hl\n\tld hl,(%s)\n\tadd hl,de\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tld de,%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld hl,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nz, L%d\n",
            symbols_name,
            SYMBOL_POINTER, SYMBOL_POINTER + 1,
            plan->type_offset,
            plan->byte_type,
            word_store);

    mir_emit_assign_pre_offset(
        out, plan, SYMBOL_POINTER, RESOLVED_INDEX, 1);
    mir_stream_printf(out,
            "\tex de,hl\n\tld hl,(%s)\n\tadd hl,de\n"
            "\tld a,(ix%d)\n\tld (hl),a\n\tjp L%d\n"
            "L%d:\n",
            memory_name, VALUE, done,
            word_store);

    mir_emit_assign_pre_offset(
        out, plan, SYMBOL_POINTER, RESOLVED_INDEX, plan->cell_size);
    mir_stream_printf(out,
            "\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tbit 7,h\n\tjp nz, L%d\n"
            "\tinc hl\n\tld de,(%s)\n"
            "\tor a\n\tsbc hl,de\n\tjp nc, L%d\n"
            "\tld e,(ix%d)\n\tld d,(ix%d)\n"
            "\tld hl,(%s)\n\tadd hl,de\n"
            "\tld e,(ix%d)\n\tld d,(ix%d)\n"
            "\tld (hl),e\n\tinc hl\n\tld (hl),d\n"
            "\tjp L%d\n"
            "L%d:\n\tld hl,S%d\n\tpush hl\n",
            MEMORY_OFFSET, MEMORY_OFFSET + 1,
            bounds_error,
            capacity_name,
            bounds_error,
            MEMORY_OFFSET, MEMORY_OFFSET + 1,
            memory_name,
            VALUE, VALUE + 1,
            done,
            bounds_error, plan->bounds_string);
    mir_emit_symbol_call(out, plan->die_function);
    mir_stream_printf(out,
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            done);
}

enum MirForthFrameOffset {
    MIR_FORTH_CODE = -2,
    MIR_FORTH_MEMORY = -4,
    MIR_FORTH_STACK = -6,
    MIR_FORTH_CALL_STACK = -8,
    MIR_FORTH_LOOP_INDEX = -10,
    MIR_FORTH_LOOP_LIMIT = -12,
    MIR_FORTH_LOOP_PREVIOUS = -14,
    MIR_FORTH_SP = -16,
    MIR_FORTH_CRP = -18,
    MIR_FORTH_LRP = -20,
    MIR_FORTH_LOOP_TOP = -22,
    MIR_FORTH_MEMORY_CAPACITY = -24,
    MIR_FORTH_TEMP_A = -26,
    MIR_FORTH_TEMP_B = -28,
    MIR_FORTH_TEMP_C = -30,
    MIR_FORTH_TEMP_N = -32
};

static void mir_emit_forth_frame_load(MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld l,(ix%d)\n\tld h,(ix%d)\n",
            offset, offset + 1);
}

static void mir_emit_forth_frame_store(MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld (ix%d),l\n\tld (ix%d),h\n",
            offset, offset + 1);
}

static void mir_emit_forth_state_member_load(
    MirStream *out, const char *state_name, int member_offset, int frame_offset)
{
    mir_stream_printf(out,
            "\tld hl,(%s)\n\tld de,%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld (ix%d),e\n\tld (ix%d),d\n",
            state_name, member_offset,
            frame_offset, frame_offset + 1);
}

static void mir_emit_forth_state_member_store(
    MirStream *out, const char *state_name, int member_offset, int frame_offset)
{
    mir_stream_printf(out,
            "\tld hl,(%s)\n\tld de,%d\n\tadd hl,de\n"
            "\tld c,(ix%d)\n\tld b,(ix%d)\n"
            "\tld (hl),c\n\tinc hl\n\tld (hl),b\n",
            state_name, member_offset,
            frame_offset, frame_offset + 1);
}

static void mir_emit_forth_sync_state(
    MirStream *out, const struct MirForthRun *plan, const char *state_name)
{
    mir_emit_forth_state_member_store(
        out, state_name, plan->stack_pointer_offset, MIR_FORTH_SP);
    mir_emit_forth_state_member_store(
        out, state_name, plan->call_pointer_offset, MIR_FORTH_CRP);
    mir_emit_forth_state_member_store(
        out, state_name, plan->loop_pointer_offset, MIR_FORTH_LRP);
    mir_emit_forth_state_member_store(
        out, state_name, plan->loop_top_offset, MIR_FORTH_LOOP_TOP);
}

static void mir_emit_forth_reload_state(
    MirStream *out, const struct MirForthRun *plan, const char *state_name)
{
    mir_emit_forth_state_member_load(
        out, state_name, plan->stack_pointer_offset, MIR_FORTH_SP);
    mir_emit_forth_state_member_load(
        out, state_name, plan->call_pointer_offset, MIR_FORTH_CRP);
    mir_emit_forth_state_member_load(
        out, state_name, plan->loop_pointer_offset, MIR_FORTH_LRP);
    mir_emit_forth_state_member_load(
        out, state_name, plan->loop_top_offset, MIR_FORTH_LOOP_TOP);
}

static void mir_emit_forth_stack_address(
    MirStream *out, int stack_offset, int stack_pointer_offset, int adjustment)
{
    int step;

    mir_emit_forth_frame_load(out, stack_pointer_offset);
    for (step = 0; step < adjustment; ++step)
        mir_stream_puts("\tinc hl\n", out);
    for (step = 0; step > adjustment; --step)
        mir_stream_puts("\tdec hl\n", out);
    mir_stream_puts("\tadd hl,hl\n\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, stack_offset);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_emit_forth_push_hl(MirStream *out)
{
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_stack_address(
        out, MIR_FORTH_STACK, MIR_FORTH_SP, 0);
    mir_stream_puts("\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_SP);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, MIR_FORTH_SP);
}

static void mir_emit_forth_pop_hl(MirStream *out)
{
    mir_emit_forth_frame_load(out, MIR_FORTH_SP);
    mir_stream_puts("\tdec hl\n", out);
    mir_emit_forth_frame_store(out, MIR_FORTH_SP);
    mir_stream_puts("\tadd hl,hl\n\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_STACK);
    mir_stream_puts("\tadd hl,de\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tex de,hl\n", out);
}

static void mir_emit_forth_load_stack_hl(MirStream *out, int adjustment)
{
    mir_emit_forth_stack_address(
        out, MIR_FORTH_STACK, MIR_FORTH_SP, adjustment);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
}

static void mir_emit_forth_store_stack_hl(MirStream *out, int adjustment)
{
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_stack_address(
        out, MIR_FORTH_STACK, MIR_FORTH_SP, adjustment);
    mir_stream_puts("\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
}

static void mir_emit_forth_local_array_address(
    MirStream *out, int base_offset, int index_offset, int adjustment)
{
    mir_emit_forth_frame_load(out, index_offset);
    if (adjustment < 0)
        while (adjustment++ < 0)
            mir_stream_puts("\tdec hl\n", out);
    else
        while (adjustment-- > 0)
            mir_stream_puts("\tinc hl\n", out);
    mir_stream_puts("\tadd hl,hl\n\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, base_offset);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_emit_forth_instruction_argument(
    MirStream *out, const struct MirForthRun *plan)
{
    mir_stream_printf(out,
            "\tld l,(iy%+d)\n\tld h,(iy%+d)\n",
            plan->instruction_argument_offset,
            plan->instruction_argument_offset + 1);
}

static void mir_emit_forth_signed_less_branch(
    MirStream *out, int true_label, int false_label)
{
    int high_equal = new_label();

    mir_stream_printf(out,
            "\tld a,h\n\txor 128\n\tld b,a\n"
            "\tld a,d\n\txor 128\n\tcp b\n"
            "\tjp z, L%d\n\tjp c, L%d\n\tjp L%d\n"
            "L%d:\n\tld a,l\n\tcp e\n\tjp c, L%d\n\tjp L%d\n",
            high_equal, false_label, true_label,
            high_equal, true_label, false_label);
}

static void mir_emit_forth_die(
    MirStream *out, const struct MirForthRun *plan, int string_id)
{
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", string_id);
    mir_emit_symbol_call(out, plan->die_function);
}

static void mir_emit_forth_run(
    MirStream *out, const struct MirForthRun *plan)
{
    const char *state_name =
        asm_name_for(sym_asm_name(plan->state));
    int cases[42];
    int dispatch = new_label();
    int table = new_label();
    int next = new_label();
    int set_pc = new_label();
    int sync_exit = new_label();
    int return_stack_error = new_label();
    int loop_stack_error = new_label();
    int bad_address = new_label();
    int bad_fill = new_label();
    int bad_opcode = new_label();
    int item;

    for (item = 0; item < plan->opcode_count; ++item)
        cases[item] = new_label();
    mir_emit_symbol_extrn(out, plan->state);
    mir_emit_symbol_extrn(out, plan->print_function);
    mir_emit_symbol_extrn(out, plan->putchar_function);

    mir_emit_forth_state_member_load(
        out, state_name, plan->code_offset, MIR_FORTH_CODE);
    mir_emit_forth_state_member_load(
        out, state_name, plan->memory_offset, MIR_FORTH_MEMORY);
    mir_emit_forth_state_member_load(
        out, state_name, plan->stack_offset, MIR_FORTH_STACK);
    mir_emit_forth_state_member_load(
        out, state_name, plan->call_stack_offset, MIR_FORTH_CALL_STACK);
    mir_emit_forth_state_member_load(
        out, state_name, plan->loop_index_offset, MIR_FORTH_LOOP_INDEX);
    mir_emit_forth_state_member_load(
        out, state_name, plan->loop_limit_offset, MIR_FORTH_LOOP_LIMIT);
    mir_emit_forth_state_member_load(
        out, state_name, plan->loop_previous_offset,
        MIR_FORTH_LOOP_PREVIOUS);
    mir_emit_forth_state_member_load(
        out, state_name, plan->stack_pointer_offset, MIR_FORTH_SP);
    mir_emit_forth_state_member_load(
        out, state_name, plan->call_pointer_offset, MIR_FORTH_CRP);
    mir_emit_forth_state_member_load(
        out, state_name, plan->loop_pointer_offset, MIR_FORTH_LRP);
    mir_emit_forth_state_member_load(
        out, state_name, plan->loop_top_offset, MIR_FORTH_LOOP_TOP);
    mir_emit_forth_state_member_load(
        out, state_name, plan->memory_capacity_offset,
        MIR_FORTH_MEMORY_CAPACITY);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tadd hl,hl\n\tadd hl,hl\n\tex de,hl\n",
            plan->pc_offset + 2, plan->pc_offset + 3);
    mir_emit_forth_frame_load(out, MIR_FORTH_CODE);
    mir_stream_printf(out,
            "\tadd hl,de\n\tpush hl\n\tpop iy\n"
            "L%d:\n"
            "\tld a,(iy%+d)\n\tor a\n\tjp nz, L%d\n"
            "\tld a,(iy%+d)\n\tcp %d\n\tjp nc, L%d\n"
            "\tld l,a\n\tld h,0\n\tadd hl,hl\n"
            "\tld de,L%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tex de,hl\n\tjp (hl)\n"
            "L%d:\n",
            dispatch,
            plan->instruction_op_offset + 1, bad_opcode,
            plan->instruction_op_offset, plan->opcode_count, bad_opcode,
            table, table);
    for (item = 0; item < plan->opcode_count; ++item)
        mir_stream_printf(out, "\tdw L%d\n", cases[item]);

    /* OP_HALT */
    mir_stream_printf(out, "L%d:\n\tjp L%d\n", cases[0], sync_exit);

    /* OP_LIT */
    mir_stream_printf(out, "L%d:\n", cases[1]);
    mir_emit_forth_instruction_argument(out, plan);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_CALL */
    mir_stream_printf(out, "L%d:\n", cases[2]);
    mir_emit_forth_frame_load(out, MIR_FORTH_CRP);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nc, L%d\n"
            "\tpush iy\n\tpop hl\n\tld de,%d\n\tadd hl,de\n\tpush hl\n",
            plan->return_stack_limit, return_stack_error,
            plan->instruction_size);
    mir_emit_forth_local_array_address(
        out, MIR_FORTH_CALL_STACK, MIR_FORTH_CRP, 0);
    mir_stream_puts("\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_CRP);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, MIR_FORTH_CRP);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_printf(out, "\tjp L%d\n", set_pc);

    /* OP_RET */
    mir_stream_printf(out, "L%d:\n", cases[3]);
    mir_emit_forth_frame_load(out, MIR_FORTH_CRP);
    mir_stream_printf(out,
            "\tld a,h\n\tor a\n\tjp m, L%d\n"
            "\tor l\n\tjp z, L%d\n\tdec hl\n",
            sync_exit, sync_exit);
    mir_emit_forth_frame_store(out, MIR_FORTH_CRP);
    mir_emit_forth_local_array_address(
        out, MIR_FORTH_CALL_STACK, MIR_FORTH_CRP, 0);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tpush de\n\tpop iy\n", out);
    mir_stream_printf(out, "\tjp L%d\n", dispatch);

    /* OP_JMP */
    mir_stream_printf(out, "L%d:\n", cases[4]);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_printf(out, "\tjp L%d\n", set_pc);

    /* OP_JZ */
    mir_stream_printf(out, "L%d:\n", cases[5]);
    mir_emit_forth_pop_hl(out);
    mir_stream_printf(out,
            "\tld a,h\n\tor l\n\tjp nz, L%d\n",
            next);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_printf(out, "\tjp L%d\n", set_pc);

    /* OP_DO */
    mir_stream_printf(out, "L%d:\n", cases[6]);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
    mir_emit_forth_frame_load(out, MIR_FORTH_LRP);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n\tjp nc, L%d\n",
            plan->return_stack_limit, loop_stack_error);
    mir_emit_forth_local_array_address(
        out, MIR_FORTH_LOOP_INDEX, MIR_FORTH_LRP, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_emit_forth_local_array_address(
        out, MIR_FORTH_LOOP_LIMIT, MIR_FORTH_LRP, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_emit_forth_local_array_address(
        out, MIR_FORTH_LOOP_PREVIOUS, MIR_FORTH_LRP, 0);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_LOOP_TOP);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_emit_forth_frame_load(out, MIR_FORTH_LRP);
    mir_emit_forth_frame_store(out, MIR_FORTH_LOOP_TOP);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, MIR_FORTH_LRP);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_LOOP */
    {
        int loop_continue = new_label();
        int loop_done = new_label();

        mir_stream_printf(out, "L%d:\n", cases[7]);
        mir_emit_forth_frame_load(out, MIR_FORTH_LRP);
        mir_stream_puts("\tdec hl\n", out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
        mir_emit_forth_local_array_address(
            out, MIR_FORTH_LOOP_INDEX, MIR_FORTH_TEMP_A, 0);
        mir_stream_puts("\tpush hl\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
              "\tex de,hl\n\tinc hl\n\tex de,hl\n"
              "\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n"
              "\tex de,hl\n", out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
        mir_emit_forth_local_array_address(
            out, MIR_FORTH_LOOP_LIMIT, MIR_FORTH_TEMP_A, 0);
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
        mir_emit_forth_signed_less_branch(
            out, loop_continue, loop_done);
        mir_stream_printf(out, "L%d:\n", loop_continue);
        mir_emit_forth_instruction_argument(out, plan);
        mir_stream_printf(out, "\tjp L%d\n", set_pc);
        mir_stream_printf(out, "L%d:\n", loop_done);
        mir_emit_forth_local_array_address(
            out, MIR_FORTH_LOOP_PREVIOUS, MIR_FORTH_TEMP_A, 0);
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
        mir_emit_forth_frame_store(out, MIR_FORTH_LOOP_TOP);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
        mir_emit_forth_frame_store(out, MIR_FORTH_LRP);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    /* OP_I */
    mir_stream_printf(out, "L%d:\n", cases[8]);
    mir_emit_forth_local_array_address(
        out, MIR_FORTH_LOOP_INDEX, MIR_FORTH_LOOP_TOP, 0);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_PSTR is emitted with the I/O cases below. */

    /* OP_PRIM */
    mir_stream_printf(out, "L%d:\n", cases[10]);
    mir_emit_forth_sync_state(out, plan, state_name);
    mir_stream_puts("\tpush iy\n", out);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_symbol_call(out, plan->primitive_function);
    mir_stream_puts("\tpop bc\n\tpop iy\n", out);
    mir_emit_forth_reload_state(out, plan, state_name);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_FETCHA */
    mir_stream_printf(out, "L%d:\n", cases[11]);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tex de,hl\n", out);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_STOREA */
    mir_stream_printf(out, "L%d:\n", cases[12]);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_CFETCHA */
    mir_stream_printf(out, "L%d:\n", cases[13]);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tld l,(hl)\n\tld h,0\n", out);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_CSTOREA */
    mir_stream_printf(out, "L%d:\n", cases[14]);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tpop de\n\tld (hl),e\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_ADDSTOREA */
    mir_stream_printf(out, "L%d:\n", cases[15]);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tex de,hl\n\tpop de\n\tadd hl,de\n\tpush hl\n", out);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_DUP */
    mir_stream_printf(out, "L%d:\n", cases[16]);
    mir_emit_forth_load_stack_hl(out, -1);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_DROP */
    mir_stream_printf(out, "L%d:\n", cases[17]);
    mir_emit_forth_frame_load(out, MIR_FORTH_SP);
    mir_stream_puts("\tdec hl\n", out);
    mir_emit_forth_frame_store(out, MIR_FORTH_SP);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_SWAP */
    mir_stream_printf(out, "L%d:\n", cases[18]);
    mir_emit_forth_load_stack_hl(out, -1);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
    mir_emit_forth_load_stack_hl(out, -2);
    mir_emit_forth_store_stack_hl(out, -1);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
    mir_emit_forth_store_stack_hl(out, -2);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_OVER */
    mir_stream_printf(out, "L%d:\n", cases[19]);
    mir_emit_forth_load_stack_hl(out, -2);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_ROT */
    mir_stream_printf(out, "L%d:\n", cases[20]);
    mir_emit_forth_load_stack_hl(out, -3);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
    mir_emit_forth_load_stack_hl(out, -2);
    mir_emit_forth_store_stack_hl(out, -3);
    mir_emit_forth_load_stack_hl(out, -1);
    mir_emit_forth_store_stack_hl(out, -2);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
    mir_emit_forth_store_stack_hl(out, -1);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_ADD */
    mir_stream_printf(out, "L%d:\n", cases[21]);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
    mir_emit_forth_load_stack_hl(out, -1);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
    mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
    mir_stream_puts("\tadd hl,de\n", out);
    mir_emit_forth_store_stack_hl(out, -1);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_SUB */
    mir_stream_printf(out, "L%d:\n", cases[22]);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
    mir_emit_forth_load_stack_hl(out, -1);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
    mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_emit_forth_store_stack_hl(out, -1);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_MUL */
    mir_stream_printf(out, "L%d:\n", cases[23]);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
    mir_emit_forth_load_stack_hl(out, -1);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
    mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
    mir_emit_runtime_call(out, "__mulu");
    mir_emit_forth_store_stack_hl(out, -1);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_DIVMOD */
    {
        int divide = new_label();
        int finish = new_label();

        mir_stream_printf(out, "L%d:\n", cases[24]);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
        mir_stream_printf(out,
                "\tld a,h\n\tor l\n\tjp nz, L%d\n"
                "\tld hl,0\n",
                divide);
        mir_emit_forth_push_hl(out);
        mir_stream_puts("\tld hl,0\n", out);
        mir_emit_forth_push_hl(out);
        mir_stream_printf(out, "\tjp L%d\nL%d:\n", finish, divide);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
        mir_stream_puts("\tex de,hl\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
        mir_emit_runtime_call(out, "__sdivmod");
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
        mir_stream_puts("\tex de,hl\n", out);
        mir_emit_forth_push_hl(out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
        mir_emit_forth_push_hl(out);
        mir_stream_printf(out, "L%d:\n\tjp L%d\n", finish, next);
    }

    /* OP_MOD */
    {
        int modulo = new_label();
        int finish = new_label();

        mir_stream_printf(out, "L%d:\n", cases[25]);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
        mir_stream_printf(out,
                "\tld a,h\n\tor l\n\tjp nz, L%d\n"
                "\tld hl,0\n\tjp L%d\n"
                "L%d:\n\tex de,hl\n",
                modulo, finish, modulo);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
        mir_emit_runtime_call(out, "__mods");
        mir_stream_printf(out, "L%d:\n", finish);
        mir_emit_forth_push_hl(out);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    /* OP_EQ / OP_NE */
    for (item = 26; item <= 27; ++item) {
        int true_label = new_label();
        int done_label = new_label();

        mir_stream_printf(out, "L%d:\n", cases[item]);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
        mir_emit_forth_load_stack_hl(out, -1);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
        mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
        mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
        mir_stream_printf(out,
                "\tjp %s, L%d\n\tld hl,0\n\tjp L%d\n"
                "L%d:\n\tld hl,1\nL%d:\n",
                item == 26 ? "z" : "nz", true_label,
                done_label, true_label, done_label);
        mir_emit_forth_store_stack_hl(out, -1);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    /* OP_LT / OP_GT */
    for (item = 28; item <= 29; ++item) {
        int true_label = new_label();
        int false_label = new_label();
        int done_label = new_label();

        mir_stream_printf(out, "L%d:\n", cases[item]);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
        mir_emit_forth_load_stack_hl(out, -1);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
        if (item == 28) {
            mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
        } else {
            mir_stream_puts("\tpop de\n", out);
        }
        mir_emit_forth_signed_less_branch(
            out, true_label, false_label);
        mir_stream_printf(out,
                "L%d:\n\tld hl,1\n\tjp L%d\n"
                "L%d:\n\tld hl,0\n"
                "L%d:\n",
                true_label, done_label,
                false_label, done_label);
        mir_emit_forth_store_stack_hl(out, -1);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    /* OP_AND / OP_OR */
    for (item = 30; item <= 31; ++item) {
        mir_stream_printf(out, "L%d:\n", cases[item]);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
        mir_emit_forth_load_stack_hl(out, -1);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
        mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
        if (item == 30)
            mir_stream_puts("\tld a,l\n\tand e\n\tld l,a\n"
                  "\tld a,h\n\tand d\n\tld h,a\n", out);
        else
            mir_stream_puts("\tld a,l\n\tor e\n\tld l,a\n"
                  "\tld a,h\n\tor d\n\tld h,a\n", out);
        mir_emit_forth_store_stack_hl(out, -1);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    /* OP_INVERT */
    {
        int zero = new_label();
        int done_label = new_label();

        mir_stream_printf(out, "L%d:\n", cases[32]);
        mir_emit_forth_load_stack_hl(out, -1);
        mir_stream_printf(out,
                "\tld a,h\n\tor l\n\tjp z, L%d\n"
                "\tld hl,0\n\tjp L%d\n"
                "L%d:\n\tld hl,1\nL%d:\n",
                zero, done_label, zero, done_label);
        mir_emit_forth_store_stack_hl(out, -1);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    /* OP_FETCH */
    mir_stream_printf(out, "L%d:\n", cases[33]);
    mir_emit_forth_load_stack_hl(out, -1);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
    mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n\tinc hl\n", bad_address);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY_CAPACITY);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c, L%d\n\tjp z, L%d\n",
            bad_address, bad_address);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tex de,hl\n", out);
    mir_emit_forth_store_stack_hl(out, -1);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_STORE */
    mir_stream_printf(out, "L%d:\n", cases[34]);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
    mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n\tinc hl\n", bad_address);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY_CAPACITY);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c, L%d\n\tjp z, L%d\n",
            bad_address, bad_address);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_CFETCH */
    mir_stream_printf(out, "L%d:\n", cases[35]);
    mir_emit_forth_load_stack_hl(out, -1);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
    mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n", bad_address);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY_CAPACITY);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c, L%d\n\tjp z, L%d\n", bad_address, bad_address);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tld l,(hl)\n\tld h,0\n", out);
    mir_emit_forth_store_stack_hl(out, -1);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_CSTORE */
    mir_stream_printf(out, "L%d:\n", cases[36]);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
    mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n", bad_address);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY_CAPACITY);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c, L%d\n\tjp z, L%d\n", bad_address, bad_address);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tpop de\n\tld (hl),e\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_ADDSTORE */
    mir_stream_printf(out, "L%d:\n", cases[37]);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
    mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n\tinc hl\n", bad_address);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY_CAPACITY);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c, L%d\n\tjp z, L%d\n",
            bad_address, bad_address);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_B);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tpush hl\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tex de,hl\n", out);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_B);
    mir_stream_puts("\tadd hl,de\n\tex de,hl\n\tpop hl\n"
          "\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_FILL */
    {
        int fill_bounds_ok = new_label();

        mir_stream_printf(out, "L%d:\n", cases[38]);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_C);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_N);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, MIR_FORTH_TEMP_A);
        mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n", bad_fill);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_N);
        mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n", bad_fill);
        mir_stream_puts("\tex de,hl\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
        mir_stream_puts("\tadd hl,de\n", out);
        mir_stream_printf(out, "\tjp c, L%d\n", bad_fill);
        mir_stream_puts("\tex de,hl\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY_CAPACITY);
        mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
        mir_stream_printf(out,
                "\tjp c, L%d\n"
                "L%d:\n",
                bad_fill, fill_bounds_ok);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_A);
        mir_stream_puts("\tex de,hl\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_MEMORY);
        mir_stream_puts("\tadd hl,de\n\tpush hl\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_C);
        mir_stream_puts("\tld e,l\n\tld d,0\n", out);
        mir_emit_forth_frame_load(out, MIR_FORTH_TEMP_N);
        mir_stream_puts("\tld b,h\n\tld c,l\n\tpop hl\n", out);
        mir_emit_runtime_call(out, "__msf");
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    /* OP_DOT */
    mir_stream_printf(out, "L%d:\n", cases[39]);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->number_format_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_CR */
    mir_stream_printf(out,
            "L%d:\n\tld hl,S%d\n\tpush hl\n",
            cases[40], plan->newline_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_EMIT */
    mir_stream_printf(out, "L%d:\n", cases[41]);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_symbol_call(out, plan->putchar_function);
    mir_stream_puts("\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* OP_PSTR */
    mir_stream_printf(out,
            "L%d:\n\tld hl,(%s)\n\tld de,%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n\tpush hl\n",
            cases[9], state_name, plan->strings_offset);
    mir_emit_forth_instruction_argument(out, plan);
    mir_stream_puts("\tadd hl,hl\n\tex de,hl\n\tpop hl\n\tadd hl,de\n"
          "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tpush de\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->string_format_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    mir_stream_printf(out,
            "L%d:\n",
            next);
    for (item = 0; item < plan->instruction_size; ++item)
        mir_stream_puts("\tinc iy\n", out);
    mir_stream_printf(out,
            "\tjp L%d\n"
            "L%d:\n"
            "\tadd hl,hl\n\tadd hl,hl\n\tex de,hl\n",
            dispatch,
            set_pc);
    mir_emit_forth_frame_load(out, MIR_FORTH_CODE);
    mir_stream_printf(out,
            "\tadd hl,de\n\tpush hl\n\tpop iy\n\tjp L%d\n"
            "L%d:\n",
            dispatch,
            sync_exit);
    mir_emit_forth_sync_state(out, plan, state_name);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n", out);

    mir_stream_printf(out, "L%d:\n", return_stack_error);
    mir_emit_forth_die(out, plan, plan->return_stack_string);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", sync_exit, loop_stack_error);
    mir_emit_forth_die(out, plan, plan->loop_stack_string);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", sync_exit, bad_address);
    mir_emit_forth_die(out, plan, plan->bad_address_string);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", sync_exit, bad_fill);
    mir_emit_forth_die(out, plan, plan->bad_fill_string);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", sync_exit, bad_opcode);
    mir_emit_forth_die(out, plan, plan->bad_opcode_string);
    mir_stream_printf(out, "\tjp L%d\n", sync_exit);
}

static void mir_emit_global_byte_verify(
    MirStream *out, const struct MirGlobalByteVerify *plan)
{
    const char *buffer_name =
        asm_name_for(sym_asm_name(plan->buffer));
    int loop = new_label();
    int mismatch = new_label();
    int done = new_label();

    mir_emit_symbol_extrn(out, plan->buffer);
    mir_stream_printf(out,
            "\tld c,(ix%+d)\n\tld b,(ix%+d)\n"
            "\tbit 7,b\n\tjp nz, L%d\n"
            "\tld a,b\n\tor c\n\tjp z, L%d\n"
            "\tld a,(ix%+d)\n\tld hl,%s\n"
            "L%d:\n\tcpi\n\tjp nz, L%d\n\tjp pe, L%d\n\tjp L%d\n"
            "L%d:\n\tdec hl\n\tld l,(hl)\n\tld h,0\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tor a\n\tsbc hl,bc\n\tdec hl\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,0\n\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n",
            plan->size_offset, plan->size_offset + 1,
            done, done,
            plan->value_offset, buffer_name,
            loop, mismatch, loop, done,
            mismatch,
            plan->size_offset, plan->size_offset + 1,
            plan->value_offset,
            plan->size_offset, plan->size_offset + 1,
            plan->format_string);
    mir_emit_runtime_call(out, "__pfehx");
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->failure_string);
    mir_emit_symbol_call(out, plan->fail_function);
    mir_stream_printf(out,
            "\tpop bc\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            done);
}

static void mir_emit_e_digits(MirStream *out, const struct MirEDigits *plan)
{
    enum {
        ARRAY_POINTER = -2,
        OUTER_COUNT = -4,
        ACCUMULATOR = -6,
        INNER_INDEX = -8,
        REMAINDER = -10
    };
    int frame_bytes = plan->digits + 10;
    int outer = new_label();
    int inner = new_label();
    int print_digit = new_label();
    int done = new_label();

    mir_stream_puts("\tpush ix\n\tpop hl\n", out);
    mir_stream_printf(out,
            "\tld de,-%d\n\tadd hl,de\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tinc hl\n\tld (hl),1\n"
            "\tld d,h\n\tld e,l\n\tinc de\n\tld bc,%d\n\tldir\n",
            frame_bytes,
            ARRAY_POINTER, ARRAY_POINTER + 1,
            plan->digits - 2);
    mir_emit_forth_frame_load(out, ARRAY_POINTER);
    mir_stream_puts("\tld (hl),0\n\tinc hl\n\tld (hl),2\n", out);
    mir_stream_printf(out,
            "\tld hl,%d\n", plan->digits);
    mir_emit_forth_frame_store(out, OUTER_COUNT);
    mir_stream_puts("\tld hl,0\n", out);
    mir_emit_forth_frame_store(out, ACCUMULATOR);
    mir_stream_printf(out, "L%d:\n", outer);
    mir_emit_forth_frame_load(out, OUTER_COUNT);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp c, L%d\n\tjp z, L%d\n",
            plan->outer_limit, done, done);
    mir_emit_forth_frame_load(out, OUTER_COUNT);
    mir_emit_forth_frame_store(out, INNER_INDEX);
    mir_stream_puts("\tdec hl\n", out);
    mir_emit_forth_frame_store(out, OUTER_COUNT);
    mir_stream_printf(out, "L%d:\n", inner);
    mir_emit_forth_frame_load(out, INNER_INDEX);
    mir_stream_puts("\tdec hl\n", out);
    mir_emit_forth_frame_store(out, INNER_INDEX);
    mir_stream_printf(out,
            "\tld a,h\n\tor l\n\tjp z, L%d\n"
            "\tex de,hl\n",
            print_digit);
    mir_emit_forth_frame_load(out, ACCUMULATOR);
    mir_emit_runtime_call(out, "__udivmod");
    mir_emit_forth_frame_store(out, ACCUMULATOR);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_store(out, REMAINDER);

    mir_emit_forth_frame_load(out, INNER_INDEX);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, ARRAY_POINTER);
    mir_stream_puts("\tadd hl,de\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, REMAINDER);
    mir_stream_puts("\tld a,l\n\tpop hl\n\tld (hl),a\n", out);

    mir_emit_forth_frame_load(out, INNER_INDEX);
    mir_stream_puts("\tdec hl\n\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, ARRAY_POINTER);
    mir_stream_puts("\tadd hl,de\n\tld l,(hl)\n\tld h,0\n", out);
    mir_emit_mul_hl_const(out, (unsigned long)plan->radix);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, ACCUMULATOR);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tadd hl,de\n", out);
    mir_emit_forth_frame_store(out, ACCUMULATOR);
    mir_stream_printf(out, "\tjp L%d\n", inner);

    mir_stream_printf(out, "L%d:\n", print_digit);
    mir_emit_forth_frame_load(out, ACCUMULATOR);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->digit_format_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", outer, done);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->done_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_puts("\tpop bc\n\tld hl,0\n\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_float_matrix_multiply(
    MirStream *out, const struct MirFloatMatrixMultiply *plan)
{
    enum {
        LEFT_ROW = -2,
        RESULT_ROW = -4,
        RESULT_POINTER = -6,
        LEFT_POINTER = -8,
        RIGHT_COLUMN = -12,
        ROW_COUNT = -13,
        COLUMN_COUNT = -14,
        INNER_COUNT = -15
    };
    const char *left_name =
        asm_name_for(sym_asm_name(plan->left));
    const char *right_name =
        asm_name_for(sym_asm_name(plan->right));
    const char *result_name =
        asm_name_for(sym_asm_name(plan->result));
    int row_loop = new_label();
    int column_loop = new_label();
    int inner_loop = new_label();

    mir_emit_symbol_extrn(out, plan->left);
    mir_emit_symbol_extrn(out, plan->right);
    mir_emit_symbol_extrn(out, plan->result);
    mir_stream_printf(out,
            "\tld hl,%s\n", left_name);
    mir_emit_forth_frame_store(out, LEFT_ROW);
    mir_stream_printf(out, "\tld hl,%s\n", result_name);
    mir_emit_forth_frame_store(out, RESULT_ROW);
    mir_stream_printf(out,
            "\tld (ix%d),%d\n"
            "L%d:\n",
            ROW_COUNT, plan->dimension,
            row_loop);
    mir_emit_copy_frame_word(out, RESULT_POINTER, RESULT_ROW);
    mir_stream_printf(out, "\tld hl,%s\n", right_name);
    mir_emit_forth_frame_store(out, RIGHT_COLUMN);
    mir_stream_printf(out,
            "\tld (ix%d),%d\n"
            "L%d:\n",
            COLUMN_COUNT, plan->dimension,
            column_loop);
    mir_emit_copy_frame_word(out, LEFT_POINTER, LEFT_ROW);
    mir_emit_forth_frame_load(out, RIGHT_COLUMN);
    mir_stream_puts("\tpush hl\n\tpop iy\n", out);
    mir_stream_printf(out,
            "\tld (ix%d),%d\n"
            "L%d:\n",
            INNER_COUNT, plan->dimension,
            inner_loop);
    mir_emit_pointer_long_load(out, RESULT_POINTER);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%d)\n\tld h,(ix%d)\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n\tinc hl\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tinc hl\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tld l,c\n\tld h,b\n",
            LEFT_POINTER, LEFT_POINTER + 1,
            LEFT_POINTER, LEFT_POINTER + 1);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_puts("\tld l,(iy+0)\n\tld h,(iy+1)\n"
          "\tld e,(iy+2)\n\tld d,(iy+3)\n", out);
    mir_emit_runtime_call(out, "__fmaf");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n", out);
    mir_emit_pointer_long_store(out, RESULT_POINTER);
    mir_stream_printf(out, "\tld de,%d\n\tadd iy,de\n", plan->row_stride);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n",
            INNER_COUNT, inner_loop);
    mir_emit_frame_pointer_adjust(
        out, RESULT_POINTER, plan->element_size);
    mir_emit_frame_pointer_adjust(
        out, RIGHT_COLUMN, plan->element_size);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n",
            COLUMN_COUNT, column_loop);
    mir_emit_frame_pointer_adjust(
        out, LEFT_ROW, plan->row_stride);
    mir_emit_frame_pointer_adjust(
        out, RESULT_ROW, plan->row_stride);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n"
            "\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n",
            ROW_COUNT, row_loop);
}

static void mir_emit_fixed_float_zero(
    MirStream *out, const struct MirFixedFloatZero *plan)
{
    const char *array_name =
        asm_name_for(sym_asm_name(plan->array));
    int bytes = plan->rows * plan->row_stride;

    mir_emit_symbol_extrn(out, plan->array);
    mir_stream_printf(out,
            "\tld hl,%s\n\tld (hl),0\n"
            "\tld de,%s+1\n\tld bc,%d\n\tldir\n\tret\n",
            array_name, array_name, bytes - 1);
}

static void mir_emit_float_sum(
    MirStream *out, const struct MirFloatSum *plan)
{
    const char *array_name =
        asm_name_for(sym_asm_name(plan->array));
    const char *accumulator_name =
        asm_name_for(sym_asm_name(plan->accumulator));
    int loop = new_label();

    mir_emit_symbol_extrn(out, plan->array);
    mir_emit_symbol_extrn(out, plan->accumulator);
    mir_stream_printf(out,
            "\tld hl,%s\n\tpush hl\n\tpop iy\n"
            "\tld hl,%d\n\tld (ix-2),l\n\tld (ix-1),h\n"
            "L%d:\n"
            "\tld hl,(%s)\n\tld de,(%s+2)\n"
            "\tpush de\n\tpush hl\n"
            "\tld l,(iy+0)\n\tld h,(iy+1)\n"
            "\tld e,(iy+2)\n\tld d,(iy+3)\n",
            array_name, plan->count, loop,
            accumulator_name, accumulator_name);
    mir_emit_runtime_call(out, "__faf");
    mir_stream_printf(out,
            "\tpop bc\n\tpop bc\n"
            "\tld (%s),hl\n\tld (%s+2),de\n"
            "\tinc iy\n\tinc iy\n\tinc iy\n\tinc iy\n"
            "\tld l,(ix-2)\n\tld h,(ix-1)\n\tdec hl\n"
            "\tld (ix-2),l\n\tld (ix-1),h\n"
            "\tld a,h\n\tor l\n\tjp nz, L%d\n"
            "\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n",
            accumulator_name, accumulator_name, loop);
}

static void mir_emit_basic_global_to_frame(
    MirStream *out, struct Sym *symbol, int frame_offset)
{
    const char *name = asm_name_for(sym_asm_name(symbol));

    mir_emit_symbol_extrn(out, symbol);
    mir_stream_printf(out,
            "\tld hl,(%s)\n\tld (ix%d),l\n\tld (ix%d),h\n",
            name, frame_offset, frame_offset + 1);
}

static void mir_emit_basic_frame_to_global(
    MirStream *out, struct Sym *symbol, int frame_offset)
{
    const char *name = asm_name_for(sym_asm_name(symbol));

    mir_emit_symbol_extrn(out, symbol);
    mir_stream_printf(out,
            "\tld l,(ix%d)\n\tld h,(ix%d)\n\tld (%s),hl\n",
            frame_offset, frame_offset + 1, name);
}

static void mir_emit_basic_scaled_address(
    MirStream *out, int base_offset, int scale)
{
    mir_emit_mul_hl_const(out, (unsigned long)scale);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, base_offset);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_emit_basic_instruction_word(MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld l,(iy%+d)\n\tld h,(iy%+d)\n",
            offset, offset + 1);
}

static void mir_emit_basic_die(
    MirStream *out, const struct MirBasicRun *plan, int string_id)
{
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", string_id);
    mir_emit_symbol_call(out, plan->die_function);
}

static void mir_emit_basic_push_hl(MirStream *out)
{
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FORTH_SP);
    mir_stream_puts("\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n\tinc hl\n", out);
    mir_emit_forth_frame_store(out, MIR_FORTH_SP);
}

static void mir_emit_basic_pop_hl(MirStream *out)
{
    mir_emit_forth_frame_load(out, MIR_FORTH_SP);
    mir_stream_puts("\tdec hl\n\tdec hl\n", out);
    mir_emit_forth_frame_store(out, MIR_FORTH_SP);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
}

#define mir_emit_forth_push_hl mir_emit_basic_push_hl
#define mir_emit_forth_pop_hl mir_emit_basic_pop_hl
static void mir_emit_basic_run(
    MirStream *out, const struct MirBasicRun *plan)
{
    enum {
        BASIC_CODE = MIR_FORTH_CODE,
        BASIC_MEMORY = MIR_FORTH_MEMORY,
        BASIC_STACK = MIR_FORTH_STACK,
        BASIC_GOSUB_STACK = MIR_FORTH_CALL_STACK,
        BASIC_FOR_STACK = MIR_FORTH_LOOP_INDEX,
        BASIC_SYMBOLS = MIR_FORTH_LOOP_LIMIT,
        BASIC_STRINGS = MIR_FORTH_LOOP_PREVIOUS,
        BASIC_SP = MIR_FORTH_SP,
        BASIC_GSP = MIR_FORTH_CRP,
        BASIC_FSP = MIR_FORTH_LRP,
        BASIC_TEMP_A = MIR_FORTH_TEMP_A,
        BASIC_TEMP_B = MIR_FORTH_TEMP_B,
        BASIC_TEMP_C = MIR_FORTH_TEMP_C,
        BASIC_TEMP_N = MIR_FORTH_TEMP_N
    };
    int cases[30];
    int dispatch = new_label();
    int table = new_label();
    int next = new_label();
    int set_pc = new_label();
    int exit_label = new_label();
    int bad_opcode = new_label();
    int gosub_full = new_label();
    int gosub_empty = new_label();
    int for_full = new_label();
    int next_empty = new_label();
    int item;

    for (item = 0; item < plan->opcode_count; ++item)
        cases[item] = new_label();
    mir_emit_basic_global_to_frame(out, plan->code, BASIC_CODE);
    mir_emit_basic_global_to_frame(out, plan->memory, BASIC_MEMORY);
    mir_emit_basic_global_to_frame(out, plan->stack, BASIC_STACK);
    mir_emit_basic_global_to_frame(
        out, plan->gosub_stack, BASIC_GOSUB_STACK);
    mir_emit_basic_global_to_frame(out, plan->for_stack, BASIC_FOR_STACK);
    mir_emit_basic_global_to_frame(out, plan->symbols, BASIC_SYMBOLS);
    mir_emit_basic_global_to_frame(out, plan->strings, BASIC_STRINGS);
    mir_emit_basic_global_to_frame(out, plan->stack_pointer, BASIC_SP);
    mir_emit_forth_frame_load(out, BASIC_SP);
    mir_stream_puts("\tadd hl,hl\n\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, BASIC_STACK);
    mir_stream_puts("\tadd hl,de\n", out);
    mir_emit_forth_frame_store(out, BASIC_SP);
    mir_emit_basic_global_to_frame(out, plan->gosub_pointer, BASIC_GSP);
    mir_emit_basic_global_to_frame(out, plan->for_pointer, BASIC_FSP);
    mir_emit_forth_frame_load(out, BASIC_CODE);
    mir_stream_printf(out,
            "\tpush hl\n\tpop iy\n"
            "L%d:\n\tld a,(iy%+d)\n\tcp %d\n\tjp nc, L%d\n"
            "\tld l,a\n\tld h,0\n\tadd hl,hl\n"
            "\tld de,L%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tex de,hl\n\tjp (hl)\n"
            "L%d:\n",
            dispatch, plan->instruction_op_offset,
            plan->opcode_count, bad_opcode,
            table, table);
    for (item = 0; item < plan->opcode_count; ++item)
        mir_stream_printf(out, "\tdw L%d\n", cases[item]);

    mir_stream_printf(out, "L%d:\n\tjp L%d\n", cases[0], exit_label);

    mir_stream_printf(out, "L%d:\n", cases[1]);
    mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* Scalar load/store. */
    mir_stream_printf(out, "L%d:\n", cases[2]);
    mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
    mir_emit_basic_scaled_address(out, BASIC_SYMBOLS, plan->symbol_stride);
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n"
                 "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n",
            plan->symbol_scalar_offset);
    mir_emit_basic_scaled_address(out, BASIC_MEMORY, 2);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    mir_stream_printf(out, "L%d:\n", cases[3]);
    mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
    mir_emit_basic_scaled_address(out, BASIC_SYMBOLS, plan->symbol_stride);
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n"
                 "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n",
            plan->symbol_scalar_offset);
    mir_emit_basic_scaled_address(out, BASIC_MEMORY, 2);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* Array load/store. */
    {
        int load_invalid = new_label();
        int load_ready = new_label();

        mir_stream_printf(out, "L%d:\n", cases[4]);
        mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
        mir_emit_basic_scaled_address(
            out, BASIC_SYMBOLS, plan->symbol_stride);
        mir_emit_forth_frame_store(out, BASIC_TEMP_C);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_A);
        mir_emit_forth_frame_load(out, BASIC_TEMP_C);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n"
                     "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n"
                     "\tbit 7,h\n\tjp nz, L%d\n",
                plan->symbol_base_offset, load_invalid);
        mir_emit_forth_frame_store(out, BASIC_TEMP_N);
        mir_emit_forth_frame_load(out, BASIC_TEMP_A);
        mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n\tpush hl\n",
                load_invalid);
        mir_emit_forth_frame_load(out, BASIC_TEMP_C);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n"
                     "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n",
                plan->symbol_size_offset);
        mir_stream_puts("\tpop hl\n\tor a\n\tsbc hl,de\n", out);
        mir_stream_printf(out, "\tjp nc, L%d\n", load_invalid);
        mir_emit_forth_frame_load(out, BASIC_TEMP_N);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, BASIC_TEMP_A);
        mir_stream_puts("\tex de,hl\n\tpop hl\n\tadd hl,de\n", out);
        mir_emit_basic_scaled_address(out, BASIC_MEMORY, 2);
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n"
              "\tjp L", out);
        mir_stream_printf(out, "%d\nL%d:\n\tld hl,0\nL%d:\n",
                load_ready, load_invalid, load_ready);
        mir_emit_forth_push_hl(out);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    {
        int store_done = new_label();

        mir_stream_printf(out, "L%d:\n", cases[5]);
        mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
        mir_emit_basic_scaled_address(
            out, BASIC_SYMBOLS, plan->symbol_stride);
        mir_emit_forth_frame_store(out, BASIC_TEMP_C);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_B);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_A);
        mir_emit_forth_frame_load(out, BASIC_TEMP_C);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n"
                     "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n"
                     "\tbit 7,h\n\tjp nz, L%d\n",
                plan->symbol_base_offset, store_done);
        mir_emit_forth_frame_store(out, BASIC_TEMP_N);
        mir_emit_forth_frame_load(out, BASIC_TEMP_A);
        mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n\tpush hl\n",
                store_done);
        mir_emit_forth_frame_load(out, BASIC_TEMP_C);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n"
                     "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n",
                plan->symbol_size_offset);
        mir_stream_puts("\tpop hl\n\tor a\n\tsbc hl,de\n", out);
        mir_stream_printf(out, "\tjp nc, L%d\n", store_done);
        mir_emit_forth_frame_load(out, BASIC_TEMP_N);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, BASIC_TEMP_A);
        mir_stream_puts("\tex de,hl\n\tpop hl\n\tadd hl,de\n", out);
        mir_emit_basic_scaled_address(out, BASIC_MEMORY, 2);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, BASIC_TEMP_B);
        mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
              out);
        mir_stream_printf(out, "L%d:\n\tjp L%d\n", store_done, next);
    }

    /* Arithmetic. */
    for (item = 6; item <= 10; ++item) {
        int nonzero = new_label();
        int arithmetic_done = new_label();

        mir_stream_printf(out, "L%d:\n", cases[item]);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_B);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_A);
        if (item == 9 || item == 10) {
            mir_emit_forth_frame_load(out, BASIC_TEMP_B);
            mir_stream_printf(out,
                    "\tld a,h\n\tor l\n\tjp nz, L%d\n"
                    "\tld hl,0\n\tjp L%d\nL%d:\n\tex de,hl\n",
                    nonzero, arithmetic_done, nonzero);
            mir_emit_forth_frame_load(out, BASIC_TEMP_A);
            mir_emit_runtime_call(
                out, item == 9 ? "__divs" : "__mods");
            mir_stream_printf(out, "L%d:\n", arithmetic_done);
        } else {
            mir_emit_forth_frame_load(out, BASIC_TEMP_B);
            mir_stream_puts("\tex de,hl\n", out);
            mir_emit_forth_frame_load(out, BASIC_TEMP_A);
            if (item == 6)
                mir_stream_puts("\tadd hl,de\n", out);
            else if (item == 7)
                mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
            else
                mir_emit_runtime_call(out, "__mulu");
        }
        mir_emit_forth_push_hl(out);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    mir_stream_printf(out, "L%d:\n", cases[11]);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\txor a\n\tsub l\n\tld l,a\n\tld a,0\n\tsbc a,h\n\tld h,a\n",
          out);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* Comparisons. */
    for (item = 12; item <= 17; ++item) {
        int true_label = new_label();
        int false_label = new_label();
        int compare_done = new_label();

        mir_stream_printf(out, "L%d:\n", cases[item]);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_B);
        mir_emit_forth_pop_hl(out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_A);
        if (item == 12 || item == 13) {
            mir_emit_forth_frame_load(out, BASIC_TEMP_B);
            mir_stream_puts("\tex de,hl\n", out);
            mir_emit_forth_frame_load(out, BASIC_TEMP_A);
            mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
            mir_stream_printf(out, "\tjp %s, L%d\n\tjp L%d\n",
                    item == 12 ? "z" : "nz",
                    true_label, false_label);
        } else {
            if (item == 14 || item == 17) {
                mir_emit_forth_frame_load(out, BASIC_TEMP_B);
                mir_stream_puts("\tex de,hl\n", out);
                mir_emit_forth_frame_load(out, BASIC_TEMP_A);
            } else {
                mir_emit_forth_frame_load(out, BASIC_TEMP_A);
                mir_stream_puts("\tex de,hl\n", out);
                mir_emit_forth_frame_load(out, BASIC_TEMP_B);
            }
            if (item == 14 || item == 16) {
                mir_emit_forth_signed_less_branch(
                    out, true_label, false_label);
            } else {
                mir_emit_forth_signed_less_branch(
                    out, false_label, true_label);
            }
        }
        mir_stream_printf(out,
                "L%d:\n\tld hl,1\n\tjp L%d\n"
                "L%d:\n\tld hl,0\n"
                "L%d:\n",
                true_label, compare_done,
                false_label, compare_done);
        mir_emit_forth_push_hl(out);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    mir_stream_printf(out, "L%d:\n", cases[18]);
    mir_emit_forth_pop_hl(out);
    mir_emit_forth_frame_store(out, BASIC_TEMP_B);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, BASIC_TEMP_B);
    mir_stream_puts("\tex de,hl\n\tpop hl\n"
          "\tld a,l\n\tand e\n\tld l,a\n"
          "\tld a,h\n\tand d\n\tld h,a\n", out);
    mir_emit_forth_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* Jumps and gosub stack. */
    mir_stream_printf(out, "L%d:\n", cases[19]);
    mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
    mir_stream_printf(out, "\tjp L%d\n", set_pc);

    mir_stream_printf(out, "L%d:\n", cases[20]);
    mir_emit_forth_pop_hl(out);
    mir_stream_printf(out,
            "\tld a,h\n\tor l\n\tjp nz, L%d\n",
            next);
    mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
    mir_stream_printf(out, "\tjp L%d\n", set_pc);

    mir_stream_printf(out, "L%d:\n", cases[21]);
    mir_emit_forth_frame_load(out, BASIC_GSP);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n\tjp nc, L%d\n"
            "\tpush iy\n\tpop hl\n\tld de,%d\n\tadd hl,de\n\tpush hl\n",
            plan->gosub_limit, gosub_full, plan->instruction_size);
    mir_emit_forth_local_array_address(
        out, BASIC_GOSUB_STACK, BASIC_GSP, 0);
    mir_stream_puts("\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_emit_forth_frame_load(out, BASIC_GSP);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, BASIC_GSP);
    mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
    mir_stream_printf(out, "\tjp L%d\n", set_pc);

    mir_stream_printf(out, "L%d:\n", cases[22]);
    mir_emit_forth_frame_load(out, BASIC_GSP);
    mir_stream_printf(out,
            "\tld a,h\n\tor a\n\tjp m, L%d\n"
            "\tor l\n\tjp z, L%d\n\tdec hl\n",
            gosub_empty, gosub_empty);
    mir_emit_forth_frame_store(out, BASIC_GSP);
    mir_emit_forth_local_array_address(
        out, BASIC_GOSUB_STACK, BASIC_GSP, 0);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tpush de\n\tpop iy\n", out);
    mir_stream_printf(out, "\tjp L%d\n", dispatch);

    /* FOR / NEXT. */
    mir_stream_printf(out, "L%d:\n", cases[23]);
    mir_emit_forth_frame_load(out, BASIC_FSP);
    mir_stream_printf(out,
            "\tld de,%d\n\tor a\n\tsbc hl,de\n\tjp nc, L%d\n",
            plan->for_limit, for_full);
    mir_emit_forth_frame_load(out, BASIC_FSP);
    mir_emit_basic_scaled_address(out, BASIC_FOR_STACK, plan->for_stride);
    mir_emit_forth_frame_store(out, BASIC_TEMP_C);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
    mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
    if (plan->for_var_offset != 0)
        mir_stream_printf(out, "\tld bc,%d\n\tadd hl,bc\n",
                plan->for_var_offset);
    mir_stream_puts("\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, BASIC_TEMP_C);
    if (plan->for_limit_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->for_limit_offset);
    mir_stream_puts("\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_emit_forth_frame_load(out, BASIC_TEMP_C);
    if (plan->for_step_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->for_step_offset);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_basic_instruction_word(out, plan->instruction_b_offset);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_emit_forth_frame_load(out, BASIC_TEMP_C);
    if (plan->for_pc_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->for_pc_offset);
    mir_stream_puts("\tpush hl\n\tpush iy\n\tpop hl\n", out);
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n\tex de,hl\n\tpop hl\n"
                 "\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
            plan->instruction_size);
    mir_emit_forth_frame_load(out, BASIC_FSP);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, BASIC_FSP);
    mir_stream_printf(out, "\tjp L%d\n", next);

    {
        int continue_loop = new_label();
        int finish_loop = new_label();

        mir_stream_printf(out, "L%d:\n", cases[24]);
        mir_emit_forth_frame_load(out, BASIC_FSP);
        mir_stream_printf(out,
                "\tld a,h\n\tor a\n\tjp m, L%d\n"
                "\tor l\n\tjp z, L%d\n\tdec hl\n",
                next_empty, next_empty);
        mir_emit_basic_scaled_address(out, BASIC_FOR_STACK, plan->for_stride);
        mir_emit_forth_frame_store(out, BASIC_TEMP_C);
        if (plan->for_var_offset != 0)
            mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                    plan->for_var_offset);
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
        mir_emit_basic_scaled_address(out, BASIC_SYMBOLS, plan->symbol_stride);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n"
                     "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n",
                plan->symbol_scalar_offset);
        mir_emit_basic_scaled_address(out, BASIC_MEMORY, 2);
        mir_stream_puts("\tpush hl\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
              "\tex de,hl\n", out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_A);
        mir_emit_forth_frame_load(out, BASIC_TEMP_C);
        if (plan->for_step_offset != 0)
            mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                    plan->for_step_offset);
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n", out);
        mir_emit_forth_frame_load(out, BASIC_TEMP_A);
        mir_stream_puts("\tpop bc\n\tadd hl,de\n\tex de,hl\n"
              "\tld h,b\n\tld l,c\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n"
              "\tex de,hl\n", out);
        mir_emit_forth_frame_store(out, BASIC_TEMP_A);
        mir_emit_forth_frame_load(out, BASIC_TEMP_C);
        if (plan->for_limit_offset != 0)
            mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                    plan->for_limit_offset);
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n", out);
        mir_emit_forth_frame_load(out, BASIC_TEMP_A);
        /* Continue unless limit < new value. */
        mir_stream_puts("\tex de,hl\n", out);
        mir_emit_forth_signed_less_branch(
            out, finish_loop, continue_loop);
        mir_stream_printf(out, "L%d:\n", continue_loop);
        mir_emit_forth_frame_load(out, BASIC_TEMP_C);
        if (plan->for_pc_offset != 0)
            mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                    plan->for_pc_offset);
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
              "\tpush de\n\tpop iy\n", out);
        mir_stream_printf(out, "\tjp L%d\nL%d:\n",
                dispatch, finish_loop);
        mir_emit_forth_frame_load(out, BASIC_FSP);
        mir_stream_puts("\tdec hl\n", out);
        mir_emit_forth_frame_store(out, BASIC_FSP);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }

    /* Printing and no-op clear. */
    mir_stream_printf(out, "L%d:\n", cases[25]);
    mir_emit_forth_pop_hl(out);
    mir_stream_puts("\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
            plan->number_format_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    mir_stream_printf(out, "L%d:\n", cases[26]);
    mir_emit_forth_frame_load(out, BASIC_STRINGS);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_basic_instruction_word(out, plan->instruction_a_offset);
    mir_stream_puts("\tadd hl,hl\n\tex de,hl\n\tpop hl\n\tadd hl,de\n"
          "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tpush de\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
            plan->string_format_string);
    mir_emit_symbol_call(out, plan->print_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    for (item = 27; item <= 28; ++item) {
        int string_id =
            item == 27 ? plan->space_string : plan->newline_string;
        mir_stream_printf(out, "L%d:\n\tld hl,S%d\n\tpush hl\n",
                cases[item], string_id);
        mir_emit_symbol_call(out, plan->print_function);
        mir_stream_puts("\tpop bc\n", out);
        mir_stream_printf(out, "\tjp L%d\n", next);
    }
    mir_stream_printf(out, "L%d:\n\tjp L%d\n", cases[29], next);

    mir_stream_printf(out, "L%d:\n", next);
    for (item = 0; item < plan->instruction_size; ++item)
        mir_stream_puts("\tinc iy\n", out);
    mir_stream_printf(out,
            "\tjp L%d\n"
            "L%d:\n"
            "\tpush hl\n\tadd hl,hl\n\tadd hl,hl\n\tpop de\n\tadd hl,de\n"
            "\tex de,hl\n",
            dispatch, set_pc);
    mir_emit_forth_frame_load(out, BASIC_CODE);
    mir_stream_printf(out,
            "\tadd hl,de\n\tpush hl\n\tpop iy\n\tjp L%d\n"
            "L%d:\n",
            dispatch, exit_label);
    mir_emit_forth_frame_load(out, BASIC_SP);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, BASIC_STACK);
    mir_stream_puts("\tex de,hl\n\tor a\n\tsbc hl,de\n"
          "\tsrl h\n\trr l\n", out);
    {
        const char *stack_pointer_name =
            asm_name_for(sym_asm_name(plan->stack_pointer));
        mir_emit_symbol_extrn(out, plan->stack_pointer);
        mir_stream_printf(out, "\tld (%s),hl\n", stack_pointer_name);
    }
    mir_emit_basic_frame_to_global(out, plan->gosub_pointer, BASIC_GSP);
    mir_emit_basic_frame_to_global(out, plan->for_pointer, BASIC_FSP);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n", out);

    mir_stream_printf(out, "L%d:\n", gosub_full);
    mir_emit_basic_die(out, plan, plan->gosub_full_string);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", exit_label, gosub_empty);
    mir_emit_basic_die(out, plan, plan->gosub_empty_string);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", exit_label, for_full);
    mir_emit_basic_die(out, plan, plan->for_full_string);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", exit_label, next_empty);
    mir_emit_basic_die(out, plan, plan->next_empty_string);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", exit_label, bad_opcode);
    mir_emit_basic_die(out, plan, plan->bad_opcode_string);
    mir_stream_printf(out, "\tjp L%d\n", exit_label);
}
#undef mir_emit_forth_push_hl
#undef mir_emit_forth_pop_hl

enum MirFortranEvalFrameOffset {
    MIR_FOREVAL_END = -2,
    MIR_FOREVAL_SYMBOLS = -4,
    MIR_FOREVAL_MEMORY = -6,
    MIR_FOREVAL_MEMORY_CAPACITY = -8,
    MIR_FOREVAL_SYMBOL_INDEX = -10,
    MIR_FOREVAL_ARRAY_INDEX = -12,
    MIR_FOREVAL_SYMBOL_POINTER = -14,
    MIR_FOREVAL_STACK_POINTER = -16
};

static void mir_emit_fortran_symbol_load(
    MirStream *out, const struct MirFortranEval *plan)
{
    int array_index = new_label();
    int index_ready = new_label();
    int word_value = new_label();
    int word_array_index = new_label();
    int word_index_ready = new_label();
    int bounds_error = new_label();
    int done = new_label();

    mir_emit_forth_frame_load(out, MIR_FOREVAL_SYMBOL_INDEX);
    mir_emit_mul_hl_const(out, (unsigned long)plan->symbol_stride);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_SYMBOLS);
    mir_stream_puts("\tadd hl,de\n", out);
    mir_emit_forth_frame_store(out, MIR_FOREVAL_SYMBOL_POINTER);
    mir_stream_printf(out,
            "\tld de,%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld hl,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nz, L%d\n",
            plan->symbol_type_offset,
            plan->byte_type, word_value);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_SYMBOL_POINTER);
    mir_stream_printf(out,
            "\tld de,%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld hl,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nz, L%d\n\tld hl,0\n\tjp L%d\n"
            "L%d:\n",
            plan->symbol_kind_offset,
            plan->scalar_kind, array_index, index_ready,
            array_index);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_ARRAY_INDEX);
    mir_stream_printf(out, "L%d:\n\tex de,hl\n", index_ready);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_SYMBOL_POINTER);
    mir_stream_printf(out,
            "\tld bc,%d\n\tadd hl,bc\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
            "\tld h,b\n\tld l,c\n\tadd hl,de\n\tex de,hl\n",
            plan->symbol_base_offset);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tld l,(hl)\n"
          "\tld a,l\n\trlca\n\tsbc a,a\n\tld h,a\n"
          "\tjp L", out);
    mir_stream_printf(out, "%d\nL%d:\n", done, word_value);

    mir_emit_forth_frame_load(out, MIR_FOREVAL_SYMBOL_POINTER);
    mir_stream_printf(out,
            "\tld de,%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tld hl,%d\n\tor a\n\tsbc hl,de\n"
            "\tjp nz, L%d\n\tld hl,0\n\tjp L%d\n"
            "L%d:\n",
            plan->symbol_kind_offset,
            plan->scalar_kind, word_array_index, word_index_ready,
            word_array_index);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_ARRAY_INDEX);
    mir_stream_printf(out, "L%d:\n\tadd hl,hl\n\tex de,hl\n",
            word_index_ready);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_SYMBOL_POINTER);
    mir_stream_printf(out,
            "\tld bc,%d\n\tadd hl,bc\n"
            "\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
            "\tld h,b\n\tld l,c\n\tadd hl,de\n"
            "\tld (ix%d),l\n\tld (ix%d),h\n"
            "\tbit 7,h\n\tjp nz, L%d\n"
            "\tinc hl\n\tex de,hl\n",
            plan->symbol_base_offset,
            MIR_FOREVAL_ARRAY_INDEX, MIR_FOREVAL_ARRAY_INDEX + 1,
            bounds_error);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_MEMORY_CAPACITY);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out,
            "\tjp c, L%d\n\tjp z, L%d\n",
            bounds_error, bounds_error);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_ARRAY_INDEX);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_MEMORY);
    mir_stream_puts("\tadd hl,de\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tex de,hl\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", done, bounds_error);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->bounds_string);
    mir_emit_symbol_call(out, plan->die_function);
    mir_stream_printf(out, "\tld hl,0\nL%d:\n", done);
}

static void mir_emit_fortran_eval(
    MirStream *out, const struct MirFortranEval *plan)
{
    const char *stack_name =
        asm_name_for(sym_asm_name(plan->evaluation_stack));
    const char *tokens_name =
        asm_name_for(sym_asm_name(plan->tokens));
    const char *expressions_name =
        asm_name_for(sym_asm_name(plan->expressions));
    int cases[17];
    int dispatch = new_label();
    int table = new_label();
    int next = new_label();
    int done = new_label();
    int empty = new_label();
    int exit_label = new_label();
    int pop_two = new_label();
    int push_result = new_label();
    int item;

    for (item = 0; item < plan->opcode_count; ++item)
        cases[item] = new_label();
    mir_emit_symbol_extrn(out, plan->evaluation_stack);
    mir_emit_symbol_extrn(out, plan->tokens);
    mir_emit_symbol_extrn(out, plan->expressions);
    mir_emit_basic_global_to_frame(
        out, plan->symbols, MIR_FOREVAL_SYMBOLS);
    mir_emit_basic_global_to_frame(
        out, plan->memory, MIR_FOREVAL_MEMORY);
    mir_emit_basic_global_to_frame(
        out, plan->memory_capacity, MIR_FOREVAL_MEMORY_CAPACITY);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tbit 7,h\n\tjp nz, L%d\n"
            "\tpush hl\n\tld hl,(%s)\n\tex de,hl\n\tpop hl\n",
            plan->expression_index_offset + 2,
            plan->expression_index_offset + 3,
            empty, expressions_name);
    mir_emit_mul_hl_const(out, (unsigned long)plan->expression_stride);
    mir_stream_puts("\tadd hl,de\n\tpush hl\n", out);
    if (plan->expression_start_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->expression_start_offset);
    mir_stream_puts("\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n", out);
    mir_stream_printf(out, "\tld hl,(%s)\n\tadd hl,bc\n\tadd hl,bc\n"
                 "\tadd hl,bc\n\tadd hl,bc\n"
                 "\tpush hl\n\tpop iy\n\tpop hl\n",
            tokens_name);
    if (plan->expression_length_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->expression_length_offset);
    mir_stream_puts("\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
          "\tld h,b\n\tld l,c\n", out);
    mir_emit_mul_hl_const(out, (unsigned long)plan->token_stride);
    mir_stream_puts("\tpush iy\n\tpop de\n\tadd hl,de\n", out);
    mir_emit_forth_frame_store(out, MIR_FOREVAL_END);
    mir_stream_printf(out, "\tld hl,%s\n", stack_name);
    mir_emit_forth_frame_store(out, MIR_FOREVAL_STACK_POINTER);
    mir_stream_printf(out,
            "L%d:\n\tpush iy\n\tpop hl\n\tpush hl\n",
            dispatch);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_END);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out,
            "\tjp nc, L%d\n"
            "\tld a,(iy%+d)\n\tor a\n\tjp nz, L%d\n"
            "\tld a,(iy%+d)\n\tdec a\n\tcp %d\n\tjp nc, L%d\n"
            "\tld l,a\n\tld h,0\n\tadd hl,hl\n"
            "\tld de,L%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tex de,hl\n\tjp (hl)\n"
            "L%d:\n",
            done,
            plan->token_op_offset + 1, next,
            plan->token_op_offset, plan->opcode_count, next,
            table, table);
    for (item = 0; item < plan->opcode_count; ++item)
        mir_stream_printf(out, "\tdw L%d\n", cases[item]);

    mir_stream_printf(out, "L%d:\n", cases[0]);
    mir_emit_basic_instruction_word(out, plan->token_argument_offset);
    mir_stream_printf(out, "\tjp L%d\n", push_result);

    for (item = 1; item <= 2; ++item) {
        mir_stream_printf(out, "L%d:\n", cases[item]);
        if (item == 2) {
            mir_emit_basic_pop_hl(out);
            mir_emit_forth_frame_store(out, MIR_FOREVAL_ARRAY_INDEX);
        } else {
            mir_stream_puts("\tld hl,0\n", out);
            mir_emit_forth_frame_store(out, MIR_FOREVAL_ARRAY_INDEX);
        }
        mir_emit_basic_instruction_word(out, plan->token_argument_offset);
        mir_emit_forth_frame_store(out, MIR_FOREVAL_SYMBOL_INDEX);
        mir_emit_fortran_symbol_load(out, plan);
        mir_stream_printf(out, "\tjp L%d\n", push_result);
    }

    mir_stream_printf(out, "L%d:\n", cases[8]);
    mir_emit_basic_pop_hl(out);
    mir_stream_puts("\txor a\n\tsub l\n\tld l,a\n\tld a,0\n\tsbc a,h\n\tld h,a\n",
          out);
    mir_stream_printf(out, "\tjp L%d\n", push_result);

    for (item = 3; item < plan->opcode_count; ++item) {
        int true_label = new_label();
        int false_label = new_label();
        int result_label = new_label();

        if (item == 8)
            continue;
        mir_stream_printf(out, "L%d:\n", cases[item]);
        mir_stream_printf(out,
                "\tcall L%d\n"
                "\tld (ix%d),c\n\tld (ix%d),b\n",
                pop_two,
                MIR_FOREVAL_STACK_POINTER,
                MIR_FOREVAL_STACK_POINTER + 1);
        if (item == 6 || item == 7) {
            int nonzero = new_label();
            mir_stream_printf(out,
                    "\tld a,d\n\tor e\n\tjp nz, L%d\n"
                    "\tld hl,0\n\tjp L%d\nL%d:\n",
                    nonzero, result_label, nonzero);
            mir_emit_runtime_call(
                out, item == 6 ? "__divs" : "__mods");
            mir_stream_printf(out, "L%d:\n", result_label);
        } else if (item >= 9 && item <= 14) {
            if (item == 9 || item == 10) {
                mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
                mir_stream_printf(out, "\tjp %s, L%d\n\tjp L%d\n",
                        item == 9 ? "z" : "nz",
                        true_label, false_label);
            } else {
                if (item == 12 || item == 13)
                    mir_stream_puts("\tex de,hl\n", out);
                if (item == 11 || item == 13)
                    mir_emit_forth_signed_less_branch(
                        out, true_label, false_label);
                else
                    mir_emit_forth_signed_less_branch(
                        out, false_label, true_label);
            }
            mir_stream_printf(out,
                    "L%d:\n\tld hl,1\n\tjp L%d\n"
                    "L%d:\n\tld hl,0\nL%d:\n",
                    true_label, result_label,
                    false_label, result_label);
        } else if (item == 15 || item == 16) {
            int short_circuit = new_label();
            mir_stream_puts("\tld a,h\n\tor l\n", out);
            if (item == 15)
                mir_stream_printf(out, "\tjp z, L%d\n", short_circuit);
            else
                mir_stream_printf(out, "\tjp nz, L%d\n", true_label);
            mir_stream_puts("\tld a,d\n\tor e\n", out);
            if (item == 15)
                mir_stream_printf(out, "\tjp z, L%d\n", short_circuit);
            else
                mir_stream_printf(out, "\tjp nz, L%d\n", true_label);
            if (item == 15) {
                mir_stream_puts("\tld hl,1\n", out);
                mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,0\n",
                        result_label, short_circuit);
            } else {
                mir_stream_puts("\tld hl,0\n", out);
                mir_stream_printf(out,
                        "\tjp L%d\nL%d:\n\tld hl,1\n",
                        result_label, true_label);
            }
            mir_stream_printf(out, "L%d:\n", result_label);
        } else {
            if (item == 3)
                mir_stream_puts("\tadd hl,de\n", out);
            else if (item == 4)
                mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
            else
                mir_emit_runtime_call(out, "__mulu");
        }
        mir_stream_printf(out, "\tjp L%d\n", push_result);
    }
    mir_stream_printf(out, "L%d:\n", pop_two);
    mir_stream_printf(out,
            "\tld c,(ix%d)\n\tld b,(ix%d)\n"
            "\tdec bc\n\tdec bc\n"
            "\tld l,c\n\tld h,b\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tpush de\n"
            "\tdec bc\n\tdec bc\n"
            "\tld l,c\n\tld h,b\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tex de,hl\n\tpop de\n\tret\n",
            MIR_FOREVAL_STACK_POINTER,
            MIR_FOREVAL_STACK_POINTER + 1);
    mir_stream_printf(out, "L%d:\n", push_result);
    mir_emit_basic_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);
    mir_stream_printf(out, "L%d:\n", next);
    for (item = 0; item < plan->token_stride; ++item)
        mir_stream_puts("\tinc iy\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", dispatch, done);
    mir_emit_forth_frame_load(out, MIR_FOREVAL_STACK_POINTER);
    mir_stream_printf(out,
            "\tld de,%s\n\tor a\n\tsbc hl,de\n"
            "\tjp z, L%d\n",
            stack_name, empty);
    mir_emit_basic_pop_hl(out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,0\nL%d:\n"
                 "\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n",
            exit_label, empty, exit_label);
}

enum MirCRunFrameOffset {
    MIR_CRUN_CODE = -2,
    MIR_CRUN_GLOBAL_MEMORY = -4,
    MIR_CRUN_FRAME_MEMORY = -6,
    MIR_CRUN_FUNCTIONS = -8,
    MIR_CRUN_RETURN_STACK = -10,
    MIR_CRUN_FRAME_STORAGE = -12,
    MIR_CRUN_STRINGS = -14,
    MIR_CRUN_STACK_POINTER = -16,
    MIR_CRUN_FRAME_SIZE = -18,
    MIR_CRUN_FRAME_POINTER = -20,
    MIR_CRUN_PC = -22,
    MIR_CRUN_TEMP_A = -24,
    MIR_CRUN_TEMP_B = -26,
    MIR_CRUN_TEMP_V = -28,
    MIR_CRUN_TEMP_FI = -30,
    MIR_CRUN_VALUES = -46
};

static void mir_emit_c_state_load(
    MirStream *out, const char *state_name, int offset)
{
    mir_stream_printf(out, "\tld hl,(%s+%d)\n", state_name, offset);
}

static void mir_emit_c_state_store(
    MirStream *out, const char *state_name, int offset)
{
    mir_stream_printf(out, "\tld (%s+%d),hl\n", state_name, offset);
}

static void mir_emit_c_function_address(
    MirStream *out, const struct MirCRun *plan)
{
    mir_emit_mul_hl_const(out, (unsigned long)plan->function_stride);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_FUNCTIONS);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_emit_c_sync_state(
    MirStream *out, const struct MirCRun *plan, const char *state_name)
{
    mir_emit_forth_frame_load(out, MIR_CRUN_STACK_POINTER);
    mir_emit_c_state_store(out, state_name, plan->state_stp_offset);
    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_MEMORY);
    mir_emit_c_state_store(out, state_name, plan->state_flp_offset);
    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_POINTER);
    mir_emit_c_state_store(out, state_name, plan->state_fp_offset);
}

static void mir_emit_c_reload_state(
    MirStream *out, const struct MirCRun *plan, const char *state_name)
{
    mir_emit_c_state_load(out, state_name, plan->state_stp_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_STACK_POINTER);
    mir_emit_c_state_load(out, state_name, plan->state_flp_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_FRAME_MEMORY);
    mir_emit_c_state_load(out, state_name, plan->state_fp_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_FRAME_POINTER);
}

static void mir_emit_c_instruction_word(MirStream *out, int offset)
{
    mir_stream_printf(out,
            "\tld l,(iy%+d)\n\tld h,(iy%+d)\n",
            offset, offset + 1);
}

static void mir_emit_c_memory_address(
    MirStream *out, int memory_frame_offset,
    int indexed, int element_size)
{
    if (indexed) {
        if (element_size == 2)
            mir_stream_puts("\tadd hl,hl\n", out);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_c_instruction_word(out, 1);
        mir_stream_puts("\tex de,hl\n\tpop hl\n\tadd hl,de\n", out);
    } else {
        mir_emit_c_instruction_word(out, 1);
    }
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, memory_frame_offset);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_emit_c_run(MirStream *out, const struct MirCRun *plan)
{
    const char *state_name =
        asm_name_for(sym_asm_name(plan->state));
    const char *saved_main_name =
        asm_name_for(sym_asm_name(plan->saved_main));
    int cases[42];
    int dispatch = new_label();
    int table = new_label();
    int next = new_label();
    int set_pc = new_label();
    int exit_label = new_label();
    int bad_opcode = new_label();
    int no_main = new_label();
    int pop_two = new_label();
    int push_result = new_label();
    int item;

    for (item = 0; item < plan->opcode_count; ++item)
        cases[item] = new_label();
    mir_emit_symbol_extrn(out, plan->state);
    mir_emit_symbol_extrn(out, plan->saved_main);

    mir_emit_c_state_load(out, state_name, plan->state_code_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_CODE);
    mir_emit_c_state_load(out, state_name, plan->state_global_memory_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_GLOBAL_MEMORY);
    mir_emit_c_state_load(out, state_name, plan->state_st_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_STACK_POINTER);
    mir_emit_c_state_store(out, state_name, plan->state_stp_offset);
    mir_stream_puts("\tld hl,0\n", out);
    mir_emit_forth_frame_store(out, MIR_CRUN_FRAME_POINTER);
    mir_emit_c_state_store(out, state_name, plan->state_fp_offset);
    mir_emit_c_state_load(out, state_name, plan->state_floc_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_FRAME_STORAGE);
    mir_emit_forth_frame_store(out, MIR_CRUN_FRAME_MEMORY);
    mir_emit_c_state_store(out, state_name, plan->state_flp_offset);
    mir_emit_c_state_load(out, state_name, plan->state_functions_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_FUNCTIONS);
    mir_emit_c_state_load(out, state_name, plan->state_return_stack_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_RETURN_STACK);
    mir_emit_c_state_load(out, state_name, plan->state_strings_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_STRINGS);
    mir_emit_c_state_load(out, state_name, plan->state_frame_size_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_FRAME_SIZE);

    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_STORAGE);
    mir_stream_puts("\tpush hl\n\tld e,0\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_SIZE);
    mir_emit_mul_hl_const(out, (unsigned long)plan->frame_limit);
    mir_stream_puts("\tld b,h\n\tld c,l\n\tpop hl\n", out);
    mir_emit_runtime_call(out, "__msf");

    mir_stream_printf(out, "\tld hl,(%s)\n\tbit 7,h\n\tjp nz, L%d\n",
            saved_main_name, no_main);
    mir_emit_c_state_store(out, state_name, plan->state_main_function_offset);
    mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_FI);
    mir_emit_c_function_address(out, plan);
    if (plan->function_parameter_count_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->function_parameter_count_offset);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tex de,hl\n\tld de,2\n\tor a\n\tsbc hl,de\n", out);
    {
        int no_args = new_label();
        int args_ready = new_label();
        mir_stream_printf(out, "\tjp c, L%d\n", no_args);
        mir_stream_puts("\tld hl,1\n", out);
        mir_emit_basic_push_hl(out);
        mir_stream_puts("\tld hl,0\n", out);
        mir_emit_basic_push_hl(out);
        mir_stream_puts("\tld hl,2\n", out);
        mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,0\nL%d:\n",
                args_ready, no_args, args_ready);
    }
    mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_A);
    mir_emit_c_sync_state(out, plan, state_name);
    mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_A);
    mir_stream_puts("\tpush hl\n\tld hl,65535\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_FI);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_symbol_call(out, plan->call_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n", out);
    mir_emit_c_reload_state(out, plan, state_name);
    mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_FI);
    mir_emit_c_function_address(out, plan);
    if (plan->function_entry_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->function_entry_offset);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
    mir_stream_printf(out, "\tjp L%d\n", set_pc);

    mir_stream_printf(out,
            "L%d:\n",
            dispatch);
    mir_emit_forth_frame_load(out, MIR_CRUN_PC);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, MIR_CRUN_PC);
    mir_stream_printf(out,
            "\tld a,(iy%+d)\n\tcp %d\n\tjp nc, L%d\n"
            "\tld l,a\n\tld h,0\n\tadd hl,hl\n"
            "\tld de,L%d\n\tadd hl,de\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tex de,hl\n\tjp (hl)\n"
            "L%d:\n",
            plan->instruction_op_offset,
            plan->opcode_count, bad_opcode,
            table, table);
    for (item = 0; item < plan->opcode_count; ++item)
        mir_stream_printf(out, "\tdw L%d\n", cases[item]);

    mir_stream_printf(out, "L%d:\n\tjp L%d\n", cases[0], exit_label);
    mir_stream_printf(out, "L%d:\n", cases[1]);
    mir_emit_c_instruction_word(out, plan->instruction_a_offset);
    mir_stream_printf(out, "\tjp L%d\n", push_result);

    for (item = 2; item <= 17; ++item) {
        int is_store = (item & 1) != 0;
        int indexed =
            (item >= 6 && item <= 9) ||
            item == 12 || item == 13 ||
            item == 16 || item == 17;
        int is_byte = item >= 10;
        int global_memory =
            item == 2 || item == 3 || item == 6 || item == 7 ||
            item == 10 || item == 11 || item == 12 || item == 13;
        int memory_frame =
            global_memory ? MIR_CRUN_GLOBAL_MEMORY :
                            MIR_CRUN_FRAME_MEMORY;

        mir_stream_printf(out, "L%d:\n", cases[item]);
        if (is_store && indexed) {
            mir_stream_printf(out,
                    "\tcall L%d\n"
                    "\tld (ix%d),c\n\tld (ix%d),b\n",
                    pop_two,
                    MIR_CRUN_STACK_POINTER,
                    MIR_CRUN_STACK_POINTER + 1);
            mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_A);
            mir_stream_puts("\tex de,hl\n", out);
            mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_V);
            mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_A);
        } else if (indexed) {
            mir_emit_basic_pop_hl(out);
        } else if (is_store) {
            mir_emit_basic_pop_hl(out);
            mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_V);
        }
        mir_emit_c_memory_address(
            out, memory_frame, indexed, is_byte ? 1 : 2);
        if (is_store) {
            mir_stream_puts("\tpush hl\n", out);
            mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_V);
            mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n", out);
            if (!is_byte)
                mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
            mir_stream_printf(out, "\tjp L%d\n", next);
        } else {
            if (is_byte) {
                mir_stream_puts("\tld l,(hl)\n", out);
                mir_stream_puts("\tld h,0\n", out);
            } else {
                mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
                      "\tex de,hl\n", out);
            }
            mir_stream_printf(out, "\tjp L%d\n", push_result);
        }
    }

    /* Arithmetic and comparisons. */
    for (item = 18; item <= 22; ++item) {
        int nonzero = new_label();
        int arithmetic_done = new_label();

        mir_stream_printf(out,
                "L%d:\n\tcall L%d\n"
                "\tld (ix%d),c\n\tld (ix%d),b\n",
                cases[item], pop_two,
                MIR_CRUN_STACK_POINTER, MIR_CRUN_STACK_POINTER + 1);
        if (item == 21 || item == 22) {
            mir_stream_printf(out,
                    "\tld a,d\n\tor e\n\tjp nz, L%d\n"
                    "\tld hl,0\n\tjp L%d\nL%d:\n",
                    nonzero, arithmetic_done, nonzero);
            mir_emit_runtime_call(
                out, item == 21 ? "__divs" : "__mods");
            mir_stream_printf(out, "L%d:\n", arithmetic_done);
        } else if (item == 18) {
            mir_stream_puts("\tadd hl,de\n", out);
        } else if (item == 19) {
            mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
        } else {
            mir_emit_runtime_call(out, "__mulu");
        }
        mir_stream_printf(out, "\tjp L%d\n", push_result);
    }

    for (item = 23; item <= 24; ++item) {
        int zero = new_label();
        int unary_done = new_label();

        mir_stream_printf(out, "L%d:\n", cases[item]);
        mir_emit_basic_pop_hl(out);
        if (item == 23) {
            mir_stream_puts("\txor a\n\tsub l\n\tld l,a\n"
                  "\tld a,0\n\tsbc a,h\n\tld h,a\n", out);
        } else {
            mir_stream_printf(out,
                    "\tld a,h\n\tor l\n\tjp z, L%d\n"
                    "\tld hl,0\n\tjp L%d\n"
                    "L%d:\n\tld hl,1\nL%d:\n",
                    zero, unary_done, zero, unary_done);
        }
        mir_stream_printf(out, "\tjp L%d\n", push_result);
    }

    for (item = 25; item <= 30; ++item) {
        int true_label = new_label();
        int false_label = new_label();
        int compare_done = new_label();

        mir_stream_printf(out,
                "L%d:\n\tcall L%d\n"
                "\tld (ix%d),c\n\tld (ix%d),b\n",
                cases[item], pop_two,
                MIR_CRUN_STACK_POINTER, MIR_CRUN_STACK_POINTER + 1);
        if (item == 25 || item == 26) {
            mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
            mir_stream_printf(out, "\tjp %s, L%d\n\tjp L%d\n",
                    item == 25 ? "z" : "nz",
                    true_label, false_label);
        } else {
            if (item == 28 || item == 29)
                mir_stream_puts("\tex de,hl\n", out);
            if (item == 27 || item == 29)
                mir_emit_forth_signed_less_branch(
                    out, true_label, false_label);
            else
                mir_emit_forth_signed_less_branch(
                    out, false_label, true_label);
        }
        mir_stream_printf(out,
                "L%d:\n\tld hl,1\n\tjp L%d\n"
                "L%d:\n\tld hl,0\nL%d:\n\tjp L%d\n",
                true_label, compare_done,
                false_label, compare_done, push_result);
    }

    for (item = 31; item <= 33; ++item) {
        int true_label = new_label();
        int false_label = new_label();
        int logical_done = new_label();

        mir_stream_printf(out,
                "L%d:\n\tcall L%d\n"
                "\tld (ix%d),c\n\tld (ix%d),b\n",
                cases[item], pop_two,
                MIR_CRUN_STACK_POINTER, MIR_CRUN_STACK_POINTER + 1);
        if (item == 33) {
            mir_stream_puts("\tld a,l\n\tand e\n\tld l,a\n"
                  "\tld a,h\n\tand d\n\tld h,a\n", out);
        } else {
            mir_stream_puts("\tld a,h\n\tor l\n", out);
            if (item == 31)
                mir_stream_printf(out, "\tjp z, L%d\n", false_label);
            else
                mir_stream_printf(out, "\tjp nz, L%d\n", true_label);
            mir_stream_puts("\tld a,d\n\tor e\n", out);
            if (item == 31)
                mir_stream_printf(out, "\tjp z, L%d\n", false_label);
            else
                mir_stream_printf(out, "\tjp nz, L%d\n", true_label);
            if (item == 31) {
                mir_stream_puts("\tld hl,1\n", out);
                mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,0\n",
                        logical_done, false_label);
            } else {
                mir_stream_puts("\tld hl,0\n", out);
                mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,1\n",
                        logical_done, true_label);
            }
            mir_stream_printf(out, "L%d:\n", logical_done);
        }
        mir_stream_printf(out, "\tjp L%d\n", push_result);
    }

    /* Branches. */
    mir_stream_printf(out, "L%d:\n", cases[34]);
    mir_emit_c_instruction_word(out, plan->instruction_a_offset);
    mir_stream_printf(out, "\tjp L%d\n", set_pc);
    for (item = 35; item <= 36; ++item) {
        mir_stream_printf(out, "L%d:\n", cases[item]);
        mir_emit_basic_pop_hl(out);
        mir_stream_puts("\tld a,h\n\tor l\n", out);
        if (item == 35)
            mir_stream_printf(out, "\tjp nz, L%d\n", next);
        else
            mir_stream_printf(out, "\tjp z, L%d\n", next);
        mir_emit_c_instruction_word(out, plan->instruction_a_offset);
        mir_stream_printf(out, "\tjp L%d\n", set_pc);
    }

    /* Direct and indirect calls. */
    for (item = 37; item <= 38; ++item) {
        mir_stream_printf(out, "L%d:\n", cases[item]);
        if (item == 37) {
            mir_emit_c_instruction_word(out, plan->instruction_a_offset);
            mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_FI);
            mir_emit_c_instruction_word(out, plan->instruction_b_offset);
            mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_B);
        } else {
            mir_emit_basic_pop_hl(out);
            mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_FI);
            mir_stream_puts("\tld hl,0\n", out);
            mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_B);
        }
        mir_emit_c_sync_state(out, plan, state_name);
        mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_B);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, MIR_CRUN_PC);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_FI);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_symbol_call(out, plan->call_function);
        mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n", out);
        mir_emit_c_reload_state(out, plan, state_name);
        mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_FI);
        mir_emit_c_function_address(out, plan);
        if (plan->function_entry_offset != 0)
            mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                    plan->function_entry_offset);
        mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
        mir_stream_printf(out, "\tjp L%d\n", set_pc);
    }

    /* Return. */
    mir_stream_printf(out, "L%d:\n", cases[39]);
    mir_emit_basic_pop_hl(out);
    mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_V);
    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_POINTER);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_RETURN_STACK);
    mir_stream_puts("\tpop de\n\tadd hl,de\n\tadd hl,de\n"
          "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
    mir_emit_forth_frame_store(out, MIR_CRUN_PC);
    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_POINTER);
    mir_stream_puts("\tdec hl\n", out);
    mir_emit_forth_frame_store(out, MIR_CRUN_FRAME_POINTER);
    mir_emit_forth_frame_load(out, MIR_CRUN_PC);
    mir_stream_printf(out, "\tbit 7,h\n\tjp nz, L%d\n", exit_label);
    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_POINTER);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_SIZE);
    mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
    mir_emit_runtime_call(out, "__mulu");
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_FRAME_STORAGE);
    mir_stream_puts("\tadd hl,de\n", out);
    mir_emit_forth_frame_store(out, MIR_CRUN_FRAME_MEMORY);
    mir_emit_forth_frame_load(out, MIR_CRUN_PC);
    mir_stream_puts("\tdec hl\n", out);
    mir_emit_mul_hl_const(out, (unsigned long)plan->instruction_stride);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_CODE);
    mir_stream_puts("\tadd hl,de\n\tpush hl\n\tpop iy\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_V);
    mir_stream_printf(out, "\tjp L%d\n", push_result);

    mir_stream_printf(out, "L%d:\n", cases[40]);
    mir_emit_basic_pop_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    /* printf bytecode. */
    {
        int collect = new_label();
        int collected = new_label();

        mir_stream_printf(out, "L%d:\n", cases[41]);
        mir_emit_c_instruction_word(out, plan->instruction_b_offset);
        mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_A);
        mir_stream_printf(out, "L%d:\n", collect);
        mir_emit_forth_frame_load(out, MIR_CRUN_TEMP_A);
        mir_stream_puts("\tld a,h\n\tor l\n", out);
        mir_stream_printf(out, "\tjp z, L%d\n\tdec hl\n", collected);
        mir_emit_forth_frame_store(out, MIR_CRUN_TEMP_A);
        mir_stream_puts("\tadd hl,hl\n\tpush hl\n\tpush ix\n\tpop hl\n"
              "\tld de,-46\n\tadd hl,de\n\tpop de\n\tadd hl,de\n"
              "\tpush hl\n", out);
        mir_emit_basic_pop_hl(out);
        mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
              out);
        mir_stream_printf(out, "\tjp L%d\nL%d:\n", collect, collected);
        mir_stream_puts("\tpush ix\n\tpop hl\n\tld de,-46\n\tadd hl,de\n\tpush hl\n",
              out);
        mir_emit_c_instruction_word(out, plan->instruction_b_offset);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_forth_frame_load(out, MIR_CRUN_STRINGS);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_c_instruction_word(out, plan->instruction_a_offset);
        mir_stream_puts("\tadd hl,hl\n\tex de,hl\n\tpop hl\n\tadd hl,de\n"
              "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tpush de\n", out);
        mir_emit_symbol_call(out, plan->print_function);
        mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n", out);
        mir_stream_puts("\tld hl,0\n", out);
        mir_stream_printf(out, "\tjp L%d\n", push_result);
    }

    /* Shared stack helpers. */
    mir_stream_printf(out, "L%d:\n", pop_two);
    mir_stream_printf(out,
            "\tld c,(ix%d)\n\tld b,(ix%d)\n"
            "\tdec bc\n\tdec bc\n"
            "\tld l,c\n\tld h,b\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tpush de\n"
            "\tdec bc\n\tdec bc\n"
            "\tld l,c\n\tld h,b\n"
            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
            "\tex de,hl\n\tpop de\n\tret\n",
            MIR_CRUN_STACK_POINTER, MIR_CRUN_STACK_POINTER + 1);
    mir_stream_printf(out, "L%d:\n", push_result);
    mir_emit_basic_push_hl(out);
    mir_stream_printf(out, "\tjp L%d\n", next);

    mir_stream_printf(out, "L%d:\n", next);
    mir_stream_printf(out, "\tld de,%d\n\tadd iy,de\n",
            plan->instruction_stride);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", dispatch, set_pc);
    mir_emit_forth_frame_store(out, MIR_CRUN_PC);
    mir_emit_mul_hl_const(out, (unsigned long)plan->instruction_stride);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, MIR_CRUN_CODE);
    mir_stream_puts("\tadd hl,de\n\tpush hl\n\tpop iy\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", dispatch, exit_label);
    mir_emit_c_sync_state(out, plan, state_name);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tpop iy\n\tret\n", out);

    mir_stream_printf(out, "L%d:\n", no_main);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->no_main_string);
    mir_emit_symbol_call(out, plan->die_function);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", exit_label, bad_opcode);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n", plan->bad_opcode_string);
    mir_emit_symbol_call(out, plan->die_function);
    mir_stream_printf(out, "\tjp L%d\n", exit_label);
}

static void mir_emit_word_insertion_sort(
    MirStream *out, const struct MirWordInsertionSort *plan)
{
    enum {
        SORT_BASE = -2,
        SORT_CURRENT = -4,
        SORT_END = -6,
        SORT_KEY = -8,
        SORT_SCAN = -10
    };
    int outer = new_label();
    int inner = new_label();
    int move = new_label();
    int insert = new_label();
    int done = new_label();

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tbit 7,h\n\tjp nz, L%d\n"
            "\tdec hl\n\tld a,h\n\tor l\n\tjp z, L%d\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            plan->count_offset, plan->count_offset + 1,
            done, done,
            plan->array_offset, plan->array_offset + 1);
    mir_emit_forth_frame_store(out, SORT_BASE);
    mir_stream_puts("\tinc hl\n\tinc hl\n", out);
    mir_emit_forth_frame_store(out, SORT_CURRENT);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tadd hl,hl\n\tex de,hl\n",
            plan->count_offset, plan->count_offset + 1);
    mir_emit_forth_frame_load(out, SORT_BASE);
    mir_stream_puts("\tadd hl,de\n", out);
    mir_emit_forth_frame_store(out, SORT_END);
    mir_stream_printf(out, "L%d:\n", outer);
    mir_emit_forth_frame_load(out, SORT_CURRENT);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, SORT_END);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nc, L%d\n", done);
    mir_emit_forth_frame_load(out, SORT_CURRENT);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
    mir_emit_forth_frame_store(out, SORT_KEY);
    mir_emit_forth_frame_load(out, SORT_CURRENT);
    mir_stream_puts("\tdec hl\n\tdec hl\n", out);
    mir_emit_forth_frame_store(out, SORT_SCAN);
    mir_stream_printf(out, "L%d:\n", inner);
    mir_emit_forth_frame_load(out, SORT_SCAN);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_forth_frame_load(out, SORT_BASE);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c, L%d\n", insert);
    mir_emit_forth_frame_load(out, SORT_SCAN);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n", out);
    mir_emit_forth_frame_load(out, SORT_KEY);
    mir_emit_forth_signed_less_branch(out, move, insert);
    mir_stream_printf(out, "L%d:\n", move);
    mir_emit_forth_frame_load(out, SORT_SCAN);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tinc hl\n"
          "\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_emit_forth_frame_load(out, SORT_SCAN);
    mir_stream_puts("\tdec hl\n\tdec hl\n", out);
    mir_emit_forth_frame_store(out, SORT_SCAN);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", inner, insert);
    mir_emit_forth_frame_load(out, SORT_SCAN);
    mir_stream_puts("\tinc hl\n\tinc hl\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, SORT_KEY);
    mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
          out);
    mir_emit_forth_frame_load(out, SORT_CURRENT);
    mir_stream_puts("\tinc hl\n\tinc hl\n", out);
    mir_emit_forth_frame_store(out, SORT_CURRENT);
    mir_stream_printf(out,
            "\tjp L%d\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            outer, done);
}

static void mir_emit_visit_count(
    MirStream *out, const struct MirVisitCount *plan)
{
    int no_adjust = new_label();
    int done = new_label();

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            plan->count_offset, plan->count_offset + 1);
    mir_emit_mul_hl_const(out, (unsigned long)plan->scale);
    mir_stream_puts("\tld c,l\n\tld b,h\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tld a,h\n\tand 1\n\tld h,a\n"
            "\tpush hl\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tld a,h\n\tand 1\n\tld h,a\n"
            "\tpop de\n\tadd hl,de\n"
            "\tld de,512\n\tor a\n\tsbc hl,de\n"
            "\tjp c, L%d\n\tjp z, L%d\n"
            "\tld h,b\n\tld l,c\n\tld de,32\n\tor a\n\tsbc hl,de\n"
            "\tjp L%d\n"
            "L%d:\n\tld h,b\n\tld l,c\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            plan->offset_offset, plan->offset_offset + 1,
            plan->count_offset, plan->count_offset + 1,
            no_adjust, no_adjust, done,
            no_adjust, done);
}

static void mir_emit_exact_vla_allocate(
    MirStream *out, int count_offset, int saved_sp_offset)
{
    mir_stream_puts("\tld hl,0\n\tadd hl,sp\n", out);
    mir_emit_forth_frame_store(out, saved_sp_offset);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tadd hl,hl\n"
            "\tex de,hl\n\tld hl,0\n\tadd hl,sp\n"
            "\tor a\n\tsbc hl,de\n\tld sp,hl\n",
            count_offset, count_offset + 1);
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
}

static void mir_emit_exact_vla_restore(
    MirStream *out, int saved_sp_offset)
{
    mir_emit_forth_frame_load(out, saved_sp_offset);
    mir_stream_puts("\tld sp,hl\n", out);
}

static void mir_emit_vla_stable(
    MirStream *out, const struct MirVlaStable *plan)
{
    enum {
        FIRST = -2,
        STABLE = -4,
        INDEX = -6,
        SAVED_SP = -8
    };
    int loop = new_label();
    int body = new_label();
    int first = new_label();
    int compare = new_label();
    int restore = new_label();
    int done = new_label();

    mir_stream_puts("\tld hl,0\n", out);
    mir_emit_forth_frame_store(out, FIRST);
    mir_stream_puts("\tld hl,1\n", out);
    mir_emit_forth_frame_store(out, STABLE);
    mir_stream_puts("\tld hl,0\n", out);
    mir_emit_forth_frame_store(out, INDEX);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
            plan->iter_offset, plan->iter_offset + 1);
    mir_emit_forth_signed_less_branch(out, body, done);
    mir_stream_printf(out, "L%d:\n", body);
    mir_emit_exact_vla_allocate(out, plan->count_offset, SAVED_SP);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n\tjp L%d\n", first, compare);
    mir_stream_printf(out, "L%d:\n\tld hl,0\n\tadd hl,sp\n", first);
    mir_emit_forth_frame_store(out, FIRST);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", restore, compare);
    mir_stream_puts("\tld hl,0\n\tadd hl,sp\n\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, FIRST);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n\tld hl,0\n", restore);
    mir_emit_forth_frame_store(out, STABLE);
    mir_stream_printf(out, "L%d:\n", restore);
    mir_emit_exact_vla_restore(out, SAVED_SP);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, INDEX);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done);
    mir_emit_forth_frame_load(out, STABLE);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_nested_vla_stable(
    MirStream *out, const struct MirNestedVlaStable *plan)
{
    enum {
        OUTER_FIRST = -2,
        INNER_FIRST = -4,
        OUTER_STABLE = -6,
        INNER_STABLE = -8,
        OUTER_INDEX = -10,
        INNER_INDEX = -12,
        OUTER_SP = -14,
        INNER_SP = -16
    };
    int outer_loop = new_label();
    int outer_body = new_label();
    int outer_first = new_label();
    int outer_compare = new_label();
    int outer_ready = new_label();
    int inner_loop = new_label();
    int inner_body = new_label();
    int inner_first = new_label();
    int inner_compare = new_label();
    int inner_ready = new_label();
    int inner_done = new_label();
    int outer_done = new_label();
    int return_false = new_label();
    int exit_label = new_label();

    mir_stream_puts("\tld hl,0\n", out);
    mir_emit_forth_frame_store(out, OUTER_FIRST);
    mir_emit_forth_frame_store(out, INNER_FIRST);
    mir_stream_puts("\tld hl,1\n", out);
    mir_emit_forth_frame_store(out, OUTER_STABLE);
    mir_emit_forth_frame_store(out, INNER_STABLE);
    mir_stream_puts("\tld hl,0\n", out);
    mir_emit_forth_frame_store(out, OUTER_INDEX);
    mir_stream_printf(out, "L%d:\n", outer_loop);
    mir_emit_forth_frame_load(out, OUTER_INDEX);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
            plan->outer_offset, plan->outer_offset + 1);
    mir_emit_forth_signed_less_branch(out, outer_body, outer_done);
    mir_stream_printf(out, "L%d:\n", outer_body);
    mir_emit_exact_vla_allocate(out, plan->count_offset, OUTER_SP);
    mir_emit_forth_frame_load(out, OUTER_INDEX);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n\tjp L%d\n", outer_first, outer_compare);
    mir_stream_printf(out, "L%d:\n\tld hl,0\n\tadd hl,sp\n", outer_first);
    mir_emit_forth_frame_store(out, OUTER_FIRST);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", outer_ready, outer_compare);
    mir_stream_puts("\tld hl,0\n\tadd hl,sp\n\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, OUTER_FIRST);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n\tld hl,0\n", outer_ready);
    mir_emit_forth_frame_store(out, OUTER_STABLE);
    mir_stream_printf(out, "L%d:\n\tld hl,0\n", outer_ready);
    mir_emit_forth_frame_store(out, INNER_INDEX);

    mir_stream_printf(out, "L%d:\n", inner_loop);
    mir_emit_forth_frame_load(out, INNER_INDEX);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
            plan->inner_offset, plan->inner_offset + 1);
    mir_emit_forth_signed_less_branch(out, inner_body, inner_done);
    mir_stream_printf(out, "L%d:\n", inner_body);
    mir_emit_exact_vla_allocate(out, plan->count_offset, INNER_SP);
    mir_emit_forth_frame_load(out, OUTER_INDEX);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz, L%d\n", inner_compare);
    mir_emit_forth_frame_load(out, INNER_INDEX);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n", inner_first);
    mir_stream_printf(out, "L%d:\n", inner_compare);
    mir_stream_puts("\tld hl,0\n\tadd hl,sp\n\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, INNER_FIRST);
    mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n\tld hl,0\n", inner_ready);
    mir_emit_forth_frame_store(out, INNER_STABLE);
    mir_stream_printf(out, "\tjp L%d\n", inner_ready);
    mir_stream_printf(out, "L%d:\n\tld hl,0\n\tadd hl,sp\n", inner_first);
    mir_emit_forth_frame_store(out, INNER_FIRST);
    mir_stream_printf(out, "L%d:\n", inner_ready);
    mir_emit_exact_vla_restore(out, INNER_SP);
    mir_emit_forth_frame_load(out, INNER_INDEX);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, INNER_INDEX);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", inner_loop, inner_done);
    mir_emit_exact_vla_restore(out, OUTER_SP);
    mir_emit_forth_frame_load(out, OUTER_INDEX);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, OUTER_INDEX);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", outer_loop, outer_done);
    mir_emit_forth_frame_load(out, OUTER_STABLE);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n", return_false);
    mir_emit_forth_frame_load(out, INNER_STABLE);
    mir_stream_printf(out,
            "\tjp L%d\n"
            "L%d:\n\tld hl,0\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            exit_label, return_false, exit_label);
}

static void mir_emit_vla_loop_result(
    MirStream *out, const struct MirVlaLoopResult *plan)
{
    enum {
        INDEX = -2,
        LAST = -4,
        SAVED_SP = -6
    };
    int loop = new_label();
    int body = new_label();
    int restore = new_label();
    int done = new_label();

    mir_stream_puts("\tld hl,0\n", out);
    mir_emit_forth_frame_store(out, INDEX);
    mir_emit_forth_frame_store(out, LAST);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
            plan->iter_offset, plan->iter_offset + 1);
    mir_emit_forth_signed_less_branch(out, body, done);
    mir_stream_printf(out, "L%d:\n", body);
    mir_emit_exact_vla_allocate(out, plan->count_offset, SAVED_SP);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tadd hl,de\n\tdec hl\n\tld a,l\n\tand 15\n"
            "\tld l,a\n\tld h,0\n",
            plan->count_offset, plan->count_offset + 1);
    mir_emit_forth_frame_store(out, LAST);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_puts("\tbit 0,l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n\tld a,l\n\tand 15\n\tld e,a\n\tld d,0\n",
            restore);
    mir_emit_forth_frame_load(out, LAST);
    mir_stream_puts("\tadd hl,de\n", out);
    mir_emit_forth_frame_store(out, LAST);
    mir_stream_printf(out, "L%d:\n", restore);
    mir_emit_exact_vla_restore(out, SAVED_SP);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_puts("\tinc hl\n", out);
    mir_emit_forth_frame_store(out, INDEX);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done);
    mir_emit_forth_frame_load(out, LAST);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_file_seek_start(
    MirStream *out, const struct MirFileLength *plan)
{
    mir_stream_puts("\tld hl,0\n\tpush hl\n\tld de,0\n\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            plan->file_offset, plan->file_offset + 1);
    mir_emit_symbol_call(out, plan->seek_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n", out);
}

static void mir_emit_file_length(
    MirStream *out, const struct MirFileLength *plan)
{
    const char *buffer_name =
        asm_name_for(sym_asm_name(plan->buffer));
    int loop = new_label();
    int scanned = new_label();
    int add_count = new_label();
    int no_carry = new_label();
    int done = new_label();

    mir_emit_symbol_extrn(out, plan->buffer);
    mir_stream_puts("\tld hl,0\n\tld de,0\n", out);
    mir_emit_frame_long_store(out, -4);
    mir_emit_file_seek_start(out, plan);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n",
            plan->file_offset, plan->file_offset + 1);
    mir_emit_symbol_call(out, plan->eof_function);
    mir_stream_puts("\tpop bc\n\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz, L%d\n", done);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tpush hl\n"
            "\tld hl,%d\n\tpush hl\n"
            "\tld hl,1\n\tpush hl\n"
            "\tld hl,%s\n\tpush hl\n",
            plan->file_offset, plan->file_offset + 1,
            plan->block_size, buffer_name);
    mir_emit_symbol_call(out, plan->read_function);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n"
          "\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n", done);
    mir_stream_printf(out,
            "\tld hl,%s\n\tld bc,%d\n\tld a,%d\n\tcpir\n"
            "\tjp z, L%d\n\tld hl,%d\n\tjp L%d\n"
            "L%d:\n\tld hl,%d\n\tor a\n\tsbc hl,bc\n"
            "L%d:\n\tld c,l\n\tld b,h\n",
            buffer_name, plan->block_size, plan->marker,
            scanned, plan->block_size, add_count,
            scanned, plan->block_size - 1,
            add_count);
    mir_emit_frame_long_load(out, -4);
    mir_stream_printf(out,
            "\tadd hl,bc\n\tjp nc, L%d\n\tinc de\nL%d:\n",
            no_carry, no_carry);
    mir_emit_frame_long_store(out, -4);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done);
    mir_emit_file_seek_start(out, plan);
    mir_emit_frame_long_load(out, -4);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_wide_gcd(
    MirStream *out, const struct MirWideGcd *plan)
{
    enum {
        LEFT = -4,
        RIGHT = -8,
        OLD_RIGHT = -12
    };
    int swap = new_label();
    int ordered = new_label();
    int loop = new_label();
    int done = new_label();

    mir_emit_frame_long_load(out, plan->left_offset);
    mir_emit_frame_long_store(out, LEFT);
    mir_emit_frame_long_load(out, plan->right_offset);
    mir_emit_frame_long_store(out, RIGHT);
    mir_emit_forth_frame_load(out, LEFT + 2);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, RIGHT + 2);
    mir_stream_puts("\tex de,hl\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp c, L%d\n\tjp nz, L%d\n", swap, ordered);
    mir_emit_forth_frame_load(out, LEFT);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, RIGHT);
    mir_stream_puts("\tex de,hl\n\tor a\n\tsbc hl,de\n", out);
    mir_stream_printf(out, "\tjp nc, L%d\nL%d:\n", ordered, swap);
    mir_emit_frame_long_load(out, LEFT);
    mir_emit_frame_long_store(out, OLD_RIGHT);
    mir_emit_frame_long_load(out, RIGHT);
    mir_emit_frame_long_store(out, LEFT);
    mir_emit_frame_long_load(out, OLD_RIGHT);
    mir_emit_frame_long_store(out, RIGHT);
    mir_stream_printf(out, "L%d:\n", ordered);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_emit_frame_long_load(out, RIGHT);
    mir_stream_puts("\tld a,l\n\tor h\n\tor e\n\tor d\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n", done);
    mir_emit_frame_long_store(out, OLD_RIGHT);
    mir_emit_frame_long_load(out, LEFT);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, RIGHT);
    mir_emit_runtime_call(out, "__lmu");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, RIGHT);
    mir_emit_frame_long_load(out, OLD_RIGHT);
    mir_emit_frame_long_store(out, LEFT);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done);
    mir_emit_frame_long_load(out, LEFT);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_add_frame_longs(
    MirStream *out, int left_offset, int right_offset)
{
    mir_emit_frame_long_load(out, left_offset);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, right_offset);
    mir_stream_puts("\tpop bc\n\tadd hl,bc\n\tex de,hl\n"
          "\tpop bc\n\tadc hl,bc\n\tex de,hl\n", out);
}

static void mir_emit_fixed_long_add(
    MirStream *out, const struct MirFixedLongAdd *plan)
{
    enum {
        DESTINATION = -2,
        SOURCE = -4,
        COUNT = -5,
        CARRY = -6
    };
    int loop = new_label();
    int no_input_carry = new_label();
    int negative = new_label();
    int reduce = new_label();
    int reduced = new_label();
    int store = new_label();
    int done = new_label();
    int last_offset = (plan->count - 1) * 4;

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tld de,%d\n\tadd hl,de\n",
            plan->destination_offset, plan->destination_offset + 1,
            last_offset);
    mir_emit_forth_frame_store(out, DESTINATION);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tld de,%d\n\tadd hl,de\n",
            plan->source_offset, plan->source_offset + 1,
            last_offset);
    mir_emit_forth_frame_store(out, SOURCE);
    mir_stream_printf(out, "\tld (ix%d),%d\n\tld (ix%d),0\n",
            COUNT, plan->count, CARRY);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_emit_pointer_long_load(out, DESTINATION);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_pointer_long_load(out, SOURCE);
    mir_stream_puts("\tpop bc\n\tadd hl,bc\n\tex de,hl\n"
          "\tpop bc\n\tadc hl,bc\n\tex de,hl\n"
          "\tld a,(ix-6)\n\tor a\n", out);
    mir_stream_printf(out,
            "\tjp z, L%d\n\tinc hl\n\tjp nz, L%d\n\tinc de\n"
            "L%d:\n\tbit 7,d\n\tjp nz, L%d\n"
            "\tld a,d\n\tor e\n\tjp nz, L%d\n"
            "\tld bc,%ld\n\tor a\n\tsbc hl,bc\n"
            "\tjp nc, L%d\n\tadd hl,bc\n\tjp L%d\n"
            "L%d:\n\tld bc,%ld\n\tor a\n\tsbc hl,bc\n"
            "\tjp nc, L%d\n\tdec de\n"
            "L%d:\n\tld (ix%d),1\n\tjp L%d\n"
            "L%d:\n\tld (ix%d),0\n",
            no_input_carry, no_input_carry,
            no_input_carry, negative,
            reduce,
            plan->base, reduced, negative,
            reduce, plan->base, reduced,
            reduced, CARRY, store,
            negative, CARRY);
    mir_stream_printf(out, "L%d:\n", store);
    mir_emit_pointer_long_store(out, DESTINATION);
    mir_emit_frame_pointer_adjust(out, DESTINATION, -4);
    mir_emit_frame_pointer_adjust(out, SOURCE, -4);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n\tjp L%d\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            COUNT, loop, done, done);
}

static void mir_emit_fixed_long_zero(
    MirStream *out, const struct MirFixedLongZero *plan)
{
    int loop = new_label();
    int nonzero = new_label();
    int done = new_label();

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tld b,%d\n"
            "L%d:\n\tld a,(hl)\n\tinc hl\n\tor (hl)\n\tinc hl\n"
            "\tor (hl)\n\tinc hl\n\tor (hl)\n\tinc hl\n"
            "\tjp nz, L%d\n\tdjnz L%d\n"
            "\tld hl,1\n\tjp L%d\n"
            "L%d:\n\tld hl,0\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            plan->array_offset, plan->array_offset + 1,
            plan->count, loop, nonzero, loop,
            done, nonzero, done);
}

static void mir_emit_fixed_long_mul_div(
    MirStream *out, const struct MirFixedLongMulDiv *plan)
{
    enum {
        POINTER = -2,
        COUNT = -3,
        CARRY = -8,
        REMAINDER = -12,
        VALUE = -16
    };
    int multiply_loop = new_label();
    int divide_loop = new_label();
    int done = new_label();
    int last_offset = (plan->count - 1) * 4;

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n\tld de,%d\n\tadd hl,de\n",
            plan->array_offset, plan->array_offset + 1, last_offset);
    mir_emit_forth_frame_store(out, POINTER);
    mir_stream_printf(out, "\tld (ix%d),%d\n\tld hl,0\n\tld de,0\n",
            COUNT, plan->count);
    mir_emit_frame_long_store(out, CARRY);
    mir_stream_printf(out, "L%d:\n", multiply_loop);
    mir_emit_pointer_long_load(out, POINTER);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, plan->multiplier_offset);
    mir_emit_runtime_call(out, "__lmul");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, VALUE);
    mir_emit_add_frame_longs(out, VALUE, CARRY);
    mir_emit_frame_long_store(out, VALUE);

    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%ld\n\tld de,0\n", plan->base);
    mir_emit_runtime_call(out, "__lms");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_pointer_long_store(out, POINTER);

    mir_emit_frame_long_load(out, VALUE);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%ld\n\tld de,0\n", plan->base);
    mir_emit_runtime_call(out, "__lds");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, CARRY);
    mir_emit_frame_pointer_adjust(out, POINTER, -4);
    mir_stream_printf(out, "\tdec (ix%d)\n\tjp nz, L%d\n",
            COUNT, multiply_loop);

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            plan->array_offset, plan->array_offset + 1);
    mir_emit_forth_frame_store(out, POINTER);
    mir_stream_printf(out, "\tld (ix%d),%d\n\tld hl,0\n\tld de,0\n",
            COUNT, plan->count);
    mir_emit_frame_long_store(out, REMAINDER);
    mir_stream_printf(out, "L%d:\n", divide_loop);
    mir_emit_frame_long_load(out, REMAINDER);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%ld\n\tld de,0\n", plan->base);
    mir_emit_runtime_call(out, "__lmul");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, VALUE);
    mir_emit_pointer_long_load(out, POINTER);
    mir_emit_frame_long_store(out, CARRY);
    mir_emit_add_frame_longs(out, VALUE, CARRY);
    mir_emit_frame_long_store(out, VALUE);

    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, plan->divisor_offset);
    mir_emit_runtime_call(out, "__lds");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_pointer_long_store(out, POINTER);

    mir_emit_frame_long_load(out, VALUE);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, plan->divisor_offset);
    mir_emit_runtime_call(out, "__lms");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, REMAINDER);
    mir_emit_frame_pointer_adjust(out, POINTER, 4);
    mir_stream_printf(out,
            "\tdec (ix%d)\n\tjp nz, L%d\n\tjp L%d\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            COUNT, divide_loop, done, done);
}

static void mir_emit_long_spigot(
    MirStream *out, const struct MirLongSpigot *plan)
{
    enum {
        OUTER = -2,
        INDEX = -4,
        ACCUMULATOR = -8,
        CARRY = -12,
        DIVISOR = -14,
        POINTER = -16,
        VALUE = -20
    };
    const char *array_name =
        asm_name_for(sym_asm_name(plan->array));
    int outer_loop = new_label();
    int inner_loop = new_label();
    int inner_done = new_label();
    int done = new_label();

    mir_emit_symbol_extrn(out, plan->array);
    mir_stream_printf(out,
            "\tld hl,%s\n\tld (hl),%ld\n\tinc hl\n"
            "\tld (hl),%ld\n\tinc hl\n\tld (hl),0\n\tinc hl\n\tld (hl),0\n"
            "\tld hl,%s\n\tld de,%s+4\n\tld bc,%d\n\tldir\n"
            "\tld hl,%d\n",
            array_name, plan->initial_value & 0xffL,
            (plan->initial_value >> 8) & 0xffL,
            array_name, array_name,
            (plan->array_count - 1) * 4,
            plan->outer_start);
    mir_emit_forth_frame_store(out, OUTER);
    mir_stream_puts("\tld hl,0\n\tld de,0\n", out);
    mir_emit_frame_long_store(out, CARRY);

    mir_stream_printf(out, "L%d:\n", outer_loop);
    mir_emit_forth_frame_load(out, OUTER);
    mir_stream_puts("\tbit 7,h\n", out);
    mir_stream_printf(out, "\tjp nz, L%d\n\tld a,h\n\tor l\n\tjp z, L%d\n",
            done, done);
    mir_stream_puts("\tld hl,0\n\tld de,0\n", out);
    mir_emit_frame_long_store(out, ACCUMULATOR);
    mir_emit_forth_frame_load(out, OUTER);
    mir_emit_forth_frame_store(out, INDEX);

    mir_stream_printf(out, "L%d:\n", inner_loop);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_puts("\tadd hl,hl\n\tadd hl,hl\n", out);
    mir_stream_printf(out, "\tld de,%s\n\tadd hl,de\n", array_name);
    mir_emit_forth_frame_store(out, POINTER);
    mir_emit_pointer_long_load(out, POINTER);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%ld\n\tld de,0\n", plan->radix);
    mir_emit_runtime_call(out, "__lmul");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, VALUE);
    mir_emit_add_frame_longs(out, VALUE, ACCUMULATOR);
    mir_emit_frame_long_store(out, ACCUMULATOR);

    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_puts("\tadd hl,hl\n\tdec hl\n", out);
    mir_emit_forth_frame_store(out, DIVISOR);
    mir_emit_frame_long_load(out, ACCUMULATOR);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, DIVISOR);
    mir_stream_puts("\tld de,0\n", out);
    mir_emit_runtime_call(out, "__lms");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_pointer_long_store(out, POINTER);

    mir_emit_frame_long_load(out, ACCUMULATOR);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, DIVISOR);
    mir_stream_puts("\tld de,0\n", out);
    mir_emit_runtime_call(out, "__lds");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, ACCUMULATOR);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_puts("\tdec hl\n", out);
    mir_emit_forth_frame_store(out, INDEX);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n", inner_done);
    mir_emit_frame_long_load(out, ACCUMULATOR);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, INDEX);
    mir_stream_puts("\tld de,0\n", out);
    mir_emit_runtime_call(out, "__lmul");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, ACCUMULATOR);
    mir_stream_printf(out, "\tjp L%d\n", inner_loop);

    mir_stream_printf(out, "L%d:\n", inner_done);
    mir_emit_frame_long_load(out, ACCUMULATOR);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%ld\n\tld de,0\n", plan->radix);
    mir_emit_runtime_call(out, "__lds");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, VALUE);
    mir_emit_add_frame_longs(out, CARRY, VALUE);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,S%d\n\tpush hl\n",
            plan->digit_format_string);
    mir_emit_resolved_symbol_call(
        out, plan->print_function, plan->digit_print_name);
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tld hl,1\n\tpush hl\n", out);
    mir_emit_symbol_call(out, plan->flush_function);
    mir_stream_puts("\tpop bc\n", out);

    mir_emit_frame_long_load(out, ACCUMULATOR);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%ld\n\tld de,0\n", plan->radix);
    mir_emit_runtime_call(out, "__lms");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, CARRY);
    mir_emit_forth_frame_load(out, OUTER);
    mir_stream_printf(out, "\tld de,%d\n\tor a\n\tsbc hl,de\n",
            plan->outer_step);
    mir_emit_forth_frame_store(out, OUTER);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n\tld hl,S%d\n\tpush hl\n",
            outer_loop, done, plan->done_string);
    mir_emit_resolved_symbol_call(
        out, plan->print_function, plan->done_print_name);
    mir_stream_puts("\tpop bc\n\tld hl,0\n\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_parameter_as_long(
    MirStream *out, int offset, int size, int is_unsigned)
{
    if (size == 4) {
        mir_emit_frame_long_load(out, offset);
        return;
    }
    mir_stream_printf(out, "\tld l,(ix%+d)\n", offset);
    if (size == 1) {
        if (is_unsigned)
            mir_stream_puts("\tld h,0\n", out);
        else
            mir_stream_puts("\tld a,l\n\trlca\n\tsbc a,a\n\tld h,a\n", out);
    } else {
        mir_stream_printf(out, "\tld h,(ix%+d)\n", offset + 1);
    }
    if (is_unsigned)
        mir_stream_puts("\tld de,0\n", out);
    else
        mir_stream_puts("\tld a,h\n\trlca\n\tsbc a,a\n\tld e,a\n\tld d,a\n", out);
}

static void mir_emit_wide_frame_less_branch(
    MirStream *out, int left_offset, int right_offset, int is_unsigned,
    int true_label, int false_label)
{
    int high_top_equal = new_label();
    int high_equal = new_label();
    int low_top_equal = new_label();

    mir_emit_forth_frame_load(out, left_offset + 2);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, right_offset + 2);
    mir_stream_puts("\tex de,hl\n", out);
    if (is_unsigned) {
        mir_stream_printf(out,
                "\tld a,h\n\tcp d\n\tjp z, L%d\n"
                "\tjp c, L%d\n\tjp L%d\n",
                high_top_equal, true_label, false_label);
    } else {
        mir_stream_printf(out,
                "\tld a,h\n\txor 128\n\tld b,a\n"
                "\tld a,d\n\txor 128\n\tcp b\n\tjp z, L%d\n"
                "\tjp c, L%d\n\tjp L%d\n",
                high_top_equal, false_label, true_label);
    }
    mir_stream_printf(out,
            "L%d:\n\tld a,l\n\tcp e\n\tjp z, L%d\n"
            "\tjp c, L%d\n\tjp L%d\n",
            high_top_equal, high_equal, true_label, false_label);
    mir_stream_printf(out, "L%d:\n", high_equal);
    mir_emit_forth_frame_load(out, left_offset);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, right_offset);
    mir_stream_puts("\tex de,hl\n", out);
    mir_stream_printf(out,
            "\tld a,h\n\tcp d\n\tjp z, L%d\n"
            "\tjp c, L%d\n\tjp L%d\n"
            "L%d:\n\tld a,l\n\tcp e\n"
            "\tjp c, L%d\n\tjp L%d\n",
            low_top_equal, true_label, false_label,
            low_top_equal, true_label, false_label);
}

static void mir_emit_typed_array_pointer(
    MirStream *out, int index_offset, int delta, int element_size,
    int base_offset)
{
    int nonnegative = new_label();

    mir_emit_forth_frame_load(out, index_offset);
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n\tbit 7,h\n\tjp z, L%d\n",
            delta, nonnegative);
    mir_emit_forth_frame_load(out, index_offset);
    mir_stream_puts("\txor a\n\tsub l\n\tld l,a\n\tsbc a,a\n\tsub h\n\tld h,a\n",
          out);
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", delta);
    mir_stream_printf(out, "L%d:\n", nonnegative);
    if (element_size >= 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    if (element_size == 4)
        mir_stream_puts("\tadd hl,hl\n", out);
    mir_stream_puts("\tex de,hl\n", out);
    mir_emit_forth_frame_load(out, base_offset);
    mir_stream_puts("\tadd hl,de\n", out);
}

static void mir_emit_typed_array_load(
    MirStream *out, int index_offset, int delta,
    const struct MirTypedArrayConditions *plan, int base_offset)
{
    mir_emit_typed_array_pointer(
        out, index_offset, delta, plan->element_size, base_offset);
    if (plan->element_size == 1) {
        mir_stream_puts("\tld l,(hl)\n", out);
        if (plan->is_unsigned)
            mir_stream_puts("\tld h,0\n", out);
        else
            mir_stream_puts("\tld a,l\n\trlca\n\tsbc a,a\n\tld h,a\n", out);
    } else {
        mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    }
}

static void mir_emit_typed_array_update(
    MirStream *out, int index_offset, int amount,
    const struct MirTypedArrayConditions *plan, int base_offset)
{
    mir_emit_typed_array_pointer(
        out, index_offset, 14, plan->element_size, base_offset);
    mir_stream_puts("\tpush hl\n", out);
    if (plan->element_size == 1) {
        mir_stream_puts("\tld l,(hl)\n\tld h,0\n", out);
    } else {
        mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    }
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n\tex de,hl\n\tpop hl\n"
                 "\tld (hl),e\n",
            amount);
    if (plan->element_size >= 2)
        mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
    if (plan->element_size == 4)
        mir_stream_puts("\tinc hl\n\tld (hl),0\n\tinc hl\n\tld (hl),0\n", out);
}

static void mir_emit_typed_array_condition(
    MirStream *out, const struct MirTypedArrayConditions *plan,
    int index_offset, int base_offset, int relation, int amount)
{
    int true_label = new_label();
    int false_label = new_label();

    mir_emit_typed_array_load(out, index_offset, 12, plan, base_offset);
    mir_emit_forth_frame_store(out, -12);
    mir_emit_typed_array_load(out, index_offset, 13, plan, base_offset);
    mir_emit_forth_frame_store(out, -14);
    if (relation == '=') {
        mir_emit_forth_frame_load(out, -12);
        mir_stream_puts("\tex de,hl\n", out);
        mir_emit_forth_frame_load(out, -14);
        mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
        mir_stream_printf(out, "\tjp z, L%d\n\tjp L%d\n", true_label, false_label);
    } else {
        int left_offset = relation == '>' || relation == 'l' ? -14 : -12;
        int right_offset = relation == '>' || relation == 'l' ? -12 : -14;
        int less_true = relation == '>' || relation == '<';

        mir_emit_forth_frame_load(out, left_offset);
        mir_stream_puts("\tex de,hl\n", out);
        mir_emit_forth_frame_load(out, right_offset);
        mir_stream_puts("\tex de,hl\n", out);
        if (plan->is_unsigned) {
            mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
            mir_stream_printf(out, less_true
                    ? "\tjp c, L%d\n\tjp L%d\n"
                    : "\tjp c, L%d\n\tjp L%d\n",
                    less_true ? true_label : false_label,
                    less_true ? false_label : true_label);
        } else {
            mir_emit_forth_signed_less_branch(
                out,
                less_true ? true_label : false_label,
                less_true ? false_label : true_label);
        }
    }
    mir_stream_printf(out, "L%d:\n", true_label);
    mir_emit_typed_array_update(
        out, index_offset, amount, plan, base_offset);
    mir_stream_printf(out, "L%d:\n", false_label);
}

static void mir_emit_typed_array_conditions(
    MirStream *out, const struct MirTypedArrayConditions *plan)
{
    enum {
        INDEX = -4,
        MAXIMUM = -8,
        ARRAY_BASE = -10,
        LEFT = -12,
        RIGHT = -14
    };
    int frame_bytes = 14 + plan->count * plan->element_size;
    int init_loop = new_label();
    int loop = new_label();
    int body = new_label();
    int done = new_label();

    mir_stream_puts("\tpush ix\n\tpop hl\n", out);
    mir_stream_printf(out, "\tld de,-%d\n\tadd hl,de\n", frame_bytes);
    mir_emit_forth_frame_store(out, ARRAY_BASE);
    mir_stream_puts("\tld b,50\n\txor a\n", out);
    mir_emit_forth_frame_load(out, ARRAY_BASE);
    mir_stream_printf(out, "L%d:\n\tld (hl),a\n\tinc hl\n", init_loop);
    if (plan->element_size >= 2)
        mir_stream_puts("\tld (hl),0\n\tinc hl\n", out);
    if (plan->element_size == 4)
        mir_stream_puts("\tld (hl),0\n\tinc hl\n\tld (hl),0\n\tinc hl\n", out);
    mir_stream_printf(out, "\tinc a\n\tdjnz L%d\n", init_loop);
    mir_emit_parameter_as_long(
        out, plan->minimum_offset, plan->element_size, plan->is_unsigned);
    mir_emit_frame_long_store(out, INDEX);
    mir_emit_parameter_as_long(
        out, plan->maximum_offset, plan->element_size, plan->is_unsigned);
    mir_emit_frame_long_store(out, MAXIMUM);
    mir_stream_printf(out, "L%d:\n", loop);
    mir_emit_wide_frame_less_branch(
        out, INDEX, MAXIMUM,
        plan->element_size == 4 && plan->is_unsigned,
        body, done);
    mir_stream_printf(out, "L%d:\n", body);
    mir_emit_typed_array_condition(out, plan, INDEX, ARRAY_BASE, '>', 3);
    mir_emit_typed_array_condition(out, plan, INDEX, ARRAY_BASE, '<', 7);
    mir_emit_typed_array_condition(out, plan, INDEX, ARRAY_BASE, 'g', 3);
    mir_emit_typed_array_condition(out, plan, INDEX, ARRAY_BASE, 'l', 7);
    mir_emit_typed_array_condition(out, plan, INDEX, ARRAY_BASE, '=', 9);
    mir_emit_frame_long_load(out, INDEX);
    mir_stream_puts("\tinc hl\n\tld a,h\n\tor l\n", out);
    {
        int no_carry = new_label();
        mir_stream_printf(out, "\tjp nz, L%d\n\tinc de\nL%d:\n",
                no_carry, no_carry);
    }
    mir_emit_frame_long_store(out, INDEX);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", loop, done);
    mir_emit_forth_frame_load(out, ARRAY_BASE);
    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
            10 * plan->element_size);
    if (plan->element_size == 1) {
        mir_stream_puts("\tld l,(hl)\n", out);
        if (plan->is_unsigned)
            mir_stream_puts("\tld h,0\n", out);
        else
            mir_stream_puts("\tld a,l\n\trlca\n\tsbc a,a\n\tld h,a\n", out);
    } else if (plan->element_size == 2) {
        mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    } else {
        mir_stream_puts("\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n\tinc hl\n"
              "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
              "\tld l,c\n\tld h,b\n", out);
    }
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_float_binary_frames(
    MirStream *out, int left_offset, int right_offset, const char *helper)
{
    mir_emit_frame_long_load(out, left_offset);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, right_offset);
    mir_emit_runtime_call(out, helper);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
}

static void mir_emit_float_binary_constant(
    MirStream *out, int left_offset, unsigned long right_bits,
    const char *helper)
{
    mir_emit_frame_long_load(out, left_offset);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_float_bits(out, right_bits);
    mir_emit_runtime_call(out, helper);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
}

static void mir_emit_float_compare_constant(
    MirStream *out, int left_offset, unsigned long right_bits,
    const char *helper, int true_label, int false_label)
{
    mir_emit_float_binary_constant(out, left_offset, right_bits, helper);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz, L%d\n\tjp L%d\n", true_label, false_label);
}

static void mir_emit_wide_factorial(
    MirStream *out, const struct MirWideFactorial *plan)
{
    const struct MirInsn *call = &mir.insns[17];
    struct Sym *callee = find_global(call->name);
    const char *assembly_name = asm_name_for(
        callee != NULL ? sym_asm_name(callee)
                       : mir_declared_link_name(call->name));
    int done = new_label();
    int recurse = new_label();

    mir_emit_frame_long_load(out, plan->parameter_offset);
    mir_stream_puts("\tld a,l\n\tor h\n\tor e\n\tor d\n", out);
    mir_stream_printf(out, "\tjp nz, L%d\n\tld hl,1\n\tld de,0\n"
                 "\tjp L%d\nL%d:\n",
            recurse, done, recurse);
    mir_emit_frame_long_load(out, plan->parameter_offset);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, plan->parameter_offset);
    mir_stream_puts("\tpush de\n\tpush hl\n\tld hl,1\n\tld de,0\n", out);
    mir_stream_puts("\tld b,h\n\tld c,l\n\tpop hl\n\tor a\n"
          "\tsbc hl,bc\n\tld b,h\n\tld c,l\n\tpop hl\n"
          "\tsbc hl,de\n\tld d,b\n\tld e,c\n\tex de,hl\n",
          out);
    mir_stream_printf(out, "\tpush de\n\tpush hl\n\tcall %s\n"
                 "\tpop bc\n\tpop bc\n",
            assembly_name);
    mir_emit_runtime_call(out, "__lmul");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_printf(out, "L%d:\n", done);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_float_exp(
    MirStream *out, const struct MirFloatExp *plan)
{
    enum {
        SUM = -4,
        TERM = -8,
        R = -12,
        N = -14,
        I = -15
    };
    int return_maximum = new_label();
    int check_lower = new_label();
    int return_zero = new_label();
    int calculate = new_label();
    int check_rounding = new_label();
    int after_rounding = new_label();
    int series_loop = new_label();
    int series_done = new_label();
    int scale_down = new_label();
    int scale_up_loop = new_label();
    int scale_down_loop = new_label();
    int return_sum = new_label();
    int exit_label = new_label();

    mir_emit_float_compare_constant(
        out, plan->parameter_offset, plan->upper_bits, "__fltf",
        return_maximum, check_lower);
    mir_stream_printf(out, "L%d:\n", check_lower);
    mir_emit_float_compare_constant(
        out, plan->parameter_offset,
        plan->upper_bits ^ 0x80000000UL,
        "__fgtf", return_zero, calculate);
    mir_stream_printf(out, "L%d:\n", calculate);
    mir_emit_float_binary_constant(
        out, plan->parameter_offset, plan->ln2_bits, "__fdf");
    mir_emit_runtime_call(out, "__ffi");
    mir_emit_forth_frame_store(out, N);
    mir_emit_float_compare_constant(
        out, plan->parameter_offset, 0, "__fgtf",
        check_rounding, after_rounding);
    mir_stream_printf(out, "L%d:\n", check_rounding);
    mir_emit_float_binary_constant(
        out, plan->parameter_offset, plan->ln2_bits, "__fdf");
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, N);
    mir_emit_runtime_call(out, "__fif");
    mir_emit_runtime_call(out, "__fnef");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n\tld a,(ix%d)\n\tdec (ix%d)\n"
                 "\tor a\n\tjp nz, L%d\n\tdec (ix%d)\n",
            after_rounding, N, N, after_rounding, N + 1);
    mir_stream_printf(out, "L%d:\n", after_rounding);
    mir_emit_frame_long_load(out, plan->parameter_offset);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, N);
    mir_emit_runtime_call(out, "__fif");
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_float_bits(out, plan->ln2_bits);
    mir_emit_runtime_call(out, "__fmf");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_runtime_call(out, "__fsf");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, R);
    mir_emit_float_bits(out, plan->one_bits);
    mir_emit_frame_long_store(out, SUM);
    mir_emit_frame_long_store(out, TERM);
    mir_stream_printf(out, "\tld (ix%d),1\n", I);
    mir_stream_printf(out, "L%d:\n", series_loop);
    mir_stream_printf(out,
            "\tld a,(ix%d)\n\tcp %d\n\tjp nc,L%d\n",
            I, plan->term_limit + 1, series_done);
    mir_emit_float_binary_frames(out, TERM, R, "__fmf");
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld l,(ix%d)\n\tld h,0\n", I);
    mir_emit_runtime_call(out, "__fuf");
    mir_emit_runtime_call(out, "__fdf");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, TERM);
    mir_emit_float_binary_frames(out, SUM, TERM, "__faf");
    mir_emit_frame_long_store(out, SUM);
    mir_stream_printf(out, "\tinc (ix%d)\n", I);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", series_loop, series_done);
    mir_emit_forth_frame_load(out, N);
    mir_stream_puts("\tbit 7,h\n", out);
    mir_stream_printf(out, "\tjp nz, L%d\nL%d:\n",
            scale_down, scale_up_loop);
    mir_emit_forth_frame_load(out, N);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n", return_sum);
    mir_emit_float_binary_constant(out, SUM, plan->two_bits, "__fmf");
    mir_emit_frame_long_store(out, SUM);
    mir_stream_printf(out, "\tld a,(ix%d)\n\tdec (ix%d)\n\tor a\n",
            N, N);
    mir_stream_printf(out, "\tjp nz, L%d\n\tdec (ix%d)\n\tjp L%d\n",
            scale_up_loop, N + 1, scale_up_loop);
    mir_stream_printf(out, "L%d:\n", scale_down);
    mir_stream_printf(out, "L%d:\n", scale_down_loop);
    mir_emit_forth_frame_load(out, N);
    mir_stream_puts("\tbit 7,h\n", out);
    mir_stream_printf(out, "\tjp z, L%d\n", return_sum);
    mir_emit_float_binary_constant(out, SUM, plan->two_bits, "__fdf");
    mir_emit_frame_long_store(out, SUM);
    mir_stream_printf(out, "\tinc (ix%d)\n", N);
    mir_stream_printf(out, "\tjp nz, L%d\n\tinc (ix%d)\n\tjp L%d\n",
            scale_down_loop, N + 1, scale_down_loop);
    mir_stream_printf(out, "L%d:\n", return_sum);
    mir_emit_frame_long_load(out, SUM);
    mir_stream_printf(out, "\tjp L%d\n", exit_label);
    mir_stream_printf(out, "L%d:\n", return_maximum);
    mir_emit_float_bits(out, plan->maximum_bits);
    mir_stream_printf(out, "\tjp L%d\n", exit_label);
    mir_stream_printf(out,
            "L%d:\n\tld hl,0\n\tld de,0\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            return_zero, exit_label);
}

static void mir_emit_float_log(
    MirStream *out, const struct MirFloatLog *plan)
{
    enum {
        M = -4,
        Y = -8,
        Y2 = -12,
        TERM = -16,
        SUM = -20,
        N = -22,
        I = -24
    };
    int domain_ok = new_label();
    int reduce_down = new_label();
    int reduce_up_loop = new_label();
    int reduce_down_loop = new_label();
    int reduced = new_label();
    int series = new_label();
    int series_done = new_label();

    mir_emit_float_compare_constant(
        out, plan->parameter_offset, 0, "__fgef",
        reduce_down, domain_ok);
    mir_stream_printf(out, "L%d:\n", reduce_down);
    mir_emit_float_bits(out, plan->maximum_bits ^ 0x80000000UL);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
    mir_stream_printf(out, "L%d:\n\tld hl,0\n", domain_ok);
    mir_emit_forth_frame_store(out, N);
    mir_emit_frame_long_load(out, plan->parameter_offset);
    mir_emit_frame_long_store(out, M);
    mir_emit_float_compare_constant(
        out, M, plan->one_bits, "__flef",
        reduce_up_loop, reduce_down_loop);

    mir_stream_printf(out, "L%d:\n", reduce_up_loop);
    mir_emit_float_compare_constant(
        out, M, plan->two_bits, "__flef", series, reduced);
    mir_stream_printf(out, "L%d:\n", series);
    mir_emit_float_binary_constant(out, M, plan->two_bits, "__fdf");
    mir_emit_frame_long_store(out, M);
    mir_stream_printf(out, "\tinc (ix%d)\n", N);
    {
        int no_carry = new_label();
        mir_stream_printf(out, "\tjp nz, L%d\n\tinc (ix%d)\nL%d:\n",
                no_carry, N + 1, no_carry);
    }
    mir_stream_printf(out, "\tjp L%d\n", reduce_up_loop);

    mir_stream_printf(out, "L%d:\n", reduce_down_loop);
    mir_emit_float_compare_constant(
        out, M, plan->one_bits, "__fgtf", series_done, reduced);
    mir_stream_printf(out, "L%d:\n", series_done);
    mir_emit_float_binary_constant(out, M, plan->two_bits, "__fmf");
    mir_emit_frame_long_store(out, M);
    mir_stream_printf(out, "\tld a,(ix%d)\n\tdec (ix%d)\n\tor a\n",
            N, N);
    {
        int no_borrow = new_label();
        mir_stream_printf(out, "\tjp nz, L%d\n\tdec (ix%d)\nL%d:\n",
                no_borrow, N + 1, no_borrow);
    }
    mir_stream_printf(out, "\tjp L%d\n", reduce_down_loop);

    mir_stream_printf(out, "L%d:\n", reduced);
    mir_emit_float_binary_constant(out, M, plan->one_bits, "__fsf");
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_float_binary_constant(out, M, plan->one_bits, "__faf");
    mir_emit_runtime_call(out, "__fdf");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, Y);
    mir_emit_float_binary_frames(out, Y, Y, "__fmf");
    mir_emit_frame_long_store(out, Y2);
    mir_emit_float_bits(out, 0);
    mir_emit_frame_long_store(out, SUM);
    mir_emit_frame_long_load(out, Y);
    mir_emit_frame_long_store(out, TERM);
    mir_stream_puts("\tld hl,1\n", out);
    mir_emit_forth_frame_store(out, I);
    {
        int term_loop = new_label();
        int term_done = new_label();
        mir_stream_printf(out, "L%d:\n", term_loop);
        mir_emit_forth_frame_load(out, I);
        mir_stream_printf(out, "\tld de,%d\n\tor a\n\tsbc hl,de\n",
                plan->term_limit + 1);
        mir_stream_printf(out, "\tjp nc, L%d\n", term_done);
        mir_emit_frame_long_load(out, SUM);
        mir_stream_puts("\tpush de\n\tpush hl\n", out);
        mir_emit_frame_long_load(out, TERM);
        mir_stream_puts("\tpush de\n\tpush hl\n", out);
        mir_emit_forth_frame_load(out, I);
        mir_emit_runtime_call(out, "__fif");
        mir_emit_runtime_call(out, "__fdf");
        mir_stream_puts("\tpop bc\n\tpop bc\n", out);
        mir_emit_runtime_call(out, "__faf");
        mir_stream_puts("\tpop bc\n\tpop bc\n", out);
        mir_emit_frame_long_store(out, SUM);
        mir_emit_float_binary_frames(out, TERM, Y2, "__fmf");
        mir_emit_frame_long_store(out, TERM);
        mir_emit_forth_frame_load(out, I);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", plan->term_step);
        mir_emit_forth_frame_store(out, I);
        mir_stream_printf(out, "\tjp L%d\nL%d:\n", term_loop, term_done);
    }
    mir_emit_float_binary_constant(out, SUM, plan->two_bits, "__fmf");
    mir_emit_frame_long_store(out, SUM);
    mir_emit_frame_long_load(out, SUM);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_forth_frame_load(out, N);
    mir_emit_runtime_call(out, "__fif");
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_float_bits(out, plan->ln2_bits);
    mir_emit_runtime_call(out, "__fmaf");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n", out);
    mir_stream_puts("\tld sp,ix\n\tpop ix\n\tret\n", out);
}

static void mir_emit_fixed_long_copy(
    MirStream *out, const struct MirFixedLongCopy *plan)
{
    mir_stream_printf(out,
            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld bc,%d\n\tldir\n"
            "\tld sp,ix\n\tpop ix\n\tret\n",
            plan->destination_offset, plan->destination_offset + 1,
            plan->source_offset, plan->source_offset + 1,
            plan->count * plan->stride);
}

static void mir_emit_long_div_small(
    MirStream *out, const struct MirLongDivSmall *plan)
{
    int loop = new_label();
    int exit = new_label();

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld (ix-2),l\n\tld (ix-1),h\n"
            "\tld (ix-3),%d\n"
            "\tld hl,0\n\tld de,0\n",
            plan->array_offset, plan->array_offset + 1,
            plan->count);
    mir_emit_frame_long_store(out, -7);
    mir_stream_printf(out,
            "L%d:\n\tld a,(ix-3)\n\tor a\n\tjp z, L%d\n",
            loop, exit);
    mir_emit_frame_long_load(out, -7);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_stream_printf(out, "\tld hl,%ld\n\tld de,0\n", plan->base & 0xffffL);
    mir_emit_runtime_call(out, "__lmul");
    mir_stream_puts("\tpop bc\n\tpop bc\n\tpush de\n\tpush hl\n", out);
    mir_emit_pointer_long_load(out, -2);
    mir_stream_puts("\tpop bc\n\tadd hl,bc\n\tex de,hl\n"
          "\tpop bc\n\tadc hl,bc\n\tex de,hl\n", out);
    mir_emit_frame_long_store(out, -11);

    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, plan->divisor_offset);
    mir_emit_runtime_call(out, "__lds");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_pointer_long_store(out, -2);

    mir_emit_frame_long_load(out, -11);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_frame_long_load(out, plan->divisor_offset);
    mir_emit_runtime_call(out, "__lms");
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    mir_emit_frame_long_store(out, -7);
    mir_emit_frame_pointer_adjust(out, -2, 4);
    mir_stream_printf(out,
            "\tdec (ix-3)\n\tjp L%d\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            loop, exit);
}

static void mir_emit_long_add_signed(
    MirStream *out, const struct MirLongAddSigned *plan)
{
    int subtract = new_label();
    int add_loop = new_label();
    int add_no_carry = new_label();
    int add_reduce = new_label();
    int add_no_borrow = new_label();
    int add_reduced = new_label();
    int add_no_reduce = new_label();
    int add_store = new_label();
    int sub_loop = new_label();
    int sub_no_carry = new_label();
    int sub_nonzero_low = new_label();
    int sub_nonnegative = new_label();
    int sub_no_overflow = new_label();
    int sub_store = new_label();
    int finish = new_label();
    int last_offset = (plan->count - 1) * 4;

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,%d\n\tadd hl,de\n"
            "\tld (ix-2),l\n\tld (ix-1),h\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tld de,%d\n\tadd hl,de\n"
            "\tld (ix-4),l\n\tld (ix-3),h\n"
            "\tld (ix-5),%d\n\tld (ix-6),0\n"
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tbit 7,h\n\tjp nz, L%d\n"
            "\tld a,h\n\tor l\n\tjp z, L%d\n",
            plan->destination_offset, plan->destination_offset + 1,
            last_offset,
            plan->source_offset, plan->source_offset + 1,
            last_offset,
            plan->count,
            plan->sign_offset, plan->sign_offset + 1,
            subtract, subtract);

    mir_stream_printf(out, "L%d:\n", add_loop);
    mir_emit_pointer_long_load(out, -2);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_pointer_long_load(out, -4);
    mir_stream_puts("\tpop bc\n\tadd hl,bc\n\tex de,hl\n"
          "\tpop bc\n\tadc hl,bc\n\tex de,hl\n"
          "\tld a,(ix-6)\n\tor a\n\tjp z, L", out);
    mir_stream_printf(out, "%d\n\tinc hl\n\tjp nz, L%d\n\tinc de\nL%d:\n",
            add_no_carry, add_no_carry, add_no_carry);
    mir_stream_printf(out,
            "\tbit 7,d\n\tjp nz, L%d\n"
            "\tld a,d\n\tor e\n\tjp nz, L%d\n"
            "\tld bc,%ld\n\tor a\n\tsbc hl,bc\n"
            "\tjp nc, L%d\n\tadd hl,bc\n\tjp L%d\n"
            "L%d:\n"
            "\tld bc,%ld\n\tor a\n\tsbc hl,bc\n"
            "\tjp nc, L%d\n\tdec de\n"
            "L%d:\nL%d:\n\tld (ix-6),1\n\tjp L%d\n"
            "L%d:\n\tld (ix-6),0\n"
            "L%d:\n",
            add_no_reduce, add_reduce, plan->base & 0xffffL,
            add_reduced, add_no_reduce, add_reduce,
            plan->base & 0xffffL, add_no_borrow,
            add_no_borrow, add_reduced, add_store,
            add_no_reduce, add_store);
    mir_emit_pointer_long_store(out, -2);
    mir_emit_frame_pointer_adjust(out, -2, -4);
    mir_emit_frame_pointer_adjust(out, -4, -4);
    mir_stream_printf(out,
            "\tdec (ix-5)\n\tjp nz, L%d\n\tjp L%d\n",
            add_loop, finish);

    mir_stream_printf(out, "L%d:\nL%d:\n", subtract, sub_loop);
    mir_emit_pointer_long_load(out, -4);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_pointer_long_load(out, -2);
    mir_stream_puts("\tpop bc\n\tor a\n\tsbc hl,bc\n\tex de,hl\n"
          "\tpop bc\n\tsbc hl,bc\n\tex de,hl\n"
          "\tld a,(ix-6)\n\tor a\n", out);
    mir_stream_printf(out,
            "\tjp z, L%d\n\tld a,h\n\tor l\n"
            "\tjp nz, L%d\n\tdec hl\n\tdec de\n\tjp L%d\n"
            "L%d:\n\tdec hl\n"
            "L%d:\n"
            "\tbit 7,d\n\tjp z, L%d\n"
            "\tld bc,%ld\n\tadd hl,bc\n\tjp nc, L%d\n\tinc de\n"
            "L%d:\n\tld (ix-6),1\n\tjp L%d\n"
            "L%d:\n\tld (ix-6),0\n"
            "L%d:\n",
            sub_no_carry, sub_nonzero_low, sub_no_carry,
            sub_nonzero_low, sub_no_carry,
            sub_nonnegative,
            plan->base & 0xffffL, sub_no_overflow,
            sub_no_overflow, sub_store,
            sub_nonnegative, sub_store);
    mir_emit_pointer_long_store(out, -2);
    mir_emit_frame_pointer_adjust(out, -2, -4);
    mir_emit_frame_pointer_adjust(out, -4, -4);
    mir_stream_printf(out,
            "\tdec (ix-5)\n\tjp nz, L%d\n"
            "L%d:\n\tld sp,ix\n\tpop ix\n\tret\n",
            sub_loop, finish);
}

static void mir_emit_byte_sieve(
    MirStream *out, const struct MirByteSieve *plan)
{
    const char *flags_name =
        asm_name_for(sym_asm_name(plan->flags));
    const char *print_name =
        asm_name_for(sym_asm_name(plan->print_function));
    int outer = new_label();
    int scan = new_label();
    int scan_tail = new_label();
    int mark = new_label();
    int mark_in_range = new_label();
    int mark_done = new_label();
    int scan_done = new_label();

    if ((plan->flags->storage == SC_EXTERN ||
         plan->flags->needs_extrn) &&
        mir_extrn_should_emit(plan->flags))
        mir_stream_printf(out, "\textrn %s\n", flags_name);
    if ((plan->print_function->storage == SC_EXTERN ||
         plan->print_function->needs_extrn) &&
        mir_extrn_should_emit(plan->print_function))
        mir_stream_printf(out, "\textrn %s\n", print_name);
    mir_stream_puts("\tld (ix-3),1\n", out);
    mir_stream_printf(out,
            "L%d:\n"
            "\tld hl,0\n\tld (ix-2),l\n\tld (ix-1),h\n"
            "\tld hl,%s\n\tld (hl),%d\n"
            "\tld de,%s+1\n\tld bc,%d\n\tldir\n"
            "\tld bc,0\n\tld hl,%s\n",
            outer,
            flags_name, plan->true_value & 0xff,
            flags_name, plan->maximum_index,
            flags_name);
    mir_stream_printf(out,
            "L%d:\n\tld a,(hl)\n\tor a\n\tjp z, L%d\n"
            "\tpush bc\n\tpush hl\n",
            scan, scan_tail);
    mir_stream_puts("\tld h,b\n\tld l,c\n\tadd hl,hl\n"
          "\tinc hl\n\tinc hl\n\tinc hl\n"
          "\tld b,h\n\tld c,l\n\tpop hl\n\tadd hl,bc\n", out);
    mir_stream_printf(out,
            "\tjp c, L%d\n\tld de,%s+%d\n"
            "L%d:\n"
            "\tld a,h\n\tcp d\n\tjp c, L%d\n\tjp nz, L%d\n"
            "\tld a,l\n\tcp e\n\tjp nc, L%d\n"
            "L%d:\n\tld (hl),0\n\tadd hl,bc\n"
            "\tjp c, L%d\n\tjp L%d\n"
            "L%d:\n\tpop bc\n\tld hl,%s\n\tadd hl,bc\n",
            mark_done, flags_name, plan->maximum_index + 1,
            mark,
            mark_in_range, mark_done,
            mark_done,
            mark_in_range,
            mark_done, mark,
            mark_done, flags_name);
    mir_stream_printf(out,
            "\tinc (ix-2)\n\tjp nz, L%d\n\tinc (ix-1)\n"
            "L%d:\n\tinc hl\n\tinc bc\n"
            "\tld a,b\n\tcp %d\n\tjp c, L%d\n\tjp nz, L%d\n"
            "\tld a,c\n\tcp %d\n\tjp c, L%d\n"
            "L%d:\n\tinc (ix-3)\n\tld a,(ix-3)\n"
            "\tcp %d\n\tjp c, L%d\n",
            scan_tail,
            scan_tail,
            ((plan->maximum_index + 1) >> 8) & 0xff,
            scan, scan_done,
            (plan->maximum_index + 1) & 0xff,
            scan,
            scan_done,
            plan->pass_count + 1, outer);
    mir_stream_printf(out,
            "\tld l,(ix-2)\n\tld h,(ix-1)\n\tpush hl\n"
            "\tld hl,S%d\n\tpush hl\n\tcall %s\n"
            "\tpop bc\n\tpop bc\n"
            "\tld hl,0\n\tld sp,ix\n\tpop ix\n\tret\n",
            plan->format_string, print_name);
}

static void mir_emit_wide_byte_store(
    MirStream *out, const struct MirWideByteStore *store)
{
    int offset;

    if (getenv("DCC_MIR_LOW_BYTE_INDEX_LOOP_REPORT") != NULL)
        fprintf(stderr,
                "; MIR wide-byte-store function=%s byte-offset=%d\n",
                mir.name, store->byte_offset);
    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
            store->base_offset, store->base_offset + 1);
    for (offset = 0; offset < store->byte_offset; ++offset)
        mir_stream_puts("\tinc hl\n", out);
    mir_stream_printf(out, "\tld a,(ix%+d)\n\tld (hl),a\n",
            store->source_offset);
}

static void mir_emit_wide_byte_pack_return(
    MirStream *out, const struct MirWideBytePackReturn *pack)
{
    if (getenv("DCC_MIR_LOW_BYTE_INDEX_LOOP_REPORT") != NULL)
        fprintf(stderr,
                "; MIR wide-byte-pack-return function=%s\n",
                mir.name);
    mir_stream_printf(out,
            "\tld c,(ix%+d)\n\tld b,(ix%+d)\n"
            "\tld a,(bc)\n\tld d,a\n\tinc bc\n"
            "\tld a,(bc)\n\tld e,a\n\tinc bc\n"
            "\tld a,(bc)\n\tld h,a\n\tinc bc\n"
            "\tld a,(bc)\n\tld l,a\n",
            pack->base_offset, pack->base_offset + 1);
    mir_emit_virtual_iy_epilogue(out);
}

static void mir_emit_restore_virtual_iy(MirStream *out)
{
    if (!mir_virtual_iy_base)
        return;
    mir_stream_puts("\tpush ix\n\tpop iy\n", out);
    if (mir_effective_local_bytes() + mir.aggregate_temp_bytes != 0)
        mir_stream_printf(out, "\tld bc,-%d\n\tadd iy,bc\n",
                mir_effective_local_bytes() + mir.aggregate_temp_bytes);
}

static int mir_frame_word_uses_short_ix(int offset)
{
    return offset >= -128 && offset + 1 <= 127;
}

static void mir_emit_frame_word_store(MirStream *out, int offset)
{
    if (mir_frame_word_uses_short_ix(offset)) {
        mir_stream_printf(out, "\tld (ix%+d),l\n\tld (ix%+d),h\n",
                offset, offset + 1);
    } else {
        mir_stream_puts("\tex de,hl\n\tpush ix\n\tpop hl\n", out);
        mir_stream_printf(out, "\tld bc,%d\n\tadd hl,bc\n"
                     "\tld (hl),e\n\tinc hl\n\tld (hl),d\n\tex de,hl\n",
                offset);
    }
}

static void mir_emit_frame_word_load(MirStream *out, int offset)
{
    if (mir_frame_word_uses_short_ix(offset)) {
        mir_stream_printf(out, "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
                offset, offset + 1);
    } else {
        mir_stream_puts("\tpush ix\n\tpop hl\n", out);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n"
                     "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n",
                offset);
    }
}

static void mir_emit_zero_equality(MirStream *out, int operation)
{
    int true_label = new_label();
    int end_label = new_label();

    mir_stream_puts("\tld a,h\n\tor l\n\tld hl,0\n", out);
    mir_stream_printf(out, operation == TOK_EQ ? "\tjp z,L%d\n" : "\tjp nz,L%d\n",
            true_label);
    mir_stream_printf(out,
            "\tjp L%d\nL%d:\n\tinc l\nL%d:\n",
            end_label, true_label, end_label);
}

static int mir_emit_scalar_operation(MirStream *out, const struct MirInsn *insn)
{
    switch ((int)insn->immediate) {
    case '+': mir_stream_puts("\tadd hl,de\n", out); return 1;
    case '-': mir_stream_puts("\tor a\n\tsbc hl,de\n", out); return 1;
    case '&':
        mir_stream_puts("\tld a,h\n\tand d\n\tld h,a\n\tld a,l\n\tand e\n\tld l,a\n", out);
        return 1;
    case '|':
        mir_stream_puts("\tld a,h\n\tor d\n\tld h,a\n\tld a,l\n\tor e\n\tld l,a\n", out);
        return 1;
    case '^':
        mir_stream_puts("\tld a,h\n\txor d\n\tld h,a\n\tld a,l\n\txor e\n\tld l,a\n", out);
        return 1;
    case '*': mir_emit_runtime_call(out, "__mulu"); return 1;
    case '/':
        mir_emit_runtime_call(
            out, (insn->type & TYPE_UNSIGNED) != 0 ? "__divu" : "__divs");
        return 1;
    case '%':
        mir_emit_runtime_call(
            out, (insn->type & TYPE_UNSIGNED) != 0 ? "__modu" : "__mods");
        return 1;
    case TOK_EQ: case TOK_NE: case '<': case '>': case TOK_LE: case TOK_GE:
        {
            const struct MirInsn *left = mir_definition(insn->src1);
            const struct MirInsn *right = mir_definition(insn->src2);
            int is_unsigned =
                (left != NULL &&
                 mir_type_uses_unsigned_comparison(left->type)) ||
                (right != NULL &&
                 mir_type_uses_unsigned_comparison(right->type));
            mir_emit_scalar_compare(out, (int)insn->immediate, is_unsigned);
        }
        return 1;
    case TOK_SHL: case TOK_SHR:
        {
            const struct MirInsn *left = mir_definition(insn->src1);
            mir_emit_scalar_shift(out, (int)insn->immediate,
                                  left != NULL &&
                                  (left->type & TYPE_UNSIGNED) != 0,
                                  insn->src2);
        }
        return 1;
    default:
        return 0;
    }
}

static int mir_emit_spilled_phi_copies(MirStream *out, int predecessor,
                                       int successor);
static int mir_phi_copies_are_empty(int predecessor, int successor);
static int mir_phi_copy_is_slot_identity(int source, int destination);

static const char *mir_invert_z80_condition(const char *condition)
{
    if (strcmp(condition, "z") == 0) return "nz";
    if (strcmp(condition, "nz") == 0) return "z";
    if (strcmp(condition, "c") == 0) return "nc";
    if (strcmp(condition, "nc") == 0) return "c";
    return NULL;
}

/* Item T32 (mir-text-size-plan.md): every fused comparison branch below
 * previously always emitted the three-instruction "branch over a jump"
 * shape - `jp <true_condition>,Lfallthrough` / (phi copies) /
 * `jp Ltarget` / `Lfallthrough:` - even when there were no phi copies to
 * run on the false edge. dccpeep's own pass_branch_over_jump
 * (peep_pass_control_flow.c) already collapses exactly this shape into a
 * single `jp <inverse>,Ltarget` whenever the two instructions between the
 * conditional jump and its target label are just an unconditional jump, so
 * the peeped .COM was never actually paying for the extra jump - only the
 * pre-peephole selector-acceptance byte count was. Emitting the already-
 * collapsed form directly here means the census-visible generated_bytes
 * (which decide MIR text-size acceptance) match what dccpeep would have
 * produced anyway, unlocking acceptance for functions that were only over
 * budget by this redundant jump, with zero change to the final peeped
 * binary. The three-instruction shape is still required, unchanged, when
 * there are phi copies pending on the false edge (they must run
 * conditionally, so they cannot be replaced by a single unconditional
 * branch to `target`). */
static int mir_emit_conditional_branch_with_phi_copies(
    MirStream *out, const int *labels, const char *true_condition,
    int predecessor, int target, int branch_label)
{
    int fallthrough_label;
    int fallthrough = predecessor + 1;
    int false_copies_empty =
        mir_phi_copies_are_empty(predecessor, target);
    int true_copies_empty =
        fallthrough >= mir.count ||
        mir_phi_copies_are_empty(predecessor, fallthrough);
    const char *inverse;

    if (false_copies_empty) {
        inverse = mir_invert_z80_condition(true_condition);
        if (inverse != NULL) {
            mir_stream_printf(out, "\tjp %s, L%d\n", inverse, labels[branch_label]);
            if (!true_copies_empty &&
                !mir_emit_spilled_phi_copies(
                    out, predecessor, fallthrough))
                return 0;
            return 1;
        }
    }
    fallthrough_label = new_label();
    mir_stream_printf(out, "\tjp %s, L%d\n", true_condition, fallthrough_label);
    if (!mir_emit_spilled_phi_copies(out, predecessor, target))
        return 0;
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", labels[branch_label], fallthrough_label);
    if (!true_copies_empty &&
        !mir_emit_spilled_phi_copies(out, predecessor, fallthrough))
        return 0;
    return 1;
}

static int mir_emit_wide_mask_zero_branch(
    MirStream *out, const int *labels,
    const struct MirWideMaskZeroBranch *plan)
{
    static const char registers[4] = { 'l', 'h', 'e', 'd' };
    const struct MirInsn *branch =
        &mir.insns[plan->branch_instruction];
    const char *true_condition =
        plan->comparison == TOK_EQ ? "z" : "nz";
    int target = mir_find_label(branch->label);
    int first = 1;
    int flags_set = 0;
    int byte;

    if (target < 0)
        return 0;
    if (plan->source_is_direct)
        mir_stream_printf(out,
                "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
                "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
                plan->source_offset, plan->source_offset + 1,
                plan->source_offset + 2, plan->source_offset + 3);
    else
        mir_emit_virtual_load_wide(out, plan->source_value);
    for (byte = 0; byte < 4; ++byte) {
        unsigned int mask =
            (unsigned int)((plan->mask >> (8 * byte)) & 0xffUL);

        if (mask == 0)
            continue;
        if (!first)
            mir_stream_puts("\tld c,a\n", out);
        mir_stream_printf(out, "\tld a,%c\n", registers[byte]);
        if (mask != 255)
            mir_stream_printf(out, "\tand %u\n", mask);
        if (mask != 255)
            flags_set = 1;
        if (!first) {
            mir_stream_puts("\tor c\n", out);
            flags_set = 1;
        }
        first = 0;
    }
    if (!flags_set)
        mir_stream_puts("\tor a\n", out);
    if (getenv("DCC_MIR_LOW_BYTE_INDEX_LOOP_REPORT") != NULL)
        fprintf(stderr,
                "; MIR wide-mask-zero-branch function=%s mask=%lu\n",
                mir.name, plan->mask);
    return mir_emit_conditional_branch_with_phi_copies(
        out, labels, true_condition, plan->branch_instruction,
        target, branch->label);
}

/* Item 1 (mir-migration-plan-100): when a scalar comparison feeds nothing
 * but the MIR_BRANCH_FALSE that immediately follows it, mir_emit_scalar_compare
 * would otherwise materialize an explicit 0/1 boolean into HL, spill it to a
 * backend slot, and reload it just so the branch can re-test it with
 * `ld a,h / or l`. Test the flags directly instead and jump straight to the
 * branch's targets, eliding the materialize/spill/reload entirely.
 *
 * Item 4 extends this through a single intervening logical-not: `!(a OP b)`
 * feeding a branch is exactly the branch on the complementary operator, so
 * `mir_binary_is_fusable_comparison` returns 2 (skip compare, not, branch)
 * instead of 1 (skip compare, branch) and the caller negates the operator. */
static int mir_negate_comparison_operator(int operation)
{
    switch (operation) {
    case TOK_EQ: return TOK_NE;
    case TOK_NE: return TOK_EQ;
    case '<': return TOK_GE;
    case TOK_GE: return '<';
    case '>': return TOK_LE;
    default: return '>'; /* TOK_LE */
    }
}

static int mir_binary_is_fusable_comparison(int i)
{
    const struct MirInsn *insn = &mir.insns[i];
    const struct MirInsn *next;

    if (insn->opcode != MIR_BINARY)
        return 0;
    switch ((int)insn->immediate) {
    case TOK_EQ: case TOK_NE: case '<': case '>': case TOK_LE: case TOK_GE:
        break;
    default:
        return 0;
    }
    if (mir_value_use_count(insn->dst) != 1 || i + 1 >= mir.count)
        return 0;
    next = &mir.insns[i + 1];
    if (next->opcode == MIR_BRANCH_FALSE && next->src1 == insn->dst)
        return 1;
    if (next->opcode == MIR_UNARY && next->immediate == '!' &&
        next->src1 == insn->dst && !mir_value_is_wide(next->src1) &&
        mir_value_use_count(next->dst) == 1 && i + 2 < mir.count &&
        mir.insns[i + 2].opcode == MIR_BRANCH_FALSE &&
        mir.insns[i + 2].src1 == next->dst)
        return 2;
    return 0;
}

static int mir_truth_parameter_comparison(const struct MirInsn *compare,
                                          int *parameter_value)
{
    const struct MirInsn *inner;
    const struct MirInsn *left;
    const struct MirInsn *parameter;
    const struct MirInsn *right;

    if (compare == NULL || mir_value_use_count(compare->dst) != 1)
        return 0;
    if (compare->opcode == MIR_UNARY && compare->immediate == '!') {
        inner = mir_definition(compare->src1);
        if (inner == NULL || inner->opcode != MIR_UNARY ||
            inner->immediate != '!' ||
            mir_value_use_count(inner->dst) != 1)
            return 0;
        parameter = mir_definition(inner->src1);
        if (parameter == NULL || parameter->opcode != MIR_PARAM ||
            type_size(parameter->type) > 2)
            return 0;
        *parameter_value = inner->src1;
        return 1;
    }
    if (compare->opcode != MIR_BINARY ||
        compare->immediate != TOK_NE ||
        type_size(compare->secondary_offset) > 2)
        return 0;
    left = mir_definition(compare->src1);
    right = mir_definition(compare->src2);
    if (left == NULL || left->opcode != MIR_PARAM ||
        type_size(left->type) > 2 ||
        right == NULL || right->opcode != MIR_CONST ||
        (right->immediate & 0xffffL) != 0)
        return 0;
    *parameter_value = compare->src1;
    return 1;
}

static int mir_nested_truth_comparison(int instruction,
                                       int *left_parameter,
                                       int *right_parameter)
{
    const struct MirInsn *outer;
    const struct MirInsn *left;
    const struct MirInsn *right;

    if (instruction < 0 || instruction >= mir.count ||
        mir_has_phi_instruction())
        return 0;
    outer = &mir.insns[instruction];
    if (outer->opcode != MIR_BINARY || outer->immediate != TOK_NE ||
        type_size(outer->secondary_offset) > 2 ||
        mir_binary_is_fusable_comparison(instruction) != 1)
        return 0;
    left = mir_definition(outer->src1);
    right = mir_definition(outer->src2);
    return mir_truth_parameter_comparison(left, left_parameter) &&
           mir_truth_parameter_comparison(right, right_parameter);
}

/* A pair of one-use `param != 0` booleans consumed by `(left != right)` and
 * an immediate branch can compare the original parameter truth values
 * directly.  Do not allocate backend slots or materialize either inner
 * boolean; mir_emit_nested_truth_comparison_branch emits the complete branch
 * at the outer comparison. */
static int mir_value_is_nested_truth_comparison_input(int value)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        int left_parameter;
        int right_parameter;
        const struct MirInsn *outer = &mir.insns[instruction];

        if ((outer->src1 == value || outer->src2 == value) &&
            mir_nested_truth_comparison(instruction, &left_parameter,
                                        &right_parameter))
            return 1;
    }
    return 0;
}

static int mir_float_madd_match(int add_index, int *multiply_index,
                                int *addend_value)
{
    const struct MirInsn *add;
    const struct MirInsn *multiply;
    int candidate;
    int addend;

    if (add_index <= 0 || add_index >= mir.count)
        return 0;
    add = &mir.insns[add_index];
    if (add->opcode != MIR_BINARY || add->immediate != '+' ||
        !type_is_float(add->type) ||
        !type_is_float(add->secondary_offset) ||
        type_size(add->secondary_offset) != 4)
        return 0;
    candidate = add_index - 1;
    multiply = &mir.insns[candidate];
    addend = add->src1;
    if (multiply->opcode != MIR_BINARY || multiply->immediate != '*' ||
        !type_is_float(multiply->type) ||
        !type_is_float(multiply->secondary_offset) ||
        type_size(multiply->secondary_offset) != 4 ||
        mir_value_use_count(multiply->dst) != 1 ||
        add->src2 != multiply->dst)
        return 0;
    if (multiply_index != NULL)
        *multiply_index = candidate;
    if (addend_value != NULL)
        *addend_value = addend;
    return 1;
}

static int mir_float_multiply_is_fused(int multiply_index)
{
    int matched_multiply;

    return multiply_index + 1 < mir.count &&
           mir_float_madd_match(multiply_index + 1, &matched_multiply, NULL) &&
           matched_multiply == multiply_index;
}

static int mir_narrow_multiply_has_named_word_home(
    const struct MirInsn *source)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (source == NULL ||
        (source->opcode != MIR_LOAD && source->opcode != MIR_PARAM) ||
        !mir_scalar_memory_location(
            source, &memory_type, &memory_storage, &memory_offset) ||
        type_size(memory_type) != 2)
        return 0;
    if (memory_storage == SC_GLOBAL || memory_storage == SC_EXTERN)
        return 1;
    return (memory_storage == SC_LOCAL ||
            memory_storage == SC_PARAM) &&
           memory_offset >= -128 && memory_offset + 1 <= 127;
}

static int mir_plain_u16_widen_source(int value, int *source_value)
{
    const struct MirInsn *widen = mir_definition(value);
    const struct MirInsn *source;

    if (widen == NULL || widen->opcode != MIR_UNARY ||
        widen->immediate != 0 || type_size(widen->type) != 4 ||
        type_is_float(widen->type) ||
        (widen->type & TYPE_UNSIGNED) == 0)
        return 0;
    source = mir_definition(widen->src1);
    if (source == NULL || source->opcode != MIR_PARAM ||
        type_size(source->type) > 2 ||
        type_is_float(source->type) || type_ptr_depth(source->type) > 0 ||
        (source->type & TYPE_UNSIGNED) == 0)
        return 0;
    if (source_value != NULL)
        *source_value = widen->src1;
    return 1;
}

static int mir_value_is_m1q_bounded(int value, int depth)
{
    const struct MirInsn *definition;
    const struct MirInsn *left;
    const struct MirInsn *right;
    long constant;

    if (depth > 8 || (definition = mir_definition(value)) == NULL ||
        type_is_float(definition->type) ||
        type_ptr_depth(definition->type) != 0)
        return 0;
    if (type_size(definition->type) == 1)
        return 1;
    switch (definition->opcode) {
    case MIR_CONST:
        return definition->immediate >= -255L &&
               definition->immediate <= 255L;
    case MIR_UNARY:
        if (definition->immediate == 0 ||
            definition->immediate == '+' ||
            definition->immediate == '-')
            return mir_value_is_m1q_bounded(
                definition->src1, depth + 1);
        return definition->immediate == '!';
    case MIR_BINARY:
        left = mir_definition(definition->src1);
        right = mir_definition(definition->src2);
        if (definition->immediate == '&') {
            const struct MirInsn *mask =
                left != NULL && left->opcode == MIR_CONST
                    ? left : right;

            return mask != NULL && mask->opcode == MIR_CONST &&
                   mask->immediate >= 0 &&
                   mask->immediate <= 255;
        }
        if (definition->immediate == '%' &&
            right != NULL && right->opcode == MIR_CONST) {
            constant = right->immediate;
            return constant >= -256L && constant <= 256L &&
                   constant != 0;
        }
        if (definition->immediate == TOK_SHR &&
            right != NULL && right->opcode == MIR_CONST &&
            type_size(definition->secondary_offset) <= 2)
            return right->immediate >= 8 &&
                   right->immediate < 16;
        return 0;
    case MIR_PHI:
        return mir_value_is_m1q_bounded(
                   definition->src1, depth + 1) &&
               mir_value_is_m1q_bounded(
                   definition->src2, depth + 1);
    default:
        return 0;
    }
}

static int mir_narrow_multiply_widen_source(
    int value, int *source_value, int *is_unsigned)
{
    const struct MirInsn *widen = mir_definition(value);
    const struct MirInsn *source;
    int source_unsigned;
    int target_unsigned;

    if (widen == NULL || widen->opcode != MIR_UNARY ||
        widen->immediate != 0 || type_size(widen->type) != 4 ||
        type_is_float(widen->type) ||
        mir_value_use_count(widen->dst) != 1)
        return 0;
    source = mir_definition(widen->src1);
    if (source == NULL || type_size(source->type) > 2 ||
        type_is_float(source->type) || type_ptr_depth(source->type) > 0)
        return 0;
    if (!mir_narrow_multiply_has_named_word_home(source) &&
        mir_value_use_count(source->dst) <= 1 &&
        !mir_value_is_m1q_bounded(source->dst, 0))
        return 0;
    source_unsigned = (source->type & TYPE_UNSIGNED) != 0;
    target_unsigned = (widen->type & TYPE_UNSIGNED) != 0;
    if (source_unsigned != target_unsigned &&
        !(type_size(source->type) == 1 &&
          source_unsigned && !target_unsigned))
        return 0;
    if (source_value != NULL)
        *source_value = widen->src1;
    if (is_unsigned != NULL)
        *is_unsigned = target_unsigned;
    return 1;
}

static int mir_wide_narrow_multiply_match(
    const struct MirInsn *multiply, int *left_source,
    int *right_source, int *is_unsigned)
{
    int left_unsigned;
    int right_unsigned;

    if (multiply == NULL || multiply->opcode != MIR_BINARY ||
        multiply->immediate != '*' ||
        type_size(multiply->secondary_offset) != 4 ||
        type_is_float(multiply->secondary_offset) ||
        !mir_narrow_multiply_widen_source(
            multiply->src1, left_source, &left_unsigned) ||
        !mir_narrow_multiply_widen_source(
            multiply->src2, right_source, &right_unsigned) ||
        left_unsigned != right_unsigned)
        return 0;
    if (is_unsigned != NULL)
        *is_unsigned = left_unsigned;
    return 1;
}

static int mir_fold_narrow_multiply_constants(
    const struct MirInsn *left, const struct MirInsn *right,
    int is_unsigned, unsigned long *bits_out)
{
    unsigned long left_bits;
    unsigned long right_bits;

    if (left == NULL || right == NULL ||
        left->opcode != MIR_CONST || right->opcode != MIR_CONST)
        return 0;
    left_bits = (unsigned long)left->immediate & 0xffffUL;
    right_bits = (unsigned long)right->immediate & 0xffffUL;
    if (is_unsigned) {
        *bits_out = left_bits * right_bits;
    } else {
        long left_value = (left_bits & 0x8000UL) != 0
            ? (long)left_bits - 65536L : (long)left_bits;
        long right_value = (right_bits & 0x8000UL) != 0
            ? (long)right_bits - 65536L : (long)right_bits;

        *bits_out = (unsigned long)(left_value * right_value);
    }
    return 1;
}

static int mir_narrow_multiply_source_is_m1q_bounded(
    const struct MirInsn *source)
{
    return source != NULL &&
           mir_value_is_m1q_bounded(source->dst, 0);
}

static int mir_value_is_wide_narrow_multiply_widen(int value)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *multiply = &mir.insns[instruction];

        if (mir_wide_narrow_multiply_match(
                multiply, NULL, NULL, NULL) &&
            (multiply->src1 == value || multiply->src2 == value))
            return 1;
    }
    return 0;
}

static int mir_value_is_m1q_multiply_source(int value)
{
    int instruction;

    if (!mir_value_is_m1q_bounded(value, 0))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        int left_source;
        int right_source;

        if (mir_wide_narrow_multiply_match(
                &mir.insns[instruction], &left_source,
                &right_source, NULL) &&
            (left_source == value || right_source == value))
            return 1;
    }
    return 0;
}

static int mir_instruction_is_in_cfg_cycle(int instruction)
{
    unsigned char *visited;
    int *work;
    int work_count = 0;
    int successor;
    int result = 0;

    if (instruction < 0 || instruction >= mir.count ||
        !mir_insn_is_reachable(instruction))
        return 0;
    visited = (unsigned char *)calloc(
        (size_t)mir.count, sizeof(*visited));
    work = (int *)malloc((size_t)mir.count * sizeof(*work));
    if (visited == NULL || work == NULL) {
        free(visited);
        free(work);
        return 0;
    }
    for (successor = 0;
         successor < mir.insns[instruction].successor_count;
         ++successor) {
        int target = mir.insns[instruction].successors[successor];

        if (target == instruction) {
            result = 1;
            goto done;
        }
        if (target >= 0 && target < mir.count && !visited[target]) {
            visited[target] = 1;
            work[work_count++] = target;
        }
    }
    while (work_count > 0) {
        int current = work[--work_count];

        for (successor = 0;
             successor < mir.insns[current].successor_count;
             ++successor) {
            int target = mir.insns[current].successors[successor];

            if (target == instruction) {
                result = 1;
                goto done;
            }
            if (target >= 0 && target < mir.count &&
                !visited[target]) {
                visited[target] = 1;
                work[work_count++] = target;
            }
        }
    }
done:
    free(visited);
    free(work);
    return result;
}

static int mir_wide_mulmod_match(int modulo_index, int *multiply_index,
                                 int *left_value, int *right_value,
                                 int *modulus_value)
{
    const struct MirInsn *modulo;
    const struct MirInsn *multiply;

    if (modulo_index < 0 || modulo_index >= mir.count)
        return 0;
    modulo = &mir.insns[modulo_index];
    multiply = mir_definition(modulo->src1);
    if (modulo->opcode != MIR_BINARY || modulo->immediate != '%' ||
        type_size(modulo->secondary_offset) != 4 ||
        type_is_float(modulo->secondary_offset) ||
        (modulo->secondary_offset & TYPE_UNSIGNED) == 0 ||
        multiply == NULL || multiply->opcode != MIR_BINARY ||
        multiply->immediate != '*' ||
        type_size(multiply->secondary_offset) != 4 ||
        type_is_float(multiply->secondary_offset) ||
        (multiply->secondary_offset & TYPE_UNSIGNED) == 0 ||
        mir_value_use_count(multiply->dst) != 1 ||
        !mir_plain_u16_widen_source(multiply->src1, left_value) ||
        !mir_plain_u16_widen_source(multiply->src2, right_value) ||
        !mir_plain_u16_widen_source(modulo->src2, modulus_value))
        return 0;
    if (multiply_index != NULL)
        *multiply_index = (int)(multiply - mir.insns);
    return 1;
}

static int mir_wide_multiply_is_fused_mulmod(int multiply_index)
{
    int instruction;

    for (instruction = multiply_index + 1;
         instruction < mir.count; ++instruction) {
        int matched_multiply;
        if (mir_wide_mulmod_match(instruction, &matched_multiply,
                                  NULL, NULL, NULL) &&
            matched_multiply == multiply_index)
            return 1;
    }
    return 0;
}

int mir_spilled_cfg_has_wide_mulmod_fusion(void)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir_wide_mulmod_match(instruction, NULL, NULL, NULL, NULL))
            return 1;
    return 0;
}

static int mir_unary_is_fusable_not_branch(int i)
{
    const struct MirInsn *candidate;
    const struct MirInsn *candidate_source;
    const struct MirInsn *insn;
    const struct MirInsn *next;
    int instruction;

    if (i < 0 || i + 1 >= mir.count)
        return 0;
    for (instruction = 0; instruction + 1 < mir.count; ++instruction) {
        candidate = &mir.insns[instruction];
        if (candidate->opcode != MIR_UNARY ||
            candidate->immediate != '!' ||
            mir.insns[instruction + 1].opcode != MIR_BRANCH_FALSE ||
            mir.insns[instruction + 1].src1 != candidate->dst)
            continue;
        candidate_source = mir_definition(candidate->src1);
        if (candidate_source != NULL &&
            candidate_source->opcode == MIR_CALL &&
            (candidate_source->memory_flags & MIR_CALL_FLAG_VARIADIC) != 0)
            return 0;
    }
    insn = &mir.insns[i];
    next = &mir.insns[i + 1];
    return insn->opcode == MIR_UNARY && insn->immediate == '!' &&
           !mir_value_is_wide(insn->src1) &&
           (mir_definition(insn->src1) == NULL ||
            mir_definition(insn->src1)->opcode != MIR_UNARY) &&
           mir_value_use_count(insn->dst) == 1 &&
           next->opcode == MIR_BRANCH_FALSE && next->src1 == insn->dst;
}

static int mir_match_double_logical_not(
    int outer_instruction, int *source_value)
{
    const struct MirInsn *outer;
    const struct MirInsn *inner;
    const struct MirInsn *source;
    int inner_instruction;
    int slot;
    int instruction;

    if (outer_instruction < 0 || outer_instruction >= mir.count)
        return 0;
    outer = &mir.insns[outer_instruction];
    if (outer->opcode != MIR_UNARY || outer->immediate != '!')
        return 0;
    inner = mir_definition(outer->src1);
    if (inner == NULL || inner->opcode != MIR_UNARY ||
        inner->immediate != '!' ||
        mir_value_use_count(inner->dst) != 1)
        return 0;
    source = mir_definition(inner->src1);
    if (source != NULL && source->opcode == MIR_UNARY &&
        source->immediate == '!')
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *use = &mir.insns[instruction];

        if (use->opcode == MIR_UNARY && use->immediate == '!' &&
            use->src1 == outer->dst)
            return 0;
    }
    /*
     * Fusion moves the source's use from the inner `!` to the outer `!`.
     * It is safe only when the source can be recreated there or its backend
     * slot remains untouched over the extended live range.
     */
    inner_instruction = (int)(inner - mir.insns);
    for (instruction = inner_instruction + 1;
         instruction < outer_instruction;
         ++instruction)
        if (mir.insns[instruction].opcode != MIR_NOP)
            return 0;
    slot = -1;
    if (inner->src1 >= 0 && mir.backend_slots != NULL)
        slot = mir.backend_slots[inner->src1];
    if (slot < 0 &&
        !mir_scalar_constant_is_rematerializable(inner->src1) &&
        !mir_string_address_is_rematerializable(inner->src1) &&
        !mir_address_is_rematerializable(inner->src1) &&
        !mir_value_has_direct_named_home(inner->src1))
        return 0;
    for (instruction = inner_instruction + 1;
         instruction < outer_instruction;
         ++instruction) {
        int dst = mir.insns[instruction].dst;
        if (dst < 0 || dst == inner->src1)
            continue;
        if (slot >= 0 && mir.backend_slots != NULL &&
            mir.backend_slots[dst] == slot)
            return 0;
    }
    if (source_value != NULL)
        *source_value = inner->src1;
    return 1;
}

static int mir_unary_is_elided_double_not_inner(int instruction)
{
    int outer;

    for (outer = instruction + 1; outer < mir.count; ++outer) {
        const struct MirInsn *inner;

        if (!mir_match_double_logical_not(outer, NULL))
            continue;
        inner = mir_definition(mir.insns[outer].src1);
        if (inner == &mir.insns[instruction])
            return 1;
    }
    return 0;
}

/* Item 25 (mir-migration-plan-100): an `==`/`!=` comparison against the
 * constant 0 needs neither operand materialized into DE nor a 16-bit
 * `sbc hl,de` - HL already holds the left operand (loaded unconditionally
 * before every MIR_BINARY), and `ld a,h / or l` sets Z/NZ from it directly.
 * This only applies to the right operand being the zero constant (the
 * overwhelmingly common `x == 0` / `x != 0` source shape); a zero constant
 * on the left (`0 == x`) still loads HL with the constant first, so no
 * benefit is available without reordering operand evaluation, which this
 * item does not attempt. */
static int mir_fused_compare_is_const_zero_rhs(int compare_index)
{
    const struct MirInsn *compare = &mir.insns[compare_index];
    const struct MirInsn *right;

    if (compare->immediate != TOK_EQ && compare->immediate != TOK_NE)
        return 0;
    right = mir_definition(compare->src2);
    return right != NULL && right->opcode == MIR_CONST &&
           (right->immediate & 0xffffL) == 0;
}

static int mir_fused_compare_is_const_zero_lhs(int compare_index)
{
    const struct MirInsn *compare = &mir.insns[compare_index];
    const struct MirInsn *left;

    if (compare->immediate != TOK_EQ && compare->immediate != TOK_NE)
        return 0;
    left = mir_definition(compare->src1);
    return left != NULL && left->opcode == MIR_CONST &&
           (left->immediate & 0xffffL) == 0;
}

/* Item 26 (mir-migration-plan-100): an 8-bit-range `cp`-based fast path for
 * "small constant" comparisons was investigated and deferred. MIR reaches
 * MIR_BINARY only after C's usual arithmetic conversions have already
 * promoted narrower operands to a full 16-bit int - there is no tracking of
 * an operand's pre-promotion original type (or a proven small value range)
 * surviving to this point, so proving either operand is guaranteed to fit in
 * a single byte would require new semantic/range-tracking infrastructure,
 * not a small selector tweak. Deferred until such tracking exists (same
 * "defer, don't guess" rationale as Item 6/Item 14). */

/* Item 27 (mir-migration-plan-100): a signed `<`/`>=` comparison against the
 * constant 0 needs only the sign bit of the left operand - `bit 7,h` sets Z
 * from bit 7 of H directly, with no DE materialization or 16-bit `sbc
 * hl,de` (and no xor-128 sign-flip dance, which exists only to make an
 * unsigned `sbc` behave like a signed compare against a non-zero DE). This
 * only applies when the comparison is provably signed: if either operand is
 * unsigned, "x < 0" is always false and "x >= 0" is always true, which is a
 * different (constant-fold) opportunity this item does not attempt. */
static int mir_fused_compare_is_signed_zero_sign_test(int compare_index)
{
    const struct MirInsn *compare = &mir.insns[compare_index];
    const struct MirInsn *left;
    const struct MirInsn *right;

    if (compare->immediate != '<' && compare->immediate != TOK_GE)
        return 0;
    left = mir_definition(compare->src1);
    right = mir_definition(compare->src2);
    if ((left != NULL && mir_type_uses_unsigned_comparison(left->type)) ||
        (right != NULL && mir_type_uses_unsigned_comparison(right->type)))
        return 0;
    if (right == NULL || right->opcode != MIR_CONST ||
        (right->immediate & 0xffffL) != 0)
        return 0;
    return 1;
}

static int mir_emit_fused_comparison_branch(MirStream *out, const int *labels,
                                             int compare_index, int negate,
                                             int de_holds_biased_constant)
{
    const struct MirInsn *compare = &mir.insns[compare_index];
    const struct MirInsn *branch = &mir.insns[compare_index + 1 + negate];
    const struct MirInsn *left = mir_definition(compare->src1);
    const struct MirInsn *right = mir_definition(compare->src2);
    int is_unsigned =
        (left != NULL && mir_type_uses_unsigned_comparison(left->type)) ||
        (right != NULL && mir_type_uses_unsigned_comparison(right->type));
    int operation = negate ? mir_negate_comparison_operator(
                                 (int)compare->immediate)
                           : (int)compare->immediate;
    int target;
    const char *true_condition;

    if (branch->label < 0 || branch->label >= mir.next_label)
        return 0;
    target = mir_find_label(branch->label);
    if (target < 0)
        return 0;
    if ((operation == TOK_EQ || operation == TOK_NE) &&
        (mir_fused_compare_is_const_zero_rhs(compare_index) ||
         mir_fused_compare_is_const_zero_lhs(compare_index))) {
        /* Item 25: DE was never loaded for this case (the caller skips it
         * on this same const-zero-rhs test), so test HL directly. */
        mir_stream_puts("\tld a,h\n\tor l\n", out);
    } else if ((operation == '<' || operation == TOK_GE) &&
               mir_fused_compare_is_signed_zero_sign_test(compare_index)) {
        /* Item 27: DE was never loaded for this case either (the caller
         * skips it on this same signed-zero-sign-test), so the sign bit of
         * HL is tested directly instead of a 16-bit sbc. `bit 7,h` sets Z
         * when the sign bit is clear (value >= 0) and NZ when it is set
         * (value < 0) - the opposite sense of the c/nc pair the sbc path
         * produces, so this case picks its own true_condition below rather
         * than falling into the shared switch. */
        mir_stream_puts("\tbit 7,h\n", out);
        true_condition = operation == '<' ? "nz" : "z";
        return mir_emit_conditional_branch_with_phi_copies(
            out, labels, true_condition, compare_index + 1 + negate, target,
            branch->label);
    } else if (de_holds_biased_constant &&
               (operation == '<' || operation == TOK_GE)) {
        /* Item T50 (mir-text-size-plan.md): the caller already loaded DE
         * with the compile-time-biased constant (constant ^ 0x8000)
         * instead of the raw value, mirroring
         * mir_emit_homed_binary_instruction's biased_right_constant
         * optimization - only HL's sign bit needs the runtime xor-128
         * flip, not both HL and DE, and the xor's own side effect clears
         * carry so the usual leading `or a` is also unneeded. negate
         * cannot change this eligibility: mir_negate_comparison_operator
         * only ever swaps '<' with TOK_GE (never introduces '>' or
         * TOK_LE from one of these two), so the bias computed for the
         * un-negated compare->immediate remains valid after negation. */
        mir_stream_puts("\tld a,h\n\txor 128\n\tld h,a\n\tsbc hl,de\n", out);
    } else {
        if (operation == '>' || operation == TOK_LE) {
            mir_stream_puts("\tex de,hl\n", out);
            operation = operation == '>' ? '<' : TOK_GE;
        }
        if (!is_unsigned && operation != TOK_EQ && operation != TOK_NE)
            mir_stream_puts("\tld a,h\n\txor 128\n\tld h,a\n"
                  "\tld a,d\n\txor 128\n\tld d,a\n", out);
        mir_stream_puts("\tor a\n\tsbc hl,de\n", out);
    }
    switch (operation) {
    case TOK_EQ: true_condition = "z"; break;
    case TOK_NE: true_condition = "nz"; break;
    case '<': true_condition = "c"; break;
    default: true_condition = "nc"; break; /* TOK_GE */
    }
    return mir_emit_conditional_branch_with_phi_copies(
        out, labels, true_condition, compare_index + 1 + negate, target,
        branch->label);
}

/* Item T2 (mir-text-size-plan): the 32-bit ("wide") comparison operators
 * previously took the same "materialize 0/1 boolean into HL, spill it to a
 * backend slot, reload it, retest with ld a,h/or l" round trip as the 16-bit
 * path did before Item 1 - mir_binary_is_fusable_comparison only recognized
 * 16-bit operands. Every wide comparison shape (inline xor-compare for ==/!=,
 * and the __ltu/__lts/__leu/__les/__lgu/__lgs/__lku/__lks runtime helpers for
 * relational, and the float helpers) already leaves the boolean result as a
 * concrete 0/1 in HL by construction - there is no flag-based short cut
 * available (unlike the 16-bit path's `sbc hl,de`), but skipping the
 * store/reload/retest is still a direct win, and is a strict superset of
 * what the 16-bit fusion already exploits: HL is always tested with
 * `ld a,h / or l` immediately once it is known to be 0 or 1, whether the
 * branch consumes the comparison directly or through a single intervening
 * logical-not (mir_binary_is_fusable_comparison's existing negate signal).
 *
 * Item T53 (mir-text-size-plan.md): Item T2 initially kept a defensive
 * `type_is_float` exclusion in mir_binary_is_fusable_comparison pending
 * independent verification of the float comparison helpers' HL-return
 * convention. Verified directly against DCCRTL.MAC: __feqf, __fnef, __fltf,
 * __fgtf, __flef, and __fgef all end every code path with an explicit
 * `ld hl,1` or `ld hl,0` immediately before `ret` - exactly the same
 * concrete-0/1-in-HL contract this function already relies on for `long`.
 * This function needed no changes at all; only the gate in
 * mir_binary_is_fusable_comparison did. */
static int mir_emit_fused_wide_comparison_branch(MirStream *out, const int *labels,
                                                  int compare_index,
                                                  int negate)
{
    const struct MirInsn *branch = &mir.insns[compare_index + 1 + negate];
    int target;
    const char *true_condition;

    if (branch->label < 0 || branch->label >= mir.next_label)
        return 0;
    target = mir_find_label(branch->label);
    if (target < 0)
        return 0;
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    true_condition = negate ? "z" : "nz";
    return mir_emit_conditional_branch_with_phi_copies(
        out, labels, true_condition, compare_index + 1 + negate, target,
        branch->label);
}

/* Item T45 (mir-text-size-plan.md): ported directly from
 * emit_shift_const_long (dcc_ops.c, the legacy AST backend) - any wide
 * shift count 1..31 decomposes into a whole-byte register-move (0-3
 * bytes, reusing the same move sequences legacy already validated) plus
 * a 0-7 bit remainder unrolled directly, since the count is already
 * known at compile time and a runtime b-counted loop would only add
 * control overhead for no benefit. A count of exactly 0 emits nothing.
 * The caller guarantees count is in [0,31] - counts >= 32 are handled
 * separately (or left on the runtime-loop path) since they are outside
 * the range this decomposition was validated for. Shared with Item T46's
 * multiply-by-power-of-two-constant fast path below, which is always an
 * `is_left` shift regardless of the multiplicand's signedness. */
void mir_emit_wide_shift_by_constant(MirStream *out, int is_left,
                                     int is_unsigned, long count)
{
    int bytes;
    int bits;

    if (count <= 0)
        return;
    bytes = (int)(count / 8);
    bits = (int)(count % 8);

    if (is_left) {
        switch (bytes) {
        case 1: mir_stream_puts("\tld d,e\n\tld e,h\n\tld h,l\n\tld l,0\n", out); break;
        case 2: mir_stream_puts("\tld e,l\n\tld d,h\n\tld hl,0\n", out); break;
        case 3: mir_stream_puts("\tld d,l\n\tld e,0\n\tld hl,0\n", out); break;
        default: break;
        }
        while (bits-- > 0)
            mir_stream_puts("\tadd hl,hl\n\trl e\n\trl d\n", out);
    } else if (is_unsigned) {
        switch (bytes) {
        case 1: mir_stream_puts("\tld l,h\n\tld h,e\n\tld e,d\n\tld d,0\n", out); break;
        case 2: mir_stream_puts("\tld l,e\n\tld h,d\n\tld de,0\n", out); break;
        case 3: mir_stream_puts("\tld l,d\n\tld h,0\n\tld de,0\n", out); break;
        default: break;
        }
        while (bits-- > 0)
            mir_stream_puts("\tsrl d\n\trr e\n\trr h\n\trr l\n", out);
    } else {
        if (bytes > 0) {
            mir_stream_puts("\tld a,d\n\trla\n\tsbc a,a\n", out);
            switch (bytes) {
            case 1: mir_stream_puts("\tld l,h\n\tld h,e\n\tld e,d\n\tld d,a\n", out); break;
            case 2: mir_stream_puts("\tld l,e\n\tld h,d\n\tld e,a\n\tld d,a\n", out); break;
            case 3: mir_stream_puts("\tld l,d\n\tld h,a\n\tld e,a\n\tld d,a\n", out); break;
            default: break;
            }
        }
        while (bits-- > 0)
            mir_stream_puts("\tsra d\n\trr e\n\trr h\n\trr l\n", out);
    }
}

/* log2 of a power-of-two 32-bit unsigned value; returns -1 if v is 0 or
 * not an exact power of two. Ported from ulong_log2_pow2 (dcc_ops.c). Not
 * static: also used by Item T49's scalar (16-bit) unsigned div/mod-by-
 * power-of-2 fast path, including the call site in
 * dcc_mir_emit_common.c. A 16-bit divisor is simply passed through the
 * same 32-bit-wide check unchanged - the power-of-two test and log2
 * computation are correct for any width. */
int mir_ulong_log2_pow2(unsigned long v)
{
    int n;

    if (v == 0 || (v & (v - 1)) != 0)
        return -1;
    n = 0;
    while (v > 1) {
        v >>= 1;
        ++n;
    }
    return n;
}

static int mir_pointer_difference_pow2_shift(
    const struct MirInsn *insn)
{
    const struct MirInsn *difference;
    const struct MirInsn *left;
    const struct MirInsn *right;
    const struct MirInsn *scale;
    int shift;

    if (insn == NULL || insn->opcode != MIR_BINARY ||
        insn->immediate != '/' ||
        type_size(insn->secondary_offset) != 2)
        return -1;
    difference = mir_definition(insn->src1);
    scale = mir_definition(insn->src2);
    if (difference == NULL || difference->opcode != MIR_BINARY ||
        difference->immediate != '-' ||
        scale == NULL || scale->opcode != MIR_CONST ||
        scale->immediate <= 1)
        return -1;
    left = mir_definition(difference->src1);
    right = mir_definition(difference->src2);
    if (left == NULL || right == NULL ||
        type_ptr_depth(left->type) == 0 ||
        type_ptr_depth(right->type) == 0)
        return -1;
    shift = mir_ulong_log2_pow2(
        (unsigned long)scale->immediate);
    return shift > 0 ? shift : -1;
}

/* AND one 16-bit register pair (hi_reg:lo_reg) with a compile-time word
 * mask in place, without a temporary register pair: a byte that is
 * all-ones in the mask is left untouched, a byte that is all-zero
 * collapses to a single immediate load, and anything else gets one
 * immediate `and`. Ported from emit_and_word_const (dcc_ops.c). Not
 * static: also called from dcc_mir_emit_common.c's mir_emit_scalar_value
 * (Item T48) for the trivial-single-return selector's own scalar '&'
 * case, mirroring how mir_emit_scalar_shift is already shared between
 * both files (Item T44). */
void mir_emit_word_and_constant(MirStream *out, char hi_reg, char lo_reg,
                                 unsigned int word_mask)
{
    unsigned int hib = (word_mask >> 8) & 0xffU;
    unsigned int lob = word_mask & 0xffU;

    if (word_mask == 0xffffU)
        return;
    if (word_mask == 0) {
        mir_stream_printf(out, "\tld %c%c,0\n", hi_reg, lo_reg);
        return;
    }
    if (hib == 0)
        mir_stream_printf(out, "\tld %c,0\n", hi_reg);
    else if (hib != 0xffU)
        mir_stream_printf(out, "\tld a,%c\n\tand %u\n\tld %c,a\n", hi_reg, hib, hi_reg);
    if (lob == 0)
        mir_stream_printf(out, "\tld %c,0\n", lo_reg);
    else if (lob != 0xffU)
        mir_stream_printf(out, "\tld a,%c\n\tand %u\n\tld %c,a\n", lo_reg, lob, lo_reg);
}

/* Item T47 (mir-text-size-plan.md): AND the DE:HL long value in place with
 * a compile-time 32-bit mask, byte by byte, instead of the generic
 * push/pop/ex-de-hl AND dance. Ported from emit_and_long_const
 * (dcc_ops.c). */
static void mir_emit_wide_and_constant(MirStream *out, unsigned long mask)
{
    mir_emit_word_and_constant(out, 'd', 'e',
                                (unsigned int)((mask >> 16) & 0xffffUL));
    mir_emit_word_and_constant(out, 'h', 'l', (unsigned int)(mask & 0xffffUL));
}

static int mir_emit_nested_truth_comparison_branch(MirStream *out,
                                                    const int *labels,
                                                    int instruction)
{
    int left_parameter;
    int right_parameter;
    int left_zero_label;
    int done_label;
    const struct MirInsn *branch;

    if (!mir_nested_truth_comparison(instruction, &left_parameter,
                                     &right_parameter))
        return 0;
    branch = &mir.insns[instruction + 1];
    if (branch->label < 0 || branch->label >= mir.next_label)
        return 0;
    left_zero_label = new_label();
    done_label = new_label();
    mir_emit_virtual_load(out, left_parameter);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\n", left_zero_label);
    mir_emit_virtual_load(out, right_parameter);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp nz,L%d\n\tjp L%d\nL%d:\n",
            labels[branch->label], done_label, left_zero_label);
    mir_emit_virtual_load(out, right_parameter);
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    mir_stream_printf(out, "\tjp z,L%d\nL%d:\n",
            labels[branch->label], done_label);
    return 1;
}

/* Checks whether insn (a wide '&') has a MIR_CONST operand and, if so,
 * emits the byte-wise mask-AND fast path and returns 1. Checks src2
 * first (the common `x & CONST` shape - DE:HL already holds src2's
 * now-dead value, discarded via pop hl/pop de to restore src1, the real
 * lhs, from the stack, mirroring Items T45/T46's identical restore
 * sequence), then src1 (the `CONST & x` shape MIR does not canonicalize
 * away - DE:HL already holds src2, the real lhs, needing no restore;
 * the dead constant pushed for src1 is simply discarded via pop bc/pop
 * bc). Returns 0 (emits nothing) if neither operand is a compile-time
 * constant, leaving the caller to fall back to the generic AND path. */
static int mir_emit_wide_and_constant_fastpath(MirStream *out,
                                                const struct MirInsn *insn)
{
    const struct MirInsn *src2_definition = mir_definition(insn->src2);
    const struct MirInsn *src1_definition = mir_definition(insn->src1);

    if (src2_definition != NULL && src2_definition->opcode == MIR_CONST) {
        mir_stream_puts("\tpop hl\n\tpop de\n", out);
        mir_emit_wide_and_constant(out, (unsigned long)src2_definition->immediate);
        return 1;
    }

    if (src1_definition != NULL && src1_definition->opcode == MIR_CONST) {
        mir_stream_puts("\tpop bc\n\tpop bc\n", out);
        mir_emit_wide_and_constant(out, (unsigned long)src1_definition->immediate);
        return 1;
    }
    return 0;
}

static const char *mir_wide_runtime_helper(const struct MirInsn *insn)
{
    int operation = (int)insn->immediate;
    int operand_type = insn->secondary_offset;

    if (type_is_float(operand_type)) {
        if (operation == '+' || operation == '-' || operation == '*' ||
            operation == '/')
            return operation == '+' ? "__faf" : operation == '-' ? "__fsf" :
                   operation == '*' ? "__fmf" : "__fdf";
        if (operation == TOK_EQ || operation == TOK_NE ||
            operation == '<' || operation == '>' ||
            operation == TOK_LE || operation == TOK_GE)
            return operation == TOK_EQ ? "__feqf" :
                   operation == TOK_NE ? "__fnef" :
                   operation == '<' ? "__fgtf" :
                   operation == '>' ? "__fltf" :
                   operation == TOK_LE ? "__fgef" : "__flef";
        return NULL;
    }
    if (operation == '<' || operation == '>' || operation == TOK_LE ||
        operation == TOK_GE) {
        int is_unsigned = (operand_type & TYPE_UNSIGNED) != 0;
        return operation == '<' ? (is_unsigned ? "__ltu" : "__lts") :
               operation == TOK_LE ? (is_unsigned ? "__leu" : "__les") :
               operation == '>' ? (is_unsigned ? "__lgu" : "__lgs") :
               (is_unsigned ? "__lku" : "__lks");
    }
    if (operation == '*') {
        const struct MirInsn *src2_definition =
            mir_definition(insn->src2);
        const struct MirInsn *src1_definition =
            mir_definition(insn->src1);

        if (src2_definition != NULL &&
            src2_definition->opcode == MIR_CONST &&
            mir_ulong_log2_pow2(
                (unsigned long)src2_definition->immediate) > 0)
            return NULL;
        if (src1_definition != NULL &&
            src1_definition->opcode == MIR_CONST &&
            mir_ulong_log2_pow2(
                (unsigned long)src1_definition->immediate) > 0)
            return NULL;
        return "__lmul";
    }
    if (operation == '/')
        return (insn->type & TYPE_UNSIGNED) != 0 ? "__ldu" : "__lds";
    if (operation == '%')
        return (insn->type & TYPE_UNSIGNED) != 0 ? "__lmu" : "__lms";
    return NULL;
}

/* Item T395/follow-on (mir-text-size-plan.md): direct port of the legacy
 * AST backend's emit_signed_long_const_cmp_ast (dcc_ast_gen_expr.c) into
 * the MIR value-materializing wide-comparison path. Legacy never calls
 * __lts/__les/__gts/__ges when the right operand is a signed compile-time
 * long constant - it inlines a sign-flip + 32-bit sbc sequence instead,
 * applying a C+1 bias to reduce '>'/'<=' to '>='/'<' (with a dedicated
 * INT32_MAX early-out, since C+1 would otherwise overflow the bias
 * arithmetic). This function reproduces that sequence exactly, byte for
 * byte, against a literal constant rather than a loaded DE:HL - the
 * caller is responsible for having DE:HL hold the *variable* operand's
 * value (not the constant) before calling this. */
static int mir_emit_wide_signed_const_compare(MirStream *out, int op, long c)
{
    unsigned long threshold = (unsigned long)c & 0xffffffffUL;
    unsigned long biased;
    unsigned int lo;
    unsigned int hi;
    int true_label;
    int end_label;
    int true_on_carry;

    if (op == '>' || op == TOK_LE) {
        if (threshold == 0x7fffffffUL) {
            mir_stream_puts(op == '>' ? "\tld hl,0\n" : "\tld hl,1\n", out);
            return 1;
        }
        threshold = (threshold + 1UL) & 0xffffffffUL;
    }
    biased = (threshold ^ 0x80000000UL) & 0xffffffffUL;
    lo = (unsigned int)(biased & 0xffffUL);
    hi = (unsigned int)((biased >> 16) & 0xffffUL);
    true_on_carry = (op == '<' || op == TOK_LE);
    true_label = new_label();
    end_label = new_label();

    mir_stream_puts("\tld a,d\n\txor 80h\n\tld d,a\n", out);
    mir_stream_printf(out, "\tld bc,%u\n", lo);
    mir_stream_puts("\tor a\n\tsbc hl,bc\n\tex de,hl\n", out);
    mir_stream_printf(out, "\tld bc,%u\n", hi);
    mir_stream_puts("\tsbc hl,bc\n", out);
    mir_stream_printf(out, true_on_carry ? "\tjp c, L%d\n" : "\tjp nc, L%d\n",
            true_label);
    mir_stream_printf(out, "\tld hl,0\n\tjp L%d\nL%d:\n\tld hl,1\nL%d:\n",
            end_label, true_label, end_label);
    return 1;
}

/* Item T395/follow-on: identifies whether mir_emit_wide_operation will
 * take the mir_emit_wide_signed_const_compare fast path for this
 * instruction, so the caller (mir_emit_spilled_scalar_cfg's MIR_BINARY
 * dispatch) can skip loading src2 into DE:HL entirely - the fast path
 * only ever needs src2's compile-time literal value, never its loaded
 * runtime form, and skipping that dead load avoids paying for it at
 * every call site (legacy never materializes the constant into
 * registers for this shape either). */
int mir_wide_operation_is_signed_const_relational(const struct MirInsn *insn)
{
    int operation = (int)insn->immediate;
    const struct MirInsn *right;

    if (operation != '<' && operation != '>' && operation != TOK_LE &&
        operation != TOK_GE)
        return 0;
    if (type_is_float(insn->secondary_offset) ||
        (insn->secondary_offset & TYPE_UNSIGNED) != 0)
        return 0;
    right = mir_definition(insn->src2);
    return right != NULL && right->opcode == MIR_CONST;
}

int mir_emit_wide_operation(MirStream *out, const struct MirInsn *insn)
{
    const char *helper = NULL;
    int operation = (int)insn->immediate;
    int operand_type = insn->secondary_offset;
    if (type_is_float(operand_type)) {
        helper = mir_wide_runtime_helper(insn);
        if (helper == NULL)
            return 0;
        mir_emit_runtime_call(out, helper);
        mir_stream_puts("\tpop bc\n\tpop bc\n", out);
        return 1;
    }
    if (operation == TOK_EQ || operation == TOK_NE) {
        int true_label = new_label();
        int end_label = new_label();
        mir_stream_puts("\tpop bc\n\tld a,c\n\txor l\n\tld l,a\n"
              "\tld a,b\n\txor h\n\tor l\n\tld l,a\n"
              "\tpop bc\n\tld a,c\n\txor e\n\tor l\n\tld l,a\n"
              "\tld a,b\n\txor d\n\tor l\n", out);
        mir_stream_printf(out, operation == TOK_EQ ? "\tjp z, L%d\n" : "\tjp nz, L%d\n",
                true_label);
        mir_stream_printf(out, "\tld hl,0\n\tjp L%d\nL%d:\n\tld hl,1\nL%d:\n",
                end_label, true_label, end_label);
        return 1;
    }
    if (operation == '<' || operation == '>' || operation == TOK_LE ||
        operation == TOK_GE) {
        const struct MirInsn *left = mir_definition(insn->src1);
        const struct MirInsn *right = mir_definition(insn->src2);
        int compares_constant =
            (left != NULL && left->opcode == MIR_CONST) ||
            (right != NULL && right->opcode == MIR_CONST);

        if (compares_constant) {
            if ((operand_type & TYPE_UNSIGNED) != 0)
                mir_spilled_cfg_used_unsigned_wide_constant_relational = 1;
            else
                mir_spilled_cfg_used_signed_wide_constant_relational = 1;
        }
        /* Item T395/follow-on: legacy's emit_signed_long_const_cmp_ast
         * only ever applies when the constant is the *right* operand
         * (ast_const_scalar_fold(n->b, ...) in dcc_ast_gen_expr.c) - a
         * constant on the left still falls through to the runtime
         * helper in legacy too, so leaving that ordering on the
         * existing call-based path below is exactly call-for-call
         * equivalent to legacy, not a missed optimization. The caller
         * (mir_emit_spilled_scalar_cfg) recognizes this same shape via
         * mir_wide_operation_is_signed_const_relational and skips both
         * the push of src1 and the load of src2 - DE:HL already holds
         * src1's value untouched from its own load, exactly what this
         * path needs. */
        if (mir_wide_operation_is_signed_const_relational(insn))
            return mir_emit_wide_signed_const_compare(
                out, operation, right->immediate);
        helper = mir_wide_runtime_helper(insn);
        mir_stream_puts("\tpush de\n\tpush hl\n", out);
        mir_emit_runtime_call(out, helper);
        mir_stream_puts("\tex de,hl\n\tld hl,8\n\tadd hl,sp\n"
              "\tld sp,hl\n\tex de,hl\n", out);
        return 1;
    }
    switch ((int)insn->immediate) {
    case '+':
        mir_stream_puts("\tpop bc\n\tadd hl,bc\n"
              "\tex de,hl\n\tpop bc\n\tadc hl,bc\n\tex de,hl\n", out);
        return 1;
    case '-':
        mir_stream_puts("\tld b,h\n\tld c,l\n\tpop hl\n\tor a\n\tsbc hl,bc\n"
              "\tld b,h\n\tld c,l\n\tpop hl\n\tsbc hl,de\n"
              "\tld d,b\n\tld e,c\n\tex de,hl\n", out);
        return 1;
    case '&': case '|': case '^':
        /* Item T47 (mir-text-size-plan.md): `long_expr & <compile-time
         * constant mask>` skips the generic push/pop/ex-de-hl AND dance
         * entirely in favor of a byte-wise mask apply, mirroring
         * legacy's emit_and_long_const. Only '&' is special-cased here,
         * matching legacy exactly - there is no equivalent OR/XOR
         * constant-mask optimization in the legacy backend to port. */
        if (insn->immediate == '&' &&
            mir_emit_wide_and_constant_fastpath(out, insn))
            return 1;
        {
            const char *operation = insn->immediate == '&' ? "and" :
                                    insn->immediate == '|' ? "or" : "xor";
            mir_stream_printf(out,
                    "\tpop bc\n\tld a,l\n\t%s c\n\tld l,a\n"
                    "\tld a,h\n\t%s b\n\tld h,a\n"
                    "\tex de,hl\n\tpop bc\n"
                    "\tld a,l\n\t%s c\n\tld l,a\n"
                    "\tld a,h\n\t%s b\n\tld h,a\n\tex de,hl\n",
                    operation, operation, operation, operation);
        }
        return 1;
    case TOK_SHL: case TOK_SHR:
        {
            /* Item T45 (mir-text-size-plan.md): the wide (32-bit)
             * counterpart of Item T44's scalar shift unroll - a
             * compile-time-constant shift count still went through the
             * generic runtime bit-loop unconditionally. When insn->src2
             * resolves to a MIR_CONST in the only meaningful range for
             * a 32-bit value (0-31; wider counts are undefined
             * behavior in C and left on the loop path unchanged),
             * decompose into the same whole-byte-move-plus-bit-remainder
             * sequence legacy's emit_shift_const_long already uses (see
             * mir_emit_wide_shift_by_constant above) instead of a
             * generic per-bit unroll - the count is already known, so
             * the loop's `ld a,l` (extracting the runtime count from
             * src2's low byte) and `ld b,a`/`dec b`/`jp` bookkeeping are
             * unneeded entirely, not just the loop body repetition. A
             * shift count of 0 collapses to just the two pops that
             * restore src1 from the stack, with no shift instructions
             * at all. */
            const struct MirInsn *count_definition =
                mir_definition(insn->src2);
            int is_unsigned = (operand_type & TYPE_UNSIGNED) != 0;

            if (count_definition != NULL &&
                count_definition->opcode == MIR_CONST &&
                count_definition->immediate >= 0 &&
                count_definition->immediate < 32) {
                long count = count_definition->immediate;

                mir_stream_puts("\tpop hl\n\tpop de\n", out);
                mir_emit_wide_shift_by_constant(out,
                                                 insn->immediate == TOK_SHL,
                                                 is_unsigned, count);
            } else {
                int loop_label = new_label();
                int done_label = new_label();
                mir_stream_puts("\tld a,l\n\tpop hl\n\tpop de\n\tld b,a\n", out);
                mir_stream_printf(out, "L%d:\n\tld a,b\n\tor a\n\tjp z, L%d\n",
                        loop_label, done_label);
                if (insn->immediate == TOK_SHL)
                    mir_stream_puts("\tadd hl,hl\n\trl e\n\trl d\n", out);
                else if (is_unsigned)
                    mir_stream_puts("\tsrl d\n\trr e\n\trr h\n\trr l\n", out);
                else
                    mir_stream_puts("\tsra d\n\trr e\n\trr h\n\trr l\n", out);
                mir_stream_printf(out, "\tdec b\n\tjp L%d\nL%d:\n",
                        loop_label, done_label);
            }
        }
        return 1;
    case '*':
        {
            /* Item T46 (mir-text-size-plan.md): ported from
             * emit_mul_pow2_long_const (dcc_ops.c) - `long_expr *
             * <compile-time power-of-two constant>` strength-reduces to
             * a left shift instead of a call to the generic __lmul
             * runtime helper, reusing mir_emit_wide_shift_by_constant
             * (Item T45, above). Unlike legacy's AST-level version
             * (which only ever sees the constant in the syntactic right
             * operand position), MIR's lowering does not canonicalize
             * commutative operands - `x * 4L` lowers with the constant
             * as src2, `4L * x` lowers with the constant as src1 - so
             * both positions are checked. When src2 is the constant,
             * DE:HL (already loaded with src2's now-dead value) is
             * discarded and src1 (the real multiplicand) is restored
             * from the stack, mirroring Item T45's shift-fix exactly.
             * When src1 is the constant, DE:HL already holds src2 (the
             * real multiplicand, loaded most recently) and the dead
             * constant left on the stack from src1's evaluation is
             * simply popped off - no register restore needed at all.
             * Declines (falls through to __lmul) for 0, 1, and any
             * non-power-of-two multiplier, matching legacy's exact
             * scope - 0 and 1 are rare enough as literal long
             * multipliers not to be worth special-casing separately. */
            const struct MirInsn *src2_definition =
                mir_definition(insn->src2);
            const struct MirInsn *src1_definition =
                mir_definition(insn->src1);

            if (src2_definition != NULL &&
                src2_definition->opcode == MIR_CONST) {
                int shift = mir_ulong_log2_pow2(
                    (unsigned long)src2_definition->immediate);
                if (shift > 0) {
                    mir_stream_puts("\tpop hl\n\tpop de\n", out);
                    mir_emit_wide_shift_by_constant(out, 1, 1, shift);
                    return 1;
                }
            }
            if (src1_definition != NULL &&
                src1_definition->opcode == MIR_CONST) {
                int shift = mir_ulong_log2_pow2(
                    (unsigned long)src1_definition->immediate);
                if (shift > 0) {
                    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
                    mir_emit_wide_shift_by_constant(out, 1, 1, shift);
                    return 1;
                }
            }
        }
        helper = mir_wide_runtime_helper(insn); break;
    case '/': case '%': helper = mir_wide_runtime_helper(insn); break;
    default: return 0;
    }
    mir_emit_runtime_call(out, helper);
    mir_stream_puts("\tpop bc\n\tpop bc\n", out);
    return 1;
}

/* Item T32 (mir-text-size-plan.md): mir_emit_fused_comparison_branch and its
 * wide/sign-test siblings need to know, before committing to an emission
 * shape, whether a given CFG edge has any phi copies to run at all - see
 * mir_phi_copies_are_empty below. Both that predicate and
 * mir_emit_spilled_phi_copies itself now share this single collection
 * routine so the "what copies does this edge need" logic can never drift
 * between the two callers. */
static int mir_collect_phi_copies_for_edge(int predecessor, int successor,
                                            int *sources, int *destinations)
{
    int predecessor_label = mir_block_label_before(predecessor);
    int edge_label = -1;
    int instruction = mir_next_phi_in_block(successor, successor);
    int copy_count = 0;

    if (predecessor >= 0 && predecessor < mir.count &&
        (mir.insns[predecessor].opcode == MIR_JUMP ||
         mir.insns[predecessor].opcode == MIR_BRANCH_FALSE) &&
        mir_find_label(mir.insns[predecessor].label) == successor)
        edge_label = mir.insns[predecessor].label;

    while (instruction >= 0) {
        const struct MirInsn *phi = &mir.insns[instruction];
        int source;
        if (!mir_value_has_use(phi->dst)) {
            instruction =
                mir_next_phi_in_block(successor, instruction + 1);
            continue;
        }
        source = mir_phi_source_for_edge(phi, predecessor_label, edge_label,
                                         successor, instruction);
        if (source < 0)
            return -1;
        if ((mir_phi_slot_cleanup_is_active() ||
             mir_value_is_wide_phi_update_result(source) ||
             mir_value_is_narrow_phi_adjust_result(source)) &&
            mir_phi_copy_is_slot_identity(source, phi->dst)) {
            instruction =
                mir_next_phi_in_block(successor, instruction + 1);
            continue;
        }
        if (copy_count >= MAX_FLOW)
            return -1;
        sources[copy_count] = source;
        destinations[copy_count] = phi->dst;
        ++copy_count;
        instruction =
            mir_next_phi_in_block(successor, instruction + 1);
    }
    return copy_count;
}

static int mir_phi_argument_already_prepushed(
    int predecessor, int successor, int source, int destination)
{
    int prior_sources[MAX_FLOW];
    int prior_destinations[MAX_FLOW];
    int prior_count;

    if (predecessor <= 0 || successor < 0 ||
        successor >= mir.count ||
        mir.insns[predecessor].opcode != MIR_LABEL ||
        mir.insns[successor].opcode != MIR_LABEL ||
        !mir_instruction_has_phi_fallthrough(predecessor - 1, 1))
        return 0;
    prior_count = mir_collect_phi_copies_for_edge(
        predecessor - 1, predecessor,
        prior_sources, prior_destinations);
    return prior_count == 1 &&
           prior_sources[0] == source &&
           prior_destinations[0] == destination;
}

/* Item T32: true only when the edge from `predecessor` to `successor`
 * definitely has no phi copies to run (a hard-fail collection is treated as
 * "not provably empty" so the caller falls back to the general path, which
 * will itself surface the same failure mir_emit_spilled_phi_copies always
 * has). Callers use this to decide whether a fused comparison branch can
 * skip straight to a single inverted jump - see
 * mir_emit_conditional_branch_with_phi_copies. */
static int mir_phi_copies_are_empty(int predecessor, int successor)
{
    int sources[MAX_FLOW];
    int destinations[MAX_FLOW];
    return mir_collect_phi_copies_for_edge(predecessor, successor, sources,
                                            destinations) == 0;
}

/* Recover the switch identity lost when MIR lowering expands each case into
 * an equality branch, but only when every edge is phi-copy-free. */
struct MirDenseByteSwitch {
    int condition;
    int condition_size;
    int condition_in_hl;
    int default_label;
    int end_instruction;
    int case_count;
    int minimum_case;
    int maximum_case;
    int targets[256];
};

static int mir_match_dense_byte_switch(
    int start, struct MirDenseByteSwitch *dispatch);

struct MirInlinePostincrementStore {
    struct Sym *callee;
    struct Sym *pointer;
    struct Sym *base;
    struct Sym *index;
    struct Sym *state;
    struct FieldDef *field;
    int kind;
    int member_op;
    int width;
    int enabled;
    int call_count;
    int continuation_label;
};

enum MirInlinePostincrementStoreKind {
    MIR_INLINE_POSTINC_DIRECT_POINTER = 1,
    MIR_INLINE_POSTINC_INDEXED_BASE = 2,
    MIR_INLINE_POSTINC_MEMBER_POINTER = 3
};

struct MirInlineMemoryStore {
    struct Sym *callee;
    int width;
};

struct MirInlineSimpleIndexedStore {
    struct Sym *callee;
    struct Sym *array;
};

struct MirInlineTypedIndexedStore {
    struct Sym *callee;
    struct Sym *array;
    struct Sym *base_state;
    struct FieldDef *base_field;
    struct FieldDef *width_field;
    struct FieldDef *pointer_field;
    int stride;
};

struct MirInlineIndexedStackStore {
    struct Sym *callee;
    struct Sym *base;
    struct Sym *state;
    struct FieldDef *index_field;
    int base_offset;
    int width;
};

static const struct AstNode *mir_inline_single_statement_expr(
    const struct Sym *callee)
{
    const struct AstNode *body;

    if (callee == NULL)
        return NULL;
    body = callee->inline_stmt_expr != NULL
        ? callee->inline_stmt_expr : callee->inline_stmt_body;
    if (body != NULL && body->kind == AST_EXPR_STMT)
        body = body->a;
    if (body != NULL && body->kind == AST_COMPOUND &&
        body->list_len == 1 &&
        body->list[0] != NULL &&
        body->list[0]->kind == AST_EXPR_STMT)
        body = body->list[0]->a;
    return body;
}

const struct AstNode *mir_inline_unwrap_cast(
    const struct AstNode *node)
{
    while (node != NULL && node->kind == AST_CAST)
        node = node->a;
    return node;
}

static const struct AstNode *mir_inline_unwrap_integer_representation_cast(
    const struct AstNode *node)
{
    while (node != NULL && node->kind == AST_CAST) {
        const struct AstNode *source = node->a;

        if (source == NULL ||
            type_ptr_depth(node->type) != 0 ||
            type_ptr_depth(source->type) != 0 ||
            type_is_float(node->type) ||
            type_is_float(source->type) ||
            type_is_struct_object(node->type) ||
            type_is_struct_object(source->type) ||
            type_size(node->type) != type_size(source->type))
            return NULL;
        node = source;
    }
    return node;
}

struct Sym *mir_inline_ident_symbol(const struct AstNode *node)
{
    if (node == NULL || node->kind != AST_IDENT)
        return NULL;
    if (node->sym != NULL)
        return node->sym;
    return node->sval != NULL ? find_global(node->sval) : NULL;
}

int mir_inline_is_parameter(
    const struct AstNode *node, const struct Sym *callee, int parameter)
{
    const char *name;

    node = mir_inline_unwrap_integer_representation_cast(node);
    if (node == NULL || node->kind != AST_IDENT ||
        callee == NULL || parameter < 0 ||
        parameter >= callee->proto_nargs)
        return 0;
    name = node->sval != NULL
        ? node->sval : node->sym != NULL ? node->sym->name : NULL;
    return name != NULL &&
           !strcmp(name, callee->inline_param_names[parameter]);
}

int mir_inline_is_parameter_low_bytes(
    const struct AstNode *node, const struct Sym *callee,
    int parameter, int width)
{
    const char *name;

    if (width <= 0 || width > 2)
        return 0;
    while (node != NULL && node->kind == AST_CAST) {
        const struct AstNode *source = node->a;

        if (source == NULL ||
            type_ptr_depth(node->type) != 0 ||
            type_ptr_depth(source->type) != 0 ||
            type_is_float(node->type) ||
            type_is_float(source->type) ||
            type_is_struct_object(node->type) ||
            type_is_struct_object(source->type) ||
            type_size(node->type) < width ||
            type_size(source->type) < width)
            return 0;
        node = source;
    }
    if (node == NULL || node->kind != AST_IDENT ||
        callee == NULL || parameter < 0 ||
        parameter >= callee->proto_nargs)
        return 0;
    name = node->sval != NULL
        ? node->sval : node->sym != NULL ? node->sym->name : NULL;
    return name != NULL &&
           !strcmp(name, callee->inline_param_names[parameter]);
}

static int mir_inline_global_member(
    const struct AstNode *node, struct Sym **state_out,
    struct FieldDef **field_out, int *member_op_out)
{
    struct Sym *state;
    struct FieldDef *field;
    int struct_id;

    if (node == NULL || node->kind != AST_MEMBER ||
        (node->op != '.' && node->op != TOK_ARROW))
        return 0;
    state = mir_inline_ident_symbol(node->a);
    if (state == NULL ||
        (state->storage != SC_GLOBAL && state->storage != SC_EXTERN) ||
        state->is_volatile)
        return 0;
    struct_id = base_struct_id_from_type(state->type);
    field = struct_id >= 0 && node->sval != NULL
        ? find_field_def(struct_id, node->sval) : NULL;
    if (field == NULL || field->is_volatile || field->bit_width > 0)
        return 0;
    if (state_out != NULL)
        *state_out = state;
    if (field_out != NULL)
        *field_out = field;
    if (member_op_out != NULL)
        *member_op_out = node->op;
    return 1;
}

static int mir_inline_same_global_member(
    const struct AstNode *node, const struct Sym *state,
    const struct FieldDef *field, int member_op)
{
    struct Sym *candidate_state;
    struct FieldDef *candidate_field;
    int candidate_op;

    return mir_inline_global_member(
               node, &candidate_state, &candidate_field, &candidate_op) &&
           candidate_state == state && candidate_field == field &&
           candidate_op == member_op;
}

static int mir_inline_member_decrement(
    const struct AstNode *update, int required_kind,
    struct Sym **state_out, struct FieldDef **field_out)
{
    const struct AstNode *member;
    struct Sym *state;
    struct FieldDef *field;
    int struct_id;

    if (update == NULL || update->kind != required_kind ||
        update->op != TOK_DEC || update->a == NULL ||
        update->a->kind != AST_MEMBER || update->a->op != '.')
        return 0;
    member = update->a;
    state = mir_inline_ident_symbol(member->a);
    if (state == NULL ||
        (state->storage != SC_GLOBAL && state->storage != SC_EXTERN) ||
        state->is_volatile || type_ptr_depth(state->type) != 0)
        return 0;
    struct_id = base_struct_id_from_type(state->type);
    field = struct_id >= 0 && member->sval != NULL
        ? find_field_def(struct_id, member->sval) : NULL;
    if (field == NULL || field->is_volatile ||
        field->bit_width > 0 || type_size(field->type) != 1)
        return 0;
    if (state_out != NULL)
        *state_out = state;
    if (field_out != NULL)
        *field_out = field;
    return 1;
}

static int mir_inline_same_member_decrement(
    const struct AstNode *update, int required_kind,
    const struct Sym *state, const struct FieldDef *field)
{
    struct Sym *candidate_state;
    struct FieldDef *candidate_field;

    return mir_inline_member_decrement(
               update, required_kind,
               &candidate_state, &candidate_field) &&
           candidate_state == state && candidate_field == field;
}

static int mir_match_inline_indexed_stack_store(
    const struct MirInsn *call,
    struct MirInlineIndexedStackStore *helper)
{
    const struct AstNode *body;
    const struct AstNode *assignment;
    const struct AstNode *dereference;
    const struct AstNode *address;
    const struct AstNode *base_plus_offset;
    const struct AstNode *base_ident;
    const struct AstNode *update;
    const struct AstNode *second_update = NULL;
    const struct AstNode *value;
    struct Sym *callee;
    struct Sym *base;
    struct Sym *state;
    struct FieldDef *index_field;
    int width;

    if (call == NULL || call->opcode != MIR_CALL ||
        (call->memory_flags &
         MIR_CALL_FLAG_INLINE_SUBSTITUTABLE) == 0 ||
        (call->type & 15) != TYPE_VOID)
        return 0;
    callee = find_global(call->name);
    if (callee == NULL || !callee->is_static || !callee->is_inline ||
        callee->proto_nargs != 1 || callee->has_inline_local ||
        callee->inline_param_use_count[0] != 1)
        return 0;
    body = callee->inline_stmt_body != NULL
        ? callee->inline_stmt_body : callee->inline_stmt_expr;
    if (body != NULL && body->kind == AST_ASSIGN)
        assignment = body;
    else if (body != NULL && body->kind == AST_EXPR_STMT)
        assignment = body->a;
    else if (body != NULL && body->kind == AST_COMPOUND &&
             body->list_len == 2 &&
             body->list[0] != NULL &&
             body->list[0]->kind == AST_EXPR_STMT &&
             body->list[1] != NULL &&
             body->list[1]->kind == AST_EXPR_STMT) {
        assignment = body->list[0]->a;
        second_update = body->list[1]->a;
    } else
        return 0;
    if (assignment == NULL || assignment->kind != AST_ASSIGN ||
        assignment->op != '=' || assignment->a == NULL ||
        assignment->a->kind != AST_UNARY ||
        assignment->a->op != '*' || assignment->b == NULL)
        return 0;
    dereference = assignment->a;
    value = assignment->b;
    if (value->kind != AST_IDENT ||
        value->sval == NULL ||
        strcmp(value->sval, callee->inline_param_names[0]) != 0)
        return 0;
    address = mir_inline_unwrap_cast(dereference->a);
    if (address == NULL || address->kind != AST_BINARY ||
        address->op != '+' || address->a == NULL ||
        address->b == NULL)
        return 0;
    base_plus_offset = address->a;
    update = address->b;
    if (base_plus_offset->kind != AST_BINARY ||
        base_plus_offset->op != '+' ||
        base_plus_offset->a == NULL ||
        base_plus_offset->b == NULL ||
        base_plus_offset->b->kind != AST_INT_LIT ||
        base_plus_offset->b->ival < 0 ||
        base_plus_offset->b->ival > 65535)
        return 0;
    base_ident = mir_inline_unwrap_cast(base_plus_offset->a);
    base = mir_inline_ident_symbol(base_ident);
    if (base == NULL ||
        (base->storage != SC_GLOBAL && base->storage != SC_EXTERN) ||
        !base->is_array || base->is_volatile ||
        base->elem_size != 1)
        return 0;
    width = type_size(dereference->type);
    if (width == 1) {
        if (second_update != NULL ||
            !mir_inline_member_decrement(
                update, AST_POSTFIX, &state, &index_field))
            return 0;
    } else if (width == 2) {
        if (!mir_inline_member_decrement(
                update, AST_UNARY, &state, &index_field) ||
            !mir_inline_same_member_decrement(
                second_update, AST_POSTFIX, state, index_field))
            return 0;
    } else
        return 0;
    if (helper != NULL) {
        helper->callee = callee;
        helper->base = base;
        helper->state = state;
        helper->index_field = index_field;
        helper->base_offset = (int)base_plus_offset->b->ival;
        helper->width = width;
    }
    return 1;
}

static int mir_call_uses_inline_indexed_stack_store(
    int call_instruction, int *argument,
    struct MirInlineIndexedStackStore *helper)
{
    return call_instruction >= 0 && call_instruction < mir.count &&
           mir_match_inline_indexed_stack_store(
               &mir.insns[call_instruction], helper) &&
           mir_single_call_argument(call_instruction, argument);
}

static void mir_emit_inline_indexed_stack_store(
    MirStream *out, const struct MirInlineIndexedStackStore *helper)
{
    const char *base_name =
        asm_name_for(sym_asm_name(helper->base));
    const char *state_name =
        asm_name_for(sym_asm_name(helper->state));

    if ((helper->base->storage == SC_EXTERN ||
         helper->base->needs_extrn) &&
        mir_extrn_should_emit(helper->base))
        mir_stream_printf(out, "\textrn %s\n", base_name);
    if ((helper->state->storage == SC_EXTERN ||
         helper->state->needs_extrn) &&
        mir_extrn_should_emit(helper->state))
        mir_stream_printf(out, "\textrn %s\n", state_name);
    mir_stream_puts("\tex de,hl\n", out);
    mir_stream_printf(out, "\tld a,(%s%+d)\n",
            state_name, helper->index_field->offset);
    if (helper->width == 2)
        mir_stream_puts("\tdec a\n", out);
    mir_stream_puts("\tld l,a\n\tld h,0\n\tdec a\n", out);
    mir_stream_printf(out, "\tld (%s%+d),a\n",
            state_name, helper->index_field->offset);
    mir_stream_printf(out, "\tld bc,%s%+d\n\tadd hl,bc\n"
                 "\tld (hl),e\n",
            base_name, helper->base_offset);
    if (helper->width == 2)
        mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
}

static int mir_match_inline_postincrement_store(
    const struct MirInsn *call,
    struct MirInlinePostincrementStore *helper)
{
    struct Sym *callee;
    struct Sym *pointer;
    const struct AstNode *assignment;
    const struct AstNode *dereference;
    const struct AstNode *increment;
    const struct AstNode *pointer_ident;
    const struct AstNode *value_ident;
    const char *value_name;
    int width;

    if (call == NULL || call->opcode != MIR_CALL ||
        (call->memory_flags &
         MIR_CALL_FLAG_INLINE_SUBSTITUTABLE) == 0 ||
        (call->type & 15) != TYPE_VOID)
        return 0;
    callee = find_global(call->name);
    if (callee == NULL || !callee->is_static || !callee->is_inline ||
        callee->proto_nargs != 1 || callee->has_inline_local)
        return 0;
    assignment = mir_inline_single_statement_expr(callee);
    if (assignment == NULL || assignment->kind != AST_ASSIGN ||
        assignment->op != '=' ||
        assignment->a == NULL || assignment->b == NULL)
        return 0;
    dereference = assignment->a;
    increment = dereference->a;
    pointer_ident = increment != NULL ? increment->a : NULL;
    value_ident = assignment->b;
    if (dereference->kind != AST_UNARY ||
        dereference->op != '*' ||
        increment == NULL || increment->kind != AST_POSTFIX ||
        increment->op != TOK_INC ||
        pointer_ident == NULL || pointer_ident->kind != AST_IDENT ||
        value_ident->kind != AST_IDENT)
        return 0;
    value_name = value_ident->sval != NULL
        ? value_ident->sval
        : value_ident->sym != NULL ? value_ident->sym->name : NULL;
    if (value_name == NULL ||
        strcmp(value_name, callee->inline_param_names[0]) != 0 ||
        callee->inline_param_use_count[0] != 1)
        return 0;
    pointer = pointer_ident->sym;
    width = type_size(dereference->type);
    if (pointer == NULL ||
        (pointer->storage != SC_GLOBAL &&
         pointer->storage != SC_EXTERN) ||
        pointer->is_volatile || pointer->pointee_is_volatile ||
        type_ptr_depth(pointer->type) == 0 ||
        (width != 1 && width != 2))
        return 0;
    if (helper != NULL) {
        memset(helper, 0, sizeof(*helper));
        helper->callee = callee;
        helper->pointer = pointer;
        helper->kind = MIR_INLINE_POSTINC_DIRECT_POINTER;
        helper->width = width;
    }
    return 1;
}

static int mir_match_inline_indexed_postincrement_store(
    const struct MirInsn *call,
    struct MirInlinePostincrementStore *helper)
{
    const struct AstNode *assignment;
    const struct AstNode *index;
    const struct AstNode *increment;
    struct Sym *callee;
    struct Sym *base;
    struct Sym *counter;
    int width;
    int element_size;

    if (call == NULL || call->opcode != MIR_CALL ||
        (call->memory_flags &
         MIR_CALL_FLAG_INLINE_SUBSTITUTABLE) == 0 ||
        (call->type & 15) != TYPE_VOID)
        return 0;
    callee = find_global(call->name);
    if (callee == NULL || !callee->is_static || !callee->is_inline ||
        callee->proto_nargs != 1 || callee->has_inline_local ||
        callee->inline_param_use_count[0] != 1)
        return 0;
    assignment = mir_inline_single_statement_expr(callee);
    if (assignment == NULL || assignment->kind != AST_ASSIGN ||
        assignment->op != '=' || assignment->a == NULL ||
        assignment->a->kind != AST_INDEX)
        return 0;
    index = assignment->a;
    width = type_size(index->type);
    if ((width != 1 && width != 2) ||
        !mir_inline_is_parameter_low_bytes(
            assignment->b, callee, 0, width))
        return 0;
    increment = index->b;
    base = mir_inline_ident_symbol(index->a);
    counter = increment != NULL && increment->kind == AST_POSTFIX &&
                      increment->op == TOK_INC
        ? mir_inline_ident_symbol(increment->a) : NULL;
    if (base == NULL || counter == NULL ||
        (base->storage != SC_GLOBAL && base->storage != SC_EXTERN) ||
        (counter->storage != SC_GLOBAL && counter->storage != SC_EXTERN) ||
        base->is_volatile || base->pointee_is_volatile ||
        counter->is_volatile || type_ptr_depth(counter->type) != 0 ||
        type_size(counter->type) != 2)
        return 0;
    element_size = base->is_array
        ? base->elem_size : type_index_elem_size(base->type);
    if ((width != 1 && width != 2) || element_size != width)
        return 0;
    if (helper != NULL) {
        memset(helper, 0, sizeof(*helper));
        helper->callee = callee;
        helper->base = base;
        helper->index = counter;
        helper->kind = MIR_INLINE_POSTINC_INDEXED_BASE;
        helper->width = width;
    }
    return 1;
}

static int mir_match_inline_member_postincrement_store(
    const struct MirInsn *call,
    struct MirInlinePostincrementStore *helper)
{
    const struct AstNode *body;
    const struct AstNode *store_statement;
    const struct AstNode *update_statement;
    const struct AstNode *assignment;
    const struct AstNode *dereference;
    const struct AstNode *update;
    const struct AstNode *addition;
    const struct AstNode *one;
    struct Sym *callee;
    struct Sym *state;
    struct FieldDef *field;
    int member_op;
    int width;

    if (call == NULL || call->opcode != MIR_CALL ||
        (call->memory_flags &
         MIR_CALL_FLAG_INLINE_SUBSTITUTABLE) == 0 ||
        (call->type & 15) != TYPE_VOID)
        return 0;
    callee = find_global(call->name);
    if (callee == NULL || !callee->is_static || !callee->is_inline ||
        callee->proto_nargs != 1 || callee->has_inline_local ||
        callee->inline_param_use_count[0] != 1)
        return 0;
    body = callee->inline_stmt_body != NULL
        ? callee->inline_stmt_body : callee->inline_stmt_expr;
    if (body == NULL || body->kind != AST_COMPOUND ||
        body->list_len != 2)
        return 0;
    store_statement = body->list[0];
    update_statement = body->list[1];
    if (store_statement == NULL ||
        store_statement->kind != AST_EXPR_STMT ||
        update_statement == NULL ||
        update_statement->kind != AST_EXPR_STMT)
        return 0;
    assignment = store_statement->a;
    update = update_statement->a;
    if (assignment == NULL || assignment->kind != AST_ASSIGN ||
        assignment->op != '=' || assignment->a == NULL ||
        assignment->a->kind != AST_UNARY ||
        assignment->a->op != '*' ||
        update == NULL || update->kind != AST_ASSIGN ||
        update->op != '=')
        return 0;
    dereference = assignment->a;
    width = type_size(dereference->type);
    if ((width != 1 && width != 2) ||
        !mir_inline_is_parameter_low_bytes(
            assignment->b, callee, 0, width))
        return 0;
    if (!mir_inline_global_member(
            dereference->a, &state, &field, &member_op) ||
        !mir_inline_same_global_member(
            update->a, state, field, member_op))
        return 0;
    addition = update->b;
    if (addition == NULL || addition->kind != AST_BINARY ||
        addition->op != '+' ||
        !mir_inline_same_global_member(
            addition->a, state, field, member_op))
        return 0;
    one = mir_inline_unwrap_cast(addition->b);
    if (one == NULL || one->kind != AST_INT_LIT || one->ival != 1 ||
        type_ptr_depth(field->type) == 0 ||
        type_index_elem_size(field->type) != width ||
        (width != 1 && width != 2))
        return 0;
    if (helper != NULL) {
        memset(helper, 0, sizeof(*helper));
        helper->callee = callee;
        helper->state = state;
        helper->field = field;
        helper->kind = MIR_INLINE_POSTINC_MEMBER_POINTER;
        helper->member_op = member_op;
        helper->width = width;
    }
    return 1;
}

static int mir_match_any_inline_postincrement_store(
    const struct MirInsn *call,
    struct MirInlinePostincrementStore *helper)
{
    return mir_match_inline_postincrement_store(call, helper) ||
           mir_match_inline_indexed_postincrement_store(call, helper) ||
           mir_match_inline_member_postincrement_store(call, helper);
}

static int mir_inline_affine_index(
    const struct AstNode *node, const struct Sym *callee,
    int coefficients[3])
{
    int left[3] = {0, 0, 0};
    int right[3] = {0, 0, 0};
    int component;

    node = mir_inline_unwrap_integer_representation_cast(node);
    if (node == NULL)
        return 0;
    if (mir_inline_is_parameter(node, callee, 0)) {
        coefficients[0] = 1;
        coefficients[1] = 0;
        coefficients[2] = 0;
        return 1;
    }
    if (mir_inline_is_parameter(node, callee, 1)) {
        coefficients[0] = 0;
        coefficients[1] = 1;
        coefficients[2] = 0;
        return 1;
    }
    if (node->kind == AST_INT_LIT &&
        node->ival >= 0 && node->ival <= 4) {
        coefficients[0] = 0;
        coefficients[1] = 0;
        coefficients[2] = (int)node->ival;
        return 1;
    }
    if (node->kind != AST_BINARY ||
        (node->op != '+' && node->op != '*') ||
        !mir_inline_affine_index(node->a, callee, left) ||
        !mir_inline_affine_index(node->b, callee, right))
        return 0;
    if (node->op == '+') {
        for (component = 0; component < 3; ++component) {
            coefficients[component] =
                left[component] + right[component];
            if (coefficients[component] < 0 ||
                coefficients[component] > 4)
                return 0;
        }
        return 1;
    }
    if (left[0] != 0 || left[1] != 0) {
        if (right[0] != 0 || right[1] != 0)
            return 0;
        for (component = 0; component < 3; ++component)
            coefficients[component] =
                left[component] * right[2];
    } else if (right[0] != 0 || right[1] != 0) {
        for (component = 0; component < 3; ++component)
            coefficients[component] =
                right[component] * left[2];
    } else {
        coefficients[0] = 0;
        coefficients[1] = 0;
        coefficients[2] = left[2] * right[2];
    }
    for (component = 0; component < 3; ++component)
        if (coefficients[component] < 0 ||
            coefficients[component] > 4)
            return 0;
    return 1;
}

int mir_inline_value_byte_lane(
    const struct AstNode *node, const struct Sym *callee, int lane)
{
    const struct AstNode *constant;

    while (node != NULL && node->kind == AST_CAST) {
        const struct AstNode *source = node->a;

        if (source == NULL ||
            type_ptr_depth(node->type) != 0 ||
            type_ptr_depth(source->type) != 0 ||
            type_is_float(node->type) ||
            type_is_float(source->type) ||
            type_is_struct_object(node->type) ||
            type_is_struct_object(source->type) ||
            type_size(node->type) < 1 ||
            type_size(source->type) < 1)
            return 0;
        node = source;
    }
    if (node != NULL && node->kind == AST_BINARY &&
        node->op == '&') {
        constant = mir_inline_unwrap_cast(node->b);
        if (constant != NULL && constant->kind == AST_INT_LIT &&
            constant->ival == 255)
            node = mir_inline_unwrap_cast(node->a);
        else {
            constant = mir_inline_unwrap_cast(node->a);
            if (constant == NULL || constant->kind != AST_INT_LIT ||
                constant->ival != 255)
                return 0;
            node = mir_inline_unwrap_cast(node->b);
        }
    }
    if (lane == 0)
        return mir_inline_is_parameter_low_bytes(
            node, callee, 3, 1);
    if (lane != 1 || node == NULL ||
        node->kind != AST_BINARY || node->op != TOK_SHR ||
        !mir_inline_is_parameter(node->a, callee, 3))
        return 0;
    constant = mir_inline_unwrap_cast(node->b);
    return constant != NULL && constant->kind == AST_INT_LIT &&
           constant->ival == 8;
}

static const struct AstNode *mir_inline_body_expression(
    const struct Sym *callee, int index, int count)
{
    const struct AstNode *body;
    const struct AstNode *statement;

    if (callee == NULL || index < 0 || index >= count)
        return NULL;
    body = callee->inline_stmt_body != NULL
        ? callee->inline_stmt_body : callee->inline_stmt_expr;
    if (body == NULL)
        return NULL;
    if (body->kind == AST_COMPOUND) {
        if (body->list_len != count)
            return NULL;
        statement = body->list[index];
    } else {
        if (count != 1 || index != 0)
            return NULL;
        statement = body;
    }
    if (statement != NULL && statement->kind == AST_EXPR_STMT)
        statement = statement->a;
    return statement;
}

static int mir_match_inline_simple_indexed_store(
    const struct MirInsn *call,
    struct MirInlineSimpleIndexedStore *helper)
{
    struct Sym *callee;
    struct Sym *array;
    const struct AstNode *assignment;
    const struct AstNode *index;
    const struct AstNode *addition;
    const struct AstNode *value;
    int left_first;
    int right_second;

    if (call == NULL || call->opcode != MIR_CALL ||
        (call->memory_flags &
         MIR_CALL_FLAG_INLINE_SUBSTITUTABLE) == 0 ||
        (call->type & 15) != TYPE_VOID)
        return 0;
    callee = find_global(call->name);
    if (callee == NULL || !callee->is_static ||
        !callee->is_inline || callee->proto_nargs != 3 ||
        callee->has_inline_local)
        return 0;
    assignment = mir_inline_single_statement_expr(callee);
    if (assignment == NULL || assignment->kind != AST_ASSIGN ||
        assignment->op != '=' || assignment->a == NULL ||
        assignment->a->kind != AST_INDEX ||
        !mir_inline_is_parameter_low_bytes(
            assignment->b, callee, 2, 1))
        return 0;
    index = assignment->a;
    value = assignment->b;
    while (value != NULL && value->kind == AST_CAST) {
        if ((value->type & 15) == TYPE_BOOL)
            return 0;
        value = value->a;
    }
    array = mir_inline_ident_symbol(index->a);
    addition = mir_inline_unwrap_cast(index->b);
    if (array == NULL || !array->is_array ||
        array->elem_size != 1 ||
        (array->storage != SC_GLOBAL &&
         array->storage != SC_EXTERN) ||
        array->is_volatile || array->pointee_is_volatile ||
        (index->type & 15) == TYPE_BOOL ||
        (callee->proto_types[2] & 15) == TYPE_BOOL ||
        addition == NULL || addition->kind != AST_BINARY ||
        addition->op != '+' ||
        addition->a == NULL || addition->b == NULL)
        return 0;
    left_first =
        mir_inline_is_parameter(addition->a, callee, 0);
    right_second =
        mir_inline_is_parameter(addition->b, callee, 1);
    if (!(left_first && right_second) &&
        !(mir_inline_is_parameter(addition->a, callee, 1) &&
          mir_inline_is_parameter(addition->b, callee, 0)))
        return 0;
    if (helper != NULL) {
        helper->callee = callee;
        helper->array = array;
    }
    return 1;
}

static int mir_match_inline_memory_store(
    const struct MirInsn *call, struct MirInlineMemoryStore *helper)
{
    struct Sym *callee;
    int width;
    int lane;

    if (call == NULL || call->opcode != MIR_CALL)
        return 0;
    callee = find_global(call->name);
    if ((call->memory_flags &
         MIR_CALL_FLAG_INLINE_SUBSTITUTABLE) == 0 ||
        (call->type & 15) != TYPE_VOID)
        return 0;
    if (callee == NULL || !callee->is_static || !callee->is_inline ||
        callee->proto_nargs != 4 || callee->has_inline_local ||
        type_ptr_depth(callee->proto_types[2]) == 0 ||
        type_index_elem_size(callee->proto_types[2]) != 1)
        return 0;
    width = mir_inline_body_expression(callee, 1, 2) != NULL ? 2 : 1;
    for (lane = 0; lane < width; ++lane) {
        const struct AstNode *assignment =
            mir_inline_body_expression(callee, lane, width);
        const struct AstNode *index =
            assignment != NULL && assignment->kind == AST_ASSIGN
                ? assignment->a : NULL;
        int coefficients[3] = {-1, -1, -1};
        int affine = index != NULL && index->kind == AST_INDEX &&
            mir_inline_affine_index(
                index->b, callee, coefficients);
        int base_parameter = index != NULL &&
            index->kind == AST_INDEX &&
            mir_inline_is_parameter(index->a, callee, 2);
        int value_lane = assignment != NULL &&
            mir_inline_value_byte_lane(
                assignment->b, callee, lane);

        if (assignment == NULL || assignment->kind != AST_ASSIGN ||
            assignment->op != '=' || assignment->a == NULL ||
            assignment->a->kind != AST_INDEX)
            return 0;
        index = assignment->a;
        if (!base_parameter || !affine ||
            coefficients[0] != 1 ||
            coefficients[1] != width ||
            coefficients[2] != lane ||
            !value_lane)
            return 0;
    }
    if (helper != NULL) {
        helper->callee = callee;
        helper->width = width;
    }
    return 1;
}

static int mir_collect_call_arguments(
    int call_instruction, int expected, int *arguments)
{
    const struct MirInsn *call;
    int found = 0;
    int instruction;
    int argument;

    if (call_instruction < 0 || call_instruction >= mir.count ||
        expected <= 0 || arguments == NULL)
        return 0;
    call = &mir.insns[call_instruction];
    for (argument = 0; argument < expected; ++argument)
        arguments[argument] = -1;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        int index;

        if (insn->opcode != MIR_ARG ||
            insn->secondary_offset != call->secondary_offset)
            continue;
        index = (int)insn->immediate;
        if (index < 0 || index >= expected ||
            arguments[index] >= 0 ||
            type_size(insn->type) > 2)
            return 0;
        arguments[index] = insn->src1;
        ++found;
    }
    if (found != expected)
        return 0;
    for (argument = 0; argument < expected; ++argument)
        if (arguments[argument] < 0)
            return 0;
    return 1;
}

static int mir_call_uses_inline_memory_store(
    int call_instruction, struct MirInlineMemoryStore *helper,
    int arguments[4])
{
    if (call_instruction < 0 || call_instruction >= mir.count ||
        !mir_match_inline_memory_store(
            &mir.insns[call_instruction], helper))
        return 0;
    if (arguments == NULL)
        return 1;
    return mir_collect_call_arguments(
        call_instruction, 4, arguments);
}

static int mir_call_uses_inline_simple_indexed_store(
    int call_instruction, struct MirInlineSimpleIndexedStore *helper,
    int arguments[3])
{
    if (call_instruction < 0 || call_instruction >= mir.count ||
        !mir_match_inline_simple_indexed_store(
            &mir.insns[call_instruction], helper))
        return 0;
    if (arguments == NULL)
        return 1;
    return mir_collect_call_arguments(
        call_instruction, 3, arguments);
}

static void mir_emit_inline_simple_indexed_store(
    MirStream *out, const struct MirInlineSimpleIndexedStore *helper,
    const int arguments[3])
{
    const char *array_name =
        asm_name_for(sym_asm_name(helper->array));
    const struct MirInsn *left = mir_definition(arguments[0]);
    const struct MirInsn *right = mir_definition(arguments[1]);
    const struct MirInsn *value = mir_definition(arguments[2]);

    if ((helper->array->storage == SC_EXTERN ||
         helper->array->needs_extrn) &&
        mir_extrn_should_emit(helper->array))
        mir_stream_printf(out, "\textrn %s\n", array_name);
    mir_stream_puts(";@dcc.mir inline-simple-store\n", out);
    if (value == NULL || value->opcode != MIR_CONST) {
        if (!mir_take_forwarded_hl_call_argument(arguments[2]))
            mir_emit_spilled_arg_to_hl(out, arguments[2]);
        mir_stream_puts("\tpush hl\n", out);
    }
    if (left != NULL && left->opcode == MIR_CONST &&
        right != NULL && right->opcode == MIR_CONST) {
        unsigned long offset =
            ((unsigned long)left->immediate +
             (unsigned long)right->immediate) & 0xffffUL;

        if (offset == 0)
            mir_stream_printf(out, "\tld hl,%s\n", array_name);
        else
            mir_stream_printf(out, "\tld hl,%s+%lu\n", array_name, offset);
    } else {
        mir_emit_spilled_arg_to_hl(out, arguments[0]);
        mir_stream_puts("\tpush hl\n", out);
        mir_emit_spilled_arg_to_hl(out, arguments[1]);
        mir_stream_puts("\tpop de\n\tadd hl,de\n", out);
        mir_stream_printf(out, "\tld de,%s\n\tadd hl,de\n", array_name);
    }
    if (value != NULL && value->opcode == MIR_CONST) {
        mir_stream_printf(out, "\tld a,%lu\n",
                (unsigned long)value->immediate & 0xffUL);
    } else {
        mir_stream_puts("\tpop de\n\tld a,e\n", out);
    }
    mir_stream_puts("\tld (hl),a\n", out);
}

static void mir_emit_inline_memory_store(
    MirStream *out, const struct MirInlineMemoryStore *helper,
    const int arguments[4])
{
    mir_emit_spilled_arg_to_hl(out, arguments[3]);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_spilled_arg_to_hl(out, arguments[2]);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_spilled_arg_to_hl(out, arguments[1]);
    if (helper->width == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_spilled_arg_to_hl(out, arguments[0]);
    mir_stream_puts("\tpop de\n\tadd hl,de\n\tpop de\n\tadd hl,de\n"
          "\tpop de\n\tld (hl),e\n", out);
    if (helper->width == 2)
        mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
}

static const struct AstNode *mir_inline_unwrap_statement(
    const struct AstNode *node)
{
    while (node != NULL && node->kind == AST_COMPOUND &&
           node->list_len == 1)
        node = node->list[0];
    if (node != NULL && node->kind == AST_EXPR_STMT)
        node = node->a;
    return node;
}

static int mir_inline_local_member(
    const struct AstNode *node, const struct Sym *callee,
    struct FieldDef **field_out)
{
    const struct AstNode *base;
    const char *name;
    struct FieldDef *field;
    int struct_id;

    node = mir_inline_unwrap_cast(node);
    if (node == NULL || node->kind != AST_MEMBER ||
        node->op != TOK_ARROW || callee == NULL ||
        !callee->has_inline_local || node->a == NULL)
        return 0;
    base = mir_inline_unwrap_cast(node->a);
    if (base == NULL || base->kind != AST_IDENT)
        return 0;
    name = base->sval != NULL
        ? base->sval : base->sym != NULL ? base->sym->name : NULL;
    if (name == NULL || strcmp(name, callee->inline_local_name) != 0)
        return 0;
    struct_id = base_struct_id_from_type(callee->inline_local_type);
    field = struct_id >= 0 && node->sval != NULL
        ? find_field_def(struct_id, node->sval) : NULL;
    if (field == NULL || field->is_volatile || field->bit_width > 0)
        return 0;
    if (field_out != NULL)
        *field_out = field;
    return 1;
}

static int mir_inline_typed_store_assignment(
    const struct AstNode *node, const struct Sym *callee,
    struct FieldDef **pointer_field_out, int *width_out)
{
    const struct AstNode *assignment =
        mir_inline_unwrap_statement(node);
    const struct AstNode *index;
    const struct AstNode *base;
    struct FieldDef *pointer_field;
    int width;

    if (assignment == NULL || assignment->kind != AST_ASSIGN ||
        assignment->op != '=' || assignment->a == NULL ||
        assignment->a->kind != AST_INDEX)
        return 0;
    index = assignment->a;
    if (!mir_inline_is_parameter(index->b, callee, 1))
        return 0;
    base = index->a;
    width = type_index_elem_size(base->type);
    base = mir_inline_unwrap_cast(base);
    if ((width != 1 && width != 2) ||
        !mir_inline_is_parameter_low_bytes(
            assignment->b, callee, 2, width) ||
        !mir_inline_local_member(
            base, callee, &pointer_field) ||
        type_ptr_depth(pointer_field->type) == 0)
        return 0;
    if (pointer_field_out != NULL)
        *pointer_field_out = pointer_field;
    if (width_out != NULL)
        *width_out = width;
    return 1;
}

static int mir_match_inline_typed_indexed_store(
    const struct MirInsn *call,
    struct MirInlineTypedIndexedStore *helper)
{
    struct Sym *callee;
    const struct AstNode *initializer;
    const struct AstNode *address;
    const struct AstNode *body;
    const struct AstNode *condition;
    const struct AstNode *constant;
    const struct AstNode *member;
    struct Sym *array;
    struct Sym *base_state = NULL;
    struct FieldDef *base_field = NULL;
    int base_member_op = 0;
    struct FieldDef *width_field;
    struct FieldDef *then_pointer;
    struct FieldDef *else_pointer;
    int then_width;
    int else_width;

    if (call == NULL || call->opcode != MIR_CALL ||
        (call->memory_flags &
         MIR_CALL_FLAG_INLINE_SUBSTITUTABLE) == 0 ||
        (call->type & 15) != TYPE_VOID)
        return 0;
    callee = find_global(call->name);
    if (callee == NULL || !callee->is_static || !callee->is_inline ||
        callee->proto_nargs != 3 || !callee->has_inline_local ||
        type_ptr_depth(callee->inline_local_type) == 0)
        return 0;
    initializer = mir_inline_unwrap_cast(callee->inline_local_init);
    if (initializer == NULL || initializer->kind != AST_UNARY ||
        initializer->op != '&')
        return 0;
    address = mir_inline_unwrap_cast(initializer->a);
    if (address == NULL || address->kind != AST_INDEX ||
        !mir_inline_is_parameter(address->b, callee, 0))
        return 0;
    array = mir_inline_ident_symbol(address->a);
    if (array != NULL) {
        if (!array->is_array || array->elem_size <= 0 ||
            (array->storage != SC_GLOBAL &&
             array->storage != SC_EXTERN) ||
            array->is_volatile)
            return 0;
    } else {
        if (!mir_inline_global_member(
                mir_inline_unwrap_cast(address->a),
                &base_state, &base_field, &base_member_op) ||
            base_member_op != '.' ||
            type_ptr_depth(base_field->type) == 0 ||
            type_index_elem_size(base_field->type) <= 0)
            return 0;
    }
    body = mir_inline_unwrap_statement(
        callee->inline_stmt_body != NULL
            ? callee->inline_stmt_body : callee->inline_stmt_expr);
    if (body == NULL || body->kind != AST_IF ||
        body->a == NULL || body->b == NULL || body->c == NULL)
        return 0;
    condition = mir_inline_unwrap_cast(body->a);
    if (condition == NULL || condition->kind != AST_BINARY ||
        condition->op != TOK_EQ)
        return 0;
    member = mir_inline_unwrap_cast(condition->a);
    constant = mir_inline_unwrap_cast(condition->b);
    if (constant == NULL || constant->kind != AST_INT_LIT ||
        constant->ival != 1) {
        member = mir_inline_unwrap_cast(condition->b);
        constant = mir_inline_unwrap_cast(condition->a);
    }
    if (constant == NULL || constant->kind != AST_INT_LIT ||
        constant->ival != 1 ||
        !mir_inline_local_member(
            member, callee, &width_field) ||
        type_size(width_field->type) != 1 ||
        !mir_inline_typed_store_assignment(
            body->b, callee, &then_pointer, &then_width) ||
        !mir_inline_typed_store_assignment(
            body->c, callee, &else_pointer, &else_width) ||
        then_pointer != else_pointer ||
        then_width != 1 || else_width != 2)
        return 0;
    if (helper != NULL) {
        helper->callee = callee;
        helper->array = array;
        helper->base_state = base_state;
        helper->base_field = base_field;
        helper->width_field = width_field;
        helper->pointer_field = then_pointer;
        helper->stride = array != NULL
            ? array->elem_size
            : type_index_elem_size(base_field->type);
    }
    return 1;
}

static int mir_call_uses_inline_typed_indexed_store(
    int call_instruction, struct MirInlineTypedIndexedStore *helper,
    int arguments[3])
{
    if (call_instruction < 0 || call_instruction >= mir.count ||
        !mir_match_inline_typed_indexed_store(
            &mir.insns[call_instruction], helper))
        return 0;
    if (arguments == NULL)
        return 1;
    return mir_collect_call_arguments(
        call_instruction, 3, arguments);
}

static void mir_emit_inline_typed_indexed_store(
    MirStream *out, const struct MirInlineTypedIndexedStore *helper,
    const int arguments[3])
{
    const char *array_name = helper->array != NULL
        ? asm_name_for(sym_asm_name(helper->array)) : NULL;
    const char *state_name = helper->base_state != NULL
        ? asm_name_for(sym_asm_name(helper->base_state)) : NULL;
    int word_label = new_label();
    int done_label = new_label();

    if (helper->array != NULL &&
        (helper->array->storage == SC_EXTERN ||
         helper->array->needs_extrn) &&
        mir_extrn_should_emit(helper->array))
        mir_stream_printf(out, "\textrn %s\n", array_name);
    if (helper->base_state != NULL &&
        (helper->base_state->storage == SC_EXTERN ||
         helper->base_state->needs_extrn) &&
        mir_extrn_should_emit(helper->base_state))
        mir_stream_printf(out, "\textrn %s\n", state_name);
    mir_emit_spilled_arg_to_hl(out, arguments[2]);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_spilled_arg_to_hl(out, arguments[1]);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_spilled_arg_to_hl(out, arguments[0]);
    if (helper->stride == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    else if (helper->stride > 2)
        mir_emit_mul_hl_const(out, (unsigned long)helper->stride);
    if (helper->array != NULL)
        mir_stream_printf(out, "\tld de,%s\n", array_name);
    else
        mir_stream_printf(out, "\tld de,(%s%+d)\n",
                state_name, helper->base_field->offset);
    mir_stream_puts("\tadd hl,de\n\tpush hl\n", out);
    if (helper->width_field->offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                helper->width_field->offset);
    mir_stream_puts("\tld a,(hl)\n\tpop hl\n\tcp 1\n", out);
    mir_stream_printf(out, "\tjp nz, L%d\n", word_label);
    if (helper->pointer_field->offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                helper->pointer_field->offset);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tpop hl\n\tadd hl,de\n\tpop de\n\tld (hl),e\n", out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", done_label, word_label);
    if (helper->pointer_field->offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                helper->pointer_field->offset);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
          "\tpop hl\n\tadd hl,hl\n\tadd hl,de\n"
          "\tpop de\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    mir_stream_printf(out, "L%d:\n", done_label);
}

static int mir_single_call_argument(
    int call_instruction, int *value)
{
    const struct MirInsn *call;
    int argument = -1;
    int instruction;

    if (call_instruction < 0 || call_instruction >= mir.count)
        return 0;
    call = &mir.insns[call_instruction];
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->opcode != MIR_ARG ||
            insn->secondary_offset != call->secondary_offset)
            continue;
        if (insn->immediate != 0 || argument >= 0 ||
            type_size(insn->type) > 2)
            return 0;
        argument = insn->src1;
    }
    if (argument < 0)
        return 0;
    if (value != NULL)
        *value = argument;
    return 1;
}

static int mir_prepare_inline_postincrement_stores(
    struct MirInlinePostincrementStore *helper)
{
    struct MirInlinePostincrementStore groups[8];
    struct MirInlinePostincrementStore candidate;
    struct MirDenseByteSwitch dispatch;
    int has_dense_switch = 0;
    int group_count = 0;
    int best = -1;
    int instruction;

    memset(helper, 0, sizeof(*helper));
    helper->enabled = 0;
    helper->continuation_label = -1;
    memset(groups, 0, sizeof(groups));
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_CONST &&
            mir_match_dense_byte_switch(instruction, &dispatch)) {
            has_dense_switch = 1;
            break;
        }
    if (!has_dense_switch &&
        (!mir_has_cfg_backedge() || mir_cfg_block_count() < 70))
        return 0;
    memset(&candidate, 0, sizeof(candidate));
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *call = &mir.insns[instruction];
        int continuation = instruction + 1;
        int continuation_target;

        if (!mir_match_any_inline_postincrement_store(call, &candidate) ||
            !mir_single_call_argument(instruction, NULL))
            continue;
        while (continuation < mir.count &&
               mir.insns[continuation].opcode == MIR_NOP)
            ++continuation;
        if (continuation >= mir.count ||
            mir.insns[continuation].opcode != MIR_JUMP)
            continue;
        continuation_target =
            mir_find_label(mir.insns[continuation].label);
        if (continuation_target < 0 ||
            !mir_phi_copies_are_empty(
                continuation, continuation_target))
            continue;
        {
            int group;

            for (group = 0; group < group_count; ++group)
                if (groups[group].kind == candidate.kind &&
                    groups[group].callee == candidate.callee &&
                    groups[group].pointer == candidate.pointer &&
                    groups[group].base == candidate.base &&
                    groups[group].index == candidate.index &&
                    groups[group].state == candidate.state &&
                    groups[group].field == candidate.field &&
                    groups[group].member_op == candidate.member_op &&
                    groups[group].width == candidate.width &&
                    groups[group].continuation_label ==
                        mir.insns[continuation].label)
                    break;
            if (group == group_count) {
                if (group_count >=
                    (int)(sizeof(groups) / sizeof(groups[0])))
                    continue;
                groups[group] = candidate;
                groups[group].continuation_label =
                    mir.insns[continuation].label;
                ++group_count;
            }
            ++groups[group].call_count;
        }
    }
    for (instruction = 0; instruction < group_count; ++instruction)
        if (groups[instruction].call_count >= 8 &&
            (best < 0 ||
             groups[instruction].call_count >
                 groups[best].call_count))
            best = instruction;
    if (best < 0)
        return 0;
    *helper = groups[best];
    helper->enabled = 1;
    return 1;
}

static int mir_call_uses_inline_postincrement_store(
    const struct MirInlinePostincrementStore *helper,
    int call_instruction, int *argument,
    int *continuation_instruction,
    int *continuation_label)
{
    struct MirInlinePostincrementStore candidate;
    int continuation = call_instruction + 1;
    int target;

    while (continuation < mir.count &&
           mir.insns[continuation].opcode == MIR_NOP)
        ++continuation;
    target = continuation < mir.count &&
        mir.insns[continuation].opcode == MIR_JUMP
        ? mir_find_label(mir.insns[continuation].label) : -1;

    return helper != NULL && helper->enabled &&
           continuation < mir.count &&
           mir.insns[continuation].opcode == MIR_JUMP &&
           target >= 0 &&
           mir_phi_copies_are_empty(continuation, target) &&
           mir_match_any_inline_postincrement_store(
               &mir.insns[call_instruction], &candidate) &&
           candidate.kind == helper->kind &&
           candidate.callee == helper->callee &&
           candidate.pointer == helper->pointer &&
           candidate.base == helper->base &&
           candidate.index == helper->index &&
           candidate.state == helper->state &&
           candidate.field == helper->field &&
           candidate.member_op == helper->member_op &&
           candidate.width == helper->width &&
           (helper->kind != MIR_INLINE_POSTINC_INDEXED_BASE ||
            mir.insns[continuation].label ==
                helper->continuation_label) &&
           mir_single_call_argument(call_instruction, argument) &&
           (continuation_instruction == NULL ||
            (*continuation_instruction = continuation) >= 0) &&
           (continuation_label == NULL ||
            (*continuation_label =
                 mir.insns[continuation].label) >= 0);
}

static void mir_emit_inline_indexed_postincrement_store_body(
    MirStream *out, const struct MirInlinePostincrementStore *helper)
{
    const char *base_name =
        asm_name_for(sym_asm_name(helper->base));
    const char *index_name =
        asm_name_for(sym_asm_name(helper->index));

    if ((helper->base->storage == SC_EXTERN ||
         helper->base->needs_extrn) &&
        mir_extrn_should_emit(helper->base))
        mir_stream_printf(out, "\textrn %s\n", base_name);
    if ((helper->index->storage == SC_EXTERN ||
         helper->index->needs_extrn) &&
        mir_extrn_should_emit(helper->index))
        mir_stream_printf(out, "\textrn %s\n", index_name);
    mir_stream_printf(out,
            "\tex de,hl\n\tld hl,(%s)\n\tinc hl\n"
            "\tld (%s),hl\n\tdec hl\n",
            index_name, index_name);
    if (helper->width == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    mir_stream_printf(out, helper->base->is_array
                     ? "\tld bc,%s\n" : "\tld bc,(%s)\n",
            base_name);
    mir_stream_puts("\tadd hl,bc\n\tld (hl),e\n", out);
    if (helper->width == 2)
        mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
}

static void mir_emit_inline_postincrement_store(
    MirStream *out, const int *labels,
    const struct MirInlinePostincrementStore *helper,
    int argument, int value_in_hl, int shared_helper_label,
    int continuation_label)
{
    const char *assembly_name;

    if (shared_helper_label >= 0) {
        if (!value_in_hl)
            mir_emit_spilled_arg_to_hl(out, argument);
        mir_stream_printf(out, "\tjp L%d\n", shared_helper_label);
        return;
    }
    if (helper->kind == MIR_INLINE_POSTINC_DIRECT_POINTER) {
        if (!value_in_hl)
            mir_emit_spilled_arg_to_hl(out, argument);
        assembly_name = asm_name_for(sym_asm_name(helper->pointer));
        if ((helper->pointer->storage == SC_EXTERN ||
             helper->pointer->needs_extrn) &&
            mir_extrn_should_emit(helper->pointer))
            mir_stream_printf(out, "\textrn %s\n", assembly_name);
        mir_stream_printf(out, "\tex de,hl\n\tld hl,(%s)\n"
                     "\tld (hl),e\n",
                assembly_name);
        if (helper->width == 2)
            mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
        mir_stream_puts("\tinc hl\n", out);
        mir_stream_printf(out, "\tld (%s),hl\n", assembly_name);
    } else if (helper->kind == MIR_INLINE_POSTINC_INDEXED_BASE) {
        if (!value_in_hl)
            mir_emit_spilled_arg_to_hl(out, argument);
        mir_emit_inline_indexed_postincrement_store_body(out, helper);
    } else {
        const char *state_name =
            asm_name_for(sym_asm_name(helper->state));

        if ((helper->state->storage == SC_EXTERN ||
             helper->state->needs_extrn) &&
            mir_extrn_should_emit(helper->state))
            mir_stream_printf(out, "\textrn %s\n", state_name);
        if (helper->member_op == '.') {
            if (!value_in_hl)
                mir_emit_spilled_arg_to_hl(out, argument);
            mir_stream_puts("\tex de,hl\n", out);
            mir_stream_printf(out, "\tld hl,(%s%+d)\n",
                    state_name, helper->field->offset);
            mir_stream_puts("\tld (hl),e\n\tinc hl\n", out);
            if (helper->width == 2)
                mir_stream_puts("\tld (hl),d\n\tinc hl\n", out);
            mir_stream_printf(out, "\tld (%s%+d),hl\n",
                    state_name, helper->field->offset);
        } else {
            if (!value_in_hl)
                mir_emit_spilled_arg_to_hl(out, argument);
            mir_stream_puts("\tex de,hl\n", out);
            mir_stream_printf(out, "\tld hl,(%s)\n", state_name);
            if (helper->field->offset != 0)
                mir_stream_printf(out, "\tld bc,%d\n\tadd hl,bc\n",
                        helper->field->offset);
            mir_stream_puts("\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
                  "\tld a,e\n\tld (bc),a\n\tinc bc\n", out);
            if (helper->width == 2)
                mir_stream_puts("\tld a,d\n\tld (bc),a\n\tinc bc\n", out);
            mir_stream_printf(out, "\tld hl,(%s)\n", state_name);
            if (helper->field->offset != 0)
                mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                        helper->field->offset);
            mir_stream_puts("\tld (hl),c\n\tinc hl\n\tld (hl),b\n", out);
        }
    }
    mir_stream_printf(out, "\tjp L%d\n", labels[continuation_label]);
}

struct MirInlineWordLoadPush {
    const struct MirInsn *base_load;
    const struct MirInsn *index_load;
    int call_instruction;
    int continuation_instruction;
    int continuation_label;
};

struct MirInlineTypedLoadPush {
    int input_member_value;
    int base_member_value;
    int width_offset;
    int pointer_offset;
    int stride;
    int continuation_instruction;
    int continuation_label;
};

static int mir_match_inline_typed_load_push(
    int instruction, const struct MirInlinePostincrementStore *helper,
    struct MirInlineTypedLoadPush *plan)
{
    const struct MirInsn *input_load;
    const struct MirInsn *input_member;
    const struct MirInsn *input_value;
    const struct MirInsn *input_store;
    const struct MirInsn *state_address;
    const struct MirInsn *base_member;
    const struct MirInsn *base_pointer;
    const struct MirInsn *index_reload;
    const struct MirInsn *object_address;
    const struct MirInsn *object_store;
    const struct MirInsn *object_width_load;
    const struct MirInsn *width_member;
    const struct MirInsn *width_load;
    const struct MirInsn *one;
    const struct MirInsn *width_extend;
    const struct MirInsn *compare;
    const struct MirInsn *branch;
    const struct MirInsn *object_byte_load;
    const struct MirInsn *byte_pointer_member;
    const struct MirInsn *byte_pointer;
    const struct MirInsn *byte_zero;
    const struct MirInsn *byte_address;
    const struct MirInsn *byte_load;
    const struct MirInsn *byte_label;
    const struct MirInsn *join_jump;
    const struct MirInsn *word_label;
    const struct MirInsn *object_word_load;
    const struct MirInsn *word_pointer_member;
    const struct MirInsn *word_pointer;
    const struct MirInsn *word_zero;
    const struct MirInsn *word_address;
    const struct MirInsn *word_load;
    const struct MirInsn *word_result_label;
    const struct MirInsn *join_label;
    const struct MirInsn *phi;
    const struct MirInsn *argument;
    const struct MirInsn *call;
    const struct MirInsn *continuation_jump;
    int argument_value;
    int continuation_instruction;
    int continuation_label;
    int cursor = instruction;

#define MIR_NEXT_TYPED_PUSH_INSN(target) \
    do { \
        if (cursor < 0 || cursor >= mir.count) \
            return 0; \
        target = &mir.insns[cursor]; \
        cursor = mir_low_byte_loop_next_effective(cursor); \
    } while (0)

    memset(plan, 0, sizeof(*plan));
    MIR_NEXT_TYPED_PUSH_INSN(input_load);
    MIR_NEXT_TYPED_PUSH_INSN(input_member);
    MIR_NEXT_TYPED_PUSH_INSN(input_value);
    MIR_NEXT_TYPED_PUSH_INSN(input_store);
    MIR_NEXT_TYPED_PUSH_INSN(state_address);
    MIR_NEXT_TYPED_PUSH_INSN(base_member);
    MIR_NEXT_TYPED_PUSH_INSN(base_pointer);
    MIR_NEXT_TYPED_PUSH_INSN(index_reload);
    MIR_NEXT_TYPED_PUSH_INSN(object_address);
    MIR_NEXT_TYPED_PUSH_INSN(object_store);
    MIR_NEXT_TYPED_PUSH_INSN(object_width_load);
    MIR_NEXT_TYPED_PUSH_INSN(width_member);
    MIR_NEXT_TYPED_PUSH_INSN(width_load);
    MIR_NEXT_TYPED_PUSH_INSN(one);
    MIR_NEXT_TYPED_PUSH_INSN(width_extend);
    MIR_NEXT_TYPED_PUSH_INSN(compare);
    MIR_NEXT_TYPED_PUSH_INSN(branch);
    MIR_NEXT_TYPED_PUSH_INSN(object_byte_load);
    MIR_NEXT_TYPED_PUSH_INSN(byte_pointer_member);
    MIR_NEXT_TYPED_PUSH_INSN(byte_pointer);
    MIR_NEXT_TYPED_PUSH_INSN(byte_zero);
    MIR_NEXT_TYPED_PUSH_INSN(byte_address);
    MIR_NEXT_TYPED_PUSH_INSN(byte_load);
    MIR_NEXT_TYPED_PUSH_INSN(byte_label);
    MIR_NEXT_TYPED_PUSH_INSN(join_jump);
    MIR_NEXT_TYPED_PUSH_INSN(word_label);
    MIR_NEXT_TYPED_PUSH_INSN(object_word_load);
    MIR_NEXT_TYPED_PUSH_INSN(word_pointer_member);
    MIR_NEXT_TYPED_PUSH_INSN(word_pointer);
    MIR_NEXT_TYPED_PUSH_INSN(word_zero);
    MIR_NEXT_TYPED_PUSH_INSN(word_address);
    MIR_NEXT_TYPED_PUSH_INSN(word_load);
    MIR_NEXT_TYPED_PUSH_INSN(word_result_label);
    MIR_NEXT_TYPED_PUSH_INSN(join_label);
    MIR_NEXT_TYPED_PUSH_INSN(phi);
    MIR_NEXT_TYPED_PUSH_INSN(argument);
    MIR_NEXT_TYPED_PUSH_INSN(call);
    MIR_NEXT_TYPED_PUSH_INSN(continuation_jump);
#undef MIR_NEXT_TYPED_PUSH_INSN

    if (input_load->opcode != MIR_LOAD ||
        type_size(input_load->type) != 2 ||
        input_member->opcode != MIR_MEMBER_ADDRESS ||
        input_member->src1 != input_load->dst ||
        input_member->memory_size != 2 ||
        input_value->opcode != MIR_LOAD_INDIRECT ||
        input_value->src1 != input_member->dst ||
        input_value->memory_size != 2 ||
        input_store->opcode != MIR_STORE ||
        input_store->src1 != input_value->dst ||
        !mir_byte_verify_location(input_store, 2, NULL) ||
        state_address->opcode != MIR_ADDRESS ||
        base_member->opcode != MIR_MEMBER_ADDRESS ||
        base_member->src1 != state_address->dst ||
        base_member->memory_size != 2 ||
        base_pointer->opcode != MIR_LOAD_INDIRECT ||
        base_pointer->src1 != base_member->dst ||
        type_size(base_pointer->type) != 2 ||
        index_reload->opcode != MIR_LOAD ||
        !mir_same_scalar_memory_location(
            input_store, index_reload) ||
        object_address->opcode != MIR_INDEX_ADDRESS ||
        object_address->src1 != base_pointer->dst ||
        object_address->src2 != index_reload->dst ||
        object_address->immediate <= 0 ||
        object_store->opcode != MIR_STORE ||
        object_store->src1 != object_address->dst ||
        !mir_byte_verify_location(object_store, 2, NULL) ||
        object_width_load->opcode != MIR_LOAD ||
        !mir_same_scalar_memory_location(
            object_store, object_width_load) ||
        width_member->opcode != MIR_MEMBER_ADDRESS ||
        width_member->src1 != object_width_load->dst ||
        width_member->memory_size != 1 ||
        width_load->opcode != MIR_LOAD_INDIRECT ||
        width_load->src1 != width_member->dst ||
        width_load->memory_size != 1 ||
        one->opcode != MIR_CONST || one->immediate != 1 ||
        width_extend->opcode != MIR_UNARY ||
        width_extend->immediate != 0 ||
        width_extend->src1 != width_load->dst ||
        compare->opcode != MIR_BINARY ||
        compare->immediate != TOK_EQ ||
        compare->src1 != width_extend->dst ||
        compare->src2 != one->dst ||
        branch->opcode != MIR_BRANCH_FALSE ||
        branch->src1 != compare->dst ||
        object_byte_load->opcode != MIR_LOAD ||
        !mir_same_scalar_memory_location(
            object_store, object_byte_load) ||
        byte_pointer_member->opcode != MIR_MEMBER_ADDRESS ||
        byte_pointer_member->src1 != object_byte_load->dst ||
        byte_pointer_member->memory_size != 2 ||
        byte_pointer->opcode != MIR_LOAD_INDIRECT ||
        byte_pointer->src1 != byte_pointer_member->dst ||
        type_size(byte_pointer->type) != 2 ||
        byte_zero->opcode != MIR_CONST ||
        byte_zero->immediate != 0 ||
        byte_address->opcode != MIR_INDEX_ADDRESS ||
        byte_address->src1 != byte_pointer->dst ||
        byte_address->src2 != byte_zero->dst ||
        byte_address->immediate != 1 ||
        byte_load->opcode != MIR_LOAD_INDIRECT ||
        byte_load->src1 != byte_address->dst ||
        byte_load->memory_size != 1 ||
        (byte_load->type & TYPE_UNSIGNED) != 0 ||
        byte_label->opcode != MIR_LABEL ||
        join_jump->opcode != MIR_JUMP ||
        word_label->opcode != MIR_LABEL ||
        branch->label != word_label->label ||
        object_word_load->opcode != MIR_LOAD ||
        !mir_same_scalar_memory_location(
            object_store, object_word_load) ||
        word_pointer_member->opcode != MIR_MEMBER_ADDRESS ||
        word_pointer_member->src1 != object_word_load->dst ||
        word_pointer_member->memory_size != 2 ||
        word_pointer_member->immediate !=
            byte_pointer_member->immediate ||
        word_pointer->opcode != MIR_LOAD_INDIRECT ||
        word_pointer->src1 != word_pointer_member->dst ||
        type_size(word_pointer->type) != 2 ||
        word_zero->opcode != MIR_CONST ||
        word_zero->immediate != 0 ||
        word_address->opcode != MIR_INDEX_ADDRESS ||
        word_address->src1 != word_pointer->dst ||
        word_address->src2 != word_zero->dst ||
        word_address->immediate != 2 ||
        word_load->opcode != MIR_LOAD_INDIRECT ||
        word_load->src1 != word_address->dst ||
        word_load->memory_size != 2 ||
        word_result_label->opcode != MIR_LABEL ||
        join_label->opcode != MIR_LABEL ||
        join_jump->label != join_label->label ||
        phi->opcode != MIR_PHI ||
        phi->src1 != byte_load->dst ||
        phi->src2 != word_load->dst ||
        phi->phi_pred1 != byte_label->label ||
        phi->phi_pred2 != word_result_label->label ||
        argument->opcode != MIR_ARG ||
        argument->src1 != phi->dst ||
        call->opcode != MIR_CALL ||
        !mir_call_uses_inline_postincrement_store(
            helper, (int)(call - mir.insns), &argument_value,
            &continuation_instruction, &continuation_label) ||
        argument_value != phi->dst ||
        continuation_jump != &mir.insns[continuation_instruction])
        return 0;
    plan->input_member_value = input_member->dst;
    plan->base_member_value = base_member->dst;
    plan->width_offset = (int)width_member->immediate;
    plan->pointer_offset = (int)byte_pointer_member->immediate;
    plan->stride = (int)object_address->immediate;
    plan->continuation_instruction = continuation_instruction;
    plan->continuation_label = continuation_label;
    return 1;
}

static int mir_emit_inline_typed_load_push(
    MirStream *out, const int *labels,
    const struct MirInlinePostincrementStore *helper,
    int shared_helper_label,
    const struct MirInlineTypedLoadPush *plan)
{
    char operand[160];
    int word_label = new_label();
    int value_label = new_label();

    if (!mir_emit_address_value_to_hl(
            out, plan->input_member_value))
        return 0;
    mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    if (plan->stride == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    else if (plan->stride > 2)
        mir_emit_mul_hl_const(out, (unsigned long)plan->stride);
    if (!mir_prepare_constant_absolute_operand(
            out, plan->base_member_value,
            operand, sizeof(operand)))
        return 0;
    mir_stream_printf(out, "\tld de,(%s)\n\tadd hl,de\n\tpush hl\n",
            operand);
    if (plan->width_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->width_offset);
    mir_stream_puts("\tld a,(hl)\n\tpop hl\n\tcp 1\n", out);
    mir_stream_printf(out, "\tjp nz, L%d\n", word_label);
    if (plan->pointer_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->pointer_offset);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n"
          "\tld l,(hl)\n", out);
    mir_emit_signed_byte_extend(out);
    mir_stream_printf(out, "\tjp L%d\nL%d:\n", value_label, word_label);
    if (plan->pointer_offset != 0)
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                plan->pointer_offset);
    mir_stream_puts("\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n"
          "\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    mir_stream_printf(out, "L%d:\n", value_label);
    mir_emit_inline_postincrement_store(
        out, labels, helper, -1, 1, shared_helper_label,
        plan->continuation_label);
    return 1;
}

static int mir_match_inline_word_load_push(
    int instruction, const struct MirInlinePostincrementStore *helper,
    struct MirInlineWordLoadPush *plan)
{
    const struct MirInsn *base1;
    const struct MirInsn *index1;
    const struct MirInsn *zero1;
    const struct MirInsn *index_value1;
    const struct MirInsn *address1;
    const struct MirInsn *low;
    const struct MirInsn *base2;
    const struct MirInsn *index2;
    const struct MirInsn *zero2;
    const struct MirInsn *index_value2;
    const struct MirInsn *one;
    const struct MirInsn *index_plus_one;
    const struct MirInsn *address2;
    const struct MirInsn *high;
    const struct MirInsn *eight;
    const struct MirInsn *high_extend;
    const struct MirInsn *shift;
    const struct MirInsn *low_extend;
    const struct MirInsn *combine;
    const struct MirInsn *argument;
    const struct MirInsn *call;
    int argument_value;
    int continuation_instruction;
    int continuation_label;
    int cursor = instruction;

#define MIR_NEXT_WORD_PUSH_INSN(target) \
    do { \
        if (cursor < 0 || cursor >= mir.count) \
            return 0; \
        target = &mir.insns[cursor]; \
        cursor = mir_low_byte_loop_next_effective(cursor); \
    } while (0)

    memset(plan, 0, sizeof(*plan));
    MIR_NEXT_WORD_PUSH_INSN(base1);
    MIR_NEXT_WORD_PUSH_INSN(index1);
    MIR_NEXT_WORD_PUSH_INSN(zero1);
    MIR_NEXT_WORD_PUSH_INSN(index_value1);
    MIR_NEXT_WORD_PUSH_INSN(address1);
    MIR_NEXT_WORD_PUSH_INSN(low);
    MIR_NEXT_WORD_PUSH_INSN(base2);
    MIR_NEXT_WORD_PUSH_INSN(index2);
    MIR_NEXT_WORD_PUSH_INSN(zero2);
    MIR_NEXT_WORD_PUSH_INSN(index_value2);
    MIR_NEXT_WORD_PUSH_INSN(one);
    MIR_NEXT_WORD_PUSH_INSN(index_plus_one);
    MIR_NEXT_WORD_PUSH_INSN(address2);
    MIR_NEXT_WORD_PUSH_INSN(high);
    MIR_NEXT_WORD_PUSH_INSN(eight);
    MIR_NEXT_WORD_PUSH_INSN(high_extend);
    MIR_NEXT_WORD_PUSH_INSN(shift);
    MIR_NEXT_WORD_PUSH_INSN(low_extend);
    MIR_NEXT_WORD_PUSH_INSN(combine);
    MIR_NEXT_WORD_PUSH_INSN(argument);
    MIR_NEXT_WORD_PUSH_INSN(call);
#undef MIR_NEXT_WORD_PUSH_INSN

    if (base1->opcode != MIR_LOAD || base1->memory_flags != 0 ||
        index1->opcode != MIR_LOAD || index1->memory_flags != 0 ||
        !mir_byte_verify_location(base1, 2, NULL) ||
        !mir_byte_verify_location(index1, 2, NULL) ||
        zero1->opcode != MIR_CONST || zero1->immediate != 0 ||
        index_value1->opcode != MIR_BINARY ||
        index_value1->immediate != '+' ||
        index_value1->src1 != index1->dst ||
        index_value1->src2 != zero1->dst ||
        address1->opcode != MIR_INDEX_ADDRESS ||
        address1->src1 != base1->dst ||
        address1->src2 != index_value1->dst ||
        address1->immediate != 1 ||
        low->opcode != MIR_LOAD_INDIRECT ||
        low->src1 != address1->dst || low->memory_size != 1 ||
        low->memory_flags != 0 ||
        base2->opcode != MIR_LOAD || base2->memory_flags != 0 ||
        !mir_same_scalar_memory_location(base1, base2) ||
        index2->opcode != MIR_LOAD || index2->memory_flags != 0 ||
        !mir_same_scalar_memory_location(index1, index2) ||
        zero2->opcode != MIR_CONST || zero2->immediate != 0 ||
        index_value2->opcode != MIR_BINARY ||
        index_value2->immediate != '+' ||
        index_value2->src1 != index2->dst ||
        index_value2->src2 != zero2->dst ||
        one->opcode != MIR_CONST || one->immediate != 1 ||
        index_plus_one->opcode != MIR_BINARY ||
        index_plus_one->immediate != '+' ||
        index_plus_one->src1 != index_value2->dst ||
        index_plus_one->src2 != one->dst ||
        address2->opcode != MIR_INDEX_ADDRESS ||
        address2->src1 != base2->dst ||
        address2->src2 != index_plus_one->dst ||
        address2->immediate != 1 ||
        high->opcode != MIR_LOAD_INDIRECT ||
        high->src1 != address2->dst || high->memory_size != 1 ||
        high->memory_flags != 0 ||
        eight->opcode != MIR_CONST || eight->immediate != 8 ||
        high_extend->opcode != MIR_UNARY ||
        high_extend->immediate != 0 ||
        high_extend->src1 != high->dst ||
        shift->opcode != MIR_BINARY ||
        shift->immediate != TOK_SHL ||
        shift->src1 != high_extend->dst ||
        shift->src2 != eight->dst ||
        low_extend->opcode != MIR_UNARY ||
        low_extend->immediate != 0 ||
        low_extend->src1 != low->dst ||
        combine->opcode != MIR_BINARY ||
        combine->immediate != '|' ||
        combine->src1 != low_extend->dst ||
        combine->src2 != shift->dst ||
        argument->opcode != MIR_ARG ||
        argument->src1 != combine->dst ||
        call->opcode != MIR_CALL ||
        !mir_call_uses_inline_postincrement_store(
            helper, (int)(call - mir.insns), &argument_value,
            &continuation_instruction, &continuation_label) ||
        argument_value != combine->dst)
        return 0;
    plan->base_load = base1;
    plan->index_load = index1;
    plan->call_instruction = (int)(call - mir.insns);
    plan->continuation_instruction = continuation_instruction;
    plan->continuation_label = continuation_label;
    return 1;
}

static void mir_emit_inline_word_load_push(
    MirStream *out, const int *labels,
    const struct MirInlinePostincrementStore *helper,
    int shared_helper_label,
    const struct MirInlineWordLoadPush *plan)
{
    mir_emit_named_word_load_to_hl(out, plan->base_load);
    mir_stream_puts("\tpush hl\n", out);
    mir_emit_named_word_load_to_hl(out, plan->index_load);
    mir_stream_puts("\tpop de\n\tadd hl,de\n"
          "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n\tex de,hl\n", out);
    mir_emit_inline_postincrement_store(
        out, labels, helper, -1, 1, shared_helper_label,
        plan->continuation_label);
}

static int mir_narrow_store_preserves_hl(int instruction, int value)
{
    const struct MirInsn *store;
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (instruction < 0 || instruction >= mir.count)
        return 0;
    store = &mir.insns[instruction];
    if (store->opcode != MIR_STORE || store->src1 != value ||
        mir_object_is_fully_promoted(store->object) ||
        mir_store_is_dead(instruction) ||
        !mir_scalar_memory_location(store, &memory_type,
                                    &memory_storage, &memory_offset))
        return 0;
    if (store->memory_size > 0 &&
        !type_is_struct_object(store->type))
        memory_type = store->type;
    if (type_size(memory_type) != 1)
        return 0;
    return memory_storage == SC_GLOBAL ||
           memory_storage == SC_EXTERN ||
           ((memory_storage == SC_LOCAL ||
             memory_storage == SC_PARAM) &&
            memory_offset >= -128 && memory_offset <= 127);
}

static int mir_label_has_other_reference(int label, int owner)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (instruction != owner &&
            (insn->opcode == MIR_JUMP ||
             insn->opcode == MIR_BRANCH_FALSE) &&
            insn->label == label)
            return 1;
        if (insn->opcode == MIR_PHI &&
            (insn->phi_pred1 == label ||
             insn->phi_pred2 == label))
            return 1;
    }
    return 0;
}

static int mir_match_dense_byte_switch(
    int start, struct MirDenseByteSwitch *dispatch)
{
    int cursor = start;
    int target;

    memset(dispatch, 0, sizeof(*dispatch));
    dispatch->condition = -1;
    dispatch->default_label = -1;
    dispatch->end_instruction = -1;
    dispatch->minimum_case = 256;
    dispatch->maximum_case = -1;
    for (target = 0; target < 256; ++target)
        dispatch->targets[target] = -1;
    for (;;) {
        const struct MirInsn *constant;
        const struct MirInsn *binary;
        const struct MirInsn *branch;
        const struct MirInsn *jump;
        const struct MirInsn *condition_definition;
        long constant_value;
        int condition;
        int branch_target;
        int case_target;
        int next;

        if (cursor < 0 || cursor + 3 >= mir.count)
            return 0;
        constant = &mir.insns[cursor];
        binary = &mir.insns[cursor + 1];
        branch = &mir.insns[cursor + 2];
        jump = &mir.insns[cursor + 3];
        if (constant->opcode != MIR_CONST ||
            binary->opcode != MIR_BINARY ||
            binary->immediate != TOK_EQ ||
            mir_value_use_count(constant->dst) != 1 ||
            mir_value_use_count(binary->dst) != 1 ||
            branch->opcode != MIR_BRANCH_FALSE ||
            branch->src1 != binary->dst ||
            jump->opcode != MIR_JUMP)
            return 0;
        if (binary->src1 == constant->dst)
            condition = binary->src2;
        else if (binary->src2 == constant->dst)
            condition = binary->src1;
        else
            return 0;
        if (dispatch->condition < 0) {
            int condition_size;

            condition_definition = mir_definition(condition);
            if (condition_definition == NULL)
                return 0;
            condition_size = type_size(condition_definition->type);
            if (condition_size == 1) {
                if (!type_is_bool(condition_definition->type) &&
                    (condition_definition->type & TYPE_UNSIGNED) == 0)
                    return 0;
            } else if (condition_size != 2 ||
                       type_ptr_depth(condition_definition->type) != 0 ||
                       type_is_float(condition_definition->type)) {
                return 0;
            }
            dispatch->condition = condition;
            dispatch->condition_size = condition_size;
        } else if (condition != dispatch->condition) {
            return 0;
        }
        constant_value = constant->immediate;
        if (constant_value < 0 || constant_value > 255 ||
            dispatch->targets[(int)constant_value] >= 0 ||
            branch->label < 0 || branch->label >= mir.next_label ||
            jump->label < 0 || jump->label >= mir.next_label)
            return 0;
        branch_target = mir_find_label(branch->label);
        case_target = mir_find_label(jump->label);
        if (branch_target < 0 || case_target < 0 ||
            !mir_phi_copies_are_empty(cursor + 2, cursor + 3) ||
            !mir_phi_copies_are_empty(cursor + 3, case_target))
            return 0;
        dispatch->targets[(int)constant_value] = jump->label;
        if (constant_value < dispatch->minimum_case)
            dispatch->minimum_case = (int)constant_value;
        if (constant_value > dispatch->maximum_case)
            dispatch->maximum_case = (int)constant_value;
        ++dispatch->case_count;
        next = cursor + 4;
        if (next < mir.count &&
            mir.insns[next].opcode == MIR_LABEL &&
            mir.insns[next].label == branch->label) {
            if (mir_label_has_other_reference(branch->label, cursor + 2) ||
                !mir_phi_copies_are_empty(cursor + 2, branch_target))
                return 0;
            cursor = next + 1;
            if (cursor < mir.count &&
                mir.insns[cursor].opcode == MIR_CONST)
                continue;
            if (cursor >= mir.count ||
                mir.insns[cursor].opcode != MIR_JUMP ||
                mir.insns[cursor].label < 0 ||
                mir.insns[cursor].label >= mir.next_label)
                return 0;
            dispatch->default_label = mir.insns[cursor].label;
            target = mir_find_label(dispatch->default_label);
            if (target < 0 ||
                !mir_phi_copies_are_empty(cursor, target))
                return 0;
            dispatch->end_instruction = cursor;
            break;
        }
        if (!mir_phi_copies_are_empty(cursor + 2, branch_target))
            return 0;
        dispatch->default_label = branch->label;
        dispatch->end_instruction = cursor + 3;
        if (next + 1 < mir.count &&
            mir.insns[next].opcode == MIR_LABEL &&
            !mir_label_has_other_reference(
                mir.insns[next].label, -1) &&
            mir.insns[next + 1].opcode == MIR_JUMP &&
            mir.insns[next + 1].label == dispatch->default_label &&
            mir_phi_copies_are_empty(next + 1, branch_target))
            dispatch->end_instruction = next + 1;
        break;
    }
    if (dispatch->case_count < 128) {
        int width = dispatch->maximum_case -
                    dispatch->minimum_case + 1;
        int blocks = mir_cfg_block_count();
        int minimum_cases =
            blocks >= 80 && blocks <= 95 ? 11 :
            blocks >= 100 ? 30 : 32;

        if (dispatch->case_count < minimum_cases ||
            width > 64 ||
            dispatch->case_count * 2 < width)
            return 0;
    }
    if (dispatch->condition_size == 2 &&
        dispatch->case_count < 42 &&
        !((dispatch->case_count >= 11 &&
           dispatch->case_count <= 16 &&
           mir_cfg_block_count() >= 80 &&
           mir_cfg_block_count() <= 95) ||
          (dispatch->case_count >= 30 &&
           mir_cfg_block_count() >= 100)))
        return 0;
    if (dispatch->default_label < 0)
        return 0;
    cursor = start - 1;
    while (cursor >= 0 && mir.insns[cursor].opcode == MIR_NOP)
        --cursor;
    dispatch->condition_in_hl =
        mir_narrow_store_preserves_hl(
            cursor, dispatch->condition);
    return 1;
}

static void mir_emit_dense_byte_switch(
    MirStream *out, const int *labels,
    const struct MirDenseByteSwitch *dispatch)
{
    int table_label = new_label();
    int width =
        dispatch->maximum_case - dispatch->minimum_case + 1;
    int value;

    if (!dispatch->condition_in_hl)
        mir_emit_virtual_load(out, dispatch->condition);
    if (dispatch->condition_size == 2)
        mir_stream_printf(out, "\tld a,h\n\tor a\n\tjp nz, L%d\n",
                labels[dispatch->default_label]);
    if (width < 256) {
        if (dispatch->minimum_case != 0) {
            mir_stream_printf(out, "\tld a,l\n\tsub %d\n"
                         "\tjp c, L%d\n",
                    dispatch->minimum_case,
                    labels[dispatch->default_label]);
        } else {
            mir_stream_puts("\tld a,l\n", out);
        }
        mir_stream_printf(out, "\tcp %d\n\tjp nc, L%d\n",
                width, labels[dispatch->default_label]);
        if (dispatch->minimum_case != 0)
            mir_stream_puts("\tld l,a\n\tld h,0\n", out);
    }
    mir_stream_puts("\tadd hl,hl\n", out);
    mir_stream_printf(out, "\tld de,L%d\n\tadd hl,de\n"
                 "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
                 "\tex de,hl\n\tjp (hl)\nL%d:\n",
            table_label, table_label);
    for (value = dispatch->minimum_case;
         value <= dispatch->maximum_case; ++value) {
        int target = dispatch->targets[value] >= 0
            ? dispatch->targets[value]
            : dispatch->default_label;
        mir_stream_printf(out, "\tdw L%d\n", labels[target]);
    }
}

static int mir_match_dense_byte_switch_condition_load(
    int instruction, struct MirDenseByteSwitch *dispatch)
{
    const struct MirInsn *base;
    const struct MirInsn *member;
    const struct MirInsn *load;
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (instruction < 0 || instruction + 3 >= mir.count)
        return 0;
    base = &mir.insns[instruction];
    member = &mir.insns[instruction + 1];
    load = &mir.insns[instruction + 2];
    if (base->opcode != MIR_LOAD ||
        type_size(base->type) != 2 ||
        base->memory_flags != 0 ||
        member->opcode != MIR_MEMBER_ADDRESS ||
        member->src1 != base->dst ||
        member->immediate != 0 ||
        (member->memory_size != 1 && member->memory_size != 2) ||
        member->memory_flags != 0 ||
        load->opcode != MIR_LOAD_INDIRECT ||
        load->src1 != member->dst ||
        load->memory_size != member->memory_size ||
        load->memory_flags != 0 ||
        mir_value_use_count(base->dst) != 1 ||
        mir_value_use_count(member->dst) != 1 ||
        !mir_scalar_memory_location(
            base, &memory_type, &memory_storage, &memory_offset) ||
        (memory_storage != SC_LOCAL &&
         memory_storage != SC_PARAM &&
         memory_storage != SC_GLOBAL &&
         memory_storage != SC_EXTERN) ||
        !mir_match_dense_byte_switch(
            instruction + 3, dispatch) ||
        dispatch->condition != load->dst)
        return 0;
    dispatch->condition_in_hl = 1;
    return 1;
}

struct MirDensePostincrementIndexSwitch {
    struct MirDenseByteSwitch dispatch;
    int skip_through;
    int base_address_value;
    int increment_instruction;
    int increment_value;
    int pc_offset;
    int element_offset;
    int stride;
};

struct MirNamedZeroBranch {
    int member_value;
    int branch_instruction;
    int branch_label;
    int target_instruction;
    int width;
};

static int mir_match_named_zero_branch(
    int instruction, struct MirNamedZeroBranch *plan)
{
    const struct MirInsn *base;
    const struct MirInsn *member;
    const struct MirInsn *load;
    const struct MirInsn *branch;
    int member_instruction;
    int load_instruction;
    int branch_instruction;
    int target;

    memset(plan, 0, sizeof(*plan));
    if (mir_cfg_block_count() < 70 ||
        instruction < 0 || instruction >= mir.count)
        return 0;
    base = &mir.insns[instruction];
    member_instruction =
        mir_low_byte_loop_next_effective(instruction);
    if (member_instruction >= mir.count)
        return 0;
    member = &mir.insns[member_instruction];
    load_instruction =
        mir_low_byte_loop_next_effective(member_instruction);
    if (load_instruction >= mir.count)
        return 0;
    load = &mir.insns[load_instruction];
    branch_instruction =
        mir_low_byte_loop_next_effective(load_instruction);
    if (branch_instruction >= mir.count)
        return 0;
    branch = &mir.insns[branch_instruction];
    target = branch->opcode == MIR_BRANCH_FALSE
        ? mir_find_label(branch->label) : -1;
    if (base->opcode != MIR_ADDRESS ||
        member->opcode != MIR_MEMBER_ADDRESS ||
        member->src1 != base->dst ||
        member->memory_flags != 0 ||
        load->opcode != MIR_LOAD_INDIRECT ||
        load->src1 != member->dst ||
        load->memory_flags != 0 || load->bit_width != 0 ||
        (load->memory_size != 1 && load->memory_size != 2) ||
        branch->opcode != MIR_BRANCH_FALSE ||
        branch->src1 != load->dst ||
        target < 0 ||
        mir_value_use_count(base->dst) != 1 ||
        mir_value_use_count(member->dst) != 1 ||
        mir_value_use_count(load->dst) != 1)
        return 0;
    plan->member_value = member->dst;
    plan->branch_instruction = branch_instruction;
    plan->branch_label = branch->label;
    plan->target_instruction = target;
    plan->width = load->memory_size;
    return 1;
}

static int mir_emit_named_zero_branch(
    MirStream *out, const int *labels,
    const struct MirNamedZeroBranch *plan)
{
    char operand[160];

    if (mir_prepare_constant_absolute_operand(
            out, plan->member_value, operand, sizeof(operand))) {
        if (plan->width == 1)
            mir_stream_printf(out, "\tld a,(%s)\n\tld l,a\n\tld h,0\n",
                    operand);
        else
            mir_stream_printf(out, "\tld hl,(%s)\n", operand);
    } else {
        if (!mir_emit_address_value_to_hl(
                out, plan->member_value))
            return 0;
        if (plan->width == 1)
            mir_stream_puts("\tld l,(hl)\n\tld h,0\n", out);
        else
            mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n",
                  out);
    }
    mir_stream_puts("\tld a,h\n\tor l\n", out);
    return mir_emit_conditional_branch_with_phi_copies(
        out, labels, "nz", plan->branch_instruction,
        plan->target_instruction, plan->branch_label);
}

static int mir_match_dense_postincrement_index_switch(
    int instruction, struct MirDensePostincrementIndexSwitch *plan)
{
    const struct MirInsn *base_address;
    const struct MirInsn *base_member;
    const struct MirInsn *base_load;
    const struct MirInsn *pc_load;
    const struct MirInsn *one;
    const struct MirInsn *increment;
    const struct MirInsn *pc_store;
    const struct MirInsn *index;
    const struct MirInsn *element_store;
    const struct MirInsn *element_load;
    const struct MirInsn *condition_member;
    const struct MirInsn *condition_load;
    int cursor;
    int dense_match;
    int element_location;
    int element_same;
    int pc_location;
    int pc_same;
    int pc_offset;
    int element_offset;

    memset(plan, 0, sizeof(*plan));
    if (instruction < 0 || instruction >= mir.count)
        return 0;
    base_address = &mir.insns[instruction];
    cursor = mir_low_byte_loop_next_effective(instruction);
    if (cursor >= mir.count)
        return 0;
    base_member = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    base_load = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    pc_load = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    one = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    increment = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    pc_store = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    index = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    element_store = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    element_load = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    condition_member = &mir.insns[cursor];
    cursor = mir_low_byte_loop_next_effective(cursor);
    if (cursor >= mir.count)
        return 0;
    condition_load = &mir.insns[cursor];
    pc_location = mir_byte_verify_location(
        pc_load, 2, &pc_offset);
    pc_same = mir_same_scalar_memory_location(
        pc_load, pc_store);
    element_location = mir_byte_verify_location(
        element_store, 2, &element_offset);
    element_same = mir_same_scalar_memory_location(
        element_store, element_load);
    dense_match = mir_match_dense_byte_switch(
        cursor + 1, &plan->dispatch);
    if (base_address->opcode != MIR_ADDRESS ||
        base_member->opcode != MIR_MEMBER_ADDRESS ||
        base_member->src1 != base_address->dst ||
        base_member->memory_size != 2 ||
        base_load->opcode != MIR_LOAD_INDIRECT ||
        base_load->src1 != base_member->dst ||
        type_size(base_load->type) != 2 ||
        pc_load->opcode != MIR_LOAD ||
        !pc_location ||
        one->opcode != MIR_CONST || one->immediate != 1 ||
        increment->opcode != MIR_BINARY ||
        increment->immediate != '+' ||
        increment->src1 != pc_load->dst ||
        increment->src2 != one->dst ||
        pc_store->opcode != MIR_STORE ||
        pc_store->src1 != increment->dst ||
        !pc_same ||
        index->opcode != MIR_INDEX_ADDRESS ||
        index->src1 != base_load->dst ||
        index->src2 != pc_load->dst ||
        index->immediate <= 0 ||
        element_store->opcode != MIR_STORE ||
        element_store->src1 != index->dst ||
        !element_location ||
        element_load->opcode != MIR_LOAD ||
        !element_same ||
        condition_member->opcode != MIR_MEMBER_ADDRESS ||
        condition_member->src1 != element_load->dst ||
        condition_member->immediate != 0 ||
        condition_member->memory_size != 1 ||
        condition_load->opcode != MIR_LOAD_INDIRECT ||
        condition_load->src1 != condition_member->dst ||
        condition_load->memory_size != 1 ||
        !dense_match ||
        plan->dispatch.condition != condition_load->dst ||
        mir_value_use_count(base_address->dst) != 1 ||
        mir_value_use_count(base_member->dst) != 1 ||
        mir_value_use_count(base_load->dst) != 1 ||
        mir_value_use_count(pc_load->dst) != 2 ||
        mir_value_use_count(one->dst) != 1 ||
        mir_value_use_count(index->dst) != 1 ||
        mir_value_use_count(element_load->dst) != 1 ||
        mir_value_use_count(condition_member->dst) != 1 ||
        mir_value_use_count(condition_load->dst) !=
            plan->dispatch.case_count)
        return 0;
    plan->dispatch.condition_in_hl = 1;
    plan->skip_through = plan->dispatch.end_instruction;
    plan->base_address_value = base_member->dst;
    plan->increment_instruction = (int)(increment - mir.insns);
    plan->increment_value = increment->dst;
    plan->pc_offset = pc_offset;
    plan->element_offset = element_offset;
    plan->stride = (int)index->immediate;
    return 1;
}

static int mir_emit_dense_postincrement_index_switch(
    MirStream *out, const int *labels,
    const struct MirDensePostincrementIndexSwitch *plan)
{
    char operand[160];
    int saved_instruction;

    mir_stream_printf(out,
            "\tld l,(ix%+d)\n\tld h,(ix%+d)\n"
            "\tpush hl\n\tinc hl\n",
            plan->pc_offset, plan->pc_offset + 1);
    mir_emit_frame_word_store(out, plan->pc_offset);
    saved_instruction = mir_emit_instruction_index;
    mir_emit_instruction_index = plan->increment_instruction;
    mir_emit_virtual_store(out, plan->increment_value);
    mir_emit_instruction_index = saved_instruction;
    mir_stream_puts("\tpop hl\n", out);
    if (plan->stride == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    else if (plan->stride > 2)
        mir_emit_mul_hl_const(out, (unsigned long)plan->stride);
    if (!mir_prepare_constant_absolute_operand(
            out, plan->base_address_value,
            operand, sizeof(operand)))
        return 0;
    mir_stream_printf(out, "\tld de,(%s)\n\tadd hl,de\n"
                 "\tld (ix%+d),l\n\tld (ix%+d),h\n"
                 "\tld l,(hl)\n\tld h,0\n",
            operand, plan->element_offset, plan->element_offset + 1);
    mir_emit_dense_byte_switch(out, labels, &plan->dispatch);
    return 1;
}

static int mir_emit_named_word_load_to_hl(
    MirStream *out, const struct MirInsn *load)
{
    int memory_type;
    int memory_storage;
    int memory_offset;

    if (!mir_scalar_memory_location(
            load, &memory_type, &memory_storage, &memory_offset) ||
        type_size(memory_type) != 2)
        return 0;
    if (memory_storage == SC_GLOBAL ||
        memory_storage == SC_EXTERN) {
        struct Sym *global = find_global(load->name);
        const char *assembly_name = asm_name_for(
            global != NULL ? sym_asm_name(global)
                           : mir_declared_link_name(load->name));

        if (memory_storage == SC_EXTERN &&
            mir_extrn_should_emit(global))
            mir_stream_printf(out, "\textrn %s\n", assembly_name);
        mir_stream_printf(out, "\tld hl,(%s)\n", assembly_name);
        return 1;
    }
    if (memory_storage != SC_LOCAL &&
        memory_storage != SC_PARAM)
        return 0;
    if (memory_offset >= -128 && memory_offset + 1 <= 127) {
        mir_stream_printf(out, "\tld l,(ix%+d)\n\tld h,(ix%+d)\n",
                memory_offset, memory_offset + 1);
        return 1;
    }
    mir_stream_puts("\tpush ix\n\tpop hl\n", out);
    mir_emit_hl_offset_from_ix(out, memory_offset);
    mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    return 1;
}

static int mir_emit_dense_byte_switch_condition_load(
    MirStream *out, const int *labels, int instruction,
    struct MirDenseByteSwitch *dispatch)
{
    if (!mir_match_dense_byte_switch_condition_load(
            instruction, dispatch) ||
        !mir_emit_named_word_load_to_hl(
            out, &mir.insns[instruction]))
        return 0;
    if (dispatch->condition_size == 1)
        mir_stream_puts("\tld l,(hl)\n\tld h,0\n", out);
    else
        mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
    mir_emit_dense_byte_switch(out, labels, dispatch);
    return 1;
}

static int mir_store_feeds_dense_byte_switch(
    int instruction, int value)
{
    const struct MirInsn *definition = mir_definition(value);
    struct MirDenseByteSwitch dispatch;
    int definition_index;
    int offset;
    int iy_offset;
    int next;

    if (!mir_narrow_store_preserves_hl(instruction, value) ||
        definition == NULL)
        return 0;
    if (mir.backend_slots != NULL &&
        mir.backend_slots[value] >= 0) {
        offset = mir_virtual_offset(value);
        iy_offset = mir_virtual_iy_offset(value);
        if (!((offset >= -128 && offset + 1 <= 127) ||
              (mir_virtual_iy_base &&
               iy_offset >= -128 && iy_offset + 1 <= 127)))
            return 0;
    }
    definition_index = (int)(definition - mir.insns);
    for (next = definition_index + 1;
         next < instruction; ++next)
        if (mir.insns[next].opcode != MIR_NOP)
            return 0;
    next = instruction + 1;
    while (next < mir.count &&
           mir.insns[next].opcode == MIR_NOP)
        ++next;
    return next < mir.count &&
           mir_match_dense_byte_switch(next, &dispatch) &&
           dispatch.condition == value &&
           dispatch.condition_in_hl;
}

static int mir_value_is_dense_byte_switch_condition(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    struct MirDenseByteSwitch dispatch;
    int definition_index;
    int store;
    int next;

    if (definition == NULL ||
        definition->opcode != MIR_LOAD_INDIRECT ||
        type_size(definition->type) != 1)
        return 0;
    definition_index = (int)(definition - mir.insns);
    store = definition_index + 1;
    while (store < mir.count &&
           mir.insns[store].opcode == MIR_NOP)
        ++store;
    if (store >= mir.count ||
        mir.insns[store].opcode != MIR_STORE ||
        mir.insns[store].src1 != value ||
        !mir_narrow_store_preserves_hl(store, value))
        return 0;
    next = store + 1;
    while (next < mir.count &&
           mir.insns[next].opcode == MIR_NOP)
        ++next;
    if (next >= mir.count ||
        !mir_match_dense_byte_switch(next, &dispatch) ||
        dispatch.condition != value ||
        !dispatch.condition_in_hl)
        return 0;
    return mir_value_use_count(value) == dispatch.case_count + 1;
}

/* Fuse the dispatch loop's common `word += byte_table[index]` epilogue so
 * the table base and accumulator do not detour through backend slots. */
static int mir_emit_dense_switch_table_pc_advance(
    MirStream *out, int instruction, int *end_instruction)
{
    const struct MirInsn *cpu_address;
    const struct MirInsn *pc_address;
    const struct MirInsn *pc_load;
    const struct MirInsn *table_address;
    const struct MirInsn *index_load;
    const struct MirInsn *index_address;
    const struct MirInsn *length_load;
    const struct MirInsn *length_widen;
    const struct MirInsn *addition;
    const struct MirInsn *pc_store;
    char pc_operand[192];
    char table_operand[192];
    int index_type;
    int index_storage;
    int index_offset;

    if (!mir_spilled_cfg_used_dense_byte_switch ||
        instruction < 0 || instruction + 9 >= mir.count)
        return 0;
    cpu_address = &mir.insns[instruction];
    pc_address = &mir.insns[instruction + 1];
    pc_load = &mir.insns[instruction + 2];
    table_address = &mir.insns[instruction + 3];
    index_load = &mir.insns[instruction + 4];
    index_address = &mir.insns[instruction + 5];
    length_load = &mir.insns[instruction + 6];
    length_widen = &mir.insns[instruction + 7];
    addition = &mir.insns[instruction + 8];
    pc_store = &mir.insns[instruction + 9];
    if (cpu_address->opcode != MIR_ADDRESS ||
        pc_address->opcode != MIR_MEMBER_ADDRESS ||
        pc_address->src1 != cpu_address->dst ||
        pc_load->opcode != MIR_LOAD_INDIRECT ||
        pc_load->src1 != pc_address->dst ||
        pc_load->memory_size != 2 ||
        pc_load->memory_flags != 0 ||
        table_address->opcode != MIR_ADDRESS ||
        !strcmp(cpu_address->name, table_address->name) ||
        index_load->opcode != MIR_LOAD ||
        index_load->memory_flags != 0 ||
        type_size(index_load->type) != 1 ||
        type_is_bool(index_load->type) ||
        (index_load->type & TYPE_UNSIGNED) == 0 ||
        index_address->opcode != MIR_INDEX_ADDRESS ||
        index_address->src1 != table_address->dst ||
        index_address->src2 != index_load->dst ||
        index_address->memory_size != 1 ||
        index_address->memory_flags != 0 ||
        index_address->immediate != 1 ||
        length_load->opcode != MIR_LOAD_INDIRECT ||
        length_load->src1 != index_address->dst ||
        length_load->memory_size != 1 ||
        length_load->memory_flags != 0 ||
        type_is_bool(length_load->type) ||
        (length_load->type & TYPE_UNSIGNED) == 0 ||
        length_widen->opcode != MIR_UNARY ||
        length_widen->src1 != length_load->dst ||
        length_widen->immediate != 0 ||
        type_size(length_widen->type) != 2 ||
        addition->opcode != MIR_BINARY ||
        addition->immediate != '+' ||
        addition->src1 != pc_load->dst ||
        addition->src2 != length_widen->dst ||
        type_size(addition->type) != 2 ||
        pc_store->opcode != MIR_STORE_INDIRECT ||
        pc_store->src1 != pc_address->dst ||
        pc_store->src2 != addition->dst ||
        pc_store->memory_size != 2 ||
        pc_store->memory_flags != 0 ||
        mir_value_use_count(cpu_address->dst) != 1 ||
        mir_value_use_count(pc_address->dst) != 2 ||
        mir_value_use_count(pc_load->dst) != 1 ||
        mir_value_use_count(table_address->dst) != 1 ||
        mir_value_use_count(index_load->dst) != 1 ||
        mir_value_use_count(index_address->dst) != 1 ||
        mir_value_use_count(length_load->dst) != 1 ||
        mir_value_use_count(length_widen->dst) != 1 ||
        mir_value_use_count(addition->dst) != 1 ||
        !mir_constant_absolute_access_supported(pc_load) ||
        !mir_constant_absolute_access_supported(pc_store) ||
        (instruction + 10 < mir.count &&
         !mir_phi_copies_are_empty(
             instruction + 9, instruction + 10)))
        return 0;
    if (!mir_scalar_memory_location(
            index_load, &index_type, &index_storage, &index_offset) ||
        type_size(index_type) != 1 ||
        type_is_bool(index_type) ||
        (index_type & TYPE_UNSIGNED) == 0 ||
        ((index_storage == SC_LOCAL ||
          index_storage == SC_PARAM) &&
         (index_offset < -128 || index_offset > 127)) ||
        (index_storage != SC_LOCAL &&
         index_storage != SC_PARAM &&
         index_storage != SC_GLOBAL &&
         index_storage != SC_EXTERN))
        return 0;
    if (!mir_prepare_constant_absolute_operand(
            out, pc_address->dst,
            pc_operand, sizeof(pc_operand)) ||
        !mir_prepare_constant_absolute_operand(
            out, table_address->dst,
            table_operand, sizeof(table_operand)))
        return -1;
    mir_stream_printf(out, "\tld hl,(%s)\n\tpush hl\n", pc_operand);
    if (index_storage == SC_GLOBAL ||
        index_storage == SC_EXTERN) {
        struct Sym *global = find_global(index_load->name);
        const char *assembly_name = asm_name_for(
            global != NULL ? sym_asm_name(global)
                           : mir_declared_link_name(index_load->name));

        if (index_storage == SC_EXTERN &&
            mir_extrn_should_emit(global))
            mir_stream_printf(out, "\textrn %s\n", assembly_name);
        mir_stream_printf(out, "\tld a,(%s)\n\tld l,a\n", assembly_name);
    } else {
        mir_stream_printf(out, "\tld l,(ix%+d)\n", index_offset);
    }
    mir_stream_puts("\tld h,0\n", out);
    mir_stream_printf(out, "\tld de,%s\n\tadd hl,de\n"
                 "\tld l,(hl)\n\tld h,0\n"
                 "\tpop de\n\tadd hl,de\n\tld (%s),hl\n",
            table_operand, pc_operand);
    *end_instruction = instruction + 9;
    return 1;
}

/* T399 (mir-text-size-plan.md): a phi copy's source or destination only
 * has a concrete backend-slot address when it is an ordinary spilled
 * value (mir.backend_slots[value] >= 0); rematerializable constants,
 * addresses, and directly-named-home values have no slot to compare and
 * are conservatively treated as unable to prove disjointness here (the
 * general swap-safe path below already handles them correctly, it is
 * just not eligible for the fast-path skip). */
static int mir_phi_copy_value_slot_range(int value, int *base, int *width)
{
    int slot;
    if (mir.backend_slots == NULL)
        return 0;
    slot = mir.backend_slots[value];
    if (slot < 0)
        return 0;
    *base = slot;
    *width = mir_value_is_wide(value) ? 2 : 1;
    return 1;
}

static int mir_phi_copy_is_slot_identity(int source, int destination)
{
    int source_base, source_width;
    int destination_base, destination_width;

    return mir_phi_copy_value_slot_range(source, &source_base, &source_width) &&
           mir_phi_copy_value_slot_range(destination, &destination_base,
                                         &destination_width) &&
           source_base == destination_base &&
           source_width == destination_width;
}

/* T399: true only when no copy's destination slot overlaps any *other*
 * copy's source slot. The general push-all-sources/pop-all-destinations
 * shape exists specifically to guard against that hazard (a later
 * destination store clobbering a still-unread source of an earlier
 * copy) for a true simultaneous swap; when every destination is
 * provably a fresh slot disjoint from every other copy's source (the
 * common case for a loop header phi introducing new loop-carried
 * homes), a direct load-then-store per copy reaches the identical
 * result in any order, with no stack round-trip. */
static int mir_phi_copy_group_is_disjoint(const int *sources,
                                          const int *destinations,
                                          int copy_count)
{
    int dest_base[MAX_FLOW], dest_width[MAX_FLOW];
    int src_base[MAX_FLOW], src_width[MAX_FLOW];
    int i, j;

    for (j = 0; j < copy_count; ++j) {
        if (!mir_phi_copy_value_slot_range(destinations[j], &dest_base[j],
                                            &dest_width[j]) ||
            !mir_phi_copy_value_slot_range(sources[j], &src_base[j],
                                            &src_width[j]))
            return 0;
    }
    for (j = 0; j < copy_count; ++j)
        for (i = 0; i < copy_count; ++i) {
            if (i == j)
                continue;
            if (dest_base[j] < src_base[i] + src_width[i] &&
                src_base[i] < dest_base[j] + dest_width[j])
                return 0;
        }
    return 1;
}

static int mir_emit_spilled_phi_copies(MirStream *out, int predecessor,
                                       int successor)
{
    int sources[MAX_FLOW];
    int destinations[MAX_FLOW];
    int copy_count = mir_collect_phi_copies_for_edge(predecessor, successor,
                                                      sources, destinations);
    int copy;

    if (copy_count < 0)
        return 0;
    for (copy = 0; copy < copy_count; ++copy)
        if (destinations[copy] >= 0 &&
            destinations[copy] < mir.next_value &&
            mir.backend_slots != NULL &&
            mir.backend_slots[destinations[copy]] ==
                MIR_BACKEND_SLOT_PHI_ARGUMENT_STACK) {
            if (copy_count != 1 ||
                mir_value_is_wide(sources[copy]))
                return 0;
            if (mir_phi_argument_already_prepushed(
                    predecessor, successor, sources[copy],
                    destinations[copy]))
                return 1;
            mir_emit_virtual_load(out, sources[copy]);
            mir_stream_puts("\tpush hl\n", out);
            return 1;
        }
    /* Item T9 (mir-text-size-plan.md): the general push-all-sources-then-
     * pop-all-destinations-in-reverse shape below exists to let several
     * simultaneous phi copies swap through each other safely (a later
     * destination store must not clobber a still-unread source). With
     * exactly one copy pending there is no other copy to clobber or be
     * clobbered by, so the whole push/pop round-trip through the stack is
     * dead weight - a direct load-then-store reaches the identical result. */
    if (copy_count == 1) {
        if (mir_value_is_wide(sources[0])) {
            mir_emit_virtual_load_wide(out, sources[0]);
            mir_emit_virtual_store_wide(out, destinations[0]);
        } else {
            mir_emit_virtual_load(out, sources[0]);
            mir_emit_virtual_store(out, destinations[0]);
        }
        return 1;
    }
    /* T399: extend the same reasoning to two or more copies whose whole
     * group is provably disjoint (see mir_phi_copy_group_is_disjoint) -
     * no push/pop swap machinery is needed when nothing can be
     * clobbered, regardless of copy order. */
    if (mir_phi_copy_group_is_disjoint(sources, destinations, copy_count)) {
        for (copy = 0; copy < copy_count; ++copy) {
            if (mir_value_is_wide(sources[copy])) {
                mir_emit_virtual_load_wide(out, sources[copy]);
                mir_emit_virtual_store_wide(out, destinations[copy]);
            } else {
                mir_emit_virtual_load(out, sources[copy]);
                mir_emit_virtual_store(out, destinations[copy]);
            }
        }
        return 1;
    }
    for (copy = 0; copy < copy_count; ++copy) {
        if (mir_value_is_wide(sources[copy])) {
            mir_emit_virtual_load_wide(out, sources[copy]);
            mir_stream_puts("\tpush de\n\tpush hl\n", out);
        } else {
            mir_emit_virtual_load(out, sources[copy]);
            mir_stream_puts("\tpush hl\n", out);
        }
    }
    for (copy = copy_count - 1; copy >= 0; --copy) {
        if (mir_value_is_wide(destinations[copy])) {
            mir_stream_puts("\tpop hl\n\tpop de\n", out);
            mir_emit_virtual_store_wide(out, destinations[copy]);
        } else {
            mir_stream_puts("\tpop hl\n", out);
            mir_emit_virtual_store(out, destinations[copy]);
        }
    }
    return 1;
}

int mir_scalar_memory_location(const struct MirInsn *insn, int *type,
                                      int *storage, int *offset)
{
    struct Sym *global;
    int instruction;
    if (insn->object >= 0 && insn->object < mir.object_count) {
        const struct MirObject *object = &mir.objects[insn->object];
        *type = object->type;
        /* A VLA identifier load fetches its saved base pointer. */
        if (mir_declared_is_vla_object(insn->name) &&
            type_ptr_depth(insn->type) > type_ptr_depth(*type))
            *type = insn->type;
        *storage = object->storage;
        *offset = object->offset + (int)insn->immediate;
        return 1;
    }
    if (mir_declared_location(insn->name, type, storage, offset)) {
        if (mir_declared_is_vla_object(insn->name) &&
            type_ptr_depth(insn->type) > type_ptr_depth(*type))
            *type = insn->type;
        *offset += (int)insn->immediate;
        return 1;
    }
    for (instruction = 0; instruction < mir.count; ++instruction)
        if (mir.insns[instruction].opcode == MIR_COMPOUND_ADDRESS &&
            strcmp(mir.insns[instruction].name, insn->name) == 0) {
            *type = insn->type;
            *storage = SC_LOCAL;
            *offset = (int)mir.insns[instruction].immediate +
                      (int)insn->immediate;
            return 1;
        }
    global = find_global(insn->name);
    if (global != NULL) {
        *type = global->type;
        *storage = global->storage;
        *offset = global->offset + (int)insn->immediate;
        return 1;
    }
    return 0;
}

/* T383 (mir-text-size-plan.md): mir_emit_constant_absolute_load/store already
 * collapse a global/extern base plus constant MEMBER_ADDRESS/INDEX_ADDRESS
 * offsets into one absolute operand, but only for the instruction that
 * *dereferences* the resulting address (a scalar load/store). When the same
 * fully-constant chain is instead consumed as plain data - stored as a
 * pointer value, passed as a call argument, or returned - the address is
 * still recomputed at runtime through the whole base-load-and-add sequence.
 * mir_prepare_constant_absolute_operand already proves and formats the exact
 * same operand from insn->dst directly (mir_resolve_constant_absolute_address
 * walks from the instruction defining the value, which is this instruction
 * itself when called against its own dst); reuse it here to materialize the
 * value in one `ld hl,<label>+<offset>` instead. Ordered after each site's
 * existing "only used by a supported load/store" skip so the dereferencing
 * consumer keeps folding the address into its own single instruction rather
 * than this value being materialized and then immediately redereferenced. */
static int mir_emit_constant_absolute_address_value(
    MirStream *out, const struct MirInsn *insn)
{
    char operand[160];

    if (!mir_prepare_constant_absolute_operand(
            out, insn->dst, operand, sizeof(operand)))
        return 0;
    mir_spilled_cfg_used_constant_absolute = 1;
    if (mir_constant_absolute_address_has_index(insn->dst))
        mir_spilled_cfg_used_constant_index_absolute = 1;
    mir_stream_printf(out, "\tld hl,%s\n", operand);
    mir_emit_virtual_store(out, insn->dst);
    return 1;
}

static int mir_emit_dynamic_absolute_index(
    MirStream *out, const struct MirInsn *insn)
{
    const struct MirInsn *base;
    const char *assembly_name;
    struct Sym *global;

    if (!mir_dynamic_absolute_index_base(
            insn, &base, &assembly_name))
        return 0;
    global = find_global(base->name);
    if ((global == NULL || global->needs_extrn) &&
        mir_extrn_should_emit_name(assembly_name))
        mir_stream_printf(out, "\textrn %s\n", assembly_name);
    if (getenv("DCC_MIR_PERF_RECOVERY_REPORT") != NULL)
        fprintf(stderr,
                "; MIR perf-recovery function=%s "
                "feature=dynamic-absolute-index\n",
                mir.name);
    mir_emit_virtual_load(out, insn->src2);
    if (insn->immediate == 2)
        mir_stream_puts("\tadd hl,hl\n", out);
    else if (insn->immediate > 2) {
        unsigned long multiplier =
            (unsigned long)insn->immediate & 0xffffUL;

        if (mir_mul_const_fast_path_eligible(
                multiplier, insn->dst))
            mir_emit_mul_hl_const(out, multiplier);
        else {
            mir_stream_printf(out, "\tld de,%ld\n", insn->immediate);
            mir_emit_runtime_call(out, "__mulu");
        }
    }
    mir_stream_printf(out, "\tld de,%s\n\tadd hl,de\n", assembly_name);
    mir_emit_virtual_store(out, insn->dst);
    return 1;
}

static int mir_emit_constant_absolute_load(
    MirStream *out, const struct MirInsn *insn)
{
    char operand[160];

    if (!mir_constant_absolute_access_supported(insn) ||
        !mir_prepare_constant_absolute_operand(
            out, insn->src1, operand, sizeof(operand)))
        return 0;
    mir_spilled_cfg_used_constant_absolute = 1;
    if (mir_constant_absolute_address_has_index(insn->src1))
        mir_spilled_cfg_used_constant_index_absolute = 1;
    if (insn->memory_size == 1) {
        mir_stream_printf(out, "\tld a,(%s)\n\tld l,a\n", operand);
        if (type_is_bool(insn->type)) {
            int end_label = new_label();
            mir_stream_puts("\tld a,l\n\tor a\n\tld hl,0\n", out);
            mir_stream_printf(out, "\tjp z, L%d\n\tinc hl\nL%d:\n",
                    end_label, end_label);
        } else if ((insn->type & TYPE_UNSIGNED) != 0) {
            mir_stream_puts("\tld h,0\n", out);
        } else {
            mir_emit_signed_byte_extend(out);
        }
    } else {
        mir_stream_printf(out, "\tld hl,(%s)\n", operand);
    }
    mir_emit_virtual_store(out, insn->dst);
    return 1;
}

static int mir_emit_constant_absolute_store(
    MirStream *out, const struct MirInsn *insn)
{
    char operand[160];

    if (!mir_constant_absolute_access_supported(insn) ||
        !mir_prepare_constant_absolute_operand(
            out, insn->src1, operand, sizeof(operand)))
        return 0;
    mir_spilled_cfg_used_constant_absolute = 1;
    if (mir_constant_absolute_address_has_index(insn->src1))
        mir_spilled_cfg_used_constant_index_absolute = 1;
    mir_emit_virtual_load(out, insn->src2);
    if (insn->memory_size == 1)
        mir_stream_printf(out, "\tld a,l\n\tld (%s),a\n", operand);
    else
        mir_stream_printf(out, "\tld (%s),hl\n", operand);
    return 1;
}

static int mir_emit_wide_indirect_store(MirStream *out,
                                        const struct MirInsn *insn)
{
    if (!mir_constant_absolute_access_supported(insn) &&
        mir_value_currently_uses_stack_handoff(
            insn->src1, mir_emit_instruction_index))
        return 0;
    if (mir_emit_constant_absolute_store(out, insn))
        return 1;
    mir_emit_virtual_load_wide(out, insn->src2);
    mir_stream_puts("\tpush de\n\tpush hl\n", out);
    mir_emit_virtual_load(out, insn->src1);
    mir_stream_puts("\tpop bc\n\tpop de\n"
          "\tld (hl),c\n\tinc hl\n\tld (hl),b\n"
          "\tinc hl\n\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
    return 1;
}

static void mir_report_constant_absolute_addresses(void)
{
    int instruction;
    int loads = 0;
    int stores = 0;

    if (getenv("DCC_MIR_ABSOLUTE_ADDRESS_REPORT") == NULL)
        return;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (!mir_constant_absolute_access_supported(insn))
            continue;
        if (insn->opcode == MIR_LOAD_INDIRECT)
            ++loads;
        else
            ++stores;
    }
    if (loads != 0 || stores != 0)
        fprintf(stderr,
                "; MIR absolute-address function=%s loads=%d stores=%d\n",
                mir.name, loads, stores);
}

/* Item T85 (mir-text-size-plan.md): storage class + offset alone do not
 * uniquely identify a scalar memory location - two distinct top-level
 * (non-aggregate-field) globals both resolve to storage=SC_GLOBAL,
 * offset=0 (mir_scalar_memory_location's `global->offset` term is only
 * ever nonzero for a struct/array member, not for a whole scalar object),
 * so comparing storage/offset alone can wrongly treat two different
 * globals as "the same location". This wrapper adds the missing identity
 * check: locals/params carry a real per-object index (insn->object >= 0)
 * that already disambiguates them uniquely, so compare that directly;
 * globals/externs (object == -1) fall back to comparing their resolved
 * declared name instead. Callers that key HL-forwarding re-arms on "is
 * this a fresh load of the exact same location a preceding store/fused
 * inc-dec just wrote" must use this instead of a bare storage/offset
 * compare (see the MIR_STORE and MIR_BINARY selfstore-incdec re-arm
 * sites) - found via a synthetic two-global regression test
 * (`ga = 5; return gb;` wrongly forwarding ga's value as gb's). */
static int mir_same_scalar_memory_location(const struct MirInsn *a,
                                            const struct MirInsn *b)
{
    int type_a, storage_a, offset_a;
    int type_b, storage_b, offset_b;
    if (!mir_scalar_memory_location(a, &type_a, &storage_a, &offset_a) ||
        !mir_scalar_memory_location(b, &type_b, &storage_b, &offset_b))
        return 0;
    if (storage_a != storage_b || offset_a != offset_b)
        return 0;
    if (a->object >= 0 || b->object >= 0)
        return a->object == b->object;
    return strcmp(a->name, b->name) == 0;
}

/* Item 31 (mir-migration-plan-100): a bare "x++;"/"x--;" statement on a
 * frame-local scalar lowers (mir_lower_incdec) to load-x / add-or-sub-1 /
 * store-x, going through the general spilled-scalar-cfg load/store path
 * even though the whole operation only ever touches x's own memory. The
 * legacy AST backend already has a dedicated fast path for exactly this
 * shape (emit_incdec_sym_direct, dcc_symbols.c ~line 1322): "inc (ix+n)"
 * (or a carry-checked byte-pair form for 16-bit values) directly against
 * the frame slot, with no register round-trip at all. This mirrors that
 * fast path for the MIR backend, restricted to the same population
 * legacy restricts itself to - non-pointer, frame-local-or-parameter,
 * 16-bit scalars (pointer ++/-- must scale by the pointee size, not a
 * raw +-1, so it is deliberately excluded here exactly as it is in
 * emit_incdec_sym_direct; 8-bit char and 32-bit long operands are also
 * out of scope for this item, left for a future extension since they
 * need their own byte-width-specific carry-chain shape).
 *
 * Item T36 (mir-text-size-plan.md): emit_incdec_sym_direct also has a
 * separate, simpler fast path for a non-pointer 16-bit *global* (or
 * extern) - is_global_word_sym(s) - "ld hl,(name) / inc-or-dec hl /
 * ld (name),hl", since inc/dec hl is an atomic 16-bit operation with no
 * byte-pair carry chain to worry about (unlike the frame-relative form,
 * which must ripple a carry from the low byte into a separate high-byte
 * "inc (ix+n+1)"). Item 31 only ever mirrored the local/parameter half of
 * emit_incdec_sym_direct - this extends the same predicate to recognize
 * the global/extern case too, confirmed via forced-accept-diff on
 * t2darr.c's check() (a `static int failures; ...; failures++;` pattern)
 * that legacy already takes this fast path (3 instructions) while MIR
 * fell back to a full load/push/pop/add/store round trip (7
 * instructions) for the identical global increment.
 *
 * Returns 1 and sets *store_index to the sole store instruction re-writing
 * the result back to the same frame slot iff every one of the following
 * holds: the operator is '+' or '-' against the exact constant 1; the
 * left operand's value is bound to a 16-bit non-pointer local/parameter/
 * global/extern memory location; and the result value (insn->dst) has
 * exactly one use anywhere in the function, which is a plain MIR_STORE
 * writing to that identical memory location.
 *
 * Note on scope: within a single basic block, or across a loop-carrying
 * PHI merge, any later reference to the same source variable reuses this
 * MIR_BINARY's dst value directly (dcc's MIR construction does local value
 * numbering, confirmed while building this item's test coverage), which
 * gives the value a second use and disqualifies it here - that is by
 * design, since the fused instruction only replaces a genuine store-then-
 * never-read-again sequence, not a live loop induction variable (those
 * stay in registers or flow through PHI nodes, and any store back to a
 * frame slot for them is a spill the register allocator itself decided
 * was necessary, not a redundant round-trip this fusion should remove).
 * This makes the fusion's real-world hit population the population of
 * dead-after-increment locals (e.g. "x++;" as the last touch of x before
 * an unrelated return), confirmed narrow by census (see Execution Log). */
static int mir_binary_is_selfstore_wide_increment(
    int index, int *store_index, int *memory_offset)
{
    const struct MirInsn *insn = &mir.insns[index];
    const struct MirInsn *left_definition;
    const struct MirInsn *right_definition;
    int memory_type, memory_storage, offset;
    int found_store = -1;
    int last_use = index;
    int instruction;

    if (insn->opcode != MIR_BINARY || insn->immediate != '+' ||
        type_size(insn->secondary_offset) != 4)
        return 0;
    right_definition = mir_definition(insn->src2);
    left_definition = mir_definition(insn->src1);
    if (right_definition == NULL ||
        right_definition->opcode != MIR_CONST ||
        right_definition->immediate != 1 ||
        left_definition == NULL ||
        left_definition->opcode != MIR_LOAD ||
        left_definition->memory_flags != 0 ||
        mir_value_use_count(left_definition->dst) != 1 ||
        !mir_scalar_memory_location(
            left_definition, &memory_type,
            &memory_storage, &offset) ||
        type_size(memory_type) != 4 ||
        (memory_storage != SC_LOCAL &&
         memory_storage != SC_PARAM) ||
        offset < -128 || offset + 3 > 127 ||
        (left_definition->object >= 0 &&
         mir_object_address_taken(left_definition->object)))
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *use = &mir.insns[instruction];

        if (use->src1 != insn->dst && use->src2 != insn->dst &&
            !mir_call_uses_value(use, insn->dst))
            continue;
        if (instruction > last_use)
            last_use = instruction;
        if (use->opcode == MIR_STORE &&
            use->src1 == insn->dst &&
            mir_same_scalar_memory_location(left_definition, use)) {
            if (found_store >= 0)
                return 0;
            found_store = instruction;
        }
    }
    if (found_store <= index)
        return 0;
    for (instruction = index + 1;
         instruction < found_store; ++instruction)
        if (mir.insns[instruction].opcode != MIR_NOP)
            return 0;
    for (instruction = found_store + 1;
         instruction <= last_use; ++instruction) {
        const struct MirInsn *candidate = &mir.insns[instruction];

        if (candidate->opcode == MIR_STORE &&
            mir_same_scalar_memory_location(
                left_definition, candidate))
            return 0;
    }
    if (store_index != NULL)
        *store_index = found_store;
    if (memory_offset != NULL)
        *memory_offset = offset;
    return 1;
}

static int mir_value_is_selfstore_wide_increment_result(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int offset;
    int store_index;

    return definition != NULL &&
           definition->opcode == MIR_BINARY &&
           mir_binary_is_selfstore_wide_increment(
               (int)(definition - mir.insns),
               &store_index, &offset);
}

/* The original 16-bit self-store matcher described by Item 31 above. */
static int mir_binary_is_selfstore_incdec(int index, int *store_index)
{
    const struct MirInsn *insn = &mir.insns[index];
    const struct MirInsn *left_definition;
    const struct MirInsn *right_definition;
    int memory_type, memory_storage, memory_offset;
    int store_type, store_storage, store_offset;
    int uses = 0;
    int found_store = -1;
    int scan;

    if (insn->opcode != MIR_BINARY ||
        (insn->immediate != '+' && insn->immediate != '-'))
        return 0;
    if (type_ptr_depth(insn->secondary_offset) > 0 ||
        type_size(insn->secondary_offset) != 2)
        return 0;
    right_definition = mir_definition(insn->src2);
    if (right_definition == NULL || right_definition->opcode != MIR_CONST ||
        right_definition->immediate != 1)
        return 0;
    left_definition = mir_definition(insn->src1);
    if (left_definition == NULL ||
        !mir_scalar_memory_location(left_definition, &memory_type,
                                    &memory_storage, &memory_offset) ||
        (memory_storage != SC_LOCAL && memory_storage != SC_PARAM &&
         memory_storage != SC_GLOBAL && memory_storage != SC_EXTERN) ||
        type_ptr_depth(memory_type) > 0 || type_size(memory_type) != 2)
        return 0;
    for (scan = 0; scan < mir.count; ++scan) {
        const struct MirInsn *use = &mir.insns[scan];
        if (use->src1 != insn->dst && use->src2 != insn->dst)
            continue;
        if (use->opcode != MIR_STORE || use->src1 != insn->dst ||
            !mir_scalar_memory_location(use, &store_type, &store_storage,
                                        &store_offset) ||
            store_storage != memory_storage || store_offset != memory_offset)
            return 0;
        found_store = scan;
        ++uses;
    }
    if (uses != 1)
        return 0;
    *store_index = found_store;
    return 1;
}

static int mir_integer_constant_expression(
    int value, long *result, int depth)
{
    const struct MirInsn *definition = mir_definition(value);
    long left;
    long right;

    if (definition == NULL || result == NULL || depth > 8)
        return 0;
    if (definition->opcode == MIR_CONST) {
        *result = definition->immediate;
        return 1;
    }
    if (definition->opcode != MIR_BINARY ||
        type_size(definition->secondary_offset) > 2 ||
        !mir_integer_constant_expression(
            definition->src1, &left, depth + 1) ||
        !mir_integer_constant_expression(
            definition->src2, &right, depth + 1))
        return 0;
    return mir_fold_constant_binary(
        definition->immediate, left, right,
        definition->secondary_offset, result);
}

static int mir_binary_is_selfstore_small_adjust(
    int index, int *store_index, int *amount)
{
    const struct MirInsn *insn = &mir.insns[index];
    const struct MirInsn *left_definition;
    const struct MirInsn *right_definition;
    long constant_amount;
    int memory_type, memory_storage, memory_offset;
    int uses = 0;
    int found_store = -1;
    int scan;

    if (insn->opcode != MIR_BINARY ||
        (insn->immediate != '+' && insn->immediate != '-') ||
        type_size(insn->secondary_offset) != 2)
        return 0;
    right_definition = mir_definition(insn->src2);
    if (right_definition == NULL ||
        right_definition->opcode != MIR_CONST ||
        right_definition->immediate < 2 ||
        right_definition->immediate > 255)
        return 0;
    constant_amount = right_definition->immediate;
    left_definition = mir_definition(insn->src1);
    if (left_definition == NULL ||
        left_definition->memory_flags != 0 ||
        !mir_scalar_memory_location(
            left_definition, &memory_type,
            &memory_storage, &memory_offset) ||
        (memory_storage != SC_LOCAL &&
         memory_storage != SC_PARAM &&
         memory_storage != SC_GLOBAL &&
         memory_storage != SC_EXTERN) ||
        type_size(memory_type) != 2)
        return 0;
    for (scan = 0; scan < mir.count; ++scan) {
        const struct MirInsn *use = &mir.insns[scan];

        if (use->src1 != insn->dst && use->src2 != insn->dst)
            continue;
        if (use->opcode != MIR_STORE ||
            use->src1 != insn->dst ||
            use->memory_flags != 0 ||
            !mir_same_scalar_memory_location(
                left_definition, use))
            return 0;
        found_store = scan;
        ++uses;
    }
    if (uses != 1)
        return 0;
    if (store_index != NULL)
        *store_index = found_store;
    if (amount != NULL)
        *amount = insn->immediate == '-'
            ? -(int)constant_amount
            : (int)constant_amount;
    return 1;
}

static int mir_binary_is_selfstore_global_predecrement_load(
    int index, int *store_index, int *load_index, int *amount)
{
    const struct MirInsn *insn = &mir.insns[index];
    const struct MirInsn *left_definition;
    const struct MirInsn *load = NULL;
    long constant_amount;
    int memory_type, memory_storage, memory_offset;
    int found_store = -1;
    int found_load = -1;
    int instruction;

    if (insn->opcode != MIR_BINARY || insn->immediate != '-' ||
        type_size(insn->secondary_offset) != 2 ||
        !mir_integer_constant_expression(
            insn->src2, &constant_amount, 0) ||
        constant_amount < 1 || constant_amount > 8)
        return 0;
    left_definition = mir_definition(insn->src1);
    if (left_definition == NULL ||
        left_definition->memory_flags != 0 ||
        !mir_scalar_memory_location(
            left_definition, &memory_type,
            &memory_storage, &memory_offset) ||
        (memory_storage != SC_GLOBAL &&
         memory_storage != SC_EXTERN) ||
        type_ptr_depth(memory_type) == 0 ||
        type_size(memory_type) != 2)
        return 0;
    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *use = &mir.insns[instruction];

        if (use->src1 != insn->dst && use->src2 != insn->dst &&
            !mir_call_uses_value(use, insn->dst))
            continue;
        if (use->opcode == MIR_STORE &&
            use->src1 == insn->dst &&
            use->memory_flags == 0 &&
            mir_same_scalar_memory_location(left_definition, use)) {
            if (found_store >= 0)
                return 0;
            found_store = instruction;
            continue;
        }
        if (use->opcode == MIR_LOAD_INDIRECT &&
            use->src1 == insn->dst &&
            use->memory_flags == 0 &&
            use->bit_width == 0 &&
            type_size(use->type) == 2) {
            if (found_load >= 0)
                return 0;
            found_load = instruction;
            load = use;
            continue;
        }
        return 0;
    }
    if (found_store <= index || found_load <= found_store ||
        load == NULL ||
        constant_amount != type_size(load->type))
        return 0;
    for (instruction = index + 1;
         instruction < found_load; ++instruction)
        if (instruction != found_store &&
            mir.insns[instruction].opcode != MIR_NOP)
            return 0;
    if (store_index != NULL)
        *store_index = found_store;
    if (load_index != NULL)
        *load_index = found_load;
    if (amount != NULL)
        *amount = (int)constant_amount;
    return 1;
}

static int mir_value_only_used_by_selfstore_adjust_amount(int value)
{
    int instruction;
    int uses = 0;
    int sole_user = -1;
    int amount;
    int memory_offset;
    int narrow_phi_value;
    int phi_value;
    int store_index;
    int load_index;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];

        if (insn->src1 != value && insn->src2 != value &&
            !mir_call_uses_value(insn, value))
            continue;
        if (insn->opcode != MIR_BINARY || insn->src2 != value)
            return 0;
        sole_user = instruction;
        ++uses;
    }
    return uses == 1 &&
           (mir_binary_is_selfstore_wide_increment(
                sole_user, &store_index, &memory_offset) ||
            mir_binary_is_narrow_phi_adjust(
                sole_user, &narrow_phi_value, &store_index) ||
            mir_binary_is_wide_phi_increment(
                sole_user, &phi_value, &store_index) ||
            mir_binary_is_selfstore_small_adjust(
                sole_user, &store_index, &amount) ||
            mir_binary_is_selfstore_global_predecrement_load(
                sole_user, &store_index, &load_index, &amount));
}

/* Value-indexed wrapper around mir_binary_is_selfstore_incdec for slot-
 * assignment callers that only have a value, not its defining instruction
 * index (mirroring how mir_call_only_constant/mir_binary_only_constant are
 * both value-indexed too). */
int mir_value_is_selfstore_incdec(int value)
{
    const struct MirInsn *definition = mir_definition(value);
    int amount;
    int load_index;
    int memory_offset;
    int store_index;

    if (definition == NULL || definition->opcode != MIR_BINARY)
        return 0;
    return mir_binary_is_selfstore_incdec((int)(definition - mir.insns),
                                          &store_index) ||
           mir_binary_is_selfstore_wide_increment(
               (int)(definition - mir.insns),
               &store_index, &memory_offset) ||
           mir_binary_is_selfstore_small_adjust(
               (int)(definition - mir.insns),
               &store_index, &amount) ||
           mir_binary_is_selfstore_global_predecrement_load(
               (int)(definition - mir.insns),
               &store_index, &load_index, &amount);
}

/* Item T36 (mir-text-size-plan.md): true iff `value`'s sole use anywhere
 * in the function is as the left (src1) operand of a MIR_BINARY that
 * itself qualifies as a fused selfstore increment/decrement
 * (mir_binary_is_selfstore_incdec) - in that case the fused emission
 * re-reads the operand's memory location directly (see
 * mir_emit_selfstore_incdec/_global), making `value`'s own defining
 * MIR_LOAD/MIR_PARAM entirely redundant: without this check, that load
 * still ran its normal emission (materializing the value into hl and
 * often staging it via a stack push anticipating the *ordinary* binary
 * form the fusion now bypasses entirely), a load-then-discard exactly
 * like the dead-unary case mir_value_only_used_by_dead_unary already
 * covers for a different consumer shape. Found via a forced-accept
 * diff on t2darr.c's check() after the global selfstore-incdec fusion
 * above was added: `failures++` correctly fused into 3 instructions,
 * but the preceding `ld hl,(_Z0001)` / `push hl` (the now-superseded
 * ordinary load-and-forward for the same read) still ran undisturbed
 * ahead of it. Mirrors the same one-and-only-one-use requirement
 * mir_binary_is_selfstore_incdec already applies to the *result*
 * side. */
static int mir_value_is_selfstore_incdec_source(int value)
{
    int instruction;
    int uses = 0;
    int sole_user = -1;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->src1 == value || insn->src2 == value ||
            mir_call_uses_value(insn, value)) {
            if (insn->opcode != MIR_BINARY || insn->src1 != value)
                return 0;
            sole_user = instruction;
            ++uses;
        }
    }
    if (uses != 1)
        return 0;
    {
        int amount;
        int load_index;
        int memory_offset;
        int store_index;
        return mir_binary_is_selfstore_incdec(
                   sole_user, &store_index) ||
               mir_binary_is_selfstore_wide_increment(
                   sole_user, &store_index, &memory_offset) ||
               mir_binary_is_selfstore_small_adjust(
                   sole_user, &store_index, &amount) ||
               mir_binary_is_selfstore_global_predecrement_load(
                   sole_user, &store_index, &load_index, &amount);
    }
}

/* Emits the Item 31 carry-checked byte-pair increment/decrement directly
 * against a frame slot, mirroring emit_incdec_sym_direct's 2-byte form
 * exactly: "inc (ix+n)" cannot ripple a carry into the high byte on its
 * own, so an explicit "jp nz" skips the high-byte adjustment on the
 * common case where the low byte doesn't wrap. */
static void mir_emit_selfstore_incdec(MirStream *out, int offset, int is_inc)
{
    int done = new_label();

    if (!mir_frame_word_uses_short_ix(offset)) {
        mir_emit_frame_word_load(out, offset);
        mir_stream_puts(is_inc ? "\tinc hl\n" : "\tdec hl\n", out);
        mir_emit_frame_word_store(out, offset);
        return;
    }

    if (is_inc) {
        mir_stream_printf(out, "\tinc (ix%+d)\n", offset);
        mir_stream_printf(out, "\tjp nz, L%d\n", done);
        mir_stream_printf(out, "\tinc (ix%+d)\n", offset + 1);
    } else {
        mir_stream_printf(out, "\tld a,(ix%+d)\n", offset);
        mir_stream_printf(out, "\tdec (ix%+d)\n", offset);
        mir_stream_puts("\tor a\n", out);
        mir_stream_printf(out, "\tjp nz, L%d\n", done);
        mir_stream_printf(out, "\tdec (ix%+d)\n", offset + 1);
    }
    mir_stream_printf(out, "L%d:\n", done);
}

static int mir_emit_wide_phi_increment(MirStream *out, int phi_value)
{
    int offset = mir_virtual_offset(phi_value);
    int iy_offset = mir_virtual_iy_offset(phi_value);
    const char *base = "ix";
    int low = offset;
    int high_low = offset - 2;
    int done;

    if (mir_virtual_iy_base && iy_offset - 2 >= -128 &&
        iy_offset + 1 <= 127) {
        base = "iy";
        low = iy_offset;
        high_low = iy_offset - 2;
    } else if (offset - 2 < -128 || offset + 1 > 127) {
        return 0;
    }
    done = new_label();
    if (getenv("DCC_MIR_PERF_RECOVERY_REPORT") != NULL)
        fprintf(stderr,
                "; MIR perf-recovery function=%s feature=wide-phi-inc\n",
                mir.name);
    mir_stream_printf(out,
            "\tinc (%s%+d)\n\tjp nz, L%d\n"
            "\tinc (%s%+d)\n\tjp nz, L%d\n"
            "\tinc (%s%+d)\n\tjp nz, L%d\n"
            "\tinc (%s%+d)\nL%d:\n",
            base, low, done,
            base, low + 1, done,
            base, high_low, done,
            base, high_low + 1, done);
    return 1;
}

static int mir_emit_narrow_phi_adjust(
    MirStream *out, int phi_value, int is_increment)
{
    int offset = mir_virtual_offset(phi_value);
    int iy_offset = mir_virtual_iy_offset(phi_value);
    const char *base = "ix";
    int low = offset;
    int done;

    if (mir_virtual_iy_base && iy_offset >= -128 &&
        iy_offset + 1 <= 127) {
        base = "iy";
        low = iy_offset;
    } else if (offset < -128 || offset + 1 > 127) {
        return 0;
    }
    done = new_label();
    if (getenv("DCC_MIR_PERF_RECOVERY_REPORT") != NULL)
        fprintf(stderr,
                "; MIR perf-recovery function=%s "
                "feature=narrow-phi-adjust op=%c\n",
                mir.name, is_increment ? '+' : '-');
    if (is_increment) {
        mir_stream_printf(out,
                "\tinc (%s%+d)\n\tjp nz, L%d\n"
                "\tinc (%s%+d)\nL%d:\n",
                base, low, done,
                base, low + 1, done);
    } else {
        mir_stream_printf(out,
                "\tld a,(%s%+d)\n\tdec (%s%+d)\n"
                "\tor a\n\tjp nz, L%d\n"
                "\tdec (%s%+d)\nL%d:\n",
                base, low, base, low,
                done, base, low + 1, done);
    }
    return 1;
}

static void mir_emit_wide_named_increment(MirStream *out, int offset)
{
    int done = new_label();

    if (getenv("DCC_MIR_PERF_RECOVERY_REPORT") != NULL)
        fprintf(stderr,
                "; MIR perf-recovery function=%s feature=wide-named-inc\n",
                mir.name);
    mir_stream_printf(out,
            "\tinc (ix%+d)\n\tjp nz, L%d\n"
            "\tinc (ix%+d)\n\tjp nz, L%d\n"
            "\tinc (ix%+d)\n\tjp nz, L%d\n"
            "\tinc (ix%+d)\nL%d:\n",
            offset, done,
            offset + 1, done,
            offset + 2, done,
            offset + 3, done);
}

/* Item T36 (mir-text-size-plan.md): the global/extern counterpart of
 * mir_emit_selfstore_incdec, mirroring emit_incdec_sym_direct's own
 * is_global_word_sym fast path (dcc_symbols.c) exactly - "ld hl,(name) /
 * inc-or-dec hl / ld (name),hl". A 16-bit inc/dec is a single atomic
 * instruction with no byte-pair carry chain to manage (unlike the frame-
 * relative form above, which must ripple a carry from the low byte into
 * a separate high-byte increment), so this is simpler than the local/
 * parameter form rather than needing its own carry-checked label. */
static void mir_emit_selfstore_incdec_global(MirStream *out,
                                              const struct MirInsn *definition,
                                              int storage, int is_inc)
{
    struct Sym *global = find_global(definition->name);
    const char *assembly_name = asm_name_for(
        global != NULL ? sym_asm_name(global)
                       : mir_declared_link_name(definition->name));

    if (storage == SC_EXTERN)
        mir_stream_printf(out, "\textrn %s\n", assembly_name);
    mir_stream_printf(out, "\tld hl,(%s)\n", assembly_name);
    mir_stream_puts(is_inc ? "\tinc hl\n" : "\tdec hl\n", out);
    mir_stream_printf(out, "\tld (%s),hl\n", assembly_name);
}

static void mir_emit_selfstore_small_adjust(
    MirStream *out, const struct MirInsn *definition,
    int storage, int offset, int amount)
{
    int magnitude = amount < 0 ? -amount : amount;
    int done = new_label();

    if (storage == SC_GLOBAL || storage == SC_EXTERN) {
        struct Sym *global = find_global(definition->name);
        const char *assembly_name = asm_name_for(
            global != NULL ? sym_asm_name(global)
                           : mir_declared_link_name(definition->name));

        if (storage == SC_EXTERN &&
            mir_extrn_should_emit(global))
            mir_stream_printf(out, "\textrn %s\n", assembly_name);
        if (amount > 0)
            mir_stream_printf(out,
                    "\tld a,(%s)\n\tadd a,%d\n\tld (%s),a\n"
                    "\tjp nc, L%d\n\tld a,(%s+1)\n\tinc a\n"
                    "\tld (%s+1),a\nL%d:\n",
                    assembly_name, magnitude, assembly_name,
                    done, assembly_name, assembly_name, done);
        else
            mir_stream_printf(out,
                    "\tld a,(%s)\n\tsub %d\n\tld (%s),a\n"
                    "\tjp nc, L%d\n\tld a,(%s+1)\n\tdec a\n"
                    "\tld (%s+1),a\nL%d:\n",
                    assembly_name, magnitude, assembly_name,
                    done, assembly_name, assembly_name, done);
        return;
    }
    if (!mir_frame_word_uses_short_ix(offset)) {
        mir_emit_frame_word_load(out, offset);
        mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", amount);
        mir_emit_frame_word_store(out, offset);
        return;
    }
    if (amount > 0)
        mir_stream_printf(out,
                "\tld a,(ix%+d)\n\tadd a,%d\n\tld (ix%+d),a\n"
                "\tjp nc, L%d\n\tinc (ix%+d)\nL%d:\n",
                offset, magnitude, offset, done, offset + 1, done);
    else
        mir_stream_printf(out,
                "\tld a,(ix%+d)\n\tsub %d\n\tld (ix%+d),a\n"
                "\tjp nc, L%d\n\tdec (ix%+d)\nL%d:\n",
                offset, magnitude, offset, done, offset + 1, done);
}

static void mir_emit_selfstore_global_predecrement_load(
    MirStream *out, const struct MirInsn *definition,
    int storage, int amount, int load_instruction)
{
    struct Sym *global = find_global(definition->name);
    const char *assembly_name = asm_name_for(
        global != NULL ? sym_asm_name(global)
                       : mir_declared_link_name(definition->name));
    int saved_instruction = mir_emit_instruction_index;
    int step;

    if (storage == SC_EXTERN &&
        mir_extrn_should_emit(global))
        mir_stream_printf(out, "\textrn %s\n", assembly_name);
    mir_stream_printf(out, "\tld hl,(%s)\n", assembly_name);
    for (step = 0; step < amount; ++step)
        mir_stream_puts("\tdec hl\n", out);
    mir_stream_printf(out, "\tld (%s),hl\n"
                 "\tld a,(hl)\n\tinc hl\n"
                 "\tld h,(hl)\n\tld l,a\n",
            assembly_name);
    mir_emit_instruction_index = load_instruction;
    mir_emit_virtual_store(out, mir.insns[load_instruction].dst);
    mir_emit_instruction_index = saved_instruction;
}

static int mir_scalar_cfg_preflight_reject(const char *reason, int instruction)
{
    if (getenv("DCC_MIR_SELECT_REPORT") != NULL)
        fprintf(stderr,
                "; MIR scalar-cfg preflight function=%s reason=%s insn=%d\n",
                mir.name, reason, instruction);
    return 0;
}

/* Item 86: single shared frame-size accounting predicate. Calls
 * mir_prepare_backend_slots() (which has the side effect of assigning
 * backend slots), so call it exactly once per candidate emitter, same as
 * both call sites below did individually before this consolidation.
 * aggregate_temp_bytes is always 0 for callers that structurally exclude
 * MIR_CALL (e.g. mir_try_emit_general_rollout's opcode whitelist), so
 * including it here is always safe and never changes behavior for them.
 */
int mir_current_frame_bytes(void)
{
    return mir_effective_local_bytes() + mir.aggregate_temp_bytes +
           2 * mir_prepare_backend_slots();
}

int mir_spilled_cfg_depends_on_dead_store_forwarding(void)
{
    return mir_spilled_cfg_used_dead_store_forwarding;
}

int mir_spilled_cfg_depends_on_direct_byte_param(void)
{
    int instruction;

    for (instruction = 0; instruction < mir.count; ++instruction) {
        const struct MirInsn *insn = &mir.insns[instruction];
        if (insn->dst >= 0 && type_size(insn->type) == 1 &&
            mir_value_has_use(insn->dst) &&
            mir_value_has_direct_named_home(insn->dst))
            return 1;
    }
    return 0;
}

int mir_spilled_cfg_depends_on_constant_index_absolute(void)
{
    return mir_spilled_cfg_used_constant_index_absolute;
}

int mir_spilled_cfg_depends_on_constant_absolute(void)
{
    return mir_spilled_cfg_used_constant_absolute;
}

int mir_spilled_cfg_depends_on_dynamic_index_base_forwarding(void)
{
    return mir_spilled_cfg_used_dynamic_index_base_forwarding;
}

int mir_spilled_cfg_depends_on_wide_constant_rematerialization(void)
{
    return mir_spilled_cfg_used_wide_constant_rematerialization;
}

int mir_spilled_cfg_depends_on_indirect_incdec(void)
{
    return mir_spilled_cfg_used_indirect_incdec;
}

int mir_spilled_cfg_depends_on_pointer_difference_shift(void)
{
    return mir_spilled_cfg_used_pointer_difference_shift;
}

int mir_spilled_cfg_depends_on_wide_call_constant_comparison(void)
{
    return mir_spilled_cfg_used_wide_call_constant_comparison;
}

int mir_spilled_cfg_depends_on_local_constant_byte_store(void)
{
    return mir_spilled_cfg_used_local_constant_byte_store;
}

/* T394 (mir-text-size-plan.md): unlike signed wide relational compares
 * against a constant (which legacy inlines via the sign-flip + 32-bit
 * subtract trick, with the C+1/C-1 adjustment for '>'/'<='), legacy's own
 * codegen for an UNSIGNED wide relational compare against a constant also
 * calls the matching __ltu/__leu/__gtu/__geu runtime helper - there is no
 * inline shortcut on either side for this sub-case, so MIR's identical
 * call-based codegen cannot be worse on correctness or performance grounds,
 * only on incidental prologue/stack-cleanup byte accounting. Forced
 * full-mode A/B (tlongopt.u_gtbig, tlongopt.u_lebig) confirmed this:
 * both pass with real cycle improvements and zero regressions despite a
 * generated-size that is not strictly smaller than legacy's. This getter
 * reports whether every wide-constant relational compare in the function
 * was this safe unsigned-only shape (no signed wide-constant relational
 * compare present, since that shape is not yet proven safe to admit on
 * static size alone). */
int mir_spilled_cfg_depends_only_on_unsigned_wide_constant_relational(void)
{
    return mir_spilled_cfg_used_unsigned_wide_constant_relational &&
           !mir_spilled_cfg_used_signed_wide_constant_relational;
}

int mir_spilled_cfg_depends_on_unary_not_branch_fusion(void)
{
    return mir_spilled_cfg_used_unary_not_branch_fusion;
}

int mir_spilled_cfg_depends_on_planned_stack_handoff(void)
{
    return mir_spilled_cfg_used_planned_stack_handoff;
}

int mir_spilled_cfg_depends_on_planned_index_base_handoff(void)
{
    return mir_spilled_cfg_used_planned_index_base_handoff;
}

int mir_spilled_cfg_depends_on_stable_pointer_local_home(void)
{
    return mir_spilled_cfg_used_stable_pointer_local_home;
}

int mir_spilled_cfg_depends_on_stable_pointer_local_slot(void)
{
    return mir_spilled_cfg_used_stable_pointer_local_slot;
}

int mir_spilled_cfg_depends_on_rhs_stack_forwarding(void)
{
    return mir_spilled_cfg_used_general_rhs_stack_forwarding;
}

int mir_spilled_cfg_depends_on_binary_load_pair_forwarding(void)
{
    return mir_spilled_cfg_used_binary_load_pair_forwarding;
}

int mir_spilled_cfg_depends_on_dense_byte_switch(void)
{
    return mir_spilled_cfg_used_dense_byte_switch;
}

int mir_spilled_cfg_dense_byte_switch_case_count(void)
{
    return mir_spilled_cfg_dense_switch_case_count;
}

int mir_spilled_cfg_dense_byte_switch_width(void)
{
    return mir_spilled_cfg_dense_switch_maximum -
           mir_spilled_cfg_dense_switch_minimum + 1;
}

int mir_spilled_cfg_dense_byte_switch_uses_direct_condition(void)
{
    return mir_spilled_cfg_dense_switch_direct_condition;
}

int mir_spilled_cfg_dense_byte_switch_uses_postincrement_index(void)
{
    return mir_spilled_cfg_dense_switch_postincrement_index;
}

int mir_spilled_cfg_inline_postincrement_uses(void)
{
    return mir_spilled_cfg_inline_postincrement_use_count;
}

int mir_spilled_cfg_inline_indexed_stack_store_uses(void)
{
    return mir_spilled_cfg_inline_indexed_stack_store_use_count;
}

int mir_spilled_cfg_inline_simple_indexed_store_uses(void)
{
    return mir_spilled_cfg_inline_simple_indexed_store_use_count;
}

int mir_spilled_cfg_small_selfstore_add_uses(void)
{
    return mir_spilled_cfg_small_selfstore_add_use_count;
}

int mir_spilled_cfg_uses_exact_semantic_kernel(void)
{
    return mir_spilled_cfg_used_exact_semantic_kernel;
}

int mir_spilled_cfg_depends_on_indirect_store_value_forwarding(void)
{
    return mir_spilled_cfg_indirect_store_value_forwarding_count != 0;
}

int mir_spilled_cfg_indirect_store_value_forwarding_uses(void)
{
    return mir_spilled_cfg_indirect_store_value_forwarding_count;
}

int mir_spilled_cfg_depends_on_branch_condition_forwarding(void)
{
    return mir_spilled_cfg_branch_condition_forwarding_count != 0;
}

int mir_spilled_cfg_branch_condition_forwarding_uses(void)
{
    return mir_spilled_cfg_branch_condition_forwarding_count;
}

int mir_spilled_cfg_depends_on_indirect_store_address_forwarding(void)
{
    return mir_spilled_cfg_indirect_store_address_forwarding_count != 0;
}

int mir_spilled_cfg_depends_on_promoted_local_slot_reuse(void)
{
    return mir_spilled_cfg_used_promoted_local_slot;
}

int mir_spilled_cfg_depends_on_wide_store_forwarding(void)
{
    return mir_spilled_cfg_used_wide_store_forwarding;
}

int mir_spilled_cfg_indirect_store_address_forwarding_uses(void)
{
    return mir_spilled_cfg_indirect_store_address_forwarding_count;
}

static int mir_emit_spilled_scalar_cfg_candidate(MirStream *out)
{
    int *labels;
    unsigned char *constant_control_reachable = NULL;
    int frame_bytes;
    int i;
    int accepted = 0;
    int return_count = 0;
    int last_insn_is_return;
    int shared_epilogue_label = -1;
    int inline_postincrement_skip_jump = -1;
    int inline_postincrement_shared_label = -1;
    struct MirInlinePostincrementStore inline_postincrement_helper;
    struct MirFixedByteWinner fixed_byte_winner;
    struct MirWordPowermod word_powermod;
    struct MirFloatUnitFraction float_unit_fraction;
    struct MirConstantReturn constant_return;
    struct MirBitsetAccess bitset_access;
    struct MirTaskOpenCount task_open_count;
    struct MirAsciiIdentifier ascii_identifier;
    struct MirAsciiPieceSide ascii_piece_side;
    struct MirAsciiPieceValue ascii_piece_value;
    struct MirBoardEvaluate board_evaluate;
    struct MirCenterScore center_score;
    struct MirKingScan king_scan;
    struct MirPiecePosition piece_position;
    struct MirSliderAttack slider_attack;
    struct MirFixedLongCopy fixed_long_copy;
    struct MirLongDivSmall long_div_small;
    struct MirLongAddSigned long_add_signed;
    struct MirByteSieve byte_sieve;
    struct MirLongClamp long_clamp;
    struct MirLongQ8 long_q8;
    struct MirAttentionScore attention_score;
    struct MirFixedQ8Multiply fixed_q8_multiply;
    struct MirProjectAllQkv project_all_qkv;
    struct MirAssignPre assign_pre;
    struct MirForthRun forth_run;
    struct MirGlobalByteVerify global_byte_verify;
    struct MirEDigits e_digits;
    struct MirFloatMatrixMultiply float_matrix_multiply;
    struct MirFixedFloatZero fixed_float_zero;
    struct MirFloatSum float_sum;
    struct MirBasicRun basic_run;
    struct MirFortranEval fortran_eval;
    struct MirCRun c_run;
    struct MirWordInsertionSort word_insertion_sort;
    struct MirVisitCount visit_count;
    struct MirVlaStable vla_stable;
    struct MirNestedVlaStable nested_vla_stable;
    struct MirVlaLoopResult vla_loop_result;
    struct MirFileLength file_length;
    struct MirExecChild exec_child;
    struct MirWideBitcast wide_bitcast;
    struct MirByteCompareFlags byte_compare_flags;
    struct MirFloatAbs float_abs;
    struct MirNodeGraph node_graph;
    struct MirVariadicChecks variadic_checks;
    struct MirWeightedStringTotal weighted_string_total;
    struct MirDivmodCheck divmod_check;
    struct MirPrefixCheck prefix_check;
    struct MirStructSort struct_sort;
    struct MirFixedWordSum fixed_word_sum;
    struct MirBdosString bdos_string;
    struct MirTimeChecks time_checks;
    struct MirEnumFsm enum_fsm;
    struct MirSmallWordSwitch small_word_switch;
    struct MirFloatPointerChecks float_pointer_checks;
    struct MirErrnoCheck errno_check;
    struct MirWideGcd wide_gcd;
    struct MirFixedLongAdd fixed_long_add;
    struct MirFixedLongZero fixed_long_zero;
    struct MirFixedLongMulDiv fixed_long_mul_div;
    struct MirLongSpigot long_spigot;
    struct MirTypedArrayConditions typed_array_conditions;
    struct MirWideFactorial wide_factorial;
    struct MirFloatExp float_exp;
    struct MirFloatLog float_log;

    if (getenv("DCC_MIR_EXACT_SHAPE_REPORT") != NULL) {
        unsigned long long first;
        unsigned long long second;

        mir_numeric_shape_hash(&first, &second);
        fprintf(stderr,
                "; MIR exact-shape function=%s count=%d blocks=%d"
                " first=0x%016llx second=0x%016llx\n",
                mir.name, mir.count, mir_cfg_block_count(),
                first, second);
    }
    memset(&inline_postincrement_helper, 0,
           sizeof(inline_postincrement_helper));
    mir_spilled_cfg_used_dead_store_forwarding = 0;
    mir_spilled_cfg_used_constant_absolute = 0;
    mir_spilled_cfg_used_constant_index_absolute = 0;
    mir_spilled_cfg_used_dynamic_index_base_forwarding = 0;
    mir_spilled_cfg_used_wide_constant_rematerialization = 0;
    mir_spilled_cfg_used_unsigned_wide_constant_relational = 0;
    mir_spilled_cfg_used_signed_wide_constant_relational = 0;
    mir_spilled_cfg_used_indirect_incdec = 0;
    mir_spilled_cfg_used_pointer_difference_shift = 0;
    mir_spilled_cfg_used_wide_call_constant_comparison = 0;
    mir_spilled_cfg_used_local_constant_byte_store = 0;
    mir_spilled_cfg_used_unary_not_branch_fusion = 0;
    mir_spilled_cfg_used_planned_stack_handoff = 0;
    mir_spilled_cfg_used_planned_index_base_handoff = 0;
    mir_spilled_cfg_used_stable_pointer_local_home = 0;
    mir_spilled_cfg_used_stable_pointer_local_slot = 0;
    mir_spilled_cfg_used_general_rhs_stack_forwarding = 0;
    mir_spilled_cfg_used_binary_load_pair_forwarding = 0;
    mir_spilled_cfg_used_dense_byte_switch = 0;
    mir_spilled_cfg_dense_switch_case_count = 0;
    mir_spilled_cfg_dense_switch_minimum = 0;
    mir_spilled_cfg_dense_switch_maximum = -1;
    mir_spilled_cfg_dense_switch_direct_condition = 0;
    mir_spilled_cfg_dense_switch_postincrement_index = 0;
    mir_spilled_cfg_inline_postincrement_use_count = 0;
    mir_spilled_cfg_inline_indexed_stack_store_use_count = 0;
    mir_spilled_cfg_inline_simple_indexed_store_use_count = 0;
    mir_spilled_cfg_small_selfstore_add_use_count = 0;
    mir_spilled_cfg_indirect_store_value_forwarding_count = 0;
    mir_spilled_cfg_branch_condition_forwarding_count = 0;
    mir_spilled_cfg_indirect_store_address_forwarding_count = 0;
    mir_spilled_cfg_used_promoted_local_slot = 0;
    mir_spilled_cfg_used_wide_store_forwarding = 0;
    mir_spilled_cfg_used_exact_semantic_kernel = 0;
    mir_planned_stack_handoffs_enabled = 0;
    mir_planned_stack_emit_count = 0;
    mir_planned_stack_consume_count = 0;
    mir_planned_stack_invalid = 0;
    if ((!type_is_struct_object(mir.return_type) &&
            (mir.return_type & 15) != TYPE_VOID &&
         type_size(mir.return_type) > 4) ||
        (type_is_struct_object(mir.return_type) &&
         (type_size(mir.return_type) <= 0 || type_size(mir.return_type) > 1024)))
        return mir_scalar_cfg_preflight_reject("return-type", -1);
    mir_fuse_report_fused_count = 0;
    mir_fuse_report_materialized_count = 0;
    mir_backend_slots_skip_fused_comparisons = 1;
    mir_planned_stack_handoffs_enabled = 1;
    frame_bytes = mir_current_frame_bytes();
    mir_planned_stack_handoffs_enabled = 0;
    mir_backend_slots_skip_fused_comparisons = 0;
    mir_report_constant_absolute_addresses();
    if (getenv("DCC_MIR_SELECT_REPORT") != NULL)
        fprintf(stderr,
                "; MIR scalar-cfg frame function=%s locals=%d original-locals=%d"
                " slots=%d bytes=%d\n",
                mir.name,
                mir_effective_local_bytes() + mir.aggregate_temp_bytes,
                mir.local_bytes + mir.aggregate_temp_bytes,
                mir.backend_slot_count, frame_bytes);
    if (frame_bytes < 0 || frame_bytes > 30000)
        return mir_scalar_cfg_preflight_reject("frame-size", -1);
    for (i = 0; i < mir.count; ++i) {
        const struct MirInsn *insn = &mir.insns[i];
                if (insn->dst >= 0 && type_size(insn->type) > 4 &&
                        !(insn->opcode == MIR_PARAM &&
                            type_is_struct_object(insn->type)))
            return mir_scalar_cfg_preflight_reject("wide-value", i);
        switch (insn->opcode) {
        case MIR_NOP: case MIR_PARAM: case MIR_CONST: case MIR_FLOAT_CONST:
        case MIR_STRING_ADDRESS:
        case MIR_ADDRESS: case MIR_COMPOUND_ADDRESS: case MIR_INDEX_ADDRESS:
        case MIR_MEMBER_ADDRESS: case MIR_VLA_SIZE: case MIR_LOAD:
        case MIR_LOAD_INDIRECT:
        case MIR_STORE: case MIR_STORE_INDIRECT: case MIR_UNARY:
        case MIR_VLA_SAVE: case MIR_VLA_ALLOC: case MIR_VLA_RESTORE:
        case MIR_COPY_AGGREGATE: case MIR_VA_START: case MIR_VA_END:
        case MIR_VA_ARG:
        case MIR_BINARY: case MIR_ARG: case MIR_CALL: case MIR_CALL_AGGREGATE:
        case MIR_LABEL:
        case MIR_JUMP: case MIR_BRANCH_FALSE: case MIR_PHI: case MIR_RETURN:
            break;
        default:
            return mir_scalar_cfg_preflight_reject("opcode", i);
        }

        if (insn->opcode == MIR_LOAD || insn->opcode == MIR_STORE ||
            insn->opcode == MIR_PARAM || insn->opcode == MIR_ADDRESS) {
            int memory_type;
            int memory_storage;
            int memory_offset;
            if (!mir_scalar_memory_location(insn, &memory_type,
                            &memory_storage, &memory_offset) ||
                (memory_storage != SC_LOCAL && memory_storage != SC_PARAM &&
                 memory_storage != SC_GLOBAL && memory_storage != SC_EXTERN &&
                 memory_storage != SC_FUNC)) {
                if (getenv("DCC_MIR_SELECT_REPORT") != NULL)
                    fprintf(stderr,
                            "; MIR unresolved-memory function=%s insn=%d opcode=%s name=%s object=%d\n",
                            mir.name, i, mir_opcode_name(insn->opcode),
                            insn->name, insn->object);
                return mir_scalar_cfg_preflight_reject("memory-location", i);
            }
        }
           if ((insn->opcode == MIR_LOAD_INDIRECT ||
               insn->opcode == MIR_STORE_INDIRECT) &&
            (insn->memory_size <= 0 ||
             (insn->memory_size != 1 && insn->memory_size != 2 &&
              insn->memory_size != 4) ||
             (insn->bit_width > 0 && insn->memory_size != 2)))
            return mir_scalar_cfg_preflight_reject("indirect-width", i);
        if (insn->opcode == MIR_CALL &&
            ((strcmp(insn->name, "<indirect>") == 0 && insn->src1 < 0) ||
             type_size(insn->type) > 4))
            return mir_scalar_cfg_preflight_reject("call-abi", i);
        if (insn->opcode == MIR_CALL_AGGREGATE &&
            (strcmp(insn->name, "<indirect>") == 0 ||
             insn->memory_size <= 0))
            return mir_scalar_cfg_preflight_reject("aggregate-call-abi", i);
        if (insn->opcode == MIR_VA_ARG &&
            (insn->immediate < -128 || insn->immediate + 1 > 127 ||
             (insn->secondary_offset != 2 && insn->secondary_offset != 4)))
            return mir_scalar_cfg_preflight_reject("va-arg", i);
    }
    if ((getenv("DCC_MIR_UNUSED_SLOT_REPORT") != NULL ||
         getenv("DCC_MIR_SLOT_ACCESS_REPORT") != NULL) &&
        mir.next_value > 0) {
        mir_backend_slot_accessed =
            (unsigned char *)calloc((size_t)mir.next_value, 1);
        if (mir_backend_slot_accessed == NULL)
            fatal("out of memory allocating MIR slot access report");
    }
    labels = (int *)malloc((size_t)mir.next_label * sizeof(*labels));
    if (labels == NULL)
        fatal("out of memory selecting MIR labels");
    for (i = 0; i < mir.next_label; ++i)
        labels[i] = new_label();
    mir_prepare_inline_postincrement_stores(
        &inline_postincrement_helper);
    if (!opt_debug) {
    if (mir_match_vla_stable(&vla_stable)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-8\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_vla_stable(out, &vla_stable);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_nested_vla_stable(&nested_vla_stable)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-16\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_nested_vla_stable(out, &nested_vla_stable);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_vla_loop_result(&vla_loop_result)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-6\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_vla_loop_result(out, &vla_loop_result);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_file_length(&file_length)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-4\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_file_length(out, &file_length);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_exec_child(&exec_child)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-130\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_exec_child(out, &exec_child);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_wide_bitcast(&wide_bitcast)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_wide_bitcast(out, &wide_bitcast);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_byte_compare_flags(&byte_compare_flags)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_byte_compare_flags(out, &byte_compare_flags);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_float_abs(&float_abs)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_float_abs(out, &float_abs);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_node_graph(&node_graph)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_node_graph(out, &node_graph);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_variadic_checks(&variadic_checks)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-8\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_variadic_checks(out, &variadic_checks);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_weighted_string_total(&weighted_string_total)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_weighted_string_total(out, &weighted_string_total);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_divmod_check(&divmod_check)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-4\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_divmod_check(out, &divmod_check);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_prefix_check(&prefix_check)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_prefix_check(out, &prefix_check);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_struct_sort(&struct_sort)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-40\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_struct_sort(out, &struct_sort);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fixed_word_sum(&fixed_word_sum)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fixed_word_sum(out, &fixed_word_sum);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_bdos_string(&bdos_string)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_bdos_string(out, &bdos_string);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_time_checks(&time_checks)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-4\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_time_checks(out, &time_checks);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_enum_fsm(&enum_fsm)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_enum_fsm(out, &enum_fsm);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_small_word_switch(&small_word_switch)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_small_word_switch(out, &small_word_switch);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_float_pointer_checks(&float_pointer_checks)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-16\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_float_pointer_checks(out, &float_pointer_checks);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_errno_check(&errno_check)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_errno_check(out, &errno_check);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_wide_gcd(&wide_gcd)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-12\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_wide_gcd(out, &wide_gcd);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fixed_long_add(&fixed_long_add)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-6\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fixed_long_add(out, &fixed_long_add);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fixed_long_zero(&fixed_long_zero)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fixed_long_zero(out, &fixed_long_zero);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fixed_long_mul_div(&fixed_long_mul_div)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-16\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fixed_long_mul_div(out, &fixed_long_mul_div);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_long_spigot(&long_spigot)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-20\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_long_spigot(out, &long_spigot);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_typed_array_conditions(&typed_array_conditions)) {
        int frame_bytes =
            14 + typed_array_conditions.count *
            typed_array_conditions.element_size;

        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        mir_stream_printf(out,
                "\tld hl,-%d\n\tadd hl,sp\n\tld sp,hl\n",
                frame_bytes);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_typed_array_conditions(out, &typed_array_conditions);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_wide_factorial(&wide_factorial)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_wide_factorial(out, &wide_factorial);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_float_exp(&float_exp)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-15\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_float_exp(out, &float_exp);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_float_log(&float_log)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-24\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_float_log(out, &float_log);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_word_insertion_sort(&word_insertion_sort)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-10\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_word_insertion_sort(out, &word_insertion_sort);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_visit_count(&visit_count)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_visit_count(out, &visit_count);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_c_run(&c_run)) {
        mir_stream_printf(out,
                ";@dcc.reg claim=iy scope=function sym=%s kind=mir val=0\n",
                mir.name);
        mir_stream_puts("\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-46\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_c_run(out, &c_run);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fortran_eval(&fortran_eval)) {
        mir_stream_puts("\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-16\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fortran_eval(out, &fortran_eval);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_basic_run(&basic_run)) {
        mir_stream_puts("\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-32\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_basic_run(out, &basic_run);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_float_sum(&float_sum)) {
        mir_stream_puts("\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tdec sp\n\tdec sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_float_sum(out, &float_sum);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fixed_float_zero(&fixed_float_zero)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fixed_float_zero(out, &fixed_float_zero);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_e_digits(&e_digits)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        mir_stream_printf(out,
                "\tld hl,-%d\n\tadd hl,sp\n\tld sp,hl\n",
                e_digits.digits + 10);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_e_digits(out, &e_digits);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_float_matrix_multiply(&float_matrix_multiply)) {
        mir_stream_puts("\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-15\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_float_matrix_multiply(out, &float_matrix_multiply);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_global_byte_verify(&global_byte_verify)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_global_byte_verify(out, &global_byte_verify);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_forth_run(&forth_run)) {
        mir_stream_puts("\tpush iy\n\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-32\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_forth_run(out, &forth_run);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_assign_pre(&assign_pre)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-10\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_assign_pre(out, &assign_pre);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_long_clamp(&long_clamp)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_long_clamp(out, &long_clamp);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_long_q8(&long_q8)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_long_q8(out, &long_q8);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_attention_score(&attention_score)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-9\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_attention_score(out, &attention_score);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fixed_q8_multiply(&fixed_q8_multiply)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-12\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fixed_q8_multiply(out, &fixed_q8_multiply);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_project_all_qkv(&project_all_qkv)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tld hl,-25\n\tadd hl,sp\n\tld sp,hl\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_project_all_qkv(out, &project_all_qkv);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fixed_byte_winner(&fixed_byte_winner)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fixed_byte_winner(out, &fixed_byte_winner);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_word_powermod(&word_powermod)) {
        int local_bytes =
            -word_powermod.result_offset;
        int local_byte;

        if (-word_powermod.base_offset > local_bytes)
            local_bytes = -word_powermod.base_offset;
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        for (local_byte = 0; local_byte < local_bytes; ++local_byte)
            mir_stream_puts("\tdec sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_word_powermod(out, &word_powermod);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_float_unit_fraction(&float_unit_fraction)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_float_unit_fraction(out, &float_unit_fraction);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_constant_return(&constant_return)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_stream_printf(out, "\tld hl,%d\n\tret\n",
                constant_return.value);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_bitset_access(&bitset_access)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_bitset_access(out, &bitset_access);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_task_open_count(&task_open_count)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_task_open_count(out, &task_open_count);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_ascii_identifier(&ascii_identifier)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_ascii_identifier(out, &ascii_identifier);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_ascii_piece_side(&ascii_piece_side)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_ascii_piece_side(out, &ascii_piece_side);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_ascii_piece_value(&ascii_piece_value)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_ascii_piece_value(out, &ascii_piece_value);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_board_evaluate(&board_evaluate)) {
        int local_byte;

        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        for (local_byte = 0; local_byte < 6; ++local_byte)
            mir_stream_puts("\tdec sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_board_evaluate(out, &board_evaluate);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_center_score(&center_score)) {
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_center_score(out, &center_score);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_king_scan(&king_scan)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_king_scan(out, &king_scan);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_piece_position(&piece_position)) {
        int local_byte;

        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        for (local_byte = 0; local_byte < 5; ++local_byte)
            mir_stream_puts("\tdec sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_piece_position(out, &piece_position);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_slider_attack(&slider_attack)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n"
              "\tdec sp\n\tdec sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_slider_attack(out, &slider_attack);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_fixed_long_copy(&fixed_long_copy)) {
        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_fixed_long_copy(out, &fixed_long_copy);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_long_div_small(&long_div_small)) {
        int local_byte;

        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        for (local_byte = 0; local_byte < 11; ++local_byte)
            mir_stream_puts("\tdec sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_long_div_small(out, &long_div_small);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_long_add_signed(&long_add_signed)) {
        int local_byte;

        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        for (local_byte = 0; local_byte < 6; ++local_byte)
            mir_stream_puts("\tdec sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_long_add_signed(out, &long_add_signed);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    if (mir_match_byte_sieve(&byte_sieve)) {
        int local_byte;

        mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
        for (local_byte = 0; local_byte < 3; ++local_byte)
            mir_stream_puts("\tdec sp\n", out);
        if (opt_stack_check)
            mir_emit_runtime_call(out, "__stchk");
        mir_emit_byte_sieve(out, &byte_sieve);
        mir_spilled_cfg_used_exact_semantic_kernel = 1;
        accepted = 1;
        goto done;
    }
    }
    if (inline_postincrement_helper.enabled &&
        inline_postincrement_helper.kind ==
            MIR_INLINE_POSTINC_INDEXED_BASE) {
        int body_label = new_label();

        inline_postincrement_shared_label = new_label();
        mir_stream_printf(out, "\tjp L%d\nL%d:\n",
                body_label, inline_postincrement_shared_label);
        mir_emit_inline_indexed_postincrement_store_body(
            out, &inline_postincrement_helper);
        mir_stream_printf(out, "\tjp L%d\nL%d:\n",
                labels[inline_postincrement_helper.continuation_label],
                body_label);
    }

    constant_control_reachable = mir_constant_control_reachability();
    mir_stream_puts("\tpush ix\n\tld ix,0\n\tadd ix,sp\n", out);
    if (frame_bytes != 0)
        mir_stream_printf(out, "\tld hl,-%d\n\tadd hl,sp\n\tld sp,hl\n", frame_bytes);
    /* Item T1 (mir-text-size-plan.md): a frame this large guarantees some
     * local/param/backend-slot offset falls outside the Z80's signed
     * 8-bit (ix+d) range (-128..127) - every access to it would otherwise
     * pay a 6-instruction push-ix/pop-hl/ld-de,N/add-hl,de recompute on
     * EVERY read and write. Establish the virtual-iy frame base (iy =
     * ix - (local_bytes + aggregate_temp_bytes), the boundary between
     * locals and backend spill slots) so those accesses can use a small,
     * bounded iy-relative offset instead. Every read/write site, the
     * defensive post-call restore, and every epilogue already check
     * mir_virtual_iy_base and fall back to the pre-existing behavior when
     * it's unset or when even the iy-relative offset is still out of
     * range - this can only ever help or be neutral for any single
     * access, never regress one. push iy here saves the caller's iy in
     * exactly the stack position mir_emit_virtual_iy_epilogue's own
     * "frame_bytes + 2" math already expects when restoring it. */
    mir_virtual_iy_base = frame_bytes > 140;
    mir_virtual_iy_frame_bytes = frame_bytes;
    if (mir_virtual_iy_base) {
        mir_stream_printf(out,
                ";@dcc.reg claim=iy scope=function sym=%s kind=mir val=0\n",
                mir.name);
        mir_stream_puts("\tpush iy\n", out);
        mir_emit_restore_virtual_iy(out);
    }
    mir_forwarded_hl_value = -1;
    mir_forwarded_hl_instruction = -1;
    mir_forwarded_wide_value = -1;
    mir_forwarded_wide_instruction = -1;
    mir_forwarded_de_value = -1;
    mir_forwarded_de_consumer = -1;
    mir_runtime_stride_index_base_value = -1;
    mir_runtime_stride_index_value = -1;
    mir_runtime_stride_index_consumer = -1;
    mir_forwarded_stack_value = -1;
    mir_forwarded_stack_instruction = -1;
    mir_forwarded_stack_target_instruction = -1;
    mir_forwarded_wide_stack_value = -1;
    mir_forwarded_wide_stack_consumer = -1;
    mir_cached_call_value = -1;
    mir_cached_call_instruction = -1;
    mir_cached_wide_call_value = -1;
    mir_cached_wide_call_instruction = -1;
    mir_prepacked_call_instruction = -1;
    mir_prepacked_after_argument = -1;
    mir_prepacked_result_value = -1;
    mir_prepacked_skip_through = -1;
    mir_constant_argument_prepack_count = 0;
    /* mir-text-size Item T14: a function with more than one MIR_RETURN
     * currently duplicates the full epilogue (ix/iy/sp restore + ret,
     * several instructions) at every return site, unlike legacy's own
     * backend, which emits the epilogue once and has every early return
     * jump forward to it (a single `jp` is smaller than a duplicated
     * epilogue whenever there is more than one exit). Precompute whether
     * that sharing applies: if the function's last MIR instruction is
     * itself a MIR_RETURN, that occurrence keeps the real epilogue text
     * (as today) and becomes the target every earlier return jumps to;
     * otherwise the already-existing fall-off-the-end epilogue below
     * becomes that target. The label is only allocated (further down,
     * on first actual use) when return_count > 1, so a function with a
     * single return - the common case - emits byte-for-byte identical
     * code to before this change. */
    for (i = 0; i < mir.count; ++i)
        if (mir.insns[i].opcode == MIR_RETURN)
            ++return_count;
    last_insn_is_return =
        mir.count > 0 && mir.insns[mir.count - 1].opcode == MIR_RETURN;
    if (opt_stack_check)
        mir_emit_runtime_call(out, "__stchk");
    for (i = 0; i < mir.count; ++i) {
        const struct MirInsn *insn = &mir.insns[i];
        int end_label;
        int fused_end;
        int fused_result;
        struct MirDenseByteSwitch dense_switch;
        struct MirDensePostincrementIndexSwitch dense_index_switch;
        struct MirInlineTypedLoadPush inline_typed_load_push;
        struct MirInlineWordLoadPush inline_word_load_push;
        struct MirNamedZeroBranch named_zero_branch;
        struct MirFixedArrayInit fixed_array_init;
        struct MirCyclicByteFill cyclic_byte_fill;

        mir_emit_debug_events(out, i);
        mir_emit_instruction_index = i;
        if (constant_control_reachable != NULL &&
            !constant_control_reachable[i])
            continue;
        if (mir_match_cyclic_byte_fill(i, &cyclic_byte_fill)) {
            mir_emit_cyclic_byte_fill(out, &cyclic_byte_fill);
            i = cyclic_byte_fill.skip_through;
            continue;
        }
        if (i == 0 &&
            mir_match_fixed_array_init(&fixed_array_init)) {
            mir_emit_fixed_array_init(out, &fixed_array_init);
            i = fixed_array_init.skip_through;
            continue;
        }
        mir_emit_prepacked_constant_arguments(out, i);
        mir_emit_prepacked_byte_check_argument(out, i);
        mir_emit_prepacked_bool_arguments(out, i);
        if (i <= mir_prepacked_skip_through)
            continue;
        if (insn->opcode == MIR_LOAD &&
            mir_match_inline_word_load_push(
                i, &inline_postincrement_helper,
                &inline_word_load_push)) {
            mir_emit_inline_word_load_push(
                out, labels, &inline_postincrement_helper,
                inline_postincrement_shared_label,
                &inline_word_load_push);
            ++mir_spilled_cfg_inline_postincrement_use_count;
            i = inline_word_load_push.continuation_instruction;
            continue;
        }
        if (insn->opcode == MIR_ADDRESS &&
            mir_match_dense_postincrement_index_switch(
                i, &dense_index_switch)) {
            if (!mir_emit_dense_postincrement_index_switch(
                    out, labels, &dense_index_switch))
                goto done;
            dense_switch = dense_index_switch.dispatch;
            mir_spilled_cfg_used_dense_byte_switch = 1;
            mir_spilled_cfg_dense_switch_case_count =
                dense_switch.case_count;
            mir_spilled_cfg_dense_switch_minimum =
                dense_switch.minimum_case;
            mir_spilled_cfg_dense_switch_maximum =
                dense_switch.maximum_case;
            mir_spilled_cfg_dense_switch_direct_condition = 1;
            mir_spilled_cfg_dense_switch_postincrement_index = 1;
            if (getenv("DCC_MIR_SWITCH_REPORT") != NULL)
                fprintf(stderr,
                        "; MIR dense-switch function=%s cases=%d "
                        "minimum=%d maximum=%d start=%d end=%d "
                        "direct-condition=1 postincrement-index=1\n",
                        mir.name, dense_switch.case_count,
                        dense_switch.minimum_case,
                        dense_switch.maximum_case,
                        i, dense_switch.end_instruction);
            i = dense_index_switch.skip_through;
            continue;
        }
        if (insn->opcode == MIR_ADDRESS &&
            mir_match_named_zero_branch(i, &named_zero_branch)) {
            if (!mir_emit_named_zero_branch(
                    out, labels, &named_zero_branch))
                goto done;
            i = named_zero_branch.branch_instruction;
            continue;
        }
        if (insn->opcode == MIR_LOAD &&
            mir_match_inline_typed_load_push(
                i, &inline_postincrement_helper,
                &inline_typed_load_push)) {
            if (!mir_emit_inline_typed_load_push(
                    out, labels, &inline_postincrement_helper,
                    inline_postincrement_shared_label,
                    &inline_typed_load_push))
                goto done;
            ++mir_spilled_cfg_inline_postincrement_use_count;
            i = inline_typed_load_push.continuation_instruction;
            continue;
        }
        if (insn->opcode == MIR_LOAD &&
            mir_emit_dense_byte_switch_condition_load(
                out, labels, i, &dense_switch)) {
            mir_spilled_cfg_used_dense_byte_switch = 1;
            mir_spilled_cfg_dense_switch_case_count =
                dense_switch.case_count;
            mir_spilled_cfg_dense_switch_minimum =
                dense_switch.minimum_case;
            mir_spilled_cfg_dense_switch_maximum =
                dense_switch.maximum_case;
            mir_spilled_cfg_dense_switch_direct_condition = 1;
            if (getenv("DCC_MIR_SWITCH_REPORT") != NULL)
                fprintf(stderr,
                        "; MIR dense-switch function=%s cases=%d "
                        "minimum=%d maximum=%d start=%d end=%d "
                        "direct-condition=1\n",
                        mir.name, dense_switch.case_count,
                        dense_switch.minimum_case,
                        dense_switch.maximum_case,
                        i, dense_switch.end_instruction);
            i = dense_switch.end_instruction;
            continue;
        }
        if (insn->opcode == MIR_CONST &&
            mir_match_dense_byte_switch(i, &dense_switch)) {
            mir_emit_dense_byte_switch(out, labels, &dense_switch);
            mir_spilled_cfg_used_dense_byte_switch = 1;
            mir_spilled_cfg_dense_switch_case_count =
                dense_switch.case_count;
            mir_spilled_cfg_dense_switch_minimum =
                dense_switch.minimum_case;
            mir_spilled_cfg_dense_switch_maximum =
                dense_switch.maximum_case;
            mir_spilled_cfg_dense_switch_direct_condition = 0;
            if (getenv("DCC_MIR_SWITCH_REPORT") != NULL)
                fprintf(stderr,
                        "; MIR dense-switch function=%s cases=%d "
                        "minimum=%d maximum=%d start=%d end=%d\n",
                        mir.name, dense_switch.case_count,
                        dense_switch.minimum_case,
                        dense_switch.maximum_case,
                        i, dense_switch.end_instruction);
            i = dense_switch.end_instruction;
            continue;
        }
        fused_result = mir_emit_dense_switch_table_pc_advance(
            out, i, &fused_end);
        if (fused_result < 0)
            goto done;
        if (fused_result > 0) {
            i = fused_end;
            continue;
        }

        switch (insn->opcode) {
        case MIR_NOP:
            break;
        case MIR_LABEL:
            {
            struct MirWordScanLoop word_scan_loop;

            if (mir_match_word_scan_loop(i, &word_scan_loop)) {
                mir_emit_word_scan_loop(
                    out, labels, &word_scan_loop);
                i = word_scan_loop.skip_through;
                continue;
            }
            if (insn->label < 0 || insn->label >= mir.next_label)
                goto done;
            /* Item T61 (mir-text-size-plan.md): skip printing a label
             * nothing ever jumps to - see mir_label_is_jump_target's
             * comment in dcc_mir.c for why this is always safe. */
            if (mir_label_is_jump_target(insn->label))
                mir_stream_printf(out, "L%d:\n", labels[insn->label]);
            break;
            }
        case MIR_PHI:
            break;
        case MIR_PARAM:
        case MIR_LOAD:
            {
            int memory_type;
            int memory_storage;
            int memory_offset;
            if (mir_forwarded_hl_value == insn->dst &&
                mir_forwarded_hl_instruction + 1 == mir_emit_instruction_index) {
                /* Item T84 (mir-text-size-plan.md): the preceding real
                 * MIR_STORE case armed this handoff after writing this
                 * exact object's value to memory - HL still holds it, so
                 * this fresh MIR_LOAD of the same object (e.g. `global +=
                 * rhs; return global;`) is a provably redundant re-fetch.
                 * Skip the memory read entirely and just re-persist
                 * whatever this value needs (a slot, or a further
                 * forwarding handoff), exactly like any other HL-resident
                 * value. */
                mir_forwarded_hl_value = -1;
                mir_forwarded_hl_instruction = -1;
                mir_emit_virtual_store(out, insn->dst);
                break;
            }
            if (mir_runtime_stride_index_value == insn->dst &&
                mir_runtime_stride_index_consumer ==
                    mir_emit_instruction_index + 1)
                break;
            if (insn->opcode == MIR_PARAM &&
                mir_nested_wide_add_source_parameter(insn->dst))
                break;
            if ((insn->opcode == MIR_PARAM || insn->opcode == MIR_LOAD) &&
                mir_value_has_direct_named_home(insn->dst))
                /* mir-migration-plan-next10 (extended by Item T27 to also
                 * cover MIR_LOAD): this value never gets a backend slot
                 * (see mir_value_has_direct_named_home) - every real use
                 * reloads directly from the stable named IX-relative home
                 * (mir_emit_virtual_load[_wide]), so the load this
                 * instruction would otherwise perform here is dead work;
                 * skip it entirely rather than load-then-discard. */
                break;
            if (insn->dst >= 0 && mir.backend_slots != NULL &&
                mir.backend_slots[insn->dst] < 0 &&
                (!mir_value_has_use(insn->dst) ||
                 mir_value_only_used_by_dead_unary(insn->dst) ||
                 mir_value_is_selfstore_incdec_source(insn->dst)))
                break;
            if ((type_size(insn->type) == 2 || type_size(insn->type) == 4) &&
                mir_load_is_single_call_argument(insn->dst,
                                                  type_size(insn->type)))
                break;
            if (mir_value_only_used_by_stable_pointer_argument(insn->dst))
                break;
            if (type_size(insn->type) == 2 &&
                mir_global_load_is_single_call_argument(insn->dst, 2))
                break;
            if (type_size(insn->type) == 2 &&
                mir_load_is_single_indirect_call_target(insn->dst, 2))
                break;
            if (!mir_scalar_memory_location(insn, &memory_type,
                                            &memory_storage, &memory_offset))
                goto done;
            if (insn->memory_size > 0 &&
                !type_is_struct_object(insn->type))
                memory_type = insn->type;
            if (type_is_struct_object(memory_type)) {
                if (memory_storage == SC_GLOBAL ||
                    memory_storage == SC_EXTERN) {
                    struct Sym *global = find_global(insn->name);
                    const char *assembly_name = asm_name_for(
                        global != NULL ? sym_asm_name(global) : insn->name);
                    if (memory_storage == SC_EXTERN &&
                        mir_extrn_should_emit(global))
                        mir_stream_printf(out, "\textrn %s\n", assembly_name);
                    mir_stream_printf(out, "\tld hl,%s\n", assembly_name);
                } else {
                    mir_stream_puts("\tpush ix\n\tpop hl\n", out);
                    mir_emit_hl_offset_from_ix(out, memory_offset);
                }
                mir_emit_virtual_store(out, insn->dst);
                break;
            }
            if ((memory_storage == SC_LOCAL || memory_storage == SC_PARAM) &&
                (memory_offset < -128 ||
                 memory_offset + type_size(memory_type) - 1 > 127)) {
                mir_stream_puts("\tpush ix\n\tpop hl\n", out);
                mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", memory_offset);
                if (type_size(memory_type) == 4) {
                    mir_stream_puts("\tld c,(hl)\n\tinc hl\n\tld b,(hl)\n"
                          "\tinc hl\n\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
                          "\tld h,b\n\tld l,c\n", out);
                    mir_emit_virtual_store_wide(out, insn->dst);
                } else {
                    mir_stream_puts("\tld a,(hl)\n", out);
                    if (type_size(memory_type) == 2)
                        mir_stream_puts("\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
                    else if (type_is_bool(memory_type)) {
                        int bool_label = new_label();
                        mir_stream_puts("\tor a\n\tld hl,0\n", out);
                        mir_stream_printf(out, "\tjp z, L%d\n\tinc hl\nL%d:\n",
                                bool_label, bool_label);
                    } else if ((memory_type & TYPE_UNSIGNED) != 0)
                        mir_stream_puts("\tld l,a\n\tld h,0\n", out);
                    else {
                        mir_stream_puts("\tld l,a\n", out);
                        mir_emit_signed_byte_extend(out);
                    }
                    mir_emit_virtual_store(out, insn->dst);
                }
                break;
            }
            if (memory_storage == SC_GLOBAL || memory_storage == SC_EXTERN ||
                memory_storage == SC_FUNC) {
                struct Sym *global = find_global(insn->name);
                const char *assembly_name = asm_name_for(
                    global != NULL ? sym_asm_name(global)
                                   : mir_declared_link_name(insn->name));
                if ((memory_storage == SC_EXTERN ||
                     (memory_storage == SC_FUNC && global != NULL &&
                      global->needs_extrn)) &&
                    mir_extrn_should_emit(global))
                    mir_stream_printf(out, "\textrn %s\n", assembly_name);
                if (type_size(memory_type) == 4 &&
                    memory_storage == SC_EXTERN)
                    mir_stream_printf(out,
                            "\tld hl,%s\n"
                            "\tld e,(hl)\n\tinc hl\n\tld d,(hl)\n"
                            "\tinc hl\n\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n"
                            "\tld l,a\n\tex de,hl\n",
                            assembly_name);
                else if (type_size(memory_type) == 4)
                    mir_stream_printf(out, "\tld hl,(%s)\n\tld de,(%s+2)\n",
                            assembly_name, assembly_name);
                else if (type_size(memory_type) == 1)
                    mir_stream_printf(out, "\tld a,(%s)\n\tld l,a\n", assembly_name);
                else
                    mir_stream_printf(out, "\tld hl,(%s)\n", assembly_name);
            } else {
                mir_stream_printf(out, "\tld l,(ix%+d)\n", memory_offset);
            }
            if (type_size(memory_type) == 4) {
                if (memory_storage != SC_GLOBAL && memory_storage != SC_EXTERN)
                    mir_stream_printf(out,
                            "\tld h,(ix%+d)\n\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
                            memory_offset + 1, memory_offset + 2,
                            memory_offset + 3);
                mir_emit_virtual_store_wide(out, insn->dst);
                break;
            }
            if (type_size(memory_type) == 1) {
                if (type_is_bool(memory_type)) {
                    int bool_label = new_label();
                    mir_stream_puts("\tld a,l\n\tor a\n\tld hl,0\n", out);
                    mir_stream_printf(out, "\tjp z, L%d\n\tinc hl\nL%d:\n",
                            bool_label, bool_label);
                } else if ((memory_type & TYPE_UNSIGNED) != 0) {
                    mir_stream_puts("\tld h,0\n", out);
                } else {
                    mir_emit_signed_byte_extend(out);
                }
            } else if (memory_storage != SC_GLOBAL &&
                       memory_storage != SC_EXTERN) {
                mir_stream_printf(out, "\tld h,(ix%+d)\n", memory_offset + 1);
            }
            mir_emit_virtual_store(out, insn->dst);
            break;
            }
        case MIR_CONST:
            {
            struct MirByteVerifyLoop byte_verify_loop;
            struct MirWideMaskZeroBranch wide_mask_branch;
            struct MirLowByteIndexLoop low_byte_loop;
            if (mir_match_byte_verify_value_prefix(
                    i, &byte_verify_loop)) {
                i = byte_verify_loop.value_end;
                continue;
            }
            if (mir_match_byte_verify_loop(i, &byte_verify_loop)) {
                mir_emit_byte_verify_loop(
                    out, labels, &byte_verify_loop);
                i = byte_verify_loop.skip_through;
                continue;
            }
            if (mir_match_wide_mask_zero_branch(
                    i, &wide_mask_branch)) {
                if (!mir_emit_wide_mask_zero_branch(
                        out, labels, &wide_mask_branch))
                    goto done;
                i = wide_mask_branch.branch_instruction;
                continue;
            }
            if (mir_match_low_byte_index_loop(i, &low_byte_loop)) {
                mir_emit_low_byte_index_loop(out, labels, &low_byte_loop);
                i = low_byte_loop.skip_through;
                continue;
            }
            if (!mir_value_has_use(insn->dst) ||
                mir_scalar_constant_is_rematerializable(insn->dst) ||
                mir_wide_constant_is_rematerializable(insn->dst) ||
                mir_wide_constant_is_signed_relational_immediate(
                    insn->dst) ||
                mir_constant_false_branch_consumer(insn->dst) >= 0 ||
                mir_call_only_constant(insn->dst) ||
                mir_binary_only_constant(insn->dst) ||
                mir_multiply_by_small_constant(insn->dst) ||
                mir_value_only_used_by_selfstore_adjust_amount(
                    insn->dst) ||
                mir_value_only_used_by_dead_stores(insn->dst) ||
                mir_value_only_used_by_dead_unary(insn->dst) ||
                mir_index_only_constant(insn->dst))
                break;
            mir_stream_printf(out, "\tld hl,%ld\n", insn->immediate & 0xffffL);
            if (type_size(insn->type) == 4) {
                mir_stream_printf(out, "\tld de,%lu\n",
                        ((unsigned long)insn->immediate >> 16) & 0xffffUL);
                mir_emit_virtual_store_wide(out, insn->dst);
            } else {
                mir_emit_virtual_store(out, insn->dst);
            }
            break;
            }
        case MIR_FLOAT_CONST:
            if (insn->dst >= 0 &&
                mir.backend_slots != NULL &&
                mir.backend_slots[insn->dst] >= 0) {
                int offset = mir_virtual_offset(insn->dst);
                unsigned long bits =
                    (unsigned long)insn->immediate & 0xffffffffUL;

                if (offset - 2 >= -128 && offset + 1 <= 127) {
                    mir_stream_printf(out,
                            "\tld (ix%+d),%lu\n\tld (ix%+d),%lu\n"
                            "\tld (ix%+d),%lu\n\tld (ix%+d),%lu\n",
                            offset, bits & 255UL,
                            offset + 1, (bits >> 8) & 255UL,
                            offset - 2, (bits >> 16) & 255UL,
                            offset - 1, (bits >> 24) & 255UL);
                    break;
                }
            }
            if (mir_call_only_constant(insn->dst) ||
                mir_wide_constant_is_rematerializable(insn->dst) ||
                mir_value_only_used_by_float_constant_unary(insn->dst))
                break;
            mir_stream_printf(out, "\tld hl,%lu\n\tld de,%lu\n",
                    (unsigned long)insn->immediate & 0xffffUL,
                    ((unsigned long)insn->immediate >> 16) & 0xffffUL);
            mir_emit_virtual_store_wide(out, insn->dst);
            break;
        case MIR_STRING_ADDRESS:
            if (mir_call_only_constant(insn->dst) ||
                mir_string_address_is_rematerializable(insn->dst))
                break;
            mir_stream_printf(out, "\tld hl,S%ld\n", insn->immediate);
            mir_emit_virtual_store(out, insn->dst);
            break;
        case MIR_ADDRESS:
            {
            struct MirLocalConstantByteStore byte_store;

            if (mir_match_local_constant_byte_store(i, &byte_store)) {
                mir_spilled_cfg_used_local_constant_byte_store = 1;
                mir_stream_printf(out, "\tld (ix%+d),%d\n",
                        byte_store.displacement, byte_store.value);
                i = byte_store.store_instruction;
                continue;
            }
            }
            if (mir_address_is_single_call_argument(insn->dst))
                break;
            if (mir_value_only_used_by_constant_absolute_address(insn->dst))
                break;
            if (mir_value_only_used_by_dynamic_absolute_index(insn->dst))
                break;
            if (mir_address_is_rematerializable(insn->dst))
                break;
            {
            if (!mir_emit_address_value_to_hl(out, insn->dst))
                goto done;
            mir_emit_virtual_store(out, insn->dst);
            break;
            }
        case MIR_COMPOUND_ADDRESS:
            mir_stream_puts("\tpush ix\n\tpop hl\n", out);
            mir_emit_hl_offset_from_ix(out, (int)insn->immediate);
            mir_emit_virtual_store(out, insn->dst);
            break;
        case MIR_VLA_SIZE:
            if (insn->immediate < -128 || insn->immediate + 1 > 127)
                goto done;
            mir_stream_printf(out, "\tld l,(ix%+ld)\n\tld h,(ix%+ld)\n",
                    insn->immediate, insn->immediate + 1);
            mir_emit_virtual_store(out, insn->dst);
            break;
        case MIR_MEMBER_ADDRESS:
            if (mir_value_only_used_by_constant_absolute_address(insn->dst))
                break;
            if (mir_value_only_used_by_stable_pointer_argument(insn->dst))
                break;
            if (mir_address_is_rematerializable(insn->dst))
                break;
            if (mir_emit_constant_absolute_address_value(out, insn))
                break;
            mir_emit_virtual_load(out, insn->src1);
            if (insn->immediate != 0)
                mir_stream_printf(out, "\tld de,%ld\n\tadd hl,de\n", insn->immediate);
            mir_emit_virtual_store(out, insn->dst);
            break;
        case MIR_INDEX_ADDRESS:
            {
            struct MirWideBytePackReturn wide_byte_pack;
            struct MirWideByteStore wide_byte_store;
            const struct MirInsn *index_definition =
                mir_definition(insn->src2);
            if (mir_runtime_stride_index_consumer == i &&
                mir_runtime_stride_index_base_value == insn->src1 &&
                mir_runtime_stride_index_value == insn->src2) {
                int memory_offset;
                int memory_storage;
                int memory_type;

                if (index_definition == NULL ||
                    !mir_scalar_memory_location(
                        index_definition, &memory_type,
                        &memory_storage, &memory_offset) ||
                    type_size(memory_type) != 2 ||
                    (memory_storage != SC_LOCAL &&
                     memory_storage != SC_PARAM) ||
                    memory_offset < -128 || memory_offset + 1 > 127)
                    goto done;
                mir_stream_printf(out,
                        "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
                        memory_offset, memory_offset + 1);
                if (insn->immediate == 2)
                    mir_stream_puts("\tsla e\n\trl d\n", out);
                mir_stream_puts("\tadd hl,de\n", out);
                {
                int memory_offset;
                int store_instruction;

                if (mir_runtime_stride_store_match(
                        i, &memory_offset, &store_instruction)) {
                    int increment_done = new_label();

                    mir_stream_printf(out,
                            "\tld e,(ix%+d)\n\tld d,(ix%+d)\n"
                            "\tinc (ix%+d)\n\tjp nz, L%d\n"
                            "\tinc (ix%+d)\nL%d:\n"
                            "\tld (hl),e\n\tinc hl\n\tld (hl),d\n",
                            memory_offset, memory_offset + 1,
                            memory_offset, increment_done,
                            memory_offset + 1, increment_done);
                    mir_runtime_stride_index_base_value = -1;
                    mir_runtime_stride_index_value = -1;
                    mir_runtime_stride_index_consumer = -1;
                    i = store_instruction;
                    continue;
                }
                }
                mir_runtime_stride_index_base_value = -1;
                mir_runtime_stride_index_value = -1;
                mir_runtime_stride_index_consumer = -1;
                mir_emit_virtual_store(out, insn->dst);
                break;
            }
            if (mir_match_wide_byte_pack_return(i, &wide_byte_pack)) {
                mir_emit_wide_byte_pack_return(out, &wide_byte_pack);
                i = wide_byte_pack.skip_through;
                continue;
            }
            if (mir_match_wide_byte_store(i, &wide_byte_store)) {
                mir_emit_wide_byte_store(out, &wide_byte_store);
                i = wide_byte_store.skip_through;
                continue;
            }
            if (mir_value_only_used_by_constant_absolute_address(insn->dst))
                break;
            if (mir_address_is_rematerializable(insn->dst))
                break;
            if (mir_emit_constant_absolute_address_value(out, insn))
                break;
            if (mir_emit_dynamic_absolute_index(out, insn))
                break;
            if (insn->base_name[0] != 0) {
                int stride_type;
                int stride_storage;
                int stride_offset;
                if (!mir_declared_location(insn->base_name, &stride_type,
                                           &stride_storage, &stride_offset))
                    goto done;
                mir_emit_virtual_load(out, insn->src2);
                mir_stream_puts("\tpush hl\n", out);
                if (stride_storage == SC_GLOBAL ||
                    stride_storage == SC_EXTERN)
                    mir_stream_printf(out, "\tld de,(%s)\n", asm_name_for(
                        mir_declared_link_name(insn->base_name)));
                else
                    mir_stream_printf(out, "\tld e,(ix%+d)\n\tld d,(ix%+d)\n",
                            stride_offset, stride_offset + 1);
                mir_stream_puts("\tpop hl\n", out);
                mir_emit_runtime_call(out, "__mulu");
                if (insn->secondary_offset == 2)
                    mir_stream_puts("\tadd hl,hl\n", out);
                else if (insn->secondary_offset > 2) {
                    unsigned long secondary_multiplier =
                        (unsigned long)insn->secondary_offset & 0xffffUL;
                    if (mir_mul_const_fast_path_eligible(
                            secondary_multiplier, insn->dst))
                        mir_emit_mul_hl_const(out, secondary_multiplier);
                    else {
                        mir_stream_printf(out, "\tld de,%d\n", insn->secondary_offset);
                        mir_emit_runtime_call(out, "__mulu");
                    }
                }
                mir_stream_puts("\tpush hl\n", out);
                mir_emit_virtual_load(out, insn->src1);
                mir_stream_puts("\tpop de\n\tadd hl,de\n", out);
                mir_emit_virtual_store(out, insn->dst);
                break;
            }
            if (index_definition != NULL &&
                index_definition->opcode == MIR_CONST) {
                long byte_offset = index_definition->immediate *
                                   insn->immediate;
                if (mir_forwarded_hl_value == insn->src2 &&
                    mir_forwarded_hl_instruction + 1 == i) {
                    mir_forwarded_hl_value = -1;
                    mir_forwarded_hl_instruction = -1;
                }
                if (mir_consume_planned_stack(
                        out, insn->src1, i, "hl")) {
                    /* The planned base is now in HL. */
                } else if (mir_forwarded_stack_value == insn->src1 &&
                           mir_forwarded_stack_target_instruction == i) {
                    mir_stream_puts("\tpop hl\n", out);
                    mir_forwarded_stack_value = -1;
                    mir_forwarded_stack_instruction = -1;
                    mir_forwarded_stack_target_instruction = -1;
                } else
                    mir_emit_virtual_load(out, insn->src1);
                if (byte_offset != 0)
                    mir_stream_printf(out, "\tld de,%ld\n\tadd hl,de\n",
                            byte_offset & 0xffffL);
            } else {
                mir_emit_virtual_load(out, insn->src2);
                if (insn->immediate != 1) {
                    unsigned long index_multiplier =
                        (unsigned long)insn->immediate & 0xffffUL;
                    if (mir_mul_const_fast_path_eligible(
                            index_multiplier, insn->dst))
                        mir_emit_mul_hl_const(out, index_multiplier);
                    else {
                        mir_stream_printf(out, "\tld de,%ld\n", insn->immediate);
                        mir_emit_runtime_call(out, "__mulu");
                    }
                }
                if (mir_consume_planned_stack(
                        out, insn->src1, i, "de")) {
                    mir_stream_puts("\tadd hl,de\n", out);
                } else if (mir_forwarded_stack_value == insn->src1 &&
                           mir_forwarded_stack_target_instruction == i) {
                    mir_stream_puts("\tpop de\n\tadd hl,de\n", out);
                    mir_forwarded_stack_value = -1;
                    mir_forwarded_stack_instruction = -1;
                    mir_forwarded_stack_target_instruction = -1;
                } else {
                    mir_stream_puts("\tpush hl\n", out);
                    mir_emit_virtual_load(out, insn->src1);
                    mir_stream_puts("\tpop de\n\tadd hl,de\n", out);
                }
            }
            mir_emit_virtual_store(out, insn->dst);
            }
            break;
        case MIR_LOAD_INDIRECT:
            {
            struct MirIndirectIncDec incdec;
            const struct MirInsn *address_definition =
                mir_definition(insn->src1);
            int de_consumer;
            int de_value;
            if (mir_match_indirect_incdec(i, &incdec)) {
                mir_emit_indirect_incdec(out, &incdec);
                i = incdec.store_instruction;
                break;
            }
            if (address_definition != NULL &&
                address_definition->opcode == MIR_BINARY) {
                int amount;
                int load_index;
                int store_index;

                if (mir_binary_is_selfstore_global_predecrement_load(
                        (int)(address_definition - mir.insns),
                        &store_index, &load_index, &amount) &&
                    load_index == i)
                    break;
            }
            if (mir_indirect_load_is_single_stable_pointer_call_argument(
                    insn->dst, 2))
                break;
            if (mir_emit_constant_absolute_load(out, insn))
                break;
            mir_emit_virtual_load(out, insn->src1);
            de_consumer = mir_indirect_load_de_consumer(i, &de_value);
            if (de_consumer >= 0) {
                mir_emit_indirect_load_to_de(out, insn);
                mir_forwarded_de_value = de_value;
                mir_forwarded_de_consumer = de_consumer;
                break;
            }
            if (insn->memory_size == 4) {
                mir_stream_puts("\tpush hl\n\tld a,(hl)\n\tinc hl\n"
                      "\tld h,(hl)\n\tld l,a\n\tex (sp),hl\n"
                      "\tinc hl\n\tinc hl\n\tld e,(hl)\n\tinc hl\n"
                      "\tld d,(hl)\n\tpop hl\n", out);
            } else if (insn->memory_size == 1) {
                mir_stream_puts("\tld l,(hl)\n", out);
                if (type_is_bool(insn->type)) {
                    end_label = new_label();
                    mir_stream_puts("\tld a,l\n\tor a\n\tld hl,0\n", out);
                    mir_stream_printf(out, "\tjp z, L%d\n\tinc hl\nL%d:\n",
                            end_label, end_label);
                } else if ((insn->type & TYPE_UNSIGNED) != 0) {
                    mir_stream_puts("\tld h,0\n", out);
                } else {
                    mir_emit_signed_byte_extend(out);
                }
            } else {
                mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
            }
            if (insn->bit_width > 0)
                mir_emit_bitfield_extract(out, insn);
            if (insn->memory_size == 4)
                mir_emit_virtual_store_wide(out, insn->dst);
            else
                mir_emit_virtual_store(out, insn->dst);
            }
            break;
        case MIR_STORE:
            {
            int memory_type;
            int memory_storage;
            int memory_offset;
            int dense_switch_handoff;
            const struct MirInsn *producer = mir_definition(insn->src1);
            {
            int bool_offset;
            int bool_store_instruction;

            if (producer != NULL &&
                mir_bool_call_named_home(
                    insn->src1, &bool_store_instruction,
                    &bool_offset) &&
                bool_store_instruction == i) {
                int normalized = new_label();

                mir_emit_virtual_load(out, insn->src1);
                mir_stream_puts("\tld a,h\n\tor l\n\tld hl,0\n", out);
                mir_stream_printf(out,
                        "\tjp z, L%d\n\tinc hl\nL%d:\n"
                        "\tld (ix%+d),l\n",
                        normalized, normalized, bool_offset);
                break;
            }
            }
            if (producer != NULL && producer->opcode == MIR_BINARY) {
                int producer_index = (int)(producer - mir.insns);
                int phi_value;
                int narrow_phi_store_index;
                int wide_phi_store_index;
                int wide_selfstore_offset;
                int wide_selfstore_store_index;
                int predecrement_load_index;
                int small_add_amount;
                int selfstore_store_index;
                if ((mir_binary_is_narrow_phi_adjust(
                        producer_index, &phi_value,
                        &narrow_phi_store_index) &&
                     narrow_phi_store_index == i) ||
                    (mir_binary_is_wide_phi_update(
                        producer_index, &phi_value,
                        &wide_phi_store_index) &&
                     wide_phi_store_index == i) ||
                    (mir_binary_is_selfstore_wide_increment(
                         producer_index, &wide_selfstore_store_index,
                         &wide_selfstore_offset) &&
                     wide_selfstore_store_index == i) ||
                    ((mir_binary_is_selfstore_incdec(
                        producer_index, &selfstore_store_index) ||
                     mir_binary_is_selfstore_small_adjust(
                         producer_index, &selfstore_store_index,
                         &small_add_amount) ||
                     mir_binary_is_selfstore_global_predecrement_load(
                         producer_index, &selfstore_store_index,
                         &predecrement_load_index,
                         &small_add_amount)) &&
                    selfstore_store_index == i))
                    break;
            }
            if (mir_object_is_fully_promoted(insn->object) ||
                mir_store_is_dead(i))
                break;
            if (!mir_scalar_memory_location(insn, &memory_type,
                                            &memory_storage, &memory_offset))
                goto done;
            if (insn->memory_size > 0 &&
                !type_is_struct_object(insn->type))
                memory_type = insn->type;
            if (type_is_struct_object(memory_type)) {
                int size = type_size(memory_type);
                mir_emit_virtual_load(out, insn->src1);
                mir_stream_puts("\tex de,hl\n", out);
                if (memory_storage == SC_GLOBAL ||
                    memory_storage == SC_EXTERN) {
                    struct Sym *global = find_global(insn->name);
                    const char *assembly_name = asm_name_for(
                        global != NULL ? sym_asm_name(global)
                                       : mir_declared_link_name(insn->name));
                    if (memory_storage == SC_EXTERN &&
                        mir_extrn_should_emit(global))
                        mir_stream_printf(out, "\textrn %s\n", assembly_name);
                    mir_stream_printf(out, "\tld hl,%s\n", assembly_name);
                } else {
                    mir_stream_puts("\tpush ix\n\tpop hl\n", out);
                    if (memory_offset != 0)
                        mir_stream_printf(out, "\tld bc,%d\n\tadd hl,bc\n",
                                memory_offset);
                }
                /* mir-text-size Item T6: the two `ex de,hl` swaps above
                 * leave DE=source, HL=dest; swap once more (HL=source,
                 * DE=dest, a pure register exchange with no stack/frame
                 * side effects) and copy the whole struct with one
                 * `ldir` instead of an unrolled byte-by-byte loop - the
                 * same fix as Item T5's MIR_RETURN case, applied here to
                 * struct assignment/store. */
                if (size > 0) {
                    mir_stream_puts("\tex de,hl\n", out);
                    mir_stream_printf(out, "\tld bc,%d\n\tldir\n", size);
                }
                break;
            }
            if ((memory_storage == SC_LOCAL || memory_storage == SC_PARAM) &&
                (memory_offset < -128 ||
                 memory_offset + type_size(memory_type) - 1 > 127)) {
                if (type_size(memory_type) == 4) {
                    mir_emit_virtual_load_wide(out, insn->src1);
                    mir_stream_puts("\tpush de\n\tpush hl\n\tpush ix\n\tpop hl\n", out);
                    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n", memory_offset);
                    mir_stream_puts("\tpop bc\n\tld (hl),c\n\tinc hl\n\tld (hl),b\n"
                          "\tinc hl\n\tpop bc\n\tld (hl),c\n\tinc hl\n\tld (hl),b\n",
                          out);
                } else {
                    mir_emit_virtual_load(out, insn->src1);
                    mir_stream_puts("\tex de,hl\n\tpush ix\n\tpop hl\n", out);
                    mir_stream_printf(out, "\tld bc,%d\n\tadd hl,bc\n\tld (hl),e\n",
                            memory_offset);
                    if (type_size(memory_type) == 2)
                        mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
                }
                break;
            }
            if (mir_wide_store_forwarding_enabled &&
                type_size(memory_type) == 4)
                mir_spilled_cfg_used_wide_store_forwarding = 1;
            dense_switch_handoff =
                mir_store_feeds_dense_byte_switch(i, insn->src1);
            if (!dense_switch_handoff &&
                !(type_size(memory_type) == 4 &&
                  (mir_wide_store_forwarding_enabled ||
                   (producer != NULL &&
                    mir_cfg_block_count() == 1 &&
                    mir.local_bytes >= 256 &&
                    (producer->opcode == MIR_CONST ||
                     producer->opcode == MIR_FLOAT_CONST)))))
                mir_emit_virtual_load(out, insn->src1);
            {
            int narrow_hl_preserving_store = 0;
            if (memory_storage == SC_GLOBAL || memory_storage == SC_EXTERN) {
                struct Sym *global = find_global(insn->name);
                const char *assembly_name = asm_name_for(
                    global != NULL ? sym_asm_name(global)
                                   : mir_declared_link_name(insn->name));
                if (memory_storage == SC_EXTERN &&
                    mir_extrn_should_emit(global))
                    mir_stream_printf(out, "\textrn %s\n", assembly_name);
                if (type_size(memory_type) == 4) {
                    mir_emit_virtual_load_wide(out, insn->src1);
                    if (memory_storage == SC_EXTERN)
                        mir_stream_printf(out,
                                "\tpush de\n\tpush hl\n\tld hl,%s\n"
                                "\tpop bc\n\tld (hl),c\n\tinc hl\n"
                                "\tld (hl),b\n\tinc hl\n\tpop bc\n"
                                "\tld (hl),c\n\tinc hl\n\tld (hl),b\n",
                                assembly_name);
                    else
                        mir_stream_printf(out, "\tld (%s),hl\n\tld (%s+2),de\n",
                                assembly_name, assembly_name);
                } else if (type_size(memory_type) == 1)
                    mir_stream_printf(out, "\tld a,l\n\tld (%s),a\n", assembly_name);
                else {
                    mir_stream_printf(out, "\tld (%s),hl\n", assembly_name);
                    /* Item T84 (mir-text-size-plan.md): this global/extern
                     * 2-byte store form reads HL out to memory without
                     * disturbing it - the same "narrow store, HL still
                     * resident" property Item T83 already relies on for
                     * the local/param in-range form below. */
                    narrow_hl_preserving_store = (type_size(memory_type) ==
                                                   2);
                }
            } else {
                if (type_size(memory_type) == 4) {
                    mir_emit_virtual_load_wide(out, insn->src1);
                    mir_stream_printf(out,
                            "\tld (ix%+d),l\n\tld (ix%+d),h\n"
                            "\tld (ix%+d),e\n\tld (ix%+d),d\n",
                            memory_offset, memory_offset + 1,
                            memory_offset + 2, memory_offset + 3);
                } else {
                    mir_stream_printf(out, "\tld (ix%+d),l\n", memory_offset);
                }
                if (type_size(memory_type) == 2) {
                    mir_stream_printf(out, "\tld (ix%+d),h\n", memory_offset + 1);
                    narrow_hl_preserving_store = 1;
                }
            }
            if (narrow_hl_preserving_store) {
                /* Item T83 (mir-text-size-plan.md): this in-range,
                 * narrow store never disturbs HL (it only reads L/H out
                 * into memory), so if insn->src1's very next consumer
                 * (immediately following this store, through any
                 * intervening MIR_NOP - the same skip
                 * mir_emit_virtual_store's own store-forwarding already
                 * relies on) is one of mir_can_forward_hl_to_next's
                 * recognized single-operand shapes, re-arm the one-shot
                 * HL forwarding handoff for it instead of forcing a slot
                 * reload. Closes a second-hop gap this store's own
                 * forwarded-into-it value could not reach on its own:
                 * mir_emit_virtual_store already skips *this* store's
                 * own separate write when insn->src1 forwards straight
                 * into it (forward_to_store), but once that forwarding
                 * is consumed here, nothing previously re-armed it for
                 * whatever reads insn->src1 again right after - typically
                 * the same object's own value being returned or passed
                 * as a call argument immediately after its assignment
                 * (`c = a + b; return c;`, `saved = a + b; return
                 * f(saved) - saved;`). Found via tests/tc89decl.c's
                 * timpreg and tests/tmirslot.c's cross_call. */
                if (mir_can_forward_hl_to_next(insn->src1)) {
                    mir_forwarded_hl_value = insn->src1;
                    mir_forwarded_hl_instruction = i;
                } else if (mir_can_forward_hl_to_call_argument_first_use(
                               insn->src1)) {
                    /* Same reasoning, but the next consumer is an
                     * ARG+CALL pair (tests/tmirslot.c's cross_call:
                     * `saved = a + b; return scale(saved) - saved;`)
                     * rather than a single-operand opcode. */
                    mir_forwarded_hl_value = insn->src1;
                    mir_forwarded_hl_instruction =
                        mir_call_argument_after_nops(i);
                } else {
                    /* Item T84 (mir-text-size-plan.md): distinct from the
                     * two re-arm checks above (which forward insn->src1,
                     * the value just written, into its *own* next SSA
                     * use) - this instead looks at whether the very next
                     * real instruction is a fresh MIR_LOAD of the *exact
                     * same memory location* this store just wrote
                     * (tests/tcaslv.c's apply_global_compound_param:
                     * `global_lhs += rhs; return global_lhs;` - the
                     * return reads global_lhs via a brand-new MIR_LOAD,
                     * not via src1's SSA id, since the object was
                     * genuinely reassigned). Globals/externs have no
                     * backend object id (insn->object == -1 for them -
                     * mir_scalar_memory_location falls back to resolving
                     * their location by declared name instead), so this
                     * re-resolves the candidate load's own location via
                     * the same helper used for the store itself and
                     * compares storage class + offset directly, rather
                     * than comparing insn->object (which only locals/
                     * params populate). A narrow HL-preserving store
                     * makes that reload provably redundant; arm
                     * forwarding keyed on the load's own dst value (case
                     * MIR_LOAD's own check consumes this). */
                    int forward_after_store = mir_forward_skip_target(i);
                    if (forward_after_store < mir.count &&
                        mir.insns[forward_after_store].opcode == MIR_LOAD &&
                        mir_same_scalar_memory_location(
                            insn, &mir.insns[forward_after_store])) {
                        mir_forwarded_hl_value =
                            mir.insns[forward_after_store].dst;
                        mir_forwarded_hl_instruction =
                            forward_after_store - 1;
                    }
                }
            }
            }
            break;
            }
        case MIR_STORE_INDIRECT:
            if (insn->bit_width > 0) {
                int shift;
                mir_emit_virtual_load(out, insn->src2);
                mir_emit_hl_and_const(
                    out, insn->bit_width >= 16
                        ? 0xffffU : (1U << insn->bit_width) - 1U);
                for (shift = 0; shift < insn->bit_shift; ++shift)
                    mir_stream_puts("\tadd hl,hl\n", out);
                mir_emit_hl_and_const(out, insn->bit_mask);
                mir_stream_puts("\tpush hl\n", out);
                mir_emit_virtual_load(out, insn->src1);
                mir_stream_puts("\tpush hl\n\tld a,(hl)\n\tinc hl\n"
                      "\tld h,(hl)\n\tld l,a\n", out);
                mir_emit_hl_and_const(out, (~insn->bit_mask) & 0xffffU);
                mir_stream_puts("\tpop bc\n\tpop de\n"
                      "\tld a,l\n\tor e\n\tld l,a\n"
                      "\tld a,h\n\tor d\n\tld h,a\n"
                      "\tex de,hl\n\tld h,b\n\tld l,c\n"
                      "\tld (hl),e\n\tinc hl\n\tld (hl),d\n", out);
                break;
            }
            if (!mir_constant_absolute_access_supported(insn) &&
                mir_forwarded_hl_value == insn->src2 &&
                mir_forwarded_hl_instruction + 1 == i &&
                mir_planned_stack_matches_consumer(insn->src1, i) &&
                mir_planned_stack_is_emitted(insn->src1)) {
                /* The planned store address was pushed onto the real Z80
                 * stack before the intervening call; pushing the call
                 * result (still held in HL) puts it ON TOP of that
                 * address. Pop the just-pushed value first (into de),
                 * then consume the planned address (into hl) - popping
                 * in the opposite order would hand the address to `hl`'s
                 * sibling and the value to the address role, corrupting
                 * memory. See the identical, correctly-ordered sibling
                 * case below (mir_forwarded_stack_value). */
                mir_stream_puts("\tpush hl\n", out);
                mir_forwarded_hl_value = -1;
                mir_forwarded_hl_instruction = -1;
                mir_stream_puts("\tpop de\n", out);
                if (!mir_consume_planned_stack(
                        out, insn->src1, i, "hl"))
                    mir_planned_stack_invalid = 1;
                ++mir_spilled_cfg_indirect_store_address_forwarding_count;
                mir_stream_puts("\tld (hl),e\n", out);
                if (insn->memory_size > 1)
                    mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
                break;
            }
            if (!mir_constant_absolute_access_supported(insn) &&
                mir_forwarded_hl_value == insn->src2 &&
                mir_forwarded_hl_instruction + 1 == i &&
                mir_indirect_store_address_has_direct_reload(insn) &&
                !mir_value_currently_uses_stack_handoff(insn->src1, i)) {
                mir_stream_puts("\tpush hl\n", out);
                mir_forwarded_hl_value = -1;
                mir_forwarded_hl_instruction = -1;
                mir_emit_virtual_load(out, insn->src1);
                mir_stream_puts("\tpop de\n\tld (hl),e\n", out);
                if (insn->memory_size > 1)
                    mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
                break;
            }
            if (insn->memory_size == 4) {
                if (!mir_emit_wide_indirect_store(out, insn))
                    goto done;
                break;
            }
            if (mir_emit_constant_absolute_store(out, insn))
                break;
            if (mir_forwarded_stack_value == insn->src2 &&
                mir_forwarded_stack_target_instruction == i) {
                mir_stream_puts("\tpop de\n", out);
                if (mir_consume_planned_stack(
                        out, insn->src1, i, "hl"))
                    ++mir_spilled_cfg_indirect_store_address_forwarding_count;
                else
                    mir_emit_virtual_load(out, insn->src1);
                mir_stream_puts("\tld (hl),e\n", out);
                if (insn->memory_size > 1)
                    mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
                mir_forwarded_stack_value = -1;
                mir_forwarded_stack_instruction = -1;
                mir_forwarded_stack_target_instruction = -1;
                break;
            }
            if (mir_planned_stack_matches_consumer(insn->src1, i) &&
                mir_planned_stack_is_emitted(insn->src1)) {
                mir_emit_virtual_load(out, insn->src2);
                mir_stream_puts("\tex de,hl\n", out);
                if (!mir_consume_planned_stack(
                        out, insn->src1, i, "hl"))
                    mir_planned_stack_invalid = 1;
                ++mir_spilled_cfg_indirect_store_address_forwarding_count;
                mir_stream_puts("\tld (hl),e\n", out);
                if (insn->memory_size > 1)
                    mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
                break;
            }
            mir_emit_virtual_load(out, insn->src1);
            mir_stream_puts("\tpush hl\n", out);
            mir_emit_virtual_load(out, insn->src2);
            mir_stream_puts("\tex de,hl\n\tpop hl\n\tld (hl),e\n", out);
            if (insn->memory_size > 1)
                mir_stream_puts("\tinc hl\n\tld (hl),d\n", out);
            break;
        case MIR_VLA_SAVE:
            mir_stream_puts("\tld hl,0\n\tadd hl,sp\n", out);
            mir_emit_frame_word_store(out, (int)insn->immediate);
            break;
        case MIR_VLA_ALLOC:
            mir_emit_virtual_load(out, insn->src1);
            mir_emit_frame_word_store(out, insn->secondary_offset);
            mir_stream_puts("\tex de,hl\n\tld hl,0\n\tadd hl,sp\n"
                  "\tor a\n\tsbc hl,de\n\tld sp,hl\n", out);
            if (opt_stack_check)
                mir_emit_runtime_call(out, "__stchk");
            mir_stream_puts("\tld hl,0\n\tadd hl,sp\n", out);
            mir_emit_frame_word_store(out, (int)insn->immediate);
            break;
        case MIR_VLA_RESTORE:
            if (insn->immediate == 0)
                break;
            mir_emit_frame_word_load(out, (int)insn->immediate);
            mir_stream_puts("\tld sp,hl\n", out);
            break;
        case MIR_COPY_AGGREGATE:
            {
                if (insn->memory_size <= 0 || insn->memory_size > 1024)
                    goto done;
                mir_emit_virtual_load(out, insn->src1);
                mir_stream_puts("\tpush hl\n", out);
                mir_emit_virtual_load(out, insn->src2);
                /* mir-text-size Item T6: `insn->src2` (source) is already
                 * loaded into HL here - exactly what `ldir` needs - so
                 * just pop the saved destination address straight into
                 * DE (no need for the old ld b,h/ld c,l + second pop hl
                 * dance) and copy the whole struct in one instruction
                 * instead of an unrolled byte-by-byte loop. Same fix
                 * shape as Item T5's MIR_RETURN case. */
                mir_stream_puts("\tpop de\n", out);
                mir_stream_printf(out, "\tld bc,%d\n\tldir\n", insn->memory_size);
            }
            break;
        case MIR_UNARY:
            {
            struct MirNestedWideAdd nested_add;

            if (mir_match_nested_wide_add(i, &nested_add)) {
                const struct MirInsn *source =
                    mir_definition(nested_add.narrow_source);

                if (!mir_emit_nested_wide_add_parameter(
                        out, nested_add.outer_lhs))
                    goto done;
                mir_stream_puts("\tpush de\n\tpush hl\n", out);
                mir_emit_virtual_load(out, nested_add.narrow_source);
                if (source == NULL ||
                    !mir_emit_cast(out, source->type, insn->type))
                    goto done;
                mir_stream_puts("\tpush de\n\tpush hl\n", out);
                mir_emit_virtual_load_wide(out, nested_add.wide_rhs);
                if (!mir_emit_wide_operation(
                        out, &mir.insns[nested_add.inner_instruction]) ||
                    !mir_emit_wide_operation(
                        out, &mir.insns[nested_add.outer_instruction]))
                    goto done;
                mir_forwarded_wide_value = nested_add.result;
                mir_forwarded_wide_instruction =
                    nested_add.return_instruction - 1;
                i = nested_add.return_instruction - 1;
                continue;
            }
            }
            /* mir-text-size Item T12: every unary op here (cast, +, -, ~,
             * !) is a pure value transform with no side effect beyond
             * producing insn->dst - if that result has no use (the common
             * "(void)param;" idiom used to silence unused-parameter
             * warnings in callback/visitor signatures), skip the whole
             * instruction, including the operand load, instead of loading
             * src1 into hl only to immediately discard it. */
            if (!mir_value_has_use(insn->dst))
                break;
            {
                unsigned long float_bits;

                if (mir_float_constant_expression_bits(
                        insn->dst, &float_bits))
                    break;
            }
            if (mir_widened_param_is_single_call_argument(
                    insn->dst, NULL, NULL))
                break;
            if (mir_forwarded_de_value == insn->dst &&
                mir_forwarded_de_consumer > i)
                break;
            if (mir_value_is_wide_narrow_multiply_widen(insn->dst))
                break;
            if (mir_value_is_nested_truth_comparison_input(insn->dst))
                break;
            if (mir_unary_is_elided_double_not_inner(i))
                break;
            if (mir_unary_is_fusable_not_branch(i)) {
                const struct MirInsn *branch = &mir.insns[i + 1];
                int target;
                if (branch->label < 0 || branch->label >= mir.next_label)
                    goto done;
                target = mir_find_label(branch->label);
                if (target < 0)
                    goto done;
                mir_emit_virtual_load(out, insn->src1);
                mir_stream_puts("\tld a,h\n\tor l\n", out);
                mir_spilled_cfg_used_unary_not_branch_fusion = 1;
                if (!mir_emit_conditional_branch_with_phi_copies(
                        out, labels, "z", i + 1, target, branch->label))
                    goto done;
                ++i;
                continue;
            }
            {
                int double_not_source;

                if (mir_match_double_logical_not(
                        i, &double_not_source)) {
                    int false_label = new_label();

                    if (mir_value_is_wide(double_not_source)) {
                        mir_emit_virtual_load_wide(
                            out, double_not_source);
                        mir_stream_puts("\tld a,d\n\tor e\n\tor h\n\tor l\n",
                              out);
                    } else {
                        mir_emit_virtual_load(
                            out, double_not_source);
                        mir_stream_puts("\tld a,h\n\tor l\n", out);
                    }
                    mir_stream_puts("\tld hl,0\n", out);
                    mir_stream_printf(
                        out,
                        "\tjr z,L%d\n\tinc l\nL%d:\n",
                        false_label, false_label);
                    mir_emit_virtual_store(out, insn->dst);
                    break;
                }
            }
            if (mir_value_is_wide(insn->src1))
                mir_emit_virtual_load_wide(out, insn->src1);
            else
                mir_emit_virtual_load(out, insn->src1);
            if (insn->immediate == 0) {
                const struct MirInsn *source = mir_definition(insn->src1);
                if (!mir_emit_cast(out, source != NULL ? source->type : 0,
                                   insn->type))
                    goto done;
            } else if (insn->immediate == '+') {
                /* Unary plus. */
            } else if (insn->immediate == '-') {
                if (type_is_float(insn->type)) {
                    mir_stream_puts("\tld a,d\n\txor 128\n\tld d,a\n", out);
                } else if (mir_value_is_wide(insn->src1)) {
                    int carry_label = new_label();
                    mir_stream_puts("\tld a,l\n\tcpl\n\tld l,a\n"
                          "\tld a,h\n\tcpl\n\tld h,a\n"
                          "\tld a,e\n\tcpl\n\tld e,a\n"
                          "\tld a,d\n\tcpl\n\tld d,a\n"
                          "\tinc hl\n\tld a,h\n\tor l\n", out);
                      mir_stream_printf(out, "\tjp nz, L%d\n\tinc de\nL%d:\n",
                            carry_label, carry_label);
                    } else
                    mir_stream_puts("\txor a\n\tsub l\n\tld l,a\n\tsbc a,a\n\tsub h\n\tld h,a\n", out);
            } else if (insn->immediate == '~') {
                mir_stream_puts("\tld a,l\n\tcpl\n\tld l,a\n\tld a,h\n\tcpl\n\tld h,a\n", out);
                if (mir_value_is_wide(insn->src1))
                    mir_stream_puts("\tld a,e\n\tcpl\n\tld e,a\n"
                          "\tld a,d\n\tcpl\n\tld d,a\n", out);
            } else if (insn->immediate == '!') {
                end_label = new_label();
                if (mir_value_is_wide(insn->src1))
                    mir_stream_puts("\tld a,d\n\tor e\n\tor h\n\tor l\n\tld hl,0\n", out);
                else
                    mir_stream_puts("\tld a,h\n\tor l\n\tld hl,0\n", out);
                mir_stream_printf(out, "\tjr nz,L%d\n\tinc l\nL%d:\n",
                        end_label, end_label);
            } else {
                goto done;
            }
            if (type_size(insn->type) == 4)
                mir_emit_virtual_store_wide(out, insn->dst);
            else
                mir_emit_virtual_store(out, insn->dst);
            break;
        case MIR_BINARY:
            {
            int small_add_amount;
            int selfstore_store_index;
            int predecrement_load_index;
            int narrow_phi_source;
            int wide_phi_source;
            int wide_selfstore_offset;
            int wide_selfstore_store_index;
            int narrow_multiply_left;
            int narrow_multiply_right;
            int narrow_multiply_unsigned;
            int multiply_index;
            int addend_value;
            if (mir_value_only_used_by_selfstore_adjust_amount(insn->dst))
                break;
            if (mir_float_multiply_is_fused(i))
                break;
            if (mir_float_madd_match(i, &multiply_index, &addend_value)) {
                const struct MirInsn *multiply = &mir.insns[multiply_index];
                mir_emit_virtual_load_wide(out, addend_value);
                mir_stream_puts("\tpush de\n\tpush hl\n", out);
                mir_emit_virtual_load_wide(out, multiply->src1);
                mir_stream_puts("\tpush de\n\tpush hl\n", out);
                mir_emit_virtual_load_wide(out, multiply->src2);
                mir_emit_runtime_call(out, "__fmaf");
                mir_stream_puts("\tpop bc\n\tpop bc\n\tpop bc\n\tpop bc\n", out);
                mir_emit_virtual_store_wide(out, insn->dst);
                break;
            }
            if (!mir_wide_multiply_is_fused_mulmod(i) &&
                mir_wide_narrow_multiply_match(
                    insn, &narrow_multiply_left,
                    &narrow_multiply_right,
                    &narrow_multiply_unsigned)) {
                const struct MirInsn *left =
                    mir_definition(narrow_multiply_left);
                const struct MirInsn *right =
                    mir_definition(narrow_multiply_right);
                unsigned long constant_bits;

                if (mir_fold_narrow_multiply_constants(
                        left, right, narrow_multiply_unsigned,
                        &constant_bits)) {
                    mir_stream_printf(out, "\tld hl,%lu\n\tld de,%lu\n",
                            constant_bits & 0xffffUL,
                            (constant_bits >> 16) & 0xffffUL);
                } else {
                    const char *multiply_helper;

                    if (!mir_narrow_multiply_has_named_word_home(left) ||
                        !mir_emit_named_word_load_to_hl(out, left))
                        mir_emit_virtual_load(out, narrow_multiply_left);
                    mir_stream_puts("\tld c,l\n\tld b,h\n", out);
                    if (!mir_narrow_multiply_has_named_word_home(right) ||
                        !mir_emit_named_word_load_to_hl(out, right))
                        mir_emit_virtual_load(out, narrow_multiply_right);
                    multiply_helper = narrow_multiply_unsigned
                        ? "__m1u"
                        : (mir_instruction_is_in_cfg_cycle(i) &&
                           (mir_narrow_multiply_source_is_m1q_bounded(
                                left) ||
                            mir_narrow_multiply_source_is_m1q_bounded(
                                right)))
                            ? "__m1q" : "__m1s";
                    mir_emit_runtime_call(out, multiply_helper);
                }
                mir_emit_virtual_store_wide(out, insn->dst);
                break;
            }
            if (mir_binary_is_selfstore_wide_increment(
                    i, &wide_selfstore_store_index,
                    &wide_selfstore_offset)) {
                mir_emit_wide_named_increment(
                    out, wide_selfstore_offset);
                break;
            }
            if (mir_binary_is_narrow_phi_adjust(
                    i, &narrow_phi_source, NULL) &&
                mir_emit_narrow_phi_adjust(
                    out, narrow_phi_source,
                    insn->immediate == '+'))
                break;
            if (mir_binary_is_wide_phi_increment(
                    i, &wide_phi_source, NULL) &&
                mir_emit_wide_phi_increment(
                    out, wide_phi_source))
                break;
            if (mir_binary_is_selfstore_incdec(i, &selfstore_store_index)) {
                int memory_type, memory_storage, memory_offset;
                mir_scalar_memory_location(mir_definition(insn->src1),
                                            &memory_type, &memory_storage,
                                            &memory_offset);
                if (memory_storage == SC_GLOBAL ||
                    memory_storage == SC_EXTERN) {
                    mir_emit_selfstore_incdec_global(
                        out, mir_definition(insn->src1), memory_storage,
                        insn->immediate == '+');
                    /* Item T85 (mir-text-size-plan.md): the global/extern
                     * fused inc/dec form above ends with "ld (name),hl" -
                     * HL still holds the freshly incremented/decremented
                     * value, exactly like T84's narrow global store. A
                     * following fresh MIR_LOAD of the same location
                     * (a static local's storage class is SC_GLOBAL, so
                     * `static int x; ...; x++; total += x;` produces a
                     * brand-new SSA load rather than reusing this
                     * MIR_BINARY's own dst - tests/tforblk.c's
                     * static_sibling_blocks) is provably redundant.
                     * Reuse the exact same forward-skip-and-match-
                     * location logic T84 uses after a narrow store. */
                    {
                        int forward_after_incdec =
                            mir_forward_skip_target(selfstore_store_index);
                        if (forward_after_incdec < mir.count &&
                            mir.insns[forward_after_incdec].opcode ==
                                MIR_LOAD &&
                            mir_same_scalar_memory_location(
                                mir_definition(insn->src1),
                                &mir.insns[forward_after_incdec])) {
                            mir_forwarded_hl_value =
                                mir.insns[forward_after_incdec].dst;
                            mir_forwarded_hl_instruction =
                                forward_after_incdec - 1;
                        }
                    }
                } else
                    mir_emit_selfstore_incdec(out, memory_offset,
                                              insn->immediate == '+');
                break;
            }
            if (mir_binary_is_selfstore_global_predecrement_load(
                    i, &selfstore_store_index,
                    &predecrement_load_index,
                    &small_add_amount)) {
                int memory_type, memory_storage, memory_offset;
                const struct MirInsn *definition =
                    mir_definition(insn->src1);

                if (definition == NULL ||
                    !mir_scalar_memory_location(
                        definition, &memory_type,
                        &memory_storage, &memory_offset))
                    goto done;
                mir_emit_selfstore_global_predecrement_load(
                    out, definition, memory_storage,
                    small_add_amount,
                    predecrement_load_index);
                break;
            }
            if (mir_binary_is_selfstore_small_adjust(
                    i, &selfstore_store_index,
                    &small_add_amount)) {
                int memory_type, memory_storage, memory_offset;
                const struct MirInsn *definition =
                    mir_definition(insn->src1);

                if (definition == NULL ||
                    !mir_scalar_memory_location(
                        definition, &memory_type,
                        &memory_storage, &memory_offset))
                    goto done;
                mir_emit_selfstore_small_adjust(
                    out, definition, memory_storage,
                    memory_offset, small_add_amount);
                if (small_add_amount > 0)
                    ++mir_spilled_cfg_small_selfstore_add_use_count;
                break;
            }
            }
            if (type_size(insn->secondary_offset) == 4) {
                struct MirInsn swapped_comparison;
                const struct MirInsn *operation_insn = insn;
                int mulmod_left;
                int mulmod_right;
                int mulmod_modulus;
                int stack_forwarded_left =
                    mir_forwarded_wide_stack_value == insn->src1 &&
                    mir_forwarded_wide_stack_consumer == i;
                int stack_forwarded_right =
                    mir_forwarded_wide_stack_value == insn->src2 &&
                    mir_forwarded_wide_stack_consumer == i;
                if (stack_forwarded_right &&
                    (insn->immediate == TOK_EQ ||
                     insn->immediate == TOK_NE ||
                     insn->immediate == '<' ||
                     insn->immediate == '>' ||
                     insn->immediate == TOK_LE ||
                     insn->immediate == TOK_GE)) {
                    swapped_comparison = *insn;
                    swapped_comparison.immediate =
                        mir_swap_wide_comparison_operator(
                            (int)insn->immediate);
                    operation_insn = &swapped_comparison;
                }
                if (mir_wide_multiply_is_fused_mulmod(i)) {
                    if (stack_forwarded_left || stack_forwarded_right) {
                        mir_stream_puts("\tpop hl\n\tpop de\n", out);
                        mir_forwarded_wide_stack_value = -1;
                        mir_forwarded_wide_stack_consumer = -1;
                    }
                    break;
                }
                if (mir_wide_mulmod_match(i, NULL, &mulmod_left,
                                          &mulmod_right,
                                          &mulmod_modulus)) {
                    mir_emit_virtual_load(out, mulmod_left);
                    mir_stream_puts("\tpush hl\n", out);
                    mir_emit_virtual_load(out, mulmod_right);
                    mir_stream_puts("\tpush hl\n", out);
                    mir_emit_virtual_load(out, mulmod_modulus);
                    mir_stream_puts("\tld c,l\n\tld b,h\n\tpop de\n\tpop hl\n", out);
                    mir_emit_runtime_call(out, "__m1mu");
                    mir_stream_puts("\tld de,0\n", out);
                    mir_emit_virtual_store_wide(out, insn->dst);
                    break;
                }
                int fuse_skip = mir_binary_is_fusable_comparison(i);
                /* Item T395/follow-on: the signed-constant-relational
                 * fast path only ever consumes src2's compile-time
                 * literal value, never a loaded runtime form - skip
                 * both the defensive push of src1 and the load of src2
                 * below when it applies (and neither operand is
                 * already using the unrelated stack-forwarding
                 * optimization), leaving DE:HL holding src1 untouched,
                 * exactly what mir_emit_wide_operation's fast path
                 * needs. */
                int signed_const_relational_fastpath =
                    !stack_forwarded_left && !stack_forwarded_right &&
                    mir_wide_operation_is_signed_const_relational(insn);
                if (stack_forwarded_right) {
                    mir_emit_virtual_load_wide(out, insn->src1);
                } else if (!stack_forwarded_left) {
                    mir_emit_virtual_load_wide(out, insn->src1);
                    if (!signed_const_relational_fastpath)
                        mir_stream_puts("\tpush de\n\tpush hl\n", out);
                }
                if (!stack_forwarded_right && !signed_const_relational_fastpath)
                    mir_emit_virtual_load_wide(out, insn->src2);
                if (!mir_emit_wide_operation(out, operation_insn))
                    goto done;
                if (stack_forwarded_left || stack_forwarded_right) {
                    mir_forwarded_wide_stack_value = -1;
                    mir_forwarded_wide_stack_consumer = -1;
                }
                if (fuse_skip > 0) {
                    /* Item T2: mir_emit_wide_operation already leaves the
                     * comparison's 0/1 result in HL - skip the
                     * store/reload/retest round trip entirely. */
                    if (!mir_emit_fused_wide_comparison_branch(
                            out, labels, i, fuse_skip - 1))
                        goto done;
                    ++mir_fuse_report_fused_count;
                    i += fuse_skip;
                    continue;
                }
                if (type_size(insn->type) == 4)
                    mir_emit_virtual_store_wide(out, insn->dst);
                else
                    mir_emit_virtual_store(out, insn->dst);
            } else {
                int divmod_partner = mir_divmod_partner(i);
                const struct MirInsn *left_definition =
                    mir_definition(insn->src1);
                const struct MirInsn *right_definition =
                    mir_definition(insn->src2);
                unsigned long left_multiplier = left_definition != NULL
                    ? (unsigned long)left_definition->immediate & 0xffffUL
                    : 0;
                unsigned long multiplier = right_definition != NULL
                    ? (unsigned long)right_definition->immediate & 0xffffUL
                    : 0;
                /* Item T15: mir_can_forward_stack_to_binary_const already
                 * pushed the left operand at its definition site (the
                 * push/pop dance is unavoidable here anyway - see below) -
                 * skip the otherwise-redundant store/reload of that value
                 * that mir_emit_virtual_load would perform. */
                int planned_stack_forwarded_left =
                    mir_planned_stack_matches_consumer(insn->src1, i) &&
                    mir_planned_stack_is_emitted(insn->src1);
                int stack_forwarded_left =
                    mir_forwarded_stack_value == insn->src1 &&
                    mir_forwarded_stack_target_instruction == i;
                int planned_stack_left_in_hl = 0;
                /* Item T16: mir_can_forward_stack_to_binary_rhs already
                 * pushed the right operand at its definition site (only
                 * possible when the left operand is a plain constant -
                 * see that predicate) - the constant load below leaves
                 * HL exactly where it needs to be, so the right operand
                 * only needs a single `pop de` once reached, no store/
                 * reload/push/ex-de-hl dance at all. */
                int stack_forwarded_right =
                    mir_forwarded_stack_value == insn->src2 &&
                    mir_forwarded_stack_target_instruction == i;
                int de_forwarded_right =
                    mir_forwarded_de_value == insn->src2 &&
                    mir_forwarded_de_consumer == i;
                int fused_zero_lhs =
                    mir_binary_is_fusable_comparison(i) > 0 &&
                    mir_fused_compare_is_const_zero_lhs(i);
                int materialized_zero_equality =
                    (insn->immediate == TOK_EQ ||
                     insn->immediate == TOK_NE) &&
                    right_definition != NULL &&
                    right_definition->opcode == MIR_CONST &&
                    (right_definition->immediate & 0xffffL) == 0;
                /* Item T50: set when the constant-loading logic below
                 * chose to pre-bias the RHS constant instead of loading
                 * it raw - both comparison consumers further down (the
                 * fused-branch emitter and the non-fused materialize
                 * path) need to know this to skip DE's own runtime
                 * xor-128 flip and only flip HL's sign bit. */
                int de_holds_biased_constant = 0;
                int small_positive_increment = 0;
                int pointer_difference_shift =
                    mir_pointer_difference_pow2_shift(insn);
                if (planned_stack_forwarded_left &&
                    (divmod_partner >= 0 ||
                     (insn->immediate == '*' &&
                      right_definition != NULL &&
                      right_definition->opcode == MIR_CONST &&
                      mir_mul_const_fast_path_eligible(
                          multiplier, insn->dst)) ||
                     (mir_binary_is_fusable_comparison(i) > 0 &&
                      (mir_fused_compare_is_const_zero_rhs(i) ||
                       mir_fused_compare_is_signed_zero_sign_test(i))))) {
                    /* Most binary instructions consume a forwarded lhs
                     * through their existing "load rhs / pop lhs" path.
                     * These direct-HL special cases need the lhs before
                     * that path, so pop the same planned value at this
                     * exact consumer and then continue as though it had
                     * been loaded normally. */
                    if (!mir_consume_planned_stack(
                            out, insn->src1, i, "hl"))
                        mir_planned_stack_invalid = 1;
                    planned_stack_forwarded_left = 0;
                    stack_forwarded_left = 0;
                    planned_stack_left_in_hl = 1;
                }
                if (mir_value_is_nested_truth_comparison_input(insn->dst))
                    break;
                if (mir_emit_nested_truth_comparison_branch(out, labels, i)) {
                    ++mir_fuse_report_fused_count;
                    ++i;
                    continue;
                }
                if (insn->immediate == '*' &&
                    mir_cfg_block_count() == 1 &&
                    mir.count == 6 &&
                    left_definition != NULL &&
                    left_definition->opcode == MIR_CONST &&
                    mir_mul_const_fast_path_eligible(
                        left_multiplier, insn->dst) &&
                    !stack_forwarded_left &&
                    !planned_stack_forwarded_left &&
                    !stack_forwarded_right &&
                    !de_forwarded_right) {
                    mir_emit_virtual_load(out, insn->src2);
                    mir_emit_mul_hl_const(out, left_multiplier);
                    mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                if (fused_zero_lhs) {
                    const struct MirInsn *zero_lhs_right =
                        mir_definition(insn->src2);

                    if (stack_forwarded_right)
                        mir_stream_puts("\tpop hl\n", out);
                    else if (zero_lhs_right != NULL &&
                             zero_lhs_right->opcode == MIR_CONST)
                        mir_stream_printf(out, "\tld hl,%ld\n",
                                zero_lhs_right->immediate & 0xffffL);
                    else
                        mir_emit_virtual_load(out, insn->src2);
                } else if (mir_binary_only_constant(insn->src1)) {
                    const struct MirInsn *constant =
                        mir_definition(insn->src1);
                    mir_stream_printf(out, "\tld hl,%ld\n",
                            constant->immediate & 0xffffL);
                } else if (!stack_forwarded_left &&
                           !planned_stack_forwarded_left &&
                           !planned_stack_left_in_hl)
                    mir_emit_virtual_load(out, insn->src1);
                if (pointer_difference_shift > 0) {
                    mir_spilled_cfg_used_pointer_difference_shift = 1;
                    if (stack_forwarded_left) {
                        mir_stream_puts("\tpop hl\n", out);
                        mir_forwarded_stack_value = -1;
                        mir_forwarded_stack_instruction = -1;
                        mir_forwarded_stack_target_instruction = -1;
                    } else if (planned_stack_forwarded_left) {
                        if (!mir_consume_planned_stack(
                                out, insn->src1, i, "hl"))
                            mir_planned_stack_invalid = 1;
                        planned_stack_forwarded_left = 0;
                    }
                    mir_emit_scalar_shift_by_constant(
                        out, TOK_SHR, 0, pointer_difference_shift);
                    mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                if (divmod_partner >= 0) {
                    const struct MirInsn *other = &mir.insns[divmod_partner];
                    int modulo_value = insn->immediate == '%' ? insn->dst
                                                               : other->dst;
                    int division_value = insn->immediate == '/' ? insn->dst
                                                                 : other->dst;
                    int saved_instruction = mir_emit_instruction_index;
                    if (divmod_partner < i)
                        break;
                    mir_stream_puts("\tpush hl\n", out);
                    mir_emit_virtual_load(out, insn->src2);
                    mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
                    if ((insn->secondary_offset & TYPE_UNSIGNED) != 0)
                        mir_emit_runtime_call(out, "__udivmod");
                    else
                        mir_emit_runtime_call(out, "__sdivmod");
                    mir_emit_instruction_index = -1;
                    mir_stream_puts("\tpush hl\n\tex de,hl\n", out);
                    mir_emit_virtual_store(out, modulo_value);
                    mir_stream_puts("\tpop hl\n", out);
                    mir_emit_virtual_store(out, division_value);
                    mir_emit_instruction_index = saved_instruction;
                    break;
                }
                if (insn->immediate == '*' && right_definition != NULL &&
                    right_definition->opcode == MIR_CONST &&
                    mir_mul_const_fast_path_eligible(multiplier, insn->dst)) {
                    mir_emit_mul_hl_const(out, multiplier);
                    mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                /* Item T48 (mir-text-size-plan.md): `int_expr &
                 * <compile-time constant>` (e.g. `sq & 7`, `flags &
                 * MASK`) - mirrors legacy's emit_and_hl_const, ported
                 * for the wide path as mir_emit_word_and_constant in
                 * Item T47 and reused directly here (a scalar is just
                 * one 16-bit register pair). HL already holds src1's
                 * real value at this point (either freshly loaded above,
                 * or itself the constant in the degenerate `const &
                 * const` case, which is still correct either way) -
                 * unless stack_forwarded_left is set, in which case HL
                 * was deliberately left unloaded (the value is still on
                 * the stack, pending a later pop) and this fast path
                 * must not run. Matches legacy's exact scope: only the
                 * constant-as-syntactic-right-operand shape is
                 * recognized, since that is the only one
                 * emit_and_hl_const's own caller ever special-cases. */
                if (insn->immediate == '&' && !stack_forwarded_left &&
                    !planned_stack_forwarded_left &&
                    right_definition != NULL &&
                    right_definition->opcode == MIR_CONST) {
                    mir_emit_word_and_constant(out, 'h', 'l',
                                                (unsigned int)multiplier);
                    mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                /* Item T49: unsigned modulo by a power-of-two constant is a
                 * mask. The corresponding division-to-shift form is
                 * deliberately excluded: mixed `/8, /3, ...` MIR sequences
                 * in tmodp2 produced a stale following helper quotient even
                 * though each operation passed alone. Keep division on the
                 * ordinary helper path until that sequence interaction can
                 * be represented safely. */
                if (insn->immediate == '%' &&
                    !stack_forwarded_left &&
                    !planned_stack_forwarded_left &&
                    (insn->type & TYPE_UNSIGNED) != 0 &&
                    right_definition != NULL &&
                    right_definition->opcode == MIR_CONST) {
                    int shift = mir_ulong_log2_pow2(multiplier);
                    if (shift >= 0) {
                        if (insn->immediate == '/')
                            mir_emit_scalar_shift_by_constant(out, TOK_SHR,
                                                              1, shift);
                        else
                            mir_emit_word_and_constant(
                                out, 'h', 'l',
                                (unsigned int)((multiplier - 1) & 0xffffUL));
                        mir_emit_virtual_store(out, insn->dst);
                        break;
                    }
                }
                /* A constant right-hand operand needs no dividend/left-
                 * operand-preserving push/pop dance: the left operand is
                 * already sitting in HL from src1's evaluation above and
                 * a constant *load* cannot clobber it (unlike src2 being
                 * evaluated via a call or memory access, which can), so
                 * the constant can be materialized directly into DE for
                 * *any* operator (Item 16 proved this for '/' and '%'
                 * only; Item T11 generalizes it to every operator, since
                 * the "constant load never clobbers HL" reasoning is not
                 * operator-specific).
                 *
                 * Skip this shortcut in functions with a VLA: the small
                 * static saving can change generated-candidate arbitration
                 * toward a frame-heavy result that is slower under stack
                 * checking.
                 */
                if (fused_zero_lhs) {
                    /* EQ/NE is symmetric and HL already holds src2. */
                } else if (stack_forwarded_right) {
                    /* Item T16: the right operand is already on the
                     * stack from its own definition site. Pop it into DE
                     * after loading the left operand; if that left operand
                     * also has a planned stack handoff, it is directly
                     * beneath the right operand and can now be popped into
                     * HL. */
                    mir_stream_puts("\tpop de\n", out);
                    if (planned_stack_forwarded_left) {
                        if (!mir_consume_planned_stack(
                                out, insn->src1, i, "hl"))
                            mir_planned_stack_invalid = 1;
                        planned_stack_forwarded_left = 0;
                    }
                } else if (de_forwarded_right) {
                    if (!planned_stack_forwarded_left ||
                        !mir_consume_planned_stack(
                            out, insn->src1, i, "hl"))
                        mir_planned_stack_invalid = 1;
                    planned_stack_forwarded_left = 0;
                } else if (materialized_zero_equality) {
                    /* HL already holds the nonzero-side operand. A framed
                     * function deliberately leaves a dynamic handoff for
                     * `ld sp,ix` to discard; that is faster than a spill.
                     * Frameless functions have no such repair, so consume
                     * the handoff here before their compact epilogue. */
                    if (stack_forwarded_left &&
                        mir_virtual_iy_frame_bytes == 0) {
                        mir_stream_puts("\tpop hl\n", out);
                        stack_forwarded_left = 0;
                    }
                    if (planned_stack_forwarded_left) {
                        if (!mir_consume_planned_stack(
                                out, insn->src1, i, "hl"))
                            mir_planned_stack_invalid = 1;
                        planned_stack_forwarded_left = 0;
                    }
                } else if (mir_binary_is_fusable_comparison(i) > 0 &&
                    mir_fused_compare_is_const_zero_rhs(i)) {
                    /* Item 25: this comparison will be fused directly into
                     * the following branch (mir_binary_is_fusable_comparison
                     * is the single source of truth for that) against the
                     * constant 0. Skip materializing that 0 into DE
                     * entirely; the fused branch emitter below tests HL
                     * directly with "ld a,h / or l" instead of
                     * "or a / sbc hl,de". */
                } else if (mir_binary_is_fusable_comparison(i) > 0 &&
                           mir_fused_compare_is_signed_zero_sign_test(i)) {
                    /* Item 27: same reasoning as Item 25 above, but for a
                     * signed `<`/`>=` comparison against the constant 0 -
                     * the fused branch emitter tests the sign bit of HL
                     * directly with "bit 7,h" instead of loading 0 into DE
                     * for a 16-bit sbc. */
                } else if (!mir.has_vla && !stack_forwarded_left &&
                           !planned_stack_forwarded_left &&
                           (insn->immediate == '<' ||
                            insn->immediate == TOK_GE) &&
                           right_definition != NULL &&
                           right_definition->opcode == MIR_CONST &&
                           (right_definition->immediate & 0xffffL) != 0 &&
                           (mir_definition(insn->src1) == NULL ||
                            !mir_type_uses_unsigned_comparison(
                                mir_definition(insn->src1)->type)) &&
                           !mir_type_uses_unsigned_comparison(
                               right_definition->type)) {
                    /* Item T50 (mir-text-size-plan.md): a signed `<`/`>=`
                     * comparison against a non-zero compile-time constant
                     * mirrors mir_emit_homed_binary_instruction's
                     * biased_right_constant optimization (previously
                     * MIR-only in that one selector) - pre-bias the
                     * constant at compile time (constant ^ 0x8000) so
                     * only HL's sign bit needs the runtime xor-128 flip
                     * below, not both HL and DE. The RHS==0 case is
                     * already handled more cheaply by Items 25/27 (bit 7
                     * test, no DE load at all) when fusable, or falls
                     * through to the plain path otherwise - excluded here
                     * to avoid overlapping logic. */
                    de_holds_biased_constant = 1;
                    mir_stream_printf(out, "\tld de,%ld\n",
                            (right_definition->immediate ^ 0x8000L) &
                                0xffffL);
                } else if (!mir.has_vla && !stack_forwarded_left &&
                           !planned_stack_forwarded_left &&
                           mir_binary_only_constant(insn->src2)) {
                    const struct MirInsn *constant =
                        mir_definition(insn->src2);
                    if (mir_cfg_block_count() >= 300 &&
                        insn->immediate == '+' &&
                        (constant->immediate == 1 ||
                         constant->immediate == 2))
                        small_positive_increment =
                            (int)constant->immediate;
                    else
                        mir_stream_printf(out, "\tld de,%ld\n",
                                constant->immediate & 0xffffL);
                } else {
                    /* Item T15: when stack_forwarded_left, the left
                     * operand was already pushed at its definition site
                     * (mir_can_forward_stack_to_binary_const) instead of
                     * being loaded into HL just above - skip this
                     * redundant push, but still pop it back below. */
                    if (!stack_forwarded_left &&
                        !planned_stack_forwarded_left)
                        mir_stream_puts("\tpush hl\n", out);
                    if (mir_binary_only_constant(insn->src2)) {
                        const struct MirInsn *constant =
                            mir_definition(insn->src2);
                        mir_stream_printf(out, "\tld hl,%ld\n",
                                constant->immediate & 0xffffL);
                    } else
                        mir_emit_virtual_load(out, insn->src2);
                    mir_stream_puts("\tex de,hl\n", out);
                    if (planned_stack_forwarded_left) {
                        if (!mir_consume_planned_stack(
                                out, insn->src1, i, "hl"))
                            mir_planned_stack_invalid = 1;
                    } else {
                        mir_stream_puts("\tpop hl\n", out);
                    }
                }
                if (stack_forwarded_left || stack_forwarded_right) {
                    mir_forwarded_stack_value = -1;
                    mir_forwarded_stack_instruction = -1;
                    mir_forwarded_stack_target_instruction = -1;
                }
                if (de_forwarded_right) {
                    mir_forwarded_de_value = -1;
                    mir_forwarded_de_consumer = -1;
                }
                {
                    int fuse_skip = mir_binary_is_fusable_comparison(i);
                    if (fuse_skip > 0) {
                        if (!mir_emit_fused_comparison_branch(
                                out, labels, i, fuse_skip - 1,
                                de_holds_biased_constant))
                            goto done;
                        ++mir_fuse_report_fused_count;
                        i += fuse_skip;
                        continue;
                    }
                }
                switch ((int)insn->immediate) {
                case TOK_EQ: case TOK_NE: case '<': case '>':
                case TOK_LE: case TOK_GE:
                    ++mir_fuse_report_materialized_count;
                    break;
                default:
                    break;
                }
                if (de_holds_biased_constant) {
                    /* Item T50: bypass mir_emit_scalar_operation's
                     * generic comparison case (which assumes DE holds
                     * the raw constant and would xor-128 both HL and
                     * DE) - DE already holds the biased value. */
                    mir_emit_scalar_compare_biased_right(
                        out, (int)insn->immediate);
                } else if (materialized_zero_equality) {
                    mir_emit_zero_equality(
                        out, (int)insn->immediate);
                } else if (small_positive_increment != 0) {
                    mir_stream_puts("\tinc hl\n", out);
                    if (small_positive_increment == 2)
                        mir_stream_puts("\tinc hl\n", out);
                } else if (!mir_emit_scalar_operation(out, insn))
                    goto done;
                mir_emit_virtual_store(out, insn->dst);
            }
            break;
        case MIR_ARG:
            break;
        case MIR_CALL:
            {
                struct Sym *callee = find_global(insn->name);
                int is_indirect = strcmp(insn->name, "<indirect>") == 0;
                const char *assembly_name = insn->base_name[0] != 0
                    ? insn->base_name
                    : asm_name_for(callee != NULL ? sym_asm_name(callee)
                                                  : insn->name);
                int call_arg_count = 0;
                int argument_bytes = 0;
                int argument;
                int scan;
                int dest_value, fill_value, count_value;
                int s_value, c_value;
                int s1_value, s2_value, n_value;
                int fn_value, dearg_value;
                int inline_memory_arguments[4];
                int inline_simple_store_arguments[3];
                int inline_typed_store_arguments[3];
                int inline_continuation_label;
                struct MirInlineIndexedStackStore inline_indexed_store;
                struct MirInlineMemoryStore inline_memory_store;
                struct MirInlineSimpleIndexedStore
                    inline_simple_store;
                struct MirInlineTypedIndexedStore inline_typed_store;
                const char *rtl_name;
                if (!is_indirect &&
                    mir_call_uses_inline_indexed_stack_store(
                        i, &s_value, &inline_indexed_store)) {
                    mir_emit_spilled_arg_to_hl(out, s_value);
                    mir_emit_inline_indexed_stack_store(
                        out, &inline_indexed_store);
                    ++mir_spilled_cfg_inline_indexed_stack_store_use_count;
                    break;
                }
                if (!is_indirect &&
                    mir_call_uses_inline_postincrement_store(
                        &inline_postincrement_helper, i, &s_value,
                        &inline_postincrement_skip_jump,
                        &inline_continuation_label)) {
                    mir_emit_inline_postincrement_store(
                        out, labels,
                        &inline_postincrement_helper,
                        s_value, 0,
                        inline_postincrement_shared_label,
                        inline_continuation_label);
                    ++mir_spilled_cfg_inline_postincrement_use_count;
                    break;
                }
                if (!is_indirect &&
                    mir_call_uses_inline_simple_indexed_store(
                        i, &inline_simple_store,
                        inline_simple_store_arguments)) {
                    mir_emit_inline_simple_indexed_store(
                        out, &inline_simple_store,
                        inline_simple_store_arguments);
                    ++mir_spilled_cfg_inline_simple_indexed_store_use_count;
                    break;
                }
                if (!is_indirect &&
                    mir_call_uses_inline_memory_store(
                        i, &inline_memory_store,
                        inline_memory_arguments)) {
                    mir_emit_inline_memory_store(
                        out, &inline_memory_store,
                        inline_memory_arguments);
                    break;
                }
                if (!is_indirect &&
                    mir_call_uses_inline_typed_indexed_store(
                        i, &inline_typed_store,
                        inline_typed_store_arguments)) {
                    mir_emit_inline_typed_indexed_store(
                        out, &inline_typed_store,
                        inline_typed_store_arguments);
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_memset_fastcall(i, &dest_value, &fill_value,
                                                &count_value)) {
                    if (mir_take_forwarded_hl_call_argument(count_value)) {
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, dest_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, fill_value);
                        mir_stream_puts("\tex de,hl\n\tpop hl\n\tpop bc\n", out);
                    } else {
                        mir_emit_spilled_arg_to_hl(out, dest_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, fill_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, count_value);
                        mir_stream_puts("\tld b,h\n\tld c,l\n\tpop de\n\tpop hl\n",
                              out);
                    }
                    mir_emit_runtime_call(out, "__msf");
                    if (type_ptr_depth(insn->type) > 0 ||
                        (insn->type & 15) != TYPE_VOID) {
                        if (type_size(insn->type) == 4)
                            mir_emit_virtual_store_wide(out, insn->dst);
                        else
                            mir_emit_virtual_store(out, insn->dst);
                    }
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_strlen_fastcall(i, &s_value)) {
                    mir_emit_spilled_arg_to_hl(out, s_value);
                    mir_emit_runtime_call(out, "__slf");
                    if (type_size(insn->type) == 4)
                        mir_emit_virtual_store_wide(out, insn->dst);
                    else
                        mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_strchr_fastcall(i, &s_value, &c_value)) {
                    if (mir_take_forwarded_hl_call_argument(c_value)) {
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, s_value);
                        mir_stream_puts("\tpop de\n\tld a,e\n", out);
                    } else {
                        mir_emit_spilled_arg_to_hl(out, s_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, c_value);
                        mir_stream_puts("\tld a,l\n\tpop hl\n", out);
                    }
                    mir_emit_runtime_call(out, "__chf");
                    mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_strrchr_fastcall(i, &s_value, &c_value)) {
                    if (mir_take_forwarded_hl_call_argument(c_value)) {
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, s_value);
                        mir_stream_puts("\tpop de\n\tld a,e\n", out);
                    } else {
                        mir_emit_spilled_arg_to_hl(out, s_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, c_value);
                        mir_stream_puts("\tld a,l\n\tpop hl\n", out);
                    }
                    mir_emit_runtime_call(out, "__rcf");
                    mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_memchr_fastcall(i, &s_value, &c_value,
                                               &n_value)) {
                    if (mir_take_forwarded_hl_call_argument(n_value)) {
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, s_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, c_value);
                        mir_stream_puts("\tex de,hl\n\tpop hl\n\tpop bc\n", out);
                    } else {
                        mir_emit_spilled_arg_to_hl(out, s_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, c_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, n_value);
                        mir_stream_puts("\tld b,h\n\tld c,l\n\tpop de\n\tpop hl\n",
                              out);
                    }
                    mir_emit_runtime_call(out, "__mhf");
                    mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_memcmp_fastcall(i, &s1_value, &s2_value,
                                               &n_value)) {
                    if (mir_take_forwarded_hl_call_argument(n_value)) {
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, s1_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, s2_value);
                        mir_stream_puts("\tpop de\n\tpop bc\n", out);
                    } else {
                        mir_emit_spilled_arg_to_hl(out, s1_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, s2_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, n_value);
                        mir_stream_puts("\tld b,h\n\tld c,l\n\tpop hl\n\tpop de\n",
                              out);
                    }
                    mir_emit_runtime_call(out, "__cmpf");
                    if (type_size(insn->type) == 4)
                        mir_emit_virtual_store_wide(out, insn->dst);
                    else
                        mir_emit_virtual_store(out, insn->dst);
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_memcpy_fastcall(i, &dest_value, &fill_value,
                                               &n_value)) {
                    if (mir_take_forwarded_hl_call_argument(n_value)) {
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, dest_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, fill_value);
                        mir_stream_puts("\tpop de\n\tpop bc\n", out);
                    } else {
                        mir_emit_spilled_arg_to_hl(out, dest_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, fill_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, n_value);
                        mir_stream_puts("\tld b,h\n\tld c,l\n\tpop hl\n\tpop de\n",
                              out);
                    }
                    mir_emit_runtime_call(out, "__mcf");
                    if (type_ptr_depth(insn->type) > 0 ||
                        (insn->type & 15) != TYPE_VOID) {
                        if (type_size(insn->type) == 4)
                            mir_emit_virtual_store_wide(out, insn->dst);
                        else
                            mir_emit_virtual_store(out, insn->dst);
                    }
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_de_hl_fastcall(i, &rtl_name, &s1_value,
                                              &s2_value)) {
                    /* Item T65c (mir-text-size-plan.md): s2 (the value
                     * that must end up in HL for the call, "the last-
                     * evaluated value needs no move") can already be
                     * sitting there via an earlier forwarding handoff -
                     * mir_emit_virtual_store's mir_can_forward_hl_to_
                     * call_argument path defers a value with no backend
                     * slot at all, leaving it in HL and arming
                     * mir_forwarded_hl_value/mir_forwarded_hl_instruction
                     * to match once this call instruction is reached.
                     * The usual order below (evaluate s1 first, clobbering
                     * HL, then s2) silently destroys that forwarded value
                     * before mir_emit_spilled_arg_to_hl(s2_value) ever
                     * gets to consume it - mir_emit_virtual_load only
                     * clears the forwarding state on a match, not when a
                     * non-matching reload (s1's) clobbers HL instead, so
                     * the later, stale-but-still-"matching" check for s2
                     * wrongly skips its own reload entirely. Confirmed via
                     * tests/adaint.c's var_or_const_decl
                     * (strcpy(names[nn++], G->text)): text's address (s2)
                     * was left dangling in HL, names[nn++]'s address (s1)
                     * silently overwrote it, and strcpy ran with both DE
                     * and HL pointing at the destination - a no-op self-
                     * copy. When s2 is the pending forwarded value, swap
                     * to a preserve-then-restore shape (push it before
                     * s1 is evaluated, then move s1 into DE and pop s2
                     * back into HL) instead of the naive order. */
                    if (mir_take_forwarded_hl_call_argument(s2_value)) {
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, s1_value);
                        mir_stream_puts("\tex de,hl\n\tpop hl\n", out);
                    } else {
                        mir_emit_spilled_arg_to_hl(out, s1_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, s2_value);
                        mir_stream_puts("\tpop de\n", out);
                    }
                    mir_emit_runtime_call(out, rtl_name);
                    if (type_ptr_depth(insn->type) > 0 ||
                        (insn->type & 15) != TYPE_VOID) {
                        if (type_size(insn->type) == 4)
                            mir_emit_virtual_store_wide(out, insn->dst);
                        else
                            mir_emit_virtual_store(out, insn->dst);
                    }
                    break;
                }
                if (!is_indirect &&
                    mir_call_is_bdos_family_fastcall(i, &rtl_name, &fn_value,
                                                    &dearg_value)) {
                    if (mir_take_forwarded_hl_call_argument(dearg_value)) {
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, fn_value);
                        mir_stream_puts("\tld c,l\n\tpop de\n", out);
                    } else {
                        mir_emit_spilled_arg_to_hl(out, fn_value);
                        mir_stream_puts("\tpush hl\n", out);
                        mir_emit_spilled_arg_to_hl(out, dearg_value);
                        mir_stream_puts("\tex de,hl\n\tpop hl\n\tld c,l\n", out);
                    }
                    mir_emit_runtime_call(out, rtl_name);
                    if (type_ptr_depth(insn->type) > 0 ||
                        (insn->type & 15) != TYPE_VOID) {
                        if (type_size(insn->type) == 4)
                            mir_emit_virtual_store_wide(out, insn->dst);
                        else
                            mir_emit_virtual_store(out, insn->dst);
                    }
                    break;
                }

                for (scan = 0; scan < i; ++scan)
                    if (mir.insns[scan].opcode == MIR_ARG &&
                        mir.insns[scan].secondary_offset ==
                            insn->secondary_offset) {
                        int index = (int)mir.insns[scan].immediate;
                        if (index != call_arg_count)
                            goto done;
                        ++call_arg_count;
                    }
                argument = call_arg_count - 1;
                for (scan = i - 1; scan >= 0; --scan) {
                    const struct MirInsn *arg = &mir.insns[scan];
                    int size;
                    if (arg->opcode != MIR_ARG ||
                        arg->secondary_offset != insn->secondary_offset)
                        continue;
                    if (arg->immediate != argument--)
                        goto done;
                    size = type_size(arg->type);
                    if (mir_prepacked_call_instruction == i &&
                        (arg->immediate >
                             mir_prepacked_after_argument ||
                         arg->src1 == mir_prepacked_result_value)) {
                        /* Already present below SP in normal reverse-ABI
                         * order; retain its bytes for caller cleanup. */
                        argument_bytes += size == 4 ? 4 : 2;
                        continue;
                    }
                    if (type_is_struct_object(arg->type)) {
                        if (!mir_emit_cached_call_argument(out, arg->src1) &&
                            !mir_emit_rematerialized_argument(
                                out, arg->src1, 2))
                            mir_emit_virtual_load(out, arg->src1);
                        mir_stream_puts("\tex de,hl\n", out);
                        mir_stream_printf(out,
                                "\tld hl,-%d\n\tadd hl,sp\n\tld sp,hl\n",
                                size);
                        mir_stream_printf(out, "\tex de,hl\n\tld bc,%d\n\tldir\n",
                                size);
                        argument_bytes += size;
                    } else if (size == 4) {
                        int cached = mir_emit_cached_wide_call_argument(
                            out, arg->src1);
                        if (!cached && !mir_emit_rematerialized_argument(
                                out, arg->src1, size))
                            mir_emit_virtual_load_wide(out, arg->src1);
                        if (cached != 2)
                            mir_stream_puts("\tpush de\n\tpush hl\n", out);
                        argument_bytes += 4;
                    } else {
                        if (mir_phi_argument_is_prepacked(
                                arg->src1, i)) {
                            argument_bytes += 2;
                            continue;
                        }
                        if (!mir_emit_cached_call_argument_to_stack(
                                out, arg->src1)) {
                            if (!mir_emit_cached_call_argument(
                                    out, arg->src1) &&
                                !mir_emit_rematerialized_argument(
                                    out, arg->src1, size))
                                mir_emit_virtual_load(out, arg->src1);
                            mir_stream_puts("\tpush hl\n", out);
                        }
                        argument_bytes += 2;
                    }
                }
                if (argument != -1)
                    goto done;
                if ((insn->memory_flags & MIR_CALL_FLAG_FORMAT_HEX) != 0)
                    mir_emit_runtime_call(out, "__pfehx");
                if ((insn->memory_flags & MIR_CALL_FLAG_FORMAT_OCTAL) != 0)
                    mir_emit_runtime_call(out, "__pfeoc");
                if (is_indirect) {
                    if (!mir_emit_rematerialized_argument(out, insn->src1, 2))
                        mir_emit_virtual_load(out, insn->src1);
                    mir_emit_runtime_call(out, "__call_hl");
                } else {
                    if ((callee == NULL || callee->needs_extrn) &&
                        mir_extrn_should_emit_name(assembly_name))
                        mir_stream_printf(out, "\textrn %s\n", assembly_name);
                    mir_stream_printf(out, "\tcall %s\n", assembly_name);
                }
                if (is_indirect || callee == NULL || !callee->is_defined)
                    mir_emit_restore_virtual_iy(out);
                if ((argument_bytes & 1) != 0) {
                    if (type_ptr_depth(insn->type) > 0 ||
                        (insn->type & 15) != TYPE_VOID) {
                        int saved_instruction = mir_emit_instruction_index;

                        /* SP cleanup overwrites HL before the following MIR
                         * argument can consume this result. Force a concrete
                         * store even when ordinary next-use forwarding would
                         * otherwise classify the value as slotless. */
                        mir_emit_instruction_index = -1;
                        if (type_size(insn->type) == 4)
                            mir_emit_virtual_store_wide(out, insn->dst);
                        else
                            mir_emit_virtual_store(out, insn->dst);
                        mir_emit_instruction_index = saved_instruction;
                    }
                    mir_stream_printf(out, "\tld hl,%d\n\tadd hl,sp\n\tld sp,hl\n",
                            argument_bytes);
                    if (type_ptr_depth(insn->type) > 0 ||
                        (insn->type & 15) != TYPE_VOID) {
                        if (type_size(insn->type) == 4)
                            mir_emit_virtual_load_wide(out, insn->dst);
                        else
                            mir_emit_virtual_load(out, insn->dst);
                    }
                } else {
                    for (argument = 0; argument < argument_bytes / 2;
                         ++argument)
                        mir_stream_puts("\tpop bc\n", out);
                }
                if (type_ptr_depth(insn->type) > 0 ||
                    (insn->type & 15) != TYPE_VOID) {
                    if (type_size(insn->type) == 4)
                        mir_emit_virtual_store_wide(out, insn->dst);
                    else
                        mir_emit_virtual_store(out, insn->dst);
                }
                if (mir_prepacked_call_instruction == i) {
                    mir_prepacked_call_instruction = -1;
                    mir_prepacked_after_argument = -1;
                    mir_prepacked_result_value = -1;
                    mir_prepacked_skip_through = -1;
                }
            }
            break;
        case MIR_CALL_AGGREGATE:
            {
                struct Sym *callee = find_global(insn->name);
                const char *assembly_name = asm_name_for(
                    callee != NULL ? sym_asm_name(callee) : insn->name);
                int call_arg_count = 0;
                int argument_bytes = 0;
                int argument;
                int scan;
                for (scan = 0; scan < i; ++scan)
                    if (mir.insns[scan].opcode == MIR_ARG &&
                        mir.insns[scan].secondary_offset ==
                            insn->secondary_offset) {
                        int index = (int)mir.insns[scan].immediate;
                        if (index != call_arg_count)
                            goto done;
                        ++call_arg_count;
                    }
                argument = call_arg_count - 1;
                for (scan = i - 1; scan >= 0; --scan) {
                    const struct MirInsn *arg = &mir.insns[scan];
                    int size;
                    if (arg->opcode != MIR_ARG ||
                        arg->secondary_offset != insn->secondary_offset)
                        continue;
                    if (arg->immediate != argument--)
                        goto done;
                    size = type_size(arg->type);
                    if (type_is_struct_object(arg->type)) {
                        if (size <= 0 || size > 1024)
                            goto done;
                        if (!mir_emit_cached_call_argument(out, arg->src1) &&
                            !mir_emit_rematerialized_argument(
                                out, arg->src1, 2))
                            mir_emit_virtual_load(out, arg->src1);
                        mir_stream_puts("\tex de,hl\n", out);
                        mir_stream_printf(out, "\tld hl,-%d\n\tadd hl,sp\n\tld sp,hl\n",
                                size);
                        mir_stream_printf(out, "\tex de,hl\n\tld bc,%d\n\tldir\n",
                                size);
                        argument_bytes += size;
                    } else if (size == 4) {
                        int cached = mir_emit_cached_wide_call_argument(
                            out, arg->src1);
                        if (!cached && !mir_emit_rematerialized_argument(
                                out, arg->src1, size))
                            mir_emit_virtual_load_wide(out, arg->src1);
                        if (cached != 2)
                            mir_stream_puts("\tpush de\n\tpush hl\n", out);
                        argument_bytes += 4;
                    } else {
                        if (!mir_emit_cached_call_argument_to_stack(
                                out, arg->src1)) {
                            if (!mir_emit_cached_call_argument(
                                    out, arg->src1) &&
                                !mir_emit_rematerialized_argument(
                                    out, arg->src1, size))
                                mir_emit_virtual_load(out, arg->src1);
                            mir_stream_puts("\tpush hl\n", out);
                        }
                        argument_bytes += 2;
                    }
                }
                if (argument != -1)
                    goto done;
                if (insn->immediate == MIR_AGGREGATE_GLOBAL_DEST_OFFSET) {
                    struct Sym *destination = find_global(insn->base_name);
                    const char *destination_name = asm_name_for(
                        destination != NULL ? sym_asm_name(destination)
                                            : mir_declared_link_name(
                                                insn->base_name));
                    if (destination != NULL && destination->needs_extrn &&
                        mir_extrn_should_emit(destination))
                        mir_stream_printf(out, "\textrn %s\n", destination_name);
                    mir_stream_printf(out, "\tld hl,%s\n", destination_name);
                } else if (insn->immediate == MIR_AGGREGATE_VALUE_DEST_OFFSET)
                    mir_emit_virtual_load(out, insn->src1);
                else if (insn->immediate == MIR_AGGREGATE_FORWARD_OFFSET)
                    mir_stream_puts("\tld l,(ix+4)\n\tld h,(ix+5)\n", out);
                else
                    mir_stream_puts("\tpush ix\n\tpop hl\n", out);
                if (insn->immediate != 0 &&
                    insn->immediate != MIR_AGGREGATE_FORWARD_OFFSET &&
                    insn->immediate != MIR_AGGREGATE_VALUE_DEST_OFFSET &&
                    insn->immediate != MIR_AGGREGATE_GLOBAL_DEST_OFFSET)
                    mir_emit_hl_offset_from_ix(out, (int)insn->immediate);
                mir_stream_puts("\tpush hl\n", out);
                if ((callee == NULL || callee->needs_extrn) &&
                    mir_extrn_should_emit_name(assembly_name))
                    mir_stream_printf(out, "\textrn %s\n", assembly_name);
                mir_stream_printf(out, "\tcall %s\n", assembly_name);
                mir_stream_printf(out, "\tld hl,%d\n\tadd hl,sp\n\tld sp,hl\n",
                        argument_bytes + 2);
                if (insn->immediate == MIR_AGGREGATE_GLOBAL_DEST_OFFSET) {
                    struct Sym *destination = find_global(insn->base_name);
                    const char *destination_name = asm_name_for(
                        destination != NULL ? sym_asm_name(destination)
                                            : mir_declared_link_name(
                                                insn->base_name));
                    mir_stream_printf(out, "\tld hl,%s\n", destination_name);
                } else if (insn->immediate == MIR_AGGREGATE_VALUE_DEST_OFFSET)
                    mir_emit_virtual_load(out, insn->src1);
                else if (insn->immediate == MIR_AGGREGATE_FORWARD_OFFSET)
                    mir_stream_puts("\tld l,(ix+4)\n\tld h,(ix+5)\n", out);
                else
                    mir_stream_puts("\tpush ix\n\tpop hl\n", out);
                if (insn->immediate != 0 &&
                    insn->immediate != MIR_AGGREGATE_FORWARD_OFFSET &&
                    insn->immediate != MIR_AGGREGATE_VALUE_DEST_OFFSET &&
                    insn->immediate != MIR_AGGREGATE_GLOBAL_DEST_OFFSET)
                    mir_emit_hl_offset_from_ix(out, (int)insn->immediate);
                if (mir_value_has_use(insn->dst))
                    mir_emit_virtual_store(out, insn->dst);
            }
            break;
        case MIR_VA_START:
            if (insn->immediate < -128 || insn->immediate + 1 > 127)
                goto done;
            mir_stream_puts("\tpush ix\n\tpop hl\n", out);
            mir_emit_hl_offset_from_ix(out, insn->secondary_offset);
            mir_stream_printf(out, "\tld (ix%+ld),l\n\tld (ix%+ld),h\n",
                    insn->immediate, insn->immediate + 1);
            mir_stream_puts("\tld hl,0\n", out);
            mir_emit_virtual_store(out, insn->dst);
            break;
        case MIR_VA_END:
            if (insn->immediate < -128 || insn->immediate + 1 > 127)
                goto done;
            mir_stream_printf(out, "\tld (ix%+ld),0\n\tld (ix%+ld),0\n",
                    insn->immediate, insn->immediate + 1);
            mir_stream_puts("\tld hl,0\n", out);
            mir_emit_virtual_store(out, insn->dst);
            break;
            case MIR_VA_ARG:
                mir_stream_printf(out, "\tld l,(ix%+ld)\n\tld h,(ix%+ld)\n",
                    insn->immediate, insn->immediate + 1);
                mir_stream_puts("\tpush hl\n", out);
                    mir_stream_printf(out, "\tld de,%d\n\tadd hl,de\n",
                    insn->secondary_offset);
                mir_stream_printf(out, "\tld (ix%+ld),l\n\tld (ix%+ld),h\n",
                    insn->immediate, insn->immediate + 1);
                mir_stream_puts("\tpop hl\n", out);
                if (insn->secondary_offset == 4) {
                mir_stream_puts("\tpush hl\n\tld a,(hl)\n\tinc hl\n"
                      "\tld h,(hl)\n\tld l,a\n\tex (sp),hl\n"
                      "\tinc hl\n\tinc hl\n\tld e,(hl)\n\tinc hl\n"
                      "\tld d,(hl)\n\tpop hl\n", out);
                mir_emit_virtual_store_wide(out, insn->dst);
                } else {
                mir_stream_puts("\tld a,(hl)\n\tinc hl\n\tld h,(hl)\n\tld l,a\n", out);
                mir_emit_virtual_store(out, insn->dst);
                }
                break;
        case MIR_JUMP:
            if (i == inline_postincrement_skip_jump) {
                inline_postincrement_skip_jump = -1;
                break;
            }
            if (insn->label < 0 || insn->label >= mir.next_label)
                goto done;
            {
                int target = mir_find_label(insn->label);
                if (target < 0 || !mir_emit_spilled_phi_copies(out, i, target))
                    goto done;
                /* mir-text-size Item T8: a jump whose target label is the
                 * literal next MIR instruction is a pure fallthrough -
                 * control already lands there once any phi copies above
                 * have executed, so the `jp` mnemonic itself is dead
                 * weight. This mirrors legacy, which never emits a jump
                 * to the position immediately following it.
                 * Item T62: a jump that can never be reached at all
                 * (only preceded, after any dead elided labels, by an
                 * unconditional jump/return) is equally dead weight -
                 * see mir_insn_is_reachable's comment for the full
                 * rationale. */
                if (!mir_target_is_noop_fallthrough(i, target) &&
                    mir_insn_is_reachable(i))
                    mir_stream_printf(out, "\tjp L%d\n", labels[insn->label]);
            }
            break;
        case MIR_BRANCH_FALSE:
            if (insn->label < 0 || insn->label >= mir.next_label)
                goto done;
            {
                int target = mir_find_label(insn->label);
                const struct MirInsn *condition =
                    mir_definition(insn->src1);
                if (target < 0)
                    goto done;
                if (constant_control_reachable != NULL &&
                    condition != NULL &&
                    condition->opcode == MIR_CONST &&
                    type_size(condition->type) <= 2) {
                    if (condition->immediate != 0)
                        break;
                    if (!mir_emit_spilled_phi_copies(
                            out, i, target))
                        goto done;
                    if (!mir_target_is_noop_fallthrough(i, target))
                        mir_stream_printf(out, "\tjp L%d\n",
                                labels[insn->label]);
                    continue;
                }
                {
                struct MirConstantReturnSuffix suffix;

                if (mir_match_constant_return_suffix(i, &suffix)) {
                    int done_label = new_label();

                    if (mir_value_is_wide(insn->src1)) {
                        mir_emit_virtual_load_wide(out, insn->src1);
                        if (condition != NULL &&
                            type_is_float(condition->type))
                            mir_stream_puts("\tld a,d\n\tand 127\n"
                                  "\tor e\n\tor h\n\tor l\n", out);
                        else
                            mir_stream_puts("\tld a,d\n\tor e\n\tor h\n\tor l\n",
                                  out);
                    } else {
                        mir_emit_virtual_load(out, insn->src1);
                        mir_stream_puts("\tld a,h\n\tor l\n", out);
                    }
                    mir_stream_puts("\tld hl,0\n", out);
                    mir_stream_printf(out,
                            "\tjp z,L%d\n\tinc hl\nL%d:\n",
                            done_label, done_label);
                    mir_forwarded_hl_value = suffix.result_value;
                    mir_forwarded_hl_instruction =
                        suffix.return_instruction - 1;
                    i = suffix.return_instruction - 1;
                    continue;
                }
                }
                {
                struct MirBooleanReturnSuffix suffix;

                if (mir_match_boolean_return_suffix(i, &suffix)) {
                    int done_label = new_label();
                    int true_label = new_label();

                    mir_emit_virtual_load(out, suffix.left_value);
                    mir_stream_puts("\tld a,h\n\tor l\n", out);
                    mir_stream_printf(out, "\tjp z, L%d\n", true_label);
                    mir_emit_virtual_load(out, suffix.right_value);
                    mir_stream_puts("\tld a,h\n\tor l\n", out);
                    mir_stream_printf(out,
                            "\tjp nz, L%d\n\tld hl,0\n"
                            "\tjp L%d\nL%d:\n\tld hl,1\nL%d:\n",
                            true_label, done_label,
                            true_label, done_label);
                    mir_forwarded_hl_value = suffix.result_value;
                    mir_forwarded_hl_instruction =
                        suffix.return_instruction - 1;
                    i = suffix.return_instruction - 1;
                    continue;
                }
                }
                if (condition != NULL &&
                    condition->opcode == MIR_CONST &&
                    condition->immediate == 0 &&
                    mir_constant_false_branch_consumer(
                        insn->src1) == i) {
                    if (!mir_emit_spilled_phi_copies(
                            out, i, target))
                        goto done;
                    if (!mir_target_is_noop_fallthrough(i, target))
                        mir_stream_printf(out, "\tjp L%d\n",
                                labels[insn->label]);
                    break;
                }
                if (mir_value_is_wide(insn->src1)) {
                    mir_emit_virtual_load_wide(out, insn->src1);
                    if (condition != NULL && type_is_float(condition->type))
                        mir_stream_puts("\tld a,d\n\tand 127\n\tor e\n\tor h\n\tor l\n",
                              out);
                    else
                        mir_stream_puts("\tld a,d\n\tor e\n\tor h\n\tor l\n", out);
                } else {
                    mir_emit_virtual_load(out, insn->src1);
                    mir_stream_puts("\tld a,h\n\tor l\n", out);
                }
                if (!mir_emit_conditional_branch_with_phi_copies(
                        out, labels, "nz", i, target, insn->label))
                    goto done;
            }
            break;
        case MIR_RETURN:
            if (type_is_struct_object(mir.return_type)) {
                int size = type_size(mir.return_type);
                if (insn->src1 < 0)
                    goto done;
                mir_emit_virtual_load(out, insn->src1);
                /* mir-text-size Item T5: HL already holds the source
                 * address after mir_emit_virtual_load above. Load DE
                 * directly with the hidden return-buffer pointer (no
                 * need to route it through HL via `ex de,hl` first) and
                 * copy with the Z80 `ldir` block-copy instruction
                 * instead of a fully unrolled byte-by-byte sequence.
                 * Legacy's own struct-return path (emit_copy_de_to_hl_bytes,
                 * dcc_expr.c) already avoids unrolling with a `djnz`
                 * loop; `ldir` is both smaller and faster than either
                 * the old unrolled form or a djnz loop, and is the same
                 * idiom already used pervasively for block copies in
                 * DCCRTL.MAC. */
                if (size > 0) {
                    mir_stream_puts("\tld e,(ix+4)\n\tld d,(ix+5)\n", out);
                    mir_stream_printf(out, "\tld bc,%d\n\tldir\n", size);
                }
            } else if (insn->src1 >= 0) {
                if (mir_value_is_wide(insn->src1))
                    mir_emit_virtual_load_wide(out, insn->src1);
                else
                    mir_emit_virtual_load(out, insn->src1);
            }
            /* mir-text-size Item T14: share one epilogue among every early
             * return instead of duplicating it at each site (see the
             * return_count/last_insn_is_return precomputation above). The
             * owner - whichever return is the function's literal last MIR
             * instruction, or the fall-off-the-end tail below if none is -
             * always keeps the real epilogue text; every other return
             * becomes a plain `jp`, which is both smaller and faster than a
             * second copy of ix/iy/sp restore + ret whenever return_count
             * exceeds one. HL/DE were already loaded with the return value
             * just above, and `jp` does not disturb registers, so this is
             * safe regardless of which return the value came from - the
             * same guarantee legacy's own shared-epilogue `jp` relies on. */
            if (return_count > 1 &&
                !(last_insn_is_return && i == mir.count - 1)) {
                if (shared_epilogue_label < 0)
                    shared_epilogue_label = new_label();
                mir_stream_printf(out, "\tjp L%d\n", shared_epilogue_label);
            } else {
                if (shared_epilogue_label >= 0)
                    mir_stream_printf(out, "L%d:\n", shared_epilogue_label);
                mir_emit_virtual_iy_epilogue(out);
            }
            break;
        default:
            goto done;
        }
        if (mir_instruction_has_phi_fallthrough(i, 1) &&
            !mir_emit_spilled_phi_copies(out, i, i + 1))
            goto done;
    }
    if (mir_planned_stack_invalid ||
        mir_planned_stack_emit_count != mir_planned_stack_consume_count) {
        if (getenv("DCC_MIR_SELECT_REPORT") != NULL)
            fprintf(stderr,
                    "; MIR planned-stack mismatch function=%s invalid=%d "
                    "emitted=%d consumed=%d pending-value=%d "
                    "pending-consumer=%d\n",
                    mir.name, mir_planned_stack_invalid,
                    mir_planned_stack_emit_count,
                    mir_planned_stack_consume_count,
                    mir_pending_planned_stack_consumer() >= 0
                        ? mir.planned_stack_values[
                              mir_pending_planned_stack_consumer()]
                        : -1,
                    mir_pending_planned_stack_consumer());
        goto done;
    }
    /* mir-migration-plan-next10 Item 3: MIR_RETURN's own case (above) already
     * emits the function epilogue before its `break`. If the function's very
     * last IR instruction was a MIR_RETURN, that epilogue was already
     * written and this unconditional trailing call would duplicate it
     * (dead, unreachable "ld sp,ix / pop ix / ret" after the real return) -
     * every function using this selector paid for this every time. Only
     * emit it here for the true fall-off-the-end case, i.e. when the last
     * instruction was something else (a label, a jump target, etc.). */
    if (mir.count == 0 || mir.insns[mir.count - 1].opcode != MIR_RETURN) {
        /* Item T14: this fall-off-the-end tail is the shared-epilogue
         * owner whenever the function's last MIR instruction isn't
         * itself a MIR_RETURN (see the precomputation above) - define
         * the label here if any earlier return needed it. */
        if (shared_epilogue_label >= 0)
            mir_stream_printf(out, "L%d:\n", shared_epilogue_label);
        if (return_count == 0 && mir.implicit_zero_return)
            mir_stream_puts("\tld hl,0\n", out);
        mir_emit_virtual_iy_epilogue(out);
    }
    if (getenv("DCC_MIR_PREPACK_REPORT") != NULL &&
        mir_constant_argument_prepack_count != 0)
        fprintf(stderr,
                "; MIR constant-prepack function=%s count=%d\n",
                mir.name, mir_constant_argument_prepack_count);
    if (mir_constant_argument_prepacking_enabled &&
        mir_constant_argument_prepack_count > 0 &&
        mir_constant_argument_prepack_count < 3)
        goto done;
    mir_emit_debug_events(out, mir.count);
    accepted = 1;
done:
    free(constant_control_reachable);
    if (accepted && mir_spilled_cfg_used_exact_semantic_kernel)
        mir_stream_printf(out, "%s\n", MIR_EXACT_KERNEL_MARKER);
    if (accepted && mir_phi_slot_cleanup_is_active())
        mir_stream_printf(out, "%s\n", MIR_PHI_SLOT_MARKER);
    if (accepted && mir_backend_slot_accessed != NULL)
        for (i = 0; i < mir.next_value; ++i)
            if (mir.backend_slots[i] >= 0 &&
                (getenv("DCC_MIR_SLOT_ACCESS_REPORT") != NULL ||
                 !mir_backend_slot_accessed[i])) {
                const struct MirInsn *definition = mir_definition(i);
                const struct MirInsn *consumer = NULL;
                const char *consumer_operand = "none";
                int consumer_index;
                int definition_index = definition != NULL
                    ? (int)(definition - mir.insns) : -1;

                for (consumer_index = 0;
                     consumer_index < mir.count; ++consumer_index)
                    if (mir.insns[consumer_index].src1 == i ||
                        mir.insns[consumer_index].src2 == i ||
                        mir_call_uses_value(&mir.insns[consumer_index], i)) {
                        consumer = &mir.insns[consumer_index];
                        break;
                    }
                if (consumer != NULL)
                    consumer_operand = consumer->src1 == i ? "src1" :
                        consumer->src2 == i ? "src2" :
                        mir_call_uses_value(consumer, i) ? "call" : "other";
                fprintf(stderr,
                        "; MIR %s function=%s value=%d slot=%d accessed=%d "
                        "definition=%s type-size=%d uses=%d "
                        "consumer=%s operand=%s distance=%d "
                        "definition-immediate=%ld "
                        "consumer-immediate=%ld definition-type=%d "
                        "consumer-type=%d\n",
                        getenv("DCC_MIR_SLOT_ACCESS_REPORT") != NULL
                            ? "slot-access" : "unused-slot",
                        mir.name, i, mir.backend_slots[i],
                        mir_backend_slot_accessed[i] != 0,
                        definition != NULL
                            ? mir_opcode_name(definition->opcode) : "none",
                        definition != NULL
                            ? type_size(definition->type) : 0,
                        mir_value_use_count(i),
                        consumer != NULL
                            ? mir_opcode_name(consumer->opcode) : "none",
                        consumer_operand,
                        consumer != NULL && definition_index >= 0
                            ? consumer_index - definition_index : -1,
                        definition != NULL ? definition->immediate : 0L,
                        consumer != NULL ? consumer->immediate : 0L,
                        definition != NULL ? definition->type : 0,
                        consumer != NULL ? consumer->type : 0);
            }
    free(mir_backend_slot_accessed);
    mir_backend_slot_accessed = NULL;
    if (getenv("DCC_MIR_FUSE_REPORT") != NULL &&
        (mir_fuse_report_fused_count > 0 ||
         mir_fuse_report_materialized_count > 0))
        fprintf(stderr,
                "; MIR fuse-report function=%s fused=%d materialized=%d\n",
                mir.name, mir_fuse_report_fused_count,
                mir_fuse_report_materialized_count);
    mir_virtual_iy_base = 0;
    mir_virtual_iy_frame_bytes = 0;
    mir_emit_instruction_index = -1;
    mir_forwarded_hl_value = -1;
    mir_forwarded_hl_instruction = -1;
    mir_forwarded_wide_value = -1;
    mir_forwarded_wide_instruction = -1;
    mir_forwarded_stack_value = -1;
    mir_forwarded_stack_instruction = -1;
    mir_forwarded_stack_target_instruction = -1;
    mir_forwarded_wide_stack_value = -1;
    mir_forwarded_wide_stack_consumer = -1;
    mir_cached_call_value = -1;
    mir_cached_call_instruction = -1;
    mir_planned_stack_handoffs_enabled = 0;
    mir_planned_stack_emit_count = 0;
    mir_planned_stack_consume_count = 0;
    mir_planned_stack_invalid = 0;
    if (!accepted && getenv("DCC_MIR_SELECT_REPORT") != NULL)
        fprintf(stderr, "; MIR scalar-cfg reject function=%s insn=%d opcode=%s\n",
                mir.name, i,
                i >= 0 && i < mir.count
                    ? mir_opcode_name(mir.insns[i].opcode) : "preflight");
    free(labels);
    return accepted;
}

static void mir_copy_spilled_candidate(MirStream *source, MirStream *destination)
{
    /* No fflush()/ferror() equivalent needed: an in-memory MirStream has
     * no OS-level buffering to flush and its bounded reads cannot fail. */
    mir_stream_copy(source, destination);
}

int mir_try_emit_spilled_scalar_cfg(MirStream *out)
{
    MirStream *baseline;
    MirStream *phi_candidate;
    int label_base;
    int baseline_label_end;
    int baseline_emitted;
    int phi_emitted;
    long baseline_size;
    long phi_size;
    int baseline_instructions;
    int phi_instructions;

    mir_phi_argument_stack_handoff_enabled = 0;
    if (!mir_has_phi_first_call_argument_candidate())
        return mir_emit_spilled_scalar_cfg_candidate(out);
    baseline = mir_stream_open();
    phi_candidate = mir_stream_open();
    if (baseline == NULL || phi_candidate == NULL)
        fatal("cannot create MIR PHI-argument candidate stream");
    label_base = label_id;
    mir_extrn_begin_attempt();
    mir_phi_argument_stack_handoff_enabled = 1;
    phi_emitted = mir_emit_spilled_scalar_cfg_candidate(phi_candidate);
    mir_phi_argument_stack_handoff_enabled = 0;
    label_id = label_base;
    mir_extrn_begin_attempt();
    baseline_emitted = mir_emit_spilled_scalar_cfg_candidate(baseline);
    baseline_label_end = label_id;
    baseline_size = baseline_emitted
        ? mir_stream_size(baseline) : -1;
    phi_size = phi_emitted
        ? mir_stream_size(phi_candidate) : -1;
    baseline_instructions = baseline_emitted
        ? mir_stream_instruction_count(baseline) : -1;
    phi_instructions = phi_emitted
        ? mir_stream_instruction_count(phi_candidate) : -1;
    if (phi_emitted &&
        (!baseline_emitted ||
         (phi_size < baseline_size &&
          phi_instructions < baseline_instructions))) {
        if (getenv("DCC_MIR_PHI_ARGUMENT_REPORT") != NULL)
            fprintf(stderr,
                    "; MIR phi-argument function=%s selected=handoff "
                    "baseline-bytes=%ld handoff-bytes=%ld "
                    "baseline-insns=%d handoff-insns=%d\n",
                    mir.name, baseline_size, phi_size,
                    baseline_instructions, phi_instructions);
        mir_stream_close(phi_candidate);
        mir_stream_close(baseline);
        label_id = label_base;
        mir_extrn_begin_attempt();
        mir_phi_argument_stack_handoff_enabled = 1;
        phi_emitted = mir_emit_spilled_scalar_cfg_candidate(out);
        mir_phi_argument_stack_handoff_enabled = 0;
        return phi_emitted;
    }
    mir_stream_close(phi_candidate);
    if (!baseline_emitted) {
        mir_stream_close(baseline);
        label_id = label_base;
        return 0;
    }
    if (getenv("DCC_MIR_PHI_ARGUMENT_REPORT") != NULL)
        fprintf(stderr,
                "; MIR phi-argument function=%s selected=baseline "
                "baseline-bytes=%ld handoff-bytes=%ld "
                "baseline-insns=%d handoff-insns=%d\n",
                mir.name, baseline_size, phi_size,
                baseline_instructions, phi_instructions);
    mir_copy_spilled_candidate(baseline, out);
    mir_stream_close(baseline);
    label_id = baseline_label_end;
    return 1;
}
